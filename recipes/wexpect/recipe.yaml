# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: wexpect
  version: "4.0.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/wexpect-${{ version }}.tar.gz
  sha256: de9e739e78ec4d74a39bf8499904dacb6c594007a674fb7e10752c9b131f6522

build:
  number: 4
  skip: unix or match(python, "<3.4")
  script: ${{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pbr
    - pip
    - python
    - setuptools
  run:
    - psutil
    - python
    - pywin32
    - setuptools

tests:
  - python:
      imports:
        - wexpect
      pip_check: true

about:
  summary: Windows alternative of pexpect
  license: MIT
  license_file: LICENSE
  homepage: https://pypi.org/project/wexpect/
  repository: https://github.com/raczben/wexpect

extra:
  recipe-maintainers:
    - thewchan
