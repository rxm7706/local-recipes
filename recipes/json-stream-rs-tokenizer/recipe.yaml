# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: json-stream-rs-tokenizer
  version: "0.5.3"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name | replace('-', '_') }}-${{ version }}.tar.gz
  sha256: 88828b11ecd24692473728f237e19d6a48b20cb6472c64a029c734b6d616fbc4

build:
  number: 0
  script:
    - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - if: build_platform != target_platform
      then: python
    - if: build_platform != target_platform
      then: cross-python_${{ target_platform }}
    - ${{ compiler('cxx') }}
    - ${{ stdlib("c") }}
    - ${{ compiler('rust') }}
    - cargo-bundle-licenses
  host:
    - python
    - setuptools
    - wheel
    - setuptools-rust >=1,<2
    - pip
  run:
    - python

tests:
  - python:
      imports:
        - json_stream_rs_tokenizer
      pip_check: true

about:
  summary: A faster tokenizer for the json-stream Python library
  license: MIT
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  homepage: https://github.com/smheidrich/py-json-stream-rs-tokenizer

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: json-stream-rs-tokenizer
  cfe-upstream-registry: pypi
  cfe-upstream-name: json-stream-rs-tokenizer
  cfe-purls:
    - pkg:conda/json-stream-rs-tokenizer@${{ version }}?channel=conda-forge
    - pkg:pypi/json-stream-rs-tokenizer@${{ version }}
  cfe-upstream-repo: https://github.com/smheidrich/py-json-stream-rs-tokenizer
  cfe-upstream-homepage: https://github.com/smheidrich/py-json-stream-rs-tokenizer
  cfe-import-names: [json_stream_rs_tokenizer]
  cfe-source-kind: pypi-sdist
  cfe-noarch: compiled
  cfe-pip-check: true
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: https://github.com/conda-forge/json-stream-rs-tokenizer-feedstock
  cfe-forge-recipe-updates-needed:
    - meta-yaml-to-recipe-yaml
    - platform-expansion
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-06-21T00:00:00Z
  cfe-generated-by-version: 8.37.0
  cfe-generated-at-datetime: 2026-06-21T00:00:00Z
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-06-21T00:00:00Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
#    # WAVE-D C1: feedstock still v0 (meta.yaml). Authored recipe.yaml at 0.5.1
#    # alongside a faithful 0.5.1 meta.yaml mirror; both coexist until the
#    # feedstock completes its v0 to v1 switch.
# build:
#    # setuptools-rust (PyO3) extension. NOT abi3 — setup.py sets
#    # py_limited_api=False (required for num_bigint compat), so this is a
#    # per-Python compiled build, NOT version_independent. The prior local
#    # recipe carried python-abi3 + version_independent: true; removed as
#    # incorrect for this package.
# requirements:
#  build:
#    # cxx + rust compilers match the deployed feedstock; stdlib(c) per the
#    # all-compiled-recipes rule (the feedstock carries it too).
#    # cargo-bundle-licenses per conda-forge Rust policy; emits THIRDPARTY.yml.
#  host:
#    # setup.py / setup.cfg project (build-system requires setuptools, wheel,
#    # setuptools-rust>=1,<2); host block mirrors the deployed feedstock.
# tests:
#    # Simple imports + pip_check (matches the deployed feedstock). The
#    # CFEP-25 cross-Python [python_min.*, "*"] matrix is NOT used here: this
#    # is a per-Python COMPILED build (one artifact per Python), so a python_min
#    # test leg would try to install a Python the single artifact was not built
#    # for (G38). rattler-build tests against the build's own Python.
#    # python_min stays out of context: upstream python_requires is >=3.8,<3.15,
#    # below the conda-forge 3.10 floor, so pinning supplies it.
# extra:
#    # platform-expansion noted: feedstock .ci_support ships only linux_64 /
#    # osx_64 / win_64. As a compiled package it should widen to osx-arm64 +
#    # linux-aarch64 at submission. meta-yaml-to-recipe-yaml: feedstock owes v1.
####
