# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.26.0"

package:
  name: tree_sitter
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/t/tree_sitter/tree_sitter-${{ version }}.tar.gz
  sha256: b40c219edccc4564530c96f8f1556f6202b37cda964d1cbd7bd2b7e68b40a245

build:
  number: 0
  script:
    - if: unix
      then: export CFLAGS="-D_BSD_SOURCE"
    - ${{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib("c") }}
    - if: build_platform != target_platform
      then:
        - cross-python_${{ target_platform }}
        - python
  host:
    - pip
    - python
    - setuptools >=43
  run:
    - python

tests:
  - python:
      imports:
        - tree_sitter
  - requirements:
      run:
        - pip
    script:
      - pip check

about:
  summary: Python bindings to the Tree-sitter parsing library
  license: MIT and EPL-2.0
  license_file:
    - LICENSE
    - tree_sitter/core/lib/src/unicode/LICENSE
  homepage: https://github.com/tree-sitter/py-tree-sitter

extra:
  recipe-maintainers:
    - danielnachun
    - thewchan
    - davidbrochart
