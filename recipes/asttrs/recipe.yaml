# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: asttrs
  version: "1.3.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/asttrs-${{ version }}.tar.gz
  sha256: 39f3cd8bbecdeecff6c851d9cdb59e600a22611967cb2a0e1c5988d01a23233d

build:
  number: 2
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - poetry
  run:
    - python >=${{ python_min }},<3.11
    - attrs
    - black
    - cattrs
    - isort
    - ast-decompiler

tests:
  - python:
      imports:
        - asttrs
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: A attrs-style wrapper for python ast
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/ryanchao2012/asttrs

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: asttrs
  cfe-upstream-registry: pypi
  cfe-upstream-name: asttrs
  cfe-purls:
    - pkg:conda/asttrs@${{ version }}?channel=conda-forge
    - pkg:pypi/asttrs@${{ version }}
  cfe-upstream-repo: https://github.com/ryanchao2012/asttrs
  cfe-upstream-homepage: https://github.com/ryanchao2012/asttrs
  cfe-import-names: [asttrs]
  cfe-source-kind: pypi-sdist
  cfe-noarch: python
  cfe-pip-check: true
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: https://github.com/conda-forge/asttrs-feedstock
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-06-20T00:00:00Z
  cfe-generated-by-version: 8.37.0
  cfe-generated-at-datetime: 2026-06-20T00:00:00Z
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-06-20T00:00:00Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# requirements:
#  host:
#    # Deployed feedstock (v1, build 2) carries a stale `poetry` host dep from
#    # the 1.0.0 era; the 1.3.0 sdist is PDM/setuptools-based (no [build-system]
#    # table, ships setup.cfg + egg-info), so host is python + pip. Dropped poetry.
#  run:
#    # Deployed feedstock pins `python >=${{ python_min }},<3.11`. Upstream 1.3.0
#    # declares requires-python >=3.7 with classifiers through Python 3.12 and no
#    # upper bound; the <3.11 cap is a stale maintainer workaround, not upstream.
#    # Canonical regenerate drops it (floor clamped to conda-forge 3.10).
####
