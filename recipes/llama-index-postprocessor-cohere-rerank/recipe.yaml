# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: 0.9.0

package:
  name: llama-index-postprocessor-cohere-rerank
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/l/llama-index-postprocessor-cohere-rerank/llama_index_postprocessor_cohere_rerank-${{ version }}.tar.gz
  sha256: c617a0568b3333ebee4088d7802eb6157ed449d6849c45152930651401dbbef0

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}
    - cohere >=5.15,<7
    - llama-index-core >=0.13.0,<0.15
    - tenacity >=8.2.0,<10
    # avoid https://github.com/conda-forge/cohere-feedstock/issues/15
    - pydantic-core >=2.18.2,<2.44.0

tests:
  - python:
      imports:
        - llama_index.postprocessor.cohere_rerank
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - '*'

about:
  summary: llama-index postprocessor cohere rerank integration
  license: MIT
  license_file: LICENSE
  homepage: https://www.llamaindex.ai/
  repository: https://github.com/run-llama/llama_index/tree/main/llama-index-integrations/postprocessor/llama-index-postprocessor-cohere-rerank/

extra:
  recipe-maintainers:
    - JaRoSchm
