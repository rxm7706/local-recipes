# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "2.7.0"

package:
  name: caveman-installer
  version: ${{ version }}

source:
  url: https://github.com/JuliusBrussee/caveman/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 0f8b833ef65be67b565573d075264b7acab645e80241cfdae016421211d41147

build:
  number: 0
  script:
    - if: unix
      then:
        - set -ex
        - NPM_VER=$(node -p "require('./package.json').version")
        - pnpm install --prod --ignore-scripts
        - pnpm-licenses generate-disclaimer --prod --output-file=ThirdPartyLicenses.txt
        - npm pack --ignore-scripts --userconfig nonexistentrc
        - npm install --userconfig nonexistentrc --global "${SRC_DIR}/caveman-installer-${NPM_VER}.tgz"
        - find "${PREFIX}/lib/node_modules/caveman-installer" -type d -name .bin -exec rm -rf {} + || true
      else:
        - for /f "delims=" %%v in ('node -p "require('./package.json').version"') do set NPM_VER=%%v
        - call pnpm install --prod --ignore-scripts
        - if %ERRORLEVEL% neq 0 exit 1
        - call pnpm-licenses generate-disclaimer --prod --output-file=ThirdPartyLicenses.txt
        - if %ERRORLEVEL% neq 0 exit 1
        - call npm pack --ignore-scripts --userconfig nonexistentrc
        - if %ERRORLEVEL% neq 0 exit 1
        - call npm install --userconfig nonexistentrc --global "%SRC_DIR%/caveman-installer-%NPM_VER%.tgz"
        - if %ERRORLEVEL% neq 0 exit 1
        - for /d /r "%PREFIX%\node_modules\caveman-installer" %%d in (.bin) do if exist "%%d" rd /s /q "%%d"

requirements:
  build:
    - nodejs >=18
    - pnpm
    - pnpm-licenses
  host:
    # nodejs held to the 24.x LTS line (carried forward from the retired
    # recipes/caveman recipe, which built the same upstream under the wrong
    # conda name): nodejs's run_exports pins the major of whatever host
    # resolves. conda-forge's own caveman-installer build resolves host
    # nodejs 26.10.0 (stamping `nodejs >=26.10.0,<27.0a0` into run), which
    # is mutually exclusive with codegraph's own build (`nodejs
    # >=24.19,<25`) in the local-recipes env -- see pixi.toml's
    # local-recipes target, which sources this SelfExplainML build
    # specifically to keep that compatibility. pyforge-guild (no codegraph)
    # sources conda-forge's own build directly instead. Upstream's true
    # floor is engines >=18; caveman-install verified clean on node 24.
    # Lift this hold when the env itself moves to nodejs 26 (i.e. when the
    # marp-cli/a2a-sdk libabseil gap closes and codegraph rebuilds on 26).
    - nodejs 24.*
  run:
    - nodejs >=18

tests:
  - script:
      - caveman-install --help

about:
  license: MIT
  license_file:
    - LICENSE
    - LICENSE.BSL
    - LICENSING.md
    - ThirdPartyLicenses.txt
  summary: One-shot installer that wires the caveman Claude Code skill into ~30 AI coding agents
  description: |
    caveman is a Claude Code skill that cuts ~65% of tokens by talking like
    caveman. This package installs the `caveman` CLI, a one-shot bootstrapper
    that auto-detects supported AI coding agents on the machine (Claude Code,
    Cursor, Codex, Gemini CLI, opencode, Continue, Cline, Windsurf, and
    ~25 more) and runs each agent's native install path. It also wires
    Claude Code hooks, a statusline badge, and the `caveman-shrink` MCP
    middleware when those targets are present. Idempotent and re-runnable.
  homepage: https://getcaveman.dev/
  repository: https://github.com/JuliusBrussee/caveman
  documentation: https://github.com/JuliusBrussee/caveman/blob/main/INSTALL.md

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: caveman-installer
  cfe-upstream-registry: github
  cfe-upstream-name: JuliusBrussee/caveman
  cfe-purls:
    - pkg:conda/caveman-installer@${{ version }}?channel=conda-forge
    - pkg:github/juliusbrussee/caveman@v${{ version }}
  cfe-upstream-repo: https://github.com/JuliusBrussee/caveman
  cfe-upstream-homepage: https://github.com/JuliusBrussee/caveman
  cfe-import-names: []
  cfe-source-kind: github-tag
  cfe-noarch: compiled
  cfe-pip-check: "false:not-a-python-package"
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: conda-forge/caveman-installer-feedstock
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-09-24T17:33:12Z
  cfe-generated-by-version: 8.90.5
  cfe-generated-at-datetime: "2026-09-24T17:33:12Z"
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-09-24T17:45:00Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
#    # 2026-09-24: consolidated from the retired recipes/caveman (package
#    # name `caveman`, never a real published name -- a local-only artifact
#    # born from not knowing this exact upstream had already graduated to
#    # conda-forge under its correct npm/conda name, caveman-installer).
#    # Bumped 2.4.0 (the version this recipe sat at while dormant) -> 2.7.0
#    # (conda-forge's live version) in the same pass. Recipe body mirrors
#    # conda-forge/caveman-installer-feedstock verbatim except for the
#    # nodejs 24.* host hold, which this repo needs and the feedstock does
#    # not (no codegraph dependency there).
# about:
#    # The "installs the `caveman` CLI" wording in the description is the
#    # feedstock's own verbatim text (product name "caveman", installed
#    # binary caveman-install) -- left as-is per convention (don't touch
#    # upstream-feedstock prose).
####
