# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "1.0.0"

package:
  name: pybase62
  version: ${{ version }}

source:
  url: https://pypi.org/packages/py3/p/pybase62/pybase62-${{ version }}-py3-none-any.whl
  sha256: 60539ad956ec9e9de091bc7ae88c9550bc2fa17f503050cf34d021b75e73cb27

build:
  noarch: python
  number: 0
  script: ${{ PYTHON }} -m pip install pybase62-${{ version }}-py3-none-any.whl --no-deps --no-build-isolation -vv

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
  run:
    - python >=${{ python_min }}

tests:
  - python:
      imports:
        - base62
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  homepage: https://pypi.org/project/pybase62/
  license: BSD-2-Clause
  license_file: LICENSE
  summary: Python module for base62 encoding
  repository: https://github.com/suminb/base62
  documentation: https://github.com/suminb/base62
  description: |
    A Python module for base62 encoding. base62 is a binary-to-text
    encoding scheme that represents binary data in an ASCII string
    format using the 62 alphanumeric characters (0-9, A-Z, a-z).

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: pybase62
  cfe-upstream-registry: pypi
  cfe-upstream-name: pybase62
  cfe-name-aliases:
    - base62
  cfe-purls:
    - pkg:conda/pybase62@${{ version }}
    - pkg:pypi/pybase62@${{ version }}
  cfe-upstream-repo: https://github.com/suminb/base62
  cfe-upstream-homepage: https://github.com/suminb/base62
  cfe-on-conda-forge-status: pending-submission-to-conda-forge
  cfe-on-conda-forge-feedstock: none
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-06-19T13:36:19Z
  cfe-generated-by-version: 8.30.1
  cfe-generated-at-datetime: 2026-06-19T13:36:19Z
####
# CFE comments
# source:
#    # Upstream publishes no sdist for 1.0.0 — only a pure-Python wheel.
#    # The `py3` path/tag segment is upstream-specific; recheck it on a version bump.
# about:
#    # Upstream ships only a wheel (no sdist); the wheel's bundled LICENSE is
#    # not reachable as a recipe-relative path, so it ships in the recipe dir.
####
