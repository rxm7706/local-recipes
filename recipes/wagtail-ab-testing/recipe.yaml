# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.14"

package:
  name: wagtail-ab-testing
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/w/wagtail-ab-testing/wagtail_ab_testing-${{ version }}.tar.gz
  sha256: 4f47b8965b0837694ebcec21fb5d0c9b5317572d5a2f0c4d8e7b5caf053f86f9

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - wagtail >=7.0
    - numpy >=1.26,<3
    - scipy >=1.15,<2
    - user-agents >=2.2,<2.3

tests:
  - python:
      imports:
        - wagtail_ab_testing
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  homepage: https://github.com/wagtail-nest/wagtail-ab-testing
  repository: https://github.com/wagtail-nest/wagtail-ab-testing
  license: BSD-3-Clause
  license_file: LICENSE
  summary: A/B Testing for Wagtail
  description: |
    A/B Testing for Wagtail — split-test traffic between two variants of a
    page and measure conversion against a configured goal. Tracks
    participants and conversions via the user-agents library and offers an
    admin UI for creating, viewing, and concluding tests.

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: wagtail-ab-testing
  cfe-upstream-registry: pypi
  cfe-upstream-name: wagtail-ab-testing
  cfe-purls:
    - pkg:conda/wagtail-ab-testing@${{ version }}
    - pkg:pypi/wagtail-ab-testing@${{ version }}
  cfe-upstream-repo: https://github.com/wagtail-nest/wagtail-ab-testing
  cfe-upstream-homepage: https://github.com/wagtail-nest/wagtail-ab-testing
  cfe-import-names: [wagtail_ab_testing]
  cfe-source-kind: pypi-sdist
  cfe-noarch: python
  cfe-pip-check: true
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: https://github.com/conda-forge/wagtail-ab-testing-feedstock
  cfe-forge-recipe-updates-needed: [recipe-regenerate]
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-06-20T00:00:00Z
  cfe-generated-by-version: 8.37.0
  cfe-generated-at-datetime: 2026-06-20T00:00:00Z
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-06-20T00:00:00Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
#    # WAVE-C C2: feedstock already v1 at 0.14; authored fresh recipe.yaml,
#    # deleted stale local meta.yaml (was 0.9).
# requirements:
#  host:
#    # setuptools kept in host — upstream build-system declares
#    # setuptools >= 77 with setuptools.build_meta backend (no separate wheel
#    # needed per G8). pyproject.toml-only project; no setup.py.
#  run:
#    # Run deps reconciled against the 0.14 sdist [project.dependencies], NOT
#    # the deployed feedstock (which is STALE at wagtail >=6.3 / numpy <2 /
#    # scipy >=1.5.4). The 0.14 sdist declares Wagtail>=7.0, numpy>=1.26,<3,
#    # scipy>=1.15,<2, user-agents>=2.2,<2.3. All upper bounds are
#    # upstream-declared and load-bearing under pip_check (G24) — mirror them.
#    # cfe-forge-recipe-updates-needed: recipe-regenerate flags that the
#    # deployed feedstock recipe needs the same dep refresh.
# tests:
#    # CFEP-25 triad with [python_min.*, "*"]. wagtail_ab_testing is a Django
#    # app package; top-level import does NOT instantiate the app registry
#    # (no models/apps loaded at import), so the standard python.imports test
#    # holds — no package_contents/DJANGO_SETTINGS_MODULE escape hatch needed.
####
