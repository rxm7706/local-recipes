# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.8.0"

package:
  name: graph-retriever
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/g/graph-retriever/graph_retriever-${{ version }}.tar.gz
  sha256: e61f1cb27184e8d4559ece6de0a6ea18920908ffbae945051f9a01380af73347

build:
  number: 0
  noarch: python
  script: |
    sed -i '/^[[:space:]]*"pytest[<>=!~ ]/d' pyproject.toml
    ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}
    - immutabledict >=4.2.1
    - numpy >=1.26.4
    - typing_extensions >=4.12.2

tests:
  - python:
      imports:
        - graph_retriever
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  homepage: https://pypi.org/project/graph-retriever/
  license: Apache-2.0
  license_file: LICENSE
  summary: Retriever combining unstructured similarity and structured document traversal.
  repository: https://github.com/datastax/graph-rag
  documentation: https://datastax.github.io/graph-rag
  description: |
    graph-retriever provides graph traversal retrieval that combines
    unstructured vector similarity with structured document traversal. It
    lets retrievers walk edges between documents (defined by metadata) so
    related context can be pulled in alongside the nearest-neighbor matches,
    independent of any particular vector store. Part of the datastax/graph-rag
    project.

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: graph-retriever
  cfe-upstream-registry: pypi
  cfe-upstream-name: graph-retriever
  cfe-name-aliases:
    - graph_retriever
  cfe-purls:
    - pkg:conda/graph-retriever@${{ version }}
    - pkg:pypi/graph-retriever@${{ version }}
  cfe-upstream-repo: https://github.com/datastax/graph-rag
  cfe-upstream-homepage: https://datastax.github.io/graph-rag
  cfe-import-names: [graph_retriever]
  cfe-source-kind: pypi-sdist
  cfe-noarch: python
  cfe-pip-check: true
  cfe-on-conda-forge-status: pending-submission-to-conda-forge
  cfe-on-conda-forge-feedstock: none
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-06-19T13:36:19Z
  cfe-generated-by-version: 8.30.1
  cfe-generated-at-datetime: 2026-06-19T13:36:19Z
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-06-19T16:58:20Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# build:
#    # Upstream declares pytest as an ungated core dependency in
#    # [project.dependencies] (a test framework leaked into runtime deps). Strip
#    # it from pyproject.toml so the built wheel's METADATA matches the runtime
#    # deps and pip_check passes — pytest is a test dep, not a runtime dep (G26).
####
