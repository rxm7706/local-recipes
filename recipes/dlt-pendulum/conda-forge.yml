# yaml-language-server: $schema=https://raw.githubusercontent.com/conda-forge/conda-smithy/main/conda_smithy/data/conda-forge.json
# conda-forge.yml — pre-seeds the feedstock
conda_build_tool: rattler-build
conda_install_tool: pixi
bot:
  automerge: true
  check_solvable: true
  inspection: hint-all
build_platform:
  linux_aarch64: linux_64
  osx_arm64: osx_64
provider:
  linux_aarch64: azure
  osx_arm64: azure
test: native_and_emulated

#### CFE metadata AND comments
# CFE comments
#    # Pre-seeds the feedstock (CFE skill default, G83): these keys are INERT in
#    # a staged-recipes PR (build_all.py reads only conda_build_tool) and are
#    # forwarded into the feedstock on merge. dlt-pendulum is COMPILED (maturin /
#    # PyO3 Rust extension), so it carries the full ARM platform matrix (osx-arm64
#    # + linux-aarch64) on top of the universal build-tool + bot block.
#    # run_deps_from_wheel is deliberately OMITTED: the recipe patches tzdata out
#    # of the wheel metadata (0001-remove-tzdata-dep-for-pip-check.patch) yet adds
#    # python-tzdata back as a run dep, so run deps intentionally diverge from the
#    # wheel METADATA -- letting the bot regenerate them from the wheel would drop
#    # python-tzdata. conda_build_tool / conda_install_tool select the v1
#    # (recipe.yaml) parser; the deployed feedstock is still v0 -- these keys land
#    # when its v0->v1 migration PR is pushed. This #### CFE block is
#    # local-recipes-only and STRIPPED before push (G62).
####
