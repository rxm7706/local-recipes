# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: gotenberg-client
  version: "0.14.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/gotenberg_client-${{ version }}.tar.gz
  sha256: a853700c6b01c3372871264c4eb9ae3375addafbcbbfd3341e411f4217a8088c

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling >=1.27.0,<1.28.dev0
    - pip
  run:
    - python >=${{ python_min }},<4
    - httpx >=0.27
    - typing_extensions

tests:
  - python:
      imports:
        - gotenberg_client
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: A Python client for interfacing with the Gotenberg API
  license: MPL-2.0
  license_file: LICENSE
  homepage: https://github.com/stumpylog/gotenberg-client/

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: gotenberg-client
  cfe-upstream-registry: pypi
  cfe-upstream-name: gotenberg-client
  cfe-purls:
    - pkg:conda/gotenberg-client@${{ version }}?channel=conda-forge
    - pkg:pypi/gotenberg-client@${{ version }}
  cfe-upstream-repo: https://github.com/stumpylog/gotenberg-client/
  cfe-upstream-homepage: https://github.com/stumpylog/gotenberg-client/
  cfe-import-names: [gotenberg_client]
  cfe-source-kind: pypi-sdist
  cfe-noarch: python
  cfe-pip-check: true
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: https://github.com/conda-forge/gotenberg-client-feedstock
  cfe-forge-recipe-updates-needed:
    - recipe-regenerate
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-06-20T00:00:00Z
  cfe-generated-by-version: 8.37.0
  cfe-generated-at-datetime: 2026-06-20T00:00:00Z
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-06-20T00:00:00Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# context:
#    # version bumped 0.12.0 -> 0.13.1 (feedstock-refresh Wave B). Full CFE
#    # regenerate diff-applied onto the local mirror; sha256 from PyPI sdist.
# requirements:
#  host:
#    # hatchling pin modernized >=1.27.0,<1.28.dev0 -> >=1.27,<2 to match
#    # upstream pyproject [build-system].requires = "hatchling>=1.27,<2".
#    # The deployed feedstock still carries the stale grayskull <1.28.dev0
#    # pin -> recipe-regenerate owed.
#  run:
#    # dropped speculative python ,<4 upper bound (upstream requires-python
#    # is bare >=3.9; no upper bound). httpx[http2] extra in upstream
#    # pyproject pulls only optional h2; pip_check does NOT enforce an
#    # unrequested extra, so plain httpx >=0.27 is correct (matches the
#    # deployed feedstock run block). No G25 flatten needed.
# about:
#    # dropped grayskull's bogus `description: | # Gotenberg API Client`
#    # README-header stub; added repository + documentation from PyPI
#    # project_urls (CFEP-25 about triad). homepage kept as the GitHub repo
#    # (matches deployed feedstock) rather than grayskull's pypi.org/project.
####
