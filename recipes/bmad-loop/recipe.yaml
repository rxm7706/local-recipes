# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.12.0"

package:
  name: bmad-loop
  version: ${{ version }}

source:
  url: https://github.com/bmad-code-org/bmad-loop/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 8a299a049aa1e276fd09a68e51b0c2ca3301e0df6f264c68cff32ca27ee6b41c

build:
  noarch: python
  number: 0
  script: python -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - bmad-loop = bmad_loop.cli:main

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling >=1.30
    - pip
  run:
    - python >=${{ python_min }}
    - pyyaml >=6.0
    - regex >=2024.11.6
    - psutil >=7.2.2
    - textual >=8.0,<9
    - tomlkit >=0.13
    - pyte >=0.8.2

tests:
  - python:
      imports:
        - bmad_loop
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"
  - script:
      - bmad-loop --version
      - bmad-loop --help
      - bmad-loop adapters

about:
  summary: Deterministic ralph-loop orchestrator for the BMAD implementation phase
  description: |
    bmad-loop drives the BMAD Method implementation phase as a
    deterministic outer loop: for each story in the sprint plan it
    dispatches DEV -> VERIFY -> REVIEW -> VERIFY -> COMMIT stages in
    fresh agent sessions (tmux-multiplexed), gating progression on
    configurable policy (.bmad-loop/policy.toml). Includes a Textual
    TUI dashboard (`bmad-loop tui`) for live run monitoring.
  homepage: https://github.com/bmad-code-org/bmad-loop
  repository: https://github.com/bmad-code-org/bmad-loop
  documentation: https://github.com/bmad-code-org/bmad-loop#readme
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: bmad-loop
  cfe-upstream-registry: github
  cfe-upstream-name: bmad-code-org/bmad-loop
  cfe-purls:
    - pkg:conda/bmad-loop@${{ version }}?channel=conda-forge
    - pkg:github/bmad-code-org/bmad-loop@v${{ version }}
  cfe-upstream-repo: https://github.com/bmad-code-org/bmad-loop
  cfe-upstream-homepage: https://github.com/bmad-code-org/bmad-loop
  cfe-import-names: [bmad_loop]
  cfe-source-kind: github-tag
  cfe-noarch: python
  cfe-pip-check: true
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: https://github.com/conda-forge/bmad-loop-feedstock
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: "2026-09-26T00:18:05Z"
  cfe-generated-by-version: 8.76.0
  cfe-generated-at-datetime: "2026-07-12T14:52:00Z"
  cfe-local-build-status: success
  cfe-local-build-datetime: "2026-09-26T00:18:05Z"
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
#    # 2026-09-26: conda-forge/bmad-loop-feedstock EXISTS (created 2026-09-20, not
#    # by this repo) and ships 0.11.1 + 0.12.0 — the pending-submission status
#    # above had gone stale (G78: cfe fields are hints; verify live). The feedstock
#    # build is NOT this recipe: it gates `__unix` and adds tmux >=3.2, rich >=14.2,
#    # httpx >=0.28 as hard run deps. This estate build stays ungated (psmux/tmux
#    # are optional backends) so the workspace can solve win-64; pixi.toml pins
#    # bmad-loop to the SelfExplainML channel for that reason — under strict
#    # channel priority the conda-forge name would otherwise shadow this build.
#    # Not published on PyPI (upstream distributes via GitHub releases only),
#    # so the source is the GitHub tag archive, not a pypi.org sdist URL.
# context:
#    # python_min 3.11 overrides the conda-forge 3.10 floor — upstream
#    # pyproject declares requires-python >=3.11.
# run:
#    # regex >=2024.11.6 became a CORE upstream dep in 0.10.x/0.11.0 (the
#    # #194 env-fault classifier needs the regex module's per-call timeout=;
#    # stdlib re has none). On conda-forge as `regex` (compiled, all subdirs).
#    # 0.11.1 declares git >=2.34 as a host-tool floor (validate/run refuse
#    # below it). That is not a conda run dep — git is the operator's VCS.
#    # 0.11.0 adds an optional [opencode] extra (httpx >=0.28) for the
#    # opencode-http adapter — NOT flattened into run (this repo does not
#    # consume it; Simplicity First). psmux >=3.3.8 is an optional backend
#    # requirement enforced at runtime selection — deliberately not a dep.
#    # psutil is a win32-only marker dep upstream (linux stays dep-free);
#    # noarch:python has one artifact for all platforms, so it is flattened
#    # to an unconditional run dep (harmless on linux/macos, required for
#    # TUI liveness on win32).
#    # textual/tomlkit/pyte are upstream's optional [tui] extra, included in
#    # run because this repo consumes bmad-loop[tui] (the TUI dashboard is a
#    # first-class feature); drop to a minimal core if ever submitted and a
#    # reviewer objects.
####
