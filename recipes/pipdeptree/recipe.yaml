context:
  name: pipdeptree
  version: "3.1.1"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/pipdeptree-${{ version }}.tar.gz
  sha256: a986376399e52c9ec7515ef69df7a2dbda0bae0be1cec90dcae23a4ba6262c89

build:
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  python:
    entry_points:
      - pipdeptree = pipdeptree.__main__:main

requirements:
  host:
    - python ${{ python_min }}.*
    - hatch-vcs >=0.4
    - hatchling >=1.18
    - pip
  run:
    - packaging >=26
    - python >=${{ python_min }}
    - tomli >=2
tests:
  - python:
      imports:
        - pipdeptree
      python_version: ${{ python_min }}.*
  - script:
      - pipdeptree --help

about:
  homepage: http://github.com/naiquevin/pipdeptree
  license: MIT
  license_file: LICENSE
  summary: Displays installed python packages in form of a dependency tree.
  description: |
    pipdeptree is a command line utility for displaying the
    installed python packages in form of a dependency tree. It works for packages
    installed globally on a machine as well as in a virtualenv. Since pip freeze
    shows all dependencies as a flat list, finding out which are the top level
    packages and which packages do they depend on requires some effort. It can also
    be tedious to resolve conflicting dependencies because pip doesn't yet have
    true dependency resolution (more on this later). This utility tries to solve
    this problem.
  documentation: https://github.com/naiquevin/pipdeptree
  repository: http://github.com/naiquevin/pipdeptree

extra:
  recipe-maintainers:
    - dlqqq
    - cdiener
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: pipdeptree
  cfe-upstream-registry: pypi
  cfe-upstream-name: pipdeptree
  cfe-purls:
    - pkg:conda/pipdeptree@${{ version }}
    - pkg:pypi/pipdeptree@${{ version }}
  cfe-upstream-repo: https://github.com/tox-dev/pipdeptree
  cfe-upstream-homepage: https://github.com/tox-dev/pipdeptree
  cfe-import-names: [pipdeptree]
  cfe-source-kind: pypi-sdist
  cfe-noarch: python
  cfe-pip-check: "true"
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: https://github.com/conda-forge/pipdeptree-feedstock
  cfe-forge-recipe-updates-needed: [version-bump-to-3.1.1]
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-07-03T19:44:41Z
  cfe-generated-by-version: 8.67.0
  cfe-generated-at-datetime: 2026-07-03T19:44:41Z
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-07-03T19:44:41Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
#    # 2026-07-03 Set E wave (tracker audit findings) — LOCAL BUILDS ONLY per user
#    # direction.
####
