# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "1.0.0b260528"
  python_min: "3.10"

package:
  name: agent-framework-devui
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/a/agent-framework-devui/agent_framework_devui-${{ version }}.tar.gz
  sha256: 81a1503ec7d4c093a322416075fbbd2077cc229fac4a738413129bd6b9585c94

build:
  noarch: python
  number: 0
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - flit-core <4.0,>=3.11
  run:
    - python >=${{ python_min }}
    - agent-framework-core <2,>=1.7.0
    - openai <3,>=1.99.0
    - opentelemetry-sdk <2,>=1.39.0
    - fastapi <0.133.1,>=0.115.0
    - uvicorn <0.42.0,>=0.30.0

tests:
  - python:
      imports:
        - agent_framework.devui
      python_version: ${{ python_min }}.*
      pip_check: true

about:
  summary: |
    Debug UI for Microsoft Agent Framework with OpenAI-compatible API server.
  homepage: https://pypi.org/project/agent-framework-devui/
  repository: https://github.com/microsoft/agent-framework
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - killua156
    - mgorny
