# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "2.6.0"

package:
  name: caveman
  version: ${{ version }}

source:
  url: https://github.com/juliusbrussee/caveman/archive/refs/tags/v${{ version }}.tar.gz
  sha256: c4d2549b3a26610edee530418471b576c7abef643d56e5f8eb2b86aa176ed782

build:
  number: 0
  # Per-arch (NOT noarch), following the openspec/bmalph pattern: each
  # platform's npm global install lays down its native bin shims (symlinks
  # on unix, .cmd on win), which a single noarch artifact cannot provide.
  script:
    - if: unix
      then:
        - export npm_config_prefix="${PREFIX}"
        - npm pack --ignore-scripts
        - npm install -g ./caveman-installer-*.tgz
      else:
        - call npm config set prefix "%PREFIX%"
        - if %ERRORLEVEL% neq 0 exit 1
        - call npm pack --ignore-scripts
        - if %ERRORLEVEL% neq 0 exit 1
        - for %%f in (caveman-installer-*.tgz) do call npm install --userconfig nonexistentrc -g %%f
        - if %ERRORLEVEL% neq 0 exit 1

requirements:
  host:
    # nodejs held to the 24.x LTS line (held since build 2, 2026-08-30;
    # carried forward across the 2.6.0 bump): nodejs's
    # run_exports pins the major of whatever host resolves, and build 1's
    # host resolved 26.6.0, stamping `nodejs >=26.6.0,<27.0a0` into run —
    # which made caveman mutually exclusive with codegraph (whose own
    # build pins nodejs >=24.19,<25) in the local-recipes env. Upstream's
    # true floor is engines >=18; caveman-install verified clean on node 24.
    # Lift this hold when the env itself moves to nodejs 26 (i.e. when the
    # marp-cli/a2a-sdk libabseil gap closes and codegraph rebuilds on 26).
    - nodejs 24.*
  run:
    - nodejs >=18

tests:
  - script:
      - caveman-install --help
  - if: unix
    then:
      script:
        - test -f "${PREFIX}/lib/node_modules/caveman-installer/package.json"
        - test -f "${PREFIX}/lib/node_modules/caveman-installer/dist/caveman.skill"
        - test -d "${PREFIX}/lib/node_modules/caveman-installer/skills"
        - test -d "${PREFIX}/lib/node_modules/caveman-installer/plugins"

about:
  homepage: https://github.com/juliusbrussee/caveman
  repository: https://github.com/juliusbrussee/caveman
  license: MIT
  license_file: LICENSE
  summary: Claude Code skill that cuts ~65% of output tokens by talking like caveman
  description: |
    "Why use many token when few token do trick." caveman is an
    ultra-compressed communication mode for Claude Code: a skill/plugin
    that measurably cuts about 65% of output tokens while keeping full
    technical accuracy.

    This package ships the upstream installer CLI. After conda install,
    run ``caveman-install`` in a project (or ``caveman-install --help``
    for scopes and options) to deploy the skill into Claude Code's
    skills/plugin directories. Upstream also maintains Codex and Gemini
    variants of the mode.

    Since v2.0.0 upstream also ships a separate "Caveman Proxy" product
    (npm package ``@caveman-ai/cli``, providing the ``caveman``/``cave``
    commands) that shrinks agent *input* tokens via a local proxy with
    BSL-1.1-licensed compression internals. This recipe packages only
    the original MIT-licensed skill installer, not the proxy.

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: caveman
  cfe-upstream-registry: github
  cfe-upstream-name: juliusbrussee/caveman
  cfe-purls:
    - pkg:conda/caveman@${{ version }}?channel=conda-forge
    - pkg:github/juliusbrussee/caveman@v${{ version }}
  cfe-upstream-repo: https://github.com/juliusbrussee/caveman
  cfe-upstream-homepage: https://github.com/juliusbrussee/caveman
  cfe-import-names: []
  cfe-source-kind: github-tag
  cfe-noarch: compiled
  cfe-pip-check: "false:not-a-python-package"
  cfe-on-conda-forge-status: pending-submission-to-conda-forge
  cfe-on-conda-forge-feedstock: none
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-09-07T13:05:00Z
  cfe-generated-by-version: 8.76.0
  cfe-generated-at-datetime: "2026-07-12T15:45:00Z"
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-09-07T13:05:00Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
#    # Packaged 2026-07-12 (user request: token-usage reduction tooling).
#    # NOT the npm "caveman" package (andrewchilds' unrelated templating
#    # library) — upstream is unpublished on npm (package.json name
#    # caveman-installer is not registered there), so the source is the
#    # GitHub release tag. Conda name follows the repo/CLI name.
# context:
#    # Recipe version tracks the upstream RELEASE TAGS. Since v2.0.0
#    # package.json's own version field also tracks the release tag
#    # (was independently "0.1.0" through v1.9.1) — both branches already
#    # glob caveman-installer-*.tgz, so no hardcoded-version edit is
#    # needed on a version bump.
# build:
#    # v2.4.0 bump is a genuine upstream product split, not just a version
#    # bump. Upstream now ships "two products, pick one or both" (README):
#    # (1) this recipe's target, the original MIT skill installer -- its
#    # own bin entry was renamed caveman -> caveman-install in the same
#    # release the CLI product below was introduced; and (2) "Caveman
#    # Proxy", a separate npm package @caveman-ai/cli (bin: caveman/cave)
#    # that shrinks agent *input* tokens via a local proxy, BSL-1.1-
#    # licensed compression internals ("BSL-1.1 runtime, MIT CLI").
#    #
#    # caveman-installer's package.json now lists @caveman-ai/cli
#    # ^1.1.0 under `dependencies`. Verified empirically: a plain `npm
#    # install -g ./caveman-installer-*.tgz` nests it under
#    # lib/node_modules/caveman-installer/node_modules/@caveman-ai/cli/
#    # (standard npm global-install semantics -- a dependency's own bin
#    # entries are NOT exposed at the top-level prefix bin/ unless the
#    # dependency itself is separately `npm install -g`'d). Only
#    # caveman-installer's own bin (caveman-install) lands in
#    # ${PREFIX}/bin/. This matches upstream's documented separate
#    # install path for the proxy (`npm install -g @caveman-ai/cli`) --
#    # not a packaging bug. Do NOT try to also expose caveman/cave here;
#    # that is a different, BSL-encumbered product needing its own
#    # recipe if ever wanted.
#    #
#    # nodejs floor stays >=18 (caveman-installer's own package.json
#    # engines field, unchanged since v1.9.1) -- @caveman-ai/cli's
#    # stricter >=22.13 floor doesn't apply since its code path is never
#    # exercised by caveman-install. Verified `caveman-install --help`
#    # runs clean under node 24.
#    #
#    # @caveman-ai/cli's own compression/metering Go binaries
#    # (caveman-engine, caveman-shrink, caveman-proxy, caveman-mcp,
#    # caveman-browse, cavemem -- published as GitHub release assets
#    # under a separate "bin-vX.Y.Z" tag scheme) are irrelevant here:
#    # they belong to @caveman-ai/cli, which this recipe doesn't install
#    # as a global CLI, and even if it were, @caveman-ai/cli fetches them
#    # lazily at first `caveman setup` (hardcoded release ref in its
#    # dist/binaries.generated.js, signature-verified) rather than
#    # bundling them.
#    #
#    # Zero runtime npm deps in caveman-installer's OWN package.json
#    # (excluding the nested-only @caveman-ai/cli above), so the
#    # canonical pnpm-licenses third-party-disclaimer step stays omitted.
#    # Converted noarch->per-arch 2026-07-12 (win-64 buildability sweep):
#    # per-arch lets npm lay down native shims per platform (openspec/
#    # bmalph precedent) instead of the earlier bash-wrapper + __unix gate.
#    #
#    # 2.4.0 -> 2.6.0 bump (2026-09-07). package.json is UNCHANGED in the
#    # fields that drive this recipe: name caveman-installer, bin
#    # caveman-install, engines node >=18, the single @caveman-ai/cli
#    # ^1.1.0 dependency. So the nodejs 24.* host hold and every note above
#    # carry forward verbatim; build.number resets to 0 on the version bump.
# about:
#    # LICENSE RE-VERIFIED at 2.6.0 -- upstream runs a SPLIT license model
#    # (already present at 2.4.0, so this is a correction of an unexamined
#    # assumption, not a new upstream change). Root LICENSE is MIT plus a
#    # scope note; LICENSE.BSL carries Business Source License 1.1 for the
#    # Engine-linked directories, and LICENSING.md is the per-directory
#    # source of truth: engine/, rewriter/, browse/, proxy/, mcp/, shrink/,
#    # the mem/ Go core and shared/platform/ are all BSL-1.1.
#    #
#    # `license: MIT` is nonetheless CORRECT for this artifact, verified
#    # against the BUILT package rather than assumed. None of the BSL
#    # directories (engine/, rewriter/, browse/, proxy/, mcp/, shrink/, the
#    # mem/ Go core, shared/platform/) appear in it. Four paths whose NAMES
#    # match those words do ship, and each was classified individually:
#    #   - src/mcp-servers/caveman-shrink/  -- declares its own
#    #     "license": "MIT" in package.json; a standalone JS MCP proxy that
#    #     compresses prose fields in pure JS (compress.js) and spawns the
#    #     WRAPPED upstream MCP server, not the BSL Go shrink binary. It is
#    #     not the BSL `shrink/` directory despite the shared word.
#    #   - skills/engine-mcp-tools.json -- a manifest; skills/ is explicitly
#    #     MIT in LICENSING.md.
#    #   - node_modules/@caveman-ai/cli/dist/proxy-fetch.js -- @caveman-ai/cli
#    #     is MIT (npm metadata + ships its own LICENSE); LICENSING.md
#    #     classifies packages/cli/ as "MIT | Funnel/on-ramp ... does not
#    #     contain engine code".
#    # The BSL Go binaries are fetched lazily at `caveman setup`, never
#    # bundled. Identical file set in 2.4.0, so this is a correction of an
#    # earlier unexamined assumption, not a 2.6.0 regression.
#    #
#    # NOTE: LICENSE.BSL itself DOES ship, even though `files` does not list
#    # it -- npm force-includes every LICENSE* at the package root regardless
#    # of the allowlist. That is the licence TEXT riding along, not licensed
#    # code; about.license_file stays the operative root LICENSE (MIT plus
#    # its scope note). Do not read the presence of LICENSE.BSL in the
#    # artifact as evidence the package is BSL-encumbered.
#    #
#    # Re-verify on any future bump by inspecting the BUILT package, not the
#    # `files` list: a directory-only listing of `npm pack` output hides both
#    # root-level files like LICENSE.BSL and the nested node_modules tree. If
#    # upstream ever ships real engine code, about.license must become a
#    # compound SPDX expression and the package stops being
#    # conda-forge-eligible (BSL-1.1 is a valid SPDX id but NOT OSI-approved
#    # -- the ragstack-ai-knowledge-store precedent).
####
