# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
context:
  name: rustworkx
  python_name: rustworkx
  version: "0.18.1"

package:
  name: rustworkx
  version: ${{ version }}

source:
  url: https://github.com/Qiskit/${{ name }}/archive/refs/tags/${{ version }}.tar.gz
  sha256: 901e3161100834877c84b3a301da658c1153399d478667b1a0bbb9c2190d6d4f

build:
  number: 7
  script:
    - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml
    - if: target_platform == "osx-arm64"
      then:
        - export MATURIN_PEP517_ARGS="--target aarch64-apple-darwin"
    - ${{ PYTHON }} -m pip install . -vv
  skip: not (match(python, python_min ~ ".*") and is_abi3)
  python:
    version_independent: true

requirements:
  build:
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - maturin
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - ${{ compiler('rust') }}
    - cargo-bundle-licenses
  host:
    - python
    - python-abi3
    - pip
    - maturin
    - setuptools >=77.0.0
  run:
    - numpy >=1.16.0,<3
    - python
  run_constraints:
    - matplotlib >=3.0
    - matplotlib-base >=3.0
    - pillow >=5.4

tests:
  - python:
      imports:
        - rustworkx
        - rustworkx.visualization
      python_version: ["${{ python_min ~ '.*' }}"]
  - script:
      - pip check
      - if: build_platform == target_platform
        then:
          - python -m unittest discover tests
    requirements:
      run:
        - if: build_platform == target_platform
          then:
            - networkx >=2.5
        - pip
    files:
      source:
        - tests/*

about:
  homepage: https://www.rustworkx.org/
  repository: https://github.com/Qiskit/rustworkx
  summary: A python graph library implemented in Rust
  license: Apache-2.0
  license_file:
    - LICENSE
    - THIRDPARTY.yml

extra:
  recipe-maintainers:
    - IvanIsCoding
    - BastianZim
    - wshanks
