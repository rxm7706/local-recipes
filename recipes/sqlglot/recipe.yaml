# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "30.17.0"

recipe:
  name: sqlglot
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/s/sqlglot/sqlglot-${{ version }}.tar.gz
  sha256: 2d6b8def93304fa300f4d20f48e3909e7f436fda56ca1fafd8975f6c561ef62c

build:
  number: 0
  noarch: python

outputs:
  - package:
      name: sqlglot
    build:
      noarch: python
      script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check
    requirements:
      host:
        - pip
        - python ${{ python_min }}.*
        - setuptools_scm
        - setuptools >=61.0
      run:
        - python >=${{ python_min }}
    tests:
      - python:
          imports:
            - sqlglot
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - "*"
      - files:
          source:
            - tests/
        requirements:
          run:
            - openjdk
            - pandas
            - pip
            - pyspark
            - pytest
            - python-dateutil
            - python-duckdb >=0.6
            - python ${{ python_min }}.*
        script:
          - pytest -vvv --capture=tee-sys --color=yes --tb=long tests
about:
  summary: An easily customizable SQL parser and transpiler
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/tobymao/sqlglot

extra:
  recipe-maintainers:
    - timkpaine
    - tobymao
    - thewchan

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: sqlglot
  cfe-upstream-registry: pypi
  cfe-upstream-name: sqlglot
  cfe-purls:
    - pkg:conda/sqlglot@${{ version }}?channel=conda-forge
    - pkg:pypi/sqlglot@${{ version }}
  cfe-upstream-repo: https://github.com/tobymao/sqlglot
  cfe-upstream-homepage: https://github.com/tobymao/sqlglot
  cfe-import-names: [sqlglot]
  cfe-source-kind: pypi-sdist
  cfe-noarch: python
  cfe-pip-check: "true"
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: https://github.com/conda-forge/sqlglot-feedstock
  cfe-forge-recipe-updates-needed: [version-bump-to-30.8.0, sqlglotrs-pin-0.13.0, drop-obsolete-cargo-version-test]
  cfe-forge-blocker-list: ["sqlglotrs 0.13.0 must land on cf first (feedstock#48); bot bump PRs stop at 30.1.0 — fresh 30.8.0 PR needed for sqlmesh"]
  cfe-last-checked: 2026-07-03T15:29:19Z
  cfe-generated-by-version: 8.66.0
  cfe-generated-at-datetime: 2026-07-03T15:29:19Z
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-07-03T15:29:19Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
#    # 2026-07-03 sqlmesh-unblock chain (tracker/sqlmesh-feedstock#82). NOT our
#    # feedstocks (sqlglot: tobymao+thewchan; sqlglotrs: thewchan) — fix material
#    # verified locally for handoff, no maintainer-edit pushes possible.
# outputs:
#    # sqlglot-rs metapackage kept per feedstock shape; at 30.8.0 the upstream rs
#    # extra also pins sqlglotc==30.8.0 — NOT added: sqlglotc ships the same
#    # sqlglot/ namespace (G88 file-clash on conda), parked.
####
