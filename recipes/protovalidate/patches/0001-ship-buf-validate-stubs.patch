--- a/pyproject.toml
+++ b/pyproject.toml
@@ -121,3 +121,9 @@
 
 [tool.ty.environment]
 extra-paths = ["gen"]
+
+# conda-forge-expert (local-only): upstream's wheel omits the buf.validate
+# protobuf stubs (generated by `buf generate` into gen/, never packaged).
+# Ship them so `import protovalidate` works. See docs/specs/flyte-conda-forge.md.
+[tool.hatch.build.targets.wheel.force-include]
+"gen/buf" = "buf"
