# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.6.5"

package:
  name: office2pdf
  version: ${{ version }}

source:
  # office2pdf-cli is the published binary crate that installs the `office2pdf`
  # command. Build from the crates.io release rather than the GitHub workspace:
  # the workspace Cargo.toml pins git forks via [patch.crates-io], which cannot
  # be fetched in an offline build, while the published crate resolves every
  # dependency from the registry and ships a Cargo.lock for a reproducible build.
  - url: https://static.crates.io/crates/office2pdf-cli/office2pdf-cli-${{ version }}.crate
    sha256: b8f7f26a1e489cb084d4594e2823a7dfd5ea5775159caa78fbad17380d8f893b
  # The published crate carries only the SPDX license string, not the license
  # text, so fetch LICENSE from the matching git tag.
  - url: https://raw.githubusercontent.com/developer0hye/office2pdf/v${{ version }}/LICENSE
    sha256: a69f60df4102c74db295b45bdeca8bb300ecaa954a319de94cbe4e8e337e98f9

build:
  number: 0
  script:
    env:
      CARGO_PROFILE_RELEASE_STRIP: symbols
      CARGO_PROFILE_RELEASE_LTO: fat
    content:
      # rattler-build does not auto-extract a .crate, so unpack it before building.
      - if: unix
        then: |
          tar -xzf office2pdf-cli-${{ version }}.crate
          cd office2pdf-cli-${{ version }}
          cargo-bundle-licenses --format yaml --output "${SRC_DIR}/THIRDPARTY.yml"
          cargo auditable install --locked --no-track --bins --root "${PREFIX}" --path .
        else: |
          tar -xzf office2pdf-cli-${{ version }}.crate
          cd office2pdf-cli-${{ version }}
          cargo-bundle-licenses --format yaml --output "%SRC_DIR%\THIRDPARTY.yml"
          cargo auditable install --locked --no-track --bins --root "%LIBRARY_PREFIX%" --path .

requirements:
  build:
    - ${{ compiler('rust') }}
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - cargo-bundle-licenses
    - cargo-auditable

tests:
  - script:
      - office2pdf --version
      - office2pdf --help
  - package_contents:
      bin:
        - office2pdf
      strict: true

about:
  homepage: https://github.com/developer0hye/office2pdf
  repository: https://github.com/developer0hye/office2pdf
  summary: Convert DOCX, XLSX, and PPTX files to PDF using pure Rust
  description: |
    office2pdf converts Microsoft Office documents (DOCX, XLSX, PPTX) to PDF.
    It is written in pure Rust and renders documents with Typst, so it needs no
    external office suite or runtime. Fonts embedded in the source documents are
    extracted automatically, and PDF/A-2b archival output is available.
  license: Apache-2.0
  license_file:
    - LICENSE
    - THIRDPARTY.yml

extra:
  recipe-maintainers:
    - killua156
    - mgorny
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: office2pdf
  cfe-upstream-registry: cargo
  cfe-upstream-name: office2pdf-cli
  cfe-purls:
    - pkg:conda/office2pdf@${{ version }}?channel=conda-forge
    - pkg:cargo/office2pdf-cli@${{ version }}
    - pkg:github/developer0hye/office2pdf@v${{ version }}
  cfe-upstream-repo: https://github.com/developer0hye/office2pdf
  cfe-upstream-homepage: https://github.com/developer0hye/office2pdf
  cfe-import-names: []
  cfe-source-kind: "crates-io-crate:published-crate-ships-cargo-lock-avoids-git-fork-patch"
  cfe-noarch: compiled
  cfe-pip-check: "false:not-python"
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: https://github.com/conda-forge/office2pdf-feedstock
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-07-28T00:16:58Z
  cfe-generated-by-version: manual
  cfe-generated-at-datetime: 2026-07-28T00:16:58Z
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-07-28T00:31:31Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
#    # Mirrored from conda-forge/office2pdf-feedstock main @ a38a6f9b
#    # (merge of PR #6, 2026-07-28T00:11:26Z). recipe/ upstream contains ONLY
#    # recipe.yaml — no build.sh / build.bat / patches. The feedstock's root
#    # conda-forge.yml is mirrored alongside this file.
# source:
#    # BOTH former blockers are RESOLVED as of the crates.io source strategy:
#    #   1. The vendored git-fork deps (umya-spreadsheet @19d82bc, docx-rs
#    #      @c57785df, pinned via [patch.crates-io] in the GitHub workspace) are
#    #      gone — the published office2pdf-cli crate resolves every dependency
#    #      from the registry, so no vendoring and no downstream patch is needed.
#    #   2. Upstream's missing Cargo.lock no longer blocks reproducibility — the
#    #      published crate ships one, so the build uses `cargo auditable install
#    #      --locked`.
#    # The prior local strategy (GitHub tag archive + 2 fork tarballs +
#    # patches/0001-vendor-git-fork-deps.patch) is therefore obsolete and was
#    # deleted with this sync; the upstream-ask CFE comments it carried are moot.
#    # cfe-source-kind uses a non-enum value: the documented taxonomy
#    # (pypi-sdist | pypi-wheel | github-tag | github-commit |
#    # github-release-binary) has no crates.io member. Recorded accurately
#    # rather than forced into a wrong bucket.
# build:
#    # `tar -xzf` before building is load-bearing: rattler-build does not
#    # auto-extract a .crate (it is a gzipped tar with a non-archive extension).
####
