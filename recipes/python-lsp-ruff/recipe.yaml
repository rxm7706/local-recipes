# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "2.3.2"
  python_check_max: "3.14"

package:
  name: python-lsp-ruff
  version: ${{ version }}

source:
  - target_directory: dist
    url: https://pypi.org/packages/source/p/python-lsp-ruff/python_lsp_ruff-${{ version }}.tar.gz
    sha256: 1b6705a7f671b760a7870fb84063d207f21ca5821fd51cb9a2483639d64a8c88
  # PyPI sdist missing test files, see https://github.com/python-lsp/python-lsp-ruff/issues/117
  - target_directory: src
    url: https://github.com/python-lsp/python-lsp-ruff/archive/refs/tags/v${{ version }}.tar.gz
    sha256: ab88cd37992ac3002818e7ed6ab3ccbf8cd4bc2646ecbf10114c22c3959f8ccc

build:
  number: 0
  noarch: python
  script:
    - cd dist
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - setuptools
  run:
    - lsprotocol >=2023.0.1
    - cattrs !=23.2.1
    - python >=${{ python_min }}
    - python-lsp-server-base
    - ruff >=0.8.0
    - tomli >=1.1.0

tests:
  - python:
      imports: pylsp_ruff
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - ${{ python_check_max }}.*
  - files:
      source:
        - src/tests/
    requirements:
      run:
        - coverage
        - pytest
        - python ${{ python_min }}.*
    script:
      - cd src
      - coverage run --source=pylsp_ruff --branch -m pytest -vv --color=yes --tb=long
      - coverage report --show-missing --skip-covered --fail-under=85

about:
  summary: Ruff linting plugin for pylsp
  license: MIT
  license_file: dist/LICENSE
  homepage: https://pypi.org/project/python-lsp-ruff
  repository: https://github.com/python-lsp/python-lsp-ruff

extra:
  recipe-maintainers:
    - bollwyvl
    - jhossbach
    - jonas-w
