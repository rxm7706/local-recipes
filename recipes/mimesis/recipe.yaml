# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: mimesis
  version: "19.1.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name }}-${{ version }}.tar.gz
  sha256: af4f14852a97657c5dbfad860be90c0efe97bf3758809e034b6581a45b13814b

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - hatchling
  run:
    - python >=${{ python_min }}

tests:
  - python:
      imports:
        - mimesis
        - mimesis.providers
      python_version: ${{ python_min }}.*
about:
  license: MIT
  license_file: LICENSE
  summary: Mimesis is a robust data generator for Python that can produce a wide range of fake data in multiple languages.
  homepage: https://github.com/lk-geimfari/mimesis
  repository: https://github.com/lk-geimfari/mimesis
  documentation: https://mimesis.readthedocs.io/

extra:
  recipe-maintainers:
    - utabe
    - janjagusch
