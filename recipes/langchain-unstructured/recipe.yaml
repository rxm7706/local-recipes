# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "1.0.1"

package:
  name: langchain-unstructured
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/l/langchain-unstructured/langchain_unstructured-${{ version }}.tar.gz
  sha256: 9b4b832b8fcdef8598ff634ec6fc0e344e6d8fdae854c5727f717d605d28e406

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - poetry-core
    - pip
  run:
    - python >=${{ python_min }}
    - langchain-core >=1.0.5,<2.0.0
    - unstructured-client >=0.27.0,<1

tests:
  - python:
      imports:
        - langchain_unstructured
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  homepage: https://pypi.org/project/langchain-unstructured/
  license: MIT
  license_file: LICENSE
  summary: An integration package connecting Unstructured and LangChain
  repository: https://github.com/langchain-ai/langchain-unstructured
  documentation: https://python.langchain.com/
  description: |
    An integration package connecting Unstructured and LangChain. Provides
    document loaders that use the Unstructured library to parse and partition
    a wide range of file formats (PDF, HTML, Office documents, images, and
    more) into LangChain Document objects.

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: langchain-unstructured
  cfe-upstream-registry: pypi
  cfe-upstream-name: langchain-unstructured
  cfe-name-aliases:
    - langchain_unstructured
  cfe-purls:
    - pkg:conda/langchain-unstructured@${{ version }}
    - pkg:pypi/langchain-unstructured@${{ version }}
  cfe-upstream-repo: https://github.com/langchain-ai/langchain-unstructured
  cfe-upstream-homepage: https://pypi.org/project/langchain-unstructured/
  cfe-on-conda-forge-status: pending-submission-to-conda-forge
  cfe-on-conda-forge-feedstock: none
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-06-19T13:36:19Z
  cfe-generated-by-version: 8.30.1
  cfe-generated-at-datetime: 2026-06-19T13:36:19Z
####
# CFE comments
# requirements:
#  run:
#    # The optional `local` extra (unstructured[all-docs] >=0.15.7,<0.16.0,
#    # gated to python<3.13) is omitted: that <0.16.0 range is not on
#    # conda-forge (only >=0.22.x is available), and it's an optional extra,
#    # not a base runtime dependency.
####
