# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: google-cloud-aiplatform
  version: "1.164.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/google_cloud_aiplatform-${{ version }}.tar.gz
  sha256: 51fb881523631e591a3d11a46cb9d7bd390e05127fe1d08c596ddce3e45fed2d

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - tb-gcp-uploader=google.cloud.aiplatform.tensorboard.uploader_main:run_main

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - google-api-core-grpc >=1.34.1,<3.0.0,!=2.0.*,!=2.1.*,!=2.2.*,!=2.3.*,!=2.4.*,!=2.5.*,!=2.6.*,!=2.7.*
    - google-auth >=2.47.0,<3.0.0
    - certifi >=2023.7.22
    - proto-plus >=1.22.3,<2.0.0
    - protobuf >=3.20.2,<7.0.0,!=4.21.0,!=4.21.1,!=4.21.2,!=4.21.3,!=4.21.4,!=4.21.5
    - packaging >=14.3
    # tightest of upstream's floors (python >=3.13) and ceilings (python <3.13)
    - google-cloud-storage >=3.10.0,<4.0.0
    - google-cloud-bigquery-core >=1.15.0,<4.0.0,!=3.20.0
    - google-cloud-resource-manager >=1.3.3,<3.0.0
    # tightest of upstream's floors (python >=3.10)
    - google-genai >=1.66.0,<3.0.0
    - pydantic <3
    - typing_extensions
    - docstring_parser <1

tests:
  - python:
      imports:
        - google
        - vertexai
      python_version:
        - ${{ python_min }}.*
        - "*"
      pip_check: true

about:
  summary: Vertex AI API client library
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/googleapis/python-aiplatform

extra:
  recipe-maintainers:
    - xylar
