# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.4.5"

package:
  name: jigsawstack
  version: ${{ version }}

source:
  url: https://github.com/jigsawstack/jigsawstack-python/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 042be08a0e256363c7b918529edb120a35b86ab875c335accf62c5fdc56ea29f

build:
  number: 0
  noarch: python
  script: sed -i.bak '/setup_requires/d' setup.py && rm -f setup.py.bak && ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
    - wheel
  run:
    - python >=${{ python_min }}
    - requests >=2.31.0
    - typing_extensions
    - aiohttp >=3.12.15

tests:
  - python:
      imports:
        - jigsawstack
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  homepage: https://github.com/jigsawstack/jigsawstack-python
  license: MIT
  license_file: LICENSE
  summary: JigsawStack - The AI SDK for Python
  description: |
    JigsawStack is the AI SDK for Python providing a unified client for the
    JigsawStack platform APIs (search, web scraping, vision/OCR, audio, text
    translation, summarization, sentiment, embeddings, prompt engine, SQL
    generation, validation, prediction, and file store).

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: jigsawstack
  cfe-upstream-registry: pypi
  cfe-upstream-name: jigsawstack
  cfe-purls:
    - pkg:conda/jigsawstack@${{ version }}
    - pkg:pypi/jigsawstack@${{ version }}
  cfe-upstream-repo: https://github.com/jigsawstack/jigsawstack-python
  cfe-upstream-homepage: https://github.com/jigsawstack/jigsawstack-python
  cfe-import-names: [jigsawstack]
  cfe-source-kind: github-tag:pypi-sdist-omits-requirements-txt-G54
  cfe-noarch: python
  cfe-pip-check: true
  cfe-on-conda-forge-status: pending-approval-on-conda-forge
  cfe-on-conda-forge-feedstock: none
  cfe-submission-pr: https://github.com/conda-forge/staged-recipes/pull/33857
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-06-25T03:00:00Z
  cfe-generated-by-version: 8.34.0
  cfe-generated-at-datetime: 2026-06-19T20:30:00Z
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-06-24T09:45:03Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
#    # MIT license verified OSI-approved (conda-forge eligible). setup.py
#    # classifier "License :: OSI Approved :: MIT License"; upstream ships
#    # NO LICENSE file (absent from sdist AND GitHub repo, GH API license=None)
#    # so LICENSE vendored in-recipe per SKILL.md license-placement pattern (2).
# source:
#    # GitHub tag archive (G54 source preference: sdist > github-source > wheel).
#    # The PyPI sdist is UNUSABLE: setup.py does `open("requirements.txt").readlines()`
#    # but requirements.txt is NOT packaged in the sdist -> FileNotFoundError. The
#    # GitHub tag archive DOES ship requirements.txt + setup.py, so it builds where
#    # the sdist can't, and is preferred over the py3-none-any wheel. requirements.txt
#    # == the recipe run deps. The build strips setup.py's `setup_requires=["pytest-
#    # runner"]` (legacy `setup.py test` only; would trigger an offline-CI network
#    # fetch — conda-forge runs its own tests). File upstream issue to package
#    # requirements.txt (or migrate to pyproject.toml) so the sdist builds, then
#    # switch source to sdist.
# build:
#  host:
#    # setuptools + wheel are the PEP 517 backend for the setup.py source build
#    # (G55: wheel installs need no backend, a source build does).
# run:
#    # All 3 deps confirmed on conda-forge (check_dependencies, 2026-06-19):
#    # requests, typing_extensions, aiohttp. Zero missing -> leaf recipe.
####
