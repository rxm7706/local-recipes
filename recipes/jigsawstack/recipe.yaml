# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.4.5"

package:
  name: jigsawstack
  version: ${{ version }}

source:
  url: https://pypi.org/packages/py3/j/jigsawstack/jigsawstack-${{ version }}-py3-none-any.whl
  sha256: 014b029d132c7a36f2c26630fe424faf6880ca2a62c6d367bf72de8cf68d3e95

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install jigsawstack-${{ version }}-py3-none-any.whl -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
  run:
    - python >=${{ python_min }}
    - requests >=2.31.0
    - typing_extensions
    - aiohttp >=3.12.15

tests:
  - python:
      imports:
        - jigsawstack
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  homepage: https://github.com/jigsawstack/jigsawstack-python
  license: MIT
  license_file: LICENSE
  summary: JigsawStack - The AI SDK for Python
  description: |
    JigsawStack is the AI SDK for Python providing a unified client for the
    JigsawStack platform APIs (search, web scraping, vision/OCR, audio, text
    translation, summarization, sentiment, embeddings, prompt engine, SQL
    generation, validation, prediction, and file store).

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: jigsawstack
  cfe-upstream-registry: pypi
  cfe-upstream-name: jigsawstack
  cfe-purls:
    - pkg:conda/jigsawstack@${{ version }}
    - pkg:pypi/jigsawstack@${{ version }}
  cfe-upstream-repo: https://github.com/jigsawstack/jigsawstack-python
  cfe-upstream-homepage: https://github.com/jigsawstack/jigsawstack-python
  cfe-on-conda-forge-status: pending-submission-to-conda-forge
  cfe-on-conda-forge-feedstock: none
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-06-19T20:30:00Z
  cfe-generated-by-version: 8.34.0
  cfe-generated-at-datetime: 2026-06-19T20:30:00Z
####
# CFE comments
# Header:
#    # MIT license verified OSI-approved (conda-forge eligible). setup.py
#    # classifier "License :: OSI Approved :: MIT License"; upstream ships
#    # NO LICENSE file (absent from sdist AND GitHub repo, GH API license=None)
#    # so LICENSE vendored in-recipe per SKILL.md license-placement pattern (2).
# source:
#    # WHEEL source (not sdist): the sdist's setup.py does
#    # `open("requirements.txt").readlines()` but requirements.txt is NOT
#    # packaged in the sdist -> `pip install .` fails with FileNotFoundError.
#    # The py3-none-any wheel is pure-Python and carries the same deps
#    # (egg-info/requires.txt == wheel Requires-Dist). File upstream issue to
#    # ship requirements.txt (or migrate to pyproject.toml) so a future sdist
#    # builds, then switch source back to sdist.
# run:
#    # All 3 deps confirmed on conda-forge (check_dependencies, 2026-06-19):
#    # requests, typing_extensions, aiohttp. Zero missing -> leaf recipe.
####
