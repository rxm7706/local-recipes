{% set tests_to_skip = "test_merge_out_of_range_nodata or test_rio_env_no_credentials" %}
{% set version = "1.5.1" %}

package:
  name: rasterio
  version: {{ version }}

source:
  url: https://github.com/rasterio/rasterio/archive/{{ version }}.tar.gz
  sha256: eb8e7062fc0bcdef635e43fd1632b15aa983b71476bec1e1f75d618326fb9e4a

build:
  number: 1
  skip: true  # [py<312]
  entry_points:
    - rio = rasterio.rio.main:main_group

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - cython                                 # [build_platform != target_platform]
    - numpy >=1.24                           # [build_platform != target_platform]
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
  host:
    - python
    - pip
    - cython
    - numpy >=1.24
    - libgdal-core
    - proj
    - setuptools
  run:
    - python
    - setuptools >=0.9.8
    - libgdal-core
    - affine
    - attrs
    - certifi
    - click >=4,!=8.2.* # https://github.com/pallets/click/issues/2939
    - cligj >=0.5
    - snuggs >=1.4.1
    - click-plugins
    - proj

test:
  source_files:
    - tests
  requires:
    - pip
    # awaiting upstream pytest=8 support
    - pytest >=2.8.2,<8
    - pytest-cov >=2.2.0
    - ipython >=2.0
    - aiohttp
    - boto3 >=1.2.4
    - fsspec >=2025.9.0
    - s3fs >=2025.9.0
    - hypothesis
    - libgdal-netcdf
    - matplotlib-base
    - packaging
    - requests
    - shapely
  files:
    - test_data/test.tif
  imports:
    - rasterio
  commands:
    - rio --help
    - rio info {{ RECIPE_DIR }}/test_data/test.tif  # [not win]
    - rio info {{ RECIPE_DIR }}\\test_data\\test.tif  # [win]
    # skip linux tests for cross-compiled targets
    # xylar 2022-Jul-6: disabling test_search_gdal_data_debian and test_decimated_no_use_overview under win.
    #                   test_decimated_no_use_overview fails because of trying to overwrite an existing file.
    # Tests skipped on all platforms:
    # - test_merge_out_of_range_nodata: unknown numpy 2 segfault.
    # - test_rio_env_no_credentials: writes an indented AWS credentials file, which recent
    #   botocore rejects with InvalidConfigError instead of reaching S3.
    {% set tests_to_skip = "test_merge_out_of_range_nodata or test_rio_env_no_credentials" %}
    - $PYTHON -m pytest -v -m "not wheel" -rxXs -k "not ({{ tests_to_skip }})" tests  # [linux and build_platform == target_platform]
    - $PYTHON -m pytest -v -m "not wheel" -rxXs -k "not ({{ tests_to_skip }})" tests  # [osx]
    - python -m pytest -v -m "not wheel" -rxXs -k "not (test_search_gdal_data_debian or test_decimated_no_use_overview or test_copyfiles_same_dataset_another_name or {{ tests_to_skip }})" tests  # [win]
    - pip check
about:
  home: https://github.com/rasterio/rasterio
  license: BSD-3-Clause
  license_file: LICENSE.txt
  summary: Rasterio reads and writes geospatial raster datasets

extra:
  recipe-maintainers:
    - weiji14
    - ocefpaf
    - ceholden
    - snowman2
    - akrherz
