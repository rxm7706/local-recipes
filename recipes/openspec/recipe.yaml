# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: openspec
  npm_name: "@fission-ai/openspec"
  npm_scope: fission-ai
  version: "1.6.0"

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://registry.npmjs.org/${{ npm_name }}/-/${{ name }}-${{ version }}.tgz
  sha256: a80477767e98a62e956464ad09a44b28cacc4fcbfde23765f7b4ba598ee13859

build:
  number: 0
  script:
    - if: unix
      then:
        - export npm_config_prefix="${PREFIX}"
        - export NPM_CONFIG_USERCONFIG=/tmp/nonexistentrc
        - npm pack
        - npm install -g fission-ai-${PKG_NAME}-${PKG_VERSION}.tgz
    - if: win
      then:
        - npm config set prefix "%PREFIX%"
        - npm pack
        - npm install --userconfig nonexistentrc -g fission-ai-%PKG_NAME%-%PKG_VERSION%.tgz

requirements:
  host:
    - nodejs >=20.19.0
  run:
    - nodejs >=20.19.0

tests:
  - script:
      - ${{ name }} --help
  - script:
      - if: unix
        then: test -f $PREFIX/bin/${{ name }}
      - if: win
        then: if not exist %PREFIX%\bin\${{ name }} exit 1

about:
  summary: AI-native system for spec-driven development
  description: |
    OpenSpec is a specification-driven development framework that aligns humans
    and AI coding assistants through structured specs before implementation.
    It enables spec-driven development for AI coding assistants with no API keys required.
  homepage: https://github.com/Fission-AI/OpenSpec
  repository: https://github.com/Fission-AI/OpenSpec
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: openspec
  cfe-upstream-registry: npm
  cfe-upstream-name: "@fission-ai/openspec"
  cfe-name-aliases:
    - openspec
  cfe-purls:
    - pkg:conda/openspec@${{ version }}?channel=conda-forge
    - pkg:npm/%40fission-ai/openspec@${{ version }}
  cfe-upstream-repo: https://github.com/Fission-AI/OpenSpec
  cfe-upstream-homepage: https://github.com/Fission-AI/OpenSpec
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: https://github.com/conda-forge/openspec-feedstock
  cfe-forge-recipe-updates-needed: version-bump-to-1.6.0
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-07-12T17:30:00Z
  cfe-generated-by-version: 8.30.1
  cfe-generated-at-datetime: 2026-06-19T13:36:19Z
####
