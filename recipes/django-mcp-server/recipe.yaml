# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.5.7"

package:
  name: django-mcp-server
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/d/django-mcp-server/django_mcp_server-${{ version }}.tar.gz
  sha256: 5077f8fabf5fb621b5ce490afd0db60f21e57b3a451ed14a9f44aef545ea4eee

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - poetry-core
    - pip
  run:
    - python >=${{ python_min }}
    - mcp >=1.8.0
    - django >=4.0
    - djangorestframework >=3.15.0
    - inflection >=0.5.1,<0.6.0
    - uritemplate >=4.1.1,<5.0.0

tests:
  - script:
      - python -c "import django; from django.conf import settings; settings.configure(DEBUG=True,INSTALLED_APPS=['mcp_server']); django.setup(); import mcp_server;"
  # CFEP-25-justified: Django app — `import mcp_server` raises
  # ImproperlyConfigured without DJANGO_SETTINGS_MODULE configured (the
  # module imports django.db.models at top level). The script test above
  # exercises the real import after settings.configure; this block keeps
  # pip_check on the dependency graph.
  - python:
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"
  - package_contents:
      site_packages:
        - mcp_server

about:
  homepage: https://github.com/omarbenhamid/django-mcp-server
  repository: https://github.com/omarbenhamid/django-mcp-server
  documentation: https://github.com/omarbenhamid/django-mcp-server/blob/main/README.md
  license: MIT
  license_file: LICENCE.md
  summary: Django MCP Server is a Django extensions to easily enable AI Agents to interact with Django Apps through the Model Context Protocol it works equally well on WSGI and ASGI
  description: |
    Django MCP Server is an implementation of the Model Context Protocol (MCP) extension for Django.
    This module allows MCP Clients and AI agents to interact with any Django application seamlessly.

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: django-mcp-server
  cfe-upstream-registry: pypi
  cfe-upstream-name: django-mcp-server
  cfe-purls:
    - pkg:conda/django-mcp-server@${{ version }}
    - pkg:pypi/django-mcp-server@${{ version }}
  cfe-upstream-repo: https://github.com/omarbenhamid/django-mcp-server
  cfe-upstream-homepage: https://github.com/omarbenhamid/django-mcp-server
  cfe-import-names: [mcp_server]
  cfe-source-kind: pypi-sdist
  cfe-noarch: python
  cfe-pip-check: true
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: https://github.com/conda-forge/django-mcp-server-feedstock
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-06-20T00:00:00Z
  cfe-generated-by-version: 8.37.0
  cfe-generated-at-datetime: 2026-06-20T00:00:00Z
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-06-20T00:00:00Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# tests:
#    # Django web-app escape hatch: bare `import mcp_server` raises
#    # ImproperlyConfigured (top-level django.db.models import) without
#    # DJANGO_SETTINGS_MODULE. The script test configures settings first;
#    # the package_contents block + pip_check cover the rest (CFEP-25).
# about:
#    # homepage typo fixed: deployed feedstock + prior local recipe had
#    # `hhttps://github.com/...`; corrected to `https://`.
# cfe-import-names:
#    # G7: grayskull guessed `django_mcp_server`; the real top-level
#    # package is `mcp_server` (pyproject [[tool.poetry.packages]]
#    # include = "mcp_server"; verified from sdist __init__.py).
# source:
#    # sdist ships LICENCE.md (British spelling) — license_file correct,
#    # no secondary source needed. grayskull regen dropped it (PLEASE_ADD).
####
