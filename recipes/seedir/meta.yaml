{% set name = "seedir" %}
{% set version = "0.5.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/seedir-{{ version }}.tar.gz
  sha256: 9fb82a6133f92a711bb2d46b2a142f56712e242a4fc03fef2fea9418d307efe2

build:
  entry_points:
    - seedir=seedir.__main__:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - natsort

test:
  imports:
    - seedir
  commands:
    - pip check
    - seedir --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/earnestt1234/seedir
  summary: Package for creating, editing, and reading folder tree diagrams.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rxm7706
