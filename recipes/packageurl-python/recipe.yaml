# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.17.6"
  python_check_max: "3.14"
  # TODO: fix after upstream fix
  # https://github.com/package-url/packageurl-python/pull/201/files#r2257224277
  use_mypy: "1"

package:
  name: packageurl-python
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/p/packageurl-python/packageurl_python-${{ version }}.tar.gz
  sha256: 1252ce3a102372ca6f86eb968e16f9014c4ba511c5c37d95a7f023e2ca6e5c25
  patches:
    # TODO: remove after https://github.com/package-url/packageurl-python/pull/213
    - 0000-fix-types.patch

build:
  number: 0
  noarch: python
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - setuptools
  run:
    - python >=${{ python_min }}

tests:
  - python:
      imports:
        - packageurl
        - packageurl.contrib
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - ${{ python_check_max }}.*
  - if: use_mypy == "1"
    then:
      requirements:
        run:
          - mypy
          - python ${{ python_min }}.*
      script:
        - mypy -p packageurl --ignore-missing-imports

about:
  summary: A purl aka. Package URL parser and builder
  license: MIT
  license_file: mit.LICENSE
  homepage: https://github.com/package-url/packageurl-python

extra:
  recipe-maintainers:
    - bollwyvl
