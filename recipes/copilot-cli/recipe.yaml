# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
#
# ==============================================================================
#  DO NOT SUBMIT THIS RECIPE TO conda-forge/staged-recipes
# ==============================================================================
#  GitHub Copilot CLI's proprietary license (LICENSE.md, Section 2) forbids
#  redistribution "on a standalone basis or as a primary product" — which is
#  exactly what a conda package is. A prior submission was rejected for this
#  reason: https://github.com/conda-forge/staged-recipes/pull/32522
#
#  This recipe is here for LOCAL builds and personal-channel hosting only,
#  pending one of:
#    - github/copilot-cli#83   (open-source the CLI)
#    - github/copilot-cli#1962 (license clarification)
#    - github/copilot-cli#2294 (clarify packaging in distro repos)
#
#  See ./DO_NOT_SUBMIT.md for the full rationale.
# ==============================================================================

schema_version: 1

context:
  version: "1.0.75"

package:
  name: copilot-cli
  version: ${{ version }}

source:
  - if: target_platform == "linux-64"
    then:
      url: https://github.com/github/copilot-cli/releases/download/v${{ version }}/copilot-linux-x64.tar.gz
      sha256: d304ef66c0c1d2de7d736b3653b36557e80b4f40a0bf8c4a71e7215f3aff7441

  - if: target_platform == "linux-aarch64"
    then:
      url: https://github.com/github/copilot-cli/releases/download/v${{ version }}/copilot-linux-arm64.tar.gz
      sha256: 0911f12dd816f612d27c4a360d4f00b62d933845a98d6c913e8d7400a69c6809

  - if: target_platform == "osx-arm64"
    then:
      url: https://github.com/github/copilot-cli/releases/download/v${{ version }}/copilot-darwin-arm64.tar.gz
      sha256: a5ede0d96dbb6cfff8bed0f6872ac3eb05bf0a4ed342d44a0a6548cb242713c2

  - if: target_platform == "osx-64"
    then:
      url: https://github.com/github/copilot-cli/releases/download/v${{ version }}/copilot-darwin-x64.tar.gz
      sha256: e8078d57accc7eabbb29565a2f4c217723acfb7c7a2563ed6cac41c45eb29acf

  - if: target_platform == "win-64"
    then:
      url: https://github.com/github/copilot-cli/releases/download/v${{ version }}/copilot-win32-x64.zip
      sha256: 18a8d469d30930cb9da5625dfaf3e261f0cd25442bfdb6754382c443bed42643

  - url: https://raw.githubusercontent.com/github/copilot-cli/refs/tags/v${{ version }}/LICENSE.md
    sha256: 1fbd0dcc55c66738b1b591632132c927de20c8443dff1d55b4851e378883e402

build:
  number: 0

requirements:
  build:
    - if: unix
      then:
        - ${{ stdlib('c') }}

tests:
  - if: unix
    then:
      - requirements:
          run:
            - cf-nvidia-tools 1.*
            - ripgrep
        script:
          content: |
            set -euxo pipefail
            copilot --version | rg '${{ version }}'
            export c_stdlib_version='${{ c_stdlib_version }}'
            check-glibc "${PREFIX}"/bin/*
    else:
      - requirements:
          run:
            - ripgrep
        script:
          content: |
            copilot --version | rg "${{ version }}"
            if errorlevel 1 exit /b 1

about:
  homepage: https://github.com/github/copilot-cli
  summary: GitHub Copilot CLI brings the power of Copilot coding agent directly to your terminal.
  license: LicenseRef-GitHub-Copilot-CLI
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - rxm7706
