# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: databricks-sql-connector
  version: "4.4.0"

recipe:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/databricks_sql_connector-${{ version }}.tar.gz
  sha256: 97d20b5e12e4aa50d16fe5fea3914f19319a207f2f4912a395a08497c25cb16f

build:
  number: 0

outputs:
  - package:
      name: ${{ name }}
    build:
      noarch: python
      script:
        - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
    requirements:
      host:
        - python ${{ python_min }}.*
        - pip
        - poetry-core >=1.0.0
      run:
        - python >=${{ python_min }},<4.0.0
        - thrift >=0.24.0,<0.25.0
        # more restrictive constraint for python >=3.13
        - pandas >=2.2.3,<4.0.0
        - lz4 >=4.4.5,<5.0.0
        - requests >=2.18.1,<3.0.0
        - oauthlib >=3.1.0,<4.0.0
        - openpyxl >=3.0.10,<4.0.0
        - urllib3 >=1.26
        - python-dateutil >=2.8.0,<3.0.0
        - pyjwt >=2.0.0,<3.0.0
        - pybreaker >=1.0.0,<2.0.0
    tests:
      - python:
          imports:
            - databricks
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - "*"

  - package:
      name: ${{ name }}-with-pyarrow
    build:
      noarch: python
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }},<4.0.0
        - ${{ pin_subpackage(name, exact=True) }}
        # most restrictive constraint for python >=3.14
        - pyarrow >=22.0.0
    tests:
      - python:
          imports:
            - databricks
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - "*"

about:
  homepage: https://databricks.com
  summary: Databricks SQL Connector for Python
  license: Apache-2.0
  license_file: LICENSE
  documentation: https://docs.databricks.com/dev-tools/python-sql-connector.html
  repository: https://github.com/databricks/databricks-sql-python

extra:
  recipe-maintainers:
    - xylar
