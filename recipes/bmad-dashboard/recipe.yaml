# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "1.2.2.dev0"
  commit: "33b330ef113c62f58a77413a241ca30f4f0b8a99"

package:
  name: bmad-dashboard
  version: ${{ version }}

source:
  url: https://github.com/bmad-code-org/bmad-method-ui/archive/${{ commit }}.tar.gz
  sha256: f3ffbe4fcad86f76a291e56300e47a24ac631daf7933379555f1e92893e1a9ad

build:
  noarch: python
  number: 0
  script: bash ${{ RECIPE_DIR }}/build.sh
  python:
    entry_points:
      - bmad-dashboard-install = bmad_dashboard.installer:main

requirements:
  build:
    - nodejs >=22
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools >=64
    - wheel
  run:
    - python >=${{ python_min }}

tests:
  - python:
      imports:
        - bmad_dashboard
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"
  - script:
      - python -m bmad_dashboard.installer --print-vsix
      - python -m bmad_dashboard.installer --help

about:
  summary: BMAD Dashboard, a VS Code extension for BMad Method V6 projects
  description: |
    BMAD Dashboard is a VS Code extension that monitors BMad Method V6
    workflow artifacts in real time, tracks sprint progress, and
    recommends the next action to take, all from inside the editor.

    This conda package bundles the prebuilt .vsix produced from the
    upstream sources and exposes a `bmad-dashboard-install` command
    that installs the extension into the user's local VS Code (or VS
    Code Insiders, VSCodium, or code-server) using the editor's CLI.

    The package does not require VS Code at install time. Run
    `bmad-dashboard-install` after install to wire the extension into
    your editor, `bmad-dashboard-install --list` to confirm, and
    `bmad-dashboard-install --uninstall` to remove it.
  homepage: https://github.com/bmad-code-org/bmad-method-ui
  repository: https://github.com/bmad-code-org/bmad-method-ui
  documentation: https://github.com/bmad-code-org/bmad-method-ui#readme
  license: MIT
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - killua156
    - mgorny
  upstream:
    type: snapshot
    commit: 33b330ef113c62f58a77413a241ca30f4f0b8a99
    date: 2026-05-23
    note: |
      No upstream git tag exists yet; this recipe pins the GitHub
      tarball by commit SHA. The package version 1.2.2.dev0 reflects
      the version declared in upstream package.json with a PEP 440
      .dev0 suffix to mark this as a pre-release snapshot.

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: bmad-dashboard
  cfe-upstream-registry: github
  cfe-upstream-name: bmad-code-org/bmad-method-ui
  cfe-purls:
    - pkg:conda/bmad-dashboard@${{ version }}?channel=conda-forge
    - "pkg:github/bmad-code-org/bmad-method-ui@33b330ef113c62f58a77413a241ca30f4f0b8a99"
  cfe-upstream-repo: https://github.com/bmad-code-org/bmad-method-ui
  cfe-upstream-homepage: https://github.com/bmad-code-org/bmad-method-ui
  cfe-import-names: [bmad_dashboard]
  cfe-source-kind: github-commit
  cfe-noarch: python
  cfe-pip-check: true
  cfe-on-conda-forge-status: pending-approval-on-conda-forge
  cfe-on-conda-forge-feedstock: none
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-submission-pr: https://github.com/conda-forge/staged-recipes/pull/33513
  cfe-last-checked: "2026-07-12T14:24:56Z"
  cfe-generated-by-version: 8.76.0
  cfe-generated-at-datetime: "2026-07-12T14:24:56Z"
  cfe-local-build-status: success
  cfe-local-build-datetime: "2026-07-12T15:53:18Z"
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
#    # CONSUME-NOT-SUBMIT mirror of conda-forge/staged-recipes PR #33513
#    # (author killua156; external PR — we consume, never compete; G58/db-gpt
#    # precedent). Body kept a faithful verbatim mirror of the PR head
#    # (killua156:add-bmad-dashboard-snapshot); all human comments preserved.
# context:
#    # DELIBERATE DIVERGENCE from the mirrored PR head (operator decision
#    # 2026-07-14): PR #33513 declares python_min "3.9", below the conda-forge
#    # 3.10 floor. That line is dropped here so conda-forge-pinning supplies the
#    # floor (SKILL.md § Python Version Policy item 6 — python_min belongs in
#    # context ONLY to override the default upward).
#    # This is the one point where the faithful-mirror rule is knowingly broken.
#    # Rationale: the repo's test_no_redundant_python_min meta-test fails fast on
#    # any below-floor python_min and refuses allowlist entries by design, so the
#    # mirror could not stay faithful AND keep the suite green. The 3.9 was
#    # inert here anyway — the cf matrix starts at 3.10, so nothing ever built
#    # against it. The real fix still belongs to PR #33513's review; if that PR
#    # lands with a corrected floor, re-mirror and delete this note.
####
