# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

schema_version: 1

context:
  version: "1.5.14"
  # Upstream requires-python is >=3.11,<3.15, so override the global 3.10 floor.

package:
  name: ibm-watsonx-ai
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/i/ibm-watsonx-ai/ibm_watsonx_ai-${{ version }}.tar.gz
    sha256: a756488bd57e87c0fc51be42dcba871143cfe0ac1e805c497c5047e1e4f13e9d
  # The PyPI sdist ships no license file (upstream packaging bug). Pull it from
  # the repo, pinned to a commit on the default branch (the repo publishes no tags).
  - url: https://raw.githubusercontent.com/IBM/watsonx-ai-python-sdk/67d74911f6159d971ef01ebfddb667fdd43b59df/LICENSE
    sha256: 39d8b78315c231c67575129fed8329eb3624be295a8ba807783297406bd79f5d
    file_name: LICENSE

build:
  number: 0
  noarch: python
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
    # Upstream sdist wrongly ships top-level `docs` and `scripts` packages;
    # drop them so they don't pollute site-packages.
    - rm -rf $SP_DIR/docs $SP_DIR/scripts

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }},<3.15
    - requests
    - httpx >=0.27,<0.29
    - urllib3
    # Upstream pins pandas per Python version (>=0.24.2,<2.4.0 below 3.14;
    # >=2.3.3 on 3.14). Merged here for the single noarch build; the range is
    # compatible with both markers.
    - pandas >=0.24.2,<2.4.0
    - certifi
    - lomond
    - tabulate
    - packaging
    - ibm-cos-sdk >=2.12.0,<2.15.0
    - cachetools

tests:
  - python:
      imports:
        - ibm_watsonx_ai
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  homepage: https://github.com/IBM/watsonx-ai-python-sdk
  repository: https://github.com/IBM/watsonx-ai-python-sdk
  documentation: https://ibm.github.io/watsonx-ai-python-sdk
  summary: IBM watsonx.ai API Client
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - killua156

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: ibm-watsonx-ai
  cfe-upstream-registry: pypi
  cfe-upstream-name: ibm-watsonx-ai
  cfe-purls:
    - pkg:conda/ibm-watsonx-ai@${{ version }}?channel=conda-forge
    - pkg:pypi/ibm-watsonx-ai@${{ version }}
  cfe-upstream-repo: https://github.com/IBM/watsonx-ai-python-sdk
  cfe-upstream-homepage: https://ibm.github.io/watsonx-ai-python-sdk
  cfe-import-names: [ibm_watsonx_ai]
  cfe-source-kind: pypi-sdist
  cfe-noarch: python
  cfe-pip-check: true
  cfe-on-conda-forge-status: pending-approval-on-conda-forge
  cfe-submission-pr: https://github.com/conda-forge/staged-recipes/pull/33968
  cfe-on-conda-forge-feedstock: none
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-07-04T22:21:00Z
  cfe-generated-by-version: 8.34.0
  cfe-generated-at-datetime: 2026-06-19T20:35:00Z
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-06-20T01:32:56Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
#    # ibm-watsonx-ai 1.5.13 satisfies lfx-ibm's ibm-watsonx-ai >=1.3.1,<2.0.0.
#    # Direct langflow blocker; needed by lfx-ibm, langflow-base, langchain-ibm.
#    # BSD-3-Clause (OSI-approved) -> cf-eligible. requires-python <3.15,>=3.11
#    # forces python_min 3.11 (declared in context, overriding the 3.10 floor).
# source:
#    # sdist ships NO LICENSE (grayskull License file: []); vendored BSD-3-Clause
#    # LICENSE into recipe dir per SKILL.md License-File Placement pattern (2).
#    # No per-version GitHub tag (v1.5.13/LICENSE -> 404); LICENSE pulled from
#    # IBM/watsonx-ai-python-sdk@main. Notify upstream the sdist omits LICENSE.
# build:
#    # noarch: python; setuptools backend (build-system requires=[setuptools,
#    # build]); dropped redundant python-build/build host dep per G8.
# requirements:
#  run:
#    # pandas: upstream markers are pandas<2.4.0,>=0.24.2 (py<3.14) and
#    # pandas>=2.3.3 (py>=3.14). noarch:python carries one artifact, so per-G35
#    # the per-Python split is collapsed to the intersection >=2.3.3,<2.4.0,
#    # which satisfies BOTH markers (load-bearing under pip_check per G24/G26).
#    # httpx <0.29 and ibm-cos-sdk <2.15.0 upper bounds are upstream-declared;
#    # mirrored in run: (load-bearing under pip_check).
#    # rag/mcp extras (langchain-chroma, langchain-milvus, pymilvus, grpcio,
#    # scikit-learn) NOT activated -> their deps deliberately omitted per G25.
#    # BLOCKER: ibm-cos-sdk meta-package (==ibm-cos-sdk-core 2.16.2 +
#    # ibm-cos-sdk-s3transfer 2.16.2 + jmespath) is not yet on conda-forge nor
#    # in the local channel; only ibm-cos-sdk-core + -s3transfer are built
#    # locally. Authoring ibm-cos-sdk meta-recipe is the prerequisite.
# tests:
#    # grayskull emitted top-level sdist dirs (dist/docs/sample_notebooks/
#    # scripts/supporting_materials) as imports; only ibm_watsonx_ai is a real
#    # module (G7, verified against sdist top-level __init__.py).
####
