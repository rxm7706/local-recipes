# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: pytest-check-meta
  version: "2.9.1"

recipe:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/pytest-check/pytest_check-${{ version }}.tar.gz
  sha256: 2d8c9f1fe5cceb0b41c480e1454e95e9ed6a3a534bf2c7e3e0f39167b11cf88b

build:
  number: 0

outputs:
  - package:
      name: pytest-check
    build:
      noarch: python
      script: ${PYTHON} -m pip install . -vv --no-deps --no-build-isolation
    requirements:
      host:
        - python ${{ python_min }}.*
        - hatchling
        - pip
      run:
        - python >=${{ python_min }}
        - pytest
        - typing-extensions >=4.12.2,<5
    tests:
      - python:
          imports:
            - pytest_check
          pip_check: true
          python_version: ${{ python_min }}.*
      - files:
          source:
            - src/
            - tests/
            - examples/
            - LICENSE.txt
            - pyproject.toml
            - README.md
            - tox.ini
        requirements:
          run:
            - python ${{ python_min }}.*
            - tox
            - tox-uv
        script:
          - tox -e py
    # Meta-package to the main package, that just installs it
    # it is here to keep-up with the legacy name, containing an
    # underscore ("_") instead of a dash.
  - package:
      name: pytest_check
    build:
      noarch: generic
    requirements:
      host:
        - python ${{ python_min }}.*
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage("pytest-check", exact=True) }}
    tests:
      - python:
          imports:
            - pytest_check
          pip_check: true
          python_version: ${{ python_min }}.*


about:
  summary: A pytest plugin that allows multiple failures per test.
  license: MIT
  license_file: LICENSE.txt
  homepage: https://github.com/okken/pytest-check

extra:
  recipe-maintainers:
    - thewchan
    - bskinn
    - anjos
