# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "2.14.3"

package:
  name: ibm-cos-sdk-s3transfer
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/i/ibm-cos-sdk-s3transfer/ibm_cos_sdk_s3transfer-${{ version }}.tar.gz
  sha256: 2251ebfc4a46144401e431f4a5d9f04c262a0d6f95c88a8e71071da056e55f72

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - ibm-cos-sdk-core >=2.14.3

tests:
  - python:
      imports:
        - ibm_s3transfer
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  homepage: https://pypi.org/project/ibm-cos-sdk-s3transfer/
  license: Apache-2.0
  license_file:
    - LICENSE.txt
    - NOTICE.txt
  summary: IBM S3 Transfer Manager
  repository: https://github.com/ibm/ibm-cos-sdk-python-s3transfer
  documentation: https://ibm.github.io/ibm-cos-sdk-python/
  description: |
    The IBM COS SDK S3 transfer manager (``ibm-cos-sdk-s3transfer``) is a
    fork of botocore's ``s3transfer`` that provides high-level, managed
    upload/download utilities for IBM Cloud Object Storage. It builds on
    ``ibm-cos-sdk-core`` (the ``ibm_botocore`` fork) and is consumed by the
    higher-level ``ibm-cos-sdk`` package. The top-level import name is
    ``ibm_s3transfer``.

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: ibm-cos-sdk-s3transfer
  cfe-upstream-registry: pypi
  cfe-upstream-name: ibm-cos-sdk-s3transfer
  cfe-name-aliases:
    - ibm_s3transfer
  cfe-purls:
    - pkg:conda/ibm-cos-sdk-s3transfer@${{ version }}
    - pkg:pypi/ibm-cos-sdk-s3transfer@${{ version }}
  cfe-upstream-repo: https://github.com/ibm/ibm-cos-sdk-python-s3transfer
  cfe-upstream-homepage: https://www.ibm.com/cloud/object-storage
  cfe-on-conda-forge-status: blocked-pending-prerequisites
  cfe-on-conda-forge-feedstock: none
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list:
    - ibm-cos-sdk-core not yet on conda-forge
  cfe-last-checked: 2026-06-19T20:45:00Z
  cfe-generated-by-version: 8.34.0
  cfe-generated-at-datetime: 2026-06-19T20:45:00Z
####
# CFE comments
# requirements:
#  run:
#    # Upstream pins ibm-cos-sdk-core==2.14.3; loosened to >= per the
#    # project pin-loosening convention (the local channel ships 2.14.3).
# tests:
#    # G7 divergence: the PyPI distribution is `ibm-cos-sdk-s3transfer` but the
#    # top-level import is `ibm_s3transfer`. Verified against the sdist
#    # top_level.txt and ibm_s3transfer/__init__.py.
####
