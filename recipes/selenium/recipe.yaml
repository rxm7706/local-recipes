# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "4.47.0"
  selenium_manager_version: ${{ (version | split('.'))[:2] | join('.') }}.*
  python_check_max: "3.14"

package:
  name: selenium
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/s/selenium/selenium-${{ version }}.tar.gz
    sha256: 4f6667c23080646e045fb91d2039687e88f549d667961f6ce85832b17384b68e
    patches:
      - 0000-no-rust.patch

build:
  number: 0
  noarch: python
  script:
    # ensure all static assets are included
    - echo "recursive-include * *.js" >> MANIFEST.in
    - echo "recursive-include * *.json" >> MANIFEST.in
    # ensure all directories have `__init__.py`
    - ${{ PYTHON }} -c "from pathlib import Path; [(p / '__init__.py').touch() for p in Path('selenium').rglob('*') if p.is_dir()]"
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - setuptools
  run:
    - certifi >=2026.2.25
    - python >=${{ python_min }}
    - selenium-manager ${{ selenium_manager_version }}
    - trio >=0.31.0,<1.0
    - trio-websocket >=0.12.2,<1.0
    - typing_extensions >=4.15.0,<5.0
    - urllib3 >=2.6.3,<3.0
    - websocket-client >=1.8.0,<2.0

tests:
  - python:
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - ${{ python_check_max }}.*
      imports:
        - selenium
        - selenium.common
        - selenium.webdriver.common
        - selenium.webdriver.chrome
        - selenium.webdriver.chromium
        - selenium.webdriver.edge
        - selenium.webdriver.firefox
        - selenium.webdriver.remote
        - selenium.webdriver.support
  - requirements:
      run:
        - python ${{ python_min }}.*
    script:
      file: run_test.py

about:
  license_file:
    - LICENSE
    - NOTICE
  license: Apache-2.0
  summary: Official Python bindings for Selenium WebDriver
  homepage: https://www.selenium.dev
  repository: https://github.com/SeleniumHQ/selenium
  documentation: https://www.selenium.dev/documentation/webdriver

extra:
  recipe-maintainers:
    - pmlandwehr
    - ocefpaf
    - bollwyvl

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: selenium
  cfe-upstream-registry: pypi
  cfe-upstream-name: selenium
  cfe-purls:
    - pkg:conda/selenium@${{ version }}?channel=conda-forge
    - pkg:pypi/selenium@${{ version }}
  cfe-upstream-repo: https://github.com/SeleniumHQ/selenium
  cfe-upstream-homepage: https://www.selenium.dev
  cfe-import-names: [selenium, selenium.common, selenium.webdriver.common, selenium.webdriver.chrome, selenium.webdriver.chromium, selenium.webdriver.edge, selenium.webdriver.firefox, selenium.webdriver.remote, selenium.webdriver.support]
  cfe-source-kind: pypi-sdist
  cfe-noarch: python
  cfe-pip-check: "true"
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: https://github.com/conda-forge/selenium-feedstock
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-08-17T02:07:04Z
  cfe-generated-by-version: 8.81.0
  cfe-generated-at-datetime: 2026-08-17T02:07:04Z
  cfe-local-build-status: failed
  cfe-local-build-datetime: 2026-08-17T02:07:04Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
#    # (agent rationale parks here, organized by recipe location — never
#    # inline in the body; see SKILL.md "Never Add AI Comments Inline")
####
