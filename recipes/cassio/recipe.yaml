# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.1.10"

package:
  name: cassio
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/c/cassio/cassio-${{ version }}.tar.gz
  sha256: 577f0a2ce5898a57c83195bf74811dec8794282477eb6fa4debd4ccec6cfab98

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - cassio-create-init-string = cassio.config.bundle_management:create_init_string_utility

requirements:
  host:
    - python ${{ python_min }}.*
    - poetry-core
    - pip
  run:
    - python >=${{ python_min }}
    - cassandra-driver >=3.28.0,<4.0.0
    - requests >=2.31.0,<3.0.0
    - numpy >=1.0

tests:
  # win: cassandra-driver has no event-loop reactor on py3.12+ (libev C-ext is
  # Unix-only; asyncore was removed in 3.12), so importing cassandra.cluster
  # fails there. Test python_min only on win; full triad elsewhere.
  - if: win
    then:
      python:
        imports:
          - cassio
        pip_check: true
        python_version:
          - ${{ python_min }}.*
    else:
      python:
        imports:
          - cassio
        pip_check: true
        python_version:
          - ${{ python_min }}.*
          - "*"
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - cassio-create-init-string

about:
  homepage: https://pypi.org/project/cassio/
  license: Apache-2.0
  license_file: LICENSE
  summary: A framework-agnostic Python library to seamlessly integrate Apache Cassandra(R) with ML/LLM/genAI workloads.
  repository: https://github.com/CassioML/cassio
  description: |
    CassIO is a framework-agnostic Python library that seamlessly integrates
    Apache Cassandra(R) with ML/LLM/genAI workloads. It provides ready-to-use
    abstractions for storing and retrieving data in Cassandra and Astra DB --
    including vector search, key-value stores, chat-history persistence, and
    caching -- so that higher-level frameworks (such as LangChain) can use
    Cassandra as a backing store with minimal boilerplate.

extra:
  recipe-maintainers:
    - rxm7706

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: cassio
  cfe-upstream-registry: pypi
  cfe-upstream-name: cassio
  cfe-purls:
    - pkg:conda/cassio@${{ version }}
    - pkg:pypi/cassio@${{ version }}
  cfe-upstream-repo: https://github.com/CassioML/cassio
  cfe-upstream-homepage: https://cassio.org
  cfe-import-names: [cassio]
  cfe-source-kind: pypi-sdist
  cfe-noarch: python
  cfe-pip-check: true
  cfe-on-conda-forge-status: pending-approval-on-conda-forge
  cfe-submission-pr: https://github.com/conda-forge/staged-recipes/pull/33946
  cfe-on-conda-forge-feedstock: none
  cfe-forge-recipe-updates-needed: none
  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-06-27T11:57:49Z
  cfe-generated-by-version: 8.30.1
  cfe-generated-at-datetime: 2026-06-19T13:36:19Z
  cfe-local-build-status: success
  cfe-local-build-datetime: 2026-06-19T16:45:01Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# tests:
#  script:
#    # The entry point has no argparse; called with no args it prints a
#    # usage line and exits 0. (--help is treated as a bundle path and
#    # crashes, so the no-arg invocation is the correct smoke test.)
####
