# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: psycopg
  version: "2.9.12"

recipe:
  name: psycopg-split
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/p/psycopg/psycopg-${{ version }}.tar.gz
    sha256: e21207764952cff81b6b8bdacad9a3939f2793367fdac2987b3aac36a651b5bc
    target_directory: psycopg
    patches:
      - 0001-remove-tzdata-for-pip-check.patch
  - url: https://pypi.org/packages/source/p/psycopg-c/psycopg_c-${{ version }}.tar.gz
    sha256: ed8106128b2d04359c185fc9641b4409abfce4d0b6fb1d1ff6800646e27f1a22
    target_directory: psycopg-c

build:
  number: 0

requirements:
  host:
    - python
  run:
    - python

outputs:
  - package:
      name: psycopg-c
    build:
      script: cd psycopg-c && ${{ PYTHON }} -m pip install . -vv  --no-deps --no-build-isolation
    requirements:
      build:
        - ${{ compiler('c') }}
        - ${{ stdlib("c") }}
        - if: build_platform != target_platform
          then: python
        - if: build_platform != target_platform
          then: cross-python_${{ target_platform }}
        - if: build_platform != target_platform
          then: postgresql
      host:
        - python
        - pip
        - libpq
        # conda-forge didn't build 80.3.1, so pin the lowest available higher version
        - setuptools
      run:
        - libpq
        - python
    tests:
      - requirements:
          run:
            - pip
        script:
          - pip check
  - package:
      name: psycopg
    build:
      # NOTE: must be kept equal to python_min
      skip: not linux64 or match(python, "!=3.10")
      noarch: python
      script: cd psycopg && ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
    requirements:
      host:
        - python ${{ python_min }}
        - pip
        - setuptools >=80.3.1
        - libpq
      run:
        - python >=${{ python_min }}
        - ${{ pin_subpackage('psycopg-c', upper_bound='x.x.x.x.x.x') }}
        # for python <3.13 but keeping it for all to stay noarch: python
        - typing-extensions >=4.6
        - libpq
        # skipping tzdata based on https://github.com/conda-forge/psycopg2-feedstock/pull/120
        # so we don't have to build separately for windows
    tests:
      - python:
          imports:
            - psycopg
            - psycopg.pq
            - psycopg_c
          pip_check: true
          python_version: ${{ python_min }}

about:
  summary: PostgreSQL database adapter for Python
  description: |
    Psycopg 3 is a newly designed PostgreSQL database adapter for the Python
    programming language. Psycopg 3 presents a familiar interface for everyone
    who has used Psycopg 2 or any other DB-API 2.0 database adapter, but allows
    to use more modern PostgreSQL and Python features.
  license: LGPL-3.0-only
  license_file: psycopg/LICENSE.txt
  homepage: https://psycopg.org/psycopg3/
  repository: https://github.com/psycopg/psycopg/
  documentation: https://www.psycopg.org/psycopg3/docs/

extra:
  recipe-maintainers:
    - msarahan
    - ocefpaf
    - xylar

#### CFE metadata AND comments
# CFE metadata
  cfe-conda-name: psycopg2
  cfe-upstream-registry: pypi
  cfe-upstream-name: psycopg2
  cfe-purls:
    - pkg:conda/psycopg2@${{ version }}?channel=conda-forge
    - pkg:pypi/psycopg2@${{ version }}
  cfe-upstream-repo: https://github.com/psycopg/psycopg/
  cfe-upstream-homepage: https://psycopg.org/psycopg3/
  cfe-import-names: []
  cfe-source-kind: pypi-sdist
  cfe-noarch: python
  cfe-pip-check: "true"
  cfe-on-conda-forge-status: confirmed-on-conda-forge
  cfe-on-conda-forge-feedstock: https://github.com/conda-forge/psycopg2-feedstock
  cfe-forge-recipe-updates-needed: 
    - meta-yaml-to-recipe-yaml
    - version-update-to-2.9.12

  cfe-forge-blocker-list: []
  cfe-last-checked: 2026-08-18T03:20:21Z
  cfe-generated-by-version: 8.81.0
  cfe-generated-at-datetime: 2026-08-18T03:20:21Z
  cfe-local-build-status: failed
  cfe-local-build-datetime: 2026-08-18T03:20:21Z
  cfe-local-build-platform: linux-64
  cfe-local-build-tool: rattler-build
####
# CFE comments
# Header:
####
