{% set name = "ag-ui-protocol" %}
{% set version = "0.1.20" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/ag_ui_protocol-{{ version }}.tar.gz
  sha256: fadc9600f492a9dde8b2b027171bb471a899430e53291a0b882a09d3f5416145

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - uv-build >=0.8.0,<0.9
    - pip
  run:
    - python >={{ python_min }}
    - pydantic >=2.11.2

test:
  imports:
    - ag_ui.core
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  summary: 'AG-UI: the Agent-User Interaction Protocol. Bring Agents into Frontend Applications.'
  license: MIT
  license_file: LICENSE
  home: https://github.com/ag-ui-protocol/ag-ui

extra:
  recipe-maintainers:
    - rxm7706
