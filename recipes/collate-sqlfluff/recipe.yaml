# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: collate-sqlfluff
  version: "3.3.5"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/collate_sqlfluff-${{ version }}.tar.gz
  sha256: 3b58bb14b98c72e84321f43de0356de746d27512c62d7a26ad3d8f5f5add1d3c

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - sqlfluff = sqlfluff.cli.commands:cli

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=40.8.0
    - wheel
    - pip
  run:
    - python >=${{ python_min }},<4.0
    - platformdirs
    - chardet
    - click
    - colorama >=0.3
    - diff-cover >=2.5.0
    - jinja2
    - pathspec
    - pytest
    - pyyaml >=5.1
    - regex
    - tblib
    - toml
    - tqdm

tests:
  - python:
      imports:
        - sqlfluff
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  homepage: https://github.com/sqlfluff/sqlfluff
  summary: The SQL Linter for Humans
  license: MIT
  license_file: LICENSE.md
  repository: https://github.com/sqlfluff/sqlfluff

extra:
  recipe-maintainers:
    - rxm7706
