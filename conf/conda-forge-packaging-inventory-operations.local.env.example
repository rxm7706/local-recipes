# Copy to conda-forge-packaging-inventory-operations.local.env (gitignored).
# The identity gist is a GitHub secret (unlisted) gist. Anyone with the id
# can read it, so the id must never be committed.
OPENTEAMS_IDENTITY_GIST_ID=
