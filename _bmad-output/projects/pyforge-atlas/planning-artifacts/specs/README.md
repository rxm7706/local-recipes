# pyforge-atlas — story specs (tracked, durable)

Same spec-durability remediation as pyforge-warden (see the repo-wide convention in
`CLAUDE.md` § *Spec-driven layout* → "Story specs are durable (tracked), NOT Tier-3").
Story specs are the per-story intent contract; they must survive worktree teardown and
live in every clone. This directory (`planning-artifacts/specs/`, Tier-2, **git-tracked**)
is that durable home. The pre-existing `spec-pyforge-atlas/` subdir is the Dream-level
bmad-spec **kernel** (SPEC.md + memlog); the `spec-<story>.md` files here are the per-story
specs.

## Provenance of the recovered set (2026-07-25)

Atlas was **more degraded than warden**: by 2026-07-25 only **2 of 32 story specs survived
intact** (0.1, B1); the rest were 0-byte husks or gone entirely, and — unlike warden — **no
run-worktree snapshots survived** to recover originals from. The compounding cause was the
2026-07-19 truncation incident (reconciled 2026-07-23) on top of the Tier-3 gitignored
paper-trail gap. What saved the contract: the tracked `epics.md` (72 KB) retained every
story's Intent + Acceptance Criteria.

| Provenance | Count | What it is |
|---|---|---|
| **original + full dev/review narrative** (recovered from the consolidated dump) | 12 | the REAL original spec incl. `## Dev Notes` / `## Dev Agent Record` / `## Review Triage Log` — Wave 0 + A + B1–B8 (0.1, A1, A2, A3, B1–B8); header `<!-- RECOVERED … ATLASBMADSPECSCONSOLIDATED.md -->` |
| **regenerated contract-spec** | 20 | authoritative Intent + ACs from `epics.md`; no full spec available — B9, B10, C1–C2, D1–D3, E1–E2, F1–F4, G1–G3, H1–H4; marked `status: regenerated` |

**Update 2026-07-25 (major recovery).** The transcript path was empty for atlas, but the
operator provided **`ATLASBMADSPECSCONSOLIDATED.md`** — a 982 KB consolidated dump generated by
`claude.ai/code` session `01FYyQvBJuXwySiaMUUYCqBZ` that embeds the **full original per-story
specs** for Waves 0/A/B1–B8. Those 12 were extracted verbatim — **including their dev/review
narrative**, which the local sources could not reach. The remaining **20** stories (B9/B10 +
Waves C–H) are not in the dump at full-spec level, so they stay as `epics.md` contract-specs
(their dev/review narrative remains unrecovered). Contract intact for all 32; migration COMPLETE
(32/32, PRs #58–#105).

> Source file: `src/shared/packages/pyforge-atlas/src/pyforge/atlas/ATLASBMADSPECSCONSOLIDATED.md`
> (also holds the Tier-1 intake spec, architecture, FRs, and the deferred-work ledger). Consider
> promoting it to a tracked planning-artifacts location — it is the richest single atlas artifact.

## Recovery attempts (2026-07-25) — all sources exhausted

Unlike warden — where 13 lost specs were recovered verbatim from the `Write`/`Edit` tool-calls in
the local Claude Code session transcripts (`~/.claude/projects/**/*.jsonl`) — **atlas's dev-session
transcripts are NOT in the local store** (a full scan found no atlas per-story spec `Write` calls;
even `B1`, intact on disk, has no transcript write). The atlas dev sessions ran on `claude.ai/code`
(web), so their history lives server-side, not locally. Sources tried, in order:

0. **Operator-provided consolidated dump** (`_recovery-source-ATLASBMADSPECSCONSOLIDATED.md`,
   session `01FYyQvBJuXwySiaMUUYCqBZ`) — **SUCCESS for 12**: the only source anywhere that carries
   the full original dev specs (incl. dev/review narrative) for Waves 0/A/B1–B8. This is what the
   web session could reach from its own context and the operator saved to disk.
1. **Local session transcripts** — atlas spec writes absent from `~/.claude/projects/`.
2. **Run worktree snapshots** — none survived (atlas run dirs gone; the 2026-07-19 truncation).
3. **`epics.md` regeneration** — the fallback used for the remaining 20 (Intent + ACs only).
4. **Two further web-session branches** (`claude/cfe-atlas-kedro-migration-5uc550`,
   `claude/refine-local-plan-n8n0gb`) produced single-file **compendiums** of the atlas contract —
   useful planning records, but each renders the 20 as `regenerated` contract-level (one even
   embeds this repo's own regen frontmatter). They add **no** per-story dev-spec fidelity.

**Conclusion:** the ceiling is **12 full dev specs + 20 epics.md contract-specs**. The 20 Kedro
stories' (B9/B10 + C–H) original dev/review narrative is unrecoverable — no accessible source holds
it. If those web sessions' server-side transcripts are ever exported, that is the only remaining
place the 20 originals could still live.
