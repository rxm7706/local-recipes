---
topic: artifactory-download-intelligence (genesis baseline bootstrapped 2026-08-22, marshal Story 25.3)
updated: 2026-09-09T16:10
---

- (event) Genesis baseline 2026-08-22 (marshal Story 25.3, spec-bmad-611-era-alignment CAP-3): .memlog.md bootstrapped so 6.11 bmad-spec updates succeed against this folder — a structural bootstrap only, per the S-13.7 pattern; no governed-file content is claimed reconciled by this entry
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (decision by claude) Excel governance rendering of the joined data DECLINED PERMANENTLY and recorded as a Non-goal; the open question closes (operator 2026-09-09, batch § 2.1 row atlas-B3). This Spec's own § Non-goals already says 'Not an Excel governance view'; atlas Stories 23-8 (workbook-free metrics universe) and 23-9 (quartet workbook retirement) are both `done`, and `scripts/tests/test_quartet_no_xlsx_surface.py` asserts zero workbook surface across the quartet actuators — keeping the question open would contradict a passing test.
- (decision by claude) Second declared open question ('which live Artifactory instance, and when its attended bring-up happens') is NOT a gap and should leave `open_questions:`: it restates this Spec's own binding Non-goal — 'Not live-instance bring-up, instance selection, or any network call to a real Artifactory'. Record it as a deferred operator event, not an unanswered question (readiness Table B row 3 / Section C-5).
- (decision by claude) Status recommendation `ready -> shipped`: Stories 15-1/15-2/15-3 are all `done`, `artifactory/aql_adapter.py`'s `_unconfigured_transport` refuses exactly as CAP-1 requires, and the catalog carries `artifactory_downloads_raw`/`_joined`; this Spec's own success signal is explicitly the mock-verified run ('No live Artifactory instance is required, named, or contacted anywhere in the verification'), and that signal is met (readiness Table A row 3, finding D-4). CAVEAT for re-derivation: this flip is a readiness-report Table-A item that follows from atlas-B3's approval but is NOT separately enumerated in the batch § 2.2 Class-B list — confirm before applying.
- (note by claude) SCOPE GUARD for the re-derive (2026-09-09, coordinator follow-up): the `ready -> shipped` entry above is a **RECOMMENDATION, not applied this pass**. `SPEC.md` still reads `status: ready` and must KEEP reading `ready` until the operator enumerates the flip explicitly — it was carried by the readiness report's Table A / finding D-4 but is NOT one of the rows the 2026-09-09 decision batch approved in § 2.2 Class B (only atlas-B3, the Excel closure, is approved for this Spec). Do not let a `bmad-spec` update read the recommendation as an instruction. The two open-question closures (atlas-B3 Excel declined; the live-instance question restated as this Spec's own Non-goal) ARE approved and should be applied.
- (event) Self-validate Pass 1 (coherence) PASS on the 2026-09-09 re-derive: CAP-1..CAP-4 unchanged with intent+success and stable ids; the Excel decision moved from Open Questions into Non-goals where it bends design; open_questions now empty
- (event) Self-validate Pass 2 (preservation) PASS: both question closures landed (Excel declined permanently with its test evidence; live-instance restated as this Spec's own Non-goal) and the SCOPE GUARD is honoured -- status still reads 'ready', with the unapplied 'ready -> shipped' recommendation recorded as an Assumption plus a frontmatter comment so a later derive cannot mistake it for an instruction
