---
title: 'Story 20.4: The CIS two-spine specs exist (CAP-7)'
type: 'feature'
created: '2026-08-27'
status: 'done'
updated: '2026-08-28'
baseline_revision: '0d34c27d794c09a2b7f8c3508b02d990740f6a16'
review_loop_iteration: 0
followup_review_recommended: true
context:
  - _bmad-output/projects/pyforge-atlas/planning-artifacts/epics.md
  - _bmad-output/projects/pyforge-atlas/planning-artifacts/deferred-work-ledger.md
  - _bmad-output/projects/pyforge-atlas/spec-archive/ATLAS-BMAD-SPECS-CONSOLIDATED.md
  - recipes/bmad-creative-intelligence-suite/recipe.yaml
warnings: []
deferred:
  - summary: >-
      epics.md still shows Story 20.4 (and 20.5) as "Status: backlog" even though the
      spec/ledger have progressed past that -- the same known epics.md-staleness pattern
      recorded by Stories 20.2/20.3's own specs.
    evidence: |-
      epics.md's own convention (per Stories 20.2/20.3) is that per-story "Status:" lines
      are regenerated by bmad-create-epics-and-stories, not hand-edited by a single-story
      dispatch; this story follows the same precedent rather than hand-patching epics.md.
    location: _bmad-output/projects/pyforge-atlas/planning-artifacts/epics.md:1821,1830
    severity: low
  - summary: >-
      The "19 unshipped pages" enumeration required deriving which of the 28 legacy CLI
      questions are still unaddressed and reconciling that against the literal epics.md
      arithmetic (28 minus PAGE_INVENTORY's length of 9) -- the two numbers do not trivially
      agree because 2 of PAGE_INVENTORY's 9 entries (estate-cache, factory-status) are not
      CLI ports, leaving 21 genuinely-unaddressed CLI questions, not 19. DESIGN.md documents
      the derivation and the one consolidation (platform-breakdown + pyver-breakdown +
      channel-split merged into one distribution-breakdown page) used to land on exactly 19
      pages covering all 21 questions. This is a design judgment call, not a discovered
      defect, but Story 20.5's implementer should re-verify the mapping before treating "19"
      as a fixed, non-negotiable page list.
    evidence: |-
      See DESIGN.md § 0 and § 6 (page-count reconciliation table) for the full derivation
      and citations (canonical script list, mcp-tools.md, atlas-phases-overview.md § 3.3).
    location: >-
      _bmad-output/projects/pyforge-atlas/planning-artifacts/DESIGN.md#0-how-carson-and-maya-derived-19
    severity: medium
  - summary: >-
      The vendored `bmad-cis-design-thinking/template.md` (copied into `.claude/skills/` by
      `bmad-cis-install`, part of the `bmad-creative-intelligence-suite` v0.3.1 conda package)
      renders a `{{project_name}}` title placeholder that no step of the skill's own SKILL.md
      ever resolves -- Step 4 "Load Config" only resolves `output_folder`/`user_name`/
      `communication_language`/`date`, unlike the sibling CIS workflows (`innovation-strategy`'s
      `{{company_name}}`, `problem-solving`'s `{{problem_title}}`) which are properly produced
      by their own Step 1. A future default-path run of this workflow would show a literal
      unfilled `{{project_name}}` in its output title. Not this story's problem to fix (it is
      pre-existing content inside the upstream package payload, not authored here) and not
      exercised by this story's own artifact, which supplies the title manually rather than
      going through the unresolved placeholder.
    evidence: |-
      `.claude/skills/bmad-cis-design-thinking/template.md` line 1 (`{{project_name}}`) against
      `.claude/skills/bmad-cis-design-thinking/SKILL.md` Step 4 "Load Config", which never
      resolves that key; confirmed the skill files are byte-identical to
      `.pixi/envs/local-recipes/share/bmad-creative-intelligence-suite/skills/bmad-cis-design-thinking/`,
      i.e. the bug is upstream in the conda package, not introduced by this story's install step.
    location: .claude/skills/bmad-cis-design-thinking/template.md:1
    severity: low
  - summary: >-
      No automated check in this repo verifies that a `sprint-status-ledger.yaml` `done` value
      or a `deferred-work-ledger.md` `resolution:` closure claim is actually backed by the
      artifact it cites -- a future single-story dispatch could mark a planning story "done"
      and close its DW entry on a false self-report (e.g. claiming N pages covered when fewer
      actually landed) and nothing in `pyforge-doctor`'s ledger source or `fleet_scan.py`'s
      `scan_deferred`/`parse_sprint_status` would catch it; both only check parsing mechanics
      and `TERMINAL`-status regression (done-to-not-done), never a not-done-to-done claim's
      truth. Pre-existing gap, not introduced by this story -- this instance's own claim (19
      pages, DW-D2-1 closure) was independently re-counted and confirmed accurate during this
      story's review pass.
    evidence: |-
      `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/ledger.py::gather` (lines
      149-261) only flags a key that was `TERMINAL` at baseline and drops out of `TERMINAL` at
      head; a `backlog -> done` transition never enters that check. `scripts/fleet_scan.py`'s
      `scan_deferred` only requires a `resolution:`/`verified:` line to exist to count an entry
      `triaged`/`closed` -- it never greps the cited artifact paths for matching content.
    location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/ledger.py:149
    severity: medium
---

<intent-contract>

## Intent

**Problem:** DW-D2-1 has recorded, since Story D2 shipped, that the full 28-page Vizro
inventory is blocked on the "CIS two-spine specs" (`DESIGN.md` + `EXPERIENCE.md`) — output of
the **Creative Intelligence Suite (CIS)**, a standalone BMad module whose Carson (Brainstorming
Coach) and Maya (Design Thinking Maestro) personas were named as the intended producers (original
spec § 2.4). As of the 2026-08-27 `epics.md` check, that precondition STILL blocks: the spine
files were never produced, and there has been no evidence-update since the 2026-07-30
verification.

**Approach:** run the CIS Carson/Maya planning pass to produce both spine files, covering every
one of the 19 unshipped Vizro pages, under `planning-artifacts/`, then close DW-D2-1 citing this
story. Concretely: the CIS module IS already conda-packaged and installed in the `local-recipes`
pixi environment (`recipes/bmad-creative-intelligence-suite`, v0.3.1 — six agents including
`bmad-cis-agent-brainstorming-coach` (Carson) and `bmad-cis-agent-design-thinking-coach` (Maya),
plus the `bmad-cis-design-thinking` workflow) — but its skills have NOT yet been copied into
`.claude/skills/` (confirmed: no `bmad-cis-*` skill is discoverable today). The package's own
`about.description` states the precondition plainly: "After installation, run `bmad-cis-install`
in your project directory to copy the skills into `.claude/skills/` so Claude Code can discover
them." This story's first real action is therefore running `bmad-cis-install`, THEN invoking the
design-thinking workflow (Carson for divergent ideation on the 19 pages, Maya for the
human-centered design pass) to actually author `DESIGN.md` + `EXPERIENCE.md`. The workflow's own
default output filename (`design-thinking-{date}.md`) is generic — this story must land the two
spine artifacts under the project's own established `DESIGN.md`/`EXPERIENCE.md` naming (per every
prior reference to them in `epics.md`/`deferred-work-ledger.md`/the original spec § 2.4), not the
workflow's raw default output name.

## Acceptance Criteria

Lifted verbatim from `epics.md` (Story 20.4):

> **Given** DW-D2-1 — checked 2026-08-27: the CIS DESIGN/EXPERIENCE gap STILL BLOCKS (the
> `DESIGN.md` + `EXPERIENCE.md` spine specs were never produced; no evidence-update since the
> 2026-07-30 verification) **When** the CIS Carson/Maya planning pass runs **Then** both spine
> files land under `planning-artifacts/` covering every one of the 19 unshipped pages **And**
> DW-D2-1's close cites them **And** until this story lands, S-20.5 must not expand the page set
> past the live-confirmed core.

## Boundaries & Constraints

**Always:** `BMAD_ACTIVE_PROJECT=pyforge-atlas`; ledger key
`20-4-the-cis-two-spine-specs-exist`; land both spine files under
`_bmad-output/projects/pyforge-atlas/planning-artifacts/` by literal physical path — never via
`scripts/bmad-switch` or the `_bmad-output/planning-artifacts` symlink; this is a
PLANNING/documentation story — its deliverable is the two spine files, not application code;
cover every one of the 19 unshipped pages (28-page target minus the 9 already in
`dashboard/app.py::PAGE_INVENTORY`) — a partial spine that skips pages does not satisfy the AC.

**Block If:** `bmad-cis-install` (or the CIS skills it copies into `.claude/skills/`) is not
available/does not run cleanly in this environment — report and stop; do not hand-author
`DESIGN.md`/`EXPERIENCE.md` as plain prose bypassing the CIS personas the spec (§ 2.4) and this
epic explicitly name as the intended producers. **This spec is minted `status: ready` for
dispatch, but the operator has not yet confirmed `bmad-cis-install` succeeds in this repo's
current session — verify that first; if it fails, the correct outcome is a documented BLOCKED
finding against this story, not an improvised substitute.**

**Never:** expand `dashboard/app.py::PAGE_INVENTORY` past its current 9 live-confirmed pages in
this story (that is Story 20.5's job, and it is explicitly gated on this story's completion);
implement any of the 19 pages themselves; touch the query-plane code from Stories 20.1–20.3.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| HAPPY_PATH | `bmad-cis-install` succeeds; Carson + Maya run the design-thinking workflow over all 19 unshipped pages | `DESIGN.md` + `EXPERIENCE.md` land under `planning-artifacts/`, covering all 19 pages | none |
| CIS_INSTALL_FAILS | `bmad-cis-install` errors or the CIS skills fail to register in `.claude/skills/` | story reports BLOCKED, cites the failure; does not hand-author a substitute | no spine files fabricated without the named personas |
| PARTIAL_PAGE_COVERAGE | the planning pass covers fewer than 19 pages | AC not satisfied — story is not done | do not close DW-D2-1 on partial coverage |
| STALE_ALREADY | DW-D2-1 is re-checked and still shows no evidence-update | story confirms the block is unchanged, proceeds to run the pass anyway (that is this story's entire job) | matches the epics.md framing exactly |

</intent-contract>

## Code Map

This is a planning/documentation story — there is no application code to touch. The relevant
map is of the CIS tooling and the deliverable's landing location:

- `recipes/bmad-creative-intelligence-suite/recipe.yaml` — the conda-forge recipe for the CIS
  module (v0.3.1), already built and installed in the `local-recipes` pixi environment
  (`.pixi/envs/local-recipes/share/bmad-creative-intelligence-suite/`). Its `tests.script`
  section proves `bmad-cis-install --help` and the `skills/` tree are present in that env.
- `.pixi/envs/local-recipes/share/bmad-creative-intelligence-suite/module.yaml` and
  `skills/bmad-cis-agent-brainstorming-coach/SKILL.md` (Carson) +
  `skills/bmad-cis-agent-design-thinking-coach/SKILL.md` (Maya) + `skills/bmad-cis-design-thinking/`
  (the workflow, `default_output_file = "{output_folder}/design-thinking-{date}.md"`) — the
  installed-but-not-yet-copied-into-`.claude/skills/` CIS assets this story activates via
  `bmad-cis-install`.
- `_bmad-output/projects/pyforge-atlas/planning-artifacts/` — the landing directory for the two
  spine files (`DESIGN.md` + `EXPERIENCE.md`), matching the physical-path convention every other
  Epic 20 spec in this same directory uses.
- `src/shared/packages/pyforge-atlas/src/pyforge/atlas/dashboard/app.py::PAGE_INVENTORY` (lines
  63-109) — the CURRENT 9-page live-confirmed inventory the two spine files must cover the
  REMAINING 19 pages beyond (28-page target per `epics.md`/the original spec's FR-9). Read-only
  reference for this story — the actual page-set expansion is Story 20.5's job.
- `_bmad-output/projects/pyforge-atlas/planning-artifacts/deferred-work-ledger.md` `DW-D2-1`
  (line 351) — the entry this story closes, citing the two new spine files, per `epics.md`'s own
  instruction.
- `_bmad-output/projects/pyforge-atlas/spec-archive/ATLAS-BMAD-SPECS-CONSOLIDATED.md:166-168` —
  the original § 2.4 definition of CIS and its Carson/Maya personas, for full context on why
  those two specific agents (not a generic UX pass) are named.

## Review Triage Log

### 2026-08-28 — Review pass

- intent_gap: 0
- bad_spec: 0
- patch: 16 (high 2, medium 11, low 3)
- defer: 2 (medium 1, low 1)
- reject: 0
- addressed_findings:
  - `[high]` `[patch]` Intent-alignment audit found the diff's `DESIGN.md`/`EXPERIENCE.md`
    content was actually produced via the generic `bmad-brainstorming` skill (matching that
    skill's `brain-methods.csv` technique taxonomy, not CIS's own EMPATHIZE/DEFINE/IDEATE/
    PROTOTYPE/TEST fields), with no CIS `bmad-cis-design-thinking` template artifact anywhere in
    the diff — a direct violation of this spec's Block-If clause against bypassing the named CIS
    personas. Fixed by genuinely invoking Maya (`bmad-cis-agent-design-thinking-coach`) and
    running her full `bmad-cis-design-thinking` workflow end to end, producing
    `design-thinking-atlas-19-pages-2026-08-28.md`, then re-deriving `DESIGN.md`/`EXPERIENCE.md`
    from it with corrected provenance headers.
  - `[high]` `[patch]` Blind Hunter: `report-artifact` `kind` was given two incompatible
    meanings — the 3 FR-9 cached-only exceptions vs. `scan-project`/`env-inspect`'s live
    per-invocation scans — leaving Story 20.5 no way to tell which behavior to build. Fixed by
    splitting into two `kind` values (`report-artifact` / `live-scan-artifact`) in `DESIGN.md`
    § 1 and reassigning `scan-project`/`env-inspect` accordingly.
  - `[medium]` `[patch]` Blind Hunter: `DESIGN.md`'s FR-9-exception cross-references pointed at
    the wrong sections (§ 4.11–4.13 instead of § 4.4–4.6, three occurrences). Corrected.
  - `[medium]` `[patch]` Blind Hunter: `DESIGN.md` § 4.3 pointed at the wrong `EXPERIENCE.md`
    section for the `universe-sbom` paging rationale (§ 4.3 instead of § 2.3). Corrected.
  - `[medium]` `[patch]` Blind Hunter: the 4 seed-gap-suggester pages in `DESIGN.md` § 5 omitted
    the `Ports` field required by the document's own § 2 template. Added to all four.
  - `[medium]` `[patch]` Blind Hunter: two brainstorm-adopted directions (`cve-watcher` →
    `library-futures` cross-link; `add-handoff`'s claim-marker forward-looking note) were never
    folded into the spine docs despite `brainstorm-intent.md` stating they would be. Added to
    both `DESIGN.md` and `EXPERIENCE.md`, both directions.
  - `[medium]` `[patch]` Blind Hunter: the non-authoritative-badge rule was adopted but never
    stated as a single cross-cutting rule, leaving several pages with no stated badge treatment.
    Added one cross-cutting rule to `DESIGN.md` § 1 and `EXPERIENCE.md` § 0, naming all six
    pages it applies to.
  - `[medium]` `[patch]` Blind Hunter: `deferred-work-ledger.md`'s DW-D2-1 closure understated
    what `bmad-cis-install` actually added (said only Carson+Maya; the package installs a full
    6-persona/4-workflow suite). Corrected to disclose full scope and cite the genuine Maya
    workflow-run artifact.
  - `[low]` `[patch]` Blind Hunter / Edge Case Hunter: `sprint-status-ledger.yaml` marked
    `20-4-…: done` while the spec's own `status` was still `in-review`. Resolves as part of this
    pass's Finalize step (spec `status` → `done`), bringing both back into agreement.
  - `[medium]` `[patch]` Blind Hunter: the spec's own already-recorded deferred item (19-vs-21
    page-mapping re-verification) had no cross-reference from DW-D2-1's closure text, so Story
    20.5 could easily miss it. Added an explicit pointer in the ledger's resolution.
  - `[medium]` `[patch]` Blind Hunter: `no-bsl-shell → bsl-shell` annotations read as an
    ambiguous fifth `kind` value. Clarified in `DESIGN.md` § 1 as an explicit lifecycle
    annotation, not a literal value.
  - `[low]` `[patch]` Blind Hunter: the `distribution-breakdown` page's adopted discoverability
    fix (a one-time onboarding tooltip for `pyver-breakdown`'s policy-check mode) was only
    partially carried through as a facet-visibility rule. Added the explicit tooltip to
    `EXPERIENCE.md` § 1.8.
  - `[medium]` `[patch]` Edge Case Hunter: `adoption-stage`'s "too few packages" fallback
    covered 1–2 packages but not 0. Extended to explicitly cover 0.
  - `[medium]` `[patch]` Edge Case Hunter: `scan-project`/`env-inspect` specified only two states
    (no-scan-yet / results-available), missing a failed/invalid-run state. Added a third state
    to both pages in `DESIGN.md` and `EXPERIENCE.md`.
  - `[low]` `[patch]` Edge Case Hunter: `find-alternative`'s "zero candidates" state didn't
    distinguish a recognized-but-orphaned package from an unrecognized package name. Split into
    two distinct edge cases.
  - `[medium]` `[patch]` Edge Case Hunter: `add-handoff` lacked the "no cached run yet" state its
    sibling FR-9 pages (`inventory-match`, `library-futures`) both specify, conflating it with
    "empty worklist, caught up." Added the missing state.
  - `[low]` `[defer]` Blind Hunter: the vendored `bmad-cis-design-thinking/template.md`
    (upstream package content, not authored by this story) renders an unresolved
    `{{project_name}}` placeholder with no SKILL.md step that ever fills it. Pre-existing
    upstream-package defect, not exercised by this story's own artifact. Recorded in frontmatter
    `deferred`.
  - `[medium]` `[defer]` Verification Gap Reviewer: no automated check in this repo verifies a
    ledger `done`/`resolution:` self-report against the artifact it cites — a structural gap in
    `pyforge-doctor`'s ledger source and `fleet_scan.py`, pre-existing and not caused by this
    story (this story's own claim was independently re-counted and confirmed accurate).
    Recorded in frontmatter `deferred`.

## Auto Run Result

**Summary of implemented change.** Ran `bmad-cis-install` (already applied by the preceding
session; confirmed byte-identical to the conda package payload during review), then genuinely
invoked Carson (`bmad-cis-agent-brainstorming-coach`, divergent EMPATHIZE/IDEATE) and Maya
(`bmad-cis-agent-design-thinking-coach`, full `bmad-cis-design-thinking` workflow EMPATHIZE
through TEST) to author the CIS two-spine specs. A first review pass found the previously-staged
`DESIGN.md`/`EXPERIENCE.md` had actually been produced via the generic `bmad-brainstorming`
skill rather than the CIS-named workflow (a direct Block-If violation); this pass fixed that by
running Maya's workflow for real and re-deriving both spine files from its output, while also
patching 15 other findings (cross-references, a `kind`-taxonomy ambiguity, missing fields,
un-generalized rules, missing edge-case states, and ledger-accuracy issues).

**Files changed:**
- `_bmad-output/projects/pyforge-atlas/planning-artifacts/design-thinking-atlas-19-pages-2026-08-28.md` (NEW) — Maya's full `bmad-cis-design-thinking` workflow run, the two spine files' authoring source.
- `_bmad-output/projects/pyforge-atlas/planning-artifacts/DESIGN.md` — corrected provenance header, split `report-artifact`/`live-scan-artifact` `kind`s, fixed 2 wrong cross-references, added `Ports` to 4 seed-gap pages, added a cross-cutting non-authoritative-badge rule, added `cve-watcher`↔`library-futures` cross-links, added `add-handoff`'s claim-marker note, clarified the `kind`-transition-arrow convention.
- `_bmad-output/projects/pyforge-atlas/planning-artifacts/EXPERIENCE.md` — corrected provenance header, added the same cross-cutting badge rule + `cve-watcher`↔`library-futures` nav rule, added failed/invalid-run states to `scan-project`/`env-inspect`, added `add-handoff`'s no-cached-run-yet state + claim-marker note, extended `adoption-stage`'s 0-package fallback, split `find-alternative`'s unknown-vs-no-alternative edge case, added `distribution-breakdown`'s onboarding tooltip, corrected the TEST-phase framing (persona walkthrough, not "out of scope").
- `_bmad-output/projects/pyforge-atlas/planning-artifacts/deferred-work-ledger.md` — corrected DW-D2-1's closure to disclose the full CIS install scope, cite the genuine workflow-run artifact, and cross-reference the 19-vs-21 re-verification deferred item for Story 20.5.
- `_bmad-output/projects/pyforge-atlas/planning-artifacts/specs/spec-20-4-the-cis-two-spine-specs-exist.md` — this file: 2 new deferred items, this Review Triage Log, this Auto Run Result.
- (Prior session's WIP commit, preserved as this run's baseline: `.claude/skills/bmad-cis-*` — the 10 CIS skill directories copied by `bmad-cis-install`; `_bmad-output/brainstorming/brainstorm-atlas-19-page-spine-2026-08-28/` — Carson's divergent-pass record; `sprint-status-ledger.yaml` — `20-4-…: done`.)

**Review findings breakdown:** 16 patched (2 high, 11 medium, 3 low), 2 deferred (1 medium, 1
low), 0 rejected, 0 intent_gap, 0 bad_spec.

**Follow-up review recommendation:** `true` — this pass's patched findings included 2 `high`
severity (the provenance violation and the `kind`-taxonomy ambiguity), which alone crosses the
threshold regardless of the medium/low count (11 medium + 3 low would independently score
3×11 + 1×3 = 36, also over the 5-point bar).

**Verification performed.** This is a planning/documentation story with no `## Verification`
section and no application code, so verification was manual: confirmed both `DESIGN.md` and
`EXPERIENCE.md` carry exactly 19 numbered page entries each (`grep -cE '^### [0-9]'`); confirmed
all 19 `DESIGN.md` entries now carry a `Ports` field; confirmed the 19 `kind` values sum to 19
(1 `bsl-shell` + 2 `live-scan-artifact` + 12 `no-bsl-shell` + 4 `report-artifact`); confirmed no
`§ 4.11–4.13` or wrong-`EXPERIENCE.md`-section references remain; confirmed the spec's YAML
frontmatter still parses after the two new `deferred` entries; confirmed the 10 vendored CIS
skill directories are byte-identical to `.pixi/envs/local-recipes/share/bmad-creative-intelligence-suite/skills/`
(re-verified from the prior pass); confirmed no unresolved `{{…}}` template placeholders in any
of the three new/edited planning documents.

**Residual risks.** The 19-vs-21 page-mapping (the `distribution-breakdown` 3-into-1
consolidation) remains a design judgment call, not a verified fact — already flagged as a
medium-severity deferred item for Story 20.5 to re-verify before treating "19" as fixed. The
`{{project_name}}` template-placeholder bug in the vendored CIS package is upstream and unfixed
(low severity, deferred, not exercised by this story's own artifact). The structural
ledger-self-report-verification gap (deferred, medium severity) means this story's own `done`/
`closed` marks are not independently machine-verified — they were manually re-counted during
this review pass, but no repo tooling currently backs that check going forward.

## Verification

**Commands:**
- `pixi run -e pyforge-atlas kedro-test` — expected: pass (this station's own verify suite; backfilled generically, no per-story claim).
- `pixi run -e pyforge-atlas kedro-catalog-check` — expected: pass (this station's own verify suite; backfilled generically, no per-story claim).
