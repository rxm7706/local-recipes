---
topic: enterprise-data-models-and-apis — parked with trigger
updated: 2026-08-22T07:51
---

- (event) Parked spec authored 2026-08-22 (operator: get all seeds ready-to-implement): the Dream's own constraints forbid building until the trigger fires; zero stories minted — parking IS the decomposition decision
- (note) Trigger bound to an owned checkpoint 2026-08-22: warden Story 8.1's host decision is the fleet's first real Django+API subject since this park — if the web face grows a queryable model/API surface (or the OCP github_metrics data wants one), the three-tier + JSON:API pattern unparks INTO that chain (fold, not own epic); a consult line added to 8.1's story text. Else this remains the archive candidate its own Dream calls most-speculative
- (decision) REFRAMED by operator 2026-08-22: parked -> EXTENSION-POINT with an EXISTING seam — the platform's AD-17 pluggable-app registry IS the integration point; the three-tier/JSON:API pattern lands as a separately-developed pluggable app whenever a subject appears (incl. air-gapped extensions). No stub story needed: the spec now documents the binding; warden 8.1's consult line points here
