---
topic: enterprise-data-models-and-apis — parked with trigger
updated: 2026-09-09T16:11
---

- (event) Parked spec authored 2026-08-22 (operator: get all seeds ready-to-implement): the Dream's own constraints forbid building until the trigger fires; zero stories minted — parking IS the decomposition decision
- (note) Trigger bound to an owned checkpoint 2026-08-22: warden Story 8.1's host decision is the fleet's first real Django+API subject since this park — if the web face grows a queryable model/API surface (or the OCP github_metrics data wants one), the three-tier + JSON:API pattern unparks INTO that chain (fold, not own epic); a consult line added to 8.1's story text. Else this remains the archive candidate its own Dream calls most-speculative
- (decision) REFRAMED by operator 2026-08-22: parked -> EXTENSION-POINT with an EXISTING seam — the platform's AD-17 pluggable-app registry IS the integration point; the three-tier/JSON:API pattern lands as a separately-developed pluggable app whenever a subject appears (incl. air-gapped extensions). No stub story needed: the spec now documents the binding; warden 8.1's consult line points here
- (decision by claude) Owner Dream `docs/dreams/enterprise-data-models-and-apis.md` moved `specified -> dreamt` (operator 2026-09-09, batch § 2.2 row atlas-B4); this Spec KEEPS `status: extension-point` — the park is deliberate and its trigger stands unchanged. `docs/dreams/README.md` is explicit that '`specified` requires a Spec at `ready` or beyond', and `extension-point` is neither `ready` nor beyond; the Dream's own Constraint says it 'should not advance past `dreamt` without a concrete PyForge-native domain', and its § What is real reads 'Nothing'. Reverts to `specified` the moment a subject is named and this Spec reaches `ready`.
- (note by claude) CROSS-STATION (steward, batch atlas-B4): `docs/dreams/README.md` should NAME `extension-point` as a recognised parked-Spec state that does NOT satisfy `specified`, and carry this Dream's corrected row/status. The 2026-08-22 extension-point reframe itself is unchanged and sound — but the socket it names is steward's shipped AD-17 pluggable-app registry (`config/engine_patterns.py`), not a surface this Dream owns, so under the realization gate this Dream has nothing of its own to exercise.
- (question by claude) Open, cross-station (readiness E-6): this Spec's Extension contract states 'warden 8.1's consult line points here' (`SPEC.md:27`). Warden Story 8.1 is `done` and its code now lives in `src/shared/packages/django-warden/` after the `2394d850db` relocation — re-verify the consult line moved with the code before this pointer is trusted.
- (event) Self-validate Pass 1 (coherence) PASS on the 2026-09-09 re-derive: this Spec carries no CAP block by design (a parked extension-point whose contract is the pattern notes); status untouched, the new cross-station question is surfaced into frontmatter rather than left in the memlog
- (event) Self-validate Pass 2 (preservation) PASS: the Dream 'specified -> dreamt' move, the reason it does not drag the Spec, the realization-gate reading of the AD-17 socket, the steward README obligation and the warden-8.1 pointer re-verify question all landed
