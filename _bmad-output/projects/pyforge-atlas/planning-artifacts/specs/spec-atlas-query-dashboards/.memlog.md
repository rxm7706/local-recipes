---
topic: atlas-query-dashboards (genesis baseline bootstrapped 2026-08-22, marshal Story 25.3)
updated: 2026-09-09T16:09
---

- (event) Genesis baseline 2026-08-22 (marshal Story 25.3, spec-bmad-611-era-alignment CAP-3): .memlog.md bootstrapped so 6.11 bmad-spec updates succeed against this folder — a structural bootstrap only, per the S-13.7 pattern; no governed-file content is claimed reconciled by this entry
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (change) `scripts/dashboard_serve.py` added to `surface:` (2026-08-29, spec-surface-drift-reconciliation ongoing practice) — it was tracked and ungoverned (a `[spec-surface] ungoverned` finding). It is the DW-D2-3 entrypoint for Story 20.5's ported Vizro dashboard (epics.md line ~1838), which this Spec's CAP-7 already describes; the script itself just lives at repo-root `scripts/`, outside the existing `src/shared/packages/pyforge-atlas/**` glob. First `--write-baseline --spec` scoped stamp for this file.
- (decision by claude) RETIRE (operator 2026-09-09, fleet-readiness-decision-batch-2026-09-09 § 2.3 C1 / row atlas-B1): delete `src/shared/packages/pyforge-atlas/src/pyforge/atlas/views/` and `tests/unit/views/` — CAP-1..CAP-4's intent is superseded by the live Vizro/BSL board (`dashboard/app.py`'s PAGE_INVENTORY, reached through the D1 BSL seam, gated by `dashboard-dryrun`, served by `pixi run -e local-recipes dashboard-serve`); the module has no importer outside its own unit tests (no CLI verb, no pixi task, no ASGI mount), and the `cli_bridge` dynamic-import evasion of the F1 DuckDB-singularity gate (`views/cli_bridge.py:11-17`) is removed with it; the widget-registry idea is kept only if a Vizro page actually asks for it.
- (decision by claude) Open question 'session bridging if these views are ever hosted against Steward's dashboard surface' CLOSED (batch § 2.1 row atlas-B2): it dies with the module — C1 retires `views/`, so no route exists to mount behind an authenticated host, and the estate already forbids the shape (`src/platform/tests/test_host_board_row_isolation.py:26` `_SECOND_STACK_TOP = frozenset({'vizro','dash'})`, AST-asserted across all nine host/portal roots).
- (decision by claude) Open question 2 is STALE and closes with it (readiness finding D-3, batch § 2.1): the CIS two-spine specs DO exist (`planning-artifacts/DESIGN.md`, `planning-artifacts/EXPERIENCE.md`), Stories 20.4 and 20.5 both read `done`, and DW-D2-1 reads `status: closed` — the 'still not produced as of 2026-08-27' text was never reconciled after delivery; § Decomposition & coverage record's 'Epic 20 (Stories 20.1–20.5, all `backlog`)' is the same missed reconcile and is now all `done`.
- (event by claude) status: in-progress -> in-progress (HELD, deliberately) — CAP-1..CAP-4 are SUPERSEDED at capability grain by batch C1; the fleet's terminal vocabulary for a retired-and-replaced Spec is `superseded` (one precedent: marshal `spec-factory-console/SPEC.md:3`), while atlas's own terminal values are `shipped` and `archived` (`spec-microsoft-org-sweep`, `spec-pyforge-atlas-intelligence-platform`). The Spec is NOT superseded whole: CAP-5..CAP-7 are the live query-plane boot script, the composed-store pipeline and the shipped Vizro page inventory. It therefore stays `in-progress` until the retirement story (atlas 25.1) lands, then flips to `shipped` — flipping today would be exactly the paper claim the realization gate exists to refuse.
- (event by claude) Owner Dream `docs/dreams/atlas-query-dashboards.md` moved `specified -> archived` with `archived-reason: retired` (batch C1). `retired` is the README-sanctioned reason already used by `docs/dreams/artifact-console.md` for a runtime that was built and stood down; `archived` is in the guild-roster `dream_statuses` vocabulary. CROSS-STATION: steward owes the matching `docs/dreams/README.md` row/status or `dreams-hygiene` emits `readme-table-drift` for this slug.
- (event by claude) Effect story minted 2026-09-09: atlas Story 25.1 'Retire the second Lane-3 runtime' under a NEW Epic 25 (batch § 2.3 C6 — 'effect stories on the owning station's epics; atlas … a small epic'). Epic 14 is closed 4/4 `done`, so a 14.5 would reopen a completed epic; Epic 25 is the first free number (Epic 11 never existed by design). Steward Epic 49 carries only the index row.
- (event) Self-validate Pass 1 (coherence) PASS on the 2026-09-09 re-derive: 7 CAPs keep intent+success and their stable ids (CAP-1..CAP-4 marked SUPERSEDED at capability grain, texts retained as the record, ids never reused); constraints still bend; Non-goals non-empty; success signal testable; open_questions now empty
- (event) Self-validate Pass 2 (preservation) PASS: every 2026-09-09 memlog claim landed — C1 retirement + rejected alternative, Story 25.1 under new Epic 25, Dream archived/retired, steward README + Unifying Kinships cross-station obligation, both question closures, and the two stale post-delivery reconciles (Epic 20 all done, DW-D2-1 closed); nothing dropped
- (event) status: in-progress -> shipped (2026-09-10, atlas Story 25.1): CAP-1..CAP-4 superseded at capability grain by the live Vizro/BSL board; CAP-5..CAP-7 remain the live contract. The `views/` package and its tests are deleted; page-count literals updated to 34.

- (event) 2026-09-17 absorbed into spec-pyforge-atlas (one-chain fold). Pointer + memlog + companions retained.
