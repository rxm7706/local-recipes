---
topic: The from-scratch packaging-inventory intake engine, governed — the quartet becomes a chartered capability
updated: 2026-09-02T06:26
---

- (note) The most spec-overdue seed in the fleet: the quartet (runner/prompt/config/replay) + data files ALREADY live in scripts/+conf/ (allowlisted 2026-08-16 with a delete-the-line-when-specced rule) — four stations independently flagged them ungoverned. This pass charters them
- (capability) CAP-1 the governed from-scratch run: Master Prompt v3.0 bound, clean-workspace regeneration from workbook+live indexes+curated feeds, PEP-503 identity + provenance + timestamped verify per package, inspectable ranking (P1-P10, Score 1-100); dated identity tabs (identity-YYYY-MM-DD, never overwrite) + pinned secret gist (id never in git). CAP-2 handoff queues: the OpenTeams universe (CDO-ENT-JFROG ∪ CDO-ENT-CONDA) issue-per-library + the dated Mason handoff tab (Fix-vulnerability/Create-recipe/File-issue/Already-tracked) + the AOSS-Free extra Mason queue; the allowlist lines DELETED as the spec's surface claims the quartet
- (event) Spec authored + decomposed 2026-08-22 as atlas Epic 16 (2 stories)
- (decision, 2026-08-22, Story 16.1) parselmouth fold placement resolves to the core pyforge-atlas mapping_manager/name_resolver chain, not this quartet's identity script — the un-deferred prefix-dev/parselmouth evaluation (memory: project_prefix_dev_mapping_upstreams, un-deferred 2026-08-22) names that chain explicitly as the fold target. `..._openteams_identity.py`'s existing direct purl-associator fetch is unaffected.
- (event, 2026-08-22, Story 16.1) `surface:` claims the quartet's 4 scripts + 2 conf data files; corresponding per-file lines + explanatory header comments deleted from `scripts/spec_surface_allowlist.txt`; `scripts/.spec-surface-baseline.json` re-stamped scoped to this spec (`--write-baseline --spec pyforge-atlas/spec-conda-forge-packaging-inventory-operations`). `pixi run -e local-recipes spec-surface-check` confirmed `ok` with zero findings against this spec afterward. The quartet's behavior is unchanged — this story is governance-only (spec-surface chartering), not new engineering.
- (deferred, 2026-08-22, Story 16.1 review) CAP-1's full success bar ("a clean run reproduces the inventory") is not freshly re-verified by this story — this diff touches zero quartet runtime code, and no cached external sources (`/tmp/ext-src/*`) or live gist credentials are available in this environment to run the quartet from scratch unattended. The claim rests on the pre-existing `identity-2026-08-20` dated tab (2 days old at story time), produced by this same toolchain before the story landed. A future pass (attended, credentialed) should execute a real from-scratch run against the chartered surface and record the result here.
- (event, 2026-08-22, post-merge correct-course) `epics.md` carried two unrelated `## Epic 16:` headings — this epic and the already-`done` "Wagtail corporate brain" epic (spec-wagtail-corporate-brain) — a duplicate-numbering defect this story's own review pass found, flagged, and correctly left out of its own surgical scope. Resolved same-day by renumbering THIS epic to **Epic 17** (Wagtail keeps 16, untouched): `epics.md` heading + both story headings, `sprint-status-ledger.yaml` + Tier-3 `sprint-status.yaml` keys (`16-1-.../16-2-...` → `17-1-.../17-2-...`, plus a genuine `epic-16` duplicate-key collision in the Tier-3 YAML split into distinct `epic-16`/`epic-17` pairs), the promoted story spec renamed `spec-16-1-...md` → `spec-17-1-...md` (with a dated note at its top explaining the renumber — its own historical narrative below that note still says "16.1"/"Epic 16" throughout, which is correct and unchanged, since that's what the number was when the work happened), and this SPEC.md's own `status`/`surface`/`open_questions`/`## Constraints` references. All prior dated entries above this one in this memlog are left as-written for the same reason — they are the accurate record of what was true at the time.
- (event, 2026-08-22, Story 17.2) CAP-2 execution-ready handoffs landed. `scripts/conda-forge-packaging-inventory-operations_openteams_identity.py`: `create_missing_issues()` — one `gh issue create` (repo `OpenTeams-WFT-CDO/mgmt-wf-python-modernization`) plus `gh project item-add` (project 1) per OpenTeams-universe name still missing `OpenTeams_Issue_URL`; dry-run by default (prints + returns the would-create names/titles, no `gh` mutation call), live behind a new opt-in `--create-issues` flag; a `gh` failure for one name logs and continues, never aborts the run. `scripts/conda-forge-packaging-inventory-operations_metrics.py`: `write_aoss_free_queue()` writes the dated `aoss-free-queue-YYYY-MM-DD.csv` (columns `Package_Name`/`Reason`/`Verification_Timestamp_UTC`) from `aoss_free_names - universe_names` only (`universe_names` = CDO-ENT-JFROG ∪ CDO-ENT-CONDA, the same `must_keep` set already computed in `main()`) — never merges into or expands the OpenTeams universe. `scripts/openteams_identity_dashboards.py`: `write_ops_canvas()` + `write_workbook_canvas()` — the second and third of the three Dream-specified dashboard canvases (catalog already existed via `conda-forge-packaging-inventory-operations_priority.py::write_canvas`), restructuring the same values `render()` already computes into `cursor/canvas` TSX files sharing `write_canvas`'s exact import block; structural-only (valid TSX + parseable embedded JSON, schema-shaped with empty `rows`/`leaders` on zero input), no live-rendering verification. New offline test file `tests/packaging/test_openteams_handoffs.py` (12 tests; every `gh` call mocked via `subprocess.check_output`/`check_call`, no network/credentials; `pytest.importorskip("openpyxl")`-guarded at module scope so it skips cleanly rather than erroring under the lean `pyforge-ci` env that `pyforge-deps-test` runs the rest of `tests/packaging` in). `priority.py` untouched (read-only pattern reference only). `scripts/.spec-surface-baseline.json` re-stamped scoped to this spec (`--write-baseline --spec pyforge-atlas/spec-conda-forge-packaging-inventory-operations`) to cover the 3 changed script paths' new content hashes.
- (event, 2026-08-22, Story 17.2 follow-up) `write_ops_canvas`/`write_workbook_canvas` were defined but not yet called from anywhere, so a real run would never produce them — wired both into `write_dashboard_markdown()` (called from both `main()`'s live-run path and `publish_gist_from_tab()`'s `--gist-only` path), following `priority.py`'s existing `--canvas`-flag convention: new `--ops-canvas`/`--workbook-canvas` CLI flags (default: the same live Cursor-project `canvases/` paths `write_canvas` already targets; overridable for tests) thread through both call sites. Verified end-to-end offline against a minimal in-memory workbook (temp paths only — never the live default paths, which stay untouched by any of this story's tests). `scripts/.spec-surface-baseline.json` re-stamped again to cover this file's updated content hash.
- (event, 2026-08-22, Story 17.2 review-triage patch pass) 8 findings from the 4-layer review (blind-hunter/edge-case-hunter/verification-gap/intent-alignment) applied on top of the two entries above, all in `scripts/conda-forge-packaging-inventory-operations_openteams_identity.py` unless noted. (1) [HIGH] `main()` now checks `args.create_issues and not gh_bin()` right after the `--gist-only` early-exit and returns 1 with a clear stderr message, instead of letting `create_missing_issues`'s internal `SystemExit` fire deep inside `main()` after all the inventory computation — a live `--create-issues` run with no `gh` on PATH previously lost the whole run's output (`write_xlsx_tab`/`write_csv`/markdown never ran). (2) `create_missing_issues`'s two `try/except subprocess.CalledProcessError` blocks now also catch `OSError` (covers `gh` vanishing mid-run, e.g. `FileNotFoundError`), so the loop genuinely never aborts as its docstring claims. (3) the `missing` filter now also requires a non-blank `Core_Python_Package_Name`, so a row with a blank/whitespace name can never reach `gh issue create` with a garbage `[Conda-Forge Packaging] ` title. (4) `row["OpenTeams_Issue_URL"]`/`board[...]` are now set only after BOTH `gh issue create` AND `gh project item-add` succeed — previously a project-item-add failure after a successful issue-create still marked the row tracked, so a future run's `board_packaging_urls` join would treat it as `Already tracked` and never retry adding it to the project board; the created issue is still reported via the return value even when item-add fails, it is just not merged into `board`/the row. (5) `write_dashboard_markdown()`'s two canvas-writer calls (added in the follow-up entry above) are now each wrapped in `try/except Exception`, logging a warning to stderr and continuing — a canvas-write failure (unwritable default Cursor-project path, a locked/corrupt xlsx on `write_workbook_canvas`'s second `load_workbook` open) must never block the already-successful gist-markdown write or the caller's subsequent `publish_gist_files` call. (6) `docs/reference/conda-forge-packaging-inventory-operations_replay.md` now documents `--create-issues` (live and irreversible, dry-run by default) alongside the existing `--skip-gist` mention, plus a new Deliverables item 7 for the two canvases and `--ops-canvas`/`--workbook-canvas`. (7) this SPEC.md's own `status:` trailing comment updated from "CAP-2 (17.2) still backlog" to "CAP-2 (17.2) code landed, live-execution verification deferred (attended, credentialed run pending)" — the `status:` value itself stays `in-progress`, unchanged. (8) [HIGH, root cause] `tests/packaging/test_openteams_handoffs.py`'s `pytest.importorskip("openpyxl")` guard meant the file was silently skipped, not run, by the ONLY pixi task that touches `tests/packaging` (`pyforge-ci`'s `pyforge-deps-test`, whose env has no runtime deps) — added `[feature.local-recipes.tasks.test-packaging]` (`pytest tests/packaging -q`) to `pixi.toml` so these tests actually execute somewhere; a pure tasks-only `pixi.toml` addition produces zero `environment.yaml` diff (`pixi project export conda-environment -e build`), confirmed, so no regen was needed. Also added 4 new regression tests (blank-name skip, `OSError` mid-run, item-add-failure non-marking, canvas-write-guard survival) for findings 2-5, bringing the file to 16 tests. Re-verified: `py_compile` exit 0; `pixi run -e local-recipes pytest tests/packaging/test_openteams_handoffs.py -q` 16 passed; `pixi run -e local-recipes test-packaging` 98 passed / 2 pre-existing unrelated `pyforge-herald`/`python-pptx` failures (confirmed present on the clean pre-Story-17.2 baseline via `git stash`, unrelated to this spec); `pixi run -e pyforge-ci pyforge-deps-test` unaffected (82 passed, 1 skipped, same 2 pre-existing failures). `scripts/.spec-surface-baseline.json` re-stamped scoped to this spec a third time to cover `..._openteams_identity.py`'s updated content hash — the docs runbook edit is a companion file already covered by the blanket `docs/reference/**` allowlist entry, not this spec's governed surface, so it needs no baseline entry.
- (event, 2026-08-22, Story 17.2 verify-repair, cross-station reconcile) Story 17.2's deterministic `python scripts/spec_surface_reconcile.py` gate red for reasons entirely outside this spec's own surface: 5 `drift` findings against `pyforge-marshal/spec-pyforge-marshal` (marshal Story 11.4, commit `b1d1ee8bfa`, landed `seed/verbs/update.py` + 3 tests + a `cli/seed.py` change without appending that spec's `.memlog.md`). That gate is repo-level and rc=1 blocks every unrelated story in the fleet, so it had to be reconciled here rather than left as "pre-existing, not ours." Verified zero surface overlap first (marshal's surface is `src/shared/packages/pyforge-marshal/**`; this story's diff never touches it). Reconciled at the source: appended the missing technical memlog entry to spec-pyforge-marshal's own `.memlog.md` naming the drifted paths, then `python scripts/spec_surface_check.py --write-baseline --spec pyforge-marshal/spec-pyforge-marshal` (scoped — diff against the pre-stamp baseline is exactly 1 key changed, 0 added, 0 removed). `python scripts/spec_surface_reconcile.py` now exits 0. This spec's own surface/baseline/memlog are unaffected by this reconciliation.
- (event, 2026-08-30, atlas Story 21.3 landed — PR #941) `scripts/conda-forge-packaging-inventory-operations_metrics.py` gains `--live-catalog PATH` / `--live-catalog-only`: the three Tier 0 verification sets (conda-forge names, PyPI names, Parselmouth conda names) read the pyforge-atlas Kedro Parquet (`core_packages_enumerated`, `pypi_universe`, `pypi_conda_mapping`) instead of live HTTP / `/tmp/ext-src` snapshots; absent the flag, behavior is byte-identical. New `scripts/tests/test_conda_forge_packaging_inventory_operations_metrics.py` (16 tests) added to this spec's `surface:`. The workbook (`--analysis-xlsx`) still supplies `records`/`tab_packages`/roles — that is NOT this story's scope. Cross-spec: the atlas course correction of the same day (`_bmad-output/projects/pyforge-atlas/planning-artifacts/change-history/sprint-change-proposal-2026-08-30.md`) mints atlas Stories 23.8 (Kedro `inventory_universe` + `--analysis-xlsx` optional) and 23.9 (quartet workbook retirement — `openpyxl` gone from `scripts/`); both will land on this surface and must append here when they do. `scripts/.spec-surface-baseline.json` re-stamped scoped to this spec.
- (event, 2026-08-31, atlas Story 21.7 landed — CAP-4, "quartet thin-out and gist wrapper") **Supersedes Story 17.1's "purl-associator fetch stays in the quartet" clause** (this file's Story 16.1 entry above, and `SPEC.md` § Constraints — both left as-written for history, with a dated superseding note added to `SPEC.md`). `scripts/conda-forge-packaging-inventory-operations_openteams_identity.py`: `main()`'s fetch+join+overlay block (live `ASSOCIATOR_URL` fetch, OpenTeams project-1 GraphQL board fetch, `feedstock-outputs.json` fetch, staged-recipes PR fetch) is replaced by one `pandas.read_parquet` read of the pyforge-atlas Kedro catalog's `identity_export_parquet` (Story 21.6's `upstream_discovery` Phase D join — see `spec-atlas-kedro-catalog-expansion/identity-contract.md`), resolved via the new `identity_export_parquet_path()` helper (`PYFORGE_ATLAS_DATA_ROOT` env override, default `src/shared/packages/pyforge-atlas/data`, mirroring `globals.yml`'s `${paths.data_root}` convention). A missing Parquet is a hard, named, non-zero-exit error (`read_identity_export_records()`) — never a live-fetch fallback. `create_missing_issues`'s `board` argument is now always `{}` (write-only bookkeeping; no longer read back — Atlas's own board join in Phase D already populated `OpenTeams_Issue_URL`). Retired the 9 dead flags feeding that fetch (`--tab-in`, `--associator`, `--refresh-associator`, `--project-items`, `--feedstock-outputs`, `--staged-prs`, `--staged-open-prs`, `--recipes-dir`, `--refresh-staged-prs`) and the helper functions/constants only they called (`lookup_assoc`, `from_assoc`, `from_inventory`, `from_board_only`, `attach_packaging_urls`, `read_inventory_tab`, `fetch_project_issues`, `board_packaging_urls`, `load_feedstock_outputs`, `load_staged_prs`, `names_from_pr_title`, `feedstock_repo_name`, `metadata_url`, `download_json`, `issue_url`, `git_purl`, `alt_purls`, `inventory_feedstock_fallback`, `na`, plus `ASSOCIATOR_URL`/`FEEDSTOCK_OUTPUTS_URL`/`METADATA_URL`/`STAGED_PR_API`/`GIT_RE`/`TITLE_PREFIX_RE`/`TITLE_JUNK_RE`/`TITLE_VERSION_RE`/`SKIP_RECIPE_DIRS`/`TAB_IN`) — verified each had no other caller (`overlay_live_local`/`load_local_recipes`/`load_local_build_status`/`name_keys`/`first_map`/`packaging_name_from_title`/`join_list`/`TITLE_STOP` all stay: `openteams_identity_dashboards.py`'s `render`/`write_ops_canvas`/`write_workbook_canvas` reach `overlay_live_local`/`packaging_name_from_title` through the `helpers = types.SimpleNamespace(**globals())` bridge `write_dashboard_markdown` builds from this script's own module namespace, so removing either would have broken the dashboards module at import time with no static signal). `publish_gist_from_tab` (`--gist-only`) rewritten: reads the ranked identity tab (unchanged missing-ranking-columns error) and the same Parquet, then merges ranking + JFROG/priority columns (new `RANKING_MERGE_COLUMNS`, derived from `GIST_COLUMNS - COLUMNS - {"Package"}`, not hand-listed) onto the Parquet rows by `Core_Python_Package_Name` via new `merge_ranking_columns()` — a Parquet name with no match in the ranked tab is dropped (P/Rank/Score/Work are GIST_SCHEMA-required) with a stderr warning naming it, never silently and never raising. `write_xlsx_tab`/`create_missing_issues`/`write_gist_markdown`/`write_dashboard_markdown`/`--skip-gist`/`--create-issues` unchanged. `docs/reference/conda-forge-packaging-inventory-operations_replay.md` Deliverable 3 documents the new Parquet source, the retired flags, and the `--gist-only` merge/skip behavior. `tests/packaging/test_openteams_handoffs.py`: 11 new tests (27 total) covering the I/O & Edge-Case Matrix — `identity_export_parquet_path()` env-override + default resolution, `read_identity_export_records()` success (NaN/`pd.NA` -> `""`) + missing-file (named error, no exception), `merge_ranking_columns()` match + per-name-miss warning + total-miss non-raising, retired-flags-absent-from-`--help`, `main()` end-to-end (Parquet -> written identity tab, `--skip-gist`) + Parquet-missing exit 1, and `publish_gist_from_tab()` end-to-end merge-and-publish (mocked `gh`/canvas writers) + the "tab missing ranking columns" row (returns 1 with the existing error message, asserts the Parquet is never even read once that check fails — added same-day after a Matrix Test Audit found this row uncovered by both this story's new tests and the pre-Story-21.7 baseline). Verified: `pixi run -e local-recipes pytest tests/packaging/test_openteams_handoffs.py -q` 27 passed; `pixi run -e local-recipes test-packaging` 145 passed; `pixi run -e pyforge-ci pyforge-deps-test` 118 passed / 1 skipped (the `openpyxl` importorskip guard, confirmed still gates the new `pandas` import too — a bare top-of-file `import pandas as pd` before that guard broke collection under the lean env on first attempt, moved below it, fixed); `python -m py_compile` on the identity script; repo-wide grep confirmed zero remaining callers of every retired symbol. `scripts/.spec-surface-baseline.json` re-stamped scoped to this spec.
- (event, 2026-08-31, atlas Story 21.7 review pass 1 loopback, re-derivation) The story spec's `## Spec Change Log` records a `bad_spec` loopback: 3 of 4 review layers independently converged on `main()`'s persisted xlsx tab/CSV and the published gist silently disagreeing on `Local_Recipes_URL`/`Local_Build_Status`/`Verification_Timestamp_UTC` within one run, because the prior attempt's Code Map named `overlay_live_local` as retired alongside the fetch+join block, then separately kept the function (correctly, since `write_gist_markdown` -- an unchanged "gist writer" -- calls it directly) but never told `main()` to keep *calling* it before persisting output. Root cause was in the Code Map, not the code: `main()`'s only overlay call ran inside `write_gist_markdown`, AFTER `write_xlsx_tab`/`write_csv` had already persisted pre-overlay `records`, and since `overlay_live_local` mutates `records` in place, the on-disk tab/CSV and the gist could diverge. Re-derivation applied the entire prior attempt verbatim (verified byte-identical against `_bmad-output/implementation-artifacts/story-21-7-prior-attempt.patch` via `git apply`) plus the amended Code Map's fix: `main()` now calls `overlay_live_local(records, REPO_ROOT / "recipes")` once, immediately after `read_identity_export_records()`, before `create_missing_issues`/`write_xlsx_tab`/`write_csv` -- `write_gist_markdown`'s own internal overlay call (unchanged) then re-runs (idempotently) on already-overlaid data for the gist step, so tab/CSV/gist agree within one run. Added the required default-path regression test, `test_main_default_path_overlay_runs_before_tab_and_csv_write` (28th test in the file): plants a real `recipes/pkg-a/recipe.yaml` whose CFE stamp differs from the Parquet fixture's `Local_Build_Status`/`Local_Recipes_URL`, runs `main()` without `--skip-gist` (gist publish mocked), and asserts the xlsx tab, the CSV, and the mocked-published gist markdown all already carry the live-scanned value. Re-verified: `python -m py_compile` on both changed files; `pixi run -e local-recipes pytest tests/packaging/test_openteams_handoffs.py -q` 28 passed; `pixi run -e local-recipes test-packaging` 146 passed; `pixi run -e pyforge-ci pyforge-deps-test` 118 passed / 1 skipped (unchanged); `--help` confirms zero retired flags; repo-wide grep re-confirmed zero orphaned callers of every retired symbol (the Story 21.6 Kedro port under `src/shared/packages/pyforge-atlas/src/pyforge/atlas/pipelines/upstream_discovery/` and `datasets/identity_sources.py` independently defines same-named functions -- a deliberate parity port, not a caller of this script). Station policy verify commands (reconciled 2026-08-30 in the spec's own `## Verification` section): `pixi run -e pyforge-atlas kedro-test` 1595 passed / 24 skipped (pre-existing, unrelated); `pixi run -e pyforge-atlas kedro-catalog-check` 61 passed. `scripts/.spec-surface-baseline.json` re-stamped scoped to this spec again (the two changed files' content hashes). Pre-existing, unrelated: `pixi run -e local-recipes spec-surface-check` exits non-zero fleet-wide (hundreds of `drift`/`drift-presumed` findings against `pyforge-steward/spec-pyforge-unifying-strategy`, `pyforge-atlas/spec-pyforge-atlas`, `pyforge-warden/spec-pyforge-warden`, and others) -- confirmed zero findings against this spec's own key; reconciling that fleet-wide backlog is out of this story's scope (`## Boundaries & Constraints` Never-list; not named in `## Tasks & Acceptance`).
- (event, 2026-08-31, atlas Story 21.7 review pass 2, 5 `patch` findings applied) All 5 were trivially fixable, no spec ambiguity. (1) `merge_ranking_columns()` now sets `out["Package"] = row.get("Core_Python_Package_Name", "")` unconditionally on every merged row -- `Package` (GIST_SCHEMA `required: yes`) was deliberately excluded from `RANKING_MERGE_COLUMNS` (the ranked tab is not its source of truth) but nothing else was setting it, so every `--gist-only` published row had a blank required column; pre-story `publish_gist_from_tab` read the ranked xlsx tab directly, which has a real `Package` column from `priority.py`, so this was a regression in the merge rewrite. (2) `publish_gist_from_tab` now guards the merge result: `if not records: ...; return 1` immediately after `merge_ranking_columns()`, mirroring the function's existing `if not ranked: ...; return 1` pattern -- previously a near/total-empty merge (every Parquet name missing from the ranked tab) would still write and publish, silently overwriting the pinned gist with near-empty content. (3) `tests/packaging/test_openteams_handoffs.py`'s `test_main_reads_parquet_writes_tab_and_skips_gist` and `test_publish_gist_from_tab_merges_ranking_and_never_falls_back_to_a_live_fetch` now both `monkeypatch.setattr(identity, "REPO_ROOT", tmp_path)` (with an empty `tmp_path/recipes` dir created first) -- neither previously did, so `overlay_live_local`'s live scan (unconditional in `main()` since the pass-1 fix, and always run inside `write_gist_markdown`) was hitting this actual repository's real `recipes/` tree during the test run instead of an isolated fixture; confirmed by the `pytest tests/packaging/test_openteams_handoffs.py -q` wall time dropping from ~3s to ~1s after the fix. (4) `test_project_item_add_failure_does_not_mark_row_as_tracked`'s docstring reworded off the retired `board_packaging_urls` name to describe current semantics (a future Atlas Phase D run's own board join), matching `create_missing_issues`'s own already-updated docstring. (5) the identity script's module docstring and this spec's companion `docs/reference/conda-forge-packaging-inventory-operations_replay.md` Story 21.7 paragraph both overclaimed that `Local_Recipes_URL`/`Local_Build_Status` come from the Parquet -- in fact `overlay_live_local` always overwrites both from a live `recipes/` scan on every run, so the Parquet's own values for those two columns are never used downstream; both reworded to say so. Explicitly deferred (per review-2 instruction, not addressed): timestamp-consistency between the tab/CSV and the gist (investigated, judged pre-existing and unrelated to this story) and a reverse-direction cross-source-match warning (judged a defensible, intentional one-directional design). Re-verified: `python -m py_compile` on both changed files; `pixi run -e local-recipes pytest tests/packaging/test_openteams_handoffs.py -q` 28 passed (1.06s, down from 3.06s); `pixi run -e pyforge-atlas kedro-test` 1595 passed / 24 skipped (unchanged, pre-existing); `pixi run -e pyforge-atlas kedro-catalog-check` 61 passed (unchanged); `pixi run -e local-recipes test-packaging` 146 passed; `pixi run -e pyforge-ci pyforge-deps-test` 118 passed / 1 skipped (unchanged). `scripts/.spec-surface-baseline.json` re-stamped scoped to this spec again.
- (event by claude, 2026-08-31) atlas Story 23.8 landed (`8fc803c19f`, done in ledger) exactly as this spec's own 2026-08-30 entry anticipated ("both will land on this surface and must append here when they do"): the workbook-free `inventory_universe` fixture set (`scripts/tests/fixtures/inventory_universe/{generate_fixtures.py, mini-workbook.xlsx, catalog/{derived,intermediate,primary,raw}/**/*.parquet}`, 7 files) — `spec-surface-check` reported these `ungoverned` (no spec surface, no allowlist entry) since the per-file `surface:` list (deliberately not a glob, per this SPEC's own header comment) hadn't been extended yet. Added all 7 to `surface:` above, per-file, same convention. `scripts/conda-forge-packaging-inventory-operations_metrics.py` and its test file (already governed) also changed in the same commit — ordinary in-surface drift, no separate note needed. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-conda-forge-packaging-inventory-operations`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 6 governed path(s) drifted against this spec's recorded baseline (1 added, 5 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `scripts/conda-forge-packaging-inventory-operations_metrics.py` (changed)
  - `scripts/conda-forge-packaging-inventory-operations_openteams_identity.py` (changed)
  - `scripts/conda-forge-packaging-inventory-operations_priority.py` (changed)
  - `scripts/openteams_identity_dashboards.py` (changed)
  - `scripts/tests/test_conda_forge_packaging_inventory_operations_metrics.py` (changed)
  - `scripts/tests/test_quartet_no_xlsx_surface.py` (added)
  Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-conda-forge-packaging-inventory-operations` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
