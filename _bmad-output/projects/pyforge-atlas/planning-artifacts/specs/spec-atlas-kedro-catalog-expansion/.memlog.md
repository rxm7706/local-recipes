---
topic: Atlas Kedro catalog expansion — self-contained data plane, inventory-aligned live indexes, upstream_discovery identity join
updated: 2026-09-13T08:33
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion`.
- (event) Surface reconcile 2026-09-07 (fleet hygiene, spec-45-2 CAP-2 pilot landing): `pixi.toml` changed to add three `local-recipes` pixi tasks (`eval-quality-smoke`, `eval-quality-review-twin-run`, `eval-quality-review-replay`) for pyforge-steward's `spec-bmad-eval-quality` CAP-2 pilot — unrelated to this spec's own surface, but this spec's blanket `pixi.toml` glob makes it a co-governor of that one file. No product work here. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion`.


- (decision) Station owner: pyforge-atlas — catalog, pipelines, upstream_discovery identity join; inventory quartet thins to consumer
- (capability) CAP-1 Kedro self-containment Phases A-B; CAP-2 public index catalog Tier 0-2; CAP-3 identity join in upstream_discovery Phase D; CAP-4 quartet --live-catalog consumer
- (decision) Phase D extends upstream_discovery not 9th pipeline; Tier 3 v1 non-goal; one spec + 4 companions + stories.yaml Epic 21
- (decision) Supersedes spec-conda-forge-packaging-inventory-operations constraint: purl-associator fold moves to Atlas on Story 21.7
- (assumption) CDO universe names from catalog Tier 2; JFROG telemetry stays inventory-only; feedstock URLs from core_feedstock_attribution without conda-forge.org scrape in v1
- (event) Spec authored 2026-08-29: SPEC.md + catalog-sources + identity-contract + verification-matrix + architecture-diagrams + stories.yaml (8 stories); self-validate pass 1+2 green
- (capability) CAP-5 optional Story 21.9: three Vizro BSL pages bootstrap-index-health identity-export-snapshot live-catalog-coverage
- (capability) CAP-6 optional Story 21.10: kedro-viz-publish.yml triggers on catalog.yml globals.yml datasets/**
- (decision) Core 18.1-18.8 no new Vizro; 21.9-21.10 optional follow-on after 21.8 green; companion operator-surfaces.md added
- (capability) CAP-7 Epic 22: Vizro parallel replacement for three identity canvases; ranking/enterprise/gist/UX deferred slices not capability blockers
- (decision) Companion vizro-canvas-parity.md; stories 22.1-22.6 + CAP-7d gist deferred; canvas deprecation only after 22.5 parity gate
- (capability) CAP-8 Epic 23: identity_complete_export + enterprise_jfrog_consumption; zero deferred data slices; complete-export-contract.md
- (decision) Enterprise handoff: Story 23.2 column contract in complete-export-contract §1; 22.1 bridge superseded by 23.5
- (decision) Epic renumber 18-20 → 21-23 (collision with epics.md Kedro hooks / skill / query-plane); epics.md appended; spec-21-1 promoted ready
- (event by claude) First-ever baseline stamp, 2026-08-30: this spec has never been stamped in `scripts/.spec-surface-baseline.json` (`no-baseline` finding, `spec-surface-check`). Established now, after Stories 21.1 (PR #932/#933) and 21.2 (PR #934) both landed against this epic's declared surface -- the stamp is a fresh reference point going forward, not a retroactive claim that prior unstamped history matched; both stories' own per-story specs (`spec-21-1-relocate-atlas-data-defaults-and-bootstrap.md`, `spec-21-2-remove-cf-atlas-db-seeds-from-production-datasets.md`) are the detailed record of what changed and why. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion`.
- (event by claude) Surface reconcile 2026-08-30, second pass: `pixi.toml` changed once more by `48ddb34b22` ("fix local recipes", already-landed) — routine dependency-pin bumps (python-crontab, asgiref, pyarrow, age) plus enabling the `ppt-master`/`codegraph` optional recipe candidates. No atlas CAP-1..4/catalog/pipeline touch. Verified via `git log --oneline -1 -- pixi.toml`. Scoped-stamped.
- (event by claude) Surface reconcile 2026-08-31: 13 drift findings, verified via `git log --oneline 48ddb34b22..HEAD` per path. Twelve are this spec's own capability progress, not off-topic drift: `catalog.yml`/`globals.yml`/`datasets/**`/`pipelines/{core,pypi_intelligence,upstream_discovery}/**` carry Epic 21's close-out (Stories 21.4 Tier-1 catalog sources, 21.5, 21.6 identity-join-and-export-parquet — landed this session via the rescued `dispatch/pyforge-atlas/21.6` branch — and 21.8 end-to-end verification gate, all `done` in the tracked ledger) plus Story 23.8's `inventory_universe` catalog entry (CAP-8, Epic 23, already-anticipated capability). `pixi.toml` changed by 2 commits: `5017329a9f` is directly Story-21.6-caused (adds `artifactory_downloads` to the bootstrap pipeline list because `build_identity_packages_primary` now reads its `enterprise_jfrog_names` output as a real cross-pipeline input — the same rescued-branch change); `d230e7fce2` (token-economy dep activation) is unrelated. No CAP text change needed — SPEC.md's surface globs and existing capability list already anticipate all of this. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion`.
- (event by claude) Surface reconcile 2026-09-01: 7 drift findings since the 2026-08-31 stamp — Atlas Story 23.1 (done, PR #974, `de24e3961e`): `catalog.yml`, `globals.yml`, `datasets/tier3_sources.py`, `datasets/__init__.py`, `pipelines/pypi_intelligence/nodes.py`, `pipeline.py` — tier-3 bulk OS index capability within CAP-8. `pixi.toml` changed by `5e7f8cac4b`/`af315d4bd7` (marshal Story 28.7 index-freshness-check task on the shared file — unrelated cross-package noise, no atlas CAP touch). Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion`.
- (event by claude) Surface reconcile 2026-09-01, second pass: `[feature.pyforge-ci.dependencies]` gained `"ruamel.yaml"` only — unrelated cross-package noise. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 6 governed path(s) drifted against this spec's recorded baseline (1 added, 5 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `.github/workflows/kedro-viz-publish.yml` (changed)
  - `pixi.toml` (changed)
  - `src/shared/packages/pyforge-atlas/conf/base/catalog.yml` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/dashboard/app.py` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/dashboard/data.py` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/dashboard/identity_gist.py` (added)
  Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (event) Surface reconcile 2026-09-04 (fleet hygiene, PR #1043): PR #1040's `pixi lock` (64f3f8e584 — `pnpm >=12.3.1` → `>=12.3.2`, lock re-solved) moved this Spec's `pixi.toml` / `pixi.lock` rows without this memlog moving. Accept the current tree as the post-land surface; no contract change, no new product work. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion` (scoped, never a bare `--write-baseline`), from a clean tree so the recorded hashes are committed content.
- (event) Surface reconcile 2026-09-04 (PR #1043, second pass — the Detectors env): `pixi.toml` gains `[feature.detectors.dependencies]` (pyforge-doctor path dep, `ruamel.yaml`, `playwright-python`) and the `detectors` environment (no-default-feature; 86 packages) that `.github/workflows/detectors.yml` now installs pixi-first with `locked: true`, pip as the visible fallback; `pixi.lock` gains its block; `environment.yaml` (derived, `build` env) re-exported and caught up (python 3.14.6→3.14.7, nodejs 24.16→24.19 floors already in `pixi.toml`). Nothing this Spec governs changed in meaning; no contract change. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion` from a clean tree.
- (change) 2026-09-04 (PR #1043, fleet CI health pass): `src/pyforge/atlas/dashboard/identity_gist.py` — `IdentityGistError` now inherits `PyforgeError` too (pyforge-core's CAP-5 exception-root sole-ownership meta test was red on this file; no behaviour change). `pixi.toml` / `pixi.lock` rows moved for OTHER features (platform-ci-test gains the pyforge-steward path dep; dbgpt-sidecar gains pytz — Platform CI was red on both); nothing this Spec governs changed in meaning. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion`.
- (event) Surface reconcile 2026-09-04 (PR #1043, fourth pass — the fleet CI health pass): `pixi.toml` rows moved for the python-agent-platform feature only: the `rjsmin` / `rcssmin` `build = "py312*"` pins were dropped (they locked cp312 builds into the py3.14 image env, so the Containerfile's `compress` step was red) and the `cachebox <6` / `openfeature-provider-flagd <0.5.1` ceilings the platform policy suite guards were restored after commit 098f0f0672 had dropped them. Nothing this Spec governs changed in meaning. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion` (scoped, never a bare `--write-baseline`).
- (event) Surface reconcile 2026-09-04 (PR #1043, seventh pass — the local Platform CI replay): `pixi.toml` gains the `platform-ci-local` task under `[feature.local-recipes.tasks]` (`bash scripts/platform-ci-local.sh`): a local replay of `.github/workflows/platform-ci.yml` — the test job step for step, the three image builds, the container job's runtime smokes and golden-path-promotion + the deploy verifier — against an ephemeral PostgreSQL 17/Redis 7 from platform-dev, so a platform change is proven before it is pushed (operator ask, after four sequential Platform CI cycles for four one-line CI-only fixes). No dependency, environment or lock change; `pixi lock --check` clean, `environment.yaml` untouched. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion`.
- (change by claude) Surface touch 2026-09-05 (PR #1045): pixi.toml's content hash moved because other stations' feature blocks (mason/steward/atlas/herald/scribe/warden) gained `pyforge-testing-kit` as a test-time dependency (retro-pyforge-steward-2026-09-04.md action item 11: the fleet's whole-branch diff-guard test mechanics (git subprocess calls computing files/commits changed since origin/main) were consolidated into a shared `pyforge.testing_kit.branch_diff_guard` module (PR #1045).). Nothing in this spec's own kedro-catalog-expansion concern was touched. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion`.
- (change by claude) Surface touch 2026-09-05: pixi.toml's content hash moved for the new `platform-policy-suite-check` pixi task (retro action item 3 (retro-pyforge-steward-2026-09-04.md, 2026-09-05): a new pyforge-doctor repo-scope source, `platform-policy-suite`, judges src/platform's manifest-only tests/policy subset by shelling out to the platform-ci-test pixi env's own interpreter (cli_bridge.run_pytest, AD-5), so a pixi.toml regression like the openfeature-provider-flagd/cachebox ceiling drop is caught even when Platform CI itself is disabled. WARN when that env is not installed, FAIL only on an actual policy-suite failure.). Nothing in this spec's own kedro-catalog-expansion concern was touched. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion`.
- (note by claude) 2026-09-05 (Story 45.1 pin PR, follows #1059): pixi.toml changed — bmad-eval-quality >=0.2.0.dev0 added to feature.local-recipes' linux-64 and osx-arm64 target tables (unix-only variant on SelfExplainML), bmad-method-wds-expansion pin dropped (upstream deprecated), bmad-labs-skills comment 22 skills; lock re-solved for the pin swap only (no other package moved). Routine suite-pin upkeep this spec's surface already anticipates; no capability change against this spec.
- (note by claude) 2026-09-05 pixi upgrade sweep PR (branch maint/pixi-upgrade-sweep-2026-09-05): .github/workflows/kedro-viz-publish.yml (setup-pixi pin -> 0.79.0), pixi.toml changed — operator `pixi upgrade --pinning-strategy latest-up` pass landed as one maintenance PR: pixi 0.78.0->0.79.0 (requires-pixi + $schema + all 15 registered external sites via bump-pixi-version, environment.yaml regenerated), bmad-method >=6.12.0, bmad-suite >=2026.9.5, ~20 other floors bumped, lock re-solved; the cachebox <6 and openfeature-provider-flagd <0.5.1 ceilings the platform policy suite guards were restored after the sweep dropped them. No spec-content change; scoped --write-baseline follows this entry.
- (note by claude) 2026-09-05 hygiene-2 PR (branch maint/hygiene-2-2026-09-05): pixi.toml changed — the conda-smithy `lint` task now runs the CI-parity CalVer conda-smithy via `pixi exec` over directories that hold a recipe file (G65); no dependency, env, or lock change. Scoped --write-baseline follows.
- (note by claude) 2026-09-05 hygiene-2 PR, second pixi.toml touch (same branch): the conda-smithy `lint` task now exports GH_TOKEN from `gh auth token` when unset (the CalVer linter's --conda-forge mode calls GitHub per recipe; anonymous is 60 req/h) and its description records the corpus runtime. No dependency, env, or lock change. Scoped --write-baseline follows.
- (event) Surface reconcile 2026-09-07 (fleet hygiene, pyforge-steward Story 46.3, spec-bmad-suite-lifecycle CAP-4 half): `pixi.toml` changed to add one `local-recipes` pixi task (`tea-test-review`, wrapping the real `tea-test-review` CLI with `--base origin/main --min-score 80` defaults) for TEA provisioning -- unrelated to this spec's own surface, but this spec's blanket `pixi.toml` glob makes it a co-governor of that one file. No product work here. Re-derived the governing set the same way Story 45.2/46.1 did (grep `pixi.toml` in every spec's `surface:` frontmatter) rather than assuming their own six-spec list still holds: it now numbers nine (this spec included), not six -- the surface set has grown since 45.2/46.1 landed. Scoped-stamped: `--write-baseline --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion`.
- (note by claude) Cross-station surface touch (pyforge-doctor Story 20.2, 2026-09-06): pixi.toml gained one new task, [feature.local-recipes.tasks.bmad-render-config-ambiguity-check] (a new doctor detector, unrelated to atlas/kedro/catalog work). No other file this spec governs (catalog.yml, globals.yml, datasets/**, pipelines/**, dashboard/**, the kedro-viz-publish workflow) was touched. Verified via git stash: pixi.toml carried zero drift against this spec's baseline immediately before this edit, so this is the sole cause. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (note by claude) Cross-station surface touch (pyforge-doctor Story 20.3, 2026-09-07): pixi.toml gained one new task, [feature.local-recipes.tasks.frozen-path-changed-check] (a new doctor detector, unrelated to atlas/kedro/catalog work). No other file this spec governs was touched. Verified via git stash: pixi.toml carried zero drift against this spec's baseline immediately before this edit, so this is the sole cause. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (note by claude) 2026-09-06 mason Story 14.1: pixi.toml changed -- reworded both bmad-eval-quality pin comments (feature.local-recipes' linux-64/osx-arm64 target tables) to state the recipe is re-verified win-ready and only the __win artifact's Windows build/upload is outstanding, matching install-matrix.md's hazard-cell wording; no dependency, env, or lock change. Scoped --write-baseline follows.
- (event by claude) status: ready -> shipped (operator 2026-09-09, batch § 2.2 row atlas-B5) — Epics 21, 22 and 23 are 100% `done` in `sprint-status-ledger.yaml`; CAP-1..CAP-8 are decomposed, landed and gated. The `ready` value had not been touched since authoring.
- (decision by claude) Owner Dream `docs/dreams/atlas-kedro-catalog-expansion.md` HOLDS at `specified` — it does not follow the Spec to a terminal state — until one recorded run materializes BOTH `identity_complete_export.parquet` and `enterprise_jfrog_consumption.parquet` (batch atlas-B5). A declared Kedro dataset (`conf/base/catalog.yml:987-989`, `:1301-1303`) is a contract, not data; splitting Spec-status from Dream-status is the shape `spec-wagtail-corporate-brain` already uses successfully. Moving both together would set the precedent that a data Dream is `realized` before any data exists — the pathology the realization gate was written for.
- (decision by claude) Both declared open questions are ANSWERED BY DELIVERY and close (readiness Section C-4): (1) CDO-ENT-JFROG universe names via `artifactory_downloads` — answered at Story 21.5 (`done`), `project_artifactory_names` ships the Tier-2 names-only projection and degrades to an empty frame carrying the names-only schema when the upstream columns are absent, telemetry columns staying inventory-side as recommended; (2) the conda-forge.org/packages scrape — the recommended path was taken and nothing proved it insufficient: `core_feedstock_attribution` is a live catalog dataset and the feedstock-name join source, and `catalog-sources.md:40` already records the scrape as `deferred` with that rationale.
- (event by claude) Effect story minted 2026-09-09: atlas Story 25.2 'Materialize CAP-8s canonical Parquets — one recorded run' under the new Epic 25 (batch § 2.3 C6 + row atlas-B5), seeded to ledger status `blocked` because it needs the ATTENDED, credentialed Artifactory path that `spec-conda-forge-packaging-inventory-operations` also waits on ('CAP-2 (17.2) code landed, live-execution verification deferred (attended, credentialed run pending)'). Two Specs, one precondition — they must not disagree about whether it has been met. Recorded as prose, deliberately NOT as a cross-project `Deps:` token. Closing this story is what moves the owner Dream to `realized`.
- (event) Self-validate Pass 1 (coherence) PASS on the 2026-09-09 re-derive: CAP-1..CAP-8 unchanged with stable ids and intact intent+success; 9 companions and 9 surface globs preserved; status now 'shipped', open_questions empty
- (event) Self-validate Pass 2 (preservation) PASS: the ready->shipped flip with its ledger evidence, both answered-by-delivery question closures, the deliberate Dream hold at 'specified' with the declared-dataset-is-a-contract reason, and Story 25.2 under Epic 25 with the shared attended-Artifactory precondition (prose, not a Deps: token) all landed
- (event) 2026-09-13 pixi.toml overlap: graphifyy on feature.pyforge-scribe. No atlas catalog product change.
- (event by claude) Surface reconcile 2026-09-14: `pixi.toml` changed for two reasons, neither of which touches this Spec's own contract. (1) The `frame-preflight` task description was restated for Frame Spec v0.3 (steward Story 53.6 moved Frame identity from `name` to the qualified-ref `identifier`). (2) `pyforge-station-tests` was REPAIRED: it landed on 2026-09-14 as `set -euo pipefail` plus a bash `for` loop, which pixi's task shell (deno_task_shell) cannot parse, so the task errored on its first token and had never run a single station despite CLAUDE.md documenting it as PR CI gate #3. Replaced with a declarative cross-environment `depends-on` over the eight `pyforge-<station>-test` tasks. Verified by running it: all 8 stations green, exit 0. `environment.yaml` re-exported and byte-identical (description/task-wiring edits only, no dependency change). Scoped-stamped.
