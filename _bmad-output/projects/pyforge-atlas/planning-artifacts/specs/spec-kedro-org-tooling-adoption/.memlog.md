---
topic: kedro-org-tooling-adoption (genesis baseline bootstrapped 2026-08-22, marshal Story 25.3)
updated: 2026-09-09T17:42
---

- (event) Genesis baseline 2026-08-22 (marshal Story 25.3, spec-bmad-611-era-alignment CAP-3): .memlog.md bootstrapped so 6.11 bmad-spec updates succeed against this folder — a structural bootstrap only, per the S-13.7 pattern; no governed-file content is claimed reconciled by this entry
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (decision by claude) Serialization defect, not a content one (readiness finding D-6): the four `open_questions:` entries at `SPEC.md:20-23` are all declared RESOLVED by the YAML comment immediately above them (`:15-19`, 'All four RESOLVED by delivery, audit Phase 3, 2026-08-10'), yet they still sit under the machine key — so any consumer reading frontmatter sees four open questions on a `shipped` Spec. Move all four verbatim under `resolved_questions:` and leave `open_questions: []`; keep the explanatory comment. Every resolution is verifiable on disk: `kedro-skills-audit-report.md` (Cap 1), `.github/workflows/kedro-viz-publish.yml` (Cap 2), `.vscode/extensions.json` + `vscode-kedro-decision.md` (Cap 3). Spec status `shipped` and Dream status `realized` are both correct and unchanged.
- (event) Self-validate Pass 1 (coherence) PASS on the 2026-09-09 re-derive: no CAP text touched, status 'shipped' unchanged, frontmatter now parses with open_questions: [] and a new resolved_questions: key holding all four verbatim
- (event) Self-validate Pass 2 (preservation) PASS: all four question texts moved verbatim, both explanatory comments kept, and the on-disk evidence for each capability's resolution recorded in the comment block
