# .memlog.md — spec-wagtail-corporate-brain

## 2026-08-15 — deferred-work-ledger.md touched by Story 16.1 (reconciliation only)

`_bmad-output/projects/pyforge-atlas/planning-artifacts/deferred-work-ledger.md`'s DW-H3 entry
gained a short annotation from Story 16.1, commit `87b545cc52`
(`../../../../../implementation-artifacts/spec-16-1-instance-deploy-definition.md`): it states that
Story 16.1
resolved the parent SPEC's deployment-substrate open question (django-lasuite 0.0.26 confirmed
live on the real conda-forge channel; Wagtail 7.4.1 has only a local recipe in this repo, upstream
feedstock status unconfirmed; no container) and the DW-H1-dependency question for the
LOCAL-REHEARSAL scope only (SQLite satisfies Story 16.2's local rehearsal instance; PostgreSQL/
MinIO stays open under DW-H1 for the eventual attended production bring-up). DW-H3's own `status:`
field was NOT changed by this annotation — it stays `open`; only descriptive text was appended.
Unrelated to this spec's own `factory/lasuite.py` / `test_lasuite.py` surface (zero diff, untouched)
and does not resolve this spec's own remaining `open_questions` item (home of the live
verification). Named here only to reconcile the S-13.7 surface-drift gate; baseline re-stamped for
this spec only in the same change.

## 2026-08-15 — Story 16.2: CAP-2/CAP-3 rehearsal + bring-up script; open question 3 resolved

`src/shared/packages/pyforge-atlas/tools/lasuite_bringup.py` (NEW) adds `build_httpx_opener()` +
a `main()` CLI — the real httpx-backed `Opener`, constructed OUTSIDE package code exactly as
CAP-2 requires; `factory/lasuite.py` gained zero edits. `src/shared/packages/pyforge-atlas/tests/
factory/test_lasuite_live_rehearsal.py` (NEW) stands up a loopback stdlib `http.server` stub
implementing `MockWagtail`'s same four routes + a Bearer-token check, loads `build_httpx_opener`
from the bring-up script via `importlib.util.spec_from_file_location` (`tools/` isn't a packaged
import path), and reproduces `test_lasuite.py`'s exact create/idempotent-skip/update/resume
counts over REAL HTTP — CAP-3's live acceptance, rehearsed offline. Runs inside the default
`kedro-test` gate (no network marker: loopback-only, no credentials); the fixture neutralizes
ambient proxy env (`httpx` defaults `trust_env=True` with no localhost bypass, so an exported
`HTTP_PROXY` would otherwise red the gate) — never via `trust_env=False` in the opener, which the
attended enterprise bring-up needs. `main()` guards the REPORT at both ends AND then proves the
CMS was actually reached, so a misconfigured bring-up can never report a hollow success: no
`outputs/` at the resolved root → exit 3 without creating anything; `created+updated+skipped == 0`
→ exit 3; and — because `skipped` means "unchanged, NO remote call made", so an all-skipped run
satisfies both while contacting nothing — a `list_documents()` probe runs before exit 0, making
exit 0 unreachable without genuine CMS contact (verified by hand: live sync → exit 0 with 2 server
hits, then the same sidecar against a dead `http://127.0.0.1:9` → exit 2, where it previously
exited 0 with a success banner). All-skipped runs stay PASSES, since that is the idempotency the
attended checklist's steps 5 and 7 exist to observe. Every documented exit code (0/1/2/3/4) has a
test. This also resolves the parent SPEC's third open question, and the prose records why DW-H3's
"do not bind a socket" clause is not contradicted. `open_questions` is now EMPTY: this story was
authored while Story 16.1 was still unmerged, so as written it removed item 3 and left items 1-2
standing, but 16.1 landed first and closed those two — so at landing time (2026-08-20) the two
sides were composed and all three questions are closed. What stays open is DW-H1's own
PostgreSQL/MinIO requirement for the attended production bring-up, which was never an open
question of this spec. `deferred-work-ledger.md`'s
`DW-H3` gained a short annotation (cites this spec at its durable `planning-artifacts/specs/`
path, where it is promoted in this same change) — `status` unchanged, still `open` (the ATTENDED
live bring-up itself remains unexecuted and unscheduled by this story). `pixi.toml`'s `httpx`
comment gained the bring-up script as a second, non-test consumer (comment-only; no dependency
change, so `environment.yaml` needs no regeneration). Baseline re-stamped for this spec only in
the same change.

## 2026-08-15 — deferred-work-ledger.md touched again, pass 2 (reconciliation only)

`_bmad-output/projects/pyforge-atlas/planning-artifacts/deferred-work-ledger.md` gained 67 more
entries in a second promotion pass: 64 previously-un-headed Tier-3 findings (the
`bmad-dev-auto step-04 defer category` flat-append format, which predates the `## DW-<id>`
heading convention entirely and so was invisible to the `tier3-only-deferral` detector used in
the first pass) minted fresh `DW-<epic>-<story>-<n>` ids, minus 6 that a bug in the first
extraction script duplicated against already-headed entries (`DW-B2-1/2/3`, `DW-14-2-1`,
`DW-14-3-1/2`) and were removed; plus 3 `review-budget-followup` entries (`DW-1`/`DW-2`/`DW-3`
in Tier-3) that a separate blind spot in the `deferred-work-check` detector — header text swept
into the same paragraph as a `#`-prefixed line and silently skipped by a content-diff check —
also missed. All real findings, unrelated to this spec's `lasuite.py` surface (still `draft`,
genesis-baseline-only). Named here only to reconcile S-13.7; baseline re-stamped for this spec
only in the same change.

## 2026-08-15 — deferred-work-ledger.md touched by an unrelated pre-shutdown audit (reconciliation only)

`_bmad-output/projects/pyforge-atlas/planning-artifacts/deferred-work-ledger.md` gained 3 new
entries (`DW-14-2`, `DW-14-3`, `DW-14-3-2`) during a repo-wide pre-shutdown promotion of every
Tier-3-only `tier3-only-deferral` finding into its tracked twin — real findings from Stories
14.2/14.3's own review passes, unrelated to this spec's `lasuite.py` surface (no story has
touched this spec's own surface yet; this spec is still `draft`, genesis-baseline-only). Named
here only to reconcile the S-13.7 surface-drift gate, since this spec's own `surface:` list
includes the whole ledger file broadly. Baseline re-stamped for this spec only in the same
change.

## 2026-08-14 — Genesis baseline

Spec drafted 2026-08-14 (`status: draft`); no story has touched or reconciled this
surface yet. This baseline is a structural bootstrap only — it establishes the
S-13.7 spec-surface-reconciliation contract (`scripts/spec_surface_check.py
--write-baseline --spec <project>/spec-<slug>`) against the surface's current,
unmodified file state. No governed-file content is claimed reconciled by this entry.
