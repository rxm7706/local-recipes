---
topic: upstream-discovery: sense GitHub-trending momentum + org-audit sweeps and carry conda-actionable candidates into the packaging factory, reframed as a Kedro node/pipeline in the shipped pyforge-atlas dataflow (not a legacy cf_atlas phase)
updated: 2026-07-25T13:02
---

- (decision) Reframe locked: docs/dreams/upstream-discovery.md 2026-07-23 note is authoritative — Phase T is born as a Kedro node/pipeline in the shipped pyforge-atlas dataflow (migration SHIPPED 2026-07-18, 32/32 stories, PRs #58-#105), not a legacy cf_atlas phase. Legacy docs/specs/trendshift-conda-forge.md (status ready, zero implementation) is SUPERSEDED for intent/contract purposes by this kernel; legacy file untouched, listed as a source (not a companion).
- (decision) Verified against shipped pyforge-atlas architecture (ARCHITECTURE-SPINE.md, epics.md D-15): Phase T was modeled ONLY as a conditional dependency (joins the migration surface if it ships before Wave B completes) and was never absorbed into the 32 shipped stories — confirmed not scoped, not assumed.
- (decision) Legacy schema-version language (v29->v30, github_trending_repos/trending_classification/v_trending_candidates tables) is dropped outright, not merely flagged stale: the shipped architecture's 'Dataset schema evolution' convention explicitly states 'No shared SCHEMA_VERSION constant returns' — additive-first per-dataset catalog metadata versioning replaces it.
- (decision) The shipped architecture closes the pipeline set to exactly 7 named pipelines (core, pypi_intelligence, vulnerability, vcs_health, universal_sbom, seed_gaps, derived_artifacts; AD rule: 'New signals join their assigned pipeline, never a new ad-hoc one'). Trending/org-audit discovery has no pre-assigned pipeline -> open question, not asserted; VCS & Health is the closest existing fit (already hosts GitHub live queries) but unconfirmed.
- (decision) Legacy phase-registration mechanics (bootstrap_data.py phase list, atlas_phase.py selectable phases, admin/maintainer/consumer profile gating as literally described) are cf_atlas-orchestrator-specific and dropped; the Kedro equivalent is node/pipeline registration + Dagster job configurations over the same DAG per architecture 5.4 - not asserted in legacy terms, left as an open question for the owning pipeline story.
- (capability) CAP-1 trending ingest: intent - the dataflow ingests GitHub-trending Python repos (daily/weekly/monthly) via HTML-scrape-primary + GitHub Search API fallback, reusing the VCS & Health pipeline's existing GitHub live-query plumbing rather than inventing new fetch code, and never hard-fails the pipeline on scrape drift (WARN + keep prior snapshot). success: a scheduled/triggered run produces a fresh trending snapshot dataset; a scrape-layout-break run leaves the prior snapshot intact and exits non-fatal, caught by a fixture-pinned parser test.
- (capability) CAP-2 tier classification: intent - a classifier node joins each ingested repo against existing atlas signals (on-conda-forge / feedstock enumeration, PyPI universe, PyPI intelligence: license, requires_python, packaging-shape, downloads, readiness) and labels it tier 1 (curated gap, noarch-python-shaped) / tier 2 (broad, compiled, or needing a prerequisite closure) / skip, with an explicit machine-readable reason on every row - never a silent drop, and an explicit 'unclassified-needs-human' escape valve when a joined signal is unavailable (e.g. a degraded/partial run). success: every ingested row in a batch carries a tier or an enumerated skip_reason; a fixture of hand-labeled repos classifies to expected tiers including one already-on-cf skip and one awesome-list skip.
- (capability) CAP-3 operator surface: intent - an operator or agent can query the tiered candidate list read-side, offline-safe, idempotent (no fetch in the read path), filterable by period/tier/top-N/not-on-cf/min-stars, as both a CLI and an MCP tool mirroring the atlas's existing dataset-passthrough MCP pattern. success: the CLI and MCP tool return matching output for the same filters with zero network calls after ingest; JSON output validates against a documented schema.
- (capability) CAP-4 fixed-source audit track (Track B): intent - the same discover-triage-tier-wave-package shape generalizes from the moving trending feed to a fixed candidate source (a named GitHub org, a curated list), reusing CAP-2's classifier instead of a one-off manual audit; the June 2026 microsoft/* org audit (~10-14 recipe gap across 3 waves) is the worked-example precedent, not committed current scope. success: a fixed-source batch (e.g. a new org) produces the same tiered/reasoned candidate shape as CAP-1/CAP-2 output, re-verified against live atlas data rather than the precedent's dated package list.
- (capability) CAP-5 downstream handoff: intent - every surviving candidate (tier 1 or 2) passes a pyforge-doctor-grade health screen (abandonment signal, license clarity) before it is handed to the packaging-factory campaign machinery as a candidate to package, never auto-submitted. success: a candidate reaching packaging carries a recorded health-screen verdict; discovery output alone never opens a staged-recipes PR.
- (constraint) Discovery never auto-submits: output is a candidate list only; a human or BMAD story decides what proceeds to packaging (carried from legacy NG7).
- (constraint) Any conda-forge recipe work downstream of a surfaced candidate is CFE-skill-governed (CLAUDE.md Rules 1 and 2) - this kernel's output feeds that governed workflow, it does not replace or bypass it; recipe authoring is out of this kernel's surface.
- (constraint) Zero new firewall-blocking dependency: both the trending HTML scrape and the GitHub Search API route through the atlas's existing HTTP/auth plumbing (the same path VCS & Health's GitHub live queries already use), so the enterprise/JFrog/air-gapped story is unaffected (carried from legacy FR-1).
- (constraint) No repo is ever silently dropped from a batch: every ingested row resolves to a tier or an enumerated skip_reason (carried from legacy FR-4).
- (constraint) Discovery stays inside the shipped dataflow's own conventions: pipeline/node/dataset registration follows the closed 7-pipeline architecture and additive-first per-dataset schema evolution (architecture AD rules) - no reintroduction of a legacy phase list, profile-gating string, or global schema-version constant.
- (decision) Non-goals carried/reframed: no trendshift.io programmatic scraping (no public API; homage only); no non-Python trending feeds in v1 (Python-scoped, legacy Q3 default); no auto-submission (see constraint); no re-packaging of already-shipped feedstocks; no CUDA-variant recipes as part of discovery output (a packaging-time concern, not a discovery one); the June-2026 org-audit package list is illustrative precedent only, not committed current scope; this kernel does not build the pyforge-doctor health-screen logic itself (owned by the pyforge-doctor project) - it only requires the gate exists before a candidate proceeds.
- (assumption) Track B's org-audit shape is a reusable workflow over CAP-2's shared classifier, not a one-off manual sweep frozen at the June 2026 snapshot - inferred from the Dream's 'Org audit track: systematic sweeps of high-yield orgs' phrasing (present tense, generalized), not a literal restatement of the legacy spec's fixed story list.
- (assumption) Downstream packaging governance (CFE skill Rules 1 and 2) is unchanged by the Kedro reframe, since it governs recipes/ output, not the atlas dataflow that produces candidates.
- (question) Which of the 7 closed pipelines (core / pypi_intelligence / vulnerability / vcs_health / universal_sbom / seed_gaps / derived_artifacts) hosts trending/org-audit discovery, or does it require an architecture correct-course to add an 8th? VCS & Health is the closest existing fit (already hosts GitHub live queries + upstream version tracking) but this is not confirmed against the architecture owner.
- (question) What catalog/dataset names does the discovery output take under the shipped naming convention (<domain>_<entity> snake_case, e.g. basilisk_vulns-style)? No SCHEMA_VERSION concept survives the migration (confirmed), but the specific dataset/pipeline names for trending ingest + classification output are not yet chosen.
- (question) Does the trending Search-API fallback and its rate-limit discipline reuse VCS & Health's existing per-host credential-scoped GitHub dataset (AD-2), or does it need its own dataset-level endpoint/credential entry? Not yet decided.
- (question) Is the June-2026 org-audit candidate list (qlib, PyRIT, promptflow, semantic-kernel, SEAL, DiskANN, microsoft/edit, etc.) still accurate 5+ weeks later - has any candidate since shipped on conda-forge independently, or gone stale/archived? Must be re-verified via lookup_feedstock before any future batch treats it as live scope.
- (question) Is pyforge-doctor's health-screen surface (abandonment, license) ready to gate candidates, or does discovery's first batch have to run without it? The pyforge-doctor project has planning artifacts as of 2026-07-25 (same day) but implementation status is unverified from this kernel.
- (question) Cadence mechanism for re-running discovery (legacy Q4 default: monthly /schedule): does it reuse the shipped migration's Dagster Sensor + RSS/poll-cursor pattern (G3, orchestration/definitions.py UPSTREAM_SENSORS) instead of a fresh /schedule routine? Not yet decided; the sensor substrate already exists and is the more architecturally consistent fit.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6+8): PASS - 5 capabilities (CAP-1..5) all intent+success; CAP-1 intent trimmed of a HOW-ish reuse-plumbing clause (moved to Constraints, no duplication); all 5 constraints rule something out; 7 non-goals; success signal composite of the 5 CAP successes, testable; capability IDs fresh CAP-1..5; lean prose, no throat-clearing.
- (event) Self-validate pass 2 (preservation): found one gap - legacy NG4 (no upstream PRs to discovered repos, present in both Track A and Track B) had not landed; added to Non-goals. Everything else walked claim-by-claim: Dream vision->Why; Q1/Q2/Q3 resolved-defaults->CAP-1/tier-taxonomy.md/Non-goals; G1-G3->CAP-1/2/3; G4-G6 (first-batch execution waves) and the guide deliverable (G7) are implementation/story-level, correctly out of kernel scope per bmad-spec convention; G8/G9 (maintainer+PR citation, CFE retro) already governed standingly by CLAUDE.md Rules 1/2 and CFE SKILL.md, not restated (packaging-factory kernel precedent); Track B background/ruled-out list->org-audit-precedent.md; NG1/NG3/NG5/NG7 landed; NG2 landed via tier-taxonomy.md skip reasons; NG6/NG8 covered by inference (CAP-1 scopes exactly 2 sources; Constraints cover read-only/no-schema-redesign via additive-evolution rule). Wrapper-only content dropped (on record, not silent): legacy v29->v30 schema numbers + table names (github_trending_repos/trending_classification/v_trending_candidates), bootstrap_data.py/atlas_phase.py profile-gating mechanics, exact wave/story numbering (A1-A5/B1-Bn/C1-Cn/D1-D2), the frozen June-2026 org-audit package list as committed scope (demoted to illustrative precedent in org-audit-precedent.md), the trending-discovery.md guide deliverable and CLI-Anything first-recipe execution detail (batch/story-level, not kernel contract).
- (event) Spec finalized (create): SPEC.md + tier-taxonomy.md + org-audit-precedent.md derived from memlog; sources docs/dreams/upstream-discovery.md + docs/specs/trendshift-conda-forge.md (superseded for intent/contract purposes, file itself untouched); status draft (6 open questions pending human/architecture decisions before epics-and-stories); no surface: declared yet (deferred to the pipeline-assignment open question; interim coverage falls under spec-pyforge-atlas's existing src/**/conf/** surface).
- (direction) Terminology (2026-07-25, Charter amendment): this artifact is 'the Spec' — retire 'kernel' (bmad-spec tool-jargon) on next re-derive. Also: the eight are 'Smiths', not 'Forgemasters'.
