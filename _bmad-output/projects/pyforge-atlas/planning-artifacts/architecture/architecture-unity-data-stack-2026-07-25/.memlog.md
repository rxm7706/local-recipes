---
scope: Unity Data Stack platform — workspace substrate, lock architecture, governance, compliance chain, data plane, delivery plane
purpose: build-substrate
altitude: initiative
updated: 2026-07-25T07:02
---

- (event) Headless/express initiative-altitude create. Inputs: PRD (60 FRs / 9 features) + PRD addendum + brief + 2 research reports. Caller asked to resolve the architecture-owned OQs.
- (direction) Paradigm chosen: Declarative Reconciliation across three planes (Workspace / Data / Delivery). Rationale: all three already reconcile a declared desired state into a materialized one - lock->environment, asset graph->data product, git->runtime. One model, three instances; nothing invented.
- (decision) AD-2 resolves OQ-1: Workspace Lock (pixi.lock) is authoritative; PEP 751 pylock.toml is a DERIVED export. Chose option (a) over (b) pylock-primary and (c) split-by-tier. Rationale: conda-native resolution is the product wedge and (b) discards it by switching production to a slim Python base; (a) keeps ONE mirror surface instead of two; pixi-pack/pixi-unpack already present in the intake root and purpose-built for offline transport. Zero-State intent survives as an export+bundle guarantee.
- (decision) AD-3 resolves OQ-16: multi-platform coverage is proven by MATERIALIZATION per platform, not inferred from environment markers. Generation mechanism selected empirically between the uv route and the PDM route; uv is favoured (already a workspace dep at >=0.9.21, and its CycloneDX export may also serve OQ-6).
- (decision) AD-4 resolves OQ-9: Stage and Environment are separate concepts. Stage = validated config record (branch policy, data classification, network, datastore, promotion policy, env ref). Environment = a solve. Intake's 12 stages collapse to ~5 distinct dependency sets; 8 redundant solves removed.
- (decision) AD-5 resolves OQ-9b: package linking stays on editable path installs (stable, already used by the intake root). Native pixi-build workspace members deferred until the preview stabilizes - R-4 argues against preview features on the critical path when a stable alternative exists. Revisit trigger: first non-Python package.
- (decision) AD-6 resolves OQ-4: compliance gate integrated as a CLI in a lean no-default-feature environment. Beats library (dependency-graph coupling), CI-action (breaks NFR-3 local/CI fidelity), and tool-server (adds a runtime service to the gate path). Report is a file, which FR-44 requires anyway.
- (decision) AD-11 addresses OQ-6 direction: SBOM generated from the BUILT ARTIFACT (installed environment), not from the lock. Recovers the dependency graph AND describes what actually ships. Empirical test still required to confirm graph fidelity.
- (decision) AD-9: every gate is a named task; CI invokes tasks and never inlines commands. This is the mechanism behind NFR-3 local/CI fidelity - byte-identical by construction rather than by discipline.
- (constraint) Inherited from PRD as binding: Constitution Art II pixi-first; Art VI 12 stages; Art VII data mesh 3-layer + naming; Art III/IV coverage + quality gates; CRA reporting 2026-09-11.
- (version) Verified 2026-07-25: pixi 0.73.0 (7 conda-forge subdirs) | dagster 1.13.15 (requires-python <3.15,>=3.10) | kedro 1.5.0 | duckdb 1.5.5 | ruff 0.16.0 | pytest 9.1.1 | uv 0.11.32 | deptry 0.25.1 | pip 26.1.2 | CycloneDX 1.7 = ECMA-424 | SLSA v1.2 | PEP 751 lock-version 1.0 | Python 3.13/3.14 bugfix, 3.12 security-only, 3.15 due 2026-10-01.
- (assumption) Stack table pins are floors verified current at authoring; the code owns them once it exists. OCP version NOT verified (docs.redhat.com 403) - left as open question, not invented.
- (question) Unresolved and NOT invented: OCP/Kubernetes baseline version (OQ-17); conda-forge coverage of the full mandated stack (OQ-15); whether osv-scanner reads pixi.lock/pylock.toml (OQ-21); SBOM dependency-graph fidelity (OQ-6, empirical).
- (event) Reviewer gate run headless: lint_spine.py clean (0 findings) + 2 finalize_reviewers lenses as parallel subagents. Currency lens PASS (14/14 verified, 0 stale). Adversarial lens changes-requested (7 holes).
- (decision) Closed all 7 adversarial holes: NEW AD-21 (Stage-differentiating behaviour lives in the deploy overlay, never the shared Environment), AD-22 (one identity shape for every ownership field), AD-23 (one Domain of record per entity per Layer - downstream references, never restates). TIGHTENED AD-2 (all derived artifacts pinned to one Workspace Lock commit SHA per release), AD-9 (globally-unique task names; <target> is always a Package name; tag slices are flags not task names), AD-16 (one mandated contract format + SemVer MAJOR=breaking), AD-17 (each machine-checkable capability names exactly ONE implementing station in the Capability Map; station prose is not the authority).
- (change) Also from adversarial §2-3: AD-6 clarified that the compliance command is itself an AD-9 task; AD-13 explicitly exempts FR-7's compatibility-detection Environment; AD-18 given an enforcement point (message-shape check) plus a declared residual third-party-error scope; AD-19 given an enforcement point (one scan covering credentials AND hostnames/endpoints).
- (event) AQ-8 RESOLVED by the currency lens: a conda-forge dagster build for python >=3.14 exists, so the intake root's <3.14 ceiling is confirmed stale. AQ-1 (OCP baseline) independently re-attempted and confirmed genuinely unreachable - open question correctly stated, not an oversight. uv conda channel lags PyPI by one patch (0.11.31 vs 0.11.32) - noted in Stack.
- (event) spine finalized — 23 ADs, lint clean, status: final
