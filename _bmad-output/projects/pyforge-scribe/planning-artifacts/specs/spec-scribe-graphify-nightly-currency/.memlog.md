---
topic: Nightly compile keeps the graphify code surface
updated: 2026-09-14T00:00
---

- (direction) Operator asked to fix the graphify extract submodule clash and keep code nodes across the 02:30 full rebuild.
- (capability) CAP-1: scribe index build / compile extra ingest resolve graphifyy's lazy public API when a same-named submodule shadows the callable (extract after collect_files).
- (capability) CAP-2: the estate-owned nightly trigger enables SCRIBE_GRAPHIFY_EXTRA for that process only so the full rebuild keeps code-kind nodes; interactive compile stays off-by-default.
- (constraint) AD-6 holds: unset SCRIBE_GRAPHIFY_EXTRA still skips the extra. Explicit 0 on the trigger wins over the nightly default.
- (constraint) graphifyy is a declared run-dep of the pyforge-scribe pixi feature so the trigger's env can import graphify (mirrors cocoindex on the same feature). Still lazy-imported only inside extras/graphify.py.
- (constraint) Missing or failing graphify ingest degrades to a warning on compile -- never aborts the nightly. index build still exits 2 when the package is absent.
- (decision) Not a post-compile index build second hop: compile-with-extra-on is one persist, one rebuild. Not a GitHub Actions workflow.
- (event) Headless express distill 2026-09-13. Self-validate pass 1 coherence PASS, pass 2 preservation PASS.
- (event) Story 8.2 minted in epics.md; durable story spec spec-8-2-the-nightly-compile-keeps-the-graphify-code-surface.md written in planning-artifacts/specs/ (not Tier-3).
- (event) CAP-1/CAP-2 exercised 2026-09-13: official scribe index build wrote 37464 code nodes; pyforge-scribe-nightly-compile then rebuilt 37834 nodes (37464 code, 0 invalidated) — full rebuild kept the code surface.
- (capability) CAP-3: compile_graph reads presentations/<slug>/facts.yaml (Herald fact ledgers) as doc nodes through the same persist port; not graphify, not the 558-file presentations/ tree.
- (constraint) Only presentations/*/facts.yaml (one file per slug). Never project/*.dc.html, fragments, dated Marp, or the deck engine copies. Kind is doc, not code.
- (decision) Operator 2026-09-13: fact ledgers should be added to the compile. Kinship spec-deck-family-currency (Herald owns derivation; Scribe only compiles the derived yaml).
- (gap) Target-list widening (platform + scripts) parked as spec-scribe-knowledge-layers CAP-8 — not a stretch of this spec's CAP-1/2.
- (event) 2026-09-13 first spec-surface baseline after Stories 8.2–8.3 landed.
- (event) 2026-09-13 spec-surface: src/shared/packages/pyforge-scribe/src/pyforge/scribe/compile.py — Story 13.1 planning pointer surface after Story 8.2/8.3 surfaces. No CAP change on this spec.
- (event) 2026-09-13 spec-surface: src/shared/packages/pyforge-scribe/src/pyforge/scribe/compile.py, src/shared/packages/pyforge-scribe/tests/unit/test_compile.py — Story 14.1 named docs extras (how-tos + library-catalog extract). This spec CAP unchanged.
- (event) 2026-09-13 spec-surface: src/shared/packages/pyforge-scribe/src/pyforge/scribe/extras/graphify.py, src/shared/packages/pyforge-scribe/src/pyforge/scribe/cli.py, src/shared/packages/pyforge-scribe/src/pyforge/scribe/compile.py, src/shared/packages/pyforge-scribe/tests/unit/test_extras_graphify.py, .claude/skills/pyforge-scribe/active/pyforge-scribe/SKILL.md — Story 15.1 named graphify target list. This spec CAP unchanged.
- (event) 2026-09-13 spec-surface: Story 17.1 — AGENTS.md / graphify.py docstring / .cursor/rules/scribe-recall.mdc name Marshal codegraph as symbol nav owner; graphify is not the nav API. This spec CAP unchanged.
- (event by claude) Surface reconcile 2026-09-14: local-recipes PR #1354 added a silo pixi dependency (platform-dev feature, commit 5dc73f0660) and a pyforge-station-tests task (local-recipes feature, commit a675e8b009) to pixi.toml -- routine, unrelated to graphify's own nightly-currency scheduling contract. Scoped-stamped: --write-baseline --spec pyforge-scribe/spec-scribe-graphify-nightly-currency.
- (event by claude) Surface reconcile 2026-09-14: `pixi.toml` changed for two reasons, neither of which touches this Spec's own contract. (1) The `frame-preflight` task description was restated for Frame Spec v0.3 (steward Story 53.6 moved Frame identity from `name` to the qualified-ref `identifier`). (2) `pyforge-station-tests` was REPAIRED: it landed on 2026-09-14 as `set -euo pipefail` plus a bash `for` loop, which pixi's task shell (deno_task_shell) cannot parse, so the task errored on its first token and had never run a single station despite CLAUDE.md documenting it as PR CI gate #3. Replaced with a declarative cross-environment `depends-on` over the eight `pyforge-<station>-test` tasks. Verified by running it: all 8 stations green, exit 0. `environment.yaml` re-exported and byte-identical (description/task-wiring edits only, no dependency change). Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (PR #1355 CI remediation): `pixi.toml` gained nine tasks and `board.py` gained coverage pragmas; neither touches this Spec's contract. (1) Eight `pyforge-<station>-coverage-gate` tasks plus a `pyforge-station-coverage-gates` aggregate now mirror `.github/workflows/coverage-gates.yml`'s `named-module-gates` matrix locally -- that gate measures only the modules a branch TOUCHES, so `pyforge.doctor.sources.board` had sat at 70.1% against an 80% floor since the Guildhall console was retired and only surfaced when an unrelated edit touched the file. (2) A `pr-preflight` aggregate runs the four lanes that actually red a PR (`detectors-ci`, `test-ci`, `pyforge-station-tests`, `pyforge-station-coverage-gates`), because two CI failures on #1355 had no local equivalent at all. (3) `board.py`'s retired-Guildhall machinery is marked `# pragma: no cover` with a block comment stating why -- it cannot execute while `retired-console-check` fails CI on reintroduction; module coverage went 72% -> 94% with no test or behaviour change, tracked for deletion as `DW-DASHBOARD-DEAD-CODE-1`. `environment.yaml` re-exported and byte-identical (task-only edits). Scoped-stamped.
