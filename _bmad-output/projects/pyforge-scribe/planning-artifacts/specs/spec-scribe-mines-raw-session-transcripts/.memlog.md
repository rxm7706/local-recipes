---
topic: Scribe reaches past curated memory into raw session transcripts
updated: 2026-09-09T15:53
---

- (capability) CAP-1 the transcript scanner: reads ~/.claude/projects/<repo>/*.jsonl (22 transcripts/161MB confirmed live), surfaces promotion CANDIDATES (discussed-never-curated decisions/facts) with transcript+position provenance, feeding the existing capture --promote review flow — never auto-promoting. The proven precedent: warden's 13 story specs recovered verbatim from transcripts 2026-07-25, done by hand, never generalized. CAP-2 Epic-2 source registration: transcripts join scribe's knowledge-graph compile source list
- (event) Spec authored + decomposed 2026-08-22 as scribe Epic 3 (2 stories)
- (decision by claude) 2026-09-09 THE ONE OPEN QUESTION (SCAN ECONOMICS: "incremental by transcript mtime VS full sweeps") IS CLOSED — ANSWERED AS **BOTH**, ACROSS 3.1 AND 3.3, NOT EITHER (fleet-readiness-decision-batch-2026-09-09 § 2.1 Class A, scribe report Table A / F-8; classification (a) answerable from evidence). `transcripts.py:25-43` ships both halves: the INCREMENTAL half is an optional mtime+size-keyed scan cache (`cache_path` — the module's only write, and only when a caller opts in; `compile.py` points it at the graph store's own gitignored directory) so a re-run over an unchanged surface skips unchanged files, with the curated-overlap check deliberately re-run every scan so a cache hit stays byte-equivalent to a cold scan; the SWEEP half is a triple bound — a file-count cap, a total-byte budget selecting newest files first, and a per-file timeout that abandons a pathological file with partial results rather than hanging the unattended run. Defaults are sized against the live surface measured for DW-FU-3-2-2 (**27 files / 631 MB**), not the Dream's own "22 files / 161 MB", which is itself now 4× stale — the answer was derived against the larger, real number. Retire from `open_questions:` on re-derive.
- (note by claude) 2026-09-09 REALIZED — BOTH CAPS ARE IN EFFECT, NOT MERELY BUILT (batch Table A, realization gate). **CAP-1:** `transcripts.py` (506 lines) scans `~/.claude/projects/<encoded-repo>/*.jsonl` for assistant-authored decision/fact marker phrases, drops anything already covered by curated `.claude/memory/`, and feeds survivors into `promote.py`'s existing proposal-then-confirm gate — read-only against its inputs, zero network calls, pure stdlib. **CAP-2:** the scan is registered as a named compile surface (`compile.py:114-117`, `:174-196`), the sixth of six, under the same provenance discipline (`transcript:` citations, `compile.py:67`). **Exercised:** `.claude/data/pyforge-scribe/transcript-scan-cache.json` exists (10 KB, written 2026-08-27) — the artifact only a real scan produces, alongside a 1.67 MB `graph.json`. Epic 3 is 3/3 `done` (3.1 scanner, 3.2 compile source, 3.3 bounds + schedule). **Residual:** the scan is invoked by hand. The unattended path exists (`graph compile --nightly` is prompt-free and `flock -n`-safe) but no schedule is installed, so "routine promotion sweeps" in the Success signal is still aspirational — vessel: scribe **Epic 8 Story 8.1**, minted this date (batch row C6).
- (event by claude) status: ready -> shipped — 2026-09-09, batch Table A (fleet-readiness-decision-batch-2026-09-09). CAP-1 and CAP-2 are both `done` and exercised, the one open question is answered in code, and Epic 3 closed 3/3; `ready` named a contract awaiting decomposition that has been decomposed and delivered. The owner Dream `docs/dreams/scribe-mines-raw-session-transcripts.md` moves `specified -> realized` in the same pass, on the strength of the scan-cache artifact.
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. CAP-1/CAP-2 preserved; status ready -> shipped; the scan-economics open question retired (ANSWERED comment kept) and its both-halves answer promoted to a Constraint with the shipped bounds; the Why paragraph's 22-files/161MB figure corrected to the measured 27 files/631MB; realization status 'in effect' plus the hand-invoked-schedule residual recorded with vessel Epic 8 Story 8.1.
