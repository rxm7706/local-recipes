---
topic: Marshal retrieve and sessions use --mode planning
updated: 2026-09-13T09:04
---

- (decision) Marshal planning-graph retrieve always passes --mode planning. Portal optional payload mode. Sessions use --mode planning for decisions.
- (capability) CAP-1: render_scribe_recall_argv appends --mode planning by default. Success: scoped retrieve argv contains --mode planning and never --kind or --mode code unless the caller passes mode explicitly.
- (capability) CAP-2: Portal recall_cli_argv adds --mode only when payload.mode is planning|memory|code. Success: empty payload has no --mode and no --kind; payload mode=planning adds the flag.
- (capability) CAP-3: AGENTS.md session path default decision command includes --mode planning. Cursor rule matches.
- (constraint) Do not overload answer() mode=. Do not force portal to planning. Still never --kind.
- (event) Self-validate: CAP-1..3 intent+success; Story 12.1 no-kind lock preserved.
