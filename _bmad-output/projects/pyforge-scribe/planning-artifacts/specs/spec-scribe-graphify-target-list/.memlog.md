---
topic: Graphify target list: packages + platform + scripts; never recipes or repo root
spec: scribe-graphify-target-list
updated: 2026-09-13T08:44
---

- (decision) Hoist parked CAP-8. Named target list, not a bigger hardcoded default and not repo-root ingest.
- (capability) CAP-1: when target is omitted, ingest_repo walks src/shared/packages, src/platform, and scripts (each existing dir). compile extra-on and index build with no --target use that list.
- (constraint) Never add recipes/ or repo root to the default list. Missing optional dirs are silent. Warn only when no default target exists, or when an explicit --target is missing. Explicit --target stays one path.
- (decision) Non-goals: CAP-14 nav owner; recipes/; cocoindex-in-compile; changing off-by-default extra.
- (event) Self-validate: CAP-1 intent+success; silent-missing-optional bends design; non-goals explicit.
- (event) 2026-09-13 CAP-1 implemented: DEFAULT_GRAPHIFY_TARGETS + ingest_repo list walk. Story 15.1.
