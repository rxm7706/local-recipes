---
topic: One navigation owner: Marshal codegraph
updated: 2026-09-13T12:30
---

- (direction by operator) Remint in worktree hub-scribe-17-mint; primary untracked drafts are not git source of record.
- (decision) Operator accepted later-caps:CAP-14: Marshal codegraph.db is the one symbol-nav owner.
- (capability) CAP-1: Session instructions and graphify module state that codegraph owns symbols; scribe --mode code is AST/report only. Success: AGENTS.md outside bmad:context names codegraph as nav owner and forbids --mode code for symbols; graphify docstring says not the nav API; a test fails if those sentences leave AGENTS.md.
- (constraint) Do not delete graphify. Do not default-recall code:. Do not codegraph install --target claude. Do not nightly-graphify recipes/ or repo root.
- (note) Non-goal: Unifying canopy:CAP-14 pgvector/semantic is a different capability (steward 49.7).
- (decision) Parked later-caps:CAP-14 row hoists to this Spec / Epic 17.
- (event) Self-validate Pass 1 (coherence) PASS: CAP-1 intent+success; constraints bend design; non-goals explicit.
- (event) Self-validate Pass 2 (preservation) PASS: Dream constraints and ACs landed; no Epic 44 flip.
- (event by claude) Surface reconcile 2026-09-14: `AGENTS.md` line 49 changed, not touching this Spec's own contract. The planning-edit rule said "epic stories and their ledger keys land in the same commit"; it now also covers EPIC headings and their `epic-<n>` keys, because doctor's `chain-completeness` INV-B gained an epic arm that day (DW-CHAIN-COMPLETENESS-7) comparing `## Epic <n>` headings against `epic-<n>` ledger keys in both directions. Two live orphans had persisted under the old story-only check -- steward Epic 18 was an `###` and marshal Epic 29 had no heading at all -- so an agent following the previous wording literally could still red the detector. Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (second AGENTS.md change, same session): `AGENTS.md` was edited a SECOND time after the entry above was written and stamped -- two `governance-currency:ignore-start/end` wrappers were added around the foundry item 6 block, because `docs/foundry/PIN.md` and `docs/foundry/modes.md` live on B (`rxm7706/python-foundry`) and are named here precisely to say they are NOT on A, so resolving them against this repo was the wrong repo rather than a dead reference. Neither change touches this Spec's own contract. Recorded because the earlier stamp was taken BEFORE this edit and therefore went stale the moment it landed -- the lesson being that a scoped `--write-baseline` is only valid until the next edit of any file in that spec's surface, and this one was re-stamped only after CI caught it.
