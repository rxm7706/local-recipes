---
topic: pyforge-scribe (Scribe) CLI — event-sourced team knowledge: capture/promote (Wave 1), graph compile/recall (Wave 2)
updated: 2026-09-10T14:05
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-scribe/spec-pyforge-scribe`.


- (direction) Distillation, not re-planning: dream+brief+prd(+addendum)+epics are an already-approved, fully-decomposed chain (15 FRs, 9 ADs, 2 epics/waves); capability IDs map to PRD §4 feature groups (4.1 capture/promotion, 4.2 graph compile, 4.3 recall, 4.4 package/CLI), constraints carry architecture ADs 1-9, non-goals from PRD §5, success signal from PRD §7. Architecture spine adopted as companion (diagram+stack+structural seed); addendum's write-boundary detail folded into constraints; legacy claude-team-memory.md is a grandparent input already fully absorbed by the PRD, not re-cited directly.
- (capability) CAP-1 (Capture & Promotion, Wave 1, PRD §4.1/FR-1-8): intent - a developer or agent runs 'scribe capture --type decision --text ...' to land a decision in checked-in team memory the moment it happens, and 'scribe capture --promote' to deliberately move a team-relevant user-local auto-memory entry into team memory with review. success - .claude/memory/<type>/*.md frontmatter (name/description/type in {feedback,project,reference}) is schema-identical to user-local auto-memory; promotion halts after producing a structured proposal and writes nothing until explicit confirmation; a promoted entry is rewritten in team voice (never a verbatim copy) and its user-local source becomes a promoted:true pointer stub, never deleted; re-invoking promotion skips already-promoted entries (idempotent); nothing is written outside .claude/memory/, Scribe's own package/graph-store paths, and the one pointer-stub exception.
- (capability) CAP-2 (Graph Compile, Wave 2, PRD §4.2/FR-9-11): intent - 'scribe graph compile --nightly' reads named tool surfaces the team already uses (.claude/memory/, .memlog.md files across BMAD projects, git history, retros, CHANGELOGs, docs/dreams/) and rebuilds the knowledge graph unattended, on a cadence, with a superseding capture invalidating (never deleting) the prior record. success - every node is traceable to its source file/commit; re-running compile with no new source activity yields an unchanged graph (no duplicate nodes, no spurious edges); a capture naming a prior record as superseded leaves that prior record queryable with ended validity, distinguishable from the current record; compile runs without prompting and without a human present.
- (capability) CAP-3 (Recall, Wave 2, PRD §4.3/FR-12-13): intent - a developer or agent runs 'scribe recall <query>' to get an answer grounded in the compiled graph, identically regardless of which operator or concurrent agent worktree asks. success - every response carries at least one citation resolvable to a real file/record, or an explicit 'no grounded answer found' result -- never a fabricated or generic answer; two different callers querying the same repo state get the identical answer, since the graph is the single shared source, not per-session state.
- (capability) CAP-4 (Package & CLI Surface, cross-cutting, PRD §4.4/FR-14-15): intent - Scribe ships as an installable, pixi-workspace-member package (dist pyforge-scribe, module pyforge.scribe, CLI scribe) with capture/graph-compile/recall as its three independently invocable top-level commands and sole public contract. success - each command is independently testable; other components (Herald, Marshal, Doctor) integrate only via the CLI or a documented, versioned API, never by importing pyforge.scribe internals directly; the three-command CLI skeleton exists from Wave 1's start so the public contract's shape does not change between waves.
- (constraint) AD-1: capture is the single mutation path for decisions/ADRs/runbooks -- append-only, never edited in place, only superseded. scribe graph compile never accepts direct graph edits; the compiled graph is 100% derived and re-computable from source records at any time, from scratch, with the same result. No component queries or mutates the compiled graph store except the compile step (write) and scribe recall (read).
- (constraint) AD-2 (promotion write-boundary): the only write Scribe performs outside .claude/memory/** and its own package/graph-store paths is the pointer-stub rewrite of a confirmed-promoted user-local entry, and only after explicit human confirmation, gated by the idempotent promoted:true check. Scribe MUST NOT write to .claude/skills/ (other than its own), .claude/scripts/, .claude/agents/, .mcp.json, recipes/, _bmad/, _bmad-output/ outside its own project directory, or root CLAUDE.md (that wiring is a human-driven edit, never automated).
- (constraint) AD-3: .claude/memory/<type>/*.md frontmatter (name, description, type) is byte-identical in shape to Claude Code's user-local auto-memory schema; type is exactly one of {feedback, project, reference}, no additional required fields in Wave 1 -- no translation layer that could silently drop or corrupt fields during promotion.
- (constraint) AD-4: fact supersession, never deletion -- a capture naming a prior record as superseded marks that prior record's validity as ended in the compiled graph; it is never deleted or overwritten. Binds at the projection-builder (compile) level, independent of which concrete GraphStore adapter is active.
- (constraint) AD-5: the storage engine sits behind a GraphStore port (write: upsert-node/invalidate-edge-shaped operations; read: query-by-citation-path). No module outside the concrete adapter implementation may import a specific storage engine's client library directly -- prevents a KuzuDB-archival-style premature lock-in recurring inside Scribe. The concrete v1 adapter and the embedded-graph-engine question are Deferred (open question 1).
- (constraint) AD-6 (air-gap by construction): the default configuration performs zero outbound network calls for scribe capture, scribe graph compile, or scribe recall. Any optional network-touching capability (e.g. a future hosted-LLM backend for recall's answer synthesis) is opt-in, off by default, and gated behind an explicit flag. scribe recall's v1 default path must not require network reachability to return a grounded answer.
- (constraint) AD-7: the scribe CLI is the sole public contract; pyforge.scribe's public __init__.py exports only what the CLI needs. Other pyforge-* stations (Herald, Marshal, Doctor) integrate via the CLI (subprocess/CliRunner-style invocation) or a documented, explicitly-versioned Python API -- never by importing pyforge.scribe.compile or pyforge.scribe.graph_store internals directly.
- (constraint) AD-8: every scribe recall response either includes at least one citation resolvable to a real captured record (file path / capture ID / commit), or returns an explicit 'no grounded answer found' result. No code path may return synthesized prose without a resolvable citation attached.
- (constraint) AD-9 (wave boundary): .claude/memory/ frontmatter (AD-3) is a stable input contract for the Wave 2 compile step; Wave 2 development reads that format as-is -- if Wave 2 needs additional metadata, it is additive (new optional fields), never a breaking rewrite of Wave 1's already-shipped schema.
- (constraint) Manual-only invocation (inherited legacy FR-9/D4): no Stop/SessionEnd/PreCompact hook triggers capture in Wave 1 or Wave 2 -- every scribe capture invocation is a deliberate, authored action.
- (constraint) Versioning (PRD §4.4 feature NFR): semver: CLI subcommand additions are MINOR, breaking flag/output-format changes are MAJOR, consistent with this repo's existing skill/package versioning discipline.
- (decision) Non-goal: no ambient/automatic capture -- Scribe never passively mines chat logs, Slack, or session transcripts for decisions; capture is always a deliberate, authored action (scribe capture). Permanent design stance, not a Wave 1 limitation.
- (decision) Non-goal: no Stop/SessionEnd/PreCompact hook automation in Wave 1 or Wave 2 (inherited legacy D4/NG2); a future wave may revisit, out of scope here.
- (decision) Non-goal: no cross-repo synchronization -- each repo's Scribe instance is self-contained (inherited legacy NG5).
- (decision) Non-goal: no plugin/marketplace packaging until a second consumer repo exists (inherited legacy NG3/D5).
- (decision) Non-goal: no claimed recall-accuracy benchmark parity (LoCoMo/LongMemEval-style) with Mem0/Zep/Graphiti -- differentiation is git-native/air-gap/capture-as-you-decide, not raw retrieval performance.
- (decision) Non-goal: no general-purpose enterprise knowledge-search product -- Scribe does not attempt Glean's cross-SaaS-connector breadth; repo-scoped by design.
- (decision) Non-goal: no TTL/decay/staleness auto-scoring on entries (inherited legacy NG7) -- humans prune; Scribe may surface a staleness signal at compile time but never auto-deletes.
- (decision) Non-goal (scoped, not fully excluded): no premature commitment to a specific graph storage engine -- the choice is deferred behind the GraphStore port (AD-5); this Spec fixes the port, not the engine.
- (decision) Success signal (PRD §7): SM-1 scribe capture used at >=1 real decision point within the first month after Wave 1 ships (not retrofitted demo data). SM-2 scribe recall returns a grounded, cited answer to a real 'why did we...' question within the first month after Wave 2 ships. SM-3 100% of promoted team-memory entries carry the correct pointer-stub + promoted:true marker in user-local memory after promotion. SM-4 scribe graph compile --nightly completes unattended, zero manual intervention, across at least 4 consecutive scheduled runs post-Wave-2. SM-5 zero required network calls observed during a capture/compile/recall invocation run with network access blocked. Counter-metrics (do not optimize): SM-C1 a high promoted-entry count with low recall usage indicates hoarding, not value -- entry count alone is not success. SM-C2 do not optimize compile runtime at the expense of FR-10 supersession correctness.
- (assumption) Wave 1 and Wave 2 are sequential, not parallel tracks -- Wave 2's compile step depends on Wave 1's .claude/memory/ layer existing as one of its inputs (PRD [ASSUMPTION 9.1]).
- (assumption) scribe recall's output format (plain text vs structured JSON vs both) is left unspecified at the product level -- treated as an architecture/API-contract decision, no user-facing requirement distinguishes them yet (PRD [ASSUMPTION 9.2]).
- (assumption) The two existing BMAD<->CFE feedback rules (feedback_bmad_uses_cfe_skill.md, feedback_bmad_runs_cfe_retro.md) are Wave 1's seed-promotion proof; no new seed content was invented (PRD D-3).
- (assumption) If Anthropic ships native team-shared memory (anthropics/claude-code#38536) during Scribe's build, .claude/memory/'s file layer may be absorbed into that native surface, leaving Scribe's differentiated value in graph-compile + recall -- a watch-item per the PRD, not currently a blocker or a scope change.
- (question) Open question 1 (PRD §8.1, chain shows mild tension): the architecture's own Deferred section calls for 'a small spike comparing' a flat-file/index adapter against an embedded graph engine before choosing the v1 GraphStore adapter, but epics Story 2.1's acceptance criteria simply names the flat-file/index adapter as the v1 implementation without confirming the spike ran. Is flat-file/index a provisional default still pending that comparison, or a settled v1 choice?
- (question) Open question 2 (PRD §8.2, unresolved through epics): the exact v1 input glob list for scribe graph compile --nightly -- which specific .memlog.md paths across BMAD projects, which retro/CHANGELOG paths, and how much of docs/dreams/ -- is fixed in shape (FR-9, AD-1, AD-9) but not enumerated by architecture or epics.
- (question) Open question 3 (PRD §8.4, untouched by architecture or epics): naming/interop with the docs/adr/-style numbering/format the domain research found as dominant practice -- should scribe capture --type decision formally adopt it, or keep its own vocabulary that happens to be ADR-shaped, and should Scribe read pre-existing docs/adr/-style files in a target repo?
- (event) Chain-resolved PRD open questions folded into Constraints, not carried forward as spec-level open questions: §8.3 (does scribe recall require a local LLM) -- resolved by epics Story 2.4's binding AC that the v1 default requires zero network calls / no LLM, captured under AD-6/AD-8; §8.6 (legacy CLAUDE.md BMAD-CFE de-dup) -- resolved as 'human decides at Wave 1 implementation, not automated by Scribe' by epics Story 1.2, captured under AD-2/AD-7's write-boundary (CLAUDE.md edits stay human-driven). §8.5 (claude-code#38536 watch-item) folded into Assumptions, not Open Questions, since the PRD itself frames it as a non-blocking watch-item.
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6+8): CLEAN. 4 capabilities each with intent+success (rule 1); intents are WHAT not HOW (rule 2); 11 constraints each rule something out -- AD-1..9 + manual-invocation + versioning (rule 3); 8 explicit non-goals (rule 4); success signal ties to named, testable SM-1..5 + 2 counter-metrics (rule 5); CAP-1..4 IDs stable/unique (rule 6); prose kept lean given rich structured input, no elicitation needed (rule 8). No fixes required.
- (event) Self-validate Pass 2 (preservation): walked PRD FR-1..15 (all 15 covered across CAP-1..4 + constraints), the cross-cutting NFRs (air-gap -> AD-6, manual-invocation, no-benchmark-claim -> non-goal, versioning), PRD §5 non-goals (8/8 present), PRD §7 SM-1..5 + counter-metrics C1/C2 (present in Success signal), PRD §8 open questions (3 -> resolved-down-chain and folded into constraints/assumptions per the chain-resolution event entry; 3 genuinely still open -> Open Questions section), PRD §9 decisions/assumptions D-1/D-3/D-4/[9.1]/[9.2] (D-1 folds into CAP-4's 'real package' framing in Why + AD-7; D-3/9.1/9.2 -> Assumptions; D-4 -> AD-6), addendum.md's write-boundary detail (-> AD-2, expanded with the explicit off-limits path list), Dream's three capture/graph/recall claims (-> CAP-1/2/3), Architecture AD-1..9 (all 9 -> Constraints), Architecture diagram/layer-table/Stack/Structural Seed (-> adopted companion ARCHITECTURE-SPINE.md, not duplicated in kernel). Wrapper-only content dropped (logged, not silent): PRD §0 Document Purpose, §3 Glossary term-definitions restating capability wording, §2.3 UJ-1..4 narrative (redundant with capability success criteria), the market-differentiation comparison table (competitive framing, not a build contract), addendum's 'Options considered for Wave 1 implementation shape' (non-binding recommendation, already superseded by architecture's actual Structural Seed commitment) and its sizing-note/legacy-story-cross-reference-table (process/audit detail, not forward-load-bearing). No load-bearing claim found undropped-and-unplaced.
- (note) Surface declared 2026-07-25: src/shared/packages/pyforge-scribe/** — the package this Spec builds (story 1.1 shipped).
- (event) Story 1.3 (promotion workflow — proposal-then-confirm, team-voice rewrite) RECOVERED and landed 2026-07-31. It was DEFERRED on 2026-07-30 with `dev session timeout` at dev x2, and its branch shows **8 insertions** — which reads as "nothing was written". That reading was wrong: `promote.py` (339 lines) and `test_promote.py` (457) existed in a DANGLING commit, `91d3571f10` ("attempt worktree snapshot", author bmad-loop), unreachable from any ref and alive only until the next `git gc`. 1,102 insertions across 6 files. Cherry-picked onto main; clean, no conflicts. scribe's suite 18 -> 47.
- (note) It was found by SCRIBE'S OWN DEV AGENT, not by any tooling. Story 1.4 could not be planned without 1.3's `promote.py`, so the session went looking: it read the sibling worktree, found a stray `promote.cpython-314.pyc` as the only trace on disk, ran `git log --all -- '*promote.py'`, located the dangling commit, confirmed the object still existed with `git cat-file -e`, traced the two consecutive `reset: moving to 491f3ec4` entries in that worktree's reflog, and escalated with the SHA. Its escalation is a better forensic report than anything the harness emits — worth remembering that a BLOCKED dev session can be an investigator, not just a failure.
- (constraint) A story branch's diffstat is NOT evidence that nothing was written. This one showed 8 insertions while 1,102 sat in an unreachable object. Before concluding a deferral was empty, check `git fsck --no-reflogs` for dangling commits in the same window. The rescue is preserved on `rescue/scribe-1-3-dangling-91d3571f` (pushed to origin) so the object cannot be collected.
- (constraint) Landed WITHOUT any review — the deferral was a DEV timeout at attempt 2, so review x0. It has 47 green tests and a full six-station + four-detector gate on the merge result, and no reviewer has read it at all. That is a weaker warrant than the three recoveries earlier today, each of which carried an applied follow-up-review commit. Treat 1.3 as delivered-but-unreviewed; the follow-up review is owed.
- (event) Dream consolidation 2026-08-02: docs/dreams/team-memory.md (its own Dream + Spec, capabilities MEM-1/MEM-2/MEM-3) archived (status: archived, archived-reason: absorbed) and folded into a freshly-rewritten docs/dreams/pyforge-scribe.md — checked capability-by-capability, not assumed identical: MEM-1 (committed, not personal) already covered by CAP-1 + AD-1; MEM-2 (recall is scoped, not a dump) already covered by CAP-3 + the pre-existing MEMORY.md 200-line-index convention; MEM-3 (entries carry provenance, so a stale one is visibly stale) already covered by CAP-2's per-node source/commit traceability plus the existing staleness non-goal. No genuinely new capability found; no CAP/AD added. spec-team-memory rewritten as a retirement record (mirrors spec-sentinel's template).
- (note) Real-world state at 2026-08-02 consolidation: Epic 1 (Team Memory - Capture & Promotion) 3 of 5 stories shipped (1.1 package scaffold + direct capture, 1.2 CLAUDE.md @import wiring, 1.3 promotion workflow recovered+landed 2026-07-31); 1.4 (pointer-stub write-back) and 1.5 (seed-promotion proof) still backlog. Epic 2 (Knowledge Graph - Compile & Recall) untouched: all 4 stories (2.1-2.4) backlog. 3/9 total. .claude/memory/ (feedback/, project/, reference/, MEMORY.md, README.md) is live in the repo today.
- (event) Self-validate on 2026-08-02 update (Spec Law 1-6+8): re-derive was a grounding refresh, not a re-scope. All 4 capabilities (CAP-1..4) and all 11 constraints (AD-1..9 + manual-invocation + versioning) preserved verbatim, IDs unchanged (rule 6). Non-goals (8) and Success signal (SM-1..5 + counter-metrics) unchanged -- team-memory's MEM-1/2/3 fully map onto existing CAP/AD, nothing added (rule 3/4/5 unaffected). Only change: one sentence appended to Why, citing the 2026-08-02 team-memory Dream consolidation and current story completion (Epic 1 3/5, Epic 2 0/4). Preservation pass (rule 7): the new source claim (team-memory Dream consolidation) is now present in Why; no other content changed so no re-walk of the full PRD/architecture chain was needed.
- (decision) DRIFT-PRESUMED RECONCILED, 2026-08-09, marshal S-13.6 (CAP-6 / FR-169). 13 entries, all under src/shared/packages/pyforge-scribe/** — 7 added / 6 changed / 0 removed. Traced: Story 2.1 (GraphStore port + flat-file v1 adapter) 2, Story 1.4 (pointer-stub write-back + idempotent re-invocation) 2, Story 2.4 (scribe recall, grounded cited answers) 1, plus "fix Epic 2 review findings" 2 and a stale-docstring fix. Every referenced story is `done` and inside a contracted capability. Contract unchanged; baseline scoped-stamped with the reasoning recorded.
- (event) Surface reconcile 2026-08-09 — steward story `7-1-one-build-whole-guild` landing (`spec-unified-container` CAP-1), a cross-project fix, not a scribe-authored story. Two governed paths moved: CHANGED `src/shared/packages/pyforge-scribe/src/pyforge/scribe/cli.py` — adds an `@app.callback()` on `app` with an eager `--version` option that echoes `pyforge.scribe.__version__` and exits — the standard Typer idiom, which is NOT what the other stations use: scribe is the fleet's only Typer CLI, and warden/doctor/mason/herald/steward all reach the same contract through argparse's `add_argument("--version", action="version", …)` (marshal through a custom `argparse.Action`). What is mirrored here is the CONTRACT — one version line, exit 0, root-only — not the mechanism; do not cite this entry as a Typer precedent, there isn't one. Closes the one CLI-contract gap named in the container epic's AC (scribe was the other of the two stations with no `--version`). ADDED coverage in `src/shared/packages/pyforge-scribe/tests/unit/test_cli.py` (`test_version_flag_prints_version_and_exits_0`). No capability or AD changed — CAP-4's "independently testable command" property is what this closes, not new scope. Verified on the landing branch: `scribe --version` exits 0; scribe's suite green. Baseline scoped-stamped to this spec only.
- (note) Audit Phase 2 landing (2026-08-10): station chain reconciled — see planning-artifacts/implementation-readiness-report-2026-08-10.md for the full finding set and per-path record; mechanical fixes (rollups/Status-lines/spec-status/promotions/README prose) landed via the audit's Phase 2 PR
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Surface reconcile 2026-08-31: 33 drift-presumed paths since baseline at `09c851f4c610`. Twenty-five commits, Epic 2-3 + Epic 6 scribe delivery + cross-station integration (all done in ledger):
  - Epic 2 (Stories 2.1-2.4): GraphStore port, flat-file adapter, nightly compile, fact supersession, grounded recall — graph/, cli.py, compile modules, tests.
  - Epic 3 (Stories 3.1-3.3): transcript scanner, transcript compile source, nightly bounded compile — compile_surface, tests.
  - Epic 6 (Stories 6.1-6.3): incremental refresh, cocoindex binding, graph-node staleness flag — pixi.toml, pyproject.toml, graph modules.
  - Cross-station: steward GraphStore/PostgreSQL/recall (`44ef5fc4f1a`–`2d264c7f5c2`), pyforge-core atomic-write consolidation (`1d60da0193a`), audit phase 2 station reconciliation (`28551a6b65a`), steward 7-1 `--version` flag (`2026-08-09` entry above), pyforge-core sole-ownership fix (`cfadb82d93`).
  No CAP text change. Scoped-stamped: `--write-baseline --spec pyforge-scribe/spec-pyforge-scribe`.
- (change by claude) Surface touch 2026-09-05 (PR #1045): three meta-test files' whole-branch diff-guard bodies (test_first_portal_slice.py, test_skf_skill_ownership.py, test_station_persona.py) refactored onto the shared mechanics (retro-pyforge-steward-2026-09-04.md action item 11: the fleet's whole-branch diff-guard test mechanics (git subprocess calls computing files/commits changed since origin/main) were consolidated into a shared `pyforge.testing_kit.branch_diff_guard` module (PR #1045).); each guard's enforced policy is unchanged. Scoped-stamped: `--write-baseline --spec pyforge-scribe/spec-pyforge-scribe`.
- (change by claude) Surface touch 2026-09-05 (steward Story 41.3 follow-up review, landing the post-merge follow-up-review commits of Stories 41.3 / 41.4 / 42.1 / 42.2 / 43.2 on `steward/land-followup-reviews-2026-09-05` (cherry-picked from 2eaa1e0d61 / 97d3d5c1d9 / 482f47fa92 / 295257ecc8 / 7e6b6c4d18 — each was committed minutes after its PR merged and never landed)): `src/shared/packages/pyforge-scribe/src/pyforge/scribe/graph_store_pg.py` (docstring: Liquibase changesets `:1`–`:4`) and `src/shared/packages/pyforge-scribe/tests/unit/conftest.py` (`_CHANGESET_SEQ` is no longer anchored to end-of-line, so a `--changeset` header carrying Liquibase attributes such as `runInTransaction:false` — the form `db/README.md`'s exception process mandates — parses). No behavior change to the store; the PG-backed tests still require a live pgvector at 127.0.0.1:5433 (unchanged; they error identically on main without one). Scoped-stamped: `--write-baseline --spec pyforge-scribe/spec-pyforge-scribe`.
- (note) Cross-station surface touch (marshal Story 30.2 review pass, 2026-09-06): tests/meta/test_skf_skill_ownership.py::test_story_does_not_edit_claude_or_agents changed -- the prior unconditional `changed_paths_since(...)` "must not edit" assertion on CLAUDE.md/AGENTS.md is replaced with a well-formedness check on AGENTS.md's bmad:context markers (balanced, at most one block). Root AGENTS.md legitimately gained a bmad:context block as part of Story 30.2's project-context-surface migration (BMAD 6.12 persistent_facts=[] surface migration, D1); the old check would have blocked that sanctioned edit. No scribe skill/persona code changed. Scoped-stamped: `--write-baseline --spec pyforge-scribe/spec-pyforge-scribe`.
- (note) Cross-station reconcile from pyforge-marshal Story 32.2 (spec-fleet-consistency-standard CAP-5): this spec's pyproject.toml raised requires-python ">=3.12" -> ">=3.14", matching the interpreter every pixi env actually installs. No source change.
- (capability) Local-database provisioning added 2026-09-08. This Spec durable GraphStore (Story 28.1) requires a real PostgreSQL with pgvector, and tests/unit/conftest.py fails LOUDLY rather than skipping when none answers on 127.0.0.1:5433 -- deliberate, so an absent database can never read as a green suite. But the pyforge-scribe environment shipped only the CLIENT (psycopg); nothing in the repo provisioned a server. CI supplies a pgvector/pgvector:pg16 service container, so the gap was invisible there and only bit locally, where 18 tests error and the sole recourse was hand-rolling a container -- contradicting platform-dev own pap:AD-16 posture, "all pixi-provisioned, zero containers or managed services". scripts/scribe_pg.py (up/down/status) now initialises and runs a per-user cluster under gitignored var/scribe-pg/, driven by three tasks in a new scribe-pg feature composed into a new pyforge-scribe-pg environment. SEPARATE feature on purpose: pgvector has NO win-64 conda-forge build and the scribe feature inherits the workspace platform list, so folding it in breaks that solve; composing leaves the lean env CI installs untouched. postgresql pinned >=18.3,<19 rather than platform-dev >=17.11,<18 -- that env pairs with psycopg2, while scribe psycopg >=3.3.4 pulls psycopg-c which needs libpq >=18.3; the 17.11 pin was tried first and failed the solve. Verified with the hand-rolled container REMOVED: cluster built from scratch, up idempotent on a live cluster, suite 318 passed exit 0, and proven non-vacuous -- with the cluster stopped the same suite gives 18 errors and exit 1. RECONCILES: scripts/scribe_pg.py (new), pixi.toml, pixi.lock. Scoped-stamped: --write-baseline --spec pyforge-scribe/spec-pyforge-scribe.
- (decision by claude) 2026-09-09 BODY § OPEN QUESTIONS Q3 (ADR NAMING/INTEROP) CLOSED — KEEP SCRIBE'S OWN VOCABULARY; NO ADR NUMBERING (operator, fleet-readiness-decision-batch-2026-09-09 § 2.1 row scribe-B1). The question asked whether `scribe capture --type decision` should adopt the `docs/adr/`-style numbering the domain research found dominant. **No, and it is a Non-goal, not a deferral.** (a) `--type` is closed at three values by AD-3 and enforced — `capture.py`'s type enum and `.claude/memory/README.md` fix `type ∈ {feedback, project, reference}`, and AD-3 requires the schema be "byte-identical in shape to Claude Code's user-local auto-memory schema", which is CAP-1's own success criterion; a fourth `decision` type breaks that parity. (b) This repo's decision-of-record format is not ADR at all — it is the memlog + Realization-log pair the Charter's Lexicon §2 makes constitutional ("derived from an append-only memlog and re-rendered, never hand-patched"), and importing a second numbering scheme is exactly the synonym-smuggled-into-prose that Charter CAP-4 forbids. (c) `docs/adr/` does not exist in this repo, so there is nothing to interop with here. **Split off:** reading pre-existing `docs/adr/`-style files in a *target* repo is a separate, later question — filed as a deferred-work entry (`DW-SCRIBE-2026-09-09-ADR-INTEROP`, scribe ledger), not as an open contract question. Rejected alternative: adopt ADR numbering for portability — weaker, it buys interop with a format this repo does not use at the cost of the one schema constraint CAP-1 is graded on.
- (decision by claude) 2026-09-09 THE THREE BODY `## Open Questions` ITEMS ARE HOISTED INTO FRONTMATTER `open_questions:` ON RE-DERIVE, THEN CLOSED THERE (batch § 2.1 scribe-B1; scribe report F-3). Today the body carries three live questions while `open_questions:` is ABSENT from frontmatter entirely, which is why the readiness inventory records `oq: 0` for a `shipped` Spec — three invisible questions inside the contract downstream binds to is a chain-integrity gap, not cosmetics. Dispositions, all three: **Q1 (flat-file/index provisional or settled?) — ANSWERED, and dissolved rather than decided.** AD-5 was honoured literally: `GraphStore` is a `typing.Protocol` (`graph_store.py:59-83`) with THREE concrete drivers behind it — `FlatFileGraphStore` (same file), `PostgresGraphStore` (`graph_store_pg.py`, owner `steward`, `graph_store.py:53`) and `PlaneGraphStore` (`graph_store_plane.py`, owner `atlas`) — selected at runtime by `open_graph_store(owner=…)` (`graph_store_plugins.py:28-84`). Flat-file is the default, not the choice; the engine spike never ran and no longer needs to. **Q2 (the v1 input glob list) — ANSWERED.** Six named surfaces enumerated at `compile.py:6-27` (`.claude/memory/`, `.memlog.md` files across BMAD projects, git history, retros, CHANGELOGs, `docs/dreams/`), plus a seventh optional graphify surface gated on `SCRIBE_GRAPHIFY_EXTRA` (`compile.py:70-78`, `extras/graphify.py:1-31`); the transcript surface is the only per-user one (`compile.py:23-27`). **Q3 — closed as a Non-goal, see the preceding entry.** Net effect on re-derive: `open_questions: []` with the answers recorded, not an empty key hiding three live items.
- (note by claude) 2026-09-09 CAP-14 IS OWNED BY STEWARD STORY 49.7, NOT BY THIS SPEC (batch § 2.3 C6 / scribe report E-1). The Charter's own rule decides it: "the owner of the outcome writes the story; the owner of the mechanism owns the verb it calls" (`docs/dreams/pyforge-charter.md` §5, Outcome and mechanism). The OUTCOME — a Unifying capability whose named criterion is actually exercised — is Epic 49's subject and steward's; the MECHANISM (`embeddings.py`, `graph_store_plugins.py`, the plane `vss` index) is scribe's, and 49.7 already names exactly those files in its `Surface:`. Minting a parallel scribe-side CAP would fork one decision into two, which is the failure mode that rule was added to close. **`spec-pyforge-scribe` therefore does NOT carry CAP-14** — recorded here so a future pass does not re-mint it. Two corrections scribe supplied to 49.7, applied by steward and re-grounded 2026-09-09: (1) the "promised dual-write" half was a MIS-DESCRIPTION, not a shortfall — `graph_store_plugins.py:60-70` selects exactly one plugin deliberately and says why (`:36-39`), and dual-write would break AD-1's "100 % derived and re-computable" property and give supersession two truths; what CAP-14 actually asks — "the same graph operations pass against both drivers" — IS satisfied and exercised in CI (`tests/unit/conftest.py:119-138` parametrizes over `["flatfile", "postgres"]` with no skip-green; `.github/workflows/pyforge-station-tests.yml:215-238` provides a `pgvector/pgvector:pg16` service on 5433 matching `_DEFAULT_PG_DSN`). (2) The real scope is the SEMANTIC half only — `embeddings.py:14` fixes `EMBEDDING_DIM = 32` over a 7-entry `_CONCEPTS` synonym map (`:24-32`) whose only passing test (`tests/unit/test_recall_semantic.py:33-60`) uses that map's own entries as its fixture — plus the default-mode question, since `recall.py:94` defaults to `mode="lexical"` and `cli.py:316-320` hides semantic behind an opt-in `--semantic` flag, so even a real model leaves the criterion unexercised until the criterion names the mode it is graded against.
- (note by claude) 2026-09-09 BODY § WHY STALE — RE-GROUND ON RE-DERIVE (batch § 2.2 herald/scribe body re-grounding; § 2.4 D1). The § Why paragraph closes "As of that date Epic 1 is 3 of 5 stories shipped (1.1, 1.2, 1.3) and Epic 2 has not started." Live is 19/19 stories across SEVEN epics, all `done`: Epic 1 team memory, Epic 2 the GraphStore port + nightly compile + supersession + recall, Epic 3 the transcript surface, Epic 4 the CAP-18 plugin registration, Epic 5 skill/persona/portal, Epic 6 the graphify + cocoindex ingest extras and the staleness flag, Epic 7 the three docs skills. A `shipped` Spec whose § Why describes a 3-of-9 station is the same defect herald carries (F-11 there): a reader who trusts the prose draws the opposite conclusion from one who trusts the status. Quote the superseded sentence when replacing it, per Charter CAP-1.
- (note by claude) 2026-09-09 BODY § NON-GOALS STALE — THE TRANSCRIPT CLAUSE CONTRADICTS SHIPPED EPIC 3 (batch § 2.2 body re-grounding; scribe report F-5). The Non-goal reads "No ambient/automatic capture — Scribe never passively mines chat logs, Slack, **or session transcripts** for decisions; capture is always a deliberate, authored action, permanently." Stories 3.1/3.2 are `done` and `scribe capture --transcripts` does mine session transcripts for promotion candidates, so the constitutive contract currently reads as a standing veto on its own satellite Dream's CAP-1. The distinction the clause was reaching for survives intact and should be restated rather than deleted: **the scan is deliberate (an operator runs it), read-only against its inputs (`transcripts.py:14-18`), and never auto-promotes** — every candidate goes through `promote.py`'s proposal-then-confirm gate. Passive is forbidden; retrospective is not. Herald handled the identical pattern correctly by striking the superseded non-goal through with a dated amendment (`spec-pyforge-herald/SPEC.md:139`); do the same here, and cite `spec-scribe-mines-raw-session-transcripts` CAP-1.
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. CAP-1..CAP-4 and AD-1..AD-9 preserved, no CAP-14 minted (recorded as a Non-goal boundary owned by steward 49.7); status stays shipped; the three body Open Questions hoisted into frontmatter and closed there (open_questions: [] with all three dispositions recorded as comments), body section removed; Q1's answer folded into AD-5 (three drivers, one-plugin-per-run rationale), Q2's into CAP-2 (six surfaces + the SCRIBE_GRAPHIFY_EXTRA seventh), Q3 into a new ADR Non-goal with the DW-SCRIBE-2026-09-09-ADR-INTEROP split-off; the transcript Non-goal struck through with a dated amendment restating passive-forbidden/retrospective-allowed; § Why re-grounded to 19/19 across seven epics, quoting the superseded sentence.
- (event by claude) Story 8.1 (spec-8-1-the-nightly-compile-gets-a-trigger-the-estate-owns-and-a-freshness-signal-that-proves-it-fired): the nightly compile finally gets a real trigger. scribe graph compile --nightly (Story 3.3) was prompt-free and flock-safe but the only documented trigger was an opt-in, hand-typed crontab -e line nobody installed (fleet-readiness batch 2026-09-09 row C6, a realization-gate gap -- built but not in effect). Added a checked-in, reviewable trigger: src/shared/packages/pyforge-scribe/ops/systemd/pyforge-scribe-nightly-compile.{service.tmpl,timer} (systemd-user, 02:30 daily, Persistent=true), scripts/scribe_install_nightly_trigger.py (renders + systemctl --user daemon-reload/enable --now -- idempotent, replaces crontab -e), and scripts/scribe_nightly_trigger.py (the pixi task pyforge-scribe-nightly-compile invokes: when PYFORGE_GRAPHSTORE_OWNER=steward it starts the local cluster via pixi run -e pyforge-scribe-pg scribe-pg-up or refuses cleanly -- exit 0, never red -- rather than compiling against an unconfirmed PostgreSQL store; otherwise invokes scribe graph compile --nightly and propagates its exit code unchanged; the default FlatFileGraphStore path needs none of this). Added scripts/scribe_graph_freshness_check.py (scope=runtime detector, task scribe-graph-freshness-check under local-recipes, excluded from detectors-ci): reports graph.json's age against the trigger's own 24h period as an advisory finding only -- never a PR gate, never a second verdict. Rewrote docs/cli-runbooks.md 'Installing the nightly trigger' (the new install act) and added its 'Scope note' subsection recording explicitly why this is not a GitHub Actions workflow (a hosted runner has none of the three surfaces .claude/memory/, per-user transcripts, or the gitignored graph store -- HARD boundary for Epic 8). Builds no new compile capability -- cli.py stays read-only reference, untouched. Scoped-stamped: --write-baseline --spec pyforge-scribe/spec-pyforge-scribe.
- (event by claude) Story 8.1 review-patch pass 2026-09-10. Four independent review layers (blind-hunter, edge-case-hunter, verification-gap, intent-alignment) found 20 raw findings on the trigger diff; 8 grouped entries patched, 2 rejected as unlikely/cosmetic, 2 refuted false (a spec-surface-baseline hash update for cli.py/recall.py that the reviewers read as scope creep was in fact pre-existing drift from an unrelated, already-landed commit predating this story), 2 deferred (SCHEDULE_PERIOD_HOURS/timer hand-sync; the "four consecutive scheduled runs" half of CAP-2's success signal, which no repo-local test can assert). Two HIGH findings patched: (1) `scripts/scribe_nightly_trigger.py`'s subprocess calls had no exception handling -- a stale/relocated `pixi` binary crashed with a raw traceback instead of the documented "refuses cleanly" contract; now wrapped in try/except OSError. (2) the rendered systemd `.service.tmpl` never forwarded `PYFORGE_GRAPHSTORE_OWNER` into the unit's environment, so under the actual scheduled firing (which does not inherit the installer's interactive shell env) both the Postgres-preflight branch AND the compile subprocess itself would silently default away from Postgres regardless of an operator's real configuration -- `scribe_install_nightly_trigger.py` now bakes an `Environment=PYFORGE_GRAPHSTORE_OWNER=...` line at install time when set, mirroring how `PIXI_BIN` was already baked in. Also added: a best-effort `loginctl enable-linger` advisory (a systemd-user timer silently stops firing once the invoking user is fully logged out unless lingering is enabled -- neither the installer nor the runbook mentioned this before), exception handling around `scribe_install_nightly_trigger.py`'s filesystem/systemctl calls, a `max(0.0, ...)` clamp on the freshness detector's age computation, a widened GitHub-Actions-boundary test (now also rejects the two new pixi task names, not only the literal `scribe graph compile` string), and runbook lines for disabling the trigger and firing it immediately to validate a fresh install. Re-scoped-stamped: `--write-baseline --spec pyforge-scribe/spec-pyforge-scribe`.
- (change by claude) 2026-09-10, pre-land: `scripts/scribe_install_nightly_trigger.py` redesigned from a single systemd-only installer into a **pluggable four-backend** installer (operator request, before Story 8.1's own PR ever landed). Motivation: the systemd-only path needed `systemctl --user enable --now` and `loginctl enable-linger` before it could be verified -- two actions genuinely outside pixi/conda/PyPI. New default: `crontab`, via the conda-forge `python-crontab` package (verified live on conda-forge, noarch: python) -- writes a plain user-crontab entry, idempotent by a fixed comment marker; standard cron ignores login/linger state entirely, so this backend needs neither systemctl nor loginctl. `systemd` (the original implementation) stays available as an explicit opt-in (`PYFORGE_SCRIBE_TRIGGER_BACKEND=systemd`). Two more backends added for parity: `apscheduler` (conda-forge, verified live) and `supercronic` (NO conda-forge or PyPI package -- verified, manual install only) -- both write their job definition but do not supervise the resulting process themselves, and the installer prints that caveat on every run rather than claiming persistence it cannot provide. `pixi.toml`'s `pyforge-scribe` feature gained `python-crontab` and `apscheduler` as real dependencies (both installed and probed live via the real library APIs, not assumed). `docs/cli-runbooks.md` 'Installing the nightly trigger' rewritten with a per-backend comparison table and one subsection per non-default backend; the `scribe-install-nightly-trigger` pixi task description updated to match. Tests: the crontab backend is exercised via an injected `cron_factory` fake (never touches the real system crontab); the systemd-specific tests now pin `backend="systemd"` explicitly since crontab is the new default; apscheduler's real-import tests are `skipif`-guarded (`pytest.importorskip`-equivalent) since apscheduler is a pyforge-scribe-only dependency not present in every env `tests/scripts/` runs under (confirmed live: they skip cleanly under the lean `pyforge-ci` env, 333 passed/11 skipped there vs. 328 passed/7 skipped under `pyforge-scribe`). No Boundaries & Constraints changed -- the story's own Code Map already left the exact trigger mechanism as "an implementation choice within the story's Surface note". Scoped-stamped: `--write-baseline --spec pyforge-scribe/spec-pyforge-scribe`.
