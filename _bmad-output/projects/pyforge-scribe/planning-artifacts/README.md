# pyforge-scribe — planning artifacts

**Scribe** — team knowledge CLI (`scribe capture/graph/recall`): shared team
memory, a nightly knowledge graph, and recall surfaces. Adopts the legacy
`claude-team-memory.md` spec and Sentinel's unbuilt core. dist `pyforge-scribe`
/ module `pyforge.scribe` / CLI `scribe`.

BMAD Tier-2 output for this project (`research/`, `briefs/`, `prds/`,
`architecture/`, `epics.md`, `specs/`).
