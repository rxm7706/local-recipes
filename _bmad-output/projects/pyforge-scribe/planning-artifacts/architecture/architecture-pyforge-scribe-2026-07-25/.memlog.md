---
scope: Scribe capture/promotion (Wave 1) + graph compile/recall (Wave 2), FR-1..FR-15
purpose: build-substrate
altitude: feature
updated: 2026-07-25T06:13
---

- (note) headless/express — Fast path, no interactive coaching; drafted from prd.md + addendum.md + both research reports + pyforge-warden's shipped packaging precedent (src/shared/packages/pyforge-warden/, pixi-build workspace member)
- (direction) paradigm = event-sourced capture with a derived, rebuildable read-model (CQRS-lite): scribe capture is the write/command path (append-only), scribe graph compile is the projection builder, scribe recall is the query path against the projection only. Chosen because it is literally the shape this repo's bmad-spec .memlog.md discipline already proves out, and matches Graphiti's bi-temporal supersession pattern from domain research without adopting its storage engine.
- (decision) packaging convention ADOPTED verbatim from pyforge-warden (the only shipped sibling pyforge-* package): src/shared/packages/pyforge-scribe/ pixi-build workspace member, src/pyforge/scribe/ namespace package, hatchling backend, [project.scripts] scribe = pyforge.scribe.cli:main, python floor >=3.12 (member-package floor, distinct from atlas's 3.14 repo-wide floor)
- (constraint) storage engine for the graph (FR-9/10/11) stays behind a GraphStore port/adapter per PRD Open Question 1 (KuzuDB archived Oct 2025, LadybugDB immature) — spine fixes the port, defers the concrete engine to Deferred
- (constraint) air-gap posture (PRD SM-5, NFR) elevated to an AD, not left as an implicit property — zero required network reachability by default for capture/compile/recall
- (version) verified against live pixi.toml (2026-07-25): python 3.14.* repo-wide but member packages float their own floor (warden precedent >=3.12); typer >=0.27.0 already in-env; gitpython >=3.1.53 already in-env (compile step's git-history reader); duckdb >=1.5.4 already in-env; pydantic pinned-but-commented in a couple feature blocks -- confirm exact version at epics/implementation time, not asserted here as bound
- (event) spine drafted and finalized in one pass (reviewer-gate/lint_spine.py dispatch skipped -- headless/express chain scope, noted as a limitation in the final report)
- (event) spine finalized; lint_spine.py clean (0 findings) after removing 2 literal TBD placeholder tokens
