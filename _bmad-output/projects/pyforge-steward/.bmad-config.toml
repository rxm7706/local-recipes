output_folder = "_bmad-output/projects/pyforge-steward"

[project]
slug = "pyforge-steward"
description = "Steward — platform/ops CLI (provision, deploy, keys, budgets, incident response). dist pyforge-steward / module pyforge.steward / CLI steward"
status = "active"

[tea]
# Story 31.1 (spec-bmad-suite-lifecycle CAP-4, AD-9 "one path pinned"):
# test-architecture.md's canonical writer for this station. TEA's
# bmad-testarch-test-design ran for real (system-level) 2026-09-07 and its
# equivalence against the generator's Story Coverage Matrix narrowed --
# TEA's risk-tiered template names ~30 of 190 story ids verbatim (it
# samples P0-P3 risks, not an exhaustive matrix). Steward's own generated
# QA doc explicitly defers the full matrix to this generator by name.
# Report: _bmad-output/projects/pyforge-marshal/planning-artifacts/reviews/tea-equivalence-2026-09-07.md
test_architecture_path = "planning-artifacts/test-architecture.md"
test_architecture_writer = "generator"
