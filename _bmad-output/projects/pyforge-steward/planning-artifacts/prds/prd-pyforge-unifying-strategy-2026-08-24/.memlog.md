---
topic: The Canopy mounts the eight stations -- extending src/platform from one station portal to eight with a services tier, event fabric, governed DDL and five-tier station symmetry
updated: 2026-08-25T13:52
---

- (decision) Intent = CREATE. Chain-scoped run folder prd-pyforge-unifying-strategy-2026-08-24 (chain, not project_name -- repo convention puts the chain in the filename AND frontmatter so a detector can cross-check). Steward's prior PRD prd-pyforge-steward-2026-07-25 is a different chain and untouched.
- (decision) 39 FRs over 12 features over 16 CAPs. Features grouped by DELIVERY SEAM rather than by CAP number, because that is also how the epic pass must group them -- CAP-10 and CAP-11 both land in Containment, CAP-15/16 both in the agent-facing tiers, and CAP-2's build and its retirement obligation stay in one feature so the ordering constraint cannot be split across epics.
- (decision) Success metrics stated as BINARY GATES, not targets. No baseline measurement exists for this estate, so any percentage would be invented -- and an invented target propagates into an acceptance criterion nobody can evaluate. The only numbers used are thresholds already fixed externally (30s router timeout) or countable denominators (8 stations x 5 tiers = 40, 4 of 4 invariants, 0 duplicated chrome). Stated explicitly in the PRD so a reviewer does not read it as an omission.
- (decision) Four counter-metrics named, each attacking a way its primary could be gamed: portal count over portal thinness, event throughput over durability, skill count over skill usefulness, migration speed over the FR-23 gate. SM-C3 is the one that matters most -- eight shallow skills satisfy SM-5 and deliver nothing.
- (decision) MVP defined as 'smallest cut where the estate behaves as one system', not 'first demonstrable slice'. Excludes governed DDL (feedstock-gated, reopens shipped stories, no prior art) and flags (five absent feedstocks). Lane 1's BUILD may land in MVP but its REMOVAL may not -- the parity precondition is not compressible.
- (assumption) Flagged the flags deferral as the one most likely to be regretted ([NOTE FOR PM] in 6.2): feature flags would de-risk every other rollout in the chain, so if packaging lands early it should be pulled forward. Recorded rather than silently deferred.
- (event) OQ-4 (portal URL scheme) is NEW, raised by this pass. The pre-audit draft asserted /stations/{station}/ as settled; live code shows the one existing portal at /compliance/. It is a design decision for the architecture pass, not research, and it directly affects FR-2's registration seam.
- (event) OQ-6 (Django patch-level exposure) is NEW, raised by this pass. conda-forge's 5.2 line stopped at 5.2.15 while upstream shipped .16 and .17; whether those carry security fixes is unaudited. If yes this escalates from a constraint to a risk, so it is tracked as a question rather than buried in the NFR.
- (decision) PRD addendum carries mechanism/technical-how per the skill's file-role rule: MCP transport + resumability, Liquibase vehicle + deploy ordering, why FR-22 enforces at the database rather than the framework, flag provider mode, the circuit-breaker wrapper, and why the chrome package is ours. Plus a rejected-alternatives table and the corrections table -- the latter duplicated from the brief addendum deliberately, because a pass reading the Dream directly would re-derive those errors in good faith.
- (event) Dispatched a console parity inventory (SPEC open question console-parity-inventory, assigned to Phase 4 by the SPEC itself). Its output answers FR-6 and is the precondition for FR-7. In flight at time of PRD drafting; FR-6 is written to be satisfied BY that artifact rather than to assume its conclusion.
- (change) Propagated the same four-not-five feedstock correction into FR-33, MVP 6.2, Integration and Dependencies, and the PRD addendum. FR-33 now distinguishes four new feedstocks from the cachebox downgrade build explicitly, because sizing it as a fifth new recipe overstates the work.
- (event) Console parity inventory returned and satisfies FR-6: 23 surfaces, 14 runtime-reproducible, 7 build-time-only, 3 mixed. The 7 collapse to FOUR decisions (live run state is three surfaces but one decision; plus detector verdicts, curated editorial content, journal-derived timing). Two findings changed FR-7's scope: 100+ inbound references to the console path across dreams/specs/presentations/pixi/workflows/tests -- its own story, not cleanup -- and the co-published Kedro-Viz tree is NOT a parity obligation (no inbound link, separate workflow) and must survive the removal.
- (decision) OQ-7 replaced rather than closed. The parity classification is answered, but it raises a genuinely new product question in its place: do live run state and journal-derived timing belong on the front door at all? Yes requires a loop-supervisor service that does not exist (new scope); the published board already degrades honestly to unavailable, so local-only is cheaper. Deferred to the architecture pass rather than decided here.
- (decision) OPERATOR RULING: live run state stays on the front door. The parity inventory recommended dropping it (local-only, matching what the published board honestly does today); operator chose to keep the surfaces and pay for the supervisor service that makes them deployable. Bound as CAP-17 + FR-40/41/42, with a Never: constraint forbidding the supervisor from reading an operator home dir and the front door from falling back to filesystem scraping. Absorbs 2 of the inventory's 4 decisions into 1 capability. Flagged in the FR notes as the one place this chain GREW rather than converged -- it arrived after the CAP set was otherwise settled and deserves a deliberate look at the readiness gate.
- (decision) OPERATOR RULING: uniform /stations/<name>/ prefix for all eight portals; compliance_face moves off /compliance/ behind a PERMANENT redirect (FR-9a -- a supported route, not a shim scheduled for removal). Closes OQ-4. Cost is migrating a shipped URL; benefit is one rule the registration seam can enforce mechanically and a switcher that derives from registration rather than a path list.
- (change) 2026-08-24 operating-model SCP approved: glossary five-tier + work_class/Path B/Tachyon; FR-1/2 registration metadata and no 01/02 tiles; FR-17 identity fields; FR-37/38/39 and SM-5 scoped to 03. No CAP-18.
- (decision) OPERATOR RULING: audit Django 5.2.16/5.2.17 for security content now rather than carrying it as a recorded constraint. If either is security-relevant, a django-feedstock 5.2 maintenance branch becomes a seventh packaging item. Dispatched with the three remaining research questions (mcp-tasks-runtime, mcp-client-revision, liquibase-7791-fixed).
- (event) bmad-prd Validate 2026-08-24: grade Fair. FRs/glossary agree with brief Update; high findings are stale §2 sixteen, §10 OQ-4+CFE, §14 architecture-then-epics. No PRD edits this intent.
- (decision) FR-43 shared contract, FR-44 Warden plugins, FR-45 station process hooks. SM-9. Stale tail fixed: eighteen CAPs, OQ-4 bound, packaging operator-owned, §14 points at Epic 32 not architecture-then-epics-from-18.
- (change) PRD §11: FastMCP vs mcp and Liquibase tracking-schema moved to Answered (canopy AD-5 hybrid, AD-9 one liquibase schema). Only lane1-serves-dw-h3 remains open. *(Footnote 2026-08-25: that remaining-open claim is historical; see next bullet.)*
- (decision) 2026-08-25 PRD §11 still-open emptied: lane1-serves-dw-h3 answered no.
