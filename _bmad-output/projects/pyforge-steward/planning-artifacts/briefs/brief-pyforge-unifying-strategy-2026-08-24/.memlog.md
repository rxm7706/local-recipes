---
topic: Extending the shipped src/platform Canopy from one station portal to eight, with a services tier, event fabric, DDL governance and five-tier station symmetry
updated: 2026-08-31T08:53
---

- (decision) Intent = CREATE (rewrite), not update. The 2026-08-23 draft rests on a greenfield premise the audit disproved; patching it would leave the frame intact. Operator sanctioned overwrite (relocate_then_overwrite, 2026-08-24). Frontmatter records supersedes: and author moves herald -> steward.
- (change) Removed all five quantified outcome claims ('80% reduction in supply-chain maintenance time', 'sub-500ms UI latency', '100% deterministic compliance gates', 'zero runaway AI agent failures', '100% native cross-platform parity'). No baseline measurement exists for any of them in this estate, so each was invented. Skill constraint: briefs do not pad and do not fabricate moats. Replaced with observable end states.
- (change) Removed the dated Gantt (2026-09-01 through 2026-11-17). No such schedule was agreed; sequencing is now expressed as dependency ordering in addendum.md.
- (change) Corrected eight factual claims inherited from the Dream's pre-audit prose: greenfield premise, pyforge_host/pyforge-agent-platform as artifacts, Lane 2 at /stations/{station}/, FastAPI ports :8001-:8008, Wagtail CRX, /mcp/sse, Scribe dual-driver, RFC-5 as written. Each is tabulated in addendum.md with the actual and the consequence, because a downstream pass would otherwise reintroduce them straight from the Dream.
- (event) VERIFIED against live code rather than inherited: warden's portal mounts at /compliance/ (config/urls.py:23), NOT /stations/warden/ -- so CAP-3's URL scheme is an open design point and compliance_face must move or the scheme must accommodate it. And rg for 800[1-8] across src/platform/ returns nothing: the eight-port assignment was fabricated.
- (assumption) Brief asserts the agent is a first-class user (five of sixteen capabilities exist mainly to make the estate legible to non-humans). Derived from the CAP set, not from a stated user requirement -- flagged so the PRD can confirm or correct the emphasis.
- (decision) addendum.md created during this pass (not at finalize) carrying: the corrections table, five sequencing constraints the epic pass must honour, six already-made design decisions with reasons so architecture does not relitigate them, Wagtail's multi-replica deployment constraints, and the adjacent-not-absorbed list.
- (change) Marked the three pre-audit companions (prfaq.md, ux.md, party-mode.md) status: superseded with a header naming their specific stale claims, rather than rewriting them. They all read status: ready while carrying claims the audit disproved -- prfaq 6 stale markers, ux 2, party-mode 0. Their narrative/elicitation value survives; SPEC.md binds.
- (change) Doc-standards review (bmad-review structure+prose) returned NOT-YET with 4 must-fixes; all applied. The load-bearing one: I wrote 'five OpenFeature packages plus Liquibase absent from conda-forge' -- wrong twice. The Dream ruling commits to FOUR OpenFeature feedstocks plus a cachebox 5.x build, and cachebox is NOT absent (conda-forge ships 6.2.5 vs the provider's <6 pin), so it is a downgrade build on an existing feedstock, a materially different size of task. Also fixed: section promised 14 capabilities and described 12 (CAP-7 and CAP-10 appeared nowhere); the agent-as-first-class-user assumption shipped as flat fact when the memlog had explicitly flagged it for surfacing; CodeRed is 3 minor releases behind (7.1 vs 7.4.3), not 2.
- (change) Review found two downstream obligations absent from both brief and addendum: Phase 5 is EIGHT correct-course runs (one per station, Marshal's additionally retiring spec-factory-console), and bmad-correct-course -- not the epic pass -- decides the ledger shape for reopening stories 11.1/11.2. An epic pass reading only these two documents would have produced a plan missing both. Added as sequencing constraints 6 and 7.
- (change) Capability count moved 16 -> 17 across the brief after the CAP-17 ruling; added a paragraph naming it as the one place the chain grew rather than converged, and stating plainly that the replacement is now held to a higher bar than the console it replaces.
- (override) Update not Create: OM bind is a scope/correct-course of an existing extension brief, not a new product. Re-running Create would fight Epics 18-30.
- (decision) Q2 five-tier is 03-only; 01/02 stay spec+script or spec+skill. Reverses this brief's first-ready line that any station missing a tier is unfinished.
- (decision) S-26.3/S-27.1 are operator packaging gates (canopy:AD-16); this chain does not author those recipes. Reverses addendum sequencing item 1 invoking CFE on those stories.
- (change) django-feedstock 5.x branch exists; 5.2.16/17 security but unreachable here. Reverses 'no maintenance branch' / unaudited pin.
- (change) OM Q1-Q8 + canopy:AD-21 table moved to brief addendum; brief keeps a pointer so it stays two pages.
- (event) Phase 5 eight Canopy + eight OM correct-courses landed. Sequencing item 6 is past tense.
- (event) MCP Tasks OQ closed as start/get; remaining open question is lane1-serves-dw-h3 only. OQ-4 URL scheme closed FR-9a. *(Footnote 2026-08-25: lane1-serves-dw-h3 answered **no**; do not reopen.)*
- (decision) PRD confirmed agent as first-class user; assumption from first ready no longer pending.
- (decision) Eighteenth capability is CAP-18 hook-spec contract. Brief next-step is S-18.1 chrome plus S-32.1 shared plugin API.
- (event) chain-currency-sweep cascade (research->brief stale, 2-day grace exceeded): folded technical-pyforge-station-dossier-2026-08-30.md (chain `pyforge-unifying-strategy` § Fleet conventions, all 8 stations + pyforge-core) into the brief. No reversal -- the dossier corroborates two existing claims rather than changing scope: (1) doc-drift pattern across stations (Marshal 4/40 commands documented, Doctor README stale story count, Steward 6/13 duties, Scribe self-contradicting README) added as evidence for the problem statement; (2) core.hooks confirmed live and used by all 7 non-core stations, cited as confirming CAP-18 formalizes an existing convention rather than inventing one. inputs: appended the dossier. updated: bumped to 2026-08-31.
