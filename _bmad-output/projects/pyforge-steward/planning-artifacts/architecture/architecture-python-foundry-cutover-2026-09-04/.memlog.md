---
scope: spec-python-foundry-cutover CAP-1..7 (fnd:) — the estate's move from rxm7706/local-recipes to python-foundry, Phases 0-6; inherits architecture-pyforge-unifying-strategy-2026-08-24 (canopy AD-1..23) and pap:AD-1..17 read-only
purpose: build-substrate (solutioning iteration 1; operator reviews and refines before implementation)
altitude: feature
updated: 2026-09-06T11:12
---

- (direction by user) 2026-09-04: solutioning only (BMAD Phase 3) — no implementation; the operator will review and refine the spine and the decomposition multiple times before Phase 4. Fast path chosen (no live coaching partner in this session); every inferred call carries [ASSUMPTION] for review.
- (direction) Purpose/audience: build-substrate spine for future builder agents plus the operator's review loop; the spine is the only deliverable this iteration (no deck/C4).
- (constraint) Inherit canopy AD-1..AD-23 (architecture-pyforge-unifying-strategy-2026-08-24) and pap:AD-1..17 read-only; cite as canopy AD-n / pap:AD-n; this file's ids are fnd:AD-n outside it. No new AD may weaken one.
- (constraint) Driving input: spec-python-foundry-cutover CAP-1..7 (fnd:) + cutover.md; Dream § Cutover; operator decisions fresh repo / fold docs / carry ops.
- (decision) Paradigm: strangler-fig repository cutover — a fresh lasting root receives the estate by manifest-driven moves over a bounded two-remote interval, the recipe plant is an island with its own lock, the old root becomes a read-only archive. Binds every phase; prevents a big-bang copy with no routing table and an open-ended dual-remote era.
- (decision) AD-1 Fresh root, pinned source [ADOPTED operator]: no history import into foundry; manifest rows carry source_sha; local-recipes archived read-only at the final SHA with a README banner. Prevents two histories and secret-leak carry-over.
- (decision) AD-2 The manifest is the routing table, derived not declared: git ls-files x spec-surface classification -> exactly one of {target path, stays, dies}; move stories consume rows and mark them moved with the foundry commit; an unmanifested move is review-blocking. Prevents two stories routing one path differently or a path silently dropped.
- (decision) AD-3 Two locks, one workspace name: root pixi.toml name=pyforge (estate lock only); factory/pixi.toml + factory/pixi.lock island; no path-dep crosses the island boundary either way; estate lock carries no solver-farm tooling (rattler-build, conda-smithy, conda-forge-pinning). Prevents the 29-env single-lock merge machine (red-team D-2 / R-17).
- (decision) AD-4 Mason reaches the island by manifest path, never by import: MASON_CFE_ROOT resolves to skills/domain/conda-forge-expert; recipe build/submit/update subprocesses run pixi run --manifest-path factory/pixi.toml <task>. Prevents the estate env regrowing the solver farm and a second CFE copy.
- (decision) AD-5 One skills tree, IDE dirs are adapters: estate-authored skills live only in skills/{stations,personas,domain}/<x>/SKILL.md; .claude/skills/<x> and .cursor/skills/<x> are relative symlinks; installer-owned skills (bmad-*, skf-*) stay real dirs where their installer writes; a regular estate skill dir under an adapter is a finding. Prevents divergent SKILL.md copies per IDE.
- (decision) AD-6 The packages fold is a path rewrite, not a rename: src/shared/packages/X -> src/packages/X with distribution and import names unchanged; every consumer path (pixi.toml path-deps ~100 sites, Containerfile COPY x5, five_tier._packages_root, script_map_from_packages_root, marshal-policy globs, spec surface: globs, CI paths) is rewritten in the same story from manifest rows; after 44.4 no src/shared/ exists. Prevents a half-moved tree with two roots.
- (decision) AD-7 The host consumes packages, never copies source (ratifies pap:AD-9 / canopy AD-16): each src/packages/* carries its own pixi.toml as a pixi-build workspace member (pattern already live for pyforge-warden); the platform image installs workspace members; Containerfile has no COPY src/packages and no sys.path insert for a package. Prevents the five COPY lines and the platformapp sys.path drift from being carried.
- (decision) AD-8 Two CI estates, disjoint triggers: estate workflows carry paths-ignore: factory/**; factory workflows carry paths: factory/**; staged-recipes linter (3 workflows), test-linux/macos/windows recipe builds and azure-pipelines are dies in the manifest; environment.yaml is a workflow-produced export or is dropped, never a by-hand step; no maintenance-label ritual. Prevents a recipe PR paying for platform CI and vice versa (R-17).
- (decision) AD-9 Every Epic 44 story is a ledger gate the operator flips: all 44.x held blocked while solutioning is under review; outward stories 44.3 (GitHub repo), 44.9 (conda-forge PRs), 44.10 (disable Azure, archive) additionally require explicit operator confirmation at dispatch; Marshal never auto-flips. Prevents a drain creating a repo or opening upstream PRs unattended.
- (decision) AD-10 Working set, not universe: factory/recipes/ admits a recipe only via a manifest row with reason in {in-flight, sole-maintainer, referenced-by-spec}; factory CI asserts count <= manifest rows. Prevents the 7,855-dir copy.
- (decision) AD-11 Two remotes are a migration interval, not the product: during Phases 1-5 every estate change lands in foundry first; a manifest row marked moved freezes its source path in local-recipes (detector frozen-path-changed); local-recipes accepts only CFE retros until 44.6 and hygiene. Prevents drift between the two trees during the interval.
- (decision) AD-12 The BMAD chain moves whole: _bmad/, _bmad-output/projects/, docs/dreams/ move in 44.5 as one unit; marker + symlinks are per-working-tree state recreated by bmad-switch / bmad-loop-worktree, never copied; the eight ~/.bmad-loops/* homes are re-provisioned against the foundry remote. Prevents a loop home still targeting local-recipes.
- (version) Estate: pixi 0.78.x (registry-enforced by pixi-version-check, 16 sites); Python 3.14.* (canopy AD-23); GitHub Actions for both CI estates. Island (from today's pixi.lock): rattler-build and conda-smithy at the versions the lock resolves — carried as-is into factory/pixi.lock; not web-verified this iteration.
- (assumption) [ASSUMPTION] Cursor and Claude Code both resolve per-skill relative symlinks under their skills dirs; the Dream's 'adapters -> ../../skills/' is per-skill, not a whole-dir link, so installer-owned skills keep working.
- (assumption) [ASSUMPTION] Foundry package versioning, naming, and the conda channel (SelfExplainML) are unchanged by the move; only paths change.
- (question) windows-symlink-adapters: win-64 is in the estate matrix; git symlinks need Developer Mode / core.symlinks on Windows — is win-64 a checkout platform for foundry, or a build-target only?
- (question) runtime-state-home: .claude/data/ (11 GB gitignored: cf_atlas.db, vdb, caches) and .claude/worktrees/ (85 GB) are runtime state under an adapter dir today — where does mutable runtime state live in foundry (XDG-style outside the tree, or a gitignored var/ at root)?
- (question) loop-home-cutover-timing: re-provision the eight ~/.bmad-loops homes before 44.5 (so 44.5 itself can be driven by a loop) or after (so no loop runs during the move)?
- (question) actions-minutes: inherited from the Spec — if the account-wide Actions billing block persists, what is CAP-1's CI evidence?
- (question) planning-history-scope: _bmad-output/projects/* is 69 MB tracked; move all of it (AD-12) or trim the 8 projects' shipped implementation history to their specs/ + ledgers?
- (event) Spine distilled (iteration 1, status draft, 12 ADs); lint_spine.py 0 findings; reviewer gate dispatched: rubric walker + reality-check lens + adversarial-pairs lens as parallel subagents writing reviews/.
- (event) Reviewer gate returned: rubric walker PASS-WITH-FIXES (3 CRITICAL), reality-check (1 CRITICAL, 3 HIGH), adversarial pairs (13 pairs: 4 CRITICAL, 4 HIGH, 5 MEDIUM). Full text in reviews/. Clear fixes applied below; AD ids stable, new ids appended.
- (decision) AD-2 rewritten: manifest rows come from git ls-files (24,858 paths; allowlisted trees .claude/** _bmad/** _bmad-output/** docs/dreams/** docs/governance/** .github/** included), the spec-surface classification supplies owner only (may be unowned), scribe index move-list supplies coupling signals (host pyforge imports, sys.path inserts, five_tier roots, CFE callers, plus a new parent-depth signal for the 59 files computing paths by parents[N]); destination resolved by ordered precedence rules; ambiguous is a row kind that blocks; machine-readable file with per-directory rollups, not a 24k-row markdown table. Closes rubric C-1, reality C-1, adversarial pairs 1-2.
- (decision) AD-3 tightened: the island owns recipe build/lint/submit tooling by role (rattler-build, conda-smithy, conda-build as engine, conda-forge-pinning, grayskull); estate exemptions enumerated by name: warden's test-only differential oracles conda-build + py-rattler-build ([feature.pyforge-warden]) and the pixi-build-python / pixi-build-rattler-build workspace-member backends. Closes adversarial pair 3.
- (decision) AD-4 amended: MASON_CFE_ROOT is a repo root whose marker (_CFE_MARKER = .claude/scripts/conda-forge-expert, resolve.py:100) moves with the cell to skills/domain/conda-forge-expert/scripts; the marker constant and both CFE detectors' path literals are manifest consumers rewritten in 44.6. New AD-13: the CFE cell (skill dir, .claude/scripts/conda-forge-expert/, .claude/tools/conda_forge_server.py, the 76 .claude/scripts refs in pixi.toml, .claude/data/conda-forge-expert/ per runtime-state-home) is one unit with one owner, Story 44.6. Closes adversarial pairs 4-5, rubric AD-4 finding.
- (decision) AD-5 tightened: Claude Code documents per-skill symlink resolution (assumption dropped); Cursor is undocumented and lists .claude/skills/ as a compat discovery location, so .cursor/skills/ adapters are minted only if open question cursor-skill-discovery says they are needed; the installer carve-out names bmad-* and skf-* *installer-written* dirs only — the eight station personas (bmad-agent-<station>) are estate-authored and move to skills/personas/<station>/; SKF compiles into skills/stations/<x>/ as its export root ([ASSUMPTION] skf-export accepts a root; open question skf-export-root). Closes reality M-1, rubric AD-5/AD-17 finding.
- (decision) AD-6 extended: parent-depth path constants are coupling rows (move-list signal), rewritten in 44.4, never a silent wrong-root fallback; scripts/.spec-surface-baseline.json is re-stamped scoped at the end of every move story; CLAUDE.md / AGENTS.md path rewrites go through skf-export (canopy AD-17 is the only writer) — recorded as conflict, not override. Closes rubric C-2, adversarial baseline/AGENTS findings.
- (decision) AD-7 corrected: ten COPY src/shared/packages/... lines (nine django portals + the pyforge-steward library) plus builder-stage COPY . /app; the pixi-build workspace-member pattern is live for all ten pyforge-* packages under the pixi-build preview flag and for none of the seven django-* packages — 44.4 adds a pixi.toml to each django-*; src/platform/ingest/github_projects/* imports pyforge.steward.keys in 8 places (live pap:AD-2 breach) — no path is deleted without its successor named (open question ingest-keys-import). Closes rubric C-3, reality H-2/H-3.
- (decision) AD-8 by classification, not count: every workflow and CI script is a manifest row; rows referencing staged-recipes die (four linter workflows, test-all/test-linux/test-macos/test-windows, scripts/linter.py where the environment.yaml sync check lives, azure-pipelines.yml, .azure-pipelines/, .scripts/). Closes reality/adversarial undercount findings.
- (decision) New AD-14 Repo operational envelope (rubric H-5): visibility decided before 44.3 (open question repo-visibility — dashboard.yml deploys GitHub Pages; visibility drives actions-minutes); default branch main, protected, merge commits only, operator + the marshal bot identity may push; secrets and vars (CRC_PULL_SECRET, HERALD_WEBHOOK_SECRET, six vars.PLATFORM_CI_*) re-provisioned through steward keys (FR-5 inventory) as manifest rows of kind secret with no value in the manifest.
- (decision) New AD-15 Identity strings are manifest rows; env ids are not renamed here: the 333 rxm7706/local-recipes occurrences in 148 files are rewritten from manifest rows of kind identity; the pixi env id local-recipes (974 pixi run -e local-recipes sites, GATE-011-frozen verify_commands) stays until a named rename story, the same rule the Dream applies to [feature.python-agent-platform]. Closes rubric H-4.
- (decision) New AD-16 Detector ranges on a fresh root: the foundry epoch SHA is recorded in the manifest and every git-range detector (cfe_rebuild_guard_check --since, mason_cfe_surface_check) takes it as floor; a zero-commit range is exit 2 could-not-run, never a clean verdict. Closes adversarial pair 4 (vacuous guards).
- (decision) AD-12 extended: after 44.5 the tracked ledger of record is foundry's; local-recipes' ledger is frozen by its manifest row; sprint-ledger-sync runs in foundry; open question loop-home-cutover-timing decides whether 44.5 itself runs under a loop.
- (version) Corrected from the lock: rattler-build 0.75.0 (pixi.toml >=0.75.0; conda-forge current); py-rattler-build 0.72.2 is warden's test oracle, not the builder; pixi-build-python / pixi-build-rattler-build are preview backends pinned by the lock; conda-smithy <4 cap is deliberate (CalVer 2026.x needs the conda pkg absent from the env) — revisit in 44.7.
- (question) cursor-skill-discovery: does Cursor need a .cursor/skills/ adapter tree, or does its documented .claude/skills/ compat discovery make the second tree redundant?
- (question) skf-export-root: can skf-export target skills/stations/<x>/ as its export root (versioned <skill>/<version>/ dirs + .export-manifest.json), or must the adapter symlink point at a version dir?
- (question) repo-visibility: private (Spec assumption) or public? dashboard.yml deploys to GitHub Pages; visibility decides Pages and the Actions-minutes budget (actions-minutes).
- (question) ingest-keys-import: src/platform/ingest/github_projects/* imports pyforge.steward.keys (8 sites) — a live pap:AD-2 breach; successor is the in-process station port (43.3) or the steward keys CLI; decide before 44.4.
- (event) Spine re-distilled after the gate: fnd:AD-1..16, status draft, iteration 1; lint 0 findings; adopted as companion by spec-python-foundry-cutover; Epic 44 ACs updated to the corrected facts. Ready for operator review iteration 1 — not finalized (status stays draft by operator direction).
- (event) Solutioning iteration 2 opened 2026-09-04 on operator review of PR #1041.
- (direction by user) Cutover is a FLAG, not a date: the Dream is evergreen, local-recipes keeps evolving until a transition point (currently after 44.5); the cutover plan must be (1) regenerable from scratch and (2) appendable with new changes since it was last generated.
- (decision) New AD-17 Cutover is a flag: root-of-record flag pyforge.cutover_root in {local-recipes, foundry} on the CAP-13 OpenFeature FILE mechanism (canopy AD-11), read in-process by the host and by the CLIs through pyforge-core; it decides the ledger of record (AD-12), Mason submit/update targets (AD-4, CAP-6), which remote the loop homes track, and which root detectors treat as primary; the transition point is the flip (after 44.5 today); flipping back is the rollback. Answers loop-home-cutover-timing: homes re-provision on the flip.
- (decision) AD-2 amended: the manifest is a generated artifact with two modes — regenerate (from scratch against HEAD) and append (delta from the manifest's recorded source_sha to HEAD: added, renamed, deleted paths gain or update rows; moved rows are preserved); generator lives in pyforge-steward (steward cutover plan --regenerate | --append) and consumes scribe index move-list for coupling; regeneration is idempotent over status.
- (decision) New AD-18 Moves are replays: every move story (44.4 fold, 44.5 estate, 44.6 CFE cell, 44.7 island, 44.8 working set) is a manifest-driven, idempotent apply step (steward cutover apply --phase N) that can be re-run into foundry after every regenerate/append until the flag flips; a hand move that the apply step cannot reproduce is review-blocking.
- (decision) AD-11 amended: before the flip local-recipes is primary and evolves normally (nothing frozen); foundry is a replayed mirror; after the flip the roles reverse and frozen-path-changed applies to local-recipes. AD-12 amended: ledger of record follows the flag, not the story.
- (decision by user) windows-symlink-adapters answered: native estate, remote host — generated links (symlink on POSIX, junction via _winapi.CreateJunction on Windows, no symlinks in git), short-root long-path preflight, no shell-only tasks, a win-64 CI leg; host and supervisor via a remote Linux dev host. Becomes AD-19 and an Epic 44 story.
- (decision by user) cursor-skill-discovery answered: per machine, tool-detected — the link step always generates .claude/skills/, generates .cursor/skills/ only where Cursor is detected or requested; both gitignored; a detector flags a real dir under an adapter. AD-5 amended.
- (decision by user) skf-export-root answered: SKF writes into skills/stations/<x>/; adapters link to the skill root; 44.5 verifies skf-export accepts a custom root. AD-5 amended.
- (decision by user) runtime-state-home answered: gitignored var/ at the repo root (var/atlas, var/cfe, var/worktrees); tool-neutral, short paths, travels with the clone. AD-13 amended (data cell) and structural seed.
- (question) Still open after iteration 2: planning-history-scope, repo-visibility, ingest-keys-import, actions-minutes.
- (event) Iteration 2 distilled: fnd:AD-1..19 (AD-17 flag, AD-18 replays, AD-19 Windows-native estate), status draft; lint 0 findings; Epic 44 now 12 stories, all blocked; four open questions remain (planning-history-scope, repo-visibility, ingest-keys-import, actions-minutes).
- (event) Solutioning iteration 3 opened 2026-09-04 on operator review.
- (direction by user) Dreams only move; the Dream is evergreen, additive and regenerative; adopt the same model for the estate so every capability is realized in foundry by either a complete rebuild or a move.
- (decision) AD-2 amended: the plan has two layers — a capability ledger (one row per capability from the Dreams and the spec-surface owner map: mode rebuild|move|retire, state planned|rebuilding|moving|verified-in-foundry|cut, dependencies) over the file manifest, which exists only under move rows; rebuild rows have no file rows (their files are stays by construction); retire rows are dies.
- (decision) New AD-20 The seed is Dreams plus memlogs: docs/dreams/ and every Spec and spine .memlog.md move unconditionally; SPEC.md, spines, epics and stories are re-rendered in foundry by bmad-spec / bmad-architecture / bmad-create-epics-and-stories; all other planning documents are archived. Prerequisite: memlog fidelity — every hand-edit in a rendered SPEC.md (the unifying strategy first) is folded back into memlog entries and each Spec proves it re-renders without loss (Story 44.13, in local-recipes, before Phase 0). Answers planning-history-scope.
- (decision) AD-18 amended: moves are replays; rebuilds are regeneration drills — Dream -> bmad-spec (from the memlog) -> bmad-architecture (inherits parent invariants) -> epics/stories -> bmad-build drained by Marshal in foundry, the archived code visible only as reference. New AD-21 The archive is the oracle: a rebuilt capability reaches verified-in-foundry only when the archived test suite passes against it or an equivalence check does, plus the Spec's success criteria; a rebuild without the oracle gate is review-blocking.
- (decision) New AD-22 Per-capability state and freeze under one global flag: pyforge.cutover_root stays the only root-of-record switch; each capability row carries its state; the flip is allowed only when every capability it depends on is verified-in-foundry; a capability entering rebuilding or moving freezes its source paths in local-recipes at once (frozen-path-changed keyed by ledger state, not by the flag); steward cutover plan --append reports any source change against a frozen row as a finding. AD-11 and AD-17 amended accordingly.
- (decision) Mode decision rule: scored by four repo signals — Spec fidelity (real memlog vs epics.md husk), coupling (scribe index move-list counts), open debt (deferred-work + red-team findings), irreplaceable state (data, locks, proofs) — operator decides per row. First pass [ASSUMPTION]: move pyforge-core, the CFE cell, atlas pipelines + plane, warden/marshal/steward/herald engines; rebuild scribe, doctor detectors, the seven django-* portals, the host's GitHub Projects ingest (into pyforge-steward behind the station port — answers ingest-keys-import), the skills tree + adapters, runtime-state home; retire Azure, staged-recipes CI, compliance_face, services/.
- (decision) Ordering by dependency, not phase number: 44.13 memlog fidelity -> 44.1 ledger + manifest -> 44.3 -> 44.11 ∥ 44.12 ∥ 44.14 rebuild harness -> pyforge-core (move) -> moved engines -> rebuilt portals and ingest against the moved host -> CFE cell placement -> flip -> island, working set, Mason, archive. Rebuilds run as ordinary fleet drains in foundry under a steward budget ceiling.
- (question) Still open after iteration 3: repo-visibility, actions-minutes.
- (event) Iteration 3 distilled: fnd:AD-1..22 (AD-20 seed, AD-21 oracle, AD-22 per-capability state/freeze), status draft; lint 0 findings; Epic 44 = 14 stories, all blocked; two open questions remain (repo-visibility, actions-minutes).
- (event) Solutioning iteration 4 opened 2026-09-04 on the operator's answers to the last two open questions.
- (decision by user) repo-visibility answered: private, permanently. AD-14 amended: visibility private; Pages on the paid plan (the public site is already served from the private local-recipes); the win-64 leg bills at 2x so the minutes budget is a standing constraint (AD-23); nothing Mason submits carries a foundry URL (binds 44.9). Deferred row "archive history rewrite: only if the archive must be published" stands.
- (decision by user) actions-minutes answered: CAP-1's evidence is a real green run on a registered runner; the AD-19 remote Linux dev host registered as a self-hosted runner is the fallback; a documented fresh-clone run of the estate gates counts as provisional evidence while both runner paths are unavailable; with no evidence path at all 44.3 does not dispatch; steward budget gains an Actions-minutes metering source.
- (decision) New AD-23 CI evidence ladder and the minutes guard: tiers hosted run > self-hosted run on the remote host > provisional fresh-clone run; estate workflows declare the self-hosted label as the runs-on fallback; steward budget check meters the account's Actions minutes (billing API through a user-scoped credential in steward keys, never a token in the manifest) and is read at 44.3's confirmation, by Marshal's foundry drains and by the rebuild harness as their ceiling (Story 44.15; CAP-10). AD-8 unchanged: classification stands, runner selection lives in AD-23.
- (note) Live fact: the 2026-08-30 billing block had cleared by 2026-09-04 (Detectors run 33899470280 ran 77 s on main with real findings); the cause is unchanged.
- (event) Iteration 4 distilled: fnd:AD-1..23 (AD-14 amended, AD-23 added), status draft; capability map gains CAP-10; Epic 44 = 15 stories, all blocked; no open question remains.
- (event) 2026-09-04: first flip under AD-9 — 44-13-memlog-fidelity to backlog (Phase 4 opens for that story; 44.13 runs in local-recipes before Phase 0 per AD-20); no AD changed.
- (decision) 2026-09-06: the foundry Stack table gains a `bmad-*` floor row (bmad-method >=6.12.0, bmad-loop >=0.11.1, skf >=2.1.0 linux-64, CIS, TEA, eval-quality, utility-skills, BMB) — relayed from spec-bmad-suite-lifecycle CAP-9; rendered into the spine by Story 47.4, not by hand now
