# Currency review — ARCHITECTURE-SPINE.md (bmad-suite lifecycle, 2026-09-06)

**Verdict: pass-with-fixes.** Every version and technology in the Stack is live-verified and correct (13/13 recipe = channel = installed via a fresh `steward suite pipeline-truth` run; npm `latest`/`next` tags re-queried today). The gaps are not stale versions — they are assertions about the *project* that were not reality-checked: ~20 story/epic ids that do not exist yet, one parent invariant (fnd:AD-5) read backwards for exactly the skill class this spine provisions, a skill-naming glob that matches 1 of 5 vendor dirs, and three "lives in" cells naming things that are not built.

Lens applied verbatim: *"Verify every committed decision was web-researched or reality-checked rather than asserted from training data … Flag anything that could be out of date and wasn't confirmed against the web, the existing project, or the current starter."* Reviewed read-only against the repo on branch `plan/bmad-suite-lifecycle-2026-09-06` (spine, PRD, Spec and Dream all untracked); spine and memlog untouched.

`SPINE` = `ARCHITECTURE-SPINE.md`, `MEMLOG` = `.memlog.md` (this folder). Other paths are repo-relative.

## Findings

| # | Sev | Finding | Evidence | Suggested fix |
|---|---|---|---|---|
| F1 | **high** | The spine binds to story/epic ids that do not exist in any epics.md or ledger: steward Epics 46/47 (46.1, 46.6, 47.3, 47.4), Story 14.9, marshal 30.5 / Epic 31 (31.1, 31.3, 31.5), doctor Epic 20 (20.2, 20.3), and the SPEC's warden 11 / herald 18 / scribe 7 / atlas 24 / mason 14. The Spec frontmatter asserts they were "decomposed the same day"; the memlog says "hand-authored, same PR" — i.e. planned, not minted. AD-7 ("never mints a story a kin chain already owns") and AD-8 ("every P line names an owner and a story") are stated as if the owners exist. | `SPINE:7` scope "governs steward Epics 46/47 + Story 14.9"; `SPINE:85,91,191-192,196-201`; `MEMLOG:21-22,29`. Last epic per station (`_bmad-output/projects/pyforge-*/planning-artifacts/epics.md`): steward 45, marshal 30 (stories 30.1–30.4 only, `epics.md:4521-4566` "the four stories"), doctor 19, warden 10, herald 17, scribe 6, atlas 23, mason 13. `pyforge-steward/…/sprint-status-ledger.yaml` has no `46-*`, `47-*`, `14-9*` keys (last: `44-15`, `45-2`). `specs/spec-bmad-suite-lifecycle/SPEC.md:3` "decomposed the same day"; its `.memlog.md:30` "Decomposition targets (hand-authored, same PR)". `git status`: all four artifact trees untracked. | Either land the decomposition (`bmad-create-epics-and-stories` + `sprint_plan.py generate` + scoped `sprint-ledger-sync`) in the same PR before the spine goes `status: final`, or mark every such id in the spine as *to be minted* (e.g. "46.6 (planned)") and correct `SPEC.md:3` from "decomposed" to "decomposition targets". Add a memlog `(event)` line recording which. |
| F2 | **high** | Parent invariant fnd:AD-5 is misread for the installer-written class. The spine (a) tags it `[ADOPTED]` — the cutover parent's heading carries no tag (its ADOPTED set is AD-1, AD-17, AD-19, AD-20, AD-23), and (b) says the suite skills under `.claude/skills/` are "adapters … generated after cutover" and that "nothing provisioned here may assume `.claude/skills/` is the durable home". The parent rule says the opposite for this class: *"Installer-written directories (`bmad-*` from the BMAD installer, `skf-*` from the forge) stay real directories"*; only estate-authored skills get per-machine adapters. This changes what the cutover does with every skill this spine provisions. | `SPINE:39` (`[ADOPTED]` + gloss), `SPINE:152` ("adapters … generated after cutover, fnd:AD-5"), `MEMLOG:12`. Parent: `architecture-python-foundry-cutover-2026-09-04/ARCHITECTURE-SPINE.md:119` (heading, no tag) and `:122` (rule text quoted above); ADOPTED headings at `:95,191,203,209,227`. | Drop `[ADOPTED]` from the fnd:AD-5 row. Rewrite the row's "Binds here" and `SPINE:152` to: installer-written suite dirs (`bmad-os-*`, `bmad-testarch-*`, builder, CIS, `skf-*`, `mc-*`) stay real directories under `.claude/skills/` after cutover; only estate-authored routing/persona skills move to `skills/personas/` and get adapters. Re-check whether AD-1's "steward is the only writer of suite skills into `.claude/skills/`" still holds once the tree is `skills/` + links (it does — but say so). |
| F3 | medium | Skill-dir naming asserted from memory: the spine's Naming convention and Structural Seed cite `bmad-bmb-*` for bmad-builder, but the vendor ships `bmad-agent-builder`, `bmad-bmb-setup`, `bmad-eval-runner`, `bmad-module-builder`, `bmad-workflow-builder` — 1 of 5 matches the glob. The spine's own companion register lists them correctly. `bmad-agent-builder` additionally collides with the `bmad-agent-<station>` persona pattern that AD-2 uses as the routing-note home and that the cutover parent moves to `skills/personas/`. Same class of slip for TEA: 8 dirs are `bmad-testarch-*`, the ninth is `bmad-teach-me-testing`. | `SPINE:125,152` (`bmad-bmb-*`, `bmad-testarch-*`); `SPINE:55` (AD-2 `bmad-agent-<station>`). Vendor: `.pixi/envs/local-recipes/share/bmad-builder/skills/` (five dirs above); `…/share/bmad-method-test-architecture-enterprise/workflows/testarch/` (9 dirs incl. `bmad-teach-me-testing`). Register: `specs/spec-bmad-suite-lifecycle/adoption-register.md:40-41`. Cutover parent `:122` ("the eight station personas (`bmad-agent-<station>`) … move to `skills/personas/`"). | Replace the globs with the vendor's real names (or "as listed in adoption-register.md § 2"); add a Naming-row note that `bmad-agent-builder` is a vendor skill, not a station persona, so persona movers/detectors must key on the eight-station roster, not the `bmad-agent-*` prefix. |
| F4 | medium | The skf Stack row records the pixi package (2.1.0, tag == npm tarball — verified) but not the installed `_bmad/skf` module tree, which is a different, unpinned source: `version: main`, `channel: next`, `source: custom`, `repoUrl: armelhbobdad/bmad-module-skill-forge`. AD-8's floor row "skf ≥2.1.0" therefore reads as if the module tree were pinned; the readiness companion already calls this "mixed (dual-installer by design)". | `SPINE:136,91`; `MEMLOG:23`. `_bmad/_config/manifest.yaml:21-28`. `specs/spec-bmad-suite-lifecycle/cutover-readiness.md:20` (P9 "mixed … steward 46.7 (pin)"). `_bmad/custom/config.toml:39` `[modules.skf]`. | Split the row: "pixi CLI 2.1.0 (npm tarball == tag v2.1.0) / `_bmad/skf` module: `main` @ channel `next` from the author fork, unpinned until 46.7". Say which of the two the AD-8 floor governs. |
| F5 | medium | AD-3 asserts "nothing under the repo's `_bmad/` references [the studio]" without checking: the live `_bmad/custom/config.user.toml` already holds a `[modules.manticore]` studio (2026-08-26) with repo-relative paths (`manticore/brand`, `manticore/formats`, …). It is gitignored, so it will not show in a diff, but it is exactly the in-repo studio AD-3 forbids, and 46.6 has no migrate/delete step for it. | `SPINE:57-61`; `MEMLOG:17`. `_bmad/custom/config.user.toml:5-25` (ignored via `_bmad/custom/.gitignore:1`). No `manticore/` or `presentations/_studio/` dir exists. Vendor confirms `mc-setup` writes `[modules.manticore]` into `_bmad/custom/config.toml` (`share/bmad-manticore/README.md:57`, `skills/module-help.csv:4`). | Add to AD-3's Prevents/Rule: the pre-existing local `[modules.manticore]` in this repo's `config.user.toml` is retired (moved to the studio root or deleted) as a 46.6 acceptance criterion; note the check is on the gitignored layer-4 file. |
| F6 | medium | CAP-7's "Lives in" names `evals/…` + "three pixi tasks", and the Structural Seed places `evals/review-catches-planted-defect/` in the repo — none exists. No `eval-quality-*` pixi task is declared; Story 45.2 (which defines them as ACs) is `blocked`. | `SPINE:158,189`. `ls evals/` → absent; `pixi.toml` has zero task headers matching `eval` (only two dependency comments mention eval-quality). `sprint-status-ledger.yaml:165` `45-2 … blocked`; `epics.md:2740-2743` defines `eval-quality-smoke` / `-review-twin-run` / `-review-replay` as 45.2 ACs. | Mark both cells "(45.2, not yet built)" so the spine does not read as describing current structure; keep the task names, they are the contract. |
| F7 | low | nodejs "24.19" is the resolved lock, not the pin: the pixi floor is `>=24.19.0,<27.0,!=25.*` (26.x admissible); what actually holds the env at 24.x is codegraph's run-export `nodejs >=24.19,<25` (the caveman build note says "lift the hold when the env moves to nodejs 26"). A relock could move the row silently. | `SPINE:146`; `MEMLOG:23`. `pixi.toml:40` (floor), `pixi.toml:1799` (caveman/codegraph note). `.pixi/envs/local-recipes/conda-meta/nodejs-24.19.0-*`. | Record "24.19.0 (lock); floor `>=24.19,<27,!=25.*`; held at 24.x by codegraph's run-export" — or drop the row, since no AD depends on it. |
| F8 | low | Registry of record is not stated. CIS 0.3.2 and bmad-builder 2.2.2 are GitHub tags; npm `latest` is 0.1.9 / 1.1.0 (live today), and bmad-loop, utility-skills, module-template, manticore, eval-quality and labs are not on npm at all (404). The values are correct — every recipe sources GitHub — but a reader running `npm view` will call this drift, and pipeline-truth already flags `upstream_npm_github_divergence` for builder and CIS. | `SPINE:137-145`. `recipes/<member>/recipe.yaml` source URLs (GitHub `archive/refs/tags/v…` or `archive/<commit>`; only skf is the npm tarball). `npm view` 2026-09-06: bmad-creative-intelligence-suite latest 0.1.9; bmad-builder latest 1.1.0; bmad-method latest 6.12.0 / next 6.12.1-next.0; TEA latest 1.24.0; skf latest 2.1.0; others E404. `steward suite pipeline-truth` drifts column. | Add a Stack footnote: "versions are GitHub tags/commits as pinned in `recipes/<member>/recipe.yaml`; npm lags or is absent for 8 of 13 — not drift." |
| F9 | low | "121 skill dirs" is a live census hard-coded into an AD (derive, don't declare). It is right today (121 dirs; AGENTS.md pitfall reads the same) and will be wrong after the first provision. | `SPINE:48`; `MEMLOG:15`. `ls -d .claude/skills/*/ \| wc -l` = 121; `AGENTS.md:57`. | Phrase as "every dir under `.claude/skills/` (121 on 2026-09-06, 16 of them skf's)". |
| F10 | low | Install-class vocabulary differs slightly from the code: `suite.py` uses `scaffold-n/a` (spine: `scaffold`) and also defines a `skip` class the paradigm omits; the memlog cites `fnd:AD-6` for "skf-export rewrites AGENTS.md", which the cutover parent attributes to canopy AD-17 (AD-6 covers the path rewrite). | `SPINE:26-27`; `MEMLOG:28`. `src/shared/packages/pyforge-steward/src/pyforge/steward/suite.py:67-77`. Cutover parent `:63,67` (canopy AD-17). | Use the `suite.py` constant names verbatim; cite "canopy AD-17 via fnd:AD-6" in the routing-note question. |

## Verified — no action

Reality-checked against the repo or a live query on 2026-09-06; the spine's assertion holds.

- **Versions (Stack, `SPINE:134-145`)**: all thirteen match `recipes/*/recipe.yaml` = SelfExplainML channel = installed conda-meta, confirmed by a fresh `pixi run -e pyforge-steward steward suite pipeline-truth` (13 packages, 0 version drifts; `wired` drifts on TEA/builder/utility-skills/manticore are the unwired modules the spine sets out to wire). `.pixi/envs/local-recipes/conda-meta/`: bmad-method 6.12.0, bmad-loop 0.11.1, skf 2.1.0 (build `_1`), CIS 0.3.2, TEA 1.24.0, builder 2.2.2, utility-skills 2.0.0, manticore 3.1.0.dev0, labs 1.0.0.dev0, eval-quality 0.2.0.dev0, module-template 0.1.0, bmad-dashboard 1.2.2.dev0; mybmad-dashboard 0.1.0.dev0 in `.pixi/envs/bmad-ui/`. `_bmad/_config/manifest.yaml:2,8,15` core/bmm 6.12.0. npm `next` 6.12.1-next.0 live.
- **Platform split (AD-8, `SPINE:91`)**: skf only in `[feature.local-recipes.target.linux-64.dependencies]` (`pixi.toml:1790`); eval-quality in linux-64 + osx-arm64 (`:1791`, `:1812`); win-64 table empty (`:1805`).
- **Parents**: steward AD-1 `[ADOPTED]`, AD-5, AD-7, AD-8 `[ADOPTED]` exist with the titles quoted (`architecture-pyforge-steward-2026-07-25/ARCHITECTURE-SPINE.md:56,81,95,101`); cutover AD-5, AD-12, AD-20 `[ADOPTED]` exist (`…cutover-2026-09-04/ARCHITECTURE-SPINE.md:119,161,209`). The cutover Stack (`:249-260`) has no `bmad-*` row yet — AD-8's "gains a floor row" is correctly forward-looking.
- **Hazards named in AD-1**: `cleanup-legacy.py` `rmtree` of `_bmad/core/config.yaml` — `provision.py:47-52` (the file exists, `_bmad/core/config.yaml`); `bmad-module-skill-forge uninstall` — the CLI really has `install`/`update`/`status`/`uninstall` (`…/bmad-module-skill-forge/tools/cli/skf-cli.js:2-5`) and the hazard is recorded at `AGENTS.md:57`.
- **`_SUPPORTED_MODULES` = {bmb, tea, cis, utility-skills, manticore}** (`provision.py:56-57`) matches the five members the Stack labels `module`; manticore installs repo-rooted via `CondaInstallBackend` (`provision.py:368`), so the `--custom-source` studio path is the right call — and the flag exists in the installed 6.12.0 installer (`…/bmad-method/tools/installer/commands/install.js:36`).
- **AD-4 lenses**: `tea-test-review` bin present with `--min-score` (`share/…enterprise/README.md:305` example 80; `cli/lib/parse-report.js:1061`); `bmad-os-review-pr` and `bmad-os-findings-triage` ship in utility-skills 2.0.0; TEA has 9 workflows + agent `bmad-tea`.
- **AD-6**: `steward suite pipeline-truth` exists with a per-class `wired` probe (`suite.py:503`, `cli.py:819-835`).
- **Plugin-path**: `npx skills add bmad-labs/skills --skill <name>` is the documented form (`share/bmad-labs-skills/README.md:36`); the `skills` CLI is live on npm (1.5.23).
- **Counts**: 10 `bmad-cis-*`, 16 `skf-*` in `.claude/skills/`; 15 `mc-*`, 22 labs skills in the packages; `cutover-readiness.md` has exactly P1–P17; `suite-members.yaml` lists 13 active + 4 deprecated; `evals/` absent (F6).
- **`installShims: true`** (`manifest.yaml:5`) — the live install still ships shims, consistent with CAP-10 being open.

## Method

Read-only. Files: the spine, memlog, Spec + four companions, PRD, both parent spines, `recipes/bmad-suite/suite-members.yaml`, `recipes/<member>/recipe.yaml` ×13, `pixi.toml`, `_bmad/_config/manifest.yaml`, `_bmad/custom/*.toml`, `provision.py`, `suite.py`, `upgrade.py`, each station's `epics.md` and `sprint-status-ledger.yaml`, `AGENTS.md`, and the installed packages under `.pixi/envs/local-recipes/{share,lib/node_modules}`. Network (fail-open, ≤60 s each): `npm view` for bmad-method, bmad-module-skill-forge, bmad-method-test-architecture-enterprise, bmad-creative-intelligence-suite, bmad-builder, bmad-utility-skills, bmad-module-template, bmad-loop, bmad-manticore, bmad-eval-quality, bmad-method-ui, `skills`; one `steward suite pipeline-truth` run (7 fail-open upstream probes returned nothing — GitHub unauthenticated, expected).
