---
scope: spec-bmad-suite-lifecycle CAP-1..10 — how thirteen suite members reach the tree, who wields them, how the estate stays current and cutover-ready
purpose: build-substrate
altitude: epic
updated: 2026-09-06T11:28
---

- (event) 2026-09-06 bmad-architecture create, headless, epic altitude; driving inputs: SPEC.md + 4 companions, prd.md + addendum.md; parents inherited read-only: steward spine (AD-1 wrap-never-reimplement, AD-5 provision wraps Marshal-owned machinery, AD-7 Duty protocol, AD-8 exit codes) and the cutover spine (fnd:AD-5 one skills tree / adapters generated, fnd:AD-12 the BMAD chain moves whole, fnd:AD-20 seed is Dreams plus memlogs)
- (constraint) Inherited steward AD-1 [ADOPTED]: wrap, never reimplement — every suite member's own installer stays the writer of its module tree; steward drives it
- (constraint) Inherited steward AD-5: provision wraps, never forks, Marshal-owned machinery (loop homes, harness policy) — the harness flip is marshal's story, steward only re-renders on request
- (constraint) Inherited steward AD-7/AD-8: one Duty protocol, exit-code sole ownership — new flags (--no-shims) and reports ride the existing upgrade duty, never a new exit domain
- (constraint) Inherited fnd:AD-5 [ADOPTED]: one skills tree; IDE directories are adapters generated per machine, never tracked — nothing this spine provisions may assume .claude/skills is the durable home after cutover
- (constraint) Inherited fnd:AD-12: _bmad/, _bmad-output/projects/ and docs/dreams/ move as one unit; marker + symlinks recreated never copied; loop homes re-provisioned at the flip with no loop running
- (constraint) Inherited fnd:AD-20 [ADOPTED]: seed is Dreams plus memlogs; every Spec/spine/epic re-rendered in foundry — this spine's memlog is what moves, the rendered file is derived
- (decision) AD-1 One install path per class; steward is the only writer of suite skills into .claude/skills [ADOPTED — provisioning spec + install-class playbook]. Binds CAP-2, CAP-5, CAP-6. Prevents hand copies, cleanup-legacy.py wiping _bmad/core/config.yaml, bmad-module-skill-forge uninstall deleting 121 dirs, two copies of one skill. Rule: module class via steward provision --module; own-installer via the member's installer; plugin-path via npx skills add --skill by name; scaffold never provisioned; a manual copy is a defect
- (decision) AD-2 Routing lives with the wielder. Binds CAP-3. Prevents two stations reaching for one skill under different contracts; routing rotting in CLAUDE.md. Rule: exactly one wielding station per adopted skill, recorded in that station's persona skill (bmad-agent-<station>) and the AGENTS.md managed block, never CLAUDE.md; the register § 2 table is the index; a second station wanting a skill changes the row, not the persona
- (decision) AD-3 The studio is a separate root. Binds CAP-5. Prevents manticore's wipe-and-reinstall ritual touching this repo's _bmad/ and _bmad-output/. Rule: the studio has its own _bmad/ and _bmad/custom/config.toml; nothing under the repo's _bmad/ references it; the render artifact is gitignored; the studio path is a register cell (open question: in-repo gitignored vs ~/pyforge-studio)
- (decision) AD-4 Advisory lenses never gate. Binds CAP-4, CAP-7, warden relays. Prevents a second PR verdict; eval trials or TEA scores joining detectors-ci. Rule: tea-test-review, bmad-os-review-pr/findings-triage and eval-quality outputs are findings beside Warden's gate (severity warn at most) or lenses inside the marshal review step; none is a pixi detectors/detectors-ci member; the gate's exit code is unchanged by them
- (decision) AD-5 Retire behind equivalence. Binds CAP-4, CAP-10. Prevents deleting a repo mechanism before its replacement is proven (the generator, its meta-tests, the shim callers). Rule: a repo-owned generator/test/caller is deleted only in the same story that records an equivalence check (generator output vs TEA output; old skill id vs live id) and the deletion happens after the check passes; a failed check narrows the capability, never forces the delete
- (decision) AD-6 The register is the wiring ledger. Binds CAP-1. Prevents wiring by side effect (a provision run nobody recorded) and pipeline-truth drifting from intent. Rule: a member's verdict/wielder/path change lands as a register row edit first; steward suite pipeline-truth's wired column agreeing with the register is the test; disagreement is a finding on the register, not on the tool
- (decision) AD-7 One cadence, mechanisms stay with their chains. Binds CAP-8, CAP-9, CAP-10. Prevents duplicate stories across chains and a second owner for the apply. Rule: release-cadence.md orders detect→catalog→pre-flight→apply→prove-landed→era round→suite refresh→flips→record; core-upgrade owns apply + --no-shims (14.9), era-alignment owns harness + guard + rulebooks (30.x), eval-quality owns the pilot (45.2), the cutover Spec owns Epic 44; this chain relays by memlog and never mints a story a kin chain already owns
- (decision) AD-8 Readiness is a gate with owners, and the foundry stack carries the bmad floor. Binds CAP-9. Prevents opening the foundry with a red BMAD line; a lean pixi.toml with no BMAD toolchain. Rule: cutover-readiness.md P1-P17 each name an owner and a story; Story 44.3 is not flipped while any P line is red; the cutover spine's Stack gains a bmad-* floor row (method >=6.12.0, loop >=0.11.1, skf >=2.1.0 linux-64 only, CIS, TEA, eval-quality, utility-skills, BMB) rendered by steward 47.4, win-64 excludes skf + eval-quality
- (version) bmad-method 6.12.0; bmad-loop 0.11.1; bmad-module-skill-forge 2.1.0 (tag v2.1.0 == npm tarball, verified 2026-09-06); bmad-creative-intelligence-suite 0.3.2; bmad-method-test-architecture-enterprise 1.24.0; bmad-builder 2.2.2; bmad-utility-skills 2.0.0 @HEAD; bmad-manticore 3.1.0.dev0 @c9bcf759; bmad-labs-skills 1.0.0.dev0 @HEAD; bmad-eval-quality 0.2.0.dev0 @3172162f; bmad-module-template 0.1.0; bmad-dashboard 1.2.2.dev0; mybmad-dashboard 0.1.0.dev0 — all read from steward suite pipeline-truth 2026-09-06 (recipe = channel = installed); nodejs 24.19 in the pixi env; npm next tag 6.12.1-next.0
- (assumption) steward provision --module manticore stays repo-rooted; the studio uses the native --custom-source path (PRD addendum) until a --studio flag earns its keep
- (assumption) TEA's workflows cover the generator's output (equivalence check decides; AD-5)
- (question) Studio root: presentations/_studio/ (gitignored, in-repo) or ~/pyforge-studio/ — decide at 46.6
- (question) tea-test-review --min-score for the lens/advisory — decide at marshal 31.3
- (question) Routing-note home after cutover: persona skill AND AGENTS block, or block only (skf-export rewrites AGENTS.md, fnd:AD-6) — decide at 46.1
- (event) Deferred (this spine will not decide): the --studio flag on steward provision; the per-station mapping of TEA's nine workflows (marshal 31.1); the frozen-path-changed and render-HALT detector designs (doctor 20.2/20.3); eval trial cost ledger (45.2); loop-home readiness contents (marshal 31.5); PROJECTS.md cutover layout wording (47.3)
- (event) Reviewer gate — lint_spine.py: 0 findings. Currency lens (review-currency.md): pass-with-fixes — applied: fnd:AD-5 row corrected (installer-written bmad-*/skf-* dirs stay real directories — the carve-out; only estate-authored skills get adapters; no [ADOPTED] tag added to a parent AD that has none), BMB's five real skill dirs named and bmad-agent-builder ruled a vendor skill never a persona, skf Stack row states the registered module is main@next until 46.7 pins v2.1.0, nodejs floor recorded, registry-of-record note added, AD-3 names the stale gitignored [modules.manticore] user-layer block 46.6 retires, evals/ marked planned, '121 dirs' → 'every skill dir', scaffold-n/a naming. Finding 1 (bound story ids did not exist) resolved the same session: epics hand-authored and ledger keys minted for all eight stations (steward 283 / marshal 270 / doctor 113 / warden 65 / herald 100 / scribe 33 / atlas 137 / mason 89 keys)
