---
scope: Steward v1: pyforge-steward CLI, keys/deploy/provision/budget duties, pixi workspace member mirroring pyforge-warden
purpose: build-substrate
altitude: feature
updated: 2026-07-25T06:14
---

- (decision) Paradigm: hexagonal/ports-and-adapters — CLI is the driving adapter, each of the 4 duties is a port with a thin adapter delegating to an existing external tool (_http.py, age, pixi, gh); mirrors Warden's Protocol-based Engine/Policy interfaces.py shape, generalized to a Duty protocol.
- (constraint) Verified by direct read: pyforge-warden's cli.py uses argparse with subparsers + a strict exit-code sole-ownership convention (main() catches KeyboardInterrupt/SystemExit/Exception, never lets a duty dictate the process exit); interfaces.py defines Protocol-typed Engine/Extractor/Router/Policy + a frozen EngineResult dataclass. Steward mirrors both conventions.
- (constraint) Verified by direct read: repo-root pixi.toml wires pyforge-warden as [feature.pyforge-warden.dependencies] path-dependency into a lean no-default-feature env, with -build-conda/-build-dist/-test/-dogfood tasks; pyforge-atlas already mirrors this shape verbatim (its own ARCHITECTURE-SPINE.md's 'Packaging & namespace (warden-aligned)' convention row). Steward is a third instance of the same convention.
- (decision) AD-2: keys' host-scoped credential resolver imports/delegates to _http.py's skip_auth-guarded auth_headers_for rather than reimplementing host-scoping (FR-1).
- (decision) AD-3: at-rest secrets are age-encrypted files in Git; age is a required external pixi run-dependency (never vendored), mirroring how Warden treats deptry/osv-scanner (FR-2, PRD D2).
- (decision) AD-5: provision shells out to pixi install -e <name> / scripts/bmad-loop-worktree; Steward's own code never mutates pixi.toml's [environments] table, only reads it (FR-12-15, PRD D4 — Marshal retains ownership of the underlying worktree machinery).
- (decision) AD-6: budget v1 is declared-not-enforced; steward budget check returns a dedicated not-configured exit code distinct from under/over, never fabricates a number (FR-16-18, PRD D1).
- (question) PRD OQ1 (credential inventory scope) resolved: keys list distinguishes issued (Steward-minted age identities) vs observed (pre-existing repo secrets like GITHUB token, sk-ant key) entries — both included for FR-4 drift-audit visibility, tagged by provenance.
- (question) PRD OQ2 (host-allowlist config source) resolved: HTTP host-scoping reads _http.py's existing resolve_*_urls/*_BASE_URL table directly (no duplicate config); non-HTTP credential scope metadata (e.g. an age identity's declared purpose) lives in the new .steward/keys-inventory.yaml.
- (question) PRD OQ3 (budget config location) resolved: repo-root .steward/ dotdir, tracked, independent of the active BMAD project — mirrors .bmad-loop/policy.toml's precedent for repo-root operational config that must survive bmad-switch.
- (version) Stack pins carried from live pixi.toml/pyforge-warden pyproject.toml at intake (2026-07-25): python >=3.12, hatchling backend, pixi-build-python 0.*, age (external, unpinned in this pass — flagged Deferred for a version-range pin per Warden's own NFR-C1 precedent).
- (event) spine finalized
