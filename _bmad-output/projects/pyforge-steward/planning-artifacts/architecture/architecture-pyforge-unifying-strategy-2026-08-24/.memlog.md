---
scope: Canopy extension over src/platform: eight station portals, MCP service faces, event backbone, governed DDL, run-state supervisor
purpose: build-substrate
altitude: feature
chain: pyforge-unifying-strategy
topic: Architecture spine for pyforge-unifying-strategy
updated: 2026-08-25T13:52
---

- (constraint) Inherited parent spine spec-python-agent-platform ARCHITECTURE-SPINE.md AD-1..AD-17 is read-only. This spine extends it. Parent SPEC CAP-1..6 are shipped. Conflict surface: parent AD-5's schema *isolation* (three schemas, search_path) still binds; its *provisioning mechanism* (RunSQL / Django data migration) is superseded by this chain's CAP-9 Liquibase path.
- (constraint) Parent AD-2: src/platform/ never imports pyforge.*. CAP-6's identity client therefore cannot live in platform host code. Portals (django-* under src/shared/packages) may import factory packages; the host may not.
- (constraint) Parent AD-4: one ASGI process. Service faces mount on that process. Eight FastAPI processes on :8001-8008 are Dream residue and forbidden.
- (constraint) Parent AD-1: infra is PostgreSQL + Redis + Kubernetes as kinds. Two Redis Deployments (cache vs broker) are still Redis-the-kind, not a fourth backing service. Vault as an in-process client would be a fourth kind; secrets enter via existing cluster secret delivery into K8s Secrets then parent AD-12 mounts.
- (constraint) Parent AD-7: Celery over Redis is the only async path. Wagtail django-tasks ships DB and RQ backends, no Celery. Binding a custom Celery BaseTaskBackend is required so the estate does not split its task story.
- (decision) Paradigm: modular monolith. Django apps as the composition unit; one ASGI process is the public edge; stations remain CLI packages in the factory and mount a portal app + an MCP face onto the host.
- (decision) AD: Portal registration is a django-pyforge AppConfig protocol discovered via apps.get_app_configs(). Adding or removing a portal changes no host URLconf except the permanent /compliance/ redirect. App switcher derives from registration, never a path list.
- (decision) AD: Uniform URL /stations/<name>/ . compliance_face relocates; /compliance/ is a permanent redirect (FR-9a). Host code outside django-pyforge must not hardcode station paths.
- (decision) AD: Reusable-app triple: distribution django-<station>, module django_<station>_<app>, label <station>_<app>. One distribution per station, allauth-shape. Existing models never move between apps. django-pyforge is chrome, not a station distribution.
- (decision) AD: CAP-4 service faces are built on the official mcp SDK (not FastMCP). Hybrid stopgap: local-recipes pins fastmcp>=3.4.7,<4 and mcp>=1.24,<2.0 until those faces land; then the mcp ceiling lifts. Dual-era 2025-03-26 through 2026-07-28; echo client revision; never sniff UA.
- (decision) AD: Disconnect-survival is start/get over PostgreSQL (not Redis, not local disk). Handles are opaque high-entropy TTL'd capabilities. Any replica serves get. No session affinity, no progress-notification-as-survival, no stream replay.
- (event) Fast path: operator said run architecture now after Phases 0-4c and a full decision trail. Coaching skipped. [ADOPTED] tags on operator rulings.
- (decision) AD: Identity-carrying client for portals lives in django-pyforge. CLI/agent client lives in pyforge.core. Same assertion format (audience-bound, signed, short-lived). No trusted header. No raw HTTP from a portal. Host code never imports pyforge.* (parent AD-2).
- (decision) AD: Event backbone is Redis Streams carrying CloudEvents 1.0. One stream family, consumer group per station, DLQ stream, loop-depth ceiling as a CloudEvents extension attribute. Poisoned events go to DLQ, never retry forever.
- (decision) AD: liquibaseSchemaName is a single shared schema named liquibase. Deploy is one Helm Job (weight -1) then migrate --fake; a global changelog lock matches that topology. Per-app tracking deferred until there is more than one Job. preserveSchemaCase stays off. App role DML-only; migration role holds DDL. Test DBs carved out. Connect the Job directly to PostgreSQL, not through a pooling proxy. Connection carries currentSchema rather than relying on Liquibase search_path.
- (decision) AD: Two Redis Deployments in the chart: redis-cache (evicts) and redis-broker (does not). Same image class, still Redis-the-kind under parent AD-1. CAP-11 proof: fill cache to eviction, queued tasks survive.
- (decision) AD: OpenFeature FILE resolver, in-process JSON, no sidecar. Flags evaluated in Django, MCP, and CLI from the same files. Packaging is operator-owned; stories blocked until feedstocks exist.
- (decision) AD: Run-state supervisor writes live state and completed-run timing into PostgreSQL. Front door queries it through the host. Never reads ~/.bmad-loops, tmux, or journals. Unreachable supervisor renders explicit unavailable plus age, does not hang past FR-26 budget.
- (decision) AD: Wagtail media on object storage (django-storages). Shared renditions cache alias on redis-cache. Search is PostgreSQL FTS (django.contrib.postgres), no Elasticsearch. OIDC group mapping required for Wagtail admin permission. Background work through Celery via a custom django-tasks backend.
- (decision) AD: Circuit breaker is PyBreaker plus a ~40-line asyncio wrapper. fail_max is coarse, never exact-count. IdP roles on every request (no local cache of authorization that outlives the token). Feature flags and chrome are ours; django-lasuite is OIDC only.
- (decision) AD: Five-tier completeness: CLI, portal, service, skill, persona. A station declared done on fewer than five is a review-blocking finding. Skills live in .claude/skills/pyforge-<station>/; personas act only through CAP-5 grammar and CAP-4 faces.
- (decision) AD: CAP-2 supersedes Marshal console. Parity inventory is the gate. Detector verdicts: scheduled job, cached result, visible age. Editorial: CMS. Kedro-Viz tree is NOT retired. 100+ inbound docs/dashboard refs are their own story.
- (question) ~~lane1-serves-dw-h3 remains open~~ (historical 2026-08-24). Answered 2026-08-25: no. This spine does not absorb spec-wagtail-corporate-brain.
- (assumption) Operator owns all conda-forge packaging (OpenFeature x4, cachebox 5.x, liquibase, optional Django 5.2.17 bump). This spine treats those as external gates, not stories to size.
- (version) Verified 2026-08-24: Django 5.2.15 on conda-forge (5.2.17 recommended), Python 3.12.*, Wagtail 7.4.3, mcp 2.0.0 (CAP-4 target) / mcp 1.28.1+fastmcp 3.4.7 (stopgap), Liquibase Community 5.0.4, openjdk 25.0.2, pybreaker 1.4.1, django-lasuite 0.0.28, django-storages 1.14.6, django-redis 7.0.0.
- (decision) AD-17: CAP-15 station domain skills are Skill Forge content skills (already installed: _bmad/skf + .claude/skills/skf-*). Compiled from src/shared/packages/pyforge-<station>/ with provenance; export is the only write to CLAUDE.md/AGENTS.md. CAP-16 personas are BMAD launcher/agent skills that consult those content skills. conda-forge-expert remains the hand-authored operating-skill exception. Source: https://github.com/armelhbobdad/bmad-module-skill-forge — operator pointed at it mid-spine for creating BMAD skills.
- (event) Reviewer gate: lint clean. Currency PASS-WITH-FLAGS applied (HTMX 2.x pin, django-tasks-celery trap, stack.md Django audit stale text). Adversarial FAIL applied as AD-18/19 plus tightened AD-1/5/6/7/8/9/11/12. Rubric walker still in flight.
- (event) Rubric NOT-YET (H1-H5) closed: H1 media=RWX PVC not MinIO (parent AD-1 kind preserved); H2 schemas=public+langflow_schema+dbgpt_schema+liquibase, run_state/mcp_handles are tables in public; H3 one ASGI Deployment, Celery workers scale (RFC-1 HTTP pools revised); H4 canopy AD-20 CAP-7=pyforge.steward.dashboard only; H5 canopy AD-19 delivery=parent AD-12 keyRefs, manager out of chain. Autofix M2 RFC-4 names into AD-8; M3 parent vs canopy citation; M4 Celery/Channels/allauth/Keycloak pins; M1 RS256+claim map; M5/M6 Deferred with revisit.
- (event) spine finalized. Rubric PASS-WITH-FLAGS: H1-H5 closed; M7 bound into canopy AD-11 (in-process FILE watch, no Reloader/flagd sidecar); L3 duplicate conventions table removed; L5 parent AD-6 RWX conflict sentence added.
- (decision) 2026-08-25 lane1-serves-dw-h3 answered no. Deferred table revisit only if a new adapter Dream is written.
