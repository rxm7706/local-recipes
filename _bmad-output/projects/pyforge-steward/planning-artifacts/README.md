# pyforge-steward — planning artifacts

**Steward** — platform/ops CLI (`steward provision/deploy/keys/budget/sync`):
runner + environment provisioning, service deployment, credential lifecycle,
resource ceilings. dist `pyforge-steward` / module `pyforge.steward` / CLI
`steward`.

BMAD Tier-2 output for this project (`research/`, `briefs/`, `prds/`,
`architecture/`, `epics.md`, `specs/`). Canopy extension chain
`spec-pyforge-unifying-strategy` (Dream `docs/dreams/pyforge-unifying-strategy.md`,
Epics 18–30) lives here; the Canopy is not a ninth station.
