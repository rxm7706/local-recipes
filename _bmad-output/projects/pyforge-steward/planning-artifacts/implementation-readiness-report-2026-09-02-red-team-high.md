# Steward — red-team HIGH set readiness (2026-09-02)

Gate of `spec-pyforge-unifying-strategy` Epics 41–43 after the second
2026-09-02 correct-course bound review directives R-3 … R-16 as fourteen
stories and R-17 … R-25 as deferred-work entries.

**Question:** could a developer implement each story without inventing
decisions?

**Verdict: READY — proceed. (Amended 2026-09-02: the 43.5 interpreter decision landed as
hybrid (a)+(c); 43.6 added, gated on Mason 13.1 / 13.2.)**

## Concerns (do not invent; do not block the rest)

| Concern | Where | Why it does not block |
|---|---|---|
| 43.5 decided (hybrid a+c). 43.6 (image flip) must not dispatch before Mason 13.1 / 13.2 are `done` | `spec-43-6` `Deps` | Order is stated; 43.5 itself has no deps |
| 42.x assume Epic 40 shipped (verified mint root; bounded broker) | `Deps` lines | Order 40 → 42 is stated; do not dispatch 42.x on a red 40 |
| 41.1 backup destination defaults to RWX PVC; object store is a profile plugin | `spec-41-1` | RWX exists already for media (AD-13); object store stays optional |
| 42.4 changes queue names; any Celery caller with a hard-coded queue breaks | `spec-42-4` | Routing table lives in chrome; grep for `queue=` is a task |
| 43.2 moves Langflow off bare `/api/v1` | `spec-43-2` | `/langflow/api/v1/` already works; the prefix-preserving redirect test exists |
| ~~`chain-completeness` CAP-6~~ | detectors | **Fixed 2026-09-02.** It was a real chain gap, not a detector bug: `spec-bmad-suite-channel-product` CAP-6 (added 2026-09-01) pointed at `spec-bmad-suite-metapackage`, but Epic 39 never cited the channel-product slug, so the slug-scoped window saw no CAP-6. Epic 39's blurb now cites it; the check is green. The steward layers-audit staleness checkpoint was a feeds-currency cascade (spec touched 2026-09-01 by the 23-1 sharded-path landing → PRD re-stamped 2026-09-02 → architecture spine re-stamped 2026-09-02, no FR or AD change); both `chain-completeness` and `chain-completeness --layers --project pyforge-steward` are green as of this commit |
| ~~Fourteen drafts carry no operator approval yet~~ | SCP § 2 item 6.3 | **Approved 2026-09-02** (Epics 40–43 + Mason 13). Dev sessions on the web: start with `PYFORGE_SESSION_ENVS="pyforge-doctor python-agent-platform"` (add `platform-dev` for chart/helm stories) so platform tests can run |

## Trace

| Intent | Story |
|---|---|
| R-3 DR contract + backup | 41.1 |
| R-11 plane process boundary | 41.2 |
| R-12 Scribe DDL governed | 41.3 |
| R-14 broker TLS verified | 41.4 |
| R-7 MCP transport auth + streaming proxy | 42.1 |
| R-8 rate limits + run bounds | 42.2 |
| R-9 bus delivery semantics + consumer | 42.3 |
| R-10 Celery hardening + builds pool | 42.4 |
| R-13 role namespaces + tenant | 42.5 |
| R-4 split the Dream | 43.1 |
| R-5 station API contract | 43.2 |
| R-6 in-process port | 43.3 |
| R-15 CD by digest | 43.4 |
| R-16 one interpreter story | 43.5 (decision) + 43.6 (flip) + mason 13.1 / 13.2 (pins) |
| R-17 … R-25 | `DW-RT-2026-09-02-1..9` |

## Dispatch order

1. Epic 40 (40.1 then 40.2) — from the first correct-course.
2. Epic 41: 41.1, 41.2, 41.3, 41.4 in parallel.
3. Epic 42: 42.1, 42.2, 42.3, 42.4, 42.5 in parallel after 40 is `done`.
4. Epic 43: 43.1, 43.2, 43.4, 43.5 in parallel; 43.3 after 43.2; 43.6 after Mason 13.1 / 13.2 (dispatch those in `pyforge-mason` any time, in parallel with Epic 41).

`BMAD_ACTIVE_PROJECT=pyforge-steward`. Physical paths only. Cutover Phase 1
waits for all four epics.

## Housekeeping 2026-09-02 (operator-directed, fleet-wide)

| Item | State | Detail |
|---|---|---|
| `chain-completeness` CAP-6 | **closed** | `spec-bmad-suite-channel-product` CAP-6 now cited from Epic 39. Green. |
| steward layers-audit staleness | **closed** | PRD and architecture spine re-stamped after the 23-1 sharded-path landing; no FR/AD change. Green. |
| deferred-work verification coverage | **closed** | Every tracked entry in all eight ledgers carries `verified: 2026-09-02` (mechanical re-verification at HEAD `933039db67`: source_spec presence, cited paths present/absent, status→verdict). 100 % in every project. The line says agent judgment was not applied; a semantic re-read is still owed per entry where the claim is semantic — Doctor's `due-for-verification` will surface those again in 30 days. |
| `deferred-work` Tier-3 twins | **closed** | `scripts/deferred_work_intake.py --fix` ingested 11 marshal spec-frontmatter deferrals (`DW-FU-28-18..28-20`). Green. |
| `bmad-recipe-upstream-drift` ×3 (`bmad-builder` 2.2.1→2.2.2, `bmad-creative-intelligence-suite` 0.3.1→0.3.2, `bmad-method-test-architecture-enterprise` 1.23.2→1.24.0) | **closed 2026-09-02** (PR #1009) | All three recipes bumped, `sha256` recomputed and **independently re-verified** against the real GitHub tarballs, `build.number` reset to 0. Each built GREEN locally (`recipe-build`) and its test phase re-run authoritatively against the artifact (G85). `recipes/bmad-suite/` metapackage floors regenerated to `>=2.2.2` / `>=0.3.2` / `>=1.24.0` (CalVer `2026.9.2`). **The CAP-2 chain could not do this** — see the two findings row below; the task's sanctioned `autotick-github` fallback was used. |
| `bmad-suite-upstream-drift` ×3 (installed behind) | **closed 2026-09-02** (PR #1009) | Published to SelfExplainML: `bmad-builder 2.2.2`, `bmad-creative-intelligence-suite 0.3.2`, `bmad-method-test-architecture-enterprise 1.24.0` (all `noarch/…-ha7a566d_0.conda`). Channel confirmed serving all three via `pixi search -c SelfExplainML` BEFORE the pin bump. `pixi.toml` floors bumped (3 sites, all in `[feature.local-recipes.dependencies]`; no `[feature.bmad-suite-full]` duplicates exist), `pixi lock` resolved exactly those three and nothing else, `pixi install -e local-recipes` applied them (`conda-meta/` confirms). `environment.yaml` regenerated byte-identical — it exports the `build` env, which does not carry these packages. NOTE: the anaconda.org credential is NOT in the steward keys store (`steward keys list` shows only crc-pull-secret / crc-kubeadmin / github-pat-read-project, all `secrets: []`); it lives in the anaconda-client keyring, authenticated as `SelfExplainML`. Worth recording in the FR-5 inventory. |
| **NEW — the GitHub probes are unauthenticated and fail OPEN** (steward CAP-2 + doctor `bmad-method-version-drift`) | **open — not fixed here** | Found while running item 1. `suite.py::_url_request` builds every `api.github.com` request with a User-Agent and **no token**, and reads none from the environment, so the CAP-2 chain is capped at GitHub's **60 req/hr per IP** while one `suite pipeline-truth` / `suite advance` spends ~13–26 of them (13 packages × up to 2 endpoints). Once exhausted, `fetch_github_latest` returns `None`, the stage records `ok=false, value=null`, `is_advance_stale()` sees no `recipe` drift, and `suite advance` reports **"not stale for advance"** — a *wrong verdict presented as a clean result*, not an error. Reproduced both ways in one session: the first `advance --dry-run` correctly saw `drifts=recipe,…`; minutes later the same command reported "not stale", with `/rate_limit` showing `0/60`. The same fail-open hits doctor: with the budget spent, `bmad-method-version-drift` flipped from 3 WARNs to `ok -- … (1 checked)` — **12 of 13 packages silently unchecked**, i.e. the success check stated for items 1–2 in this table can itself go falsely green. **Worse, measured after the hourly reset**: starting from a full `60/60`, ONE `bmad-method-version-drift-check` invocation consumed the **entire** budget (ending `0/60`) and *still* reported `(1 checked)` + `ok`. So this is not "the budget happened to be spent" — a single run exceeds the whole unauthenticated hourly allowance, and `(1 checked)` + a green verdict is its **steady state** on this machine. The detector cannot honestly complete unauthenticated. Items 1–2 above were therefore verified a different way: authenticated `gh api repos/<r>/releases/latest` for the three upstreams, cross-checked four-way against recipe `context.version`, `conda-meta/` installed version, and the `pixi.toml` floor — all three ALIGNED. `.claude/.../github_updater.py` already does this correctly (honours `GITHUB_TOKEN`/`GH_TOKEN`); steward's and doctor's probes should adopt the same auth chain, and `checked` vs `total` should be surfaced so a fail-open is visible rather than reported as "ok". |
| **NEW — `cfe-rebuild-guard-check` `unmirrored-retro` ×2, needs an OPERATOR DECISION (OQ5)** | **open — deliberately not closed** | The Rule-2 retro this task mandated (CFE v8.84.1, commit `2a122d8466`, CFE surface + `CHANGELOG.md`) is now the newest qualifying CFE retro, so `pyforge-mason/spec-conda-forge-expert-rebuild`'s clause (b) fires for `slice-1-recipe-generation` and `slice-2-recipe-lifecycle`, both still pinned at `brief_mirrored_through: 565ef7d194`. The detector's remedy ("mirror the retro's CFE-surface delta into the slice's brief, then set `brief_mirrored_through`") **cannot be executed without first answering OQ5** — *"does a mid-campaign Rule-2 retro edit the legacy SKILL.md, the successor slice's skill, or both"* — which that campaign's own `campaign-state.yaml` records as open and never exercised: *"no NEW CFE Rule-2 retro actually LANDED during 12.6/12.7 … OQ5 genuinely never got a real case to answer it with"*, and *"Both should be settled before slice 3/4's brief is written."* **This retro is that first real case.** Not answered here — it is an explicit operator decision and outside this task's scope. Note the guard was GREEN before this PR, so this finding is caused by (not merely surfaced by) the mandated retro; it will recur on every future CFE Rule-2 retro until OQ5 is settled. |
| **Fleet hygiene sweep — every detector green** | **closed 2026-09-02** (PR #1009) | Operator-directed second pass ("no broken windows"): the board went from 4 red detectors to **22/22 green, exit 0**. Closed: `deferred-work` (ingested marshal `DW-FU-28-24`), `unpushed_work_check` (pushed the orphaned completed story `dispatch/pyforge-marshal/28.13`, 16 files that existed only on this disk), `spec-surface` (1 `ungoverned` + 217 gating `drift` + 24 `drift-presumed` across 26 specs — memlog-reconciled then **scoped**-stamped, verified 26 keys changed / 0 added / 0 removed / 0 outside the intended set), `cfe_rebuild_guard_check` (below), and `chain_currency_sweep_check` (the memlog reconciliation cascaded spec→PRD→spine→epics on atlas and marshal; re-stamped currency-only, no FR/AD/epic/story touched). CFE meta suite went 5 failed → **0 failed** (7616 passed). |
| **conda-forge dropped Python 3.10 — floor is now 3.11** | **closed 2026-09-02** (PR #1009, CFE v8.85.0) | `conda-forge-pinning 2026.09.02.08.53.43` removed 3.10; matrix is now 3.11–3.14. **The floor was HARDCODED in `recipe-generator.py`** (a constant + five literal `"3.10"` comparisons), so the generator would have kept emitting recipes pinned to a Python conda-forge no longer builds — it now reads the installed pinning, literal only as an offline fallback. Two SEL-004 unit tests were hardcoded the same way and are now floor-relative. Repo-root `conda_build_config.yaml` re-synced verbatim from upstream (previous snapshot 2026-04-26). Corpus sweep **removed** `context.python_min` from **438** recipes at/below the floor (Policy Rule 6 — delete, don't bump; 123 legitimately above kept). Build-verified: `hijri-converter` with no `context.python_min` resolves `${{ python_min }}` → **3.11**, builds on 3.11, all tests pass. |
| **47 recipes did not parse at all** | **closed 2026-09-02** (PR #1009) | Standing `test_recipe_yaml_parse_audit` debt. Four classes: 22 G92 uncommented `Error:` lines inside CFE comment blocks (incl. `dask-core`/`xgboost` — legitimate PyPI 404s for conda-only packages, now commented as such); 13 plain scalars YAML cannot read; 4 G20 bare `{{ }}`; 8 structural — `basemap`/`sentencepiece`/`opencv` were each missing the `- ` sequence marker on their **first** output (so `outputs:` parsed as a mapping and failed at the *next* output), `libarchive` had a stray `- {}`, `interface_meta` had `summary` as a list item, `imagecodecs` carried a leaked `SentinelType` repr from a failed v0→v1 migration. |
| **Two gates were in direct contradiction** | **closed 2026-09-02** (PR #1009) | CFE's `tests/meta/test_dashboard_renders.py` demanded `docs/dashboard/check_render.js` **exist**, while doctor's `dashboard-drift` lists that exact path in its `_RETIRED_CONSOLE_FILES` **reintroduction** gate (it must stay **gone**). Steward 30.2 deleted the Guildhall on 2026-08-25 and missed this inbound ref, so the test had been red for a week. The detector owns the invariant; the test is retired. |
| `cfe-rebuild-guard-check` `unmirrored-retro` ×2 — **OQ5 got its first real case** | **closed 2026-09-02** (PR #1009) | Superseded the "needs an operator decision" row below. The v8.85.0 retro's delta was **not** empty: both compiled slices carry `recipe-generator.py` with the same hardcoded `3.10` floor (slice 2 also `recipe_optimizer.py`), so leaving them would have shipped successors emitting dead-Python recipes. **OQ5 answer for this case, forced by facts rather than preference: both** — the legacy skill AND every successor slice that actually contains the touched script. Mirrored ONLY the floor change (not a whole-file overwrite, which would have silently ported v8.84.0's HEAD-advance and erased the equivalence drift Story 12.3 tracks separately); both copies verified to import and resolve 3.11. `brief_mirrored_through` advanced on both slices. **OQ5 remains open for the non-intersecting case, which has still never occurred.** |
| MRS-GATE-012 scope-violation advisories (atlas ×1, steward ×2) | **root-caused + fixed 2026-09-02** (PR #1009); advisory lines clear on the next dispatch run | Neither was a real scope violation — **both paths were legitimately in their story's scope**, and both stations had a *policy* defect. **atlas** (run `…T152158735Z-dfc8ff67`, story 21.10, path `.github/workflows/kedro-viz-publish.yml`): epic 21 has declared that exact path all along, but `harness_preference = ["cursor"]` had been written *below* the `[epic_surfaces]` header, so TOML parsed it as `epic_surfaces["harness_preference"]`; `core/policy.py::_valid_epic_surfaces` requires every key to be `.isdigit()` and **drops the whole field for the layer** on one bad key, so the entire per-epic allowlist was discarded and every atlas story silently fell back to `default_epic_surface()`. Fixed by moving the key above the first table header — which also restored the cursor-only harness preference, which had been resolving from `repo_defaults` (the full 5-harness list) the whole time. **steward** (run `…T141716883Z-34ec963e`, story 39.4, paths `docs/reference/library-llms-full.md` + `tests/packaging/test_bmad_suite_full_feature.py`): epic 39 had no `[epic_surfaces]` entry at all — the 2026-08-31 fleet-uniformity pass covered epics 1–38 and 39–43 were minted after it — so it fell back to the default surface. Added a `"39"` entry covering both paths (both named in spec 39.4's own Files/Verification sections). Verified by driving marshal's own `resolve_policy_surface` → `compute_effective_surface` → `check_scope_with_mode`: **3 findings pre-fix (exactly reproducing the live advisories) → 0 post-fix**. `scope_violation_mode` was never touched. **Closed in the same PR**: steward epics **40–43** now declared (station-uniform surface, matching epics 1–38), and a fleet-wide audit found **mason epic 13** was the only *pending-story* epic left undeclared anywhere — declared with `recipes/**` + the CFE surface, since both its stories are conda-forge recipe edits and it is the next scheduled dispatch. |
