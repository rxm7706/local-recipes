---
topic: Factory stdio MCP translator — slice 2 parked
updated: 2026-08-29T02:20
---

- (direction) Slice 2 is the pasted BMAD-SPEC-2026-MCP translator for factory FastMCP 3 stdio, not CRC.
- (constraint) Do not implement the engine in this program pass; spec + I/O matrix only.
- (constraint) Same-process translator cannot load mcp 1.x and mcp 2.x.
- (capability) CAP-1: modern client reaches a FastMCP 3 child via stdio wrap.
- (capability) CAP-2: docs never claim this fixes CRC gunicorn ImportError.
- (event) Headless express distill 2026-08-26 from MCP era program plan.
- (event) Slice 1 shipped (#858); operator scheduled slice 2 implementation 2026-08-26 (no GHA).
- (event) Translator is stdlib JSON-RPC newline stdio; does not import mcp 1.x or 2.x. Tests: tests/scripts/test_mcp_factory_stdio_translator.py.
- (change) `scripts/mcp_factory_stdio_translator.py` added to `surface:` (2026-08-29, spec-surface-drift-reconciliation ongoing practice) — it was tracked and ungoverned (a `[spec-surface] ungoverned` finding); it is the slice-2 translator this Spec's own CAP-1 already describes, and its own header comment names this Spec by id. Also this Spec's first-ever `--write-baseline --spec` scoped stamp (it had a real memlog but had never been baselined).
- (change) Retroactive decomposition (chain-completeness `spec-not-decomposed`, operator-confirmed 2026-08-29): CAP-1/CAP-2 were never cited in steward's epics.md even though both shipped `2026-08-26` (`ba4ae74f73`) the same day the operator authorized slice 2 — SPEC.md's "Parked... do not implement" blockquote was simply never updated once slice 1 (`#858`) landed. Filed as steward Epic 38 / Story 38.1, marked `done`, and the blockquote corrected to reflect reality. No new code; this entry and the epics.md addition are the only changes.
