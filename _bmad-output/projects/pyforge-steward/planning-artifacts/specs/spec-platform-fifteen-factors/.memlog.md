---
topic: src/platform earns its 15 factors: OIDC, telemetry, startup refusals, policy-as-tests, pixi deps
updated: 2026-09-17T02:30
---

- (direction) Operator (2026-08-22): incorporate into backlog now
- (note) Reference implementations MIT (django-15-factor-base + devinfra substrate) — borrowable with notices; intake report 2026-08-22 carries the feature inventory
- (capability) CAP-1 OIDC-delegated identity (idp_subject key, roles from IdP claims per-auth, no local passwords; Keycloak realm-as-code locally); CAP-2 telemetry (structlog+OTel, request/trace ids, OTLP-when-configured); CAP-3 startup refusal contract (two-stage fail-fast); CAP-4 policy-as-tests (dependency/credential/typing/coverage); CAP-5 pixi-sourced deps (retire requirements/*.txt)
- (event) Spec seeded lean 2026-08-22; decomposed as steward Epic 16
- (change) `surface:` moved from `[]` to naming `scripts/platform_ci_test_requirements_check.py` (2026-08-29, spec-surface-drift-reconciliation ongoing practice) — that file was tracked and ungoverned (a `[spec-surface] ungoverned` finding), and its own docstring cites Story 16.1 (CAP-5) exactly. Also this Spec's first `--write-baseline --spec` scoped stamp now that it governs a real file.
- (decision by claude) Open question 'OIDC provider for real deployments (Keycloak is the local substrate) -- decide before CAP-1's production story' closed: KEYCLOAK IN-CLUSTER, deployed by the Foundry chart, as the default profile; the `COMPONENT_OIDC_*` seam (base.py:564-580, all defaulting to '') stays the BYO-IdP escape hatch. The seam exists and is entirely unpopulated -- production.py overrides none of it and base.py:214-215 has IDP_CLAIMS_SNAPSHOT = None / IDP_USERINFO = None, the same fact Story 49.6 exists to fix, so this is one deployment decision co-decided with 49.6, not two. Infra-kinds check: NOT a breach of the lock -- Keycloak is a chart Deployment and stores its state in the existing PostgreSQL; the lock is on backing services (PostgreSQL + Redis + Kubernetes). Vessel: new steward Story 48.9 on Epic 48, beside 48.4's secrets profile -- never the `blocked` Epic 44 (fleet-readiness-decision-batch-2026-09-09 rows stB-B1 / C7).
- (event) Self-validate Pass 1 (coherence) PASS on the 2026-09-09 re-derive: CAP ids/texts preserved, CAP-1 gains the decided production profile, status stays 'ready', open_questions now empty
- (event) Self-validate Pass 2 (preservation) PASS: the B1 answer landed with the COMPONENT_OIDC_* escape hatch, the base.py:214-215 evidence, the explicit infra-kinds non-breach reasoning, the co-decision with 49.6, and the vessel (Story 48.9 on Epic 48, never the blocked Epic 44)
- (event) 2026-09-17 absorbed into spec-pyforge-steward (pointer + companions kept).
