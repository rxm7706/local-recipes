---
topic: src/platform earns its 15 factors: OIDC, telemetry, startup refusals, policy-as-tests, pixi deps
updated: 2026-08-22T07:16
---

- (direction) Operator (2026-08-22): incorporate into backlog now
- (note) Reference implementations MIT (django-15-factor-base + devinfra substrate) — borrowable with notices; intake report 2026-08-22 carries the feature inventory
- (capability) CAP-1 OIDC-delegated identity (idp_subject key, roles from IdP claims per-auth, no local passwords; Keycloak realm-as-code locally); CAP-2 telemetry (structlog+OTel, request/trace ids, OTLP-when-configured); CAP-3 startup refusal contract (two-stage fail-fast); CAP-4 policy-as-tests (dependency/credential/typing/coverage); CAP-5 pixi-sourced deps (retire requirements/*.txt)
- (event) Spec seeded lean 2026-08-22; decomposed as steward Epic 16
- (change) Spec-surface gap closed (2026-08-28, fleet-wide drift reconciliation): `scripts/platform_ci_test_requirements_check.py` (commit `ab193ca355`, Story 16.1, `done` in `sprint-status-ledger.yaml` line 55) realizes CAP-5 — retires `src/platform/requirements/{base,local,production}.txt` as an install source and fails CI if any is resurrected; pixi's `[feature.platform-ci-test.dependencies]` is the sole authority. Surface was declared empty (`[]`) at seed time (2026-08-22, before any story landed) and never updated after 16.1 shipped, so the file reported `[ungoverned]` rather than drift. Added now; no contract change, CAP-5 already describes exactly this behavior. Baseline re-stamped `--spec pyforge-steward/spec-platform-fifteen-factors` immediately after.
