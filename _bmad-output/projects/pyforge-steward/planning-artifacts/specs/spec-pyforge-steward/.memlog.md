---
topic: pyforge-steward (Steward) CLI — hexagonal wrap-never-reimplement platform/ops station: keys, deploy, provision, budget
updated: 2026-09-20T03:14
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.


- (direction) Distillation, not re-planning: dream+brief+prd+epics is an already-approved, fully-decomposed chain (18 FRs across 4 duties, 9 ADs, 4 epics); capability IDs map to PRD §4 feature groups (4.1 keys, 4.2 deploy, 4.3 provision, 4.4 budget), constraints carry architecture ADs 1-9 + key NFRs, non-goals from PRD §5, success signal from PRD §7. Architecture spine adopted as companion (diagram+layer table+stack+structural seed). Notably clean chain: all 3 of PRD's own §9 open questions were resolved down at the architecture stage (Consistency Conventions table) -- captured as constraints, not open questions.
- (capability) CAP-1 (Keys, PRD §4.1/FR-1-7): intent - operator can issue, scope, rotate, audit, inventory, and record revocation of credentials through one CLI (steward keys), with the historical JFROG_API_KEY cross-host leak pattern closed as a named automated regression test. success - a credential declared with an explicit host allowlist is never attached to a request outside it; encrypt/decrypt round-trips a fixture exactly via age; rotate re-encrypts every secret under a newly generated identity and the old identity fails to decrypt afterward, with no calendar/cron auto-rotation path; audit --drift reports the historical unconditional-injection pattern against a fixture and reports clean against today's fixed _http.py; list enumerates identities with scope/last-rotated metadata and never prints a raw secret value under any flag; revoke marks an identity retired and prints manual remediation guidance with zero third-party API calls; a dedicated conformance test fails loudly if the host-gating logic is ever removed or bypassed.
- (capability) CAP-2 (Deploy, PRD §4.2/FR-8-11): intent - operator can build and publish the Pages dashboard through one reconciled command (steward deploy dashboard) instead of hand-running dashboard-gen + git push. success - running twice with no source change between runs results in zero commits on the second run; a real change to the generated output results in exactly one new commit containing exactly the changed files, pushed to the existing Pages-serving branch; --dry-run performs the build+diff and prints it without committing or pushing (git state unchanged); status reports the last successful deploy's commit SHA and timestamp read from Git history alone, with no separate state store.
- (capability) CAP-3 (Provision, PRD §4.3/FR-12-15): intent - operator or an unattended bmad-loop session can materialize any named pixi environment or a bmad-loop runner+environment together, list what exists, and verify the environment.yaml sync gate, without recalling raw pixi/bmad-loop-worktree syntax. success - a valid environment name materializes via 'pixi install -e'; an invalid name reports a clear error listing valid names rather than surfacing pixi's raw error; --runner bmad-loop --env <name> wraps scripts/bmad-loop-worktree to materialize a worktree plus its named environment in one call, surfacing underlying failures clearly rather than leaving orphaned state unreported; --list enumerates every pixi.toml [environments] entry with its composing features (plus --json); --verify wraps the existing environment.yaml<->pixi.toml sync-gate check and reports drift with a non-zero exit when out of sync.
- (capability) CAP-4 (Budget, PRD §4.4/FR-16-18): intent - operator can declare and query a machine-readable resource ceiling and get an honest signal about spend rather than a fabricated number. success - 'budget set --cap <amount><currency>/<period>' records a stable, documented schema and rejects a malformed cap value without writing a corrupt entry; 'budget show' prints the declared ceiling(s) in human and --json form, and reports clearly (not a crash or misleading zero) when none is declared; 'budget check' returns one of three distinct exit codes -- not-configured / under-budget / over-budget -- never collapsing 'no data' into a pass, with no cloud-cost-SDK or Kubecost/OpenCost/Infracost client import anywhere in the codebase.
- (constraint) AD-1 (wrap, never reimplement): every duty adapter's implementation is a subprocess call or a thin import into an existing tool (_http.py, age, pixi, git, gh, scripts/bmad-loop-worktree). A duty module containing its own copy of logic that already exists elsewhere in this repo is a review-blocking finding, not a style nit.
- (constraint) AD-2 (keys' single chokepoint): steward.keys's host-scoping resolver imports and delegates to _http.py's existing auth_headers_for(url, skip_auth=...) pattern for anything HTTP-shaped; no duty module constructs its own request carrying ambient auth headers. Host-allowlist configuration reads _http.py's existing resolve_*_urls/*_BASE_URL table directly -- no duplicate Steward-owned URL config (resolves PRD open question on host-allowlist config source).
- (constraint) AD-3 (age-encrypted at rest, no standing service): age/age-keygen are required external pixi run-dependencies, never vendored or reimplemented. steward keys encrypt/decrypt/rotate shell out to the age CLI; no Vault/Infisical-class secrets-manager server enters the dependency graph.
- (constraint) AD-4 (deploy is CLI-invoked reconciliation only): steward deploy dashboard computes a diff (freshly-built docs/dashboard/ output vs. the currently committed tree) before any git mutation; a no-diff run performs zero commits. No daemon, scheduler, or new GitHub Actions workflow ships in v1 -- the operator or an existing workflow invokes the CLI directly against the existing Pages-serving branch.
- (constraint) AD-5 (provision wraps, never forks, Marshal-owned machinery): steward provision subcommands shell out to 'pixi install -e <name>' and scripts/bmad-loop-worktree verbatim. Steward's own code reads pixi.toml's [environments] table for inventory but never writes to it and never re-implements pixi's dependency-resolution logic.
- (constraint) AD-6 (budget v1 is declared-not-enforced): steward budget check returns one of three distinct exit codes -- not-configured, under-budget, over-budget -- never collapsing 'no data' into a pass. No cost-integration import (cloud SDK, Kubecost/OpenCost client) exists in the codebase until a future story adds a real metered spend source.
- (constraint) AD-7 (one shared Duty protocol; missing duty degrades, never crashes): every duty module exposes a Duty-protocol-conforming object (name, run(ns) -> DutyResult); cli.py dispatches through the protocol only. A duty not yet implemented for a given subcommand returns a DutyResult carrying an explicit 'not implemented' status rather than raising.
- (constraint) AD-8 (exit-code sole ownership): main() is the only place that returns a process exit code -- it catches KeyboardInterrupt (fixed SIGINT exit), SystemExit raised inside a duty (projected as an internal error, never trusted verbatim), and any other Exception (fixed internal-error exit, never the bare interpreter default 1). A duty module never calls sys.exit directly.
- (constraint) AD-9 (enterprise routing inherited, never duplicated): any new outbound HTTP endpoint Steward introduces is added as one new row to _http.py's existing resolve_*_urls convention (env-var override, no committed URL/credential) -- never a parallel Steward-specific config mechanism. Steward's air-gap posture is the same posture every other pyforge tool has.
- (constraint) Credential provenance convention: steward keys list entries carry a provenance field, 'issued' (an age identity Steward itself minted) or 'observed' (a pre-existing repo credential Steward's audit discovered but did not create, e.g. GITHUB_TOKEN, JFROG_API_KEY). Both are listed for drift-audit visibility; only 'issued' entries are rotatable via steward keys rotate in v1 -- an observed entry's rotation is external.
- (constraint) Credential values are never printed: steward keys list/audit output never contains a raw secret value under any flag combination -- enforced by a dedicated meta-tier invariant test, mirroring pyforge-warden's own invariant-test convention.
- (constraint) Provision's pixi invocations are explicit, user-triggered CLI actions only, never an import-time or build-time side effect -- matches pyforge-warden's 'pixi is a build/dev-env floor, never a runtime dependency' precedent.
- (constraint) Config file location: repo-root .steward/ dotdir, tracked (not gitignored, not under _bmad-output/) -- budget.yaml (FR-16 ceiling), keys-inventory.yaml (FR-5 credential metadata, never a secret value), and *.age encrypted payloads all live here, surviving a bmad-switch to a different active BMAD project (resolves PRD open questions on budget-config location and inventory-scope: a durable operational fact does not belong under a per-project planning-artifacts tree).
- (decision) Non-goal: no standing secrets-manager service (Vault/Infisical/OpenBao-class server) -- keys is a CLI + encrypted files only.
- (decision) Non-goal: no Backstage-class software catalog or scaffolder platform -- provision is a CLI face over pixi, not a new IDP.
- (decision) Non-goal: no ArgoCD/Flux-class GitOps control plane -- deploy's reconciliation is CLI-invoked, not a standing controller.
- (decision) Non-goal: no Kubecost/OpenCost/Infracost-class cost-allocation integration in v1 -- no live cloud spend exists to allocate against yet.
- (decision) Non-goal: no third-party credential-revocation API integration (JFrog, GitHub, Anthropic) -- keys revoke records intent and guides manual remediation, it does not call provider APIs.
- (decision) Non-goal: no presenton-pixi-image on OpenShift or air-gap bundle install support in v1 -- named in the Dream as Steward's eventual territory, explicitly deferred; deploy's v1 scope is the Pages dashboard only.
- (decision) Non-goal: no multi-operator / team features -- Steward is single-operator dogfooded tooling, not a product for external users.
- (decision) Non-goal: no calendar-based automatic key rotation scheduler -- rotation in v1 is on-demand/risk-triggered, per current credential-lifecycle best practice.
- (decision) Success signal (PRD §7): SM-1 the JFROG_API_KEY cross-host leak pattern has a passing, named regression test in Steward's conformance suite. SM-2 steward deploy dashboard replaces 100% of manual dashboard-gen + push invocations going forward. SM-3 steward provision --env <name> succeeds for every entry in pixi.toml's [environments] table without the operator consulting raw pixi syntax. SM-4 steward budget show is queried at least once after any Dream/spec update that touches the ceiling doctrine, replacing a Dream-file grep. Counter-metrics (do not optimize): SM-C1 number of duties/features shipped is not optimized -- budget growing Kubecost-scale before real spend exists is over-building against this Spec's own non-goals. SM-C2 key rotation frequency is not optimized as a vanity metric -- more rotations is not better.
- (assumption) Steward has exactly one user (this repo's maintainer, acting through every other pyforge persona and every bmad-loop session); no team/multi-tenant surface -- not independently re-validated with the user this session.
- (assumption) Third-party credential-revocation API integration is out of scope for v1 because Steward has no existing credential to call such APIs with today -- inferred from the repo's current tooling, not confirmed by the user.
- (assumption) The '1500usd/month' figure in FR-16's example is illustrative, carried verbatim from the Dream's own CLI cadence example -- the actual ceiling value is a config input, not a Spec claim about real spend.
- (assumption) age-based at-rest encryption (constraint AD-3) is the PRD's own lowest-confidence decision (medium confidence, flagged for architecture confirmation) -- assumed durable only while Steward's secret inventory stays a handful of API keys; revisit if it grows materially.
- (assumption) Direct-push deploy with no new GitHub Actions workflow (constraint AD-4) is a medium-confidence PRD decision -- assumed sufficient until scheduled/push-button deploy automation becomes a real want, at which point it should be revisited, not silently reversed.
- (event) No spec-level open_questions[] carried: all 3 of PRD §9's own open questions (credential-inventory scope, host-allowlist config source, budget-config location) were explicitly resolved by the architecture's Consistency Conventions table and are folded into Constraints above (provenance convention; AD-2's _http.py-table reuse; the .steward/ location rule). Epics.md's own 2 leftover items (exact YAML schema field names; static-pattern-match vs AST-based drift detection) are HOW-level implementation choices explicitly framed by epics itself as story-level decisions, not spec-worthy WHAT-level gaps -- omitted per Spec Law rule 2. D2 (age encryption) and D6 (direct-push deploy) are the PRD's own flagged medium-confidence decisions -- carried as Assumptions with their revisit trigger stated, not as open questions, since a decision was actually made.
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6+8): CLEAN. 4 capabilities each with intent+success (rule 1); intents are WHAT not HOW (rule 2); 13 constraints each rule something out -- AD-1..9 + provenance convention + credential-print guard + provision-invocation guard + config-location rule (rule 3); 8 explicit non-goals (rule 4); success signal ties to named, testable SM-1..4 + 2 counter-metrics (rule 5); CAP-1..4 IDs stable/unique (rule 6); prose kept lean given rich structured input, no elicitation needed (rule 8). No Open Questions section -- legitimately empty, recorded via the chain-resolution event entry rather than silently omitted. No fixes required.
- (event) Self-validate Pass 2 (preservation): walked PRD FR-1..18 (all 18 covered across CAP-1..4 + constraints), the feature-specific and cross-cutting NFRs from epics.md (NFR-1/NFR-4 -> AD-1/AD-3; NFR-2 -> AD-4; NFR-3 -> the provision-invocation constraint; NFR-5 -> AD-8; NFR-6 -> AD-9; NFR-7 -> the credential-print constraint), PRD §5 non-goals (8/8 present), PRD §7 SM-1..4 + counter-metrics C1/C2 (present in Success signal), PRD §8 Decisions D1-D6 (D1/D3/D4/D5 fully resolved into constraints/capabilities with no residual ambiguity; D2/D6 carried as Assumptions with their PRD-stated confidence level and revisit trigger, not silently treated as settled), PRD §9's own 3 Open Questions (all 3 resolved by architecture's Consistency Conventions table -> folded into constraints, per the chain-resolution event entry), PRD §10 Assumptions Index (3/3 present), Dream's four duty claims (provision/deploy/keys/budget -> CAP-3/CAP-2/CAP-1/CAP-4), Architecture AD-1..9 (all 9 -> Constraints), Architecture diagram/layer-table/Stack/Structural Seed (-> adopted companion ARCHITECTURE-SPINE.md, not duplicated in kernel). Wrapper-only content dropped (logged, not silent): PRD §0 Document Purpose, §3 Glossary term-definitions restating capability wording, §2.3 UJ-1..4 narrative (redundant with capability success criteria), the 'What Makes This Different' competitive-positioning prose (framing, not a build contract), epics.md's own 2 leftover HOW-level open items (exact YAML schema field names; static-vs-AST drift-detection choice -- explicitly framed by epics itself as story-level, not spec-worthy). No load-bearing claim found undropped-and-unplaced.
- (note) Surface declared 2026-07-25: src/shared/packages/pyforge-steward/** — the package this Spec will build (not yet created), matching the marshal/mason convention.
- (event) Story 1.1 SHIPPED 2026-07-25: src/shared/packages/pyforge-steward/ — installable CLI, argparse dispatcher with one subparser per duty (keys/deploy/provision/budget), the Duty Protocol + DutyResult dataclass (AD-7) with a NullDuty null-engine, and main() as sole owner of the exit code (AD-8). Task names mirror pyforge-warden's verbatim per the AC. 19 tests across the three tiers (unit/conformance/meta).
- (constraint) Exit codes are PROJECTED, never inherited (AD-8): 0 ok / 1 a duty ran and reported ok=False / 2 usage / 70 internal error / 130 interrupted. A crash must never surface as 1, because an undocumented 1 is indistinguishable from a duty that legitimately failed — the exact false signal a gate must not emit. Pinned by a test asserting rc != EXIT_FAILED on a crash, and by a meta-test that AST-parses every non-cli module for sys.exit() calls (the first version substring-matched and false-positived on a docstring).
- (change) Governed scripts/llms_full_check.py + docs/reference/library-llms-full.md. The catalog describes what is IN the pixi envs and Steward provisions envs; the subject is environment CONTENTS, not the Dream->Code chain, which is why it does not sit with the chain detectors under regenerable-factory.
- (event) 2026-07-30 `docs/reference/library-llms-full.md` reconciled against `pixi.toml` — governed surface, so recorded here. `llms-full-check` was RED at 27 findings (pre-existing, not caused by any recent commit): 24 `floor-drift` + 3 `undocumented-dep`. Fixed surgically rather than by the header's "rewrite the catalog" prompt — the file is 717 lines of curated capability prose, and a regex reflow would destroy the thing worth keeping to correct 27 facts. Detector now exits 0 (231 active deps / 211 versioned entries, clean).
- (constraint) Verified by direct read of `pixi.lock`, not assumed: `office2pdf` now ships on ALL THREE platforms (linux-64/osx-arm64/win-64) and is an unconditional `[feature.local-recipes.dependencies]` entry — the upstream defect ledger's "no osx-arm64 build" note for it is STALE. `ocrmypdf` is `noarch` but stays target-gated to linux-64 because it hard-deps `pngquant`, which conda-forge ships only for linux-64/osx-64; a shared dep makes the workspace lock unsolvable on osx-arm64. `bmad-manticore` is `noarch` and rides a `.dev0` pre-release because upstream wrote 2.0.0 but never tagged it.
- (event) 2026-08-01 docs/reference/library-llms-full.md reconciled against pixi.toml again -- llms-full-check was RED at 25 floor-drift findings (pre-existing, not caused by any recent commit -- accumulated pixi.toml version bumps since the 2026-07-30 sync). Fixed surgically (25 exact-match version-number replacements, verified each against pixi.toml directly before applying), same approach as 2026-07-30 -- not the header's full-rewrite prompt. Notable floors: mcp 1.28.1 -> 2.0.0 (major bump), fastmcp 3.4.5 -> 2.14.3 (pixi.toml's actual pin is LOWER than the stale catalog claimed -- verified this is pixi.toml's real current value, not a typo introduced here). Detector now exits 0 (231 active deps / 211 versioned entries, clean).
- (change) RECONCILES the 23-file Epic-2/3 delivery (2026-08-09, Marshal S-13.4): src/shared/packages/pyforge-steward/{pixi.toml,pyproject.toml}, src/pyforge/steward/{budget.py,deploy.py,provision.py,cli.py,keys.py}, the 16 tests/conformance/test_{budget,deploy,keys,provision}_*.py, and tests/meta/test_invariants.py. Determined to be IMPLEMENTATION of already-contracted capabilities rather than a contract change: this SPEC already names budget, deploy and provision across CAP-1..CAP-4, and 8 Epic-2/3 stories read done in the tracked ledger. The code caught up to the contract; the contract did not move. Deliberately NOT bulk-stamped on the strength of being the largest cluster — each path is named here so the claim is per-file under S-13.2, and re-derivation was ruled out only after checking the CAPs actually cover it.
- (change) RECONCILES src/shared/packages/pyforge-steward/src/pyforge/steward/provision.py and src/shared/packages/pyforge-steward/tests/conformance/test_provision_runner.py (2026-08-09, Story 5.1). provision --runner bmad-loop RETIRED: it wrapped the legacy scripts/bmad-loop-worktree while marshal init is a strict superset, so two stations shipped two ways to make the same thing. It now REPORTS and names marshal init <slug>, never provisioning; run_bmad_loop_worktree and its stdout parser are DELETED (a retirement leaving the old path importable is a deprecation). Reports rather than delegates because this station imports nothing from pyforge.marshal — proxying the front door would be its first cross-station coupling. --env is untouched. AD-5 amended in this station's own ARCHITECTURE-SPINE.
- (change) RECONCILES src/shared/packages/pyforge-steward/src/pyforge/steward/deploy.py, src/shared/packages/pyforge-steward/tests/conformance/test_deploy_build.py, src/shared/packages/pyforge-steward/tests/conformance/test_deploy_dry_run.py, src/shared/packages/pyforge-steward/tests/conformance/test_deploy_ledger_refusal.py (new), src/shared/packages/pyforge-steward/tests/conformance/test_deploy_reconcile.py, and src/shared/packages/pyforge-steward/tests/meta/test_invariants.py (2026-08-09, Story 5.2). `steward deploy dashboard` now refuses (named `DutyResult(ok=False, ...)`, never a silent fallback) when Steward's own tracked sprint-status-ledger.yaml is missing, unreadable, or unshaped, before `build_dashboard()` runs — bare/--build/--dry-run alike; `deploy status` is unaffected. `deploy.py` still performs zero story-status derivation of its own (AD-1) — the guard only checks the ledger's presence and shape (a line-exact `development_status:` block match, matching `generate.py::parse_sprint_status`'s own detection), pinned by a new AST-based structural test asserting no story-status vocabulary comparison exists in the module. Implementation of already-contracted CAP-2 (deploy); the contract did not move.
- (change) RECONCILES src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py, src/shared/packages/pyforge-steward/src/pyforge/steward/provision.py, and src/shared/packages/pyforge-steward/tests/conformance/test_provision_module.py (new) (2026-08-09, Story 6.1). `provision` grows a fourth flag, `--module NAME` (help text names the supported set, kept in sync with `provision.py`'s own `_SUPPORTED_MODULES` by comment cross-reference rather than an eager import — `resolve_duty` stays the one place `cli.py` imports a duty module), landing as the new top precedence check in `ProvisionDuty.run()` ahead of `--verify`/`--list`/`--runner`/`--env`. This is the sibling spec spec-bmad-module-provisioning's CAP-1 (own memlog entry there has the full backend detail: BMB's `bmad-bmb-setup` scripts driven as `uv run` subprocesses, no `--legacy-dir`, `cleanup-legacy.py` never invoked). Relevant to THIS spec's own contract: `ProvisionDuty`'s except tuple widens to catch `yaml.YAMLError` alongside its existing `RuntimeError`/`FileNotFoundError`/`tomllib.TOMLDecodeError` (AD-8's duty-failure boundary, unchanged in kind — one more caught exception type, not a new boundary), and AD-5's "provision wraps, never forks, Marshal-owned machinery" rule now also covers a second external target (BMB's own setup-skill scripts) alongside pixi — not yet reflected in ARCHITECTURE-SPINE.md's AD-5 text, which still names only pixi and the retired bmad-loop-worktree wrap; a documented gap, not a contradiction, since `--module`'s own dedicated Spec (spec-bmad-module-provisioning) is where CAP-1's intent and constraints actually live. `--env`/`--verify`/`--list`/`--runner` are untouched. Implementation of already-contracted CAP-3 (provision) plus the sibling Spec's CAP-1; the contract did not move.
- (change) RECONCILES src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py, src/shared/packages/pyforge-steward/src/pyforge/steward/provision.py, and src/shared/packages/pyforge-steward/tests/conformance/test_provision_list_modules.py (new) (2026-08-09, Story 6.2). `provision` grows a fifth flag, `--list-modules [--json]` (`cli.py`'s `--json` argument help text extended to mention it; `_add_provision_subparsers`'s own docstring updated), landing as the new top precedence check in `ProvisionDuty.run()` ahead of `--module`/`--verify`/`--list`/`--runner`/`--env` (the class docstring's precedence line and `_PROVISION_HELP` updated to match). This is the sibling spec spec-bmad-module-provisioning's CAP-2 (own memlog entry there has the full derivation detail: a pure, read-only `_bmad/config.yaml` top-level-key membership check against `_SUPPORTED_MODULES`, no subprocess call, no new state file). Relevant to THIS spec's own contract: no new exception type is caught at `ProvisionDuty`'s boundary — a malformed `_bmad/config.yaml` propagates as the ALREADY-caught `yaml.YAMLError` (widened at Story 6.1), so AD-8's duty-failure boundary is unchanged in kind, and `--json` is honored on that error path via the existing `_render_error` (same guard as `--list --json`'s Story 3.3 regression). `--module`/`--env`/`--verify`/`--list`/`--runner` are untouched. Implementation of already-contracted CAP-3 (provision) plus the sibling Spec's CAP-2; the contract did not move.
- (change) RECONCILES src/shared/packages/pyforge-steward/src/pyforge/steward/provision.py and src/shared/packages/pyforge-steward/tests/conformance/test_provision_module.py (2026-08-09, Story 6.3). `provision --module`'s failure/success paths are no longer taken at face value: a new `_format_called_process_error()` helper dedupes the `` `{cmd}` exited {code}: {stderr} `` formatting `ProvisionDuty.run()`'s own except block already applied inline, and `_run_module` now wraps its `provision_module()` call in a local `try/except (subprocess.CalledProcessError, RuntimeError)` (comparing a before/after state snapshot via the new `_module_install_state_or_none` wrapper, review-pass hardened against a re-run-against-an-already-installed-module false positive and a masked-original-error read failure) plus a post-success `module_install_states` verification gate before reporting `ok=True`. This is the sibling spec spec-bmad-module-provisioning's CAP-3 (own memlog entry there has the full failure-naming/verification detail). Relevant to THIS spec's own contract: no new exception TYPE is caught at `ProvisionDuty`'s boundary — `subprocess.CalledProcessError`/`RuntimeError` raised by `provision_module()` are now caught one level earlier, inside `_run_module` itself, rather than propagating to `ProvisionDuty.run()`'s existing except tuple; AD-8's duty-failure boundary is unchanged in kind (still exactly one place ever returns a process exit code, `main()`; still exactly one `DutyResult(ok=False, ...)` per failure, just a more specific catch site for this one flag's two exception types). `ProvisionDuty.run()`'s own `CalledProcessError` except block is refactored to call the new shared `_format_called_process_error()` helper rather than duplicating its formatting inline — no behavior change to any other flag's (`--env`/`--verify`) error path, verified by the full suite staying green. `--list-modules`/`--list`/`--runner` are untouched. Implementation of already-contracted CAP-3 (provision) plus the sibling Spec's CAP-3; the contract did not move.
- (change) RECONCILES .steward/sync-config.example.yaml, src/shared/packages/pyforge-steward/src/pyforge/steward/sync.py, src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py, src/shared/packages/pyforge-steward/tests/conformance/test_sync_config.py, src/shared/packages/pyforge-steward/tests/conformance/test_sync_duty.py, src/shared/packages/pyforge-steward/tests/conformance/test_sync_reconcile_propagation.py, and src/shared/packages/pyforge-steward/tests/unit/test_cli.py (2026-08-10, Story 8.1 verification repair). New tracked file `.steward/sync-config.example.yaml` is the first file to land under the per-kind `.steward/*` surface globs just added above — this SPEC's own "Config file location" constraint already claimed the whole dotdir, so the glob closes a coverage gap rather than granting anything new. Relevant to THIS spec's own contract: `sync` is a fifth `Duty`-protocol-conforming module dispatched through `cli.py` exactly like the existing four (AD-7, still verified generically by `test_duty_protocol.py` with no edits needed); no other AD is newly exercised in a way this SPEC's own contract governs (its auth path runs through `keys.resolve_headers`/`HostScopedCredential`, the same AD-8-lineage delegate every other duty already uses). No CAP is claimed here — the sync duty's own intent/constraints (GitHub Projects V2 <-> Jira bidirectional status reconciliation) are NOT authored by this SPEC; they live in the sibling spec-jira-github-projects-sync SPEC.md. This entry reconciles surface/drift bookkeeping only, for files landing under spec-pyforge-steward's package-path and `.steward/*` globs, per S-13.2 (a memlog reconciles the paths it names).
- (note) Audit Phase 1 landing (2026-08-10): _bmad-output/projects/pyforge-steward/planning-artifacts/epics.md (10 false Status lines corrected to done: 5.2, 6.1-6.3, 7.1-7.5, 8.1; Epic-8 audit note added — 8.3/8.4 NEEDS-RESPEC before dispatch, no producer story for the schedule enumerator, stale Tier-3 blocked record for 8.2), sprint-status-ledger.yaml (epic-5/6/7 rollups backlog->done via Tier-3 fix + sprint-ledger-sync), implementation-readiness-report-20260810.md (new — Phase 1 gate report, verdict matrix + 21-story sample). Evidence: implementation-readiness-report-20260810.md
- (note) Phase 1 blind-review convergence (2026-08-10), correcting the prior entry: 8-1's landing commit is 553ecc9e79 (PR #397 merge e3155a32e7), not 3cca43c521 (a merge-of-main). Additional paths changed this landing: docs/dashboard/data.js (regenerated), src/shared/packages/pyforge-steward/README.md + planning-artifacts/README.md (duty list 4->5, sync added), epics.md frontmatter currency + 8.1 delivery note (status-only slice per frozen spec, assignee/link un-owned = AF-5), Tier-3 epic-8 rollup -> in-progress + ledger re-sync. Verdict flips from review: 8-3 -> STILL-VALID narrowed, 8-5 -> NEEDS-RESPEC (boundary collision sync.py:421-422 + GitHub-direction raw pass-through). Full record: implementation-readiness-report-20260810.md
- (decision) Story 8-3 intent contract RE-ISSUED (2026-08-11, operator decision). The dev pass built both specified tests (314 green, sync.py untouched); the blind review found the FROZEN CONTRACT inaccurate — it claimed the opposite-identifier redelivery covers AD-9's out-of-order arrival, but AD-9 rule 2 is stale-value convergence and the opposite-identifier case only proves entry-point symmetry. Per the intent_gap protocol the session reverted rather than patch its own contract and escalated CRITICAL; steward paused (and discarded the graceful-stop request that arrived after). Operator chose relabel + ADD the real AD-9 rule 2 test now (over relabel-and-defer or dropping the test). Applied: Tier-3 contract's Problem statement, Always bullet and I/O matrix row corrected; a third test + matrix row added for a superseded-value late delivery converging to current; the same re-issue recorded durably in planning-artifacts/epics.md story 8.3 because the contract itself is gitignored Tier-3. NOTE for the next session: the run is paused on an unresolved escalation, so `marshal factory resume` will refuse (marshal:AD-45) until it is resolved — resolve first, then resume run 20260810-193158-7f2b.
- (change) RECONCILES src/shared/packages/pyforge-steward/pyproject.toml (changed — new `[dashboard]` optional extra, django + asgiref + channels + daphne pins), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/{__init__.py,apps.py,cache.py,declarations.py,middleware.py} (new — ASGI identity middleware, adopter-declaration schema, shared cache layer), and tests/unit/test_dashboard_{cache,declarations,middleware}.py (new) (2026-08-11, Story 9.1, landed via `land/steward-9-1`). No CAP is claimed here — the dashboard app's own intent/constraints (identity arrives at the ASGI boundary and authenticates no one; the adopter DECLARES its access column and roles; two concurrent users of different roles produce one upstream fetch and a role-filtered frame is never written back to the shared cache) are NOT authored by this SPEC; they live in the sibling `spec-secure-live-dashboards` SPEC.md (Epic 9, Story 9.1 / CAP-1, CAP-2; AD-4, AD-5, sld:AD-14). This entry reconciles surface/drift bookkeeping only, for files landing under spec-pyforge-steward's package-path glob, per the same convention as the sync-duty entry above (S-13.2: a memlog reconciles the paths it names). `pyproject.toml`'s new extra is mirrored byte-identical into root `pixi.toml`'s `[feature.pyforge-steward.dependencies]` django pin, enforced by `tests/meta/test_invariants.py::test_django_pin_matches_the_dashboard_extra` (unchanged by this story — already governed).
- (change) RECONCILES src/shared/packages/pyforge-steward/pyproject.toml (changed — `[dashboard]` extra gains `channels`/`daphne`/`asgiref`/`channels_redis` pins), src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py (changed — `_add_deploy_subparsers` gains the new `perimeter` subparser), src/shared/packages/pyforge-steward/src/pyforge/steward/deploy.py (changed — new `DeploymentTopology`, `UnshareableStateError`, `check_shareable_state()`, `render_daphne_unit()`, `render_edge_config()`, and the `perimeter` verb wired into `_DEPLOY_VERBS`), src/shared/packages/pyforge-steward/tests/conformance/test_deploy_perimeter.py (new), and src/shared/packages/pyforge-steward/tests/meta/test_invariants.py (changed — pin-sync assertions for the three new packages, mirroring `test_django_pin_matches_the_dashboard_extra`) (2026-08-12, Story 9.5, landed via commit a691c547265ccf9636c7fd1dd5a01fd4f360da87). No CAP is claimed here — the deployment perimeter's own intent/constraints (closing `cache.py`'s explicitly-deferred AD-5 cross-worker shareability refusal via an allowlist-based `check_shareable_state()`, and rendering a daphne systemd unit + nginx edge config with TLS termination and trusted-address network policy) are NOT authored by this SPEC; they live in the sibling `spec-secure-live-dashboards` SPEC.md (Epic 9, Story 9.5 / CAP-6). This entry reconciles surface/drift bookkeeping only, for files landing under spec-pyforge-steward's package-path glob, per the same convention as the Story 9.1 entry immediately above (S-13.2: a memlog reconciles the paths it names). `pyproject.toml`'s four new pins are mirrored byte-identical into root `pixi.toml`'s `[feature.pyforge-steward.dependencies]`, enforced by the new pin-sync tests added to `tests/meta/test_invariants.py` in this same change. `deploy.py` still imports nothing from `pyforge.steward.dashboard` (the existing import-boundary invariant, unchanged in kind).
- (change) RECONCILES src/shared/packages/pyforge-steward/pixi.toml (changed — adds `pyforge-core` as a real run-dependency), src/shared/packages/pyforge-steward/src/pyforge/steward/budget.py and src/shared/packages/pyforge-steward/src/pyforge/steward/keys.py (both changed — each own hand-written tmp-in-dir + `os.replace` write helper replaced by `pyforge.core.atomic_write`) (2026-08-12, landed by hand 2026-08-13 after a cross-station deferral — see `pyforge-marshal/spec-pyforge-marshal`'s own memlog for the full account; commit `1d60da0193`, marshal Story 14.2). No CAP is claimed here — the atomic-write primitive's own intent/constraints (CAP-2 "atomic write has one implementation") are NOT authored by this SPEC; they live in the sibling `pyforge-marshal/spec-pyforge-core` SPEC.md. This entry reconciles surface/drift bookkeeping only, for files landing under spec-pyforge-steward's package-path glob, per the same convention as every other entry in this section (S-13.2: a memlog reconciles the paths it names). This finding presented as [drift-presumed] (soft, non-gating) rather than [drift] because this memlog had already moved recently enough to be presumed covering — it was not; named explicitly now for accuracy, matching the same standard the gating findings on marshal/herald were held to.
- (change) RECONCILES src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/__init__.py (changed — module-split docstring extended for Stories 9.2 and 9.4), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/{export.py,filtering.py,navigation.py,views.py} (new), src/shared/packages/pyforge-steward/tests/meta/test_invariants.py (changed — both django-import/django-free pin guards extended for `export.py`/`navigation.py`/`filtering.py`/`views.py`), and tests/unit/test_dashboard_{export,filtering,navigation,views}.py (new) (2026-08-12, Stories 9.4 and 9.2, landed by hand 2026-08-13 after a cross-story integration gap — see the Realization notes below). No CAP is claimed here — the export-authorization gate's (Story 9.4, CAP-5) and the role-filtered navigation/row-filtering's (Story 9.2, CAP-2/CAP-3) own intent/constraints are NOT authored by this SPEC; they live in the sibling `spec-secure-live-dashboards` SPEC.md. This entry reconciles surface/drift bookkeeping only, for files landing under spec-pyforge-steward's package-path glob, per the same convention as every other entry in this section (S-13.2: a memlog reconciles the paths it names).
- (note) DEFERRED, NOT LANDED BY THE LOOP: both stories' own dev commits (Story 9.4 `cfbee8bdf1` off an early-Epic-9 base predating 9.1/9.3/9.5; Story 9.2 `6725b773fd`+`2c3a04771c` off a base after 9.5 but before 9.3) never merged — the same class of failure as marshal 14.2/3-12/3-13 and mason 2-6: each story's own verify step gated on spec-surface drift its own memlog never named, and 9.4's staleness additionally meant its `dashboard/__init__.py` edit conflicted with 9.1/9.3/9.5's own accumulated docstring updates. Landed by hand 2026-08-13: cherry-picked 9.4 first (chronologically earlier) onto `loop/pyforge-steward`'s current tip (already carrying 9.1/9.3/9.5), then both of 9.2's commits on top, resolving the `__init__.py` module-split docstring and the `tests/meta/test_invariants.py` django-import-list conflicts by combining both stories' additions rather than picking one side. One real integration gap surfaced by the merge, not by either story's own defect: 9.2's own branch predates 9.3's review-pass-4 addition of `test_the_dashboard_module_split_is_pinned_not_merely_documented` (the SECOND, django-importing-side pin guard), so 9.2's new `views.py` — a genuine Django view factory — was never added to that guard's `documented` set; fixed as its own follow-up commit on the landing branch, matching the exact drift scenario that guard's own docstring names as its reason to exist. `pyforge-steward-test`: 602 passed post-fix.
- (change) RECONCILES src/shared/packages/pyforge-steward/tests/unit/test_dashboard_isolation_proof.py (new) (2026-08-13, Story 9.6, commit `8f9cd4ee47`). No CAP is claimed here — the CAP-7 isolation-proof suite's own intent/constraints (every dashboard-isolation guard proven by a test that impersonates two roles, asserts both directions, and is paired with a companion proving the same check fails when its guard is removed) are NOT authored by this SPEC; they live in the sibling `spec-secure-live-dashboards` SPEC.md (Epic 9, Story 9.6 / CAP-7). This entry reconciles surface/drift bookkeeping only, for the one new file landing under spec-pyforge-steward's package-path glob, per the same convention as every other entry in this section (S-13.2: a memlog reconciles the paths it names). Test-only — no production code changed, no new import, no other governed path touched.
- (change) RECONCILES src/shared/packages/pyforge-steward/src/pyforge/steward/deploy.py (changed — new `StaticPanel` frozen dataclass, `render_static_index()`, `_is_board_output_dir_safe_to_write()`, `_run_static()`, `"static"` added to `_DEPLOY_VERBS` plus an explicit `DeployDuty.run` dispatch branch, and the pre-existing `dashboard_diff()` (Story 2.2) extended to also detect genuinely new, untracked files under `docs/dashboard/`), src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py (changed — `_add_deploy_subparsers` gains the `static` subparser: required `--board`, repeatable `--panel`, optional `--access-column`), src/shared/packages/pyforge-steward/tests/conformance/test_deploy_static.py (new), and src/shared/packages/pyforge-steward/tests/conformance/test_deploy_reconcile.py (changed — new case proving a brand-new untracked file under `docs/dashboard/` now produces a non-empty `dashboard_diff()` result, alongside the existing tracked-file-only cases) (2026-08-13, Story 9.7, commit `ee7af29b19`). No CAP is claimed here — the static-delivery verb's own intent/constraints (CAP-8, sld:AD-10: a board that has declared an access column may not be delivered by static export — refuses rather than warns) are NOT authored by this SPEC; they live in the sibling `spec-secure-live-dashboards` SPEC.md. This entry reconciles surface/drift bookkeeping only, for files landing under spec-pyforge-steward's package-path glob, per the same convention as every other entry in this section (S-13.2: a memlog reconciles the paths it names). `deploy.py` still imports nothing from `pyforge.steward.dashboard`, `django`, or `channels` (the existing import-boundary invariant, unchanged in kind); no new third-party dependency was added — panels arrive as caller-supplied, pre-rendered HTML strings, so `pyforge-steward` never imports `plotly`.
- (change) RECONCILES src/shared/packages/pyforge-steward/src/pyforge/steward/deploy.py (changed — new `_is_path_gitignored()` helper plus its call in `_run_static()` before the write; `UnicodeEncodeError` added alongside `OSError` on the write-path `except`; `StaticPanel`/module docstrings corrected to `to_html(full_html=False)`) and src/shared/packages/pyforge-steward/tests/conformance/test_deploy_static.py (changed — a new section adding a real scratch-git-repo helper and three tests: a gitignored board slug refuses, a publishable board is visible to `dashboard_diff()`, and a non-UTF-8 `--panel` label refuses without crashing or leaving a stray temp file) (2026-08-13, Story 9.7 follow-up review pass 7). Three review-driven patches on top of Story 9.7's own commit `ee7af29b19`; no re-derivation, no intent-contract change, no new dependency, no new verb or flag. The behavioral delta is one added refusal: `--board` values that a `.gitignore` rule would swallow (`build`, `dist`, `out`, `lib`, `env`, `venv`, `logs`, `target`, `node_modules` — all valid under `^[A-Za-z0-9_-]+$`, all matched at any depth by rules already in this repo) previously wrote a file and reported success while `dashboard_diff()` could never see it, so the board silently never published. No CAP is claimed here — CAP-8 and sld:AD-10 remain owned by the sibling `spec-secure-live-dashboards` SPEC.md, exactly as in the Story 9.7 entry immediately above. This entry reconciles surface/drift bookkeeping only, for files landing under spec-pyforge-steward's package-path glob, per the same convention as every other entry in this section (S-13.2: a memlog reconciles the paths it names). `deploy.py` still imports nothing from `pyforge.steward.dashboard`, `django`, or `channels` (the existing import-boundary invariant, unchanged in kind), and still never imports `plotly` — the new helper uses only `subprocess`, already imported.
- (change) RECONCILES src/shared/packages/pyforge-steward/src/pyforge/steward/sync.py (changed — `_parse_field_values` factored out of `get_project_item`'s inline field-parsing loop, `_LIST_PROJECT_ITEMS_QUERY`, `list_linked_github_items()`, and `reconcile_schedule_batch()`), src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py (changed — `--schedule` added to `_add_sync_subparsers`'s `identifier_group`), and src/shared/packages/pyforge-steward/tests/conformance/{test_sync_duty.py,test_sync_reconcile_propagation.py} (both changed — a new `ScheduleFakeTransport` sibling fake plus batch-dispatch/CLI-threading coverage for zero candidates, multiple candidates converging cleanly, one candidate failing mid-batch, the bulk listing call itself failing, and a paginated multi-page board) (2026-08-13, Story 8.4, commit `45a20349da`, patched same day — see this spec's own Review Triage Log). No CAP is claimed here — the schedule-trigger candidate-enumeration's own intent/constraints (FR-27/AD-2/AD-5, decomposing `trigger=schedule`'s batch mode) are NOT authored by this SPEC; they live in the sibling `spec-jira-github-projects-sync` SPEC.md, per the same convention as the Story 8.1 sync-duty entry above. This entry reconciles surface/drift bookkeeping only, for files landing under spec-pyforge-steward's package-path glob, per the same convention as every other entry in this section (S-13.2: a memlog reconciles the paths it names). `reconcile()`'s own signature and the pre-existing `--github-item`/`--jira-issue` CLI surface are unchanged; `sync` remains one `Duty`-protocol-conforming module dispatched through `cli.py` (AD-7, verified generically by `test_duty_protocol.py`, no edits needed).
- (change) RECONCILES src/shared/packages/pyforge-steward/src/pyforge/steward/sync.py (changed) and src/shared/packages/pyforge-steward/tests/conformance/test_sync_reconcile_propagation.py (changed) (2026-08-13, Story 8.5, commit `4e2418749b`, reconciled by name at landing time — the loop's own dev-verify pass never reconciled this Spec for the story). No CAP is claimed here — the "fail loud, fail alone" batch-failure isolation contract (FR-30, CAP-4) is NOT authored by this SPEC; it lives in the sibling `spec-jira-github-projects-sync` SPEC.md, per the same convention as the Story 8.1/8.4 sync-duty entries above. `list_linked_github_items` (Story 8.4) widens: every deduplicated board item is now a candidate, not just ones whose link field parses non-empty — candidacy is no longer gated on any field value, only `id`/`updatedAt` are read per node, and the now-unused-here `_parse_field_values` call site is removed from this function (the helper itself is untouched, still used by `get_project_item`). An unlinked candidate reaches `reconcile_schedule_batch` -> `reconcile()` -> `_read_both_sides`, which already raises the pre-existing `SyncUnlinkedError` (a `SyncError` subclass) the moment it finds no link; `reconcile()`'s existing except-`SyncError` handling turns that into an `ok=False` `DutyResult`, folded into that candidate's own failed batch entry without aborting the rest of the batch — closing FR-30 by widening candidacy into an already-correct failure path rather than adding new exception plumbing. This entry reconciles surface/drift bookkeeping only, for files landing under spec-pyforge-steward's package-path glob, per the same convention as every other entry in this section (S-13.2: a memlog reconciles the paths it names). `pyforge-steward-test` 687 -> 688 passed (net +1, the story's own coverage).
- (event) Spec-surface reconciliation for the S-8.5 landing complete as of this entry; baseline re-stamped `--spec pyforge-steward/spec-pyforge-steward` immediately after.
- (change) RECONCILES .steward/sync-config.example.yaml (changed), src/shared/packages/pyforge-steward/src/pyforge/steward/sync.py (changed), src/shared/packages/pyforge-steward/tests/conformance/test_sync_config.py (changed) and src/shared/packages/pyforge-steward/tests/conformance/test_sync_reconcile_propagation.py (changed) (2026-08-13, Story 8.6, commit `4df3acad23`, reconciled by name at landing time -- the loop's own dev-verify pass never reconciled this Spec for the story). No CAP is claimed here -- the explicit status-vocabulary translation contract (FR-31, AD-6/CAP-5) is NOT authored by this SPEC; it lives in the sibling `spec-jira-github-projects-sync` SPEC.md, per the same convention as every other sync-duty entry above. `SyncConfig` gains an operator-declared `status_mapping: dict[str, str]` field (type-checked at load time only -- never vocabulary-validated, since no closed set of valid status names exists); `reconcile()`'s `push_to_github` branch now translates `target_value` through it before writing, raising the new `SyncUnmappedStatusError` (a `SyncError` subclass, so it folds into the existing failure path exactly like `SyncUnlinkedError`) for any non-null value with no mapping entry -- an explicit `None` (a clear) bypasses the lookup entirely (sld:AD-10: an absent baseline key and an explicit null mean opposite things, not a status word to translate). `push_to_jira` and the `no_op` path are unchanged -- Jira's own transition-name lookup already hard-fails on an unrecognized status, so only the GitHub-write direction needed a mapping. `.steward/sync-config.example.yaml` documents the new key with a worked example. This entry reconciles surface/drift bookkeeping only, for files landing under spec-pyforge-steward's package-path and `.steward/*` globs, per the same convention as every other entry in this section (S-13.2: a memlog reconciles the paths it names). `pyforge-steward-test` 688 -> 695 passed (net +7, the story's own coverage).
- (event) Spec-surface reconciliation for the S-8.6 landing complete as of this entry; baseline re-stamped `--spec pyforge-steward/spec-pyforge-steward` immediately after.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (change) Steward 30.2 / FR-7: RECONCILES `src/shared/packages/pyforge-steward/src/pyforge/steward/deploy.py` (Kedro-Viz marker, no-op build; no dashboard-gen), `src/shared/packages/pyforge-steward/tests/conformance/test_deploy_build.py`, `src/shared/packages/pyforge-steward/tests/conformance/test_deploy_ledger_refusal.py`, `src/shared/packages/pyforge-steward/tests/conformance/test_deploy_static.py`, `src/shared/packages/pyforge-steward/tests/meta/test_invariants.py`, `src/shared/packages/pyforge-steward/tests/meta/test_five_tier_check.py`.
- (event) NOTE: the entry immediately above was never followed by a scoped `--write-baseline`, so its own files stayed drifted on top of everything below. Folded into this entry's stamp instead of a separate one.
- (change) Post-Epic-13 drift catch-up (2026-08-29, spec-surface-drift-reconciliation ongoing practice, not a new story). 16 `[drift]` findings against this Spec's blanket `src/shared/packages/pyforge-steward/**` glob, checked individually against git history and this repo's own per-file convention (S-13.2) rather than stamped for being one cluster:
  - `src/pyforge/steward/cli.py`, `src/pyforge/steward/fresh_clone.py`, `src/pyforge/steward/provision.py`, `tests/unit/test_fresh_clone_class_path.py`, `tests/unit/test_provision_install_class_playbook.py` — verified: precisely named in `spec-bmad-suite-install-class-wiring`'s own `surface:` (that Spec currently reports zero gating findings, i.e. its own memlog IS current for these exact paths). No CAP claimed here for the same reason as the sync/dashboard entries above — this Spec's blanket glob is a coincidental directory-level overlap on a package-path this Spec builds, not a claim that CAP-1..4 describe fresh-clone class-path verification. Bookkeeping only.
  - `src/pyforge/steward/suite.py`, `tests/unit/test_suite_wired_class_predicates.py` — verified against `epics.md` Epic 15 (Story 15.1, "one command reports the whole pipeline's truth") and the dedicated flat spec `specs/spec-15-1-one-command-reports-the-whole-pipelines-truth.md`. No CAP claimed here; the bmad-suite-channel report's own intent lives in that spec, not this one. Bookkeeping only.
  - `src/pyforge/steward/five_tier.py` — verified against `epics.md` Epic 29/31/37 (canopy:FR-39, canopy:AD-14) and the dedicated flat specs `specs/spec-29-3-five-tier-check.md` / `specs/spec-37-1-declare-eight-stations-five-tier-complete.md`. `tests/meta/test_five_tier_check.py` was already named in the Steward-30.2 entry immediately above (never stamped); `five_tier.py` itself was missed by that entry — named here instead. No CAP claimed; the five-tier completeness contract lives in those specs. Bookkeeping only.
  - `src/pyforge/steward/deploy_profiles.py`, `tests/unit/test_deploy_profile_plugins.py` — verified against `epics.md` Epic 32 / Story 32.2 (canopy:FR-45, canopy:AD-21) and the dedicated flat spec `specs/spec-32-2-steward-deploy-profile-adapters-are-plugins.md`. This is real, tracked, decomposed growth beyond this Spec's original v1 CAP-2 (Pages-dashboard-only) scope — vendor deploy-profile plugins (Harness/Splunk/StorageGRID/EPLX GHA/Tachyon/Jira) are a materially different capability CAP-2's text does not describe. No CAP added to THIS Spec's kernel because the real, reviewed contract already exists in the dedicated Story spec named above; adding a duplicate description here would just create a second copy to drift. Bookkeeping only.
  - `tests/unit/test_first_portal_slice_provision_list.py` — verified against the dedicated flat spec `specs/spec-33-2-first-portal-slice-provision-list.md` (Epic 33). No CAP claimed here. Bookkeeping only.
  - `tests/meta/test_skf_steward_skill.py`, `tests/unit/test_steward_persona.py` — verified against the dedicated flat spec `specs/spec-33-1-skf-skill-and-persona-for-steward.md` (Epic 33/29). No CAP claimed here. Bookkeeping only.
  - `docs/reference/library-llms-full.md`, `src/shared/packages/pyforge-steward/pyproject.toml` — verified via `git log` as routine catalog/dependency maintenance (duckdb-server catalog entry, dashboard-extra pin sync), the exact kind of change this Spec's own surface note (line 7-12) and existing CAP-2/CAP-3 already anticipate. No new capability; routine upkeep of an already-governed file.
  - Every file above traces to already-decomposed, git-history-confirmed work (no ad hoc/undocumented change found). Scoped-stamped immediately after this entry: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (event) 2026-08-29 drift reconcile (continuation of the Post-Epic-13 entry above, same file pair, new commits): `docs/reference/library-llms-full.md`'s MCP section rewritten for the mcp>=2.0 migration (`8cd2e23d3e`); `src/shared/packages/pyforge-steward/pyproject.toml`'s `[dashboard]` django/asgiref/channels_redis pins resynced to pixi.toml (`daa35ee171`). Same class as the entry above — routine catalog/dependency upkeep already anticipated by this spec's surface note and CAP-2/CAP-3. No CAP change. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (event by claude) Surface reconcile 2026-08-30, two more hand-driven fix PRs, verified via `git log --oneline -3` (each lands in exactly one commit, nothing else pending): PR #925 `cfadb82d93` (pyforge-core sole-ownership fix wave, 13 sites across the fleet) migrated `provision.py`'s ad hoc file write to the shared `pyforge.core.atomic_write` port; PR #924 `a7721dd610` swapped `test_skf_domain_skills.py`'s raw `"<!-- SKF:BEGIN" not in text` string check (false-fails on any legitimate SKF-managed file) for the real `skf-rebuild-managed-sections.py check` invocation. RECONCILES: src/shared/packages/pyforge-steward/src/pyforge/steward/provision.py, src/shared/packages/pyforge-steward/tests/meta/test_skf_domain_skills.py.
- (event by claude) Surface reconcile 2026-08-31: `src/shared/packages/pyforge-steward/pyproject.toml` changed by `0ab34e02b2` ("sync dashboard extra's asgiref pin to pixi.toml's floor" — routine pin resync, same class as the prior two reconciled entries) since the last stamp; `docs/reference/library-llms-full.md` changed by `d230e7fce2` (token-economy dep activation's "hygiene ripple" — new entries, unparked lines, 3 floor-drift fixes; `llms-full-check` reported clean per that commit's own message). Verified via `git log --oneline a7721dd610..HEAD` on each path. Both are the same routine catalog/dependency upkeep this spec's own surface note anticipates. No CAP change. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 1 governed path(s) drifted against this spec's recorded baseline (1 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `docs/reference/library-llms-full.md` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (event) Surface reconcile 2026-09-04 (fleet hygiene, PR #1043): `docs/reference/library-llms-full.md` version-floor re-sync — 31 catalog floors caught up to `pixi.toml` (`llms-full-check` DRIFT 31 → clean); prose and section structure untouched; no contract change. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward` (scoped, never a bare `--write-baseline`), from a clean tree.
- (event) Surface reconcile 2026-09-04 (PR #1043, second pass): `docs/reference/library-llms-full.md` § envs table gains the `detectors` row (the Detectors workflow's locked env: pyforge-doctor path dep + run-deps, `ruamel.yaml`, `playwright-python`; runs the repo-scope detector subset, builds and tests nothing) — a one-row addition derived from `pixi.toml`'s new `[feature.detectors]`; `llms-full-check` clean. No contract change. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward` from a clean tree.
- (change) 2026-09-04 (PR #1043, fleet CI health pass): `pyproject.toml` drops the `[tool.hatch.build.targets.wheel.force-include]` of `bmad_core_releases` (the dir has an `__init__.py`, hatchling already packages it; current hatchling refused the duplicate path, red on fresh-clone / five-tier CI; wheel verified to ship one `6.11.0.yaml`); `docs/reference/library-llms-full.md` gains the `pytz` row (dbgpt-sidecar). No CAP change. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (event) Surface reconcile 2026-09-04 (PR #1043, fourth pass — the fleet CI health pass): `tests/meta/test_skf_steward_skill.py`: `test_conda_forge_expert_not_replaced` now enforces the Rule-2 sanction — a commit touching `.claude/skills/conda-forge-expert/` on the branch must be a `retro:` commit that modifies the skill CHANGELOG, and uncommitted CFE edits also fail — instead of a bare 'nothing changed vs origin/main' check that reddened on any branch carrying a sanctioned CFE retro (PR #1043 carries v8.85.2). Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward` (scoped, never a bare `--write-baseline`).
- (change by claude) Surface touch 2026-09-05 (PR #1045): five meta-test files' whole-branch diff-guard bodies (test_first_portal_slice_provision_list.py, test_skf_domain_skills.py, test_skf_steward_skill.py, test_station_persona.py, test_steward_persona.py) refactored onto the shared mechanics (retro-pyforge-steward-2026-09-04.md action item 11: the fleet's whole-branch diff-guard test mechanics (git subprocess calls computing files/commits changed since origin/main) were consolidated into a shared `pyforge.testing_kit.branch_diff_guard` module (PR #1045).); each guard's enforced policy is unchanged. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (change by claude) Surface touch 2026-09-05 (Story 42.2 follow-up review, landing the post-merge follow-up-review commits of Stories 41.3 / 41.4 / 42.1 / 42.2 / 43.2 on `steward/land-followup-reviews-2026-09-05` (cherry-picked from 2eaa1e0d61 / 97d3d5c1d9 / 482f47fa92 / 295257ecc8 / 7e6b6c4d18 — each was committed minutes after its PR merged and never landed)): `src/shared/packages/pyforge-steward/src/pyforge/steward/revoke.py`, `src/shared/packages/pyforge-steward/tests/unit/test_revoke_duty.py` — terminal-wins revoke (the ledger, not the broker, is the record of a revoke). `pyforge-steward-test`: 1009 passed at landing. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (event by claude) 2026-09-05 suite roster change (Story 45.1): src/shared/packages/pyforge-steward/src/pyforge/steward/suite.py — new INSTALL_CLASS_CLI + SuitePackageDef.cli_bin + probe_wired shutil.which branch (runnable/missing), bmad-method-wds-expansion replaced by bmad-eval-quality in SUITE_PACKAGES and in BASELINE_2026_08_22 (row dated 2026-09-05: npm/github 0.1.0, recipe 0.2.0.dev0, channel/installed None, wired missing); src/shared/packages/pyforge-steward/tests/unit/test_suite_pipeline_truth.py and src/shared/packages/pyforge-steward/tests/unit/test_suite_wired_class_predicates.py follow (wds assertions -> eval-quality; new cli-class PATH test; skip class kept via a synthetic def). provision.py _SKIPPED_MODULES['wds'] untouched.
- (event by claude) 2026-09-05 pin follow-through (Story 45.1 done): src/shared/packages/pyforge-steward/src/pyforge/steward/suite.py BASELINE row bmad-eval-quality now records channel 0.2.0.dev0 / installed 0.2.0.dev0 / wired runnable (SelfExplainML upload + pixi pin landed the same day); src/shared/packages/pyforge-steward/tests/unit/test_suite_pipeline_truth.py asserts runnable with no drifts.
- (note by claude) 2026-09-05 pixi upgrade sweep PR (branch maint/pixi-upgrade-sweep-2026-09-05): docs/reference/library-llms-full.md (18 version floors re-synced to pixi.toml, llms-full-check clean) changed — operator `pixi upgrade --pinning-strategy latest-up` pass landed as one maintenance PR: pixi 0.78.0->0.79.0 (requires-pixi + $schema + all 15 registered external sites via bump-pixi-version, environment.yaml regenerated), bmad-method >=6.12.0, bmad-suite >=2026.9.5, ~20 other floors bumped, lock re-solved; the cachebox <6 and openfeature-provider-flagd <0.5.1 ceilings the platform policy suite guards were restored after the sweep dropped them. No spec-content change; scoped --write-baseline follows this entry.
- (event by steward) 2026-09-06 Epic 14 first live use: src/pyforge/steward/data/bmad_core_releases/6.12.0.yaml added (CAP-1 catalog for the 6.11.0->6.12.0 apply, authored from the unpacked conda package; renames checkpoint-preview->walkthrough, no removals, trap 1 live on resolve_config.py, legacy names incl. bmad-checkpoint-preview[.user]). Apply landed on branch steward/bmad-core-upgrade-6.12.0; live findings = spec-bmad-method-core-upgrade failure-modes.md traps 12-16 (installer -y directory prompt no-op; -y deletes cached custom module skf; marketplace skill list + config regeneration; CAP-5 env/uv gaps; in-repo skill customizations regenerated away). Surface hash moves for the catalog add; scoped re-stamp follows
- (change by claude) Story 14.6 / CAP-6, 2026-09-06 (branch steward/bmad-core-upgrade-6.12.0-followups): `src/pyforge/steward/upgrade.py` — installer argv is now `install --action update -y --directory <repo> --modules <every module `_bmad/_config/manifest.yaml` lists, core first>` (read from the manifest, never hardcoded; an empty/missing `modules:` refuses before the review branch exists — trap 13); `InstallerRunner` becomes a Protocol with keyword-only `env` (pre-14.6 two-positional runners are still called without it, via signature inspection); `resolve_installer_environment` prepends `<repo>/.pixi/envs/local-recipes/bin` to PATH only when `bmad-method`/`node` miss PATH and records the resolution as a note; `default_installer_runner` closes stdin (`DEVNULL`); `ApplyReport.zero_diff` (strictly exit 0 + zero changed paths) is a refusal naming trap 12 with the review branch left in place, and `UpgradeDuty._bmad_core` ANDs it into `ok`. `cli.py` `--apply` help names the new argv and the refusal. `tests/unit/test_upgrade_apply.py` (+12 tests; fixture manifest now lists core/bmm/skf in the real shape) and `tests/unit/test_upgrade_reconcile.py` (fixture likewise). Six `test_upgrade_*.py` files: 60 passed. This stamp also absorbs the un-memlogged earlier commits on this same branch/spec — `ca005c9c57` (Story 14.5 CAP-5 fix: `upgrade.py`, `test_upgrade_native_path_spot_checks.py`, `test_upgrade_prove_landed.py`) and the 6.12 live-use catalog add announced in the entry above — each reviewed when it landed, not re-litigated here. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward` (scoped, never a bare `--write-baseline`).
- (note by claude) Correction to the Story 14.6 entry above, same session: the `cli.py` `--apply` help-text touch was reverted before commit — `cli.py` is also named verbatim in the surfaces of `spec-scratch-worktree-lifecycle` and `spec-bmad-suite-install-class-wiring`, and Story 14.6's own spec marks `cli.py` read-only (flags unchanged), so a help-string edit is not worth two foreign reconciles. The help still quotes the pre-14.6 bare argv; recorded on the 14.6 story spec as a follow-up. `cli.py` is byte-identical to HEAD; re-stamped scoped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (note by claude) Correction to the Story 14.6 entry above (review finding): the 6.12.0 catalog's baseline row was already stamped before this session — its hash does not move here. Beyond 14.6's own diff, this scoped stamp absorbs exactly `ca005c9c57`'s content in `tests/unit/test_upgrade_native_path_spot_checks.py` and `tests/unit/test_upgrade_prove_landed.py` (no hunks in the 14.6 diff; a reviewer of the 14.6 patch alone cannot verify those two hashes — verify them against `ca005c9c57`).
- (event by claude) Story 14.6 review pass, 2026-09-06: 28 findings across four layers; patched in `upgrade.py` — an installer binary unresolvable on the resolved PATH is now refused before any review branch exists (when steward spawns the process itself), `--directory` passes `repo.resolve()`, the trap-12 note says the checkout was switched to the review branch, `import os` sorted (I001 clean); `tests/unit/test_upgrade_apply.py` +2 tests (unresolvable-binary refusal, bare-string manifest entries). Six `test_upgrade_*.py` files: 62 passed. Deferred on the story spec: the stale `cli.py --apply` help (low; cli.py is in two foreign surfaces) and the pre-existing branch-level `test_context_files_not_hand_edited` red (medium; from `8906bfbb22`). Re-stamped scoped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (change by claude) Story 14.7 / CAP-7, 2026-09-06 (branch steward/bmad-core-upgrade-6.12.0-followups): `src/pyforge/steward/upgrade.py` — `CustomModuleDef` / `load_custom_modules` (catalog `custom_modules:`), `read_installed_module_sources` sharing the manifest parsing with `read_installed_modules` (behaviour and error text unchanged), `CustomModuleFinding` on `PreflightReport.custom_modules` (trap 13 on any mismatch; `## Custom modules` section in `format_preflight`), `ConfigRestore` / `SkillDirVerification` / `CustomModuleReport` on `ApplyReport.custom_modules` + `custom_modules_ok` (additive: defaults, `to_dict` gains keys), `apply_bmad_core_upgrade(custom_installer_runner=...)` with the pre-branch custom-selection guard, `--pin` argv, config snapshot/restore, own-installer run (default runner, stdin closed, env via `resolve_installer_environment`; unresolvable binary / OSError reported never raised), skill-dir verification, `[modules.<name>]` unified diff, `## CAP-7 custom modules` in `format_apply`, `_bmad_core` ok gate ANDs `custom_modules_ok`; `_fingerprint_tree` generalises `fingerprint_custom_tree`; trap constant 14. `src/pyforge/steward/data/bmad_core_releases/6.12.0.yaml` — `custom_modules:` skf entry (`pin: null`, packaged_source = the pixi-env node_modules src). `src/pyforge/steward/cli.py` — `--apply` help string now quotes the real argv (`--directory`, `--modules`, `--pin`) and the custom-module follow-through (14.6's deferred item; no flag/verb change). `tests/unit/test_upgrade_apply.py` — fixture grows `with_custom_module` (skf tree, IDE skill dirs, packaged source under the ignored .pixi path, pinned `[modules.skf]`), `_fake_own_installer_script`, `_write_custom_catalog`, 26 new tests (one per I/O-matrix row, engine + CLI JSON, malformed catalog x6, CLI help shape); `tests/unit/test_upgrade_preflight.py` — `test_catalog_ships_612_custom_modules` + a 6.10-manifest no-findings assertion (pre-existing ruff I001/F401 cleared). 90/90 upgrade tests; steward suite 1052 pass + the accepted pre-existing `test_context_files_not_hand_edited` red; mypy stays at the 17 pre-existing; ruff I001/F clean. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (change by claude) Story 14.7 review-fix pass, 2026-09-06 (same branch): `upgrade.py` — CustomModuleFinding.trap_id now threads the actual case (13 for not-installed/wrong-source, 14 for installed-custom-absent-from-catalog, appended into PreflightReport.trap_ids per finding instead of a hardcoded 13); a CustomModuleFinding.notes field surfaces the catalog's authored per-module narrative in format_preflight's Custom modules section; _verify_custom_module_skill_dirs now skips (returns None) when packaged_source holds zero <name>-* dirs instead of a vacuous expected=0/ok=True; module docstring's Story 14.7 paragraph reworded so the apply's custom-module-selected check reads as a structural invariant / defense-in-depth assertion (guaranteed by Story 14.6's identical-manifest-walk design, never a live catch) and 'trap 14' clearly names what the CAP-7 phase addresses, not zero_diff itself. `data/bmad_core_releases/6.12.0.yaml` — trailing comment heading corrected to traps 12-14 (trap 15 already closed separately by CAP-5, ca005c9c57); the unselected-custom-module bullet reworded the same way. `tests/unit/test_upgrade_apply.py` — the absent-from-catalog test now asserts trap 14 (not 13); `_fake_own_installer_script` gains `clobber_custom`; five new tests (own installer clobbers _bmad/custom/** engine+CLI, ENOEXEC OSError path, injected custom_installer_runner, core installer deletes a config path outright); the core-non-zero-exit test gains verification.missing/ok assertions. 95/95 upgrade tests (was 90); mypy stays at 17; ruff clean. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-steward.
- (change by claude) Story 14.8 ships CAP-8 in src/pyforge/steward/upgrade.py (governed here per this SPEC.md's surface:, per spec-bmad-method-core-upgrade's own SPEC.md 'surface: []' note) + cli.py: new TRAP_LOCAL_CUSTOMIZATION = 16 constant, _RATTLER_PKGS_CACHE_RELATIVE_PATH, LocalCustomizationFinding dataclass + PreflightReport.local_customizations field (additive, default ()), default_installed_package_root (best-effort ~/.cache/rattler/cache/pkgs/bmad-method-<v>-*/lib/node_modules/bmad-method glob, never raises), _find_packaged_skill_dir (rglob under src/bmm-skills and src/core-skills, skips v6-shims), _scripts_package_path, _skill_customization_findings / _scripts_customization_findings / _local_customization_findings (the scan; excludes catalog upstream_touched_paths), build_preflight_report gains an installed_package_root param + a '## Local customizations' section in format_preflight; CustomizationReapplyFinding / CustomizationReapplyReport dataclasses, _three_way_merge (real git merge-file -p subprocess in a tempdir), snapshot_local_customizations, _installer_owned_package_paths, reapply_local_customizations (the CAP-8 apply-side three-way merge: merged_clean / conflict_needs_manual_merge / unchanged / skipped_no_snapshot / skipped_no_package_match); reconcile_clobbered_custom_surfaces gains installed_package_root / package_root kwargs and the delta-replay amendment (only on restored_from_bak / restored_from_snapshot, never intact / flagged); apply_bmad_core_upgrade threads both roots, snapshots pre-apply, calls reapply_local_customizations after the CAP-7 phase and before the CAP-3 reconcile call, and ApplyReport gains local_customizations_reapply / local_customizations_ok (additive); format_apply gains a '## CAP-8 local customizations re-applied' section; UpgradeDuty._bmad_core reads the new --installed-package-root CLI flag and ANDs local_customizations_ok into ok. cli.py -- --installed-package-root flag on the bmad-core subparser. tests/unit/test_upgrade_preflight.py -- 10 new tests (glob helper x3, no-root-no-match report-only, skill/script edited, v6-shims skip, no-packaged-counterpart, upstream_touched_paths exclusion). tests/unit/test_upgrade_apply.py -- a dedicated 3-flagged-file fixture (2 clean merges at different lines via a MID context line git's diff3 needs to keep adjacent-line edits from collapsing into one conflicting hunk, 1 real same-line conflict) exercised end-to-end through apply_bmad_core_upgrade and the CLI; direct reapply_local_customizations unit tests for unchanged / skipped_no_package_match / no-roots-noop; a --help flag-shape test. tests/unit/test_upgrade_reconcile.py -- 3 new tests: delta-replay clean, delta-replay conflict (plain restore kept, no .customization-conflict sibling on this marker-based path), and an explicit no-roots-passed regression guard. 114/114 upgrade-suite tests (was 95); full pyforge-steward-test 1076 pass + the accepted pre-existing test_context_files_not_hand_edited red (CLAUDE.md, unrelated to this story); mypy stays at 17 (fixed two loop-variable-reuse collisions -- renamed item->entry in format_preflight and finding->reapply_finding in format_apply -- that would otherwise have added 4 new errors); ruff I001/F clean. Live-verified against this repo's own 6.12.0 install (see spec-bmad-method-core-upgrade's entry for detail). Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-steward.
- (note by claude) Follow-up to the Story 14.8 entry above (same session): two cosmetic full-ruleset ruff findings introduced by that change (ISC004 implicit string concat in reapply_local_customizations' skip-notes tuple; SIM102 nested-if in the reconcile_clobbered_custom_surfaces delta-replay block) fixed by parenthesizing the concatenation and combining the nested if into one condition -- no behavior change, all 114 upgrade tests and the full pyforge-steward-test suite (1076 pass + the same pre-existing test_context_files_not_hand_edited red) re-verified green; mypy stays at 17, full-ruleset ruff on upgrade.py back to the pre-14.8 baseline of 7. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-steward.
- (change by claude) Story 14.8 review-fix pass, 2026-09-06 (same branch): `upgrade.py` — CAP-8's skill scan now excludes `__pycache__`/`*.pyc`/`*.pyo` bytecode noise (`_is_pycache_noise`), confirmed live to have caused 2 false-positive findings; `_skill_customization_findings` gains the same `exclude: frozenset[str]` parameter `_scripts_customization_findings` already had (the "no file covered by both mechanisms" boundary now holds for skills too); `LocalCustomizationFinding.reason` no longer embeds the resolved absolute `installed_package_root`/packaged `Path` (machine-independent report text); `build_preflight_report`'s scan-skipped note now also fires when an explicit `--installed-package-root` resolves to a non-directory (previously only noted when unset with no cache match); `reconcile_clobbered_custom_surfaces` tracks delta-replay conflicts locally and ANDs them into `all_clear` (a marker-based delta-replay conflict — e.g. on `resolve_config.py` — now correctly fails `ReconcileReport.all_clear` / `UpgradeDuty.ok`, closing a gap where CAP-8's general reapply path gated on conflict but this CAP-3 amendment did not), and notes when a delta-replay is skipped because a matching packaged file is missing on one side; `reapply_local_customizations` gains a `skipped_installer_removed_file` action so a clean three-way merge can never resurrect a file the core installer intentionally deleted. `failure-modes.md` trap 16's self-contradictory "found all 9... not genuine edits" wording corrected. Tests: `test_upgrade_preflight.py` +3 (skill-shaped `upstream_touched_paths` exclusion, pycache-noise skip, explicit-bad-path scan-skipped note); `test_upgrade_reconcile.py` — `all_clear` assertions added to the existing delta-replay clean/conflict tests, +1 new test (delta-replay skipped when a packaged file is missing on one side); `test_upgrade_apply.py` +1 (`skipped_installer_removed_file`) and `format_apply` text-rendering assertions added to the existing 3-file/2-clean/1-conflict test (closing a verification-gap finding — the CAP-8 report section was previously only exercised via dataclass/JSON, never the human-readable formatter). 119/119 upgrade-suite tests (was 114); full `pyforge-steward-test` unit tier re-verified green (528 passed) + the accepted pre-existing `test_context_files_not_hand_edited` red (unrelated, CLAUDE.md); mypy stays at 17; ruff I001/F clean, full-ruleset stays at the pre-14.8 baseline of 7. Review triage: 24 findings (0 high, 5 medium, 12 low, 6 false, 0 maybe-false) — 9 patched (this entry), 1 deferred (real-installer round-trip for the reapply half is the epic's own established, accepted pattern per 14.6/14.7's identical caveat), 14 rejected on their refutations (see the spec's own Review Triage Log for the full breakdown). Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-steward.
- (change by claude) Story 14.8 follow-up review pass, 2026-09-06 (same branch; bmad-build-auto's single allowed follow-up, spec's followup_review_recommended flag now false): a four-layer review (Blind Hunter, Edge Case Hunter, Verification Gap, Intent Alignment) re-examined the CAP-8 three-way-merge/reapply machinery in upgrade.py, checking each finding against the prior pass's 24-row triage log first (6 findings carried forward on refutations already on record, none re-patched). 27 new findings; 8 patched: _three_way_merge's git merge-file now passes -L labels (repo customization / old upstream (installed) / new upstream (target)) instead of leaking ephemeral temp-dir paths into a .customization-conflict sibling; reapply_local_customizations clears a stale .customization-conflict sibling from an earlier failed apply before deciding this run's outcome, so a file that merges cleanly now never keeps looking conflicted; default_installed_package_root wraps its body in try/except (OSError, RuntimeError) so Path.home() failing can no longer violate its own never-raises contract; a new _looks_like_bmad_method_package check (build_preflight_report / format_preflight) means an --installed-package-root that is a real, existing, but wrong-shaped directory (missing src/bmm-skills, src/core-skills, and src/scripts) is now noted as scan-skipped instead of silently reading as a verified-clean scan -- the exact trap-16 false-confidence CAP-8 exists to prevent. Tests: test_upgrade_preflight.py +1 (wrong-shape root); test_upgrade_apply.py +3 (skipped_no_package_match's actually-reachable new-side-missing branch; a nested 2-level skill-file path through the real three-way-merge, matching the motivating incident's file shape; stale-conflict-sibling cleanup) plus meaningful-label assertions added to the existing 3-file/2-clean/1-conflict test; test_upgrade_reconcile.py +1 (the CAP-3 delta-replay-conflict all_clear gate proven through the real apply_bmad_core_upgrade path, not only a direct reconcile_clobbered_custom_surfaces call -- _fake_installer gains a clobber_body param). 124/124 upgrade-suite tests (was 119); mypy stays at the pre-existing 17; ruff I001/F clean. 2 deferred (already-accepted 14.6/14.7 pattern: real-installer round-trip untested; CAP-3 delta-replay's non-gating skipped_installer_removed_file precedent unchanged), 17 rejected (see spec's own Review Triage Log for the full breakdown, 6 of them carried). followup_review_recommended forced false at HALT per the skill's own single-follow-up contract. A pre-existing uncommitted cli.py help-text touch (clarifying --package-root is also required for reapply) found in the working tree at session start was reverted rather than paid for with two foreign-spec reconciles (spec-scratch-worktree-lifecycle / spec-bmad-suite-install-class-wiring both name cli.py verbatim) -- same tradeoff the Story 14.6 entry above already made for an identical situation. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-steward.
- (note by claude) Story 14.8 second follow-up review pass, 2026-09-06 (same branch): re-applied one item from a fresh review pass that a concurrent follow-up-review session (see the entry immediately above) had deliberately reverted -- cli.py's --installed-package-root help string now also cross-references --package-root as required at --apply time for the actual three-way re-apply to run (silent report-only degradation otherwise). Also: (1) customization-inventory.md row C5 gains the same pycache-filtering clarification failure-modes.md's trap 16 row already carried. (2) upgrade.py's _is_pycache_noise (used by _skill_customization_findings) now also excludes any .customization-conflict suffix -- a prior conflicted re-apply's own output artifact would otherwise self-pollute the next pre-flight scan as a spurious new local customization; no test-visible behavior change since no fixture left one behind pre-scan. (3) reapply_local_customizations reordered so the installer-removed check (target.is_file()) runs before the old/new package-match lookup -- the more specific skipped_installer_removed_file diagnostic now wins when both conditions hold; fixed a related ordering bug my own first attempt introduced (moved unchanged before the package-match check, breaking test_reapply_local_customizations_skipped_no_package_match and the new-side-missing test) by restoring unchanged to run AFTER the package-match check, matching pre-existing test expectations; added test_reapply_local_customizations_removed_file_wins_over_no_package_match for the previously-uncovered both-conditions-hold case. (4) added test_default_installed_package_root_home_unresolvable_returns_none covering the already-landed Path.home() RuntimeError guard. (5) test_upgrade_reconcile.py's two roots-related tests (delta_replay_skipped_when_package_missing_match, roots_absent_is_byte_identical_to_pre_14_8) now assert report.all_clear is True, not just restore bytes/notes text. 128/128 upgrade-suite tests across the three files touched (test_upgrade_preflight.py 25, test_upgrade_apply.py 68, test_upgrade_reconcile.py -- ran narrow, not the full six-file count this pass). CONFLICT FLAGGED: the cli.py touch re-creates the exact cross-spec reconciliation the entry above chose to avoid (spec-bmad-suite-install-class-wiring and spec-scratch-worktree-lifecycle both name cli.py verbatim in their own surface:) -- reconciled here rather than left to drift, since this pass's review explicitly asked for the text; a human/orchestrator may prefer the prior pass's revert instead. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-steward.
- (note by claude) Correction to the Story 14.8 follow-up review entry above (same session): the scoped stamp it announced did not run. Mid-pass, cli.py (also governed by this spec's broad surface glob) was found modified in the shared working tree by what appears to be a concurrent process (not this task; the content is the --package-root-also-required help-text clarification this same memlog deferred on the Story 14.6 entry above) -- reverted once, then reappeared seconds later, unrelated to and untouched by this review pass. Stamping spec-pyforge-steward's baseline right now would sweep that unverified, possibly-still-in-flight file into the recorded contract hash alongside this pass's own upgrade.py/tests changes. Left unstamped rather than risk that: spec-surface-check will keep reporting drift for upgrade.py/test_upgrade_apply.py/test_upgrade_preflight.py/test_upgrade_preflight.py/test_upgrade_reconcile.py until a later pass (once cli.py's own state settles) runs --write-baseline --spec pyforge-steward/spec-pyforge-steward covering both changes together. cli.py itself was left exactly as found -- not committed, not reverted again -- to avoid clobbering whatever owns it.
- (note by claude) Correction to the entry immediately above (same session, caught before returning): the per-file/total test counts were wrong. Verified by actually running pytest tests/unit/test_upgrade_preflight.py tests/unit/test_upgrade_apply.py tests/unit/test_upgrade_reconcile.py -q just now: 105 passed (25 + 67 + 13), not '128/128' with apply at '68'. test_upgrade_reconcile.py WAS included in that run despite the prior entry's 'ran narrow' hedge. No code or test file changed by this correction, only the prior entry's arithmetic.
- (note by claude) Second follow-up review pass wrap-up (same session): also added test_scan_skips_own_customization_conflict_sibling (test_upgrade_preflight.py) covering the _is_pycache_noise .customization-conflict exclusion from two entries above. Verified final counts by actually running pytest on each file: test_upgrade_preflight.py 26, test_upgrade_apply.py 67, test_upgrade_reconcile.py 13 -- 106 passed across the three, all green; ruff --select I001,F clean; mypy stays at the pre-existing 17. spec-surface-check re-verified green after re-stamping pyforge-steward/spec-pyforge-steward, pyforge-steward/spec-bmad-suite-install-class-wiring, pyforge-steward/spec-scratch-worktree-lifecycle.
- (note) Cross-station surface touch (marshal Story 30.2 review pass, 2026-09-06): tests/meta/test_skf_steward_skill.py::test_context_files_not_hand_edited changed -- the prior unconditional `diff_text_since(...)` zero-diff-vs-origin/main assertion on CLAUDE.md/AGENTS.md is replaced with the SKF managed-section well-formedness check, matching the sibling test_skf_domain_skills.py's already-loosened form (this station carried two duplicate copies of the same invariant, out of sync with each other). Root AGENTS.md legitimately gained a bmad:context block as part of Story 30.2's project-context-surface migration (BMAD 6.12 persistent_facts=[] surface migration, D1); the old check would have blocked that sanctioned edit. No steward CLI/provision code changed. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (change by claude) Surface reconcile 2026-09-06 (Story 46.7, spec-bmad-suite-lifecycle): `data/bmad_core_releases/6.12.0.yaml`'s `custom_modules[skf].pin` flips `null` -> `v2.1.0` (the 2026-09-06 Q3 decision: npm 2.1.0 tarball `src/` byte-identical to GitHub tag `v2.1.0`, 337 files, `diff -rq` empty); `tests/unit/test_upgrade_apply.py` gains `test_real_catalog_pins_skf_v2_1_0` (loads the REAL packaged catalog via `load_release_catalog("6.12.0")`, asserts the pin -- closing the gap the existing synthetic-fixture mechanism test leaves); `tests/unit/test_upgrade_preflight.py`'s pre-existing `test_catalog_ships_612_custom_modules` updated from `pin is None` to `pin == "v2.1.0"` to match. Full detail lives on spec-bmad-method-core-upgrade's own memlog (the capability-intent owner); this entry exists only to move spec-pyforge-steward's surface hash since these three files also sit under its broad governed glob. `pyforge-steward-test`: 1091 passed. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (note by claude) Story 46.7 review-fix pass, 2026-09-06 (same branch): the `6.12.0.yaml` catalog comment directly above `custom_modules[skf].pin` still read "the `--pin skf=v2.1.0` question stays open" after the pin flip landed in the entry above — corrected to record the verification evidence and the resolved state (npm 2.1.0 tarball `src/` == GitHub tag `v2.1.0`, 337 files, `diff -rq` empty). `customization-inventory.md` rows C6/C19 and § 3 step 4 / § 4's open-question bullet (all four still described the pin as unresolved after Story 46.7 landed) reworded to close the question with the same evidence. No code or test change. `spec-bmad-method-core-upgrade`'s own spec-surface baseline (its `customization-inventory.md` companion) was also found stale by this same pass (drifted since an earlier edit never re-stamped) — re-stamped `--write-baseline --spec pyforge-steward/spec-bmad-method-core-upgrade` alongside this. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (change by claude) Story 14.9 (spec-bmad-suite-lifecycle) ships CAP-9 in steward (`src/pyforge/steward/upgrade.py` + `cli.py` + `data/bmad_core_releases/6.12.0.yaml`, governed here per `SPEC.md`'s `surface: []` note on spec-bmad-method-core-upgrade): `PreflightReport.shims_to_retire: tuple[str, ...] = ()` (appended last, unconditionally computed in `build_preflight_report` as the sorted intersection of the catalog's new `shims_to_retire:` list — 21 entries: 20 `v6-shims` skill-manifest.csv paths + `bmad-generate-project-context`, which ships `lifecycle: shim` under `bmm-skills/plan/` rather than a `v6-shims/` dir — against `_read_installed_skill_names`; never added to `trap_ids`, since it previews a future retirement rather than naming something already wrong); `format_preflight` gains a `## Shims to retire (--no-shims candidates) [N]` section; new `_MARSHAL_HARNESS_TEMPLATE_RELATIVE_PATH` / `_RETIRED_DEV_SKILL_PATTERN` constants and `_shim_retirement_blockers` / `refuse_shim_retirement_not_ready` functions (report-only enumeration of two marshal-owned foreign surfaces — the embedded `harness_bmadloop.py` policy.toml template and any rendered `<loop-home>/.bmad-loop/policy.toml` — still naming the retired `bmad-dev-auto` skill id; raises `UpgradeError`, never edits); `apply_bmad_core_upgrade` gains `no_shims: bool = False, loops_home: Path | None = None` — the new refusal runs immediately after the existing unconditional `refuse_legacy_custom(preflight)` (trap 2 holds unchanged under `--no-shims`, no code change needed there), `--no-shims` is inserted into `installer_cmd` right after `--modules <csv>` and before any `--pin` pairs, and a note is appended when set; `UpgradeDuty._bmad_core` threads both through from the CLI namespace. `cli.py` — `--no-shims` and `--loops-home` added to the `bmad-core` subparser. 15 new tests across `test_upgrade_preflight.py` (6: catalog∩installed intersection, disjoint/empty, absent key defaults to empty, `format_preflight` rendering both populated and empty, the real 6.12.0 catalog's 21-entry list) and `test_upgrade_apply.py` (9: argv position, same-version real-diff via a fixture shim dir deletion, trap-2 refusal still holds under `--no-shims`, harness-template refusal, loop-home-policy refusal, plain-apply never scans either foreign surface, both-clear proceeds, `--help` text, full CLI-to-apply plumbing) — 109/109 upgrade-apply+preflight tests (was 94); `pyforge-steward-test`: 1106 passed (was 1091); mypy stays at the pre-existing 17; ruff I001/F clean. This story does NOT execute the real `--no-shims` apply against this repo (its own `harness_bmadloop.py` and real loop-homes still name `bmad-dev-auto` today — a live run would correctly refuse); that live exercise is a separate, later step gated on marshal Story 30.5 landing first. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (change by claude) Story 14.9 review-fix pass, 2026-09-06 (same branch): a four-layer review (Blind Hunter, Edge Case Hunter, Verification Gap Reviewer, Intent Alignment Auditor) found 17 findings; 7 patched in `upgrade.py`/tests closed real gaps: `test_apply_no_shims_argv_carries_flag_after_modules_before_pins` and `test_apply_no_shims_same_version_target_is_a_real_diff_not_zero_diff` now pass an explicit `loops_home=` (an empty `tmp_path` dir) instead of falling through to the real, unmocked `~/.bmad-loops` on whatever machine runs the suite; the same-version test now asserts the fixture shim directory is actually gone and named in `changed_paths`, proving the deletion (not the routine `updated.txt` writes) produced the diff; `test_shims_to_retire_absent_from_real_612_catalog_stays_none_by_default` renamed to `test_shims_to_retire_absent_key_defaults_to_empty` (it tests the pre-CAP-9 6.11.0 catalog, not 6.12.0); new `test_real_612_catalog_shims_to_retire_has_21_entries` loads the REAL packaged `6.12.0.yaml` and asserts the full 21-entry list, closing the gap every other `shims_to_retire` test (synthetic fixtures only) left; `_shim_retirement_blockers` now wraps both foreign-file reads in `try/except (OSError, UnicodeDecodeError)`, failing closed (treated as a blocker, never silently clear) on an unreadable file; the same function now raises `UpgradeError` when an explicit `--loops-home` override does not resolve to a directory (fail loud on an operator typo, instead of silently reporting zero blockers — the default `loops_home=None` path stays silent, unchanged); `build_preflight_report`'s `shims_to_retire` computation now dedupes via a set comprehension before sorting. 1 finding rejected on refutation during patching (a stray unused helper introduced mid-fix, removed as dead code, not itself a review finding). 9 findings deferred (ordering with a non-empty `--pin` list untested, the default `loops_home` resolution branch untested, relative `--loops-home` CWD-resolution matches identical pre-existing sibling-flag behavior elsewhere in this file — not a regression, `--no-shims` without `--apply` silently no-ops matching identical pre-existing `--branch`/`--installer` behavior — not a regression), 3 rejected as low/out-of-scope (regex brittleness against quote-style variance — real but requires disproportionate TOML/AST parsing to close; no preflight indicator of whether the two blocking surfaces are currently clear — the spec's own Design Notes deliberately scope this refusal to apply-time only; cosmetic non-alphabetical ordering in the static catalog list — no runtime effect since the code always re-sorts). 15/15 new CAP-9 tests still pass, 109/109 upgrade-apply+preflight total; `pyforge-steward-test`: 1106 passed; mypy 17; ruff I001/F clean. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (note by claude) Foreign-surface reconcile, same session: `cli.py`'s `--no-shims`/`--loops-home` additions (Story 14.9 entry above) also sit under `spec-scratch-worktree-lifecycle`'s own governed surface (it names `cli.py` verbatim, same situation the Story 14.6 entry earlier in this memlog recorded). No content in that spec's own subject matter changed — only the `bmad-core` subparser gained two new optional flags. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-scratch-worktree-lifecycle`.
- (change by claude) Story 14.9 follow-up correction, 2026-09-06 (same branch; operator-directed spec correction, found during marshal Story 30.5): `upgrade.py`'s CAP-9 `_shim_retirement_blockers`/`refuse_shim_retirement_not_ready` functions, the `_MARSHAL_HARNESS_TEMPLATE_RELATIVE_PATH`/`_RETIRED_DEV_SKILL_PATTERN` constants, and the `loops_home` parameter (on `apply_bmad_core_upgrade` and threaded through `UpgradeDuty._bmad_core`) are REMOVED — the check they implemented has no valid target: `bmad_loop`'s `DevPolicy.skill` is a permanent internal adapter discriminator (`DEV_SKILLS = {"bmad-dev-auto"}`, hard-validated by the installed 0.11.1 package) that must read `"bmad-dev-auto"` forever, on every era; the skill actually invoked is resolved separately from disk at runtime, independent of this field. `cli.py`'s `--loops-home` flag (existed only to parameterize the removed check) is removed; `--no-shims`'s help text drops the false refusal clause. `apply_bmad_core_upgrade`'s docstring bumped to `CAP-2+3+6+7+8+9` with a short CAP-9 paragraph. `test_upgrade_apply.py`: 3 tests deleted (tested only the false behavior), 5 rewritten (dropped `loops_home=`/`--loops-home` plumbing, 2 renamed), 1 new test added (`test_apply_no_shims_argv_order_holds_with_custom_module_pins`, closing a genuine coverage gap the follow-up review found: `--no-shims` argv ordering was never jointly exercised with a nonempty `--pin` list). `no_shims`'s argv insertion, the `shims_to_retire` preview machinery, and the pre-existing trap-2 `refuse_legacy_custom` check are all unaffected. `pyforge-steward-test`: 1104 passed (was 1106; net -2 from the 3 deletions + 1 addition). mypy stays at 17; ruff I001/F clean. Full detail + review triage on spec-bmad-method-core-upgrade's own memlog and Story 14.9's own spec file. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (change by claude) 2026-09-07 fleet CI/hygiene sweep, closing three shortlisted deferred-work-ledger items (worktree `hygiene-steward`, branch `chore/ci-hygiene-sweep-2026-09-07-steward`): (1) DW-9-4-3/DW-9-4-4 — `dashboard/export.py`'s `_post_refusal_webhook` now HMAC-SHA256-signs every export-refusal POST (`resolve_webhook_secret`, env `STEWARD_EXPORT_WEBHOOK_SECRET`, `X-Steward-Signature-256: sha256=<hex>` header, mirroring `pyforge.herald.webhook`'s "never a default fallback" secret contract) and refuses to send at all — fail-closed, unsigned and unsent — when no secret is configured; `ExportPolicy` gains `allow_private_webhook_targets: bool = False` and construction-time + per-delivery SSRF checks (`_is_blocked_ip_address`/`_resolve_hostname_ips`/`_webhook_target_is_blocked`) refusing a `webhook_url` that is or resolves to loopback/link-local (incl. 169.254.169.254)/RFC1918-private/reserved, unless explicitly opted in. `tests/unit/test_dashboard_export.py` grows from 28 to 44 tests (SSRF construction-time cases, the fail-closed no-secret case, a DNS-rebind-shaped blocked-resolution case, the opt-in case, `resolve_webhook_secret` unit tests, and a real HMAC-signature assertion on the existing webhook-POST test) — proved against a pre-fix revert (import failure) before restoring. (2) DW-10-3-10 — new `tests/meta/test_workflow_path_filters_match.py` parses every `.github/workflows/*.yml`, finds every workflow declaring both `on.pull_request.paths` and `on.push.paths` (7 today: `cfe-regression-net.yml`, `coverage-gates.yml`, `platform-ci.yml`, `pyforge-core.yml`, `pyforge-station-tests.yml`, `pyforge-steward-five-tier.yml`, `pyforge-steward-fresh-clone.yml`), and asserts the two lists are identical — proved to actually catch drift by temporarily deleting one `platform-ci.yml` push-side entry and confirming the test failed, then restoring it. (3) DW-10-3-8 — re-verified, not re-fixed: the root `Containerfile`'s pixi image tag and `pixi.toml`'s `requires-pixi` floor were already both `0.79.0` at session start (converged by the later `scripts/pixi_version_registry.py`/`bump-pixi-version` fleet-wide sync this same ledger entry named as the durable remedy); `pixi-version-check` reports 17/17 sites clean, and a real `docker build -f Containerfile --target builder .` completed successfully (`pixi install -e pyforge-container` succeeded — the exact failure mode the ledger entry describes did not reproduce). `pyforge-steward-test`: 1196 passed + the one pre-existing, unrelated `test_conda_forge_expert_not_replaced` red (the already-landed `9ef5b8cd79` CFE retro commit's `fix(cfe):` subject not matching the `retro:` sanction pattern — present before this sweep's changes, confirmed by reverting them and re-running). Ledger entries DW-9-4-3/DW-9-4-4/DW-10-3-10/DW-10-3-7/DW-10-3-8 each gain a `verified: 2026-09-07 — resolved` line. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (note by claude) Follow-up to the entry above (same session): a post-stamp mypy pass on `export.py` found one real finding the entry above's own verification missed — `_resolve_hostname_ips`'s set comprehension over `info[4][0]` typed as `str | int` (mypy cannot narrow `sockaddr`'s IPv4/IPv6 union shape at index 0), fixed with an explicit `str(info[4][0])`; `ruff check --select I001,F --fix` also reordered `test_dashboard_export.py`'s new imports (alphabetization only, no content change). No behavior change; `test_dashboard_export.py` re-verified 44/44 passed, `mypy` on `export.py` alone now clean. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (change by claude) CI/hygiene sweep 2026-09-07 follow-up: test_adoption_register.py's test_wired_column_agrees_with_live_pipeline_truth_for_every_row checks the register's documented Wired/runnable status against the CURRENT machine's real provisioning state for all 13 SUITE_PACKAGES. PR #1081's own new CI job (the first time this suite ever ran in a fresh, stateless environment) failed it: bmad-eval-quality (pending-submission-to-conda-forge, no public channel) and bmad-manticore (a separate, multi-GB, out-of-repo studio install at $PYFORGE_STUDIO_ROOT) are genuinely operator-machine artifacts that document THIS maintainer's own provisioned machine, not something any fresh checkout can reproduce. Added a CI-only exemption (os.environ.get("CI")) for exactly these two named members; the other 11 real, repo-installable members are still checked in every environment, and all 13 are still checked locally/on the operator's own machine where the exemption never triggers. RECONCILES: src/shared/packages/pyforge-steward/tests/meta/test_adoption_register.py.
- (note) Cross-station reconcile from pyforge-marshal Story 32.2 (spec-fleet-consistency-standard CAP-5): this spec's pyproject.toml raised requires-python to ">=3.14", and tests/conformance/test_sync_reconcile_propagation.py's docstring citation followed the implementation-readiness-report rename to hyphenated ISO (Story 32.3, CAP-4) -- the compact -YYYYMMDD form is unmatched by bmad_drift_check.py's classifier and would read 'uncovered'. Docstring only; 73 tests pass unchanged.
- (note) Story 32.5 (spec-fleet-consistency-standard CAP-2): the fleet converged on one test-suite vocabulary -- tests/unit/ + tests/integration/ + tests/meta/ (BMAD 6.12's Python default plus the one addition with 8/8 adoption), with fixtures/ and _support/ never collected. Eight suite names collapsed to three; the coverage gate's suite map now needs no per-station special case. This station: tests/conformance/ (32 CLI-verb contract tests) folded into tests/unit/ -- its own test-architecture.md already classified them as unit level, and the gate recognised no tier by that name, so not one was ever measured. conformance/fixtures moved to the standard tests/fixtures/ and three Path(__file__).parent fixture lookups became .parent.parent. epics.md:74's normative test-tier declaration was rewritten (not just the files moved); the two shipped-story ACs keep their text with a dated pointer. 1197 tests pass.
- (change) Sweep follow-ups 2026-09-08: `tests/meta/test_adoption_register.py` `_persona_mentions` fixed -- the shared helper two projects ledgers indicted independently (steward/DW-FU-46-1-2 scan-breadth, atlas/DW-FU-24-1 match-precision) without either entry referencing the other. `_mention_re` now anchors both ends on a non-word boundary so `bmad-spec` no longer matches inside `bmad-spec-foo`, and the scan covers README.md and every reference/*.md under the persona skill dir alongside SKILL.md and customize.toml (`_PERSONA_ROUTING_FILES` + a sorted glob). 8 tests pass. atlas/DW-FU-24-1 deliberately stays open: its own claim is that the check never asserts the routing line stated grammar-constraint text, which this does not do -- that needs a per-skill expected-constraint source that does not exist yet. RECONCILES: src/shared/packages/pyforge-steward/tests/meta/test_adoption_register.py. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-steward.
- (event by claude) Surface reconcile 2026-09-14 (bulk, post-outage): five independent, already-landed, already-reviewed steward stories drifted this Spec's baseline while the ~5-day GitHub Actions billing outage (see .claude/memory/ project notes) kept spec-surface CI from ever running. d021af5df0 (cutover flag reader + replay harness): steward/cutover.py, cli.py, and their tests. c8f4eb44f8 (per-run track.json assembly from existing loop evidence): steward/track.py, tests/unit/test_track.py, tests/fixtures/track-run/{gate-record.json,journal.jsonl,state.json}, tests/fixtures/track-run-sparse/journal.jsonl. 39ff0ad964 (Hub Guard library exposure, no second PR verdict minted): steward/guards.py, tests/unit/test_guards.py. 18f8823cbb (foundry strangler kit, Frames v0.3/case-list/Epic 55): steward/frames.py, data/track.schema.json, tests/unit/test_frame_preflight.py. f30fa3d846 (suite-skip authoring tool + consume sidecar): steward/provision.py, tests/unit/test_restore_duty.py, tests/unit/test_story_52_1_consume_sidecar.py. tests/unit/test_cli.py picked up cli.py changes from the cutover-flag story. docs/reference/library-llms-full.md updated by 2d2ab9c50d (platform-dev local Django settings), unrelated to steward's own contract. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-steward.
- (change by claude) Surface reconcile 2026-09-14 (steward Story 53.6, spec-intelligence-hub CAP-2 -- full entry lives in that Spec's memlog): `frames.py` re-keyed the in-repo Frame preflight from `name` onto `identifier`. Frame Spec v0.3 §4.2.1 makes `identifier` the mandatory identity element and SHOULDs a `qualified-ref` (`publisher "/" frame-name`, §5.3), while the element profile marks `title` -- which the Markdown key `name` aliases -- as MUST NOT be slug-constrained. Keying preflight off `name` was therefore keying it off the label rather than the identity, and off a string that was simultaneously the Frame key and the Python distribution name (`pyforge-steward`). Now `COMPANY_IDENTIFIER = "pyforge/company"` + `PUBLISHER = "pyforge"`; REQUIRED_FIELDS gained `identifier` and swapped the unregistered `owner` for the registered repeatable `maintainer`; new `scalar-repeatable` finding enforces §6.2.1's "A writer MUST emit a sequence" for `maintainer`/`inherits`; `unexpected-name` became `unexpected-identifier`; `path-ref` parent resolution unchanged. The `name`/`inherits` KEYS are deliberately kept -- §6.2.1 requires those aliases of a Markdown writer, and `title:`/`composition:` belong to the YAML/JSON encodings only. Tests 13 -> 21. Full steward suite green; all 8 stations green. RECONCILES: src/shared/packages/pyforge-steward/src/pyforge/steward/frames.py, src/shared/packages/pyforge-steward/tests/unit/test_frame_preflight.py. Scoped-stamped.
- (direction) 2026-09-16 operator: local-recipes is the wrong default environment for pixi.toml and for every agent/harness (Claude, Cursor, Copilot, Gemini, Cursor Cloud Agents in RXM-LOCAL-RECIPES); it predates the Guild and is massive (222 conda deps, 171 tasks, 10 GB). Create a minimal environment carrying the current state of PyForge and the future of the foundry, and make it the default. Entered Dream-append-first: dated section on docs/dreams/pyforge-steward.md, CAP on this station Spec, Story in epics.md — no new Dream file, no satellite Spec.
- (capability) CAP-5 — pyforge-guild: one minimal environment is the default for every agent and harness. intent: a pixi feature+environment 'pyforge-guild' carries pyforge-core, pyforge-doctor (+ its warden/testing-kit seam), pyforge-marshal, pyforge-steward, pyyaml, ruamel.yaml, jsonschema, tomli, pytest, gh — and nothing of the recipe factory; the 48 Guild/planning tasks (every *-check detector, detectors/detectors-ci, sprint-ledger-sync, fleet-picture, governance-currency, the pr-preflight / pyforge-station-tests / pyforge-station-coverage-gates aggregates) move from [feature.local-recipes.tasks] to [feature.pyforge-guild.tasks]; the local-recipes environment INCLUDES the new feature so every existing 'pixi run -e local-recipes <task>' keeps working. success: 'pixi install -e pyforge-guild' from cold lands under 1 GB on disk (local-recipes: 10 GB) and 'pixi run -e pyforge-guild detectors-ci' is green on main; AGENTS.md, CLAUDE.md, .cursor/rules/*.mdc, the eight station SKILL.md files and the cloud-environment install command name pyforge-guild as the session default; scribe recall stays -e pyforge-scribe; environment.yaml regenerated in the same PR; pyforge-station-tests green (shared surface). Foundry mode: rebuild (B re-derives the same split).
- (constraint) CAP-5: tasks move, they are not duplicated — a task name exists in exactly one feature (vocabulary-one-name-one-job CAP-6 applied to pixi tasks); back-compat comes from environment composition, never from a second copy. dashboard-layout-check, tea-playwright-*, retired-console-check and every recipe-factory task stay in local-recipes (they import the atlas/CFE stack).
- (direction) 2026-09-16 operator: Frames adoption is on the critical path to the intelligence hub, the unifying strategy and launching pyforge-foundry; the two PRs we grounded on (openteams-ai/frame-spec#28, #29) have had edits — analyze and replan. We continue to operate as if the latest heads become v0.3. NO commits or comments to openteams-ai repositories.
- (capability) CAP-6 — Frame draft re-grounding: the estate claims conformance the way the draft now asks. intent: (a) the nine Frames drop the invented version token — bare 'type: frame' (draft §6.2.1 09-14: a document written to the draft omits the token until a release assigns one); (b) docs/foundry/frames/README.md carries an 'Upstream pin' block naming the #28 and #29 head SHAs we conform to, re-pinned only with a memlog line; (c) an opt-in steward task 'frame-upstream-check' fetches openteams-ai/frame-spec at the pinned #29 SHA into a temp dir and runs its validate_frame.py over docs/foundry/frames/ (and --check-profile over our profile) — never vendored, never a gate, never a write upstream; (d) PyForge publishes its conformance profile docs/foundry/frames/conformance-profile.yaml (§7 MUST; #29 YAML shape; specification draft-mcandrew-frame-spec-00): reads markdown, writes none, resolves no composition, treats visibility as declared intent, and — §9 — declares that no trust configuration exists yet so every reachable source is trusted, which is exactly the state the future loader must change. success: upstream validate_frame.py at the pinned SHA reports 9/9 OK and --check-profile accepts the profile; in-repo frame-preflight stays green; the deferred-work accepted-risk entry on #28 carries today's date and the two SHAs.
- (constraint) CAP-6: §9 Security Considerations (record source beside content; MUST NOT load from an untrusted source; trust-every-source declared, never inferred; composed set made visible before loading; identifier is asserted, not authenticated) are acceptance criteria for any story that loads a Frame into an agent's context (marshal-token-economy [context.wire]); no such loader exists yet, and none may land without them. #29's spec/fixtures/composition are the conformance fixtures for a composing reader.
- (decision) 2026-09-16 status shipped -> ready: a station Spec is perpetually ready (spec-one-chain-per-station CAP-6, CHAIN-STANDARD § 4); 'shipped' was the 08-15 bookkeeping flip for CAP-1..4 and is now false with CAP-5/CAP-6 open. CAP-1..4 keep their verified lines. SPEC.md re-derived from this log (CAP-5, CAP-6, two constraints appended; nothing above them edited).
- (decision) 2026-09-16 CAP-5 corrected (operator question: does the minimal env drive marshal token efficiency by default?): the task-import method gave a functional floor only; the token-economy kit leaves no trace in task commands because marshal reaches it by shutil.which / skill-file detection and degrades silently when absent (MRS-DISP-033 WARN, the 2026-09-01 spend-scare shape). Probed: lean pyforge-marshal env has bmad-loop 0.11.1 + tmux but NO headroom; caveman is absent from every env (it is a per-harness skill installed via npx skills add, so the env can only guarantee nodejs). CAP-5 now: pyforge-guild includes the python feature (nodejs, gh, bmad-method) and declares headroom-ai explicitly; success adds 'marshal seed check reports the headroom kit item present and a dispatch dry-run in pyforge-guild raises no MRS-DISP-033'; Epic 46 (marshal-token-economy CAP-19..24, all backlog) names pyforge-guild as the environment its stories are verified in -- the env is the precondition, 46.x is the mechanism.
- (change) 2026-09-16 Story 63.1 landed: feature.pyforge-guild (deps) + feature.guild-tasks (the 51 moved Guild tasks, NO deps) + environment pyforge-guild = python + pyforge-guild + guild-tasks, no-default-feature; local-recipes includes guild-tasks only. Two facts found at the first solve reshaped CAP-5's mechanism, not its intent: (1) a deps-carrying feature cannot be included by local-recipes because station packages range-pin engines to the verified minor (pyforge-warden osv-scanner >=2.4,<2.5; pyforge-marshal tomlkit >=0.13,<0.13.3) while the factory floats (>=2.5.1; >=0.15.1) -- so tasks and deps split into two features, one task name still in exactly one feature; (2) pyforge-warden is NOT in pyforge-guild: doctor does not require it at runtime (run-deps jsonschema/pyyaml/mcp/pyforge-core) and degrades the seam to cannot-evaluate, which is how detectors-ci has always run in local-recipes; the warden seam stays -e pyforge-doctor / -e pyforge-warden. tomli dropped (3.14 stdlib tomllib; llms-full-check flagged it undocumented). The nine adopted bmad-suite members added (~13 MB) because marshal seed check reports each as not installed. Measured: 859 MB on disk (local-recipes 10 GB); detectors-ci 32/32 green in pyforge-guild; headroom, node/npx, gh, git, uv, tmux, bmad-loop 0.11.1, marshal, steward all on PATH; local-recipes still resolves every moved task. marshal seed check residual FAILs are non-environmental: bmad-method-wds-expansion is referenced by the seed manifest but carried by no env (pre-existing; adoption-register item), .marshal/seed-state.yml never adopted (pre-existing), and the three context layers are declared OFF in policy -- Epic 46 (marshal-token-economy CAP-19..24) is the mechanism that flips them; this env is the floor it lands on. default-env: directive in pixi.toml now names pyforge-guild (scripts/load-env.sh reads it). pixi_env_matrix updated on the unifying-strategy Dream.
- (decision) 2026-09-16 (operator) pixi `default` environment = an ALIAS of pyforge-guild (identical feature list). Measured first: moving the 51 tasks into guild-tasks made a bare `pixi run <guild task>` AMBIGUOUS (pixi: provided by local-recipes, pyforge-guild) and bare `pixi shell` pointed at a non-existent default env; verified in a scratch workspace that pixi resolves an ambiguous task to the env NAMED default. So the Guild env is the default by the tool, not by a doc: bare `pixi run fleet-picture` runs in default, `pixi shell-hook` activates envs/default. Cost accepted: a second ~860 MB directory when both names are used. The rename alternative (env named default, no pyforge-guild) was declined -- the name is what every surface and CAP-5 carry.
- (decision) 2026-09-16 (operator) Tier-3 feed in a fresh clone: SEED BY COPYING THE TWIN. Verified in a fresh shallow clone: implementation-artifacts/ does not exist (it is a symlink to the primary checkout only in local worktrees), `sprint-ledger-sync --repair-feed` SKIPS a missing feed ('no Tier-3 feed at ...', NOTHING promoted), so a cloud agent could not land a ledger flip. The twin is written in the same shape as the feed (promote_sprint_status.py docstring), and `mkdir -p implementation-artifacts && cp planning-artifacts/sprint-status-ledger.yaml implementation-artifacts/sprint-status.yaml` then `sprint-ledger-sync` reports `unchanged` with a clean tree. This is the one sanctioned remedy; Story 63.4's session check prints it when the feed is absent, and every fold brief carries it as step 0. No code change to promote_sprint_status.py (marshal's) -- a procedure, not a mode.
- (change) 2026-09-16 Story 63.2 landed and Stories 63.3/63.4 minted (operator: 'you decide -- ready for the cloud/Cursor handoff'). 63.2: 21 guild-task invocations flipped -e local-recipes -> -e pyforge-guild in the LIVING guidance only (AGENTS.md, CLAUDE.md, _bmad-output/PROJECTS.md, docs/dreams/README.md, docs/reference/README.md, docs/reference/library-llms-full.md); archived Dreams and legacy docs/specs keep their text as record. Mechanism surfaces, not prose: .cursor/environment.json force-tracked (Cursor Cloud Agents install pyforge-guild; .cursor/ is gitignored so -f), .claude/hooks/session-start.sh installs pyforge-guild instead of pyforge-doctor alone (Claude Code web), .github/workflows/copilot-setup-steps.yml NEW (the Copilot coding agent sandbox had no pixi at all -- every documented command was dead), tests/test_load_env.sh read the live pixi.toml and went 7/11 red on the directive flip -> 11/11. pr-preflight's test-ci leg pinned to local-recipes (the CFE suite is a factory task; the first pr-preflight from the Guild env failed 'could not find the task test-ci'); pr-preflight from pyforge-guild then exit 0 (detectors-ci 32/32, CFE 9130 passed, 8 station suites + coverage gates). Enforcement survey recorded as 63.3 (one closed session_denials list in guild-roster.json + one hook script for Claude PreToolUse Bash/Edit/Write deny and Cursor beforeShellExecution deny; Gemini/Copilot CLI/Devin named instruction-only -- no verified deny surface) and 63.4 (pyforge steward session check: pixi/env/bmad-method/token kit/gh auth+rate limit/codegraph/Tier-3 feed/scribe reachability as findings, called identically from the four entry points). CI is already lean (pyforge-ci, detectors, per-station envs) -- not a gap; a later story may fold pyforge-ci and detectors into pyforge-guild (three lean envs doing the Guild's job is a one-name-one-job smell). 64.1/64.2 rows promoted to done in the same sync (merged in PR #1387 but left backlog -- the 'respawns on every drain' pitfall).
- (change) 2026-09-16 PR #1388 first CI red: scripts-suite (tests/scripts in -e pyforge-ci, detectors.yml) -- tests/scripts/test_deck_facts.py pinned bmad-groundtruth to cfg['feature']['local-recipes']['tasks'] (KeyError after the 63.1 move). Fixed to resolve the task through whichever ONE feature declares it (asserts exactly one). Same sweep: pyforge-steward tests/unit/test_guards.py anchored on '[feature.local-recipes.tasks.detectors-ci]', find() returned -1 and its 'guards not in ci_block' assertion had gone vacuously true -- re-anchored on guild-tasks and asserts the anchor is found. The two dashboard not-in assertions (test_fresh_clone_class_path, test_invariants) guard RETIRED tasks and stay valid. Lesson applied: pr-preflight gains a fifth pinned leg {pyforge-doctor-scripts-test, pyforge-ci} -- that lane had no local equivalent, the gap pr-preflight exists to close. Local: scripts-suite 489 passed, steward guards 13 passed.
- (event) 2026-09-17 steward one-chain fold: remint 82 CAPs from 31 absorbed Spec folders + existing station CAPs; covers-dreams 35; status stays ready (CAP-6).
- (provenance) 2026-09-17 absorbed spec-bmad-eval-quality into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-bmad-method-core-upgrade into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-bmad-module-provisioning into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-bmad-suite-channel-product into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-bmad-suite-install-class-wiring into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-bmad-suite-lifecycle into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-bmad-suite-metapackage into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-build-league-scorecard into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-developer-machine-bootstrap into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-enterprise-airgap into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-intelligence-hub into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-jira-github-projects-sync into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-local-ocp-hybrid-environment into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-mcp-era-isolation into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-mcp-factory-stdio-translator into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-mcp-host-real-station-tools into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-multi-repo-workspaces into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-ocp-as-a-portability-profile into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-platform-datastores-consumed-not-self-hosted into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-platform-dev-boots-local into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-platform-fifteen-factors into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-platform-image-one-pixi-env into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-platform-object-storage-kind into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-python-agent-platform into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-scratch-worktree-lifecycle into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-secure-live-dashboards into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-self-hosted-bmad-marketplace into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-suite-scaffold-and-mybmad-sidecar into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-unified-container into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-vocabulary-one-name-one-job into spec-pyforge-steward (pointer + memlog + companions kept).
- (provenance) 2026-09-17 absorbed spec-work-passports-dated-extracts into spec-pyforge-steward (pointer + memlog + companions kept).
- (capability) CAP-1 — from spec-pyforge-steward: intent - An operator can issue, scope, rotate, audit, inventory, and record revocation of credentials through one CLI (`steward keys`), with the historical `JFROG_API_KEY` cross-host leak pattern closed as a named automated regression test. success - A credential declared with an explicit host allowlist is never attached to a request outside it; `encrypt`/`decrypt` round-trips a fixture exactly via `age`; `rotate` re-encrypts every secret under a newly generated identity and the old identity fails to decrypt afterward, with no calendar/cron auto-rotation path; `audit --drift` reports the historical unconditional-injection pattern against a fixture and reports clean against today's fixed `_http.py`; `list` enumerates identities with scope/last-rotated metadata and never prints a raw secret value under any flag; `revoke` marks an identity retired and prints manual remediation guidance with zero third-party API calls; a dedicated conformance test fails loudly if the host-gating logic is ever removed or bypassed. ← spec-pyforge-steward CAP-1 (shipped 2026-09-17)
- (capability) CAP-2 — from spec-pyforge-steward: intent - An operator can build and publish the Pages dashboard through one reconciled command (`steward deploy dashboard`) instead of hand-running `dashboard-gen` + `git push`. success - Running twice with no source change between runs results in zero commits on the second run; a real change to the generated output results in exactly one new commit containing exactly the changed files, pushed to the existing Pages-serving branch; `--dry-run` performs the build+diff and prints it without committing or pushing; `status` reports the last successful deploy's commit SHA and timestamp read from Git history alone, with no separate state store. ← spec-pyforge-steward CAP-2 (shipped 2026-09-17)
- (capability) CAP-3 — from spec-pyforge-steward: intent - An operator or an unattended bmad-loop session can materialize any named pixi environment or a bmad-loop runner+environment together, list what exists, and verify the `environment.yaml` sync gate, without recalling raw pixi/`bmad-loop-worktree` syntax. success - A valid environment name materializes via `pixi install -e`; an invalid name reports a clear error listing valid names rather than surfacing pixi's raw error; `--runner bmad-loop --env <name>` wraps `scripts/bmad-loop-worktree` to materialize a worktree plus its named environment in one call, surfacing underlying failures clearly rather than leaving orphaned state unreported; `--list` enumerates every `pixi.toml` `[environments]` entry with its composing features (plus `--json`); `--verify` wraps the existing sync-gate check and reports drift with a non-zero exit when out of sync. ← spec-pyforge-steward CAP-3 (shipped 2026-09-17)
- (capability) CAP-4 — from spec-pyforge-steward: intent - An operator can declare and query a machine-readable resource ceiling and get an honest signal about spend rather than a fabricated number. success - `budget set --cap <amount><currency>/<period>` records a stable, documented schema and rejects a malformed cap value without writing a corrupt entry; `budget show` prints the declared ceiling(s) in human and `--json` form, and reports clearly (not a crash or misleading zero) when none is declared; `budget check` returns one of three distinct exit codes — not-configured / under-budget / over-budget — never collapsing "no data" into a pass, with no cloud-cost-SDK or Kubecost/OpenCost/Infracost client import anywhere in the codebase. ← spec-pyforge-steward CAP-4 (shipped 2026-09-17)
- (capability) CAP-5 — from spec-bmad-eval-quality: intent - Absorbed contract of spec-bmad-eval-quality (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-bmad-eval-quality CAP-1 (shipped 2026-09-09)
- (capability) CAP-6 — from spec-bmad-method-core-upgrade: intent - Absorbed contract of spec-bmad-method-core-upgrade (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-bmad-method-core-upgrade CAP-1 (shipped 2026-09-09)
- (capability) CAP-7 — from spec-bmad-module-provisioning: intent - Absorbed contract of spec-bmad-module-provisioning (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-bmad-module-provisioning CAP-1 (shipped 2026-09-09)
- (capability) CAP-8 — from spec-bmad-suite-channel-product: intent - Absorbed contract of spec-bmad-suite-channel-product (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-bmad-suite-channel-product CAP-1 (shipped 2026-09-09)
- (capability) CAP-9 — from spec-bmad-suite-install-class-wiring: intent - Absorbed contract of spec-bmad-suite-install-class-wiring (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-bmad-suite-install-class-wiring CAP-1 (shipped 2026-09-17)
- (capability) CAP-10 — from spec-bmad-suite-lifecycle: intent - Absorbed contract of spec-bmad-suite-lifecycle (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-bmad-suite-lifecycle CAP-1 (shipped 2026-09-13)
- (capability) CAP-11 — from spec-bmad-suite-metapackage: intent - Absorbed contract of spec-bmad-suite-metapackage (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-bmad-suite-metapackage CAP-1 (shipped 2026-09-09)
- (capability) CAP-12 — published measure catalog.: intent - an operator-authored set covering human, agent, and team, success - the eight first-cut ids are in `measure-catalog.md` and ← spec-build-league-scorecard CAP-1 (shipped 2026-09-15)
- (capability) CAP-13 — add, switch, and archive sources.: intent - a new already-counted source is a new row; a dead source is success - a new row starts `off`; flipping `on` / `off` / `archived` ← spec-build-league-scorecard CAP-2 (shipped 2026-09-15)
- (capability) CAP-14 — consumers cite `on` rows only.: intent - Herald, Atlas, Marshal, and Doctor (and Hub Outcome Guards success - citing `off` or `archived` — or an id not in the catalog — ← spec-build-league-scorecard CAP-3 (shipped 2026-09-15)
- (capability) CAP-15 — from spec-developer-machine-bootstrap: intent - Absorbed contract of spec-developer-machine-bootstrap (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-developer-machine-bootstrap CAP-1 (shipped 2026-09-09)
- (capability) CAP-16 — from spec-enterprise-airgap: intent - Absorbed contract of spec-enterprise-airgap (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-enterprise-airgap CAP-1 (shipped 2026-09-09)
- (capability) CAP-17 — Vocabulary alignment only.: intent - the Charter's Lexicon and the station roster map once to the paper's six shared success - every row of `vocabulary-map.md` names its Charter term and its gap, and the ← spec-intelligence-hub CAP-1 (shipped 2026-09-13)
- (capability) CAP-18 — A declared validation strategy per run.: intent - a bmad-loop run or `marshal factory spin` declares its Guards by stage, its success - one run yields one structured Track record with enumerated fields and a stated ← spec-intelligence-hub CAP-3 (shipped 2026-09-13)
- (capability) CAP-19 — Guards as a library.: intent - detectors, Warden axes and review lenses exposed as reusable Guards, each with a success - a Spec can name which Guard categories it lacks; Warden stays the sole PR ← spec-intelligence-hub CAP-4 (shipped 2026-09-13)
- (capability) CAP-20 — bidirectional propagation of status, assignee, and identity link.: intent - Moving a GitHub Projects V2 item's status reaches the linked Jira issue, and success - a status change made on either board appears on the other without manual ← spec-jira-github-projects-sync CAP-1 (shipped 2026-09-17)
- (capability) CAP-21 — zero-loop guarantee.: intent - An update one side makes *because of a sync* never triggers a sync back the success - a demonstrated (not merely claimed) test in which a synced update provably ← spec-jira-github-projects-sync CAP-2 (shipped 2026-09-17)
- (capability) CAP-22 — idempotent update processing.: intent - Re-processing the same update payload twice produces the same end state both success - delivering an identical payload twice leaves both systems byte-identical to ← spec-jira-github-projects-sync CAP-3 (shipped 2026-09-17)
- (capability) CAP-23 — fail loud, fail alone on broken links.: intent - An item missing its cross-system link fails loudly in a log — never silently success - a batch containing one unlinked item completes for all other items and emits ← spec-jira-github-projects-sync CAP-4 (shipped 2026-09-17)
- (capability) CAP-24 — explicit status-vocabulary translation.: intent - Status vocabulary differences between the two systems ("Done" vs "Closed") are success - every status value crossing the boundary passes through the translation ← spec-jira-github-projects-sync CAP-5 (shipped 2026-09-17)
- (capability) CAP-25 — from spec-local-ocp-hybrid-environment: intent - Absorbed contract of spec-local-ocp-hybrid-environment (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-local-ocp-hybrid-environment CAP-1 (shipped 2026-09-17)
- (capability) CAP-26 — dual-era atlas face on the host path: intent - An agent can POST to `/stations/atlas/mcp` on the live host success - CRC `/ht/` is 200. POST initialize `protocolVersion` ← spec-mcp-era-isolation CAP-1 (shipped 2026-08-27)
- (capability) CAP-27 — isolated mcp-host env: intent - A pixi environment exists that materializes mcp 2.x and the success - `pixi install --frozen -e mcp-host` solves. That env's ← spec-mcp-era-isolation CAP-2 (shipped 2026-08-27)
- (capability) CAP-28 — host proxies; sidecar down is loud: intent - The host ASGI process keeps the `/stations/<name>/mcp` success - With the URL set, dispatch does not import `MCPServer` in ← spec-mcp-era-isolation CAP-3 (shipped 2026-08-27)
- (capability) CAP-29 — cluster overlay cannot omit mcp-host: intent - A Helm install / `helm template` of the platform chart (vanilla success - `helm template` fails if `mcpHost.image.repository` is empty. ← spec-mcp-era-isolation CAP-4 (shipped 2026-08-27)
- (capability) CAP-30 — stdio wrap of a FastMCP 3 child: intent - A modern client can start a factory tool over stdio and success - Child sees `initialize` handshake when the client is ← spec-mcp-factory-stdio-translator CAP-1 (shipped 2026-08-26)
- (capability) CAP-31 — explicit non-CRC claim: intent - Operators never treat this process as the host-face pin fix. success - Docs and CLI help state it does not change gunicorn ← spec-mcp-factory-stdio-translator CAP-2 (shipped 2026-08-26)
- (capability) CAP-32 — from spec-mcp-host-real-station-tools: intent - An agent that calls a station's real MCP tool (e.g. marshal's success - `POST /stations/marshal/mcp` with `publish_loop_run` on a real deployed ← spec-mcp-host-real-station-tools CAP-1 (shipped 2026-09-12)
- (capability) CAP-33 — from spec-mcp-host-real-station-tools: intent - A station with no real in-process MCP app keeps working exactly as success - `spec-mcp-era-isolation`'s own CAP-1..3 acceptance criteria (dual-era ← spec-mcp-host-real-station-tools CAP-2 (shipped 2026-09-12)
- (capability) CAP-34 — from spec-mcp-host-real-station-tools: intent - CAP-3's own attended-CRC exercise can complete its one unproven item. success - `spec-run-state-one-publisher`'s verification record is re-run and its ← spec-mcp-host-real-station-tools CAP-3 (shipped 2026-09-12)
- (capability) CAP-35 — from spec-multi-repo-workspaces: intent - Absorbed contract of spec-multi-repo-workspaces (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-multi-repo-workspaces CAP-1 (shipped 2026-09-09)
- (capability) CAP-36 — from spec-ocp-as-a-portability-profile: intent - Absorbed contract of spec-ocp-as-a-portability-profile (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-ocp-as-a-portability-profile CAP-1 (shipped 2026-09-09)
- (capability) CAP-37 — from spec-platform-datastores-consumed-not-self-hosted: intent - Absorbed contract of spec-platform-datastores-consumed-not-self-hosted (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-platform-datastores-consumed-not-self-hosted CAP-1 (shipped 2026-09-11)
- (capability) CAP-38 — Local leaf on platform-dev.: intent - The operator can load `config.settings.local` and run success - `django-debug-toolbar` is declared on ← spec-platform-dev-boots-local CAP-1 (shipped 2026-09-13)
- (capability) CAP-39 — from spec-platform-fifteen-factors: intent - Absorbed contract of spec-platform-fifteen-factors (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-platform-fifteen-factors CAP-1 (shipped 2026-09-09)
- (capability) CAP-40 — one frozen env: intent - The platform image is produced from a single `pixi install --frozen -e success - After `pixi lock`, a planted overlap (e.g. pip `mcp` vs conda `mcp`) is a ← spec-platform-image-one-pixi-env CAP-1 (shipped 2026-08-25)
- (capability) CAP-41 — Containerfile drops the pip installer: intent - The runtime image no longer runs `python3 -m pip install --no-deps` for success - `rg 'pip install --no-deps' src/platform/Containerfile` is empty; the ← spec-platform-image-one-pixi-env CAP-2 (shipped 2026-08-25)
- (capability) CAP-42 — pixitainer-docker re-eval: intent - Re-test the **Docker/Podman** pixitainer backend against the Story 10.3 success - A dated Design Note lists each table row pass/fail with the CLI/package ← spec-platform-image-one-pixi-env CAP-3 (shipped 2026-08-25)
- (capability) CAP-43 — from spec-platform-object-storage-kind: intent - Absorbed contract of spec-platform-object-storage-kind (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-platform-object-storage-kind CAP-1 (shipped 2026-09-10)
- (capability) CAP-44 — The host renders from the accelerator shape.: intent - A cookiecutter-django service with FastAPI integration is the platform's one success - The rendered host boots against PostgreSQL + Redis with no other ← spec-python-agent-platform CAP-1 (shipped 2026-09-12)
- (capability) CAP-45 — Langflow joins as a pluggable Django application.: intent - Pattern A of langflow-django-plugin: ASGI mount forwarding `/api/v1/`, success - Langflow flows execute through the mounted app with its tables confined to ← spec-python-agent-platform CAP-2 (shipped 2026-09-12)
- (capability) CAP-46 — DB-GPT joins the platform through its configured integration pattern.: intent - DB-GPT integrates via whichever pattern pap:AD-17's per-engine config switch success - Text-to-SQL / data-chat round-trips succeed end-to-end through whichever ← spec-python-agent-platform CAP-3 (shipped 2026-09-12)
- (capability) CAP-47 — Async work never blocks Django.: intent - Celery over Redis carries LLM/AWEL work (the plugin dreams' Pattern B/D success - A long-running agent task completes via the worker path while the host stays ← spec-python-agent-platform CAP-4 (shipped 2026-09-12)
- (capability) CAP-48 — One environment, factory-sourced, 3.14-bound.: intent - The platform's environment is a single conda-space solve from mirrored success - The lockfile solves reproducibly from a mirror-only channel config; the 3.14 ← spec-python-agent-platform CAP-5 (shipped 2026-09-12)
- (capability) CAP-49 — Air-gap parity is a test, not a hope.: intent - Every deployment artifact resolves inside the boundary: internal-registry success - A build + deploy executed with external egress blocked succeeds end-to-end; ← spec-python-agent-platform CAP-6 (shipped 2026-09-12)
- (capability) CAP-50 — from spec-scratch-worktree-lifecycle: intent - `steward workspace start <slug> [--from <branch>]` creates a worktree at a success - One command yields a ready worktree; its path is printed (and in `--json`, ← spec-scratch-worktree-lifecycle CAP-1 (shipped 2026-09-09)
- (capability) CAP-51 — from spec-scratch-worktree-lifecycle: intent - `steward workspace ls` enumerates every currently-open scratch worktree this tool success - `ls` returns instantly regardless of worktree count and reports only ← spec-scratch-worktree-lifecycle CAP-2 (shipped 2026-09-09)
- (capability) CAP-52 — from spec-scratch-worktree-lifecycle: intent - `steward workspace status [<slug>]` reports, per worktree, what `ls` deliberately success - For each (or the named) tool-created worktree, `status` reports the three health ← spec-scratch-worktree-lifecycle CAP-3 (shipped 2026-09-09)
- (capability) CAP-53 — from spec-scratch-worktree-lifecycle: intent - `steward workspace clean [--merged-only]` removes scratch worktrees whose branch success - After `clean --merged-only`, no merged tool-created worktree remains open; ← spec-scratch-worktree-lifecycle CAP-4 (shipped 2026-09-09)
- (capability) CAP-54 — from spec-scratch-worktree-lifecycle: intent - The tool keeps a bookkeeping record of which worktrees *it* created, and success - With loop-home worktrees present on disk, `ls` and `clean` enumerate zero of ← spec-scratch-worktree-lifecycle CAP-5 (shipped 2026-09-09)
- (capability) CAP-55 — identity and role arrive from the request; the pattern authenticates no one.: intent - An adopting dashboard obtains the caller's identity and group membership from success - the same dashboard runs unmodified behind two proxies that use different ← spec-secure-live-dashboards CAP-1 (shipped 2026-09-09)
- (capability) CAP-56 — row-level isolation is declared, not implemented, by the adopter.: intent - A dashboard states which column carries access and which roles exist; the success - two concurrent users of different roles produce **one** upstream fetch and ← spec-secure-live-dashboards CAP-2 (shipped 2026-09-09)
- (capability) CAP-57 — an unauthorized page is absent, not hidden.: intent - The navigation tree is constructed from the caller's role, so a page a user success - the served DOM for a low-privilege caller contains no reference to a ← spec-secure-live-dashboards CAP-3 (shipped 2026-09-09)
- (capability) CAP-58 — the audit trail records what was seen, not merely that something happened.: intent - Every data load, filter, navigation and export lands in a durable trail success - for any completed session the trail can answer "who saw how many rows of ← spec-secure-live-dashboards CAP-4 (shipped 2026-09-09)
- (capability) CAP-59 — export is gated server-side, and may be encrypted.: intent - Only authorized roles obtain a data export. Authorization is enforced where success - a direct request to the export endpoint by an unauthorized caller — with no ← spec-secure-live-dashboards CAP-5 (shipped 2026-09-09)
- (capability) CAP-60 — the deployment perimeter ships with the pattern.: intent - An adopter receives a production-shaped runtime rather than assembling one: success - the same artifact runs on a laptop against a file-backed database and in ← spec-secure-live-dashboards CAP-6 (shipped 2026-09-09)
- (capability) CAP-61 — the security boundaries are proven by tests that cannot pass vacuously.: intent - Isolation is asserted by a suite that impersonates different identities and success - each isolation test fails when its guard is removed, demonstrated rather ← spec-secure-live-dashboards CAP-7 (shipped 2026-09-09)
- (capability) CAP-62 — the same dashboard definition ships hosted or static, without a fork.: intent - A board that needs no isolation can be published as a free static site on success - the same board is published both ways without anyone hand-re-deriving its ← spec-secure-live-dashboards CAP-8 (shipped 2026-09-09)
- (capability) CAP-63 — estate catalog and config.: intent - listings are edited in git we control; backends and sources success - `bmad-method install` / `--custom-content` and Claude/Codex ← spec-self-hosted-bmad-marketplace CAP-1 (shipped 2026-09-15)
- (capability) CAP-64 — publish path plus steward trust review.: intent - a module enters the catalog through Builder / module-template success - a listing cannot appear without a recorded review, unless it ← spec-self-hosted-bmad-marketplace CAP-2 (shipped 2026-09-15)
- (capability) CAP-65 — ship backends.: intent - an air-gapped or estate-local machine gets a snapshot without success - default ship path is a noarch pixi/conda index package on ← spec-self-hosted-bmad-marketplace CAP-3 (shipped 2026-09-15)
- (capability) CAP-66 — thin browse list.: intent - an operator can read the catalog without opening YAML by success - a generated index and/or a page in existing chrome lists ← spec-self-hosted-bmad-marketplace CAP-4 (shipped 2026-09-15)
- (capability) CAP-67 — estate Frame index.: intent - list, share, and add-as-a-reviewed-listing the Frames that success - `docs/foundry/frames/` is a source; a new Frame listing is a ← spec-self-hosted-bmad-marketplace CAP-7 (shipped 2026-09-15)
- (capability) CAP-68 — from spec-suite-scaffold-and-mybmad-sidecar: intent - `bmad-module-template` is an authoring tool beside success - Register row 11 is `wield (authoring tool only)`. ← spec-suite-scaffold-and-mybmad-sidecar CAP-1 (shipped 2026-09-13)
- (capability) CAP-69 — from spec-suite-scaffold-and-mybmad-sidecar: intent - mybmad uses the estate PostgreSQL and estate Keycloak/OIDC. success - Prisma applies only in schema `mybmad`. Login is ← spec-suite-scaffold-and-mybmad-sidecar CAP-2 (shipped 2026-09-13)
- (capability) CAP-70 — from spec-suite-scaffold-and-mybmad-sidecar: intent - The host can show mybmad after the same OIDC session. success - django-pyforge chrome lists or embeds mybmad as a ← spec-suite-scaffold-and-mybmad-sidecar CAP-3 (shipped 2026-09-13)
- (capability) CAP-71 — from spec-unified-container: intent - Absorbed contract of spec-unified-container (no CAP bullets parsed). success - Pointer folder + memlog remain; work lives on the station Spec. ← spec-unified-container CAP-1 (shipped 2026-09-11)
- (capability) CAP-72 — the Spec ladder is declared.: intent - the eight live Spec statuses get a stated naming rule, a success - `draft`, `ready`, `in-progress` (grandfathered only), `shipped`, ← spec-vocabulary-one-name-one-job CAP-1 (shipped 2026-09-15)
- (capability) CAP-73 — the BMAD↔Lexicon cross-walk.: intent - BMAD's daily nouns (Epic, Story, Sprint, PRD, Retrospective) map success - the Charter carries the walk (map, never join) and records ← spec-vocabulary-one-name-one-job CAP-3 (shipped 2026-09-15)
- (capability) CAP-74 — the Design tier stops drifting.: intent - practice vocabulary is teaching-only (except method-vs-machinery success - the deck says it is teaching-only; Herald owns the pull; ← spec-vocabulary-one-name-one-job CAP-5 (shipped 2026-09-15)
- (capability) CAP-75 — mint-time story identity and new `DW-` ids.: intent - one function, at mint time, makes the three story spellings success - heading `### Story N.N:` is human-canonical; the ledger key is ← spec-vocabulary-one-name-one-job CAP-6 (shipped 2026-09-15)
- (capability) CAP-76 — remaining declared shapes.: intent - long vs short station form, frontmatter casing, `S-N.N`, commit success - one roster of eight stations (LONG for paths/packages/envs, ← spec-vocabulary-one-name-one-job CAP-7 (shipped 2026-09-15)
- (capability) CAP-77 — `check=` is a finding code.: intent - the field that carries `spec-surface` does not also carry a success - the six `atlas.py` call sites put runtime data in `evidence`; ← spec-vocabulary-one-name-one-job CAP-8 (shipped 2026-09-15)
- (capability) CAP-78 — extract corridor and transports.: intent - inbound vendor extract and outbound filtered extract load success - re-dropping last week's file does not clone passports; an ← spec-work-passports-dated-extracts CAP-1 (shipped 2026-09-15)
- (capability) CAP-79 — work passport and frozen core schema.: intent - UUID is identity; Jira keys and GitHub numbers are nicknames; success - two extracts cannot merge by title; high-value first links ← spec-work-passports-dated-extracts CAP-2 (shipped 2026-09-15)
- (capability) CAP-80 — as-of glass (standup and shipped).: intent - vendor data on the existing Postgres app is dated; empty success - testers see shipped-from-last-inbound; standup cites a ← spec-work-passports-dated-extracts CAP-3 (shipped 2026-09-15)
- (capability) CAP-81 — outbound slice gate.: intent - default deny, named slice, named outbound-signer role success - an unsigned or over-cap file does not leave; they load what ← spec-work-passports-dated-extracts CAP-4 (shipped 2026-09-15)
- (capability) CAP-82 — quarantine (no auto-link).: intent - unlinked inbound rows wait for a human. First 14 days success - no title-match endpoint exists. After the window, a row ← spec-work-passports-dated-extracts CAP-5 (shipped 2026-09-15)
- (event) 2026-09-17 fold surface merge: absorbed Spec file globs unioned onto spec-pyforge-steward (59 globs). parse_surface does not strip YAML quotes — do not quote ** or ordinary paths (that un-governs the tree). Quote only [feature.*] flow tokens. pixi.toml/pixi.lock stay on spec-pixi-candidate-currency. Scoped stamp follows.
- (event by claude) Surface reconcile 2026-09-18 (this station's own Story 59.2 verify red on this repo-wide gate, unrelated to 59.2's own diff -- reconciled here by name). Two drifted paths: (1) commit `eec68aede4` ("docs: fold getting-started and air-gap cluster; stub the binders", pyforge-doctor Story 23.2) changed `docs/keys-runbook.md` by one line, repointing a stale inbound reference into `docs/` after the air-gap-cluster docs/reference/ redirect stubs were deleted -- a mechanical repoint, not a change to this Spec's own contract. (2) commit `25e10952e9` ("fix(ci): refresh doctor/steward live-baseline tests after absorbed specs", PR #1442) changed `tests/unit/test_story_52_1_consume_sidecar.py`: an unrelated red on PR #1436 (this station asserted phrases on an absorbed SPEC stub) was fixed by refreshing the live-baseline fixture assertions to match the now-absorbed Spec's current shape. Both already landed on `main`, neither touches Story 59.2's own diff. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-steward.
- (event by claude) Surface reconcile 2026-09-19: `docs/reference/library-llms-full.md` (co-governed with spec-pyforge-marshal) changed — § 1a gains a `tornado` entry (pyforge-atlas nested run-dep behind the Bokeh server) and the header changelog records it; closes the one `undocumented-dep` finding `llms-full-check` reported on main. Catalog-only; no steward contract moved; scoped-stamped.
- (event by claude) Surface reconcile 2026-09-19 (marshal Story 52.2, spec-pyforge-core CAP-8 — the conformance suite is a PR gate): `.github/workflows/pyforge-pip-install.yml` changed in its header comment ONLY (lines 3-15) — the claim that `pyforge-station-tests.yml` "already runs every one of the tests below" was false (no workflow invoked `pyforge-core-test`; the four sole-ownership meta-tests had never run on a PR) and now reads that the station-tests workflow's new `core-test` job runs pyforge-core's whole `tests/` tree via the pixi task, while this lane's own value is the outside-pixi installation path. No job step, trigger, or venv changed; no steward contract moved. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (decision by claude) One-chain fold 2026-09-19: PR #1507 (a parallel session) minted a standalone Dream + Spec (`spec-sprint-ledger-query-module`, `fold-exemption: cross-station-seam`) for a steward-owned feature — the exemption token is reserved for the kernel/testing-kit seams every station imports (one-chain-per-station), so the pair is folded here: the Dream becomes a dated seed on docs/dreams/pyforge-steward.md (the standalone file archived in place), the Spec's capabilities become CAP-146..149 below and its passport capability binds to the existing CAP-140 (Story 61.2 remains the story of record — #1507's `WorkPassport` Django model is a partial, parallel realization outside 61.2's declared surface: no `vendor_id`, not the existing Postgres join store, 61.1's corridor not landed). Story 63.5 re-binds to CAP-146..149 + CAP-140.
- (capability by claude) CAP-146 — one query engine over every station's tracked ledger ← spec-sprint-ledger-query-module CAP-1 (folded 2026-09-19). Intent: an operator, agent or dashboard filters and inspects story/epic telemetry across all eight stations through one engine (`pyforge.steward.sprint_ledger_query.SprintLedgerQueryEngine`) reading each station's TRACKED sprint-status-ledger.yaml and epics.md — never the Tier-3 feed, never docs/dashboard/data.js — with presets and station/epic/status/text filters. Success: per-station done/backlog/blocked counts equal fleet-picture's for the same tree (agreement test against scripts/fleet_scan.py::parse_sprint_status); every preset/filter has a fixture test.
- (capability by claude) CAP-147 — pluggable, hookable, feature-flagged ← spec-sprint-ledger-query-module CAP-2 (folded 2026-09-19). Intent: formatters, sources and lifecycle hooks are registered, not hard-coded, and every optional integration sits behind a flag resolved from flags.json / environment / CLI override (no SDK dependency in v1), so the core query path has no runtime dependency on any exporter's target. Success: a flag left off refuses the gated export (ok=false), never silently runs it; a registered plugin/hook is exercised by test without editing the engine; the module's only third-party import is yaml.
- (capability by claude) CAP-148 — every consumer gets its own shape from one engine ← spec-sprint-ledger-query-module CAP-4 (folded 2026-09-19). Intent: one query result renders as markdown/summary/table, schema-validated json (the schema ships with the package), Herald's facts-ledger shape, an atlas-dataset payload (steward exports, Atlas renders — canopy:AD-13 / canopy:AD-23), jira-csv, github-json and get_runnable_backlog() for Marshal — with strict stdout/stderr separation and no default write into another station's tree. Success: each formatter round-trips a fixture; the json payload validates; get_runnable_backlog() agrees with marshal's Deps grammar (S-x.y, none, station:S-x.y); diagnostics never reach stdout.
- (capability by claude) CAP-149 — three front doors, one engine ← spec-sprint-ledger-query-module CAP-5 (folded 2026-09-19). Intent: `steward ledger-query` (a counted duty), the pixi tasks sprint-ledger-query / sprint-ledger-postgres-sync, and the bmad-sprint-ledger-query skill wrap the engine rather than re-implement it; the HTMX backlog view reads through it. Success: the duty-count invariants include it; --format choices come from the registry; the skill's documented invocations run verbatim.
- (constraint by claude) Folded from spec-sprint-ledger-query-module (2026-09-19): steward never imports vizro (canopy:AD-13 / canopy:AD-23); exporters never write into another station's tree by default (flag + explicit --output only); the minted UUID is identity and tracker keys are aliases (CAP-140's rule); reads come from TRACKED ledgers only and fleet_scan.parse_sprint_status stays the reader of record; stdout carries payload only.
- (event by claude) Story 63.5 landed 2026-09-19 (PR #1507, parallel session; reviewed and remediated by the operator's session before merge — three review layers, findings on deps grammar, epic status, flags never wired, stdout purity, unescaped HTML, a stock admin on the CAP-4 audit trail, a Postgres sync that could only no-op; all patched or filed as DW): CAP-146..149 shipped; CAP-140 partially realized (WorkPassport model + admin + passport_sync; 61.2 stays backlog for vendor_id / join-store / corridor). Files: sprint_ledger_query.py, dashboard/{models,admin,views_htmx,passport_sync}.py, dashboard/migrations/0002_workpassport.py, cli.py (ledger-query duty), data/sprint-ledger-query.schema.json, tests; pixi tasks; .claude/skills/bmad-sprint-ledger-query/SKILL.md. AGENTS.md gains a pre-PR checklist (items 2/5/6 corrected at review: pr-preflight, memlog-then-stamp, no fold-exemption for station-owned work).
- (event by claude) Correction 2026-09-19: the story of record for CAP-146..149 is Story 65.1 (Epic 65 — the estate sprint-ledger query engine), not 63.5: the 63-5 key never reached main, and a story appended to a done epic reopens it (ledger-regression), so it was re-minted as 65.1 at PR #1507's review. Remediation also added the --epic CLI filter CAP-146 names (the engine had it, the duty did not expose it) and repointed every live code/pixi/schema reference from 63.5 to 65.1; tracked story spec at specs/spec-65-1-reusable-pluggable-feature-flagged-estate-sprint-ledger-query-module-and-bmad-skill.md carries the triage log and Auto Run Result.
- (decision by claude) CAP-149 front doors, correction 2026-09-19: the sprint-ledger-postgres-sync pixi task moves from feature.guild-tasks to feature.pyforge-steward — no env carrying guild-tasks (pyforge-guild/default/local-recipes) has django, so there the task could only ever refuse; pyforge-steward (the [dashboard] extra) is the one env with both django and the steward CLI. sprint-ledger-query stays in guild-tasks. A task registered where it cannot succeed is a trap, not a front door.
- (event by claude) Surface reconcile 2026-09-19 (Story 65.1, PR #1507 review + remediation), every path this landing moves: src/shared/packages/pyforge-steward/src/pyforge/steward/sprint_ledger_query.py, src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py (--epic, --flag, choices from the registry, empty-summary guard), src/shared/packages/pyforge-steward/src/pyforge/steward/data/sprint-ledger-query.schema.json, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/{__init__.py,models.py,admin.py,views_htmx.py,passport_sync.py,migrations/0002_workpassport.py}, src/shared/packages/pyforge-steward/tests/unit/test_sprint_ledger_query.py (added, 54), src/shared/packages/pyforge-steward/tests/unit/test_dashboard_admin_and_htmx.py (added), src/shared/packages/pyforge-steward/tests/unit/test_restore_duty.py (duty count 19), src/shared/packages/pyforge-steward/tests/unit/test_cli.py, src/shared/packages/pyforge-steward/tests/meta/test_invariants.py (lazy-reach pin), pixi.toml (sprint-ledger-query in guild-tasks; sprint-ledger-postgres-sync moved to feature.pyforge-steward), .claude/skills/bmad-sprint-ledger-query/SKILL.md, AGENTS.md (checklist items 2/5/6/7), docs/dreams/pyforge-steward.md (seed entry), docs/dreams/sprint-ledger-query-module.md (archived), docs/dreams/README.md, docs/foundry/capability-ledger.yaml (CAP-146..149 rows), planning-artifacts/epics.md (Epic 65), planning-artifacts/sprint-status-ledger.yaml (65-1 done, epic-65 done), planning-artifacts/deferred-work-ledger.md (DW-FU-65-1, -2, -3), planning-artifacts/specs/spec-65-1-reusable-pluggable-feature-flagged-estate-sprint-ledger-query-module-and-bmad-skill.md (tracked story spec, added), planning-artifacts/specs/spec-61-2-work-passport-and-core-schema.md (note), planning-artifacts/specs/spec-sprint-ledger-query-module/{SPEC.md,.memlog.md,.memlog.pre-bmad-spec.md} (absorbed). Baseline re-stamped scoped for spec-pyforge-steward after this entry.
- (note by claude) Derivation record of the folded spec-sprint-ledger-query-module (folder removed 2026-09-19 — chain-sprawl reads no status, so a post-ruling absorbed folder or archived Dream is a finding forever; the detector's own remedy is 'a dated section of a station Dream and a CAP on that station's Spec', which now exist, and the 20-line record lives here). Origin: the parallel session that opened PR #1507 minted a 35-line Intent/Boundaries/Interface sketch plus a two-line 'Decision Log' (2026-09-19 — Initial Specification: architecture approved: pluggable formatters, source plugins, OpenFeature SDK feature flags, Work Passports UUID minting, Django Admin/HTMX views, multi-system exporters) and Story 63.5 citing CAP-1..4 nothing defined. Re-derived through bmad-spec the same day as CAP-1..5 (→ CAP-146, 147, [CAP-140], 148, 149), constraints (never imports vizro; no default cross-station writes; UUID is identity; TRACKED reads only; no hard-coded public URLs; stdout payload only), non-goals (no second ledger parser — fleet_scan.parse_sprint_status stays the reader of record; no Vizro rendering in steward; no live Jira/GitHub API calls in v1). Assumption 'openfeature-sdk import is guarded' resolved by reading the code: no openfeature import at all, flags are file/env/CLI. Q1 (do exporters' default target paths inside other stations' trees need those Specs to declare them?) resolved at remediation: there are no default target paths any more — only an explicit --output writes. Self-validate pass 1 (coherence) PASS; pass 2 (preservation) PASS. The sketch and its two-liner survive in git at c01fdb3f4d / 473e622f51.
- (note by claude) Citation-form fix 2026-09-19 on this log's own same-day CAP-148 and constraint entries: the slash-joined canopy pair qualified as 'canopy:AD-13 / canopy:AD-23' (ad-citation reads the second half of a slash-joined pair as an unqualified id this project does not define). Wording untouched; no decision changed.
- (event by claude) Surface reconcile 2026-09-19 (fleet preflight; found when doctor 23.4 and steward 60.1 were refused at MRS-GATE-010 within minutes of each other): every queued pre-authored story spec under planning-artifacts/specs/ gains a '## Verification' → '**Commands:**' section bound to this station's marshal-policy verify_commands exactly (271 story specs on disk; touched: spec-59-3-the-charter-carries-the-bmad-cross-walk-and-pitched-stays-optional.md spec-59-4-design-teaching-is-named-the-pull-cannot-silently-rot.md spec-59-5-one-mint-time-slugify-and-two-dw-families.md spec-59-6-shape-hygiene-roster-s-n-n-commits-status-comments.md spec-59-7-atlas-check-is-a-finding-code-only.md spec-60-1-the-catalog-config-names-backends-and-sources.md spec-60-2-publish-uses-tools-we-wield-steward-records-the-review.md spec-60-3-ship-backends-conda-channel-default.md spec-60-4-frame-index-and-a-thin-browse-list.md spec-61-1-corridor-transports-upload-default.md spec-61-2-work-passport-and-core-schema.md spec-61-3-as-of-glass-and-mailed-query.md spec-61-4-signed-outbound-slice.md spec-61-5-quarantine-mint-then-reject.md spec-62-1-the-catalog-names-eight-measures-and-their-states.md spec-62-2-add-switch-and-archive-without-a-rewrite.md spec-62-3-consumers-cite-on-rows-only.md spec-63-3-one-deny-list-one-hook-the-guild-session-guardrails-are-enforced-not-asserted.md spec-63-4-steward-session-check-one-verdict-for-the-session-preconditions-run-from-every-entry-point.md ). The 2026-09-18 mint generated these specs from epics.md without the section, so gate.check_spec_binding saw declared_commands=None; marshal 47.x declared '-k recall' variants not in policy (MRS-GATE-011) — those move to a Manual checks list. Contract text untouched. Scoped stamp after this entry.
- (event by claude) Story 65.1 record closed 2026-09-19: specs/spec-65-1-reusable-pluggable-feature-flagged-estate-sprint-ledger-query-module-and-bmad-skill.md final_revision set to 1ff4b6d212 (PR #1507's merge commit, 'Merge pyforge-steward/65-1 into main'); no other change.
- (event by claude) Story 60.1 dispatched 2026-09-19 (in review; lands with its PR) (CAP-117 — estate catalog and config ← spec-self-hosted-bmad-marketplace CAP-1): git is the edit store and backends/sources are declared in config; a new backend or source is a plugin, not a rewrite. NEW catalog home `src/shared/packages/pyforge-steward/catalog/` (tracked; not in the wheel): `catalog.yaml` (three backends conda-channel on / object-storage off / git-bundle available; five sources estate-listings on / wielded-suite on / public-bmad-catalog off + cite / estate-frames on + path / claude-skill-registry off + `plugin: null` (the companion table's empty slot; skillsctl/SKU B is the later binding); `edit_store.dedicated_repo: null` — no GitHub catalog repo minted, operator confirm pending), `registry/estate.yaml` (`source: estate-listings`, `modules: []`, upstream registry-schema header), GENERATED `.claude-plugin/marketplace.json` (4 wielded module plugins, github source + `source:`/`trust:` tags) and `.agents/plugins/marketplace.json` (`plugins: []`). NEW `src/pyforge/steward/catalog.py` (CatalogConfigError; frozen BackendDecl/SourceDecl/EditStore/CatalogConfig; load_config; Listing; CatalogSourcePlugin/ShipBackendPlugin ABCs; SourceRegistry/BackendRegistry dup→ValueError; defaults EstateListingsSource/WieldedSuiteSource/EstateFramesSource + CondaChannelBackend/ObjectStorageBackend/GitBundleBackend; CatalogEngine check/listings/render/drift/pointers — render and render --check refuse on any collect finding (config-source, listing-no-source, listing-source-mismatch, listing-duplicate, listing-no-repository) and write nothing; CatalogDuty verbs check|list|render [--check]|pointers, --json, --catalog DIR). `cli.py`: `catalog` is the twentieth duty (DUTIES, _HELP, _add_catalog_subparsers, resolve_duty). Tests: NEW `tests/unit/test_catalog.py` (91; catalog.py 98% unit coverage); `tests/unit/test_cli.py` (twenty-duty tuple), `tests/unit/test_restore_duty.py` (len 20). Pre-existing, fixed in the same sitting per team memory: `tests/unit/test_track.py` `_SCHEMA` pointed at `src/shared/packages/src/...` (`_PKG.parents[1]`; latent because `importorskip("jsonschema")` skipped it in the steward env) → `_PKG.parent`; `tests/unit/test_provision_module_installers.py::test_provision_installer_missing_share_raises_before_subprocess` and `tests/unit/test_provision_plugin.py::test_missing_share_package_raises_before_any_write` read the ambient `CONDA_PREFIX` (pyforge-guild ships the tea/labs share trees, so "missing share" was not missing) → pinned to an empty tmp env. `.claude/settings.json` and `pixi.toml` untouched (pointers verb prints the `--custom-source` / `extraKnownMarketplaces` directory forms; github forms wait on dedicated_repo). Interpretation recorded on the story spec: `slots` = every declared backend/source whose state is not `on` (the AC lists object-storage and git-bundle, which do have default plugins; `bound` rides on each row). Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-steward`.
- (event by claude) Story 60.1 hand-landed 2026-09-19 (dispatch run pyforge-steward-20260919T165815607Z-e206103c refused at MRS-GATE-010/011 on the mint's Verification section; #1517 bound it; branch merged with main, Verification rebound to the policy command, landed by hand): CAP-117 (spec-self-hosted-bmad-marketplace CAP-1) — src/shared/packages/pyforge-steward/catalog/{catalog.yaml,registry/estate.yaml,.claude-plugin/marketplace.json,.agents/plugins/marketplace.json}, src/shared/packages/pyforge-steward/src/pyforge/steward/catalog.py (added), cli.py (catalog duty; 20 duties), tests/unit/test_catalog.py (added) + test_cli.py/test_provision_module_installers.py/test_provision_plugin.py/test_restore_duty.py/test_track.py counts. Ledger 60-1 promoted by dispatch_land_finalize after the merge. Scoped stamp after this entry.
- (event by claude) Story 62.1 landed 2026-09-19 (dispatch run pyforge-steward-20260919T195520859Z-92df4b58, merge 6ec56abaad, ledger promote 07cc5a6c9d — autonomous landing; twin promoted by hand, spec-pyforge-marshal:CAP-248 / spec-pyforge-marshal:CAP-250 collision): the catalog names eight measures and their states — planning-artifacts/specs/spec-build-league-scorecard/measure-catalog.md (10 lines). Twin's deferrals ingested by this fallout. Scoped stamp after this entry.
- (event) Surface reconcile 2026-09-19: updated Diátaxis docs and package READMEs
- (event by claude) Story 61.1 landed 2026-09-19 (dispatch run pyforge-steward-20260919T202712518Z-5f5a720a, merge 532673c1ca, ledger promote e6997a3a52 — autonomous; twin promoted by finalize): corridor transports, upload default — src/shared/packages/pyforge-steward/corridor/corridor.yaml (added), src/pyforge/steward/corridor.py (added), cli.py (corridor duty), dashboard/{admin.py,models.py,corridor_load.py,migrations/0003_corridorload.py,__init__.py}, tests/unit/test_corridor.py (added), tests/meta/test_invariants.py, tests/unit/test_cli.py. Same PR (#1529, doctor Story 30.1): .steward/keys-inventory.yaml recording-procedure pointer → docs/how-to/ocp-cluster-bringup.md §5 (the runbook moved out of src/platform/deploy); src/shared/packages/pyforge-steward/README.md docs pointer line. Scoped stamp after this entry.
- (note by claude) Story 61.1 path manifest (expanded): src/shared/packages/pyforge-steward/corridor/corridor.yaml, src/shared/packages/pyforge-steward/src/pyforge/steward/corridor.py, src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py, src/shared/packages/pyforge-steward/tests/unit/test_corridor.py, src/shared/packages/pyforge-steward/tests/meta/test_invariants.py, src/shared/packages/pyforge-steward/tests/unit/test_cli.py, .steward/keys-inventory.yaml, src/shared/packages/pyforge-steward/README.md.
- (note by claude) Story 61.1 path manifest addendum (dashboard surface, expanded): src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/admin.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/models.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/corridor_load.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/migrations/0003_corridorload.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/__init__.py.
- (event by claude) Story 61.2 landed 2026-09-20 (dispatch run pyforge-steward-20260919T233259926Z-5aab25a5, PR #1532, merge 03112ba2d7, ledger promote 00e52b3d37 — autonomous; twin promoted by finalize): work passport + core schema (vendor_id, mint duty). Paths: src/shared/packages/pyforge-steward/src/pyforge/steward/passport.py (added), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/passport_mint.py (added), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/migrations/0004_workpassport_vendor_id.py (added), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/models.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/admin.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/__init__.py, src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py, src/shared/packages/pyforge-steward/tests/unit/test_passport_mint.py (added), src/shared/packages/pyforge-steward/tests/unit/test_cli.py, src/shared/packages/pyforge-steward/tests/unit/test_dashboard_admin_and_htmx.py, src/shared/packages/pyforge-steward/tests/unit/test_restore_duty.py, src/shared/packages/pyforge-steward/tests/meta/test_invariants.py, _bmad-output/projects/pyforge-steward/planning-artifacts/specs/spec-61-2-work-passport-and-core-schema.md. The session reconciled no memlog; this entry is the landing ritual's. Its one deferral (PR label) ingested and closed — the label was applied by dispatch_land. Scoped stamp after this entry.
- (event by claude) Story 61.3 landed 2026-09-20 (dispatch run pyforge-steward-20260920T003216355Z-ce1d1a5c, PR #1539, merge 83d4162aa3, ledger promote 2179bfb24d — autonomous): as-of glass and mailed query — src/shared/packages/pyforge-steward/src/pyforge/steward/glass.py (added), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/glass_query.py (added), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/views_htmx.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/__init__.py, src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py, src/shared/packages/pyforge-steward/tests/unit/test_glass.py (added), src/shared/packages/pyforge-steward/tests/unit/test_cli.py, src/shared/packages/pyforge-steward/tests/unit/test_dashboard_admin_and_htmx.py, src/shared/packages/pyforge-steward/tests/unit/test_restore_duty.py, src/shared/packages/pyforge-steward/tests/meta/test_invariants.py, _bmad-output/projects/pyforge-steward/planning-artifacts/specs/spec-61-3-*.md. The session reconciled no memlog; this entry is the landing ritual's.
- (direction by claude) 2026-09-20 update from docs/dreams/pyforge-steward.md Realization-log entry 'the ledger query answers what is done, what is running, what is next in one call' (operator ask 08:00Z, ruling: seed it and mint into Epic 65). Mint CAP-150; Story 65.2 on the done Epic 65 is re-opened by that story (a new story on a closed epic — the epic key returns to in-progress until it lands), per the operator's explicit choice of Epic 65 over a new epic.
- (capability by claude) CAP-150 — the ledger query answers done / running / next in one call. Intent: every story the engine returns carries a next field — done (status done), running (a live dispatch or loop run is on it), ready (backlog with every declared dependency done — get_runnable_backlog()'s predicate, the Deps grammar restated from marshal's spec_deps), waits on S-x.y[, …] (backlog with an unmet dependency, naming them), blocked (status blocked), or ? (running fact unavailable); --ready and --running filter to those values; the summary carries per-station ready and running counts beside done/backlog/blocked; table/markdown/json carry the column. The running fact is read from marshal's own CLI (marshal watch --fleet --format json through the pyforge.core process primitive, one call per query) — steward never parses marshal's journal and never imports pyforge.marshal; when marshal is absent or the call fails, every next that would read running reads ? and one WARN names the cause (fail-open, CAP-15's posture); the fact is the current checkout's (marshal's Tier-3 is per clone — documented in --help). Success: sprint-ledger-query -- --unimplemented --format table reproduces the 08:00Z hand-composed view (doctor 29.1 running, 24.1/30.2 ready, 24.2/24.3/30.3 waiting; marshal 46.7 running, 46.1/46.2/46.6/46.9/46.10/47.1 ready; steward 61.4 running, 61.5/59.3–59.7/60.2–60.4/62.2/62.3/63.3 ready) from a fixture of those ledgers, epics and a recorded watch payload; --ready equals get_runnable_backlog() exactly; --running equals the watch payload's running rows; unreachable marshal → ? + WARN, exit unchanged; the bmad-sprint-ledger-query skill documents the column and the two filters; pyforge-steward-test green; duty-count invariants unchanged (no new duty — flags on the existing one).
- (event by claude) Story 65.2 minted 2026-09-20 (CAP-150): docs/dreams/pyforge-steward.md (2026-09-20 entry), epics.md (Story 65.2; Epic 65 re-opened), sprint-status-ledger.yaml (65-2 backlog, epic-65 in-progress), specs/spec-65-2-the-ledger-query-answers-done-running-next-in-one-call.md, docs/foundry/capability-ledger.yaml (CAP-150), this Spec's SPEC.md (CAP-150 rendered). Planning only; the skill (.claude/skills/bmad-sprint-ledger-query/SKILL.md) and docs/how-to/monitor-the-fleet.md are the story's own surface at implementation. Scoped stamp after this entry.
- (event by claude) Story 61.4 landed 2026-09-20 (dispatch run pyforge-steward-20260920T073626634Z-f87ff173, merge fa1b45cce4, ledger promote caac08640b — autonomous): signed outbound slice — src/shared/packages/pyforge-steward/src/pyforge/steward/corridor.py, src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/corridor_load.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/models.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/admin.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/migrations/0005_corridorload_signer_corridorload_slice_name.py (added), src/shared/packages/pyforge-steward/tests/unit/test_corridor.py, _bmad-output/projects/pyforge-steward/planning-artifacts/specs/spec-61-4-*.md. The session reconciled no memlog; this entry is the landing ritual's. Epic 61: 61.5 left.
