---
topic: OpenShift runs as a CI portability profile — overlay proven on a real cluster
updated: 2026-09-09T17:52
---

- (direction) Operator (2026-08-24): close pre-existing dream-without-spec INV-1 for ocp-as-a-portability-profile; bind the chain Spec to existing Story 12.9; do not mint a new epic
- (note) Source: docs/dreams/ocp-as-a-portability-profile.md. Dream frontmatter already said specified (2026-08-23) pointing at spec-12-9-ocp-as-a-portability-profile, but that folder never existed — INV-1 matches Dream slug, so the chain Spec must be spec-ocp-as-a-portability-profile/
- (note) Ground: Story 12.1 overlay + invariant tests (template/lint only); Story 12.2 gke-portability-smoke is the sibling CI pattern; Story 12.3 air-gap is orthogonal; spec-local-ocp-hybrid-environment cluster-bringup-facts.md (CRC 2.63.0 / OCP 4.22.7, internal-registry push); deploy/README.md still names no OCP live-cluster verification; no ocp-portability-smoke job in platform-ci.yml today
- (decision) Decompose onto existing Story 12.9 (Epic 12 extension), not a new epic — same shape as the 12.4–12.8 hybrid-environment extension. Story 12.9 remains the implementation vehicle.
- (decision) Ledger 12-9-ocp-as-a-portability-profile currently reads done with no job in platform-ci.yml; flip to backlog so the Spec is not falsely realized. Does not replace Story 12.7 attended closeout.
- (capability) CAP-1 optional OCP smoke job: Platform CI gains ocp-portability-smoke, default off (repo var PLATFORM_CI_OCP_PORTABILITY_SMOKE=true or workflow_dispatch), P1 fan-in on build-platform-image, helm/kubectl from platform-dev (pap:AD-16), CRC/OpenShift Local as named system exception not kind. Success: the job exists, is skippable, and a dispatch with the var set reaches cluster-up or fails naming why the cluster never started
- (capability) CAP-2 Route and SCC proof: smoke asserts Route admission, edge-terminated HTTPS to /ht/ and /admin/login/, migrate hook completion, postgres/redis pods without fixed runAsUser under SCC-assigned UIDs — never port-forward or direct Service curl. Success: a green run records those assertions; a dated note in the 12.1 orbit names any postgres/redis image contingency
- (capability) CAP-3 runner honesty: first implementation proves the chosen runner class (GitHub-hosted ubuntu via crc-github-action, or labeled self-hosted RHEL/Fedora) before claiming OCP verification. Success: the job never reports green on a cluster that never started; unsupported OS is a named skip or a documented self-hosted runner, not a silent pass
- (constraint) pap:AD-11 overlay-only: core chart unchanged; OCP kinds only via existing overlays/ocp/core-overrides.yaml + platform-ocp Route chart
- (constraint) pap:AD-16: helm/kubectl from pixi; CRC and oc are explicit named system exceptions alongside kind
- (constraint) pap:AD-15: paths-filtered Platform CI; non-recipes/ PRs carry the maintenance label
- (constraint) Default off on PR push — CRC is 30–90 min, ~35 GiB, needs CRC_PULL_SECRET; every-PR CRC cost is rejected
- (constraint) One engine (Docker) for CRC, same rationale as 12.2; do not duplicate container job's docker+podman matrix
- (question) Runner class: prove ubuntu-latest + crc-org/crc-github-action, or require a labeled self-hosted RHEL/Fedora runner? OpenShift Local docs exclude Ubuntu/Debian for the Podman Desktop path. Decide at Story 12.9 implementation; never claim verification on a cluster that never started
- (assumption) CRC 2.63.0 / OpenShift 4.22.7 and the internal-registry push commands in cluster-bringup-facts.md are the baseline this job copies
- (event) Self-validate pass 1 (coherence): PASS — 3 CAPs intent+success, constraints bend, non-goals explicit, success signal testable, CAP ids stable
- (event) Self-validate pass 2 (preservation): PASS — Dream claims walked into SPEC.md; adopted companions cluster-bringup-facts.md + spec-12-2; overlay-only/default-off/runner-honesty/non-goals (12.7, prod DNS, every-PR cost) preserved
- (decision by claude) Open question 'runner class: prove ubuntu-latest + crc-org/crc-github-action, or require a labeled self-hosted RHEL/Fedora runner' -- the RUNNER-CLASS half is ANSWERED BY IMPLEMENTATION: `ubuntu-latest` with the CRC action (.github/workflows/platform-ci.yml:1251), 90-minute budget, CRC internal-registry push. The residual is not the class but the PROOF: no run has ever started a cluster (fleet-readiness-decision-batch-2026-09-09 row stB-B2(a)).
- (decision by claude) First-green funding: the operator FUNDS one green `ocp-portability-smoke` run BEFORE Story 44.10 -- set CRC_PULL_SECRET plus PLATFORM_CI_OCP_PORTABILITY_SMOKE and dispatch once. Today `gh variable list` returns only ACTIONS_ENABLED=false and `gh secret list` has no CRC_PULL_SECRET, so the job hard-fails at platform-ci.yml:1265-1272 and CAP-2 ('a green run records those assertions') / CAP-3 ('the first implementation proves the chosen runner class') are unmet. 44.10 disables this repo's CI, so without that run CAP-2/CAP-3 are never provable in either repo and deploy/README.md:134 stays permanently conditional. This is also the gate on any Intelligence-Hub NIC-profile story (fleet-readiness-decision-batch-2026-09-09 rows stB-B2(b) / C14).
- (event) Self-validate Pass 1 (coherence) PASS on the 2026-09-09 re-derive: CAP ids/texts and both companions preserved, status stays 'ready', the runner-class question is replaced by the narrower proof question that is genuinely open
- (event) Self-validate Pass 2 (preservation) PASS: the answered-by-implementation runner class (ubuntu-latest + CRC action at platform-ci.yml:1251), the live evidence that no cluster ever started (ACTIONS_ENABLED=false only, no CRC_PULL_SECRET, hard fail at :1265-1272), the funding decision with its before-44.10 deadline and rejected template-only alternative, and the Intelligence-Hub NIC gate all landed
