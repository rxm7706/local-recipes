---
topic: MCP era isolation — slice 1 host-face pin sidecar
updated: 2026-08-26T05:46
---

- (direction) Three problems: pin (CRC import), dual-era wire (already coded), factory stdio (later translator). Do not merge them.
- (decision) Slice 1 is Pattern B mcp-host sidecar with mcp 2.x; no Langflow; no FastMCP 3. Host proxies POST /stations/<name>/mcp.
- (constraint) Do not lift python-agent-platform to mcp 2.x; do not fold mcp-types/httpx2 into that env.
- (constraint) Do not remint unifying-strategy architecture; lane1-serves-dw-h3 stays no; no FastMCP 4 from PyPI; no Tasks wait.
- (capability) CAP-1: operator reaches dual-era atlas MCP on CRC through host path pattern while web image still imports langflow.
- (capability) CAP-2: mcp-host env solves without FastMCP 3; official MCPServer imports.
- (capability) CAP-3: host dispatch proxies when MCP_HOST_SIDECAR_BASE_URL is set; sidecar down is loud, not a silent skip.
- (note) Pasted BMAD-SPEC-2026-MCP is slice 2 input only (factory stdio).
- (event) Headless express distill from docs/dreams/mcp-era-isolation.md and MCP era program plan 2026-08-26.
- (event) Slice 1 CRC 2026-08-26: /ht/ 200; POST initialize 2025-06-18 echoes; header 2026-07-28 accepted (tools/list -32602 not -32022); unsupported -32022; GET 405 Allow POST; import langflow in web; mcp-host env mcp 2.1.1 no FastMCP. Overlay image crc-20260826c carries mcp_http proxy; sidecar platform-mcp-host:crc-20260826c. python-agent-platform lock remains mcp 1.28.1.
- (decision) 2026-08-26 CAP-4: cluster overlay requires mcp-host (fail-loud Helm + production check). Not slice 3. Not Epic 34. Story 35.1. Analysis: cluster-required.md.
- (event) 2026-08-29 drift reconcile: `pixi.toml` changed by 4 commits since last stamp — mcp ceiling `>=1.28.1`→`>=2.1.1` (`06fe64f603`, on-topic: this spec's own CAP-2 "mcp-host env solves without FastMCP 3; official MCPServer imports" — the ceiling bump is exactly what makes that possible, no CAP text change needed), nodejs floor widened to LTS-only 24||26 (`583bdf4eb5`), django/asgiref/channels_redis pin restore (`daa35ee171`), pixi builder registry sync 0.77.0→0.78.0 (`6ff2c008b4`). Verified via `git log`/`git show`; all four are already-landed, already-understood changes, none altering CAP-1..4. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.
- (event by claude) Surface reconcile 2026-08-30: `pixi.toml` changed once more since the entry above, by `c4aeaace30` (atlas Story 21.1, PR #932) — adds `[feature.pyforge-atlas.tasks.pyforge-atlas-bootstrap]`, an atlas-owned pixi task with no `mcp-host`/CAP-1..4 touch. Verified via `git log --oneline -1 -- pixi.toml` (exactly one commit since last stamp). Scoped-stamped.
- (event by claude) Surface reconcile 2026-08-30, second pass: `pixi.toml` changed once more by `48ddb34b22` ("fix local recipes", already-landed) — routine dependency-pin bumps (python-crontab, asgiref, pyarrow, age) plus enabling `ppt-master`/`codegraph` recipe candidates. No `mcp-host`/CAP-1..4 touch. Scoped-stamped.
- (event by claude) Surface reconcile 2026-08-31: `pixi.toml` changed by 2 more commits since the entry above, verified via `git log --oneline 48ddb34b22..HEAD -- pixi.toml`: `d230e7fce2` (token-economy dep activation — isolates conda-recipe-manager/feedrattler into a grayskull-only feature, unblocks headroom-ai/dbt trio/caveman, no `mcp`/`mcp-host` touch) and `5017329a9f` (atlas 21.6 autosave — only rewrites the `pyforge-atlas-bootstrap` task description/cmd to add `artifactory_downloads` to its pipeline list). Neither touches `mcp` ceiling, `[feature.mcp-host]`, or CAP-1..4. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.
- (event by claude) Surface reconcile 2026-09-01: `pixi.toml` changed once since the 2026-08-31 stamp by `af315d4bd7` (marshal Story 28.7, done) — local-recipes detector task only; no `mcp`/`mcp-host`/CAP-1..4 touch. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.
