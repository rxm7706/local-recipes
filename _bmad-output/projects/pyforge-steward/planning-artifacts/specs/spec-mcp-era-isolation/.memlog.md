---
topic: MCP era isolation — slice 1 host-face pin sidecar
updated: 2026-09-05T13:49
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.


- (direction) Three problems: pin (CRC import), dual-era wire (already coded), factory stdio (later translator). Do not merge them.
- (decision) Slice 1 is Pattern B mcp-host sidecar with mcp 2.x; no Langflow; no FastMCP 3. Host proxies POST /stations/<name>/mcp.
- (constraint) Do not lift python-agent-platform to mcp 2.x; do not fold mcp-types/httpx2 into that env.
- (constraint) Do not remint unifying-strategy architecture; lane1-serves-dw-h3 stays no; no FastMCP 4 from PyPI; no Tasks wait.
- (capability) CAP-1: operator reaches dual-era atlas MCP on CRC through host path pattern while web image still imports langflow.
- (capability) CAP-2: mcp-host env solves without FastMCP 3; official MCPServer imports.
- (capability) CAP-3: host dispatch proxies when MCP_HOST_SIDECAR_BASE_URL is set; sidecar down is loud, not a silent skip.
- (note) Pasted BMAD-SPEC-2026-MCP is slice 2 input only (factory stdio).
- (event) Headless express distill from docs/dreams/mcp-era-isolation.md and MCP era program plan 2026-08-26.
- (event) Slice 1 CRC 2026-08-26: /ht/ 200; POST initialize 2025-06-18 echoes; header 2026-07-28 accepted (tools/list -32602 not -32022); unsupported -32022; GET 405 Allow POST; import langflow in web; mcp-host env mcp 2.1.1 no FastMCP. Overlay image crc-20260826c carries mcp_http proxy; sidecar platform-mcp-host:crc-20260826c. python-agent-platform lock remains mcp 1.28.1.
- (decision) 2026-08-26 CAP-4: cluster overlay requires mcp-host (fail-loud Helm + production check). Not slice 3. Not Epic 34. Story 35.1. Analysis: cluster-required.md.
- (event) 2026-08-29 drift reconcile: `pixi.toml` changed by 4 commits since last stamp — mcp ceiling `>=1.28.1`→`>=2.1.1` (`06fe64f603`, on-topic: this spec's own CAP-2 "mcp-host env solves without FastMCP 3; official MCPServer imports" — the ceiling bump is exactly what makes that possible, no CAP text change needed), nodejs floor widened to LTS-only 24||26 (`583bdf4eb5`), django/asgiref/channels_redis pin restore (`daa35ee171`), pixi builder registry sync 0.77.0→0.78.0 (`6ff2c008b4`). Verified via `git log`/`git show`; all four are already-landed, already-understood changes, none altering CAP-1..4. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.
- (event by claude) Surface reconcile 2026-08-30: `pixi.toml` changed once more since the entry above, by `c4aeaace30` (atlas Story 21.1, PR #932) — adds `[feature.pyforge-atlas.tasks.pyforge-atlas-bootstrap]`, an atlas-owned pixi task with no `mcp-host`/CAP-1..4 touch. Verified via `git log --oneline -1 -- pixi.toml` (exactly one commit since last stamp). Scoped-stamped.
- (event by claude) Surface reconcile 2026-08-30, second pass: `pixi.toml` changed once more by `48ddb34b22` ("fix local recipes", already-landed) — routine dependency-pin bumps (python-crontab, asgiref, pyarrow, age) plus enabling `ppt-master`/`codegraph` recipe candidates. No `mcp-host`/CAP-1..4 touch. Scoped-stamped.
- (event by claude) Surface reconcile 2026-08-31: `pixi.toml` changed by 2 more commits since the entry above, verified via `git log --oneline 48ddb34b22..HEAD -- pixi.toml`: `d230e7fce2` (token-economy dep activation — isolates conda-recipe-manager/feedrattler into a grayskull-only feature, unblocks headroom-ai/dbt trio/caveman, no `mcp`/`mcp-host` touch) and `5017329a9f` (atlas 21.6 autosave — only rewrites the `pyforge-atlas-bootstrap` task description/cmd to add `artifactory_downloads` to its pipeline list). Neither touches `mcp` ceiling, `[feature.mcp-host]`, or CAP-1..4. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.
- (event by claude) Surface reconcile 2026-09-01: `pixi.toml` changed once since the 2026-08-31 stamp by `af315d4bd7` (marshal Story 28.7, done) — local-recipes detector task only; no `mcp`/`mcp-host`/CAP-1..4 touch. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.
- (event by claude) Surface reconcile 2026-09-01, second pass: `[feature.pyforge-ci.dependencies]` gained `"ruamel.yaml"` only; no `mcp`/`mcp-host`/CAP-1..4 touch. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 1 governed path(s) drifted against this spec's recorded baseline (1 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `pixi.toml` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (event) Surface reconcile 2026-09-04 (fleet hygiene, PR #1043): PR #1040's `pixi lock` (64f3f8e584 — `pnpm >=12.3.1` → `>=12.3.2`, lock re-solved) moved this Spec's `pixi.toml` / `pixi.lock` rows without this memlog moving. Accept the current tree as the post-land surface; no contract change, no new product work. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation` (scoped, never a bare `--write-baseline`), from a clean tree so the recorded hashes are committed content.
- (event) Surface reconcile 2026-09-04 (PR #1043, second pass — the Detectors env): `pixi.toml` gains `[feature.detectors.dependencies]` (pyforge-doctor path dep, `ruamel.yaml`, `playwright-python`) and the `detectors` environment (no-default-feature; 86 packages) that `.github/workflows/detectors.yml` now installs pixi-first with `locked: true`, pip as the visible fallback; `pixi.lock` gains its block; `environment.yaml` (derived, `build` env) re-exported and caught up (python 3.14.6→3.14.7, nodejs 24.16→24.19 floors already in `pixi.toml`). Nothing this Spec governs changed in meaning; no contract change. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation` from a clean tree.
- (event) Surface reconcile 2026-09-04 (PR #1043, third pass): `pixi.toml` / `pixi.lock` rows moved for OTHER features (platform-ci-test gains the pyforge-steward path dep; dbgpt-sidecar gains pytz — Platform CI was red on both); nothing this Spec governs changed in meaning. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.
- (event) Surface reconcile 2026-09-04 (PR #1043, fourth pass — the fleet CI health pass): `pixi.toml` rows moved for the python-agent-platform feature only: the `rjsmin` / `rcssmin` `build = "py312*"` pins were dropped (they locked cp312 builds into the py3.14 image env, so the Containerfile's `compress` step was red) and the `cachebox <6` / `openfeature-provider-flagd <0.5.1` ceilings the platform policy suite guards were restored after commit 098f0f0672 had dropped them. Nothing this Spec governs changed in meaning. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation` (scoped, never a bare `--write-baseline`).
- (event) Surface reconcile 2026-09-04 (PR #1043, seventh pass — the local Platform CI replay): `pixi.toml` gains the `platform-ci-local` task under `[feature.local-recipes.tasks]` (`bash scripts/platform-ci-local.sh`): a local replay of `.github/workflows/platform-ci.yml` — the test job step for step, the three image builds, the container job's runtime smokes and golden-path-promotion + the deploy verifier — against an ephemeral PostgreSQL 17/Redis 7 from platform-dev, so a platform change is proven before it is pushed (operator ask, after four sequential Platform CI cycles for four one-line CI-only fixes). No dependency, environment or lock change; `pixi lock --check` clean, `environment.yaml` untouched. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.
- (change by claude) Surface touch 2026-09-05 (PR #1045): pixi.toml's content hash moved because unrelated stations' feature blocks gained a `pyforge-testing-kit` test-time dependency (retro-pyforge-steward-2026-09-04.md action item 11: the fleet's whole-branch diff-guard test mechanics (git subprocess calls computing files/commits changed since origin/main) were consolidated into a shared `pyforge.testing_kit.branch_diff_guard` module (PR #1045).); nothing in this spec's own MCP-era-isolation concern was touched. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.
- (change by claude) Surface touch 2026-09-05: pixi.toml's content hash moved for the new `platform-policy-suite-check` task (retro action item 3 (retro-pyforge-steward-2026-09-04.md, 2026-09-05): a new pyforge-doctor repo-scope source, `platform-policy-suite`, judges src/platform's manifest-only tests/policy subset by shelling out to the platform-ci-test pixi env's own interpreter (cli_bridge.run_pytest, AD-5), so a pixi.toml regression like the openfeature-provider-flagd/cachebox ceiling drop is caught even when Platform CI itself is disabled. WARN when that env is not installed, FAIL only on an actual policy-suite failure.); nothing in this spec's own MCP-era-isolation concern was touched. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.
- (change by claude) Surface touch 2026-09-05 (Stories 42.1 + 42.2 follow-up reviews, landing the post-merge follow-up-review commits of Stories 41.3 / 41.4 / 42.1 / 42.2 / 43.2 on `steward/land-followup-reviews-2026-09-05` (cherry-picked from 2eaa1e0d61 / 97d3d5c1d9 / 482f47fa92 / 295257ecc8 / 7e6b6c4d18 — each was committed minutes after its PR merged and never landed)): `src/shared/packages/django-pyforge/src/django_pyforge/mcp_http.py` — 42.1 widens the sidecar hop's failure catch; 42.2 moves the rate limiter off the request loop. No change to the mcp-host isolation boundary. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-mcp-era-isolation`.
- (note by claude) 2026-09-05 (Story 45.1 pin PR, follows #1059): pixi.toml changed — bmad-eval-quality >=0.2.0.dev0 added to feature.local-recipes' linux-64 and osx-arm64 target tables (unix-only variant on SelfExplainML), bmad-method-wds-expansion pin dropped (upstream deprecated), bmad-labs-skills comment 22 skills; lock re-solved for the pin swap only (no other package moved). Routine suite-pin upkeep this spec's surface already anticipates; no capability change against this spec.
- (note by claude) 2026-09-05 pixi upgrade sweep PR (branch maint/pixi-upgrade-sweep-2026-09-05): pixi.toml changed — operator `pixi upgrade --pinning-strategy latest-up` pass landed as one maintenance PR: pixi 0.78.0->0.79.0 (requires-pixi + $schema + all 15 registered external sites via bump-pixi-version, environment.yaml regenerated), bmad-method >=6.12.0, bmad-suite >=2026.9.5, ~20 other floors bumped, lock re-solved; the cachebox <6 and openfeature-provider-flagd <0.5.1 ceilings the platform policy suite guards were restored after the sweep dropped them. No spec-content change; scoped --write-baseline follows this entry.
- (note by claude) 2026-09-05 hygiene-2 PR (branch maint/hygiene-2-2026-09-05): docs/dreams/mcp-era-isolation.md changed (frontmatter `status: in-progress` -> `specified`; the off-vocabulary value is kept in an inline note; the Spec is the in-progress artifact) and pixi.toml changed (`lint` task -> CI-parity conda-smithy via pixi exec; no dependency/lock change). Scoped --write-baseline follows.
