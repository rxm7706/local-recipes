---
topic: One container, eight stations — a single reproducible image the whole Guild runs from
updated: 2026-09-13T08:33
---

- (provenance) THIS RECORD STARTS HERE. Created 2026-08-09 by marshal S-13.5 (spec-surface-drift-reconciliation CAP-5 / canopy:FR-168): this Spec shipped declaring a `surface:` with no .memlog.md, so spec_surface_check's contract_hash() returned "" and the contract could never move — every governed change would have reported a hard [drift] whose printed remedy ("reconcile the spec") was unreachable, there being no contract to move. Nothing that happened before this entry was captured; do not read the absence of earlier entries as an absence of decisions. The baseline was scoped-stamped in the SAME change that created this file, because a new memlog moves the contract hash off "" and would otherwise downgrade this surface's next real drift from gating [drift] to informational [drift-presumed].
- (note) Governs 1 tracked file at the time of writing; the image is unbuilt.
- (capability) CAP-1 one build, whole Guild — one `podman build` produces the image all stations run from.
- (capability) CAP-2 the image ships the repo at a FIXED SHORT path — not cosmetic: a long root panics pixi-build-python, the same failure mode that broke every pixi gate inside deep bmad-loop worktrees.
- (capability) CAP-3 credentials never enter image layers — age identity files and tokens mount at run time; a layer is forever, and a leaked layer is a rotation.
- (capability) CAP-4 state outlives the container — mutable state mounts as volumes rather than living in the image.
- (capability) CAP-5 the image proves itself at build time — smoke gates run in the build, so a broken image never ships green.
- (change) RECONCILED BY NAME, 2026-08-09: pixi.toml changed under this surface — doctor's Epic 6 S-6.2 added the `pyforge-doctor-scripts-test` task (feature.pyforge-ci). A task addition only: no dependency change, so the container image this Spec contracts is unaffected and CAP-1's "one build, whole Guild" is untouched. Acknowledged rather than stamped because pixi.toml is genuinely inside this surface.
- (note) First movement of this contract since marshal S-13.5 created it on 2026-08-09 (this Spec was one of the seven found drift-blind — a declared surface with no .memlog.md, contract_hash() == "", nothing reconcilable except by stamping).
- (change) RECONCILED BY NAME, 2026-08-09: pixi.toml changed under this surface — mason's Story 1.9 (fake-cfe-root-fixture-and-test-harness) split the `pyforge-mason-test` task into a fast default (`-m "not slow"`) plus a new `pyforge-mason-test-slow` task, mirroring pyforge-marshal-test-slow's split (pyforge-warden's equivalent task predates the `slow`-marker convention and is named `pyforge-warden-test-corpus-oracle`, not `*-test-slow`). A task-definition edit only: no dependency change, so the container image this Spec contracts is unaffected and CAP-1's "one build, whole Guild" is untouched. Acknowledged rather than stamped because pixi.toml is genuinely inside this surface. (See spec-pyforge-core's memlog for a related correction: pixi.toml is governed by exactly two surfaces, not three — spec-pyforge-doctor never included it.)
- (change) RECONCILED BY NAME, 2026-08-09: pixi.toml changed under this surface — doctor's Epic 6 S-6.6 adjusted the root manifest while moving the chain verdicts into pyforge-doctor. environment.yaml was verified UNCHANGED by re-export, so the container image this Spec contracts is unaffected and CAP-1's "one build, whole Guild" is untouched.
- (change) RECONCILED BY NAME, 2026-08-09: pixi.toml changed under this surface — S-5.7 added the `fleet-picture` task. Task-only; environment.yaml unchanged, so the container image this Spec contracts is unaffected.
- (event) Story 7-1-one-build-whole-guild LANDED, 2026-08-09: CAP-1's first real implementation. `Containerfile` ADDED (multi-stage build: `ghcr.io/prefix-dev/pixi:0.76.1` builder materializes the new `pyforge-container` env and a shell-hook; `ubuntu:24.04` runtime stage copies the checkout + hook, `CMD ["marshal"]`, both stages pinned `--platform=linux/amd64`). `.dockerignore` ADDED (added to this Spec's `surface:` in the same change — excludes `.git/`, `.pixi/`, credentials/secrets, `presentations/`, and other build-irrelevant paths from `COPY . /pyforge`). `pixi.toml` CHANGED — adds the `[environments] pyforge-container` entry (the eight `pyforge-*` features, `no-default-feature = true`), the composition CAP-1 names. Also fixed `pyforge-atlas` and `scribe`'s missing `--version` (tracked under their own Specs' memlogs, not this surface). Verified: `docker build` + all 8 station CLIs' `--version` + default `marshal` run, all green. Baseline scoped-stamped to this spec only.
- (change) RECONCILED BY NAME, 2026-08-09 — story `7-1`'s follow-up review pass; three surface paths changed, no new ones. `.dockerignore`: the depth-agnostic patterns were written in `.gitignore` syntax, but Docker matches each pattern against the WHOLE context-relative path, so bare `__pycache__/`, `*.log`, `*.egg-info/`, `node_modules/` etc. only ever matched at the root — 67 nested `__pycache__` dirs under `src/**` were riding into the image. Added `**/` prefixes (verified in-image: 67 -> 0). Also: `_bmad-output/implementation-artifacts` is only a SYMLINK, so the pattern was a no-op against the real per-project Tier-3 trees under `projects/*/` — added `_bmad-output/projects/*/implementation-artifacts` (verified 0 in-image). Also added `.steward/` (keys-inventory.yaml carries identity_path pointers, provenance and rotation history; budget.yaml the spend ceilings), plus `.private` and `SDKs/`, both gitignored secret-shaped roots the original secrets block missed. Corrected the `presentations/` comment, which claimed the tree is "irrelevant to any station CLI" — herald's whole deck duty is rooted there (`deck_pipeline.py`: `repo_root / "presentations" / slug`), so the exclusion stands on size but `herald deck` is host-only in this image BY DESIGN, and the comment now says so. `Containerfile`: added an explicit SCOPE block naming what the image cannot do — no `git`/`gh`/`pixi`/`tmux` binary, so marshal's git-worktree orchestration and steward's `provision`/`deploy build` cannot run in-container. That follows from CAP-1's own "compose the eight features verbatim, no new dependency curation" constraint rather than being an oversight; logged to deferred-work.md for the story that can lift it. `pixi.toml`: the `requires-pixi` comment enumerated six pin sites and claimed they match — there are SEVEN (`.github/workflows/kedro-viz-publish.yml` was unlisted) and they do NOT match (feature pins + environment.yaml at 0.76.1, this floor and both workflow pins at 0.75.0). Comment now states the real values; the floor mismatch itself is pre-existing and deferred. Re-verified end to end on the landing branch: `docker build` rc 0, all 8 station CLIs `--version` rc 0 (atlas now 1 line, not 9), bare `docker run` -> marshal usage rc 0. Baseline scoped-stamped to this spec only.
- (change) RECONCILED BY NAME, 2026-08-09 — story `7-1`'s THIRD review pass; the same three surface paths, no new ones. `.dockerignore`: the single load-bearing fix is that `.pixi/` swallowed `.pixi/config.toml`, which is GIT-TRACKED and carries `run-post-link-scripts = "insecure"` — without it `pixi install` skips every post-link script, so the image's env silently differed from every host install. Proven, not argued: `loaders.cache` appears in NO conda package's `paths.json` (it is generated by `.gdk-pixbuf-post-link.sh`), and it is now present in the built image at `/pyforge/.pixi/envs/pyforge-container/lib/gdk-pixbuf-2.0/2.10.0/loaders.cache`. Rewritten as `.pixi/*` + `!.pixi/config.toml` so Docker drops each child wholesale rather than walking the multi-GB env, plus `recipes/*/.pixi/` and `src/**/.pixi/` for the nested workspaces. Second tracked-file swallow fixed the same way: `**/*.log` took `.claude/skills/conda-forge-expert/tests/fixtures/error_logs/unmatched.log`, the repo's ONLY tracked `.log`, whose absence recreates the exact false-green `.gitignore` was patched to fix on 2026-08-09 (two failure-analyzer cases assert "Log file not found" instead of the no-match contract) — negated narrowly, mirroring `.gitignore`. ADDED `.bmad-loop/` (a loop home is itself a full repo checkout and therefore a valid `docker build .` root; 11GB of run state, transcripts and patches in the steward home), `/.herald/` (AD-5's repo-local bridge/etag store — the exact analogue of the `.steward/` entry pass 2 added), `.claude/skills/data/`, `_bmad/custom/.active-project` + `_bmad/config.user.toml` + `_bmad/_memory/` + `_bmad-output/planning-artifacts` (build-host BMAD identity: baking it in makes two hosts produce two different images from one commit), `_root-fallback-fork-*`, the unfinished half of the cache sweep (`.mypy_cache`, `.ruff_cache`, `.hypothesis`, `htmlcov`, `.coverage`, `.venv`), and conda-factory leavings (`conda-bld/`, `miniforge3/`, `*.conda`, `*.tar.bz2`). Widened `.idea/`/`.vscode/`/`.junie/` to `**/` — the header's claim that un-prefixed entries are "the only place they exist" was false: `recipes/.idea/` is tracked, 10 files. Header rewritten to say what it actually verified and to name tracked files as the hazard. `Containerfile`: the `--frozen` comment claimed it "fail[s] loudly on a stale pixi.lock" — per `pixi install --help` it does the opposite (`--locked` is the flag that aborts; `--frozen` is silent about staleness). Flag KEPT (`--frozen` is this repo's convention at every call site, per its own recorded decision); comment now states what it really guarantees and whose job the lock is. Added a BUILD line (the `-f` is not optional under BuildKit, which only auto-detects `Dockerfile`) and an EVERY-INVOCATION-MUST-GO-THROUGH-THE-ENTRYPOINT block: `docker exec <ctr> marshal` fails "executable file not found" because PATH exists only inside the sourced hook, and that is deliberately NOT papered over with a baked `ENV PATH`, which would resolve the binary while still missing the activation env — trading a loud failure for a quiet one. `docker exec <ctr> /entrypoint.sh marshal ...` is the working form. `pixi.toml`: the `requires-pixi` comment said SEVEN pin sites; there are EIGHT (the `"$schema"` URL two lines above it also pins v0.75.0) and it wrongly called all three workflow pins EXACT (staged-recipes-linter's is a floor, `pixi>=0.76.2`). Corrected; the version mismatch itself stays deferred. Re-verified end to end: `docker build -f Containerfile` rc 0, all 8 station CLIs `--version` rc 0 (marshal prints 2 lines by its own pre-existing design, `marshal 0.1.0` + `bmad-loop 0.9.0`), bare `docker run` -> marshal usage rc 0, image 3.73GB / 8,104 context files / 109MB context. Baseline scoped-stamped to this spec only.
- (change) RECONCILED BY NAME, 2026-08-09 — story `7-1`'s FOURTH review pass; the same three surface paths, no new ones, and no Python touched. `.dockerignore`: the one substantive fix is that the secrets block had NO `conf/` rule, so `src/shared/packages/pyforge-atlas/conf/local/credentials.yml` — Kedro's canonical local-credentials path, auto-generated by atlas's `tests/orchestration/conftest.py` on any checkout that has run its orchestration gates, keys `bigquery_adc`/`github_token`, header "Replace with real values for live runs" — was BAKED INTO THE IMAGE (verified present at 436 bytes in the built image). Atlas's own `.gitignore` declares that path secret twice (`conf/local/**`, `conf/**/*credentials*`); pass 3 REJECTED adding a counterpart on the premise "no such file exists tracked or by convention here; `conf/` holds only `base/`" — both halves refuted: there are three `conf/` trees and two carry a `local/`. Added `**/conf/local/*` + `!**/conf/local/.gitkeep` (both .gitkeeps are tracked) + `**/conf/**/*credentials*`. Second: the blanket `.bmad-loop/` pass 3 added swallowed the git-tracked `.bmad-loop/bmad_loop_hook.py` — its comment claimed to "mirror .gitignore's four entries", but those four are narrow PRECISELY so the hook stays tracked; negated, comment corrected. Third: `**/data.locks/` — ~90 gitignored admission-lock/holder files, each recording a build-host pid + run id, rode in on any image built while atlas tests were running (harmless by luck, since exclusion is a live `flock` no image can carry, but the image should not be a function of what ran on the build host — same doctrine already applied to `.claude/data/`). Header updated: four negations now, not two, and a new paragraph naming untracked-secret-shaped files as the half the tracked-file audit cannot see by construction. `Containerfile`: `exec "$@"` → `exec -- "$@"` — without the terminator bash parsed a leading-dash first argument as its OWN option, so `docker run <image> --version` died "exec: --: invalid option" (rc 2, a message about bash, not the image) and `docker run <image> -c /usr/bin/env` SILENTLY ran with an empty environment at rc 0, the exact quiet failure this file's header refuses to trade a loud one for; both are now honest rc-127 not-founds. Also corrected the entrypoint rationale, which named `SSL_CERT_FILE`/`GDK_PIXBUF_MODULE_FILE`/`FONTCONFIG_FILE` as what the hook sets — all three are EMPTY in the real image and no activate.d script sets them (the env's three are libarrow/libglib/libxml2-split, contributing `GSETTINGS_SCHEMA_DIR` and `XML_CATALOG_FILES`); the decision stands, only its evidence was wrong. And sharpened the SCOPE block: `herald deck` is not uniformly host-only — `seed` raises, but `status` returns `[]` at rc 0 because `_known_slugs()` guards on `presentations_dir.is_dir()`, inverting its own docstring guarantee in-container (herald's own code is out of scope here; deferred). `pixi.toml`: the `requires-pixi` comment said EIGHT pin sites; there are NINE — `.github/actions/sync-pypi-mappings/action.yml` pins EXACT `v0.73.0` and its own adjacent comment says to keep it in step with this key, making it a FOURTH exact site and the only one currently BELOW the floor. Corrected. Re-verified end to end on the rebuilt image: `docker build -f Containerfile` rc 0, all 8 station CLIs `--version` rc 0 (atlas exactly one clean line), bare `docker run` → marshal usage rc 0. Full context audit is now clean in BOTH directions for the first time: ZERO untracked files in the image outside `.pixi/`, and ZERO tracked files swallowed beyond the deliberate `archive/`/`presentations/`/IDE sets. `environment.yaml` re-exported byte-identical; atlas version tests 6 passed, scribe CLI tests 17 passed. Baseline scoped-stamped to this spec only.
- (change) RECONCILED BY NAME, 2026-08-09 — story `7-2-the-repo-at-a-fixed-short-path` LANDED. `Containerfile` CHANGED, comment-only, no new surface paths: added a rationale block at the builder stage's `WORKDIR /pyforge` documenting CAP-2 in the artifact itself rather than only in review-trail prose — the `pixi-build-backends` unchecked-`usize`-underflow panic mechanism, the ~173-byte empirical ceiling, `/pyforge`'s ~20x margin, and a citation to this Spec's own source doc (`technical-steward-pixi-workspace-member-research-2026-07-25.md` § A3.1); added a one-line cross-reference (no restated substance) at the runtime stage's `WORKDIR /pyforge` pointing back to it. No stage, dependency, or build-behavior change — CAP-1 ("one build, whole Guild") is untouched. A new stdlib-only regression test (`tests/packaging/test_containerfile_checkout_path.py`, covered by this repo's `tests/**` allowlist entry, not this Spec's surface) parses both `WORKDIR` lines and fails if either stops being the literal `/pyforge` or gains `$`/`{{` host-or-build derivation. Verified: `pixi run -e pyforge-ci pyforge-deps-test` green (new test alongside the existing manifest-parity suite); comment-only Containerfile edit, so 7-1's four already-green `docker build` verification passes stand unaffected. Baseline scoped-stamped to this spec only.
- (change) RECONCILES `pixi.toml` (2026-08-09) — DW-7-1-2 RESOLVED at the operator's direction, and it is this Spec's surface because Story 7.1's Containerfile is one of the nine sites. Nine places carry a pixi version and they had drifted THREE ways: feature pins + `environment.yaml` + the Containerfile image tag at `0.76.1`; `requires-pixi` + the `$schema` URL + the dashboard/kedro-viz `pixi-version` pins at `0.75.0`; and `.github/actions/sync-pypi-mappings/action.yml` at `0.73.0` — BELOW the floor, so a `workflow_dispatch` of Sync PyPI-Conda Mappings installed a pixi that could not parse this manifest. All nine now read `0.76.1`.
- (decision) `0.76.1` chosen over the minimal fix (lifting only sync-pypi-mappings to the old `0.75.0` floor) because `dashboard.yml` states the governing rule for itself — *"pixi-version stays pinned to the lock's producer... Bump it in step with pixi.lock"* — and the lock's producer is 0.76.1. The minimal fix would have closed the break while leaving two EXACT pins stale against their own written invariant, which is how this drifted in the first place.
- (event) Verified, not assumed: the `v0.76.1` schema URL resolves to a real schema (`$id` matches; HTTP 200 only after following the redirect — the bare 301 proves nothing); `pixi info` accepts the manifest under the raised floor; `pixi lock --check` reports already-up-to-date; `environment.yaml` regenerates byte-identical, since raising the floor does not touch the `build` env's resolved contents.
- (note) RESIDUAL, deliberately not closed: the nine-site equality is COMMENT-MAINTAINED. Nothing enforces it, which is precisely how it drifted three ways unnoticed. A cross-site pixi-version detector is the durable fix; DW-7-1-2 stays in the ledger as the record of why one is wanted, marked `resolved` for the break and carrying the residual.
- (change) RECONCILED BY NAME, 2026-08-09: pixi.toml changed under this surface — atlas's Story 13.3 (CAP-3 operator surface, canopy:FR-66) added the `[feature.pyforge-atlas.tasks.trending-candidates]` task (`cmd = "python -m pyforge.atlas.trending_candidates"`), mirroring the existing atlas-native task shape. A task addition only: no dependency change, so the container image this Spec contracts is unaffected and CAP-1's "one build, whole Guild" is untouched. Acknowledged rather than stamped because pixi.toml is genuinely inside this surface.
- (change) RECONCILED BY NAME, 2026-08-10: pixi.toml changed under this surface — atlas's Story 13.5 (CAP-5 downstream handoff to Mason, canopy:FR-68) added the `[feature.pyforge-atlas.tasks.trending-handoff]` task (`cmd = "python -m pyforge.atlas.trending_candidates.handoff_main"`), mirroring the existing `trending-candidates` task shape (13.3). A task addition only: no dependency change, so the container image this Spec contracts is unaffected and CAP-1's "one build, whole Guild" is untouched. Acknowledged rather than stamped because pixi.toml is genuinely inside this surface.
- (change) RECONCILED BY NAME, 2026-08-09 — story `7-3-credentials-never-enter-image-layers` LANDED, CAP-3's first real implementation. `scripts/container-gates` ADDED to this Spec's `surface:` in the same change (a repo-root script, no package of its own) — `secrets-scan <root> [<root> ...]` delegates to the existing `steward keys audit --secrets <path>` primitive (no second scanner) over every top-level entry of a directory root or a file root directly, skipping a `.pixi/` child entirely. Two real, pre-existing false-positive blockers were found and resolved before writing any code, not assumed away: (1) `age-keygen`'s own compiled binary carries a literal `AGE-SECRET-KEY-1...` string (an upstream baked-in test vector, confirmed via `strings`), which trips the scanner on every build regardless of anything this repo does — this is why `.pixi/` (the vendored `pyforge-container` env `age`/`age-keygen` ships in) is excluded from the scan target rather than recursed into; verified live that no OTHER nested `.pixi/` can reach the build context (the only 9 pixi workspaces in the repo are already stripped by Story 7.1's own `recipes/*/.pixi/`/`src/**/.pixi/` `.dockerignore` rules). (2) `pyforge-steward`'s own conformance tests deliberately contain secret-shaped literals to prove the scanner works — a repo-wide grep confirmed this is confined to exactly 3 files, all in that package's `tests/`, so `.dockerignore` CHANGED to add `**/tests/` (test suites are never needed to run a station CLI regardless). `Containerfile` CHANGED — a `RUN` step added to the final stage, after the existing `/entrypoint.sh` creation, sourcing `/shell-hook.sh` then invoking `container-gates secrets-scan` over `/pyforge`, `/shell-hook.sh`, and `/entrypoint.sh` (everything the final stage adds beyond the `ubuntu:24.04` base); a nonzero exit here fails `docker build`/`podman build` itself, not a later `docker run` — matching the AC's "the build FAILS" literally, since BuildKit natively aborts on a failing `RUN`. Landed as `RUN bash -c "source ... && python3 ..."`, not the plain `RUN source ...` first drafted: BuildKit's default `/bin/sh` (dash) has no `source` builtin, confirmed live (`/bin/sh: 1: source: not found`, exit 127). `pixi.toml` CHANGED — new `pyforge-steward-container-gates-test` task (needs a real `steward` binary, so it lives under `pyforge-steward`, not the dependency-free `pyforge-ci`). A parallel adversarial review pass (Blind Hunter + Edge Case Hunter) added defensive hardening not in the first draft: a `subprocess.run` timeout (a hung `steward` would otherwise hang the build indefinitely) and explicit `FileNotFoundError`/`OSError` handling (steward missing from PATH, or an unreadable directory, now fail the gate cleanly instead of an unhandled traceback) — 8 real, non-mocked subprocess tests in `tests/scripts/test_container_gates.py` cover the full I/O matrix including the PATH-missing case. Re-verified end to end AFTER the review patches, not just before: a real clean `docker build` (gate reports clean in ~4.5s across all three roots), a real negative-path build with an injected `sk-ant-...` probe file (gate fails, `docker build` exits 1, no image tagged, probe never committed), and — specifically re-checking the `**/tests/` exclusion doesn't regress Story 7.1's own AC — `docker run <img> <cli> --version` for all 8 station CLIs (marshal, steward, pyforge-atlas, warden, doctor, mason, herald, scribe), all green. `steward keys audit --secrets` scans still find nothing under `/pyforge` outside the excluded `.pixi/`; `.git/` (the pre-existing, unmodified `.dockerignore` line 33 exclusion) never reaches the build context, so the 2026-07-24 historical-secret-purge incident's still-unrotated-force-push concern doesn't apply here. Baseline scoped-stamped to this spec only.
- (change) RECONCILED BY NAME, 2026-08-09 — story `7-4-state-outlives-the-container` LANDED, CAP-4's first real implementation. `scripts/container-gates` CHANGED — new `volumes-roundtrip --image IMAGE --mount PATH [--mount PATH ...]` subcommand, duty-agnostic like `secrets-scan` (AD-2 delegation-purity: it knows how to write/read a marker file through `docker run`/`docker volume`, nothing about `steward`'s own commands, so it runs against any image with a shell). Deliberately a post-build verification tool, not a Containerfile `RUN` gate: a single `docker build` layer has no "stop this container, restart it with the same volume" operation, so proving a *restart* preserves state structurally requires a real `docker run` → (`--rm` teardown) → `docker run` sequence against an already-tagged image — a structural difference from 7.3's build-time secrets gate, not an oversight. Per mount: create a fresh uuid-suffixed named docker volume, write a token marker via one `docker run`, destroy that container, read it back via a second `docker run` against the same volume, compare; `docker volume rm` in a `finally` regardless of outcome; never short-circuits across multiple `--mount`s (aggregate exit 1, per-mount diagnostics). A parallel adversarial review pass (Blind Hunter + Edge Case Hunter) hardened the first draft: `--mount` values are validated (`_validate_mount`/`_MOUNT_PATH_RE`, absolute path, no `:`/NUL) and `shlex.quote()`-wrapped before entering the `sh -c` string (a `:` would corrupt the `-v SRC:DST` flag; an unquoted shell metacharacter would execute inside the ephemeral container) — both flagged independently by both reviewers; the volume-leak test scopes `docker volume ls` to `--filter name=container-gates-roundtrip-` rather than diffing the full daemon-wide list (shared-host false positives); the test skip condition checks the image is actually cached (`docker image inspect`, no pull) rather than only that `docker` is on PATH; a nonexistent-image failure no longer emits a spurious "no such volume" line from `_remove_volume` attempting to clean up a volume that was never created; `_remove_volume`'s cleanup gained a general `except OSError` fallback so an unexpected error can't mask an already-decided pass/fail verdict with a raw traceback; `_docker_run` now passes an explicit `--name` and the timeout handler best-effort `docker rm -f`s both possible container names, since a CLI-side timeout only kills the local `docker` client, not a server-side-orphaned container still holding the volume. `Containerfile` CHANGED — a `VOLUME ["/pyforge/.steward", "/pyforge/.claude/data/conda-forge-expert", "/root/.bmad-loops"]` declaration added after Story 7.3's secrets-scan `RUN` gate, before `ENTRYPOINT`/`CMD` (must stay the LAST content-writing instruction in the stage — a later `RUN`/`COPY` writing into one of these paths would silently vanish from the image layer, the classic Docker `VOLUME` gotcha, now called out explicitly for future editors), with a comment resolving PRD FR-25's prose ("loop homes, the Tier-3 store and mutable runtime caches") 1:1 onto CAP-4's concrete three-path list (this story's own Design Notes: `.steward/` is Steward's durable store, not CLAUDE.md's BMAD-artifact Tier-3 vocabulary) and confirming live that `HOME=/root` (no `USER` directive anywhere in this Containerfile, so the runtime stage runs as root by ubuntu:24.04's own default) — also re-noting the pre-existing, unchanged gap that `git`/`gh`/`pixi`/`tmux` are absent from this stage, so `steward provision --runner bmad-loop` still cannot materialize a worktree in-container (Story 7.1's SCOPE comment; this story proves only that the mount point itself preserves whatever bytes land there). `pixi.toml` CHANGED — new `[feature.pyforge-steward.tasks.pyforge-steward-container-volumes-test]`, mirroring the placement/rationale of `pyforge-steward-container-gates-test` (needs a real `docker` binary on PATH; lives under `pyforge-steward` because that's the env that builds+installs `steward`, even though `volumes-roundtrip` itself is duty-agnostic and never calls `steward`). `tests/scripts/test_container_volumes_roundtrip.py` is NEW but not in this Spec's `surface:` — covered by this repo's `tests/**` allowlist entry, same as 7-2's regression test. Verified end to end: `pixi run -e pyforge-steward pyforge-steward-container-volumes-test` green (full I/O matrix, real `docker` subprocess calls against the locally-cached `ubuntu:24.04` fixture image, including the "read-back from a different volume must NOT report a false pass" case and a 2-of-3-mounts-fail aggregation case); a real `docker build` tagged image inspected with `docker inspect --format '{{json .Config.Volumes}}'` lists all three paths; `volumes-roundtrip` run for real against that built image with all three CAP-4 mounts reports a clean round trip at exit 0; the duty-level proof — `steward budget set --cap 1500usd/month` in one container instance with `.steward` volume-mounted, container removed, a fresh container with the SAME volume attached, `steward budget show` — reported the same declared ceiling across the replacement. Baseline scoped-stamped to this spec only.
- (change) RECONCILED BY NAME, 2026-08-09 — story `7-4`'s repair-pass review (a repo-wide `spec_surface_check.py` gate had caught this Spec's memlog not moving after the story's own review pass landed; a fresh dev-auto session repaired the reconciliation gap and then ran a full Blind Hunter + Edge Case Hunter pass over the whole story diff, same as any other review pass). `scripts/container-gates` CHANGED — `_roundtrip_mount`'s exception handling around the two `_docker_run` calls only caught `FileNotFoundError`/`subprocess.TimeoutExpired`; a `docker` binary present but not executable (`PermissionError`) or an `--image` value `subprocess.run` itself rejects (e.g. embedded NUL, raising `ValueError`) would have escaped as a raw traceback, violating the story's own "never a raw traceback" constraint — independently flagged by both reviewers. Fixed: added `except (OSError, ValueError)` after the existing specific handlers, mirroring `_remove_volume`'s own general-`OSError` fallback. `Containerfile` CHANGED — the `/pyforge/.steward` comment cited it as "tracked... survives bmad-switch" without noting `.dockerignore` (unchanged, line 121) already excludes `.steward/` from the build context, so the "tracked" framing on its own could mislead a reader into thinking host content ships into the image at that mount point; confirmed live via `grep -n '\.steward' .dockerignore`. Fixed: the comment now states explicitly that no tracked host content ever ships in a layer and every fresh volume starts empty. `tests/scripts/test_container_volumes_roundtrip.py` CHANGED (not in this Spec's `surface:`, per the existing `tests/**` allowlist convention) — `_image_cached()`'s `docker image inspect` call ran at `pytestmark`/collection time with no exception handling; a transient docker-daemon hiccup (this repo runs several bmad-loop worktrees against the same daemon) would crash collection of the whole module rather than cleanly skip it. Fixed: wrapped in `except (OSError, subprocess.TimeoutExpired): return False`. Three findings deferred to `deferred-work.md` as pre-existing/out-of-scope rather than patched: this test file's spec-surface non-governance (matches Story 7-2's identical, already-accepted gap), `_MOUNT_PATH_RE`'s acceptance of a bare `/`/`..`-containing values (low risk, trusted-operator-only usage today), and the volume-leak guard's residual `pytest-xdist`-parallel race (not hit by this story's own task, which passes no `-n`). Re-verified end to end: `pixi run -e pyforge-steward pyforge-steward-container-volumes-test` (9 passed) and `pyforge-steward-container-gates-test` (8 passed, unaffected) both green; `python3 -m py_compile scripts/container-gates` clean. Baseline scoped-stamped to this spec only.
- (change) RECONCILED BY NAME, 2026-08-09 — story `7-5-the-image-proves-itself-at-build-time` LANDED, CAP-5's first real implementation. `scripts/container-gates` CHANGED — new `cli-smoke --cli "<cmd> ..." [--cli "<cmd> ..." ...]` subcommand, duty-agnostic like `secrets-scan`/`volumes-roundtrip` (AD-2 delegation-purity: each `--cli` value is a full invocation string, `shlex.split`'d with no shell and run directly via `subprocess.run`, so the script carries no station-name list of its own). Proves the eight station CLIs actually work inside the exact composed `pyforge-container` env the image ships — a real, already-observed risk this closes: that composed env resolves a different package set than any per-station test env (e.g. `dagster` 1.13.17 in `pyforge-container` vs. 1.13.16 under plain `pyforge-atlas`), so an import break there was previously invisible until a later `docker run` (per a Story 7.1 deferred-work entry naming this exact gap as "Story 7.5's build-time smoke-gate scope"). Invokes `--help`, not `--version`: `pixi.toml`'s own `pyforge-marshal-smoke` task documents that `marshal --version` "always exits 0 (informational, never a gate)" for that one station, while `--help` requires the parser (and therefore every subcommand-defining import) to have built successfully and exits non-zero on failure uniformly across all eight — confirmed live. A parallel adversarial review pass (Blind Hunter + Edge Case Hunter) hardened the first draft: `_check_cli`'s `subprocess.run` call gained a general `except (OSError, ValueError)` fallback (a non-executable/permission-denied target, or a `--cli` value `subprocess.run` itself rejects, previously escaped as a raw traceback — mirrors `_roundtrip_mount`'s identical, already-fixed 7.4 gap); a non-zero exit with empty stderr (or a CLI that explains itself on stdout instead) now falls back to the stdout tail, then a `(no output captured)` placeholder, instead of printing a bare `exited N: ` with no diagnostic content — both flagged independently by both reviewers. `Containerfile` CHANGED — a new `RUN bash -c "source /shell-hook.sh && python3 /pyforge/scripts/container-gates cli-smoke --cli 'marshal --help' ... --cli 'scribe --help'"` step added after the Story 7.3 secrets-scan `RUN` and before the Story 7.4 `VOLUME` line (per that line's own "add new RUN/COPY steps ABOVE this line" instruction), naming the real eight console scripts — a finding here fails `docker build`/`podman build` itself, not a later `docker run`. `tests/scripts/test_container_cli_smoke.py` is NEW but not in this Spec's `surface:` (same established pattern as 7.2's and 7.4's own regression tests — covered by the repo's `tests/**` allowlist entry); uses only synthetic `tmp_path` fixture scripts, no real station binary or Docker needed, so it rides the existing `pyforge-doctor-scripts-test` sweep of `tests/scripts/` with no dedicated task added (unlike `secrets-scan`/`volumes-roundtrip`, which each needed a *real* `steward`/`docker` binary to avoid that sweep just skipping them). Three findings deferred to `deferred-work.md` rather than patched: SPEC.md's own CAP-5 text still names `steward provision --verify` as part of the gate, but the canonical, more-recently-updated `epics.md`/PRD FR-26 (2026-08-02, predating this story) narrowed the AC to exactly what shipped here and omits that clause — the implementation matches the canonical text, SPEC.md itself is what's unreconciled; the eight-station list is hand-duplicated between `pixi.toml`'s `pyforge-container` composition and this `RUN` line with nothing tying them together (matches the pre-existing pattern in `secrets-scan`'s root list and `volumes-roundtrip`'s mount list, not a new gap); and no test in any of the three container-gates stories parses `Containerfile` itself to confirm a gate line survives future edits (pre-existing since Story 7.3). Verified end to end: `pixi run -e pyforge-ci pyforge-doctor-scripts-test` (26 passed, 8 pre-existing skips unaffected, up from 21 before this story's 5 new tests); `python3 -m py_compile scripts/container-gates` clean; a real `docker build -f Containerfile` succeeded with the `cli-smoke` `RUN` step reporting all eight real stations OK; a negative-path scratch-Containerfile build with a ninth, bogus `--cli "does-not-exist-binary --help"` appended failed exactly at that `RUN` step with a clean message naming the missing binary (no traceback, no image produced), scratch file and all throwaway images removed afterward. Baseline scoped-stamped to this spec only.
- (change) RECONCILED BY NAME, 2026-08-10: pixi.toml changed under this surface — Story 4.15 (canopy:FR-177, "one pusher, not two") removed the `loop-push-watch` task (the standalone interval-push watcher retired as fully subsumed by the supervisor's own canopy:FR-61 stage-boundary + interval-fallback push) and added one confirming sentence to `unpushed-work-check`'s description naming it the standing signal for between-runs staleness. Task-only edit, no dependency change, so the container image this Spec contracts (CAP-1) is unaffected. Acknowledged rather than stamped because pixi.toml is genuinely inside this surface.
- (change) RECONCILED BY NAME, 2026-08-15: two files under this surface changed across Story 10.3 ("One image, both engines," a pyforge-steward story whose own owning spec is `spec-python-agent-platform`) and its follow-up review pass. **This surface had been missed entirely until the follow-up review caught it** — the story reconciled `pyforge-mason/spec-django-accelerator-framework` and then `pyforge-steward/spec-python-agent-platform`, both of which govern `src/platform/**`, and stopped there; `.dockerignore` and `pixi.toml` are governed HERE, and nothing named them. The repo-wide gate stayed green throughout, which is the part worth recording: this Spec's baseline has been deliberately un-stamped since the 2026-08-10 entry above ("Acknowledged rather than stamped"), so its contract hash had already moved and every subsequent governed change reported informational `[drift-presumed]` rather than gating `[drift]`. A surface that is reconciled by NAME only is exactly as good as the naming discipline; two passes of this story provided none. Named now: (1) `.dockerignore` — Story 10.3 added a "src/platform/" section excluding dev-only platform content (`requirements/local.txt`, `docs/`, `utility/`, `config/settings/{local,test}.py`), a `src/platform/.env` guard the file's root-anchored patterns do not reach, and `staticfiles/`/`platformapp/media/` build-host output; the follow-up review corrected that section's own claim that both output dirs are gitignored (only `staticfiles/` is — `src/platform/.gitignore` carries a stale cookiecutter `platform/media/` pattern, which is logged as deferred work against Story 10.1's tree, not fixed here). This matters to CAP-3 in one direction only: it removes candidate leak surface from a build context, never adds any, and the repo-root ignore file is the one both this Spec's own image and `src/platform/Containerfile` consult. (2) `pixi.toml` — COMMENT ONLY, twice. Story 10.3 registered its own builder-stage pixi image tag in the `requires-pixi` pin-site enumeration, and the follow-up review then found that enumeration factually inverted and undercounted: it read "TEN places … NINE OF THE TEN ARE 0.76.1" when live state is thirteen sites, twelve at 0.76.2, and exactly one — THIS Spec's own root `Containerfile`, CAP-1's image — still at 0.76.1, i.e. below the floor `requires-pixi` declares, which means that builder stage fails outright (`this project requires pixi '>=0.76.2', but you have pixi 0.76.1`, hit live). The enumeration now states that. **That is a live CAP-1 finding against this Spec** and it is NOT fixed here — raising the root Containerfile's tag is outside a steward platform story's Code Map, and it is recorded as deferred work. `environment.yaml` re-exported and confirmed byte-identical (comment-only edits do not move the derived export), so the ungated env-sync check stays green. No dependency, task, feature, or environment definition changed. Following this file's own most recent precedent, this entry RECONCILES BY NAME and does NOT re-stamp the baseline — and the two files differ in a way worth stating exactly, because a first pass at this entry got it wrong and the correction is the whole reason for the decision. `.dockerignore` was IN SYNC at Story 10.3's baseline commit `64e717d135`: the baselined digest `2419b625` is precisely that commit's content, so this Spec's `.dockerignore` drift is Story 10.3's own and nobody else's. `pixi.toml` is the opposite: the baselined `f3e313fe` matches neither `64e717d135` (`0ae029cc`) nor anything this story produced, so that file was ALREADY unreconciled here before Story 10.3 touched it — consistent with the 2026-08-10 entry above having deliberately acknowledged-without-stamping. Stamping now would be right for `.dockerignore` and wrong for `pixi.toml`, folding an unaudited pre-existing delta into a stamp taken under an entry that names only Story 10.3's changes — precisely the laundering this pass corrected in the mason memlog. Naming both, stamping neither, is the honest option available from here. Whoever next stamps this Spec should first name whatever moved `pixi.toml` between `f3e313fe` and `0ae029cc`.

## 2026-08-15 — Story 10.3, third follow-up review pass (`.dockerignore`, named not stamped)

`.dockerignore` — governed here — changed again in this pass. One addition:
`src/platform/compose/`. The runtime stage's `COPY src/platform/ /app/` was shipping the local
compose stack into the production image, where nothing reads it and where it carries a placeholder
`DJANGO_SECRET_KEY` and a local `POSTGRES_PASSWORD` in plain text. That is the same hygiene class
this section's own stated rule already excluded `config/settings/local.py` and `test.py` for.
Verified in the rebuilt image: `/app/compose` is gone, and `secrets-scan` still reports
`clean (2 root(s))`.

Recorded in the same entry because it is the non-obvious half: `src/platform/requirements/` CANNOT
be excluded the same way even though the runtime image has no use for it either — `.dockerignore`
governs the whole build context, and the BUILDER stage's pip layer reads
`requirements/{base,production}.txt` out of it. A future reader tempted to "finish the job" would
break the build.

Deliberately NOT re-stamped, for the reason the previous pass recorded and which still holds: this
spec's baselined `pixi.toml` digest matches neither this story's baseline commit nor anything this
story produced, so it carries genuine pre-existing drift that a stamp would launder under an entry
naming only Story 10.3's changes. The structural half — that a memlog which moved for an unrelated
reason downgrades a gating `drift` FAIL to a non-gating `drift-presumed` WARN — is already carried
as deferred work.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event) Surface reconcile 2026-09-07 (fleet hygiene, pyforge-steward Story 46.3, spec-bmad-suite-lifecycle CAP-4 half): `pixi.toml` changed to add one `local-recipes` pixi task (`tea-test-review`, wrapping the real `tea-test-review` CLI with `--base origin/main --min-score 80` defaults) for TEA provisioning -- unrelated to this spec's own surface, but this spec's blanket `pixi.toml` glob makes it a co-governor of that one file. No product work here. Re-derived the governing set the same way Story 45.2/46.1 did (grep `pixi.toml` in every spec's `surface:` frontmatter) rather than assuming their own six-spec list still holds: it now numbers nine (this spec included), not six -- the surface set has grown since 45.2/46.1 landed. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-unified-container`.
- (decision by claude) Open question 'one image or two: the lean all-stations image (Mode L) vs a `pyforge-factory-full` packaging tier' closed: STALE -- already decided by the architecture run. `uc:AD-2` (ARCHITECTURE-SPINE.md:1318-1334) rules ONE image, with the second tier deferred-not-rejected under a named trigger, 'when recipe builds need to run inside the container'. Retire it from open_questions and record it as resolved; the Spec's own frontmatter comment already said the spine answers it, so frontmatter and open_questions were contradicting each other inside one file (fleet-readiness-decision-batch-2026-09-09 row stB-B3).
- (decision by claude) Open question 'baked checkout vs bind-mount as the primary mode -- decide at epic time' closed: STALE -- decided. The spine's own topology ratifies the baked checkout at /pyforge (ARCHITECTURE-SPINE.md:1291); bind-mount over /pyforge stays the dev override. 'Epic time' was Epic 7, closed 5/5 in PR #372 (fleet-readiness-decision-batch-2026-09-09 row stB-B4).
- (question by claude) Q3 (worktree placement for in-container bmad-loop runners: container fs vs volume, deferred with Mode I) and Q4 (whether Mode L and Mode I are one image with different mounts/limits, or Mode I forks) stay GENUINELY OPEN and are kept (fleet-readiness-decision-batch-2026-09-09 row stB-B5).
- (constraint by claude) NEW blocker, 2026-09-09 -- neither Mode question is decidable until the image is built by something. No CI workflow and no pixi task builds the root `Containerfile` (grep -rn Containerfile .github/workflows/*.yml matches only src/platform/Containerfile and the two sidecar files; pixi.toml carries no docker build / podman build), and `pyforge-steward-container-gates-test` / `-container-volumes-test` (pixi.toml:556, :569) run in ZERO CI jobs. CAP-5 is titled 'the image proves itself at build time' and there is no build time. Vessel: new steward Story 48.10 (fleet-readiness-decision-batch-2026-09-09 rows stB-B5 / stB-B7 / C7).
- (event) Self-validate Pass 1 (coherence) PASS on the 2026-09-09 re-derive: CAP-1..CAP-5 untouched, 4 surface entries and the spine companion preserved, status stays 'shipped', open_questions 4 -> 3 (two stale retirements, one new blocker entry)
- (event) Self-validate Pass 2 (preservation) PASS: Q1 and Q2 retired with their spine citations (uc:AD-2 at :1318-1334; baked checkout at :1291; Epic 7 closed 5/5 in PR #372), Q3/Q4 kept verbatim, and the nothing-builds-the-image blocker landed both as a frontmatter entry and as a section naming Story 48.10 and the 44.10 CI-window deadline
- (note by claude) `surface:` gains `scripts/guild_image_ci.sh` 2026-09-12 (fleet hygiene sweep, discovered via mason 15.1's failed verify): Story 48.10 ("the one-container Guild proves itself at build time") authored this CI script but the Spec's surface never widened to claim it, so it reported `[ungoverned]`. No CAP text change. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-unified-container.
- (event by claude) Surface reconcile 2026-09-12 (mason 15.1 recovery landing, same day as the note above): `pixi.toml` changed again — a two-line comment/description reword on the (unrelated) `test-ci` task, part of mason's own CFE-rebuild-campaign closure. No CAP text change; nothing in this spec's own `Containerfile`/`.dockerignore`/`scripts/container-gates`/`scripts/guild_image_ci.sh` surface touched. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-unified-container.
- (event) 2026-09-12: reconciled a spec-surface drift finding on pixi.toml (spec-mcp-host-real-station-tools's own unrelated PR #1285 added [feature.mcp-host.dependencies] entries -- django/psycopg/django-environ/pyjwt/cryptography -- for the mcp-host sidecar). Confirmed this spec's own actual concern (the pyforge-container/pyforge-ci composed envs, pixi.toml lines ~585-900+) is untouched by that change -- nothing to reconcile in this spec's own content. Re-stamping baseline to clear the false drift: fail.
- (event) 2026-09-13 pixi.toml overlap: graphifyy on feature.pyforge-scribe. No container product change.
- (event by claude) Surface reconcile 2026-09-14: `pixi.toml` changed for two reasons, neither of which touches this Spec's own contract. (1) The `frame-preflight` task description was restated for Frame Spec v0.3 (steward Story 53.6 moved Frame identity from `name` to the qualified-ref `identifier`). (2) `pyforge-station-tests` was REPAIRED: it landed on 2026-09-14 as `set -euo pipefail` plus a bash `for` loop, which pixi's task shell (deno_task_shell) cannot parse, so the task errored on its first token and had never run a single station despite CLAUDE.md documenting it as PR CI gate #3. Replaced with a declarative cross-environment `depends-on` over the eight `pyforge-<station>-test` tasks. Verified by running it: all 8 stations green, exit 0. `environment.yaml` re-exported and byte-identical (description/task-wiring edits only, no dependency change). Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (PR #1355 CI remediation): `pixi.toml` gained nine tasks and `board.py` gained coverage pragmas; neither touches this Spec's contract. (1) Eight `pyforge-<station>-coverage-gate` tasks plus a `pyforge-station-coverage-gates` aggregate now mirror `.github/workflows/coverage-gates.yml`'s `named-module-gates` matrix locally -- that gate measures only the modules a branch TOUCHES, so `pyforge.doctor.sources.board` had sat at 70.1% against an 80% floor since the Guildhall console was retired and only surfaced when an unrelated edit touched the file. (2) A `pr-preflight` aggregate runs the four lanes that actually red a PR (`detectors-ci`, `test-ci`, `pyforge-station-tests`, `pyforge-station-coverage-gates`), because two CI failures on #1355 had no local equivalent at all. (3) `board.py`'s retired-Guildhall machinery is marked `# pragma: no cover` with a block comment stating why -- it cannot execute while `retired-console-check` fails CI on reintroduction; module coverage went 72% -> 94% with no test or behaviour change, tracked for deletion as `DW-DASHBOARD-DEAD-CODE-1`. `environment.yaml` re-exported and byte-identical (task-only edits). Scoped-stamped.
- (change by claude) Surface reconcile 2026-09-14: `scripts/container-gates` raised `_CLI_SMOKE_TIMEOUT_SECONDS` 10 -> 30, with the measurement recorded inline. `guild-container (docker)` failed on PR #1355 with `cli-smoke: pyforge-atlas: timed out (> 10s)` while the other seven CLIs passed, and the same job had passed on the immediately preceding run -- the budget had become a coin-flip rather than a bound. The constant's own comment had reserved per-station tuning "absent evidence any one is structurally slower"; that evidence now exists. Measured as direct console scripts (no `pixi run`, no `docker run`, warm cache): **pyforge-atlas 2.26s vs warden 0.23s**, ~10x, because `pyforge/atlas/__init__.py:9` imports pandas at module scope to pin `future.infer_string` off (AUD-ATLAS-011) -- so every atlas entry point, `--help` included, pays pandas + numpy; `python -X importtime` attributes 290ms of a 290ms package import to pandas alone. A cold container on a shared runner multiplies that past 10s, which is what CI saw. 30s keeps this a HANG bound, which is the gate's stated job, rather than a latency assertion it was never meant to be. The 10x startup cost itself is atlas's to fix and is tracked as `DW-ATLAS-CLI-STARTUP-1` -- deliberately NOT solved here by deleting the pandas pin, which is a correctness contract the atlas package docstring guarantees to every importer. `tests/scripts/test_container_cli_smoke.py` green (13 passed); it parameterises the timeout rather than asserting the constant, so no test changed. RECONCILES: scripts/container-gates. Scoped-stamped.
- (correction by claude) 2026-09-14 — `scripts/container-gates`' `_CLI_SMOKE_TIMEOUT_SECONDS` comment was corrected: the earlier entry blamed atlas's module-scope pandas import for the 10s `cli-smoke` timeout, and that diagnosis was WRONG. Re-measured as direct console scripts in atlas's own env, warm cache: `pyforge-atlas --version` **0.34s** (Story 7.1 intercepts it before the Kedro imports), `pyforge-atlas --help` **2.29s**, `import pyforge.atlas.__main__` **0.36s** of which pandas is ~0.28s. So imports are ~16% of the run and pandas ~12% — not the cause. The real cost is a full **Kedro framework bootstrap**: `main()` calls `configure_project()` and hands off to Kedro's own Click group, loading `kedro.framework.cli` and `kedro.framework.project` (whose rich-logging config prints the `rich_logging.yml` INFO banner visible on any atlas `--help`). The original reading came from `python -X importtime` inside an already-running interpreter, which measured only the import half and attributed the whole gap to it. The 10 -> 30 raise stands and is now justified by the right reason — an inherent station bootstrap, not a fixable import — and the comment now warns explicitly against "fixing" it by touching the pandas pin, which is a correctness contract (AUD-ATLAS-011) worth ~12% of the cost. No behaviour change; comment only. RECONCILES: scripts/container-gates. Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (herald Story 21.1, commit b3b220b7f77b0d25cc2ac96e55897d7c95f30244): `pixi.toml` gained one new task, `[feature.local-recipes.tasks.deck-trio]`, for deriving a deck's Infographic head from its standalone poster -- unrelated to this Spec's own `pyforge-container`/`pyforge-ci` composed-env contract. No `Containerfile`, `.dockerignore`, `scripts/container-gates`, or `scripts/guild_image_ci.sh` change. Verified via `git show --stat`: this commit's only tracked-file touches are `docs/specs/presentation-deck.md`, `pixi.toml`, `scripts/deck_trio.py`, `tests/scripts/test_deck_trio.py` -- none in this spec's surface but `pixi.toml`. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-unified-container.
