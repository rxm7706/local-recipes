---
topic: Fresh machine to validate-fast passing through steward verbs
updated: 2026-08-22T07:16
---

- (direction) Operator (2026-08-22): incorporate all 3-station dream seeds into the story backlog
- (capability) CAP-1 steward bootstrap verbs: init (prereq detection: pixi/git/gh/podman + floors), shell-init (PATH/completions/env), setup (clone+pixi install+hooks), initrepo (per-repo onboarding) — the sibling org's command surface adopted as the shape (intake report; pattern only, unlicensed). Success: fresh-machine-to-green documented and scripted, verified in a clean container
- (constraint) Consumes, never duplicates: provision --env/--runner (Epic 3), the OCP 12.4 bring-up (cluster substrate), devinfra-fold backing services (fifteen-factors chain); detection floors read pixi_version_registry, never hand-pinned
- (event) Spec seeded lean 2026-08-22 (decompose-now operator directive); deep elaboration at story time
