---
topic: Fresh machine to validate-fast passing through steward verbs
updated: 2026-09-17T02:30
---

- (direction) Operator (2026-08-22): incorporate all 3-station dream seeds into the story backlog
- (capability) CAP-1 steward bootstrap verbs: init (prereq detection: pixi/git/gh/podman + floors), shell-init (PATH/completions/env), setup (clone+pixi install+hooks), initrepo (per-repo onboarding) — the sibling org's command surface adopted as the shape (intake report; pattern only, unlicensed). Success: fresh-machine-to-green documented and scripted, verified in a clean container
- (constraint) Consumes, never duplicates: provision --env/--runner (Epic 3), the OCP 12.4 bring-up (cluster substrate), devinfra-fold backing services (fifteen-factors chain); detection floors read pixi_version_registry, never hand-pinned
- (event) Spec seeded lean 2026-08-22 (decompose-now operator directive); deep elaboration at story time
- (event by claude) status: ready -> in-progress -- Epic 17 is 2/2 `done` and the four verbs are live (pyforge/steward/cli.py:51-54,:89-101,:166), but this Spec's own success signal (a recorded clean-container run: nothing -> validate-fast, zero improvised steps) has never run, and spec-17-2 is a 54-line contract husk with no dev log (fleet-readiness-decision-batch-2026-09-09 Class B row stA).
- (decision by claude) Open question 'container-verified bootstrap: podman-in-podman or a dedicated CI lane' closed: NEITHER in local-recipes -- run the proof as a DEDICATED CI LANE against python-foundry after Story 44.3. Story 44.10 archives local-recipes, so proving a bootstrap against the repo that is about to stop being the working tree buys nothing; a dedicated lane also avoids the privileged-nesting surface podman-in-podman would add for one job. This places the Dream foundry-side (fleet-readiness-decision-batch-2026-09-09 rows stA-B4 / C17).
- (event) Self-validate Pass 1 (coherence) PASS on the 2026-09-09 re-derive: CAP-1 id/text untouched, status ready -> in-progress with its evidence, one open question remaining (the residue, not the answered one)
- (event) Self-validate Pass 2 (preservation) PASS: the B4/C17 answer (dedicated python-foundry CI lane after 44.3, never podman-in-podman here, because 44.10 archives local-recipes) is recorded as a retirement comment, the unmet success signal is flagged in place, and the genuinely-open residue (who schedules that lane, against which milestone) is the one surviving question
- (event) 2026-09-17 absorbed into spec-pyforge-steward (pointer + companions kept).
