---
topic: scratch-worktree-lifecycle (legacy memlog; frontmatter added by the 2026-08-22 format migration, marshal Story 25.3 — body below is the untouched original)
updated: 2026-08-22T05:39
---

# .memlog.md — spec-scratch-worktree-lifecycle

## 2026-08-14 — Genesis baseline

Spec drafted 2026-08-14 (`status: draft`); no story has touched or reconciled this
surface yet. This baseline is a structural bootstrap only — it establishes the
S-13.7 spec-surface-reconciliation contract (`scripts/spec_surface_check.py
--write-baseline --spec <project>/spec-<slug>`) against the surface's current,
unmodified file state. No governed-file content is claimed reconciled by this entry.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Surface reconcile 2026-08-31: 2 drift-presumed paths since baseline at `02fc94294119`. Twenty commits, Epic 13-17 steward workspace/bootstrap delivery (all done in ledger) on governed `cli.py` and `workspace.py`:
  - Epic 13 (Stories 13-1 through 13-4): `workspace.py` landed (`b204fdb5e8` start/ls/clean over git worktree); cli.py gained workspace status, repo-set start, set-level teardown (`5e063f5b9b`–`350e03ba2b`).
  - Epic 14 (Stories 14-1 through 14-5): bmad-core upgrade pre-flight, deliberate apply, clobbered-custom detect/re-apply, pin fan-out, prove-landed gate — cli.py wiring (`926034abac`–`88c819dba5`).
  - Epic 15 (Stories 15-1 through 15-3): suite pipeline-truth duty, one-command suite advance, provision --module — cli.py (`35d625d55f`–`1223903480`).
  - Epic 17 (Stories 17-1, 17-2): init/shell-init/bootstrap verbs — cli.py (`3f1b90195d`, `99c1c6f54d`).
  - Story 31.3: fresh-clone install-class path proof — cli.py (`2081bd7c25`).
  No CAP text change — code caught up to contract already correct after spec went ready. RECONCILES: `src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py`, `src/shared/packages/pyforge-steward/src/pyforge/steward/workspace.py`. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-scratch-worktree-lifecycle`.
