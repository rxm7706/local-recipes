---
topic: Non-module bmad-suite pieces are provisioned by install class, not forced through --module
updated: 2026-09-08T19:04
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-bmad-suite-install-class-wiring`.


- (direction) Operator (2026-08-24): close pre-existing dream-without-spec INV-1 for bmad-suite-install-class-wiring; Epic 15 stays done — mint Epic 31 for the three non-module wiring stories; do not reopen CAP-3 five or un-skip WDS
- (note) Source: docs/dreams/bmad-suite-install-class-wiring.md (status dreamt). Parent spec-bmad-suite-channel-product CAP-3 wires five modules via --module and skips WDS; this Dream names how method/loop/skf/labs/dashboards/template get on PATH and wired by install class
- (decision) Refuse steward provision --module skf as a non-goal — skf is not proven the same family as bmb; wrap stays npx bmad-module-skill-forge install + pixi pin. Dream allowed the Spec to refuse.
- (decision) Decompose as new Epic 31 (three stories), not 15.5+ — Epic 15 is done; same convention as Canopy vs Epic 11
- (capability) CAP-1 class-keyed playbook: one tracked companion tells the operator, for each of the six non-module pieces, pixi path, native wire (cited from upstream README / install-matrix.md), and steward verb/task — discoverable from steward provision --help (or equivalent CLI pointer), not tribal knowledge. Success: the playbook exists, cites the matrix, and provision --help names it
- (capability) CAP-2 class-correct wired-or-not: pipeline-truth CAP-1's wired-or-not column reports per-class predicates (installer tree / runner home / plugin enabled / VS Code extension / scaffold N/A), not a boolean only --module targets can satisfy. Success: run against a fresh clone it names each of the six by class without claiming wired for template-into-this-repo
- (capability) CAP-3 fresh-clone class-path: method core installed, loop runner provisionable via --runner, skf skills present via its own installer, labs plugin path documented, dashboard install task runnable, template N/A unless scaffolding — without hand-driving npm Installer classes from a chat transcript. Success: a recorded fresh-clone run (or CI equivalent) reaches that state with zero improvised steps
- (constraint) Never wire-everything through --module; install class is load-bearing; nothing joins _SUPPORTED_MODULES that is not a BMAD expansion module merging into _bmad/ + .claude/skills
- (constraint) Never absorb bmad-method first-install (Epic 14 owns reconcile/upgrade of an already-installed core); never absorb bmad-loop (wrap via --runner only)
- (constraint) Native commands come from upstream READMEs and install-matrix.md — cited, never invented; npm collision denylist travels with the matrix
- (constraint) Template is scaffold-only forever; provisioning it into this monorepo is out of scope
- (constraint) Dual-path (pixi + native) remains the suite product contract; this Spec names only the wiring half for non-module classes
- (assumption) Parent Epic 15 CAP-3 five and WDS skip remain; CAP-1 pipeline-truth command already exists as the report this Spec's wired-or-not language extends
- (event) Self-validate pass 1 (coherence): PASS — 3 CAPs intent+success, constraints bend, non-goals explicit (incl. refused --module skf), success signal testable
- (event) Self-validate pass 2 (preservation): PASS — six-row class table landed in install-class-playbook.md; wrap-don't-absorb, dual-path, template-N/A, CAP-3-five-untouched walked into SPEC.md
- (change) RECONCILED BY NAME, 2026-08-25: Story 31.3 lands CAP-3 proof — playbook fresh-clone class-path (cited natives only), `steward provision --prove-class-path`, tests refusing improvised npm Installer / `--module` skill-forge / resurrected Guildhall pixi tasks, CI job `pyforge-steward-fresh-clone`. Dashboard runnable = Kedro-Viz / `steward deploy dashboard`; operator console Lane 1 `/console/`. Epic 15 CAP-3 five and WDS skip untouched.
- (event by claude) Surface reconcile 2026-08-31: 6 drift-presumed paths since Story 31.3 memlog entry (2026-08-25) which narrated delivery but did not name governed paths for per-file reconciliation. One primary landing commit `2081bd7c25` (Story 31.3, done) plus supporting Epic 15 wiring commits on the same branch:
  - `install-class-playbook.md` — six-row class table + fresh-clone proof procedure (CAP-3).
  - `cli.py`, `provision.py`, `fresh_clone.py` — `--prove-class-path` verb and fresh-clone install-class path.
  - `test_fresh_clone_class_path.py` — refuses improvised npm Installer / `--module` skill-forge.
  - `.github/workflows/pyforge-steward-fresh-clone.yml` — CI job proving the class-path.
  Prior memlog entry above is the narrative source; this entry names the paths for spec-surface. No CAP text change. RECONCILES all 6 governed paths. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-bmad-suite-install-class-wiring`.
- (event by claude) 2026-09-05 (Story 45.1): _bmad-output/projects/pyforge-steward/planning-artifacts/specs/spec-bmad-suite-install-class-wiring/install-class-playbook.md gains the bmad-eval-quality row — install class 'Bare CLI, no wiring target' (INSTALL_CLASS_CLI): predicate is the eval-quality executable on PATH -> runnable, else missing; not a --module target; the WDS skip row is unchanged in provision.
- (note by claude) 2026-09-05: _bmad-output/projects/pyforge-steward/planning-artifacts/specs/spec-bmad-suite-install-class-wiring/install-class-playbook.md bmad-eval-quality row — Pixi/PATH column now names the landed pin (bmad-eval-quality >=0.2.0.dev0 in feature.local-recipes' linux-64/osx-arm64 target tables; unix-only variant on the channel).
- (note by claude) 2026-09-05 (PR #1071, BMAD 6.12.0 re-check): install-class-playbook.md's two native bmad-loop citations move v0.11.0 -> v0.11.1 in lock-step with the parent install-matrix.md (re-verified against pipeline-truth the same day); the fresh-clone prover (fresh_clone.py: every native fragment in the CAP-3 section must appear verbatim in install-matrix.md) caught the split when only the matrix moved — the pyforge-steward-fresh-clone 'prove' check went red on the PR. No class, command shape, or skip decision changed.
- (note by claude) 2026-09-06 Story 14.7: `src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py` `--apply` help-string correction only — the `upgrade bmad-core --apply` help now quotes the argv CAP-6/CAP-7 really build (`--directory`, `--modules`, `--pin`, then each catalog custom module's own installer, e.g. `bmad-module-skill-forge update`); 14.6's deferred item. No CAP text change, no class/verb/flag change; `fresh_clone.py`, `provision.py`, `test_fresh_clone_class_path.py`, the fresh-clone CI workflow and `install-class-playbook.md` are untouched (diff-free at stamp time). Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-bmad-suite-install-class-wiring`.
- (note by claude) Correction to the Story 14.7 entry above (review finding, same session): install-class-playbook.md is untouched by this diff, but its scoped-stamp baseline hash moved (c88240c5... -> 3f3a0a47...) because main's stamped baseline for that file was already stale — absorbed from the already-merged 660b1aea7b (2026-09-06, install-class-playbook bmad-loop v0.11.1 lock-step), mirroring the precedent spec-pyforge-steward's own memlog sets for ca005c9c57.
- (note by claude) 2026-09-06 Story 14.8: src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py gains only the new --installed-package-root help string on the upgrade bmad-core subparser (CAP-8); no class, command shape, verb, or skip decision changed; fresh_clone.py, provision.py, test_fresh_clone_class_path.py, the fresh-clone CI workflow and install-class-playbook.md are untouched (diff-free at stamp time). Scoped-stamped: --write-baseline --spec pyforge-steward/spec-bmad-suite-install-class-wiring.
- (note by claude) 2026-09-06 Story 14.8 follow-up: cli.py's --installed-package-root help string gains one clause cross-referencing --package-root as also required at --apply time; no class, command shape, verb, or skip decision changed; fresh_clone.py, provision.py, test_fresh_clone_class_path.py, the fresh-clone CI workflow and install-class-playbook.md are untouched (diff-free at stamp time). Scoped-stamped: --write-baseline --spec pyforge-steward/spec-bmad-suite-install-class-wiring.
- (event) SPEC re-derived 2026-09-06: status ready → shipped — Epic 31 (31.1–31.3) done since 2026-08-24; Dream specified → realized the same day; the `--module skf` refusal stands (skf stays own-installer + custom-module registered per spec-bmad-suite-lifecycle 2026-09-06)
- (note by claude) Cross-station surface touch (steward Epic 46, Stories 46.4/46.5/46.9, 2026-09-06/07): fresh_clone.py gained one `_EXPECTED_WIRED` value change (`bmad-labs-skills`: `documented` -> `wired`, Story 46.9, once the plugin-path probe read the real `.claude/skills` census instead of only the playbook-text check); provision.py gained the bmb skill-copy mechanism (Story 46.4) and the labs `--plugin labs --skill <name>` CLI surface (Story 46.5); test_fresh_clone_class_path.py's assertions moved to match. All three are governed primarily by spec-bmad-suite-lifecycle (Epic 46's own spec), which already reconciled and stamped them under its own memlog; this note closes the parallel surface-glob overlap this spec also declares over the same paths. No class, command shape, verb, or skip decision this spec's own CAP set governs changed in meaning. RECONCILES: src/shared/packages/pyforge-steward/src/pyforge/steward/fresh_clone.py, src/shared/packages/pyforge-steward/src/pyforge/steward/provision.py, src/shared/packages/pyforge-steward/tests/unit/test_fresh_clone_class_path.py.
- (note) Story 32.8 (CI follow-up to 32.5, spec-fleet-consistency-standard CAP-2): atlas's dashboard/, publish/ and wasm/ moved from tests/unit/ to tests/integration/. All three are fail-loud gates needing provisioned prerequisites (Playwright's managed Chromium, the DuckDB httpfs extension, a built WASM artifact) that pyforge-station-tests.yml provisions for its whole-tree run but a coverage lane has no business installing. Integration weight by the same reasoning that placed warden's oracle gates there. Ten pixi tasks that still named pre-move atlas paths were repointed, and the eight test-architecture.md files regenerated from the live inventory. No source change for this spec; its surface intersects the regenerated test-architecture.md set and the steward workflow python pin.
- (note) CI lane consolidation 2026-09-08 (marshal branch fix/marshal-status-findings-and-ci-lanes): the three pip-install-outside-pixi workflows -- pyforge-core.yml, pyforge-steward-five-tier.yml and this spec's own pyforge-steward-fresh-clone.yml -- collapsed into one lane, .github/workflows/pyforge-pip-install.yml. CAP-3's proof is UNCHANGED in substance: the same tests/unit/test_fresh_clone_class_path.py still runs, still pip-installed --no-deps into a venv with no pixi workspace present, in its own venv separate from the with-deps pyforge-core venv precisely so the --no-deps assertion survives. Only the runner boundary moved: a steward diff was paying checkout + setup-python + pip TWICE to run 18 tests that finish in under a second, and GitHub Actions minutes are exhausted. All five run steps of the consolidated lane were executed verbatim locally before the delete (161 + 8 + 10 = 179 tests, every step exit 0). Surface line repointed from the deleted workflow to the consolidated one. RECONCILES: .github/workflows/pyforge-pip-install.yml.
