---
topic: One pixi env for the platform image
chain: platform-image-one-pixi-env
updated: 2026-09-07T04:45
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env`.


- (decision) Owner steward; Dream docs/dreams/platform-image-one-pixi-env.md; 12-7 is done so this follow-up may start.
- (capability) CAP-1 one frozen python-agent-platform env contains Django-host extras; overlap fails at lock.
- (capability) CAP-2 Containerfile drops pip --no-deps; retire platform-image-pip installer.
- (capability) CAP-3 re-eval pixitainer-docker against 10.3 table; fail keeps hand-rolled file minus pip RUN.
- (constraint) No pixi in runtime image; UBI9-minimal; platform-ci-test stays separate; mcp 2.0 not in scope.
- (constraint) Never mix recipe formats; conda-forge-expert if recipes touched; write spec under pyforge-steward physical path.
- (question) extras-on-python-agent-platform-vs-composed-env: feature table vs composed env, still one pixi install -e.
- (decision) 2026-08-25 extras on `[feature.python-agent-platform.dependencies]` (conda-forge; whitenoise >=6.11). mcp-types/httpx2 omitted (mcp 2.x).
- (decision) 2026-08-25 CAP-3: pixitainer-docker 0.8.3 dry-run fails all four 10.3 must-holds; keep hand-rolled Containerfile; only drop pip --no-deps. Mason presenton untouched.
- (event) 2026-08-25 CAP-1/2 proof: `podman build -f src/platform/Containerfile -t localhost/platform:one-pixi-env .` succeeded (id 8cbee8e8f879). Image python imports django_structlog + rjsmin (py312). rjsmin/rcssmin conda build pin `py312*` (solver had picked py313). Compress needed `django.contrib.humanize` for allauth MFA templates. Next CRC/Helm upgrade applies Liquibase :17/:18 — not a 12-7 re-prove. SPEC shipped.
- (note) Sources: NEXT-AFTER-12-7.md, spec-10-3 pixitainer rejection 2026-08-14, CRC pip uninstall mcp 2026-08-25.
- (event) Self-validate pass 1 (coherence): CAP-1..3 have intent+success; non-goals named; success signal testable.
- (event) Self-validate pass 2 (preservation): queue-note intent, pixitainer table, 10.3 runtime constraints, ci-test split, mcp caveat landed.
- (event) Self-validate pass 3 (pointers): Dream specified; NEXT-AFTER-12-7.md and drain STATUS point at this SPEC ready; unifying Why residual names this chain.
- (event) 2026-08-29 drift reconcile: `pixi.toml`/`pixi.lock`/`src/platform/Containerfile` changed by the same batch as spec-mcp-era-isolation's entry above (`6ff2c008b4` pixi 0.78.0 registry sync, `583bdf4eb5` nodejs floor widen, `06fe64f603` mcp ceiling bump, `daa35ee171` django pin restore) — Containerfile's builder pin is the direct 0.78.0 bump; pixi.toml/pixi.lock carry the shared-file edits. None touch this spec's own CAP-1..3 (Containerfile still drops `pip --no-deps`, no pixi in the runtime image, pixitainer-docker verdict unchanged); this spec's "mcp 2.0 not in scope" constraint is about the runtime image, which the dev-tooling mcp ceiling in pixi.toml doesn't affect. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env`.
- (event by claude) Surface reconcile 2026-08-30: `pixi.toml`/`pixi.lock` changed by 2 more commits since the entry above, verified via `git log --oneline -3` (both already-landed, `src/platform/Containerfile` untouched): `78ccbe4468` (atlas bootstrap-chain fix -- adds pyarrow to the vuln-db feature for parquet reads, regenerates the lock; dev-tooling dependency, no runtime-image change) and `c4aeaace30` (atlas Story 21.1, PR #932 -- adds an atlas-owned `pyforge-atlas-bootstrap` pixi task). Neither touches CAP-1..3 or the runtime image. Scoped-stamped.
- (event by claude) Surface reconcile 2026-08-30, second pass: `pixi.toml`/`pixi.lock` changed once more by `48ddb34b22` ("fix local recipes", already-landed) — routine dependency-pin bumps (python-crontab, asgiref, pyarrow, age) plus enabling `ppt-master`/`codegraph` recipe candidates. `src/platform/Containerfile` untouched; no runtime-image change. Scoped-stamped.
- (event by claude) Surface reconcile 2026-08-31: `pixi.toml` changed by 2 more commits since the last stamp (`d230e7fce2` token-economy dep activation, `5017329a9f` atlas 21.6 autosave rewriting only the `pyforge-atlas-bootstrap` task description/cmd), `pixi.lock` by 1 (`d230e7fce2`'s regenerated lock). Verified via `git log --oneline 48ddb34b22..HEAD -- pixi.toml pixi.lock`; `src/platform/Containerfile` untouched in this range. Both commits are dev-tooling/atlas-task changes — no runtime-image change, no CAP-1..3 touch. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env`.
- (event by claude) Surface reconcile 2026-09-01: `pixi.toml` changed once since the 2026-08-31 stamp by `af315d4bd7` (marshal Story 28.7, done) — `[feature.local-recipes.tasks.index-freshness-check]` dev-tooling task; `src/platform/Containerfile` untouched; no CAP-1..3 runtime-image change. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env`.
- (event by claude) Surface reconcile 2026-09-01, second pass: `[feature.pyforge-ci.dependencies]` gained `"ruamel.yaml"` + lock regen; `Containerfile` untouched; no CAP-1..3 touch. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 2 governed path(s) drifted against this spec's recorded baseline (2 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `pixi.lock` (changed)
  - `pixi.toml` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (event) Surface reconcile 2026-09-04 (fleet hygiene, PR #1043): PR #1040's `pixi lock` (64f3f8e584 — `pnpm >=12.3.1` → `>=12.3.2`, lock re-solved) moved this Spec's `pixi.toml` / `pixi.lock` rows without this memlog moving. Accept the current tree as the post-land surface; no contract change, no new product work. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env` (scoped, never a bare `--write-baseline`), from a clean tree so the recorded hashes are committed content.
- (event) Surface reconcile 2026-09-04 (PR #1043, second pass — the Detectors env): `pixi.toml` gains `[feature.detectors.dependencies]` (pyforge-doctor path dep, `ruamel.yaml`, `playwright-python`) and the `detectors` environment (no-default-feature; 86 packages) that `.github/workflows/detectors.yml` now installs pixi-first with `locked: true`, pip as the visible fallback; `pixi.lock` gains its block; `environment.yaml` (derived, `build` env) re-exported and caught up (python 3.14.6→3.14.7, nodejs 24.16→24.19 floors already in `pixi.toml`). Nothing this Spec governs changed in meaning; no contract change. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env` from a clean tree.
- (change) 2026-09-04 (PR #1043, fleet CI health pass): `src/platform/Containerfile` runtime stage copies `pyforge-core/src/pyforge/core` beside steward's `pyforge/` (collectstatic died `ModuleNotFoundError: pyforge.core` — Story 14.2's steward→core edge was never carried into the image); `pixi.toml`/`pixi.lock`: platform-ci-test gains the pyforge-steward path dep, dbgpt-sidecar gains pytz. The one-env image contract is unchanged. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env`.
- (event) Surface reconcile 2026-09-04 (PR #1043, fourth pass — the fleet CI health pass): in this Spec's own domain — the one image env `python-agent-platform` carried cp312 builds of `rjsmin` / `rcssmin` (build-string pins in a py3.14 env), so django-compressor's `compress --force` failed in the Containerfile's static stage; `pixi.toml` rows moved for the python-agent-platform feature only: the `rjsmin` / `rcssmin` `build = "py312*"` pins were dropped (they locked cp312 builds into the py3.14 image env, so the Containerfile's `compress` step was red) and the `cachebox <6` / `openfeature-provider-flagd <0.5.1` ceilings the platform policy suite guards were restored after commit 098f0f0672 had dropped them; `pixi.lock` re-solved by `pixi update rjsmin rcssmin` (rjsmin 1.2.5 / rcssmin 1.2.2 `py314` builds in the four envs that carry them; `pixi lock --check` clean, `environment.yaml` unchanged). The static pipeline (collectstatic → compress → collectstatic) replayed locally with the Containerfile's exact environment (`config.settings.production`, `COMPONENT_RUNTIME=local`): 224 files, 2 blocks from 166 templates. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env` (scoped, never a bare `--write-baseline`).
- (event) Surface reconcile 2026-09-04 (PR #1043, seventh pass — the local Platform CI replay): `pixi.toml` gains the `platform-ci-local` task under `[feature.local-recipes.tasks]` (`bash scripts/platform-ci-local.sh`): a local replay of `.github/workflows/platform-ci.yml` — the test job step for step, the three image builds, the container job's runtime smokes and golden-path-promotion + the deploy verifier — against an ephemeral PostgreSQL 17/Redis 7 from platform-dev, so a platform change is proven before it is pushed (operator ask, after four sequential Platform CI cycles for four one-line CI-only fixes). No dependency, environment or lock change; `pixi lock --check` clean, `environment.yaml` untouched. In this Spec's own domain: the replay is the local twin of the CI the image contract is proven by. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env`.
- (change by claude) Surface touch 2026-09-05 (PR #1045): pixi.lock/pixi.toml re-solved for the new `pyforge-testing-kit` test-time dependency added to several stations' feature blocks (retro-pyforge-steward-2026-09-04.md action item 11: the fleet's whole-branch diff-guard test mechanics (git subprocess calls computing files/commits changed since origin/main) were consolidated into a shared `pyforge.testing_kit.branch_diff_guard` module (PR #1045).); the one-pixi-env-per-image contract this spec tracks is unaffected (test-time-only dependency, not a run-dep). Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env`.
- (change by claude) Surface touch 2026-09-05 (retro action item 9, fix/steward-changeset-gate-and-dockerignore): `src/platform/Containerfile` now writes `.pixi/config.toml` directly via `RUN mkdir -p .pixi && printf ... > .pixi/config.toml` instead of relying on `.dockerignore`'s `!.pixi/config.toml` negation (which forced BuildKit/Buildah to walk the whole multi-GB `.pixi/` tree on a raw `docker build .` to resolve the re-inclusion). The one-pixi-env-per-image contract this spec tracks is unaffected -- same env, same lock, same install; only how one static config file reaches the builder stage changed. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env`.
- (change by claude) Surface touch 2026-09-05: pixi.toml's content hash moved for the new `platform-policy-suite-check` task (retro action item 3 (retro-pyforge-steward-2026-09-04.md, 2026-09-05): a new pyforge-doctor repo-scope source, `platform-policy-suite`, judges src/platform's manifest-only tests/policy subset by shelling out to the platform-ci-test pixi env's own interpreter (cli_bridge.run_pytest, AD-5), so a pixi.toml regression like the openfeature-provider-flagd/cachebox ceiling drop is caught even when Platform CI itself is disabled. WARN when that env is not installed, FAIL only on an actual policy-suite failure.); the one-pixi-env-per-image contract this spec tracks is unaffected (a new local-recipes-env pixi task, not a platform image dependency). Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env`. Merged with `main` (independently landed the item-9 entry above); file-level changes merged clean, only the append-only memlogs and the JSON baseline needed manual reconciliation.
- (note by claude) 2026-09-05 (Story 45.1 pin PR, follows #1059): pixi.toml and pixi.lock changed — bmad-eval-quality >=0.2.0.dev0 added to feature.local-recipes' linux-64 and osx-arm64 target tables (unix-only variant on SelfExplainML), bmad-method-wds-expansion pin dropped (upstream deprecated), bmad-labs-skills comment 22 skills; lock re-solved for the pin swap only (no other package moved). Routine suite-pin upkeep this spec's surface already anticipates; no capability change against this spec.
- (note by claude) 2026-09-05 pixi upgrade sweep PR (branch maint/pixi-upgrade-sweep-2026-09-05): pixi.toml, pixi.lock, src/platform/Containerfile (builder-stage image tag -> 0.79.0) changed — operator `pixi upgrade --pinning-strategy latest-up` pass landed as one maintenance PR: pixi 0.78.0->0.79.0 (requires-pixi + $schema + all 15 registered external sites via bump-pixi-version, environment.yaml regenerated), bmad-method >=6.12.0, bmad-suite >=2026.9.5, ~20 other floors bumped, lock re-solved; the cachebox <6 and openfeature-provider-flagd <0.5.1 ceilings the platform policy suite guards were restored after the sweep dropped them. No spec-content change; scoped --write-baseline follows this entry.
- (note by claude) 2026-09-05 hygiene-2 PR (branch maint/hygiene-2-2026-09-05): pixi.toml changed — `lint` task -> CI-parity conda-smithy via pixi exec over real recipe dirs; no dependency, env, or lock change. Scoped --write-baseline follows.
- (note by claude) 2026-09-05 hygiene-2 PR, second pixi.toml touch (same branch): the conda-smithy `lint` task now exports GH_TOKEN from `gh auth token` when unset (the CalVer linter's --conda-forge mode calls GitHub per recipe; anonymous is 60 req/h) and its description records the corpus runtime. No dependency, env, or lock change. Scoped --write-baseline follows.
- (event) Surface reconcile 2026-09-07 (fleet hygiene, spec-45-2 CAP-2 pilot landing): `pixi.toml` changed to add three `local-recipes` pixi tasks (`eval-quality-smoke`, `eval-quality-review-twin-run`, `eval-quality-review-replay`) for pyforge-steward's `spec-bmad-eval-quality` CAP-2 pilot — unrelated to this spec's own surface, but this spec's blanket `pixi.toml` glob makes it a co-governor of that one file. No product work here; the one-pixi-env-per-image contract this spec tracks is unaffected. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env`.
- (event) Surface reconcile 2026-09-07 (fleet hygiene, pyforge-steward Story 46.3, spec-bmad-suite-lifecycle CAP-4 half): `pixi.toml` changed to add one `local-recipes` pixi task (`tea-test-review`, wrapping the real `tea-test-review` CLI with `--base origin/main --min-score 80` defaults) for TEA provisioning -- unrelated to this spec's own surface, but this spec's blanket `pixi.toml` glob makes it a co-governor of that one file. No product work here. Re-derived the governing set the same way Story 45.2/46.1 did (grep `pixi.toml` in every spec's `surface:` frontmatter) rather than assuming their own six-spec list still holds: it now numbers nine (this spec included), not six -- the surface set has grown since 45.2/46.1 landed. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-platform-image-one-pixi-env`.
- (note by claude) Cross-station surface touch (pyforge-doctor Story 20.2, 2026-09-06): pixi.toml gained one new task, [feature.local-recipes.tasks.bmad-render-config-ambiguity-check], under [feature.local-recipes.tasks] -- a different table entirely from this spec's own [feature.python-agent-platform] surface. No other file this spec governs (the Containerfile, pixi.lock, platform_image_pip_layer.py) was touched. Verified via git stash: pixi.toml carried zero drift against this spec's baseline immediately before this edit, so this is the sole cause. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (note by claude) Cross-station surface touch (pyforge-doctor Story 20.3, 2026-09-07): pixi.toml gained one new task, [feature.local-recipes.tasks.frozen-path-changed-check], under [feature.local-recipes.tasks] -- a different table entirely from this spec's own [feature.python-agent-platform] surface. No other file this spec governs was touched. Verified via git stash: pixi.toml carried zero drift against this spec's baseline immediately before this edit, so this is the sole cause. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (note by claude) 2026-09-06 mason Story 14.1: pixi.toml changed -- reworded both bmad-eval-quality pin comments (feature.local-recipes' linux-64/osx-arm64 target tables) to state the recipe is re-verified win-ready and only the __win artifact's Windows build/upload is outstanding, matching install-matrix.md's hazard-cell wording; no dependency, env, or lock change. Scoped --write-baseline follows.
