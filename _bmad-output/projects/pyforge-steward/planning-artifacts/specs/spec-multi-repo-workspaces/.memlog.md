---
topic: One workspace opens every repo a story touches — the multi-repo layer above Epic 13's single-repo verbs
updated: 2026-09-09T17:54
---

- (direction) Operator (2026-08-22): incorporate into backlog now
- (decision) Extend-vs-new RESOLVED: EXTEND Epic 13 (stories 13.3/13.4) — the single-repo verbs 13.1/13.2 are this layer's substrate and both are backlog; two worktree systems would be debt (convergence rule)
- (capability) CAP-1 declarative repo sets ([projects.<slug>] model) + workspace start cutting one worktree per registered repo on a shared f-<feature> branch + generated .code-workspace; CAP-2 workspace-level status (dirty/unpushed across the set) + refusal-on-dirty removal. Pattern from the sibling org's developer-workspace-management dream (unlicensed — pattern only)
- (event) Spec seeded lean 2026-08-22; decomposed as Epic 13 stories 13.3/13.4
- (decision by claude) PARK with a named trigger, 2026-09-09: 'when a second repo joins the estate' -- which is Story 44.3 (python-foundry). CAP-1/CAP-2 are built and `done` (Stories 13.3/13.4: start_repo_set / status_repo_set / clean_repo_set and .code-workspace generation at workspace.py:277, :394, :413, :239; registry path .steward/repo-sets.yaml at :38) and have never been invoked -- no `.steward/repo-sets.yaml` exists (git ls-files .steward/ returns only keys-inventory.yaml plus the two .example.yaml files). Adopting now would mean writing a repo-set naming a repo that does not exist, so parking is honest rather than dismissive (fleet-readiness-decision-batch-2026-09-09 rows stB-B8 / C7).
- (decision by claude) Open question 'where the repo-set registry lives (steward config vs a tracked manifest)' closed: it answers itself and should say so cleanly -- resolved at Story 13.3 as `.steward/repo-sets.yaml` (workspace.py:38); the resolution belongs in the Spec Change Log, not as a live question. open_questions: [] (fleet-readiness-decision-batch-2026-09-09 row stB-C7).
- (event) Self-validate Pass 1 (coherence) PASS on the 2026-09-09 re-derive: CAP-1/CAP-2 untouched, status stays 'ready' with a park note naming the trigger, open_questions now empty
- (event) Self-validate Pass 2 (preservation) PASS: the park decision with its 44.3 trigger, the built-but-never-invoked evidence (three verbs plus .code-workspace generation; no .steward/repo-sets.yaml in git ls-files) and the reason parking is honest rather than dismissive all landed as a dated Spec Change Log entry; the registry-location retirement points at the 2026-08-23 entry that already holds the answer
