---
topic: python-agent-platform (legacy memlog; frontmatter added by the 2026-08-22 format migration, marshal Story 25.3 — body below is the untouched original)
updated: 2026-09-02T06:26
---

# .memlog.md — spec-python-agent-platform

## 2026-08-14 — Story 10.2: one factory-sourced environment (repair pass)

`pixi.toml` changed: added `[feature.python-agent-platform]` (+ `.dependencies` pinning
`python = "3.12.*"`, `langflow`, `dbgpt`, `dbgpt-serve`, `django`, `fastapi`,
`django-health-check`, `psycopg2`, `redis-py`, `channel-priority = "flexible"`) and the
`[environments].python-agent-platform` entry, plus uncommented, bumped (`>=0.7.1` -> `>=0.8.3`),
and relocated the stale `pixitainer` pin into
`[feature.local-recipes.target.linux-64.dependencies]`. Full task list,
Design Notes and Spec Change Log live in
`_bmad-output/projects/pyforge-steward/implementation-artifacts/spec-10-2-one-factory-sourced-environment.md`.
`environment.yaml` (also in this spec's surface) is byte-identical to its pre-story baseline —
the `build` env's own deps are unaffected by the new feature, so no drift there.

`src/platform/docs/make.bat` also reports changed against the committed baseline, but this is a
checkout artifact, not new content: the file is `text eol=crlf` (`.gitattributes`), so its
on-disk bytes are CRLF in any freshly checked-out worktree while the committed blob (and the
baseline hash, stamped from a working tree where the file had just been written pre-checkout) is
LF-normalized. `git diff` against `HEAD` is empty; `sha1sum` of the raw CRLF bytes simply differs
from `git show`'s LF bytes. No content change since Story 10.1's own follow-up review pass
(above) actually landed. Named here so `--write-baseline` can absorb the checkout-normalized
hash going forward.

Baseline re-stamped for this spec only (`python scripts/spec_surface_check.py --write-baseline
--spec pyforge-steward/spec-python-agent-platform`) in the same change;
`python scripts/spec_surface_reconcile.py` confirmed `OK: every tracked file governed or
allowlisted; no drift.` (exit 0) afterward.

## 2026-08-14 — Story 10.1: the host renders into src/platform

Rendered cookiecutter-django (`--checkout 2025.07.27`) into `src/platform/**` per
CAP-1. Full task list and Design Notes (incl. the `platform` -> `platformapp`
inner-package rename, the FastAPI ASGI seam, django-health-check wiring, and the
`platform-ci.yml` workflow) live in
`_bmad-output/projects/pyforge-steward/implementation-artifacts/spec-10-1-the-host-renders-into-src-platform.md`.

Surface's `src/platform/` glob corrected to `src/platform/**` in this same change —
the bare-directory form matched zero tracked files (git tracks files, not
directories), which is why every rendered file under `src/platform/` was reporting
`[ungoverned]` despite the surface nominally declaring the directory.

## 2026-08-14 — Story 10.1 follow-up review pass (repair session)

Second review pass (Blind Hunter + Edge Case Hunter) over the full diff found five
low-severity patches, applied in this change:

- `src/platform/docs/Makefile`, `src/platform/docs/make.bat` — `APP` still pointed
  at the pre-rename `../platform`; corrected to `../platformapp`.
- `src/platform/pyproject.toml` — `[tool.importlinter]` `root_packages` and the
  boundary contract's `source_modules` didn't include `tests`, leaving the
  `pyforge.*` import boundary unenforced under `src/platform/tests/`; added.
- `src/platform/config/asgi.py` — the composed dispatcher's lifespan branch acked
  startup/shutdown locally instead of forwarding to `fastapi_application` (so a
  future FastAPI startup/shutdown hook would never fire); now forwards. A bare
  `/api` (no trailing slash) fell through to Django instead of FastAPI; fixed via
  a shared `_is_api_path` predicate.
- `src/platform/config/fastapi_app.py` — docstring clarified: HTTP-only path
  routing (websockets aren't path-routed by this seam yet), the no-`Mount`
  prefix convention future routes must follow, and `/api/health` vs `/ht/`'s
  distinct purposes.
- `.github/workflows/platform-ci.yml` — removed the redundant standalone
  import-boundary CI step (the full-suite step already re-runs the same test).

## 2026-08-14 — landing pass for Story 10.1 (land/steward-10-1)

- `src/platform/docs/make.bat` — rendered by 10.1's cookiecutter render but written
  after the dev session's in-worktree spec-surface stamp, so the landed baseline
  missed exactly this one path. Named here and scoped-stamped at landing; no content
  change beyond the render itself.

## 2026-08-15 — Story 10.3 ("One image, both engines") + its follow-up review pass

**This spec's own surface had never been reconciled for Story 10.3.** The story's earlier
repair pass reconciled `pyforge-mason/spec-django-accelerator-framework` — which only
*incidentally* overlaps `src/platform/**` — and stopped there, so the repo-wide
`spec_surface_reconcile.py` gate went green while THIS spec (whose `surface:` is
`src/platform/**`, `pixi.toml`, `environment.yaml`, i.e. the actual owner of every file
the story touched) still carried six unreconciled paths. They showed as `drift-presumed`
WARN rather than `drift` FAIL only because this memlog's hash had already moved for
Story 10.1's landing — the change was riding a pre-existing desync past the gate. Caught
by the follow-up review pass (Blind Hunter); this entry closes it.

Paths changed under this spec's surface across Story 10.3 (both the story itself and the
follow-up review pass that produced this entry):

- `src/platform/Containerfile` — ADDED. Multi-stage UBI9-minimal image: a
  `ghcr.io/prefix-dev/pixi:0.76.2` builder materializes `python-agent-platform`, a pip
  layer adds the Django-host extras that env does not cover, a second build-time-only
  `pyforge-steward` env runs the `container-gates secrets-scan` gate, and the runtime
  stage copies only the materialized env + app code, runs a three-pass
  `collectstatic`/`compress`/`collectstatic`, and serves as a non-root arbitrary-UID user.
- `src/platform/compose/compose.yml` — ADDED. Local platform + `postgres:17` + `redis:7`
  stack, portable to `docker compose` and `podman-compose`; migrates before serving and
  now declares its own healthcheck.
- `src/platform/config/settings/base.py` — CHANGED. `INSTALLED_APPS` narrowed from
  `["health_check", "health_check.db", "health_check.cache"]` to `["health_check"]`: the
  conda-sourced `django-health-check >=4.5.0` this spec's own env pins has no such
  sub-apps, and `django.setup()` raised `ModuleNotFoundError` under the old wiring.
- `src/platform/config/urls.py` — CHANGED. `/ht/` moved off the deprecated
  `include("health_check.urls")` onto `HealthCheckView.as_view(checks=[...])` with
  dotted-string check references — the one invocation shape valid under BOTH the pip-pinned
  3.24.0 (`requirements/base.txt`, the `test` job) and the conda-pinned 4.5.0 (the image).
- `src/platform/platformapp/static/css/vendor/bootstrap.min.css` and
  `.../js/vendor/bootstrap.min.js` — CHANGED. Trailing `sourceMappingURL` comments removed;
  the referenced `.map` files were never vendored, and the runtime stage's strict WhiteNoise
  manifest build hard-fails on a missing referenced file.
- `pixi.toml` — CHANGED (comment only). The `requires-pixi` pin-site enumeration was
  `NINE places … ALL NINE ARE 0.76.1`; Story 10.3 added a tenth site
  (`src/platform/Containerfile`'s builder image tag, at 0.76.2) without registering it.
  Now enumerated as TEN with the 0.76.2 exception explained. `environment.yaml` re-exported
  and confirmed byte-identical — a comment-only change does not move the derived export.

Two findings from the same review pass are worth recording here because they constrain
future edits to `[feature.python-agent-platform]` specifically:

1. **The pip layer must exclude every package the LOCK provides, not every package the
   feature DECLARES.** The exclusion list was derived from the feature's nine declared
   dependencies, so five requirement lines whose packages arrive transitively in the
   395-package solve were silently replacing their conda builds — `pillow` 12.3.0→11.3.0,
   `celery` 5.6.3→5.5.3, `uvicorn` 0.52.3→0.35.0, `gunicorn` 26.0.0→23.0.0, and
   `argon2-cffi` same-version. conda-forge packages ship real `.dist-info`, so pip
   uninstalls them first. The celery one was actively out of spec: pip's 5.5.3 declares
   `kombu<5.6` against this env's `kombu 5.6.2`, a constraint `--no-deps` suppresses.
2. **`uvicorn` and `uvicorn-worker` are now a coupled pair in this env.** Fixing (1)
   exposed that `requirements/base.txt`'s `uvicorn-worker==0.3.0` calls
   `uvicorn.Config.setup_event_loop()`, removed in uvicorn 0.36.0 — a hard gunicorn
   worker-boot failure against the conda `uvicorn 0.52.3`. The old pip downgrade had been
   masking it. The image now installs `uvicorn-worker>=0.4.0`. Any future uvicorn bump
   across an API break needs the matching worker release; the failure mode is a boot loop.

Verified live before stamping (docker engine; this sandbox has no podman — the
`platform-ci.yml` `container` job's podman leg remains the first automated proof):
`docker compose up -d --build --wait` reports **all three services healthy**, `/ht/` 200
(Database OK, Cache OK), `/` 200 with all three rendered static-asset URLs 200,
`/admin/login/` 200 against 24 migrated tables, `/api/health` 200, `manage.py check` clean,
a full arbitrary-UID boot (`--user 24680:0`, no passwd entry) serving 200 with `$HOME`
writes succeeding, and `docker history` clean of every credential shape.

Baseline stamped for this spec only (`python scripts/spec_surface_check.py --write-baseline
--spec pyforge-steward/spec-python-agent-platform`) in the same change.

## 2026-08-15 — Story 10.3 follow-up review pass (second pass on a `done` spec)

An independent follow-up review of Story 10.3 landed six further changes under this spec's
surface. Two of them are behavioural fixes to the image; the rest tighten what CI can
actually catch.

Paths changed under this spec's surface in this pass:

- `src/platform/Containerfile` — CHANGED, three substantive edits.
  1. **`$HOME` moved off `/app` onto a dedicated `/app/.home`.** The previous pass gave an
     arbitrary UID a writable `$HOME` with `chgrp 0 /app && chmod g+rwX /app`. On POSIX,
     permission to unlink or rename an entry comes from the DIRECTORY, so that grant let the
     runtime user `rm /app/entrypoint.sh` and write its own in place, and rename `/app/config`
     — reproduced live under `--user 24680:0` before the fix, against the Containerfile's own
     comment asserting the source tree stayed read-only. `/app` is now back to root:root 0755
     and `$HOME` is a group-0-writable subdirectory. Re-verified live: gunicorn 26's control
     socket lands at `/app/.home/.gunicorn/gunicorn.ctl` owned by the arbitrary UID,
     `entrypoint.sh` is no longer removable, `config/` is no longer renameable.
  2. **`STATIC_ROOT` is now group-READ-ONLY (`g+rX`), not group-writable.** Nothing writes it
     after the build — WhiteNoise serves it read-only from a baked manifest — and it is the one
     tree whose contents reach every visitor's browser as executable JS. Consequence recorded
     in the file: `collectstatic` can no longer be re-run inside a running container, which is
     the intended posture for an immutable image.
  3. **The nine explicitly-named pip extras are now exact `==` pins, not floors.** They were
     the only packages in the image resolved live from PyPI at build time, so rebuilding the
     same commit could produce a different image — and this file already documents that a wrong
     version here (the uvicorn/uvicorn-worker pair) is a gunicorn boot loop, not an import
     error. Versions captured from the verified build: `django-timezone-field==7.2.2`,
     `python-crontab==3.3.0`, `cron-descriptor==2.1.0`, `django-appconf==1.2.0`, `rjsmin==1.2.2`,
     `text-unidecode==1.3`, `fido2==2.2.1`, `qrcode==8.2`, `uvicorn-worker==0.4.0`. The cost is
     deliberate: a security bump to `fido2`/`qrcode` now needs a manual edit here.
     Two comment corrections rode along: the exclusion-filter count ("14 kept, 10 excluded" was
     stale by one — it is 13/11), and the `psycopg[c]` note, which cited a deferred-work entry
     that lives only in this story's gitignored Tier-3 ledger and is therefore unreadable from a
     clone; the gap is now written out in full at the point of use instead of cited.
- `src/platform/config/urls.py` — CHANGED, comment only. Records what `/ht/` does and does not
  prove: `health_check.Database` is a CONNECTIVITY probe under both pinned versions, not the
  `TestModel` write/read/delete round-trip the removed `health_check.db` sub-app registered, so
  a 200 does not mean the schema is migrated or that writes succeed. Prior passes corrected this
  claim in the story spec's Design Notes; it had never been recorded in the repo itself, where a
  future reader of the endpoint would look.
- `src/platform/tests/test_health_endpoint.py` — NEW. The `/ht/` route had zero runtime coverage
  on the pip-pinned `django-health-check==3.24.0` leg that `platform-ci.yml`'s `test` job runs.
  That matters because `config/urls.py` documents a real trap — 3.24.0's `get_plugins()` catches
  only `ValueError` on its per-entry unpack, so passing raw CLASSES instead of dotted strings
  raises an uncaught `TypeError` — and `get_plugins()` is reached only from an actual HTTP
  request, never from `as_view()` or URLconf loading. Proven, not assumed: with the raw-class
  form injected, `manage.py check` still reported "no issues" while the new tests failed. The
  container job cannot cover this either; it runs the conda 4.5.0 side, which catches both.

Verified live in this pass (docker engine; this sandbox still has no podman, so
`platform-ci.yml`'s podman matrix leg remains the first automated proof of the dual-engine
claim): a fresh `docker build` clean with `secrets-scan: clean (2 root(s))`; `pip list` inside
the image confirms the conda builds are still unshadowed (Django 5.2.15, pillow 12.3.0, celery
5.6.3, uvicorn 0.52.3, gunicorn 26.0.0, argon2-cffi 25.1.0, psycopg2 2.9.12) and the nine extras
match their new exact pins; `docker compose up -d --build --wait` exits 0 with all three services
healthy; `/ht/`, `/`, `/admin/login/` (against 24 migrated tables) and `/api/health` all 200, with
each of the three static URLs the home page actually renders independently 200; a full
arbitrary-UID application boot (`--user 24680:0`) reaches "Application startup complete" with no
traceback and serves 200; `docker history` and a recursive filesystem grep find zero credential
shapes. On the pip side, in a `python:3.12` container against a real postgres: `ruff check .`
clean, `mypy platformapp config tests` clean (46 files), `pytest` **24 passed** (21 before).

Baseline stamped for this spec only (`python scripts/spec_surface_check.py --write-baseline
--spec pyforge-steward/spec-python-agent-platform`) in the same change.

Note for whoever owns this surface next: `pixi.toml` also changed in this pass (comment only, the
`requires-pixi` pin-site enumeration — it was factually inverted and undercounted; live state is
thirteen sites, twelve at 0.76.2, and the ROOT `Containerfile` alone at 0.76.1, below the floor
its own manifest declares, so that image's builder stage currently fails). `pixi.toml` is also
governed by `pyforge-steward/spec-unified-container`, which owns that root Containerfile; the
finding is named in that spec's own memlog and carried as deferred work rather than fixed from
here.

## 2026-08-15 — Story 10.3, third follow-up review pass (surface reconcile)

Changed under this spec's `surface:` (`src/platform/**`, `pixi.toml`, `environment.yaml`), all
five paths named explicitly rather than folded into a count:

- `src/platform/Containerfile` — the pip layer now FAILS THE BUILD if pip displaces any
  conda-provided package. The exclusion alternation is hand-maintained against a 395-package
  solve and nothing re-derives it, which is how the previously-found five-package shadowing
  (pillow/celery/uvicorn/gunicorn/argon2-cffi, incl. a `celery`→`kombu<5.6` constraint violation
  `--no-deps` suppresses) got in. pip announces every replacement as `Attempting uninstall:`;
  since no kept line should ever displace anything, that string appearing at all is now fatal.
  `set -o pipefail` added because the new `| tee` would otherwise mask a failing `pip install`.
- `src/platform/config/urls.py` — the `/ht/` comment stated an unqualified default `checks` list
  in a block whose entire purpose is 3.24.0-vs-4.5.0 divergence. It was 4.5.0's. Read from both
  installed packages: 3.24.0 defaults to `health_check.{Cache,Database,Mail,Storage}`, 4.5.0 to
  `health_check.checks.{Cache,Database,DNS,Mail,Storage}` — a different module path plus a DNS
  check that would make this endpoint depend on outbound name resolution. Corrected, and the
  consequence (dropping the explicit list is a behaviour change) stated.
- `src/platform/tests/test_health_endpoint.py` — `test_home_page_does_not_shadow_the_health_route`
  asserted only `reverse("home") == "/"`, which stays true no matter where `/ht/` is routed, so
  the property it names was untested. Now asserts forward resolution:
  `resolve("/ht/").func.view_class is HealthCheckView`. Proven, not assumed — with `/ht/` re-pointed
  at a `TemplateView` the test fails; restored, 24/24 pass.
- `src/platform/compose/compose.yml` — comment-only. `restart: on-failure` short form is
  UNBOUNDED; the comment claimed a broken image "fails after a few attempts". What actually makes
  `docker compose up --wait` terminate is the `healthcheck:` (verified live in a prior pass, which
  reported `container compose-platform-1 is unhealthy`). Corrected, and the bound deliberately
  left off with the reason.
- `pixi.toml` — comment-only, and `environment.yaml` re-exported and confirmed BYTE-IDENTICAL, so
  the ungated env-sync check stays green. The pin-site enumeration's headline total was wrong for
  the third consecutive time: it said THIRTEEN while its own list named FIFTEEN (and "TWELVE OF THE
  THIRTEEN" should have been fourteen of fifteen). Rewritten with the list numbered (1)–(15) and an
  explicit instruction to count the enumeration rather than trust the numeral, since the numeral is
  the part that has rotted every time.

Also changed in the same commit but governed elsewhere: `.github/workflows/platform-ci.yml`
(ungoverned) and `.dockerignore` (`spec-unified-container` — named in that spec's own memlog).

Verified live in this pass (docker engine; this sandbox still has no podman, so `platform-ci.yml`'s
podman matrix leg remains the first automated proof of the dual-engine claim): a fresh
`docker build` green end-to-end with `secrets-scan: clean (2 root(s))` and no `Attempting
uninstall:`; the new guard negative-tested by installing `pillow==11.3.0` over the conda build in a
throwaway container as root — `Attempting uninstall: pillow`, guard fires. (First negative attempt
ran as the image's own uid 1001 and pip silently user-installed instead, which is not the builder
stage's posture; re-run as root, which is.) Against a real postgres+redis under `--user 24680:0`:
migrate applied, `/ht/` `/admin/login/` `/` `/api/health` all 200, all three rendered `/static/`
URLs 200, `manage.py check` clean, and the filesystem contract probe green including its new
assertion that `/app` itself is NOT writable (`rm /app/entrypoint.sh` and `mv /app/config` both
`Permission denied`). Image default identity is `uid=1001 gid=0`. On the pip side in a
`python:3.12` container: `pytest` **24 passed**.

Baseline stamped for this spec only (`python scripts/spec_surface_check.py --write-baseline
--spec pyforge-steward/spec-python-agent-platform`) in the same change.

## 2026-08-15 — CI lint + type fixes during landing (`tests/test_health_endpoint.py`)

`src/platform/tests/test_health_endpoint.py` — CHANGED twice, both no behavioral change. (1)
The PR's CI `ruff check .` job failed on `I001` (unsorted import block): `from django.urls
import resolve, reverse` split into two single-import lines. (2) The PR's CI `mypy` job then
failed on `resolve("/ht/").func.view_class` — `view_class` is attached dynamically by `View.
as_view()`'s own closure, not declared on the `Callable` type django-stubs gives
`ResolverMatch.func`; added `# type: ignore[attr-defined]` with a comment naming why. Baseline
re-stamped for this spec only in the same change.

## 2026-08-15 — incidental overlap, unrelated pyforge-doctor Story 11.1 landing

`pixi.toml` changed (commit d682235b1f): a new `[feature.local-recipes.tasks.due-for-verification-
check]` task was added, wiring `python -m pyforge.doctor.sources due-for-verification` in as a
detector task. Unrelated to this spec's own `[feature.python-agent-platform]` block or any
Django/platform capability — pyforge-doctor's Story 11.1 (Epic 11/CAP-1, `spec-deferred-work-
resolution-sweep`'s own contract) landed a `gather_due_for_verification` selector and registered
its pixi entrypoint in the same shared root `pixi.toml` this spec also governs. Named here only so
this spec's own baseline can be scoped-stamped without accepting any OTHER pending drift as
reconciled. `pixi.toml` itself parses and every existing `python-agent-platform` task is
byte-for-byte unchanged; no re-verification of this spec's own capability was needed.

## 2026-08-15 — pyforge-mason Story 5.2 (governance detector), incidental `pixi.toml` touch

`pixi.toml` changed by an unrelated pyforge-mason story (Story 5.2, FR-45/AD-15: the new
`scripts/mason_cfe_surface_check.py` repo-scope detector, which scans commit history to prove
Mason never modifies the conda-forge-expert surface). The only edit under this spec's `surface:`
is a NEW task block, `[feature.local-recipes.tasks.mason-cfe-surface-check]`, added to the
existing `local-recipes` feature — no dependency change, no existing task edited. `environment.yaml`
(also in this spec's surface) is unaffected: the `build` env this file exports has no relationship
to the new task, and `pixi project export conda-environment -e build` reproduces it byte-identical.
`src/platform/**` untouched.

Baseline stamped for this spec only (`python scripts/spec_surface_check.py --write-baseline
--spec pyforge-steward/spec-python-agent-platform`) in the same change.

## 2026-08-15 — incidental overlap, unrelated pyforge-doctor Story 10.1 landing

`pixi.toml` changed (commits e16a2e0f58 + 0f01997c52, the latter a review-pass patch): a new
`[feature.local-recipes.tasks.bmad-method-version-drift-check]` task was added, wiring
`python -m pyforge.doctor.sources bmad-method-version-drift` in as a detector task, mirroring
`due-for-verification-check`'s exact shape. Unrelated to this spec's own
`[feature.python-agent-platform]` block or any Django/platform capability — pyforge-doctor's
Story 10.1 (Epic 10/CAP-1, `spec-bmad-method-version-drift`'s own contract) landed a `gather`
selector comparing `pixi.toml`'s own declared `bmad-method` floor against
`_bmad/_config/manifest.yaml`'s installed version, and registered its pixi entrypoint in the
same shared root `pixi.toml` this spec also governs. Named here only so this spec's own
baseline can be scoped-stamped without accepting any OTHER pending drift as reconciled.
`pixi.toml` itself parses and every existing `python-agent-platform` task is byte-for-byte
unchanged; no re-verification of this spec's own capability was needed.

## Cross-package reconciliation for doctor Story 11.2 (2026-08-15)

- (change) SURFACE DRIFT RECONCILED — `pixi.toml` (commit `34fcec5c6d`, a DOCTOR-owned story
  (Story 11.2, `spec-deferred-work-resolution-sweep` CAP-2), not a steward story — reconciled
  here at landing time, same cross-package pattern as the "Cross-package reconciliation for
  doctor Story 11.1" entry directly above. One governed path changed: `pixi.toml`'s existing
  `[feature.local-recipes.tasks.due-for-verification-check]` task description gains a paragraph
  documenting the new churn-skip behavior, and its `Contract:` line now names `CAP-1 + CAP-2`
  instead of `CAP-1` alone — no dependency added, no new task, so `environment.yaml` stays
  byte-identical and the ungated env-sync check is unaffected. This Spec claims no capability
  for the task itself — CAP-2 ("churn-based cost filtering") is `pyforge-doctor/spec-deferred-
  work-resolution-sweep`'s own contract; this entry reconciles surface/drift bookkeeping only.
  Full accounting of the other two changed paths (both doctor-owned) lives in `pyforge-doctor/
  spec-pyforge-doctor`'s own memlog, Story 11.2 entry.
- (event) Baseline stamped for this spec only (`python scripts/spec_surface_check.py
  --write-baseline --spec pyforge-steward/spec-python-agent-platform`) in the same change.

## Cross-package reconciliation for doctor Story 11.3 (2026-08-15)

- (change) SURFACE DRIFT RECONCILED — `pixi.toml` (a DOCTOR-owned story (Story 11.3,
  `spec-deferred-work-resolution-sweep` CAP-3), not a steward story — reconciled here at landing
  time, same cross-package pattern as the "Cross-package reconciliation for doctor Story 11.1/11.2"
  entries directly above. One governed path changed: `pixi.toml`'s existing
  `[feature.local-recipes.tasks.due-for-verification-check]` task description gains a paragraph
  documenting the mechanical-verdict behavior, and its `Contract:` line now names
  `CAP-1 + CAP-2 + CAP-3` instead of `CAP-1 + CAP-2` — no dependency added, no new task, so
  `environment.yaml` stays byte-identical and the ungated env-sync check is unaffected. This Spec
  claims no capability for the task itself — CAP-3 ("mechanically-checkable claims are verified
  without an agent") is `pyforge-doctor/spec-deferred-work-resolution-sweep`'s own contract; this
  entry reconciles surface/drift bookkeeping only. Full accounting of the other four changed paths
  (all doctor-owned) lives in `pyforge-doctor/spec-pyforge-doctor`'s own memlog, Story 11.3 entry.
- (event) Baseline stamped for this spec only (`python scripts/spec_surface_check.py
  --write-baseline --spec pyforge-steward/spec-python-agent-platform`) in the same change.

## 2026-08-20 — Story 11.1: Langflow joins as a pluggable app

Commit `8cf4a13413`. Twelve governed paths changed, naming all twelve per this reconciler's own
contract:

- `pixi.toml` — CHANGED. New `[feature.platform-dev]` (postgresql, pgvector, redis-server,
  kubernetes-helm, kubernetes-client) composed onto `python-agent-platform`, registered as the
  `platform-dev` environment (AD-16 containerless local dev baseline). Also adds four hard-import
  deps to `[feature.python-agent-platform.dependencies]` discovered live (`chromadb`,
  `langchain-chroma`, `elevenlabs`, `psycopg`) — `langflow.main.create_app()` unconditionally
  imports the first three and needs a Postgres v3 driver for its own SQLAlchemy engine; none were
  `run` deps of the `langflow-suite` recipe (soft `run_constraints` only) or present at all
  (`psycopg`/`asyncpg`). `environment.yaml` (also in this spec's surface) re-exported and confirmed
  BYTE-IDENTICAL — the `build` env's own deps are unaffected by either feature edit, same pattern
  as Story 10.2's entry above.
- `src/platform/config/asgi.py` — CHANGED. Dispatch extended: `/api/v1/*` and bare
  `/health`/`/health_check` forward unchanged to Langflow's app; `/langflow/*` forwards with the
  `/langflow` segment stripped; everything else still falls through to Django, `/api/health`
  unaffected (AD-4). The dispatcher's lifespan handling now drives BOTH sub-apps' (platform stub +
  Langflow) startup/shutdown for the process lifetime via two `_LifespanManager` instances entered
  into one `contextlib.AsyncExitStack` (queue-driven, not the broken single-shot `receive()` replay
  this story's own Design Notes originally specced and then corrected — see the story spec's own
  Spec Change Log) — Langflow's own Alembic bootstrap runs against `langflow_schema` at startup.
- `src/platform/config/fastapi_app.py` — CHANGED, docstring only. Records that `/api/v1/*` is now
  dispatched ahead of this seam directly in `config/asgi.py` (Langflow builds and owns its own
  FastAPI app), and that DB-GPT's `/api/dbgpt/` mount (CAP-3, Story 11.2) is not yet attached.
  `/api/health` itself is unaffected.
- `src/platform/config/settings/base.py` — CHANGED. `langflow_integration` registered in
  `INSTALLED_APPS`; `LANGFLOW_DATABASE_URL` derived from `DATABASES["default"]` + a
  `search_path=langflow_schema` query-string suffix (never a second stored credential);
  `LANGFLOW_DB_DRIVER_CONNECTION_SETTINGS` added alongside it (a second env var — Langflow's own
  `DatabaseService._get_connect_args()` hardcodes `connect_args` for its main session engine,
  which wins over the URL's own query-string `options` on that key and silently drops the main
  ORM engine back to `public`; the URL suffix stays for Alembic, this is the fix for everything
  else); `LANGFLOW_CACHE_TYPE=redis` wired to the existing `REDIS_URL`; `LANGFLOW_CONFIG_DIR`/
  `LANGFLOW_KNOWLEDGE_BASES_DIR` set to explicit non-default paths under `src/platform/.langflow/`
  (added to `.gitignore`, ephemeral runtime scratch, same category as `staticfiles/`/`media/`) —
  kills the local-disk-state default (AD-6).
- `src/platform/langflow_integration/__init__.py`, `apps.py`,
  `migrations/{__init__.py,0001_create_langflow_schema.py}` — ADDED. New migration-only Django app
  (no models); the migration provisions `langflow_schema` via `RunSQL` (`CREATE SCHEMA IF NOT
  EXISTS`, idempotent) — Django's ORM never creates tables there, real not conventional schema
  isolation (AD-5).
- `src/platform/langflow_integration/asgi.py` — ADDED. Builds Langflow's `create_app()` once at
  import time, exported for the dispatcher; hosts the shared `_LifespanManager` class both this
  file's own tests and `config/asgi.py` use (moved here byte-for-byte during this story's own
  review-pass fix, not reimplemented, since `config/asgi.py` already imports from this module).
- `src/platform/langflow_integration/tests.py` — ADDED. Asserts `langflow_schema` exists and owns
  Langflow's tables (`flow`, `user`, `variable`, `vertex_build`, `transaction`, `apikey`, etc.),
  `public` owns none of them (`pg_tables` inspection) — proves AD-5 for real; drives a full
  end-to-end flow execution (`auto_login` → API key mint → `POST /api/v1/run/<flow-id>`) against
  live `postgres:17`/`redis:7`, confirming every written row lands in `langflow_schema` only.
- `src/platform/tests/test_asgi_seam.py` — CHANGED. `pytest.importorskip("langflow")` added — this
  pre-existing Story 10.1 test imports `config.asgi.application` unguarded, and `config/asgi.py`
  now unconditionally imports `langflow_integration.asgi`, which unconditionally imports
  `langflow`; the pip-only CI `test` job never installs it and was failing collection
  (`ModuleNotFoundError`, confirmed live) until this guard was added, matching the pattern this
  story's own two new test files already established.
- `src/platform/tests/test_langflow_mount.py` — ADDED. Covers the 5 I/O-matrix routing scenarios
  (proves AD-4 dispatch order didn't regress `/api/health`) plus a coverage-gap test that drives
  `config.asgi.application`'s actual production `lifespan` scope directly (real ASGI client:
  `lifespan.startup`, a live request through the dispatch to Langflow's `/health_check`, then
  `lifespan.shutdown`) — the exact code path a prior review pass found the original single-shot
  lifespan bug shipped through undetected.

Also changed in the same commit but governed elsewhere: `.github/workflows/platform-ci.yml`
(ungoverned — CI reordered to migrate before the container starts, plus a Langflow smoke-test
step) and `docs/reference/library-llms-full.md` (hand-regenerated per its own prompt, not part of
this spec's `surface:`).

Verified live (real `postgres:17`/`redis:7` containers): `manage.py migrate` creates
`langflow_schema` with zero Langflow tables in `public` (24 Django tables only, zero overlap); all
5 dispatch routing scenarios pass; the dual-lifespan fix verified both positively (services stay
live across a full flow-execution round-trip) and negatively (reintroducing the old single-shot
`_run_lifespan` into a scratch copy of `config/asgi.py` reproduces the exact `NoFactoryRegisteredError`
the new coverage-gap test catches); `pixi install -e platform-dev` solves clean. Full narrative,
Design Notes, and the story's own Spec Change Log (the lifespan-bug root cause and fix) are in
`spec-11-1-langflow-joins-as-a-pluggable-app.md`.

Baseline stamped for this spec only (`python scripts/spec_surface_check.py --write-baseline
--spec pyforge-steward/spec-python-agent-platform`) in the same change.

## 2026-08-20 — Story 11.1, review pass 3 (post-verify-repair)

Two further governed paths changed under this pass, both `patch`-category fixes from an
adversarial review pass run after reconciling this spec's own surface drift (above):

- `src/platform/config/asgi.py` — CHANGED. `_strip_langflow_prefix` mirrored Starlette `Mount`
  semantics incorrectly (physically slicing `/langflow` off `scope["path"]`/`raw_path`), which
  silently dropped the prefix from any redirect Langflow's own router issues through the alias.
  Root-caused against this env's installed `starlette.routing.Mount.matches`/`get_route_path`
  source: a real `Mount` leaves `path`/`raw_path` untouched and extends only `root_path`. Fixed
  by rewriting the function to extend `root_path` only.
- `src/platform/tests/test_langflow_mount.py` — CHANGED + one test ADDED. The existing prefix-
  strip test's assertion updated to expect the alias preserved in the redirect Location (proving
  the fix above); a new `test_lifespan_startup_failure_rolls_back_already_started_subapp` closes
  a coverage gap named in `_dispatch_lifespan`'s own docstring (a startup-failure rollback claim
  that no test had exercised).

Full triage (16 rejects, 3 deferred as `DW-FU-11-1`/`-2`/`-3`) is in the story spec's own Review
Triage Log, pass 3. Verified live against real `postgres:17`/`redis:7` containers: 16/16 tests
pass (`tests/` + `langflow_integration/tests.py`).

Baseline stamped for this spec only (`python scripts/spec_surface_check.py --write-baseline
--spec pyforge-steward/spec-python-agent-platform`) in the same change.

## Cross-package reconciliation for doctor Story 10.2 (2026-08-20)

- (change) SURFACE DRIFT RECONCILED — `pixi.toml` (commit `3749c93afa`, a DOCTOR-owned story
  (Story 10.2, `spec-bmad-method-version-drift` CAP-2), not a steward story — reconciled here at
  landing time, same cross-package pattern as the "Cross-package reconciliation for doctor Story
  11.1/11.2/11.3" entries above. One governed path changed: `pixi.toml`'s existing
  `[feature.local-recipes.tasks.bmad-method-version-drift-check]` task description gains a clause
  documenting the new live-upstream-npm comparison, and its `Contract:` line now names both
  `spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported` and
  `spec-10-2-the-installed-core-is-compared-against-the-latest-upstream-release` instead of the
  former alone — no dependency added, no new task, so `environment.yaml` stays byte-identical and
  the ungated env-sync check is unaffected. This Spec claims no capability for the task itself —
  CAP-2 ("installed-vs-upstream-latest drift") is `pyforge-doctor/spec-bmad-method-version-drift`'s
  own contract; this entry reconciles surface/drift bookkeeping only. Full accounting of the other
  four changed paths (all doctor-owned) lives in `pyforge-doctor/spec-pyforge-doctor`'s own
  memlog, Story 10.2 entry.
- (event) Baseline stamped for this spec only (`python scripts/spec_surface_check.py
  --write-baseline --spec pyforge-steward/spec-python-agent-platform`) in the same change.

## Cross-package reconciliation for atlas Story 16.2 (2026-08-20)

- (change) SURFACE DRIFT RECONCILED — `pixi.toml` (an ATLAS-owned story, Story 16.2,
  `pyforge-atlas/spec-wagtail-corporate-brain` CAP-2, not a steward story — reconciled here at
  landing time, same cross-package pattern as the doctor entries above). One governed path
  changed, COMMENT-ONLY: the `[feature.pyforge-atlas.dependencies]` block's `httpx` rationale
  comment gains a note that Story 16.2 added a second, non-test consumer — the shipped operator
  tool `src/shared/packages/pyforge-atlas/tools/lasuite_bringup.py` hard-imports `httpx` to build
  the real La Suite/Wagtail opener for the attended DW-H3 bring-up — so pruning the starlette
  test never takes the declaration with it. The `httpx = ">=0.28.1"` constraint itself is
  BYTE-IDENTICAL: no dependency added, removed, or repinned, so `environment.yaml` stays
  byte-identical and the ungated env-sync check is unaffected (verified by regenerating and
  diffing at landing time). This Spec claims no capability here; CAP-2 is
  `pyforge-atlas/spec-wagtail-corporate-brain`'s own contract, and the full accounting of the
  other six changed paths (all atlas-owned) lives in that spec's memlog, Story 16.2 entry. This
  work was authored 2026-08-15 but stranded uncommitted in a stale run worktree; it was rescued
  to `rescue/atlas-16-2-revised-work` and landed 2026-08-20.
- (event) Baseline stamped for this spec only (`python scripts/spec_surface_check.py
  --write-baseline --spec pyforge-steward/spec-python-agent-platform`) in the same change.

## 2026-08-20 — platform-ci follow-up: Langflow boot under arbitrary UID + ruff

Story 11.1's in-process Langflow mount shipped three ruff hits and a container boot
loop that red'd platform-ci (`test`, `container` docker/podman) on every subsequent
PR that touched a workflow-filter path -- including atlas PR #567, which never
touched `src/platform/`. The staged-recipes `linter` job was already green.

- `src/platform/config/settings/base.py` -- CHANGED. COM812 trailing comma on
  `LANGFLOW_CONFIG_DIR = env(...)` (sibling `LANGFLOW_KNOWLEDGE_BASES_DIR` already
  had one). Comment extended to name the Containerfile coupling: the defaults under
  `BASE_DIR / ".langflow"` are ephemeral runtime scratch (AD-6, gitignored), and the
  arbitrary-UID runtime must keep `/app/.langflow` group-writable because
  `langflow.main.create_app()` mkdir's them at import time.
- `src/platform/tests/test_langflow_mount.py` -- CHANGED. `import config.asgi as
  asgi_module` moved to module top (PLC0415); the lifespan-rollback test now drives
  the public `application({"type": "lifespan"}, ...)` entrypoint instead of calling
  `_dispatch_lifespan` directly (SLF001). Same coverage: the monkeypatched sub-apps
  still prove AsyncExitStack unwind on startup failure.
- `src/platform/langflow_integration/asgi.py` -- CHANGED. `_LifespanManager._task`
  is `Task | None`; mypy (which never ran on #567 because ruff failed first) flags
  the two `await self._task` sites. Narrowed with `assert self._task is not None`
  -- the task is assigned in `__aenter__` before either await, and `__aexit__`
  only runs after a successful enter.
- `src/platform/Containerfile` -- CHANGED. `/app/.langflow` added as the third
  group-writable runtime path (GID 0, `g+rwX`), alongside `MEDIA_ROOT` and
  `$HOME`. Without it, gunicorn's `USER 1001:0` worker dies with
  `PermissionError: [Errno 13] Permission denied: '/app/.langflow'` during
  `create_app()` and `/ht/` never 200s. `/app` itself stays root:root 0755 -- this
  is a new scratch dir, not a reopening of the `$HOME=/app` grant Story 10.3's
  review closed.

`pixi.toml` / `environment.yaml` untouched. Verified: `ruff check .` in
`src/platform/` is clean.

Baseline stamped for this spec only (`python scripts/spec_surface_check.py
--write-baseline --spec pyforge-steward/spec-python-agent-platform`) in the same
change.

## 2026-08-21 — Surface reconcile (foreign-drift documentation)

41 `src/platform/**` files accumulated steward Epics 10–12 landings (host
render, factory-sourced env, one-image-both-engines, DB-GPT sidecar +
Pattern-B, Celery worker, isolation proofs, vanilla chart + OCP overlay —
PRs #595–#605 era) without this umbrella spec's memlog moving; the
per-story specs and merged PRs are the record. No code change rides this entry.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (change) Spec-surface gap closed (2026-08-31, recovered from an orphaned 2026-08-28 reconciliation commit, re-verified against current main before landing): `scripts/build-pixi-mirror.py` (commits `eeff596ac1`/`5aeea4f47c`/`891dcbf099`/`683b35a35d`/`0526ddaad7`/`0d672b646e`, Story 12.3, `done` in `sprint-status-ledger.yaml`) realizes CAP-6 ("Air-gap parity is a test, not a hope") — the pixi-mirror builder the `air-gap-parity` CI job runs before blocking egress, per the file's own docstring citing "Story 12.3 (spec-12-3-air-gap-parity-is-a-failing-check, AD-13/CAP-6)". Added to `surface:` now; no contract change.

## Cross-package reconciliation for marshal Story 28.7 (2026-09-01)

- (change) SURFACE DRIFT RECONCILED — `pixi.toml` (a MARSHAL-owned story, Story 28.7,
  `spec-28-7-index-freshness-is-an-advisory-finding` CAP-10, not a steward story — reconciled
  here at landing time, same cross-package pattern as the doctor/atlas entries above). One
  governed path changed: `pixi.toml` gains `[feature.local-recipes.tasks.index-freshness-check]`
  wiring `scripts/index_freshness_check.py` as a runtime-scope detector task — no dependency
  added, no existing `[feature.python-agent-platform]` task edited, so `environment.yaml`
  stays byte-identical and the ungated env-sync check is unaffected. This Spec claims no
  capability for the task itself — CAP-10 is `pyforge-marshal/spec-28-7-index-freshness-is-an-
  advisory-finding`'s own contract; this entry reconciles surface/drift bookkeeping only.
- (event) Baseline stamped for this spec only (`python scripts/spec_surface_check.py
  --write-baseline --spec pyforge-steward/spec-python-agent-platform`) in the same change.
- (event by claude) Surface reconcile 2026-09-01, second pass: `[feature.pyforge-ci.dependencies]` gained `"ruamel.yaml"` only — pyforge-ci test wiring, no python-agent-platform CAP touch. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-python-agent-platform`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 1 governed path(s) drifted against this spec's recorded baseline (1 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `pixi.toml` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-python-agent-platform` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
