---
topic: Local OpenShift (CRC) hybrid environment: cluster bring-up, 12.1 chart Tier-3 verification, sidecar-in-chart, Redis hardening, dlt Projects V2 bridge
updated: 2026-08-22T07:05
---

- (direction) Operator (2026-08-22): run the deep research + spec pass. Four decisions locked via AskUserQuestion: chain home = EXTEND EPIC 12 (12.4+, not a new epic); DB-GPT sidecar joins the chart NOW; Redis = keep emptyDir + add AUTH + NetworkPolicy; canonical image path = INTERNAL-REGISTRY PUSH
- (note) Sources: docs/dreams/local-ocp-hybrid-environment.md + verbatim intake docs/intake/local-ocp-hybrid-environment/technical-spec.md + two 2026-08-22 research passes (fleet reconciliation brief; external-claims verification) distilled into companions reconciliation-and-corrections.md + cluster-bringup-facts.md
- (decision) STRIKE from the intake (evidence-locked): Phase-2 fresh cookiecutter scaffold (src/platform IS the app layer); the Traefik/ASGI-multiplexer topology (shipped as config/asgi.py in-process dispatch behind AD-4's one front door; CRC router is HAProxy anyway); DBGPT_DB_URL-on-Postgres (0 code hits + metadata store is SQLite/MySQL/OceanBase-only, dual-verified); logspace/langflow image (dead namespace, no 1.x tags; platform pins langflowai >=1.11.4); oc expose svc/dbgpt (AD-4/AD-7); hand-psql schema creation (migrate Job owns it); ?options=search_path for Langflow (connect_args clobber — platform already uses LANGFLOW_DB_DRIVER_CONNECTION_SETTINGS); bmad-marshal-detectors-init + 3 wrong binary names (wiring routes through steward 15.3); dlt init github_projects (no such verified source — custom GraphQL source required)
- (capability) CAP-1 cluster bring-up + registry posture: Podman Desktop OpenShift Local extension flow (CRC 2.63.0/OpenShift 4.22.7; 4 cores/10.5GB/35GB; Linux needs crc binary on PATH) to Running + oc authenticated; the internal-registry push pattern (podman login/tag/push to default-route-openshift-image-registry.apps-crc.testing -> ImageStream) minted as steward's anticipated OpenShift/registry-posture AD; pull secret + kubeadmin creds + PAT recorded per keys discipline (hand-authored first inventory entries — the keys-issue verb gap is accepted). Success: documented bring-up reproduces on a fresh workstation; image pullable in-cluster from the ImageStream
- (capability) CAP-2 DB-GPT sidecar joins the chart: Deployment + dedicated SQLite PVC at /app/.home/.dbgpt/workspace/pilot/meta_data + singleton semantics (replicas 1, Recreate) + internal-only Service consumed via DBGPT_SIDECAR_BASE_URL; the 12.1 inventory test's sidecar REJECTION flips to expectation; the Epic-12-kickoff follow-up closes. Success: chart renders the sidecar under restricted-v2; invariant tests updated and green
- (capability) CAP-3 Redis hardened, ephemeral kept: emptyDir stays (Celery re-queues), AUTH (existingSecret key) + NetworkPolicy restricting to platform pods added — closing 12.1's named unauthenticated-Redis follow-up. Success: rendered NetworkPolicy + AUTH-wired REDIS_URL; invariant tests cover both
- (capability) CAP-4 Tier-3 attended verification run: helm install core+overlay on the live cluster proves the four honestly-unverified items — Route admission, SCC enforcement, PVC binding, official postgres/redis images under SCC-assigned arbitrary UID (fallback plan: RH/bitnami images or PGDATA-subdir seam) — plus the fresh-install migration window observation. Success: a dated verification record lands in the 12.1 spec's orbit; failures become findings not silent notes
- (capability) CAP-5 dlt Projects V2 bridge: a small custom dlt GraphQL source (ProjectV2/items/fields; reuse steward sync.py's queries) loads the github_metrics dataset into cluster Postgres via port-forward; classic PAT with read:project (fine-grained PATs cannot reach user-owned projects); 5000 GraphQL points/hr + 100-item pages budgeted; board created + repo-linked via gh project link. Success: board items queryable in github_metrics; kin-declared to spec-jira-github-projects-sync Mode B, never a second sync engine
- (constraint) Wiring consumes steward 15.3 (provision --module) — never ad-hoc installer runs; TEA validation arrives via 15.3 (tea-test-review exists standalone meanwhile). Secrets: pap:AD-12 chart existingSecret + keys discipline; inline debug creds are placeholder-only. Langflow is IN-PROCESS (no Langflow pod, no LANGFLOW_DATABASE_URL by hand). oc new-app --docker-image deprecated; use --image / ImageStreams. cookiecutter-django currently generates py3.14/Django 6/postgres 18-default — irrelevant, we deploy src/platform
- (decision) Absorption family recorded at dream level (same landing): asgi-multiplexer-monolith, langflow-django-plugin, db-gpt-django-plugin, enterprise-multi-agent-orchestration all self-closed 2026-08-14 into python-agent-platform — pointer specs authored, dreams stamped absorbed; residual carried: thread-pool sizing under load (asgi dream) noted for a future platform story
- (event) Self-validate pass 1 (coherence): PASS — 5 CAPs intent+success; constraints bend; non-goals in Dream carried (GKE/air-gap stay 12.2/12.3; no Jira; no prod OCP)
- (event) Self-validate pass 2 (preservation): PASS — intake walked phase-by-phase into CAPs/strikes with citations in companions; both research briefs' corrections all landed; operator's four decisions recorded verbatim; no silent drops
