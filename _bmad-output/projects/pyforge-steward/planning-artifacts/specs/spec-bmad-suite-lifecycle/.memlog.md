---
topic: The whole bmad-suite is wielded by named stations, kept current per release by one cadence runbook, and carried cutover-ready into python-foundry — adoption register, module wave, TEA full adoption, manticore studio, labs by consent, eval-quality pilot, release cadence, cutover readiness, shim retirement
updated: 2026-09-13T11:31
---

- (event) 2026-09-06 bmad-spec create, headless/express: input docs/dreams/bmad-suite-lifecycle.md (rich: catalog table with operator verdicts, 6.12 era tail at HEAD 35ddefbbc5, 73-item open register, cutover prerequisites P1-P17 / gaps G1-G11); project pyforge-steward (the Dream's owner); physical path _bmad-output/projects/pyforge-steward/planning-artifacts/specs/spec-bmad-suite-lifecycle/ (BMAD_ACTIVE_PROJECT pinned, never the symlink)
- (direction) Repo frontmatter convention (spec:/status:/owner-dream:/surface:/companions:/sources:/assumptions:/open_questions:) used instead of the template's id: line — the dream-chain and spec-surface detectors parse these keys; status ready because every operator decision is locked and the epics land in the same PR
- (decision) Operator 2026-09-06: shims are retired NOW (not at the v7 cut) — era-alignment's 'shims stay through v7' and the bmad-dev-auto harness discriminator constraints are superseded by memlog on that Spec; steward gains --no-shims; the same-version --no-shims apply is the first live exercise of core-upgrade CAP-6/7/8
- (decision) Operator 2026-09-06 adoption verdicts: utility-skills adopted (10 bmad-os-* routed by station), manticore adopted for Herald in a dedicated studio, labs-skills skill-by-skill under consent (mcp-builder, slides-generator, multi-repo-git-ops, release-please), eval-quality pilot unblocked (Story 45.2 blocked -> backlog; budget-ceilinged), bmad-builder provisioned BESIDE skf, TEA FULL adoption (repo generator bmad_tea_playwright.py retired behind an equivalence check); module-template scaffold-only; mybmad-dashboard out of the platform; bmad-dashboard opt-in dev surface
- (decision) Operator 2026-09-06: skf stays registered as a bmad-method custom module (bmad-help routing for 15 skills is worth CAP-7's cost); provisioning stays own-installer; release catalog pins skf v2.1.0 — verified npm 2.1.0 tarball src/ == GitHub tag v2.1.0 (337 files, diff empty; tag 1a64c486); the three [modules.skf] pins already sit in _bmad/custom/config.toml
- (decision) Operator 2026-09-06: full planning chain (Spec -> PRD -> epic-altitude spine -> hand-authored epics across eight stations -> sprint ledgers -> drain queue) in ONE planning PR; implementation begins next session with the era tail
- (capability) CAP-1 Adoption register governs wiring. intent: every one of the 13 suite members has a recorded verdict (wield|skip), wielding station, provisioning path, hazards and status in adoption-register.md, and a member's wiring changes only by changing its row. success: pipeline-truth's wired column agrees with the register for 13/13; the channel-product 'never wire-everything' constraint and one-front-door row-6 triage are recorded as superseded/closed by memlog
- (capability) CAP-2 Module wave lands by install class. intent: steward provision --module utility-skills|tea|bmb (and a CIS re-provision) place each module's skills in .claude/skills with manifest sections — never a hand copy. success: --list-modules reports the four installed; retired-ID guard + integrity meta-tests green; CIS SKILL.md files carry --project-root (15 lines / 10 files); the bmb cleanup-legacy hazard is proven never invoked by a test
- (capability) CAP-3 Station routing. intent: each adopted skill has exactly one wielding station, recorded in that station's persona skill (bmad-agent-<station>) and the AGENTS.md managed block. success: a routing table in adoption-register.md maps every adopted skill to one station and each named persona skill cites it; CLAUDE.md carries none of it
- (capability) CAP-4 TEA full adoption. intent: TEA's bmad-testarch-* workflows produce every station's planning-artifacts/test-architecture.md and tea-test-review runs as a Marshal review lens and a Warden advisory finding; the repo generator _bmad/scripts/bmad_tea_playwright.py, its two marshal meta-tests and its two pixi tasks are retired. success: 8/8 test-architecture docs regenerated by TEA with an equivalence check recorded against the generator's last output; tea-test-review --base origin/main --min-score N exits with a real code on a fixture PR; the generator and tests are deleted in the same story; Warden's gate verdict unchanged
- (capability) CAP-5 Manticore studio for Herald. intent: Herald renders a station video from a deck's speaker notes through manticore installed in a dedicated studio root outside this repo's _bmad/. success: the studio provisions via --custom-source with [modules.manticore] in the studio's own _bmad/custom/config.toml; one station .mp4 renders and is gitignored; this repo's _bmad/ is byte-identical before and after
- (capability) CAP-6 labs-skills by consent. intent: exactly the consented labs skills (mcp-builder -> Atlas, slides-generator -> Herald, multi-repo-git-ops -> Marshal, release-please -> Steward) are installed by name. success: each has a register row and lands via npx skills add bmad-labs/skills --skill <name>; no other labs skill is present in .claude/skills
- (capability) CAP-7 eval-quality pilot runs. intent: the designed twin-run contract measures the edge-case-hunter reviewer against a planted file:line defect under a per-trial budget ceiling. success: eval-quality-smoke, eval-quality-review-twin-run and eval-quality-review-replay exist as pixi tasks; one trial cites pkg/discount.py:17 on the mutated arm and not on the clean arm; none of the three joins detectors/detectors-ci (Story 45.2, spec-bmad-eval-quality CAP-2)
- (capability) CAP-8 Release cadence is one runbook. intent: every BMAD-METHOD release goes detect -> apply -> align -> refresh -> flip through release-cadence.md with named owners, and the @next prerelease is its optional rehearsal. success: the next release is processed with zero improvised steps against the runbook; the rehearsal recipe (throwaway worktree, --installer npx bmad-method@next, planted conflict, PATH without node) exists and has run once report-only
- (capability) CAP-9 BMAD-estate cutover readiness. intent: every prerequisite the python-foundry cutover assumes about _bmad/, .claude/skills/, _bmad-output/ and the loop homes is listed with an owner in cutover-readiness.md, the untracked gaps are relayed to the cutover chain by memlog, and the gate is every line green before Story 44.3 opens the foundry. success: the checklist reads green on P1-P17; G1-G11 each name a story; steward upgrade bmad-core preflight reports zero ungoverned local customizations
- (capability) CAP-10 Shim retirement (relay). intent: the 21 deprecated shims and every live caller are gone from this repo before the foundry opens. success: harness policy names bmad-build-auto, 8 loop homes re-rendered and validate clean, callers glossed, guard widened to the harness file, one --no-shims apply lands with installShims: false and 21 fewer skill dirs (era-alignment CAP-12 / marshal 30.5; core-upgrade CAP-9 / steward 14.9)
- (constraint) Provision by install class only: no hand copies into .claude/skills, never cleanup-legacy.py (would rmtree _bmad/core/config.yaml), never bmad-module-skill-forge uninstall (would delete every skill dir)
- (constraint) The manticore studio is a separate root; its upgrade ritual (wipe the studio's _bmad/ + _bmad-output/) never touches this repo's _bmad/
- (constraint) TEA and eval-quality verdicts are advisory (Warden) or review lenses (Marshal); Warden's gate stays the sole PR verdict
- (constraint) Customizations live in _bmad/custom/** or are re-applied by core-upgrade CAP-8; every in-place edit of an installer-owned file is under a spec surface (P7/P13)
- (constraint) Retired-name sweeps gloss shipped history (decks, docs/specs, pixi.toml comments); live docs and code lead with the live names (era-alignment convention kept)
- (constraint) Parallel agents address projects by physical path with BMAD_ACTIVE_PROJECT per invocation; sprint ledgers change only through sprint_plan.py generate + scoped sprint-ledger-sync; never bmad-switch, never an unscoped sync
- (constraint) Outward acts (upstream PRs, a new repo, budget-spending eval trials) need an explicit operator ask; the eval pilot's --max-budget-usd ceiling is per trial
- (note) Non-goals: bmad-method first-install (foundry's); suite conda-recipe refresh (CFE flow, Mason); mybmad-dashboard into the platform; module-template provisioning; the whole labs marketplace; upstream PRs (skf marketplace.json, sprint-plan key fix, skf uninstall bug); the cutover itself (Epic 44)
- (note) Decomposition targets (hand-authored, same PR): steward Story 14.9 (--no-shims), Epic 46 (CAP-1..8, 10 stories), Epic 47 (CAP-9, 5 stories); marshal Story 30.5 (harness + callers), Epic 31 (TEA replaces the generator, spec-surface widening, loop-home readiness, gh-triage/multi-repo routing); doctor Epic 20 (suite drift 7->13, render-HALT + frozen-path detectors, RCA routing, version-drift status); warden Epic 11; herald Epic 18; scribe Epic 7; atlas Epic 24; mason Epic 14 (eval-quality __win variant)
- (assumption) TEA's bmad-testarch-test-design / -framework outputs can cover what bmad_tea_playwright.py generates (epics + live test inventory -> test-architecture.md, deterministic, TBD = hard fail); the equivalence check in CAP-4 decides — if TEA cannot, the generator stays and CAP-4 narrows to the review lens
- (assumption) steward provision --module manticore cannot target a studio root today (CondaInstallBackend installs into the repo); CAP-5 uses the documented native path (npx bmad-method install --custom-source in the studio) until a --studio flag earns its keep
- (assumption) bmad-builder's five skills and skf's sixteen coexist without name collision (bmad-bmb-* / bmad-agent-builder vs skf-*); the collision check in steward provision decides at wire time
- (question) Where does Herald's manticore studio live — presentations/_studio/ (inside the repo, gitignored) or ~/pyforge-studio/ (outside)? CAP-5 needs one root; the studio's own _bmad/ must not sit under this repo's _bmad/
- (question) tea-test-review --min-score threshold for the Marshal lens / Warden advisory — start at upstream's example 80 or calibrate on the first ten PRs?
- (question) CAP-3 routing notes: persona skill AND the AGENTS.md block, or the block only (AGENTS.md is rewritten by skf-export at cutover — fnd:AD-6)?
- (question) eval-quality trial cost accounting: steward budget (Story 44.15 metering) or a local ledger under evals/ — carried from spec-bmad-eval-quality
- (event) Self-validate pass 1 (coherence): PASS — CAP-1..10 each carry intent+success; intents name outcomes (the steward verbs cited are the sanctioned interfaces, not prescriptions); 7 constraints each rule something out; 5 non-goals; success signal is demonstrable (pipeline-truth 13/13 + register agreement, --list-modules, a rendered mp4, a zero-improvisation release, checklist green, installShims false); IDs unique; three assumptions and four open questions recorded
- (event) Self-validate pass 2 (preservation): PASS — Dream § Grounding catalog + verdicts -> adoption-register.md; era tail + 73-item register -> open-items-register.md; cutover P1-P17 / G1-G11 -> cutover-readiness.md; § Decisions -> memlog decisions + SPEC constraints; § Whose job -> CAP-3 + register § 2 routing; § What it looks like -> CAP-1..10; § Constraints/Non-goals -> kernel. Wrapper-only content dropped: § Kinships (Dream-level cross-links) and § Realization log (process record)
- (event) Spec finalized 2026-09-06: status ready; companions adoption-register.md, release-cadence.md, cutover-readiness.md, open-items-register.md; next = bmad-prd create, bmad-architecture create (epic altitude), hand-authored epics (steward 46/47 + 14.9; marshal 30.5 + 31; doctor 20; warden 11; herald 18; scribe 7; atlas 24; mason 14), sprint ledgers, drain queue
- (decision) 2026-09-06 (spine reviewer gate, adversarial F-6): open question 1 closed — Herald's manticore studio root is OUTSIDE the repo, declared once per machine by PYFORGE_STUDIO_ROOT (default ~/pyforge-studio/), cited by the register cell and herald's CLI; mc-* never provisioned into the repo tree (lifecycle spine AD-3)
- (note) 2026-09-06 spine adopted by citation (architecture-bmad-suite-lifecycle-2026-09-06, AD-1..12): AD-9 fixes the provisioned-module roster to _bmad/custom/config.toml [modules.<code>]; suite:AD-10 makes cross-station prerequisites blocked rows + in-story checks (Deps tokens stripped); suite:AD-11/suite:AD-12 surface ownership and skf's single root; the @next rehearsal is core-upgrade Story 14.10 and 46.10 is the runbook verification only
- (event) SPEC re-derived 2026-09-06 (surgical): open question 1 removed (decided), constraint added for the studio declaration; CAP text unchanged
- (decision) 2026-09-06 (spine AD-2, adversarial F-4): open question 'routing home' closed — the persona skill is the one durable home; AGENTS.md carries one pointer line to adoption-register.md § 2 placed once by bmad-project-context, never per-skill lines; a meta-test (46.1) proves one persona per skill dir
- (decision) 2026-09-06 (spine AD-1, adversarial F-11): CAP-6's writer is the steward-wrapped, pinned steward provision --plugin labs --skill <name>; a raw npx skills add beside the conda member is a second writer and a defect
- (event) SPEC re-derived 2026-09-06 (surgical, second pass): CAP-3 and CAP-6 success wording aligned to AD-2/AD-1, CAP-9 reads P1–P18, routing-home open question removed (decided); companions adoption-register.md § 2/§ 3 and cutover-readiness.md P18 updated
- (correction) 2026-09-06 CAP-10 spec correction (found during marshal Story 30.5, operator-directed "spec correction" over "new story"): the Success bar naming "the harness policy names bmad-build-auto, the 8 loop homes re-render and validate clean... the guard is widened to the harness file" is FALSE, verified against the installed bmad_loop 0.11.1 package source: DevPolicy.skill is a permanent internal adapter discriminator (DEV_SKILLS = {"bmad-dev-auto"}) that must read "bmad-dev-auto" forever; the skill actually invoked is resolved from disk at runtime, independent of this field. SPEC.md's CAP-10 corrected in place. Matching corrections: era-alignment CAP-12 (pyforge-marshal), core-upgrade CAP-9 (this project, spec-bmad-method-core-upgrade) — the latter's own false refusal check (_shim_retirement_blockers, plus the --loops-home CLI flag) is removed from upgrade.py in the same session, via a follow-up bmad-build-auto review pass on Story 14.9's own spec.
- (event) Story 46.1 live 2026-09-07 re-verification: `steward suite pipeline-truth --json` re-run against adoption-register.md § 1's `Wired 2026-09-06` column — 13/13 packages agree at the base category (bmad-method present, bmad-loop provisionable, bmad-module-skill-forge present, bmad-creative-intelligence-suite wired, bmad-eval-quality runnable, bmad-method-test-architecture-enterprise unwired, bmad-builder unwired, bmad-utility-skills unwired, bmad-labs-skills documented, bmad-module-template n/a, bmad-manticore unwired, bmad-dashboard runnable, mybmad-dashboard runnable); zero disagreements. New meta-test `tests/meta/test_adoption_register.py` (5 tests) ships and passes under `pyforge-steward-test`, encoding AD-6 status-cell hygiene and AD-2 routing-home for every currently-provisioned § 2 row.
- (finding) Story 46.1 CIS routing-text gap (named, not silently fixed): grepped every `bmad-agent-*/SKILL.md` and `customize.toml` today — none mentions `bmad-cis` by name; the register's own "already routed (`herald-pitch`)" annotation for the CIS row does not correspond to any literal routing text anywhere in the repo outside the register itself. Per this story's own Never clause this is recorded rather than fabricated; the CIS row was already exempt from the single-station positive assertion anyway (multi-station: herald, scribe), so the meta-test only asserts CLAUDE.md never mentions it (true today) and does not assert a persona reference for CIS. Closing this gap (adding the actual routing text to bmad-agent-herald/bmad-agent-scribe) is Story 46.8's or a follow-up's surface, not this story's.
- (event) Story 46.2 (2026-09-07): AD-9 roster-writer migration lands -- provision.py's _record_module_manifest (shared by every CondaInstallBackend module: tea/cis/utility-skills/manticore) now writes [modules.<name>] into _bmad/custom/config.toml via targeted text editing instead of _bmad/config.yaml; module_install_states becomes a two-location read (new config.toml first, legacy config.yaml fallback for cis's still-unmigrated Story 46.8 entry), verified live against the real repo (addition-only diff, idempotent second run, --list-modules reports both cis and utility-skills installed). Added a live-tree DW-FU-15-3-4 assertion for utility-skills (discovered share/bmad-utility-skills/skills/ names == the 10 expected bmad-os-* names), closing the utility-skills half (CIS half already closed by 46.8). Added one routing pointer line each to bmad-agent-{herald,doctor,warden,scribe,marshal,steward}/SKILL.md naming their assigned bmad-os-* skill(s) per adoption-register.md § 2. adoption-register.md § 1 row 8 (bmad-utility-skills) flipped unwired -> wired. Story 46.1's two previously-failing meta-tests (test_skill_routing_matches_ad2_for_every_currently_provisioned_row, test_wired_column_agrees_with_live_pipeline_truth_for_every_row) now pass; full pyforge-steward-test suite green (1116 passed).
- (event) Story 46.3 (2026-09-07): TEA provisioning gaps closed -- (1) flattening: CondaInstallBackend gains flatten_nested_dirs/module_yaml_relative_path (opt-in, tea only); _installer_skill_names predicts the flattened leaf names for pre-install collision checks AND the post-install missing-skills check; _flatten_nested_skill_dirs moves each of the nine workflows/testarch/bmad-testarch-* leaves (+ bmad-teach-me-testing) up to .claude/skills/<name> and removes the empty testarch/ container, idempotent across re-provision (a re-run's installer rewrites a fresh nested copy the flatten step discards). (2) module.yaml-answers: _module_yaml_answers reuses _module_variable_defaults verbatim and overrides only test_artifacts to the UNRESOLVED "{output_folder}/planning-artifacts" template; _record_module_manifest merges the 14-variable answers dict into [modules.tea] alongside provisioned_by/installer/skills (Code Map's own '"ten variables"' count was stale -- the live module.yaml declares 14). (3) tea-test-review pixi task added to feature.local-recipes (default --base origin/main --min-score 80; never joins detectors/detectors-ci). Verified live against this repo's own real bmad-tea-install binary and share tree (not just fixtures): --module tea --json produces ten flattened skill dirs, testarch/ absent, idempotent across three consecutive runs, and re-running tea-test-review's own probe now resolves .claude/skills/bmad-testarch-test-review (previously not found). adoption-register.md Section 1 row 6 (bmad-method-test-architecture-enterprise) flipped unwired -> wired (mirrors 46.2's utility-skills precedent) to keep test_wired_column_agrees_with_live_pipeline_truth_for_every_row green. New/updated tests in test_provision_module_installers.py cover: fresh + idempotent flattening, the missing-skills check still catching a genuinely missing non-nested skill post-flatten, module.yaml answers with test_artifacts overridden and every other variable at its own default, and cis/utility-skills/manticore's own CondaInstallBackend entries carrying neither field. Full pyforge-steward-test suite green (1126 passed).
- (finding) Story 46.3 render_skill.py/TEA-workflow-format finding (recorded, not fixed -- this story's own Never clause): live-verified post-flatten -- `uv run _bmad/scripts/render_skill.py --project-root . --skill .claude/skills/bmad-testarch-test-design` still HALTs with "render entry is missing: .../workflow.md". TEA's nine workflow skills ship workflow.yaml + instructions.md, a structurally different third-party skill-authoring format render_skill.py was never built to read; teaching it a second format is materially larger than this story's S estimate and is not named as a task anywhere else in this epic. The epic's own literal acceptance clause ("so render_skill.py renders bmad-testarch-test-design on the first try") is not achievable as written without that larger change; Claude Code's own native skill-loading (SKILL.md + workflow.yaml + instructions.md directly) is the live hypothesis for how these skills are actually meant to be invoked, per this story's own Design Notes.
- (event) Story 46.4 (2026-09-07): bmb's own long-standing skill-copy gap closed -- SetupSkillBackend gains skills_source_dir (opt-in, bmb only, share/bmad-builder/skills); a new _setup_skill_names discovers its five immediate child directories (directory-only iterdir(), mirroring _installer_skill_names's own filter, so the sibling module.yaml/module-help.csv files at that level are excluded for free); _provision_setup_skill gates the copy on the SAME _check_skill_name_collisions every CondaInstallBackend module already uses, then _copy_setup_skill_dirs lands each one under .claude/skills/<name> via rmtree-then-copytree (idempotent overwrite-in-place on re-provision). A plain file operation, never a subprocess call -- the pre-existing cleanup-legacy.py/--legacy-dir argv guard test needed zero modification and still passes. _bmad/config.yaml still gains bmb via the unchanged merge-config.py mechanism; bmb remains outside the AD-9 _bmad/custom/config.toml migration. Verified live against this repo's own real bmad-builder share tree (not just fixtures): --module bmb --json lands all five skills (bmad-bmb-setup, bmad-agent-builder, bmad-workflow-builder, bmad-module-builder, bmad-eval-runner) under .claude/skills/, zero collision against the live 16 skf-* dirs, idempotent across two consecutive runs, _bmad/config.yaml gains bmb. New tests in test_provision_module.py cover: fresh copy of all five skills, idempotent re-provision (a stray leftover file inside a landed skill proves the second run replaces rather than merges), a foreign-collision refusal (nothing copied, nothing written), and CLI --json/text reporting of skills_copied. adoption-register.md § 1 row 7 (bmad-builder) flipped unwired -> wired (mirrors 46.2/46.3 precedent); added the two-pipeline routing text (builder skills for persona/workflow authoring, skf-* for domain-skill compilation) to bmad-agent-steward/SKILL.md's existing "Utility skill routing (AD-2)" section, closing test_skill_routing_matches_ad2_for_every_currently_provisioned_row and test_wired_column_agrees_with_live_pipeline_truth_for_every_row (both went red the moment the live provision ran, both green again after these two doc edits). No pixi.toml change this story -- the blanket-glob spec-surface re-derivation step is skipped per the story's own instruction. Full pyforge-steward-test suite green (1135 passed).
- (event) Story 46.5 (2026-09-07): the plugin-path install class's first real wiring. New `PluginBackend`/`_SUPPORTED_PLUGINS` registry (deliberately separate from `_SUPPORTED_MODULES`/`ModuleBackend`, since every module backend installs its whole declared skill set atomically while the plugin path installs exactly one named skill per call) backs a new `steward provision --plugin labs --skill <name>` CLI surface; `provision_plugin_skill` reuses `_check_skill_name_collisions` and `_copy_setup_skill_dirs` verbatim (never `npx skills add` -- AD-1), refuses any name outside the fixed four-name 2026-09-06 consent list (`mcp-builder`, `slides-generator`, `multi-repo-git-ops`, `release-please`) even though the package's real 22-skill share tree makes the other 18 technically copyable, and accumulates every installed name into `[modules.labs]` via a new `_module_toml_skills` reader (never a wholesale roster replacement that would wipe a previously-installed sibling skill). Verified live against this repo's own real bmad-labs-skills share tree: all four consented skills provisioned for real (`.claude/skills/{mcp-builder,slides-generator,multi-repo-git-ops,release-please}`, byte-identical to the pixi share tree via `diff -rq`), `_bmad/custom/config.toml` gained `[modules.labs]` with all four names. Independent 4-reviewer pass found and fixed a real bug matching an already-precedented severity class: the original draft copied the skill directory BEFORE recording the roster, reintroducing Story 46.4's own "copy-before-manifest-write self-locks a retry" bug (empirically reproduced) -- fixed by reordering to record-then-copy, mirroring 46.4's fix direction exactly. Also patched: the reused collision-check message called `labs` a "module" (added an opt-in `kind` parameter, default unchanged for every other caller); the AD-2 routing meta-test's carve-out for the three not-yet-routed sibling skills (mcp-builder -> atlas 24.1, slides-generator -> herald 18.3, multi-repo-git-ops -> marshal 31.6) was skipping the exclusivity half of the check too, not just the "owning station mentions it" half (narrowed to skip only the latter); `_run_plugin`'s `--plugin`/`--skill` validation order swapped so an unregistered plugin is named even without `--skill`. `bmad-agent-steward` routes `release-please` only -- the other three skills' directories now exist on disk but are routed by their own owning stations' not-yet-landed stories, per the epic's own "station-side halves are their own stories" boundary. `adoption-register.md` row 10's Wired cell deliberately stays `documented` (not `wired`) -- verified against the live `suite.py::probe_wired` code that the plugin-path class's probe only checks playbook documentation, never `.claude/skills` state; Story 46.9 owns fixing that probe. `install-class-playbook.md`'s labs row + numbered bullet now cite the real wrapper command and consent list. `suite.py` untouched. New `test_provision_plugin.py` (24 tests) + 2 new tests in `test_adoption_register.py`. Full pyforge-steward-test suite green (1161 passed).
- (event) Story 46.9 (2026-09-07): two independent pipeline-truth bugs fixed. (1) The installer-tree class's `installed` stage now reads `read_applied_core_version` (`_bmad/_config/manifest.yaml`'s `installation.version`) FIRST, falling back to the existing conda-meta scan only when the manifest is absent; `_installer_tree_installed_stage` names a disagreement via a stable `detail` sentinel (`"core-applied-env-drift"`) that `name_drifts` matches into a new `core_applied_env_drift` drift tag -- `build_package_truth` branches on `install_class == INSTALL_CLASS_INSTALLER_TREE` to call it, every other class's computation byte-for-byte unchanged. Verified live: this repo's own manifest (6.12.0) and conda-meta (6.12.0) currently agree, so the fix is a no-op today -- the exact 2026-09-05 divergent shape (env 6.12.0 / `_bmad/` 6.11.0) is replayed as a fixture instead. (2) `probe_wired`'s per-class predicates now observe the provisioning path the register actually names: `SuitePackageDef` gains `wire_skill_names_all` (ALL-of, vs. `wire_skill_names`'s ANY) and `module_code` (the `provision.py` `_SUPPORTED_MODULES` key); `_module_census_hit` checks `wire_skill_names_all` first; the plugin-path branch (labs) checks it before falling back to the playbook-text-only `_plugin_path_documented`; a new `INSTALL_CLASS_STUDIO_MODULE` class (manticore, AD-3) checks `$PYFORGE_STUDIO_ROOT` (default `~/pyforge-studio`) for its own `_bmad/` + a `mc-*` skill, entirely outside this repo, replacing the old generic-fallback in-repo `mc-*` census AD-3 explicitly retired; the bottom generic fallback reads `module_install_states` (imported read-only from `.provision`) when `module_code` is set (tea/cis/utility-skills), instead of re-deriving an imprecise skills/_bmad census. `bmad-builder` drops its incomplete 3-of-5 `wire_skill_prefixes` for the explicit five-name `wire_skill_names_all` allowlist. Verified live against this repo's own real state: labs/bmb/tea/cis/utility-skills all now report `wired` (Story 46.5's four consented labs skills and Story 46.4's five bmb skills are genuinely present; tea/utility-skills are in `_bmad/custom/config.toml`, cis in the legacy `_bmad/config.yaml` fallback); manticore reports `unwired` (honest -- Story 46.6 halted blocked 2026-09-07, `~/pyforge-studio` exists but is empty, verified live and left untouched). A real, live (non-baseline) `steward suite pipeline-truth --json` run confirms only row 10 (`bmad-labs-skills`) changes category (`documented` -> `wired`); every other row's live value agrees with its current register cell -- `adoption-register.md` row 10 updated accordingly; `install-matrix.md`'s installed-stage caveat sentence removed (bug #1 is now fixed, not a documented limitation). Found, and fixed as a necessarily-in-scope deviation from this story's own stated Surface line (recorded in Implementation Notes below): `fresh_clone.py`'s `_EXPECTED_WIRED["bmad-labs-skills"]` and its own `test_fresh_clone_class_path.py::test_live_checkout_proves_six_class_outcomes` assertion both hardcoded the same stale `"documented"` expectation this story corrects -- both updated to `"wired"`, matching the live repo's own real, honest state. New tests: `test_suite_pipeline_truth.py` (`read_applied_core_version`, `_installer_tree_installed_stage` -- manifest-only, disagreement/drift, manifest-absent fallback, live no-op, `build_package_truth` branch); `test_suite_wired_class_predicates.py` (labs/bmb all-five/bmb-partial/tea-cis-utility-skills roster/manticore absent-empty-wired, plus one live-repo confirmation test covering all five newly-wired members and manticore's honest unwired). The three explicitly-protected pre-existing tests (`test_labs_skill_census_alone_is_not_wired`, `test_labs_plugin_path_documented_from_playbook`, `test_wired_column_agrees_with_live_pipeline_truth_for_every_row`) pass unmodified; `test_live_repo_names_six_and_template_is_n_a` (pre-existing, NOT in that protected set) needed a one-line carve-out for labs' now-correct `wired` outcome. Independent 4-reviewer pass found and fixed a HIGH-severity bug (found independently by two reviewers, empirically reproduced against this repo's own real `_bmad/config.yaml`): `_module_census_hit`'s `wire_skill_names_all` check for bmb was "checked first," not exclusive -- a miss on the five-name census fell through to bmb's own auxiliary `wire_bmad_config_keys=("bmb",)` check, which independently reports a hit from the `bmb:` key alone regardless of whether all five dirs actually landed, silently defeating the fix's own stated guarantee; made `wire_skill_names_all` the sole, exclusive signal when declared (returns immediately whether hit or miss). Also fixed a medium finding: `PYFORGE_STUDIO_ROOT=""` (set but empty) silently fell back to the process's cwd instead of the documented default (`os.environ.get(key, default)`'s classic "default only applies when absent" gotcha) -- switched to `os.environ.get(key) or default`. One deferred (low, a wasted `iterdir()` call, no behavioral effect) plus one pre-existing lint issue (an unnecessary quoted forward-reference annotation on the new code, fixed to match this file's own ruff baseline). Full pyforge-steward-test suite green (1180 passed).
- (event) Story 47.1 (2026-09-07): re-ran the live, report-only `steward upgrade bmad-core --target 6.12.0 --json` pre-flight against this repo's own state (exit 0; git status --short identical before/after). Found 9 P7-evidence files today (8 local_customizations + 1 locally_modified `_bmad/scripts/resolve_config.py`), up from the epic's 2026-09-06 assumption of 7 -- one additional skill-file edit landed 2026-09-06->09-07. Cross-referenced all 9 against every project's every SPEC.md surface: field (full-repo grep): 4 governed (compile-epic-context.md under spec-marshal-token-economy; step-01-clarify-and-route.md, step-04-review.md, spec-template.md under spec-marshal-single-story-dispatch), 5 ungoverned (brain-methods.csv, generate-tracking.md, sprint_plan.py, test_sprint_plan.py, resolve_config.py) -- the ungoverned count matches P13's pre-existing '5 ungoverned' claim exactly even as the total pool grew, recorded as two independent facts. cutover-readiness.md P7/P13 cells updated with the dated, corrected findings plus a new '## Re-run' section giving the copy-pasteable command (--installed-package-root omitted, relying on upgrade.py's own documented default_installed_package_root auto-glob, confirmed identical output). Governing the 5 ungoverned files remains marshal Story 31.4's job, untouched here. Review pass found the "5 ungoverned" figure needed a caveat: `bmad/adoption-readiness-2026-09-06-marshal-r1` (commit `6ba6bd9eb6`, "Story 31.4," unmerged at this pass) already governs 3 of those 5 (`generate-tracking.md`, `sprint_plan.py`, `test_sprint_plan.py`) -- once merged, a re-run correctly reports 2 of 9 ungoverned, not 5; the checklist's P13 cell and Re-run note now say so explicitly rather than implying a settled fact. Also simplified the documented re-run command after finding `--package-root` isn't actually needed for the P7 finding at all (confirmed live) and the earlier draft's hardcoded build-hash path wasn't portable -- the command is now the bare `pyforge steward upgrade bmad-core --target 6.12.0 --json`, no path flags.
- (event) Story 46.6 (2026-09-07, third attempt — succeeded): the sanctioned native command completed for real, non-interactively, after two prior attempts blocked on an unanswerable `Installation directory:` prompt. The missing piece both prior attempts lacked was `--directory $PYFORGE_STUDIO_ROOT` -- `--yes --tools <48 ids>` alone (attempt 2) still hit the identical prompt; adding `--directory` on top (attempt 3, this run) let `npx bmad-method install --directory $PYFORGE_STUDIO_ROOT --custom-source https://github.com/bmad-code-org/bmad-manticore --yes --tools claude-code,codex,cursor,github-copilot,adal,antigravity-cli,auggie,goose,cline,codebuddy,codewhale,command-code,crush,droid,firebender,gemini,antigravity,grok,hermes,bob,iflow,junie,kilo,kimi-code,kiro,kode,mistral-vibe,mux,neovate,ona,openclaw,opencode,openhands,pi,pochi,polytoken,qoder,qwen,replit,roo,rovo-dev,cortex,amp,trae,warp,windsurf,zcode,zencoder` complete cleanly with cwd irrelevant -- all 48 tool integrations reported `✓ <tool> (23 skills -> <target-dir>)`, ending `Installed to: /home/rxm7706/pyforge-studio/_bmad`. Checksum bracket (this repo's `_bmad/` + `_bmad-output/`, `find ... -type f | sort | xargs sha256sum | sha256sum`, taken immediately before/after the one subprocess call, no other edit to this repo in between) matched byte-identical (`d6619ecbb6b081f9b4ad2e5cd06fe02df525730e547700e10ca377aa762cecea`, both) -- meaningfully this time, since the install genuinely wrote a large tree (2 modules x 48 tools x 23 skills) that stayed entirely confined to the studio root; this repo's own `.claude/skills/` stayed at zero `mc-*` dirs. **Real observed shape diverges from the epic's literal phrasing:** no `[modules.manticore]` TOML table exists anywhere in the studio's `_bmad/` -- `_bmad/custom/config.toml` is confirmed untouched (the installer's own documented behavior); the actual "module installed" record is `_bmad/_config/manifest.yaml`'s `modules:` array (`{name: manticore, version: main, source: custom, repoUrl: ..., channel: next}`), plus `_bmad/manticore/config.yaml` (per-module config) and `_bmad/config.toml`'s new `[agents.mc-agent]` section. The fifteen `mc-*` skills (`mc-agent`, `mc-assets`, `mc-audio`, `mc-beats`, `mc-braindump`, `mc-cut`, `mc-graphics`, `mc-new`, `mc-outline`, `mc-package`, `mc-pipeline`, `mc-retro`, `mc-script`, `mc-setup`, `mc-stream-pack`) landed exactly as named, confirmed under `$PYFORGE_STUDIO_ROOT/.claude/skills/mc-*` (and mirrored, alongside 8 shared BMad Core skills = 23 total, into all 48 tool-specific directories). `which ffmpeg` confirmed absent on this machine (uv/node/git all resolved) -- documented, not installed, per the story's own either/or AC. **Register correction (not a silent rewrite):** row 9's `Wired` cell flipped `unwired` -> `wired` -- the spec's own boundary text assumed it would stay `unwired` mirroring Story 46.5's precedent, but that assumption predated this run's actual success; live-verified via `steward suite pipeline-truth --json` (`wired.value == "wired"`, detail `"studio module: /home/rxm7706/pyforge-studio/_bmad + mc-* skill present"`) against Story 46.9's own already-landed `INSTALL_CLASS_STUDIO_MODULE` probe, and confirmed necessary: `test_wired_column_agrees_with_live_pipeline_truth_for_every_row` was RED before this fix, green after. Provisioning-path cell also corrected to name the two required flags (`--directory`, `--yes --tools <ids>`), which the pre-existing cell omitted. `install-class-playbook.md` corrected: title "the six" -> "the eight that are not CAP-3 modules" (accounting for both the already-present 45.1 eval-quality row and this story's new manticore row), intro sentence "CAP-3 five" -> "CAP-3 four ... manticore graduated to the studio row below (46.6)", new table row + new numbered Fresh-clone bullet (item 7) added citing the real working command; the pre-existing, unrelated `bmad-eval-quality` omission from that numbered list is untouched (a different story's gap, per this story's own boundary). New `docs/reference/manticore-studio.md` written naming the env var + default, the prerequisite check (ffmpeg absence), the exact working command with its flag-discovery history, the real observed config/skill shape, and the checksum proof. `_bmad/custom/config.user.toml` confirmed absent in this worktree (Task 6, nothing to remove); `provision.py`/`suite.py` untouched. Two live-truth-driven test-fixture fixes needed to keep the suite green: `install-class-playbook.md`'s new Fresh-clone bullet had wrapped its cited native command across two lines inside one backtick span, which `fresh_clone.py`'s citation-lock regex flagged as an invented fragment -- fixed by citing the matrix's own exact single-line fragment and moving the extra flags into a separate, non-scanned backtick span; `test_suite_wired_class_predicates.py::test_live_repo_wired_predicates_ad9_and_five_name_and_studio` had hardcoded manticore's expectation to `unwired` with a docstring explicitly tied to "Story 46.6's own blocked, empty-studio state" -- updated to `wired` (mirrors 46.9's own precedent fixing the same class of staleness for labs). `pyforge-steward-test` re-run green afterward: 1180 passed, 0 failed. Review pass found two real problems with the same root cause: row 9's new "wired" register cell and a new unit-test assertion both stated a fact true only on THIS machine (the studio lives outside git by design, AD-3) as if it were portable like every other "wired" row -- a fresh clone or any other machine would honestly report `unwired` until the same install runs there. Fixed both: the register cell now reads "wired (this machine, 2026-09-07 -- ... NOT portable like the other rows above ...)" while keeping the literal token "wired" first so the existing agreement meta-test's own parsing stays intact; the test now derives its expected manticore value from the same live filesystem check `probe_wired` itself performs, instead of a hardcoded `"wired"`, so it is correct on any machine. Also recorded, not fixed: the studio module tracks `main`/`next` (unpinned, floating) with no lockfile, unlike every other custom module in this register -- an open reproducibility risk added to row 9's Hazards cell.
- (event) Story 47.2 (2026-09-07): `skf-export-skill` actually run headless, end to end, in a throwaway `git worktree add <scratchpad>/skf-export-scratch HEAD` copy of this repo (never in this worktree; real `_bmad/skf/config.yaml` confirmed untouched throughout via `git status`/`git diff`, worktree removed after) -- Task 1-5 of spec-47-2 executed as an acting agent walking the real SKILL.md stages (Load Skill -> Package -> Generate Snippet -> Update Context -> Token Report -> Summary -> Workflow Health Check), not simulated. Config's `skills_output_folder` flipped `.claude/skills` -> `skills/stations`; `snippet_skill_root_override` left at `.claude/skills/` per the story's own instruction. Two runs: Run 1 (skill referenced at its existing OLD `.claude/skills/pyforge-herald` location, config already flipped) HALTs for real at `load-skill.md` step 1 SS2 -- exit 3 `resolution-failure` -- because SKF's read-side resolution (manifest -> active symlink -> flat path) is scoped entirely to the CURRENT `skills_output_folder`, with zero fallback to a prior root; empirically confirmed via `test -f`/`test -L` against all three candidate paths under the new root, all absent. This is a genuine, previously-unnamed gap: flipping the suite:AD-12 declaring key alone does not migrate or even discover already-exported packages -- the on-disk tree must move (or be freshly forged) at the new root first. Run 2 (the same skill staged flat at the new root, exercising SKF's own documented flat-to-versioned auto-migration from `knowledge/version-paths.md`) completed for real end-to-end: `skills/stations/pyforge-herald/0.1.0/pyforge-herald/SKILL.md` + `skills/stations/.export-manifest.json` both land exactly as the target layout assumes (AD-5's root-acceptance half holds); the real `skf-validate-output.py`/`skf-count-tokens.py`/`skf-rebuild-managed-sections.py`/`skf-manifest-ops.py` helpers were invoked for real, not narrated. The fnd:AD-19 link-generation question is now empirically settled, not re-asserted from prose: `git diff --stat -- .claude/skills/` in the scratch worktree came back byte-empty after the full run -- nothing under `.claude/skills/` was created, touched, or removed by any step; CLAUDE.md/AGENTS.md's managed sections gained only a timestamp bump, and the re-exported skill's `root:` pointer still reads the stale `.claude/skills/pyforge-herald/` (override correctly left untouched, so nothing forced a rewrite) even though the package now lives at `skills/stations/pyforge-herald/...` -- a live instance of the exact adapter gap the cutover's own target-tree diagram assumes away. Separately, P10's declaring-key half re-verified against live code (`upgrade.py::_restore_custom_module_configs`, `path.write_bytes(before)`): CAP-7 today is a pre-apply byte snapshot/restore of `_bmad/skf/config.yaml` itself, entirely independent of `_bmad/custom/config.toml [modules.skf].skills_output_folder` (confirmed present, still `.claude/skills` today) -- the two files have no derivation link yet; a gap, not built, recorded rather than fixed (zero code changes this story). `cutover-readiness.md` G5/P10 updated with the full, dated findings; a mirroring finding entry lands in `spec-python-foundry-cutover`'s own `.memlog.md` naming the link-generation gap as that project's to close. Health-check step (terminal, real) surfaced three genuine self-improvement findings (the Run-1 halt as a `gap`; the stale-root-pointer-after-config-flip interaction as a `gap`; the User Review Gate's own missing headless-default annotation, unlike every other gate in this workflow, as a `friction`) -- deliberately NOT auto-submitted live to `armelhbobdad/bmad-module-skill-forge` (no explicit operator consent for an external network/repo action, and the gate's own headless default is undocumented) -- noted here instead for a human to decide. `pyforge-steward-test` re-run clean afterward (no source changed this story).
- (event) Story 47.3 (2026-09-07): two small, mechanical gaps closed (G1, G8). `marshal-policy.toml`'s `[epic_surfaces] "44"` array gains `"_bmad/**"` (placed beside the other top-level repo-root globs `.gitignore`/`pixi.toml`/`pixi.lock`/`environment.yaml`) -- suite:AD-12's Story 44.5 move of `_bmad/`, `_bmad-output/projects/`, and `docs/dreams/` "as one unit" would otherwise trip `MRS-GATE-007`'s hard scope refusal the moment it runs; verified live before editing that `_bmad/**` was genuinely absent from the "44" array and that every other epic's array is untouched. `_bmad-output/PROJECTS.md` gains a new "## Cutover target (foundry, suite:AD-12)" subsection, inserted after § Adding a new project and before § Reading another project's artifacts: states that the marker file and both planning symlinks are per-working-tree state, recreated fresh by `bmad-switch`/`bmad-loop-worktree`, never copied -- mirroring fnd:AD-19's own generated-per-machine-links convention for `.claude/skills`/`.cursor/skills` (the cutover's target-tree diagram, read directly) -- and explicitly states this is a future state gated on the fnd:AD-17 flip, not true today. No code change; no other section of PROJECTS.md touched. `cutover-readiness.md` G1 and G8 Relay cells updated with dated RESOLVED notes (mirroring G5's own established resolution-note convention in the same table); no other P or G row touched. `sprint-status-ledger.yaml` `47-3-...` flipped `backlog` -> `done`. `pyforge-steward-test` re-run clean afterward (no source changed this story).
- (event) Story 47.4 (2026-09-07): the 2026-09-06 `(decision)` entry above (line 81) rendered into the actual spine. `ARCHITECTURE-SPINE.md` § Stack gains a 9th row, `bmad-* suite floor`, appended after the existing `GitHub Actions` row -- content transcribed verbatim from this memlog's own decision text and from `epics.md`'s Story 47.4 acceptance clause (both already fully specified the package list, so nothing was re-decided or re-verified here): `bmad-method >=6.12.0, bmad-loop >=0.11.1, bmad-module-skill-forge >=2.1.0 (linux-64 only), bmad-creative-intelligence-suite, bmad-method-test-architecture-enterprise, bmad-eval-quality, bmad-utility-skills, bmad-builder` -- win-64 (44.11) excludes skf and eval-quality. No AD id added, changed, or renumbered; no other row, the mermaid diagram, or the Structural Seed block touched. `cutover-readiness.md` G9's Relay cell gained a dated RESOLVED note (mirroring G1/G5/G8's own established convention in the same table); no other P or G row touched. `sprint-status-ledger.yaml` `47-4-...` flipped `backlog` -> `done`. `pyforge-steward-test` re-run clean afterward (no source changed this story).
- (event) Story 31.6 (2026-09-07, pyforge-marshal) closed CAP-3's marshal-side routing for both bmad-os-gh-triage and multi-repo-git-ops in .claude/skills/bmad-agent-marshal/SKILL.md; removed the now-landed multi-repo-git-ops entry from pyforge-steward's own _ROUTING_STORY_NOT_YET_LANDED test carve-out in test_adoption_register.py (only mcp-builder/slides-generator remain not-yet-landed); and answered DW-HYGIENE-2026-09-05-1's open coverage question in pyforge-marshal's deferred-work-ledger.md (answer: no, multi-repo-git-ops does not cover that wish -- no .gitmodules in this repo).
- (note) doctor Story 20.3 landed frozen-path-changed (spec-20-3-frozen-path-changed-exists.md): sources/frozen_path.py, Source frozen-path-changed, scope=repo, subject_station=steward, owning_station=doctor -- reads docs/foundry/manifest.{yaml,yml,json} (absent pre-cutover), diffs origin/main..HEAD via cli_bridge.run_git, FAILs one Finding per (capability, path) pair when a rebuilding/moving capability's frozen path is touched. Wired into detectors-ci + pixi frozen-path-changed-check; reports ok (no capability ledger found, pre-cutover) against today's real state; full pyforge-doctor-test suite green (1434 passed, 1 skipped). A later steward pass can flip cutover-readiness.md's G11 row from 'doctor 20.3 -- never built' to landed.
- (note) 2026-09-06 relay from marshal Story 31.5 (`spec-loop-home-fleet-refresh`): G10 / `cutover-readiness.md` P16's "readiness undefined" resolved — the readiness definition for what a re-provisioned loop home must contain after the python-foundry cutover flip (remote points at the foundry repo; `pyforge.toml`'s `[project] name` matches the foundry repo, not `local-recipes`; `marshal homes --json`'s `desynced`/`active_project` fields agree; the rendered `.bmad-loop/policy.toml`'s `[dev] skill` field reads `"bmad-dev-auto"` — the permanent internal discriminator, per this memlog's own CAP-10 spec correction above, not the name of whichever harness skill is actually invoked; refreshed `_bmad-output` planning/implementation relays; no in-flight run) now lives in `_bmad-output/projects/pyforge-marshal/planning-artifacts/specs/spec-loop-home-fleet-refresh/loop-home-cutover-readiness.md`. Every item names an existing proving check (`marshal homes --json`, `bmad-loop validate`, manual `git remote -v` / `cat pyforge.toml` / `readlink -f` inspection, `marshal status` / `bmad-loop status`) — no new detector — and states the runner as steward Story 44.12, attended, per P16. The doc also reconciles `DW-CC-2026-09-04-1`'s "retired" wording (one line-item within its 268-worktree aggregate entry) with P16's "re-provisioned" wording as the same eight homes' old-vs-new state around the `pyforge.cutover_root` flag flip (epics.md Story 44.12/44.5), without asserting a specific low-level mechanism beyond what epics.md states. `cutover-readiness.md` itself is not edited by this note; this note gives steward what it needs to update G10/P16's State column, by whatever means steward's own process uses for that companion doc.
- (event by claude) status: ready -> in-progress -- Epic 46 is 9/10 and Epic 47 5/5 `done`, and all eight station relays are `done` (doctor 20.1-20.4, marshal 30.5, warden 11.1-11.2, herald 18.1-18.3, scribe 7.1, atlas 24.1, mason 14.1, steward 14.9), so `ready` is stale. Open: 46.10 (CAP-8 rehearsal), marshal 31.4/31.5 (fleet-readiness-decision-batch-2026-09-09 Class B row stA).
- (decision by claude) Open question '`tea-test-review --min-score` for the Marshal lens / Warden advisory -- upstream's example 80, or calibrate on the first ten PRs' closed: 80 STANDS, it already shipped (pixi.toml:884, Story 46.3 `done`). The residue is a measurement, not a decision: the task explicitly never joins detectors/detectors-ci, so the threshold gates nothing today. If wanted, record the score of the next ten PRs as an AC on a marshal story, then revisit (fleet-readiness-decision-batch-2026-09-09 row stA-B3).
- (decision by claude) Open question 'eval-quality trial cost accounting: steward budget (Story 44.15 metering) or a local ledger under evals/' is DELETED from this Spec as a verbatim duplicate -- one decision, one owner. It is answered and owned on spec-bmad-eval-quality (local ledger under evals/, now; fold into 44.15 only if that story ships); cite that Spec rather than restating it here (fleet-readiness-decision-batch-2026-09-09 row stA-B1).
- (note by claude) Companion cutover-readiness.md (CAP-9) re-derived 2026-09-09 against live code: P1 130 SPEC.md / 153 memlogs (verdict 'not done' stands, 44.13 still backlog); P2 exception cite moves to AGENTS.md:19; P3 flips from 'satisfied (11/11)' to NOT satisfied -- 8 of 9 spines have a sibling .memlog.md, warden's architecture spine has none, which blocks Story 44.13 as widened by 47.5 (G6); P7 pool is 11 against the now-applied 6.12.0 baseline (10 local_customizations + resolve_config.py); P13 is 4 of 11 ungoverned; G4/G7/G11 marked RESOLVED (producers marshal 30.2, doctor 20.2, doctor 20.3 all done), G10 stays open on marshal 31.5; P11/P12 re-confirmed green live; P16 still correctly undefined. Companion edited in place -- it is a hand-maintained tracked companion, not a bmad-spec-rendered one (fleet-readiness-decision-batch-2026-09-09 Class B row stA / steward-A section C5).
- (event) Self-validate Pass 1 (coherence) PASS on the 2026-09-09 re-derive: all CAP ids preserved, 4 companions and 3 assumptions intact, status ready -> in-progress with its ledger evidence, open_questions now empty
- (event) Self-validate Pass 2 (preservation) PASS: the min-score-80 closure with its measurement residue, the deleted duplicate cost question pointing at spec-bmad-eval-quality as sole owner, and the full cutover-readiness re-derivation (P1/P2/P3-flips-to-NOT-satisfied/P7/P13, G4-G7-G11 resolved, G10 open, P11/P12 green, P16 undefined) all landed; the companion itself stays hand-maintained and was not re-rendered
- (event) 2026-09-10 Story 46.10 (CAP-8 runbook verification): `release-cadence.md` now carries a **Verified pass** block under each of the nine numbered steps (owner, command, exit, evidence from the live 6.12.0 apply commits `ab52117795`/`4fa185be56`/`e7b5d6d05e` and era-tail stories 30.x/46.x) plus the fixture `@next` rehearsal (Story 14.10, `test_upgrade_next_rehearsal.py`, exit 0). § The `@next` rehearsal argv fence matches `NEXT_REHEARSAL_ARGV` token-for-token; meta test `test_release_cadence_runbook.py` gates the nine blocks, argv parity, disclaimer, and no-improvised wording. Rehearsal explicitly disclaimed as live proof for `spec-bmad-method-core-upgrade` shipped flip (AD-7).
- (decision) 2026-09-13 operator: flip the 2026-09-06 skip pair. bmad-module-template wields as authoring tool only beside bmad-builder — never steward provision --module into .claude/skills/. mybmad-dashboard wields as isolated opt-in sidecar via the mybmad launcher — never /console/, never platform OIDC, never a PR gate. Why not the console: mybmad has own Postgres + own auth; /console/ is django-pyforge + Keycloak/OIDC + DATABASE_URL; retired-console-check forbids a second Guildhall-class face; bmad-dashboard (row 12) already holds the same never-console rule. Story 52.1 owns the register rows and the bmad-spec re-derive of Non-goals. Do not hand-edit SPEC.md.
