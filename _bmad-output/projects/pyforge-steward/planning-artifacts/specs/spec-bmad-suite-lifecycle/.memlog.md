---
topic: The whole bmad-suite is wielded by named stations, kept current per release by one cadence runbook, and carried cutover-ready into python-foundry — adoption register, module wave, TEA full adoption, manticore studio, labs by consent, eval-quality pilot, release cadence, cutover readiness, shim retirement
updated: 2026-09-06T11:44
---

- (event) 2026-09-06 bmad-spec create, headless/express: input docs/dreams/bmad-suite-lifecycle.md (rich: catalog table with operator verdicts, 6.12 era tail at HEAD 35ddefbbc5, 73-item open register, cutover prerequisites P1-P17 / gaps G1-G11); project pyforge-steward (the Dream's owner); physical path _bmad-output/projects/pyforge-steward/planning-artifacts/specs/spec-bmad-suite-lifecycle/ (BMAD_ACTIVE_PROJECT pinned, never the symlink)
- (direction) Repo frontmatter convention (spec:/status:/owner-dream:/surface:/companions:/sources:/assumptions:/open_questions:) used instead of the template's id: line — the dream-chain and spec-surface detectors parse these keys; status ready because every operator decision is locked and the epics land in the same PR
- (decision) Operator 2026-09-06: shims are retired NOW (not at the v7 cut) — era-alignment's 'shims stay through v7' and the bmad-dev-auto harness discriminator constraints are superseded by memlog on that Spec; steward gains --no-shims; the same-version --no-shims apply is the first live exercise of core-upgrade CAP-6/7/8
- (decision) Operator 2026-09-06 adoption verdicts: utility-skills adopted (10 bmad-os-* routed by station), manticore adopted for Herald in a dedicated studio, labs-skills skill-by-skill under consent (mcp-builder, slides-generator, multi-repo-git-ops, release-please), eval-quality pilot unblocked (Story 45.2 blocked -> backlog; budget-ceilinged), bmad-builder provisioned BESIDE skf, TEA FULL adoption (repo generator bmad_tea_playwright.py retired behind an equivalence check); module-template scaffold-only; mybmad-dashboard out of the platform; bmad-dashboard opt-in dev surface
- (decision) Operator 2026-09-06: skf stays registered as a bmad-method custom module (bmad-help routing for 15 skills is worth CAP-7's cost); provisioning stays own-installer; release catalog pins skf v2.1.0 — verified npm 2.1.0 tarball src/ == GitHub tag v2.1.0 (337 files, diff empty; tag 1a64c486); the three [modules.skf] pins already sit in _bmad/custom/config.toml
- (decision) Operator 2026-09-06: full planning chain (Spec -> PRD -> epic-altitude spine -> hand-authored epics across eight stations -> sprint ledgers -> drain queue) in ONE planning PR; implementation begins next session with the era tail
- (capability) CAP-1 Adoption register governs wiring. intent: every one of the 13 suite members has a recorded verdict (wield|skip), wielding station, provisioning path, hazards and status in adoption-register.md, and a member's wiring changes only by changing its row. success: pipeline-truth's wired column agrees with the register for 13/13; the channel-product 'never wire-everything' constraint and one-front-door row-6 triage are recorded as superseded/closed by memlog
- (capability) CAP-2 Module wave lands by install class. intent: steward provision --module utility-skills|tea|bmb (and a CIS re-provision) place each module's skills in .claude/skills with manifest sections — never a hand copy. success: --list-modules reports the four installed; retired-ID guard + integrity meta-tests green; CIS SKILL.md files carry --project-root (15 lines / 10 files); the bmb cleanup-legacy hazard is proven never invoked by a test
- (capability) CAP-3 Station routing. intent: each adopted skill has exactly one wielding station, recorded in that station's persona skill (bmad-agent-<station>) and the AGENTS.md managed block. success: a routing table in adoption-register.md maps every adopted skill to one station and each named persona skill cites it; CLAUDE.md carries none of it
- (capability) CAP-4 TEA full adoption. intent: TEA's bmad-testarch-* workflows produce every station's planning-artifacts/test-architecture.md and tea-test-review runs as a Marshal review lens and a Warden advisory finding; the repo generator _bmad/scripts/bmad_tea_playwright.py, its two marshal meta-tests and its two pixi tasks are retired. success: 8/8 test-architecture docs regenerated by TEA with an equivalence check recorded against the generator's last output; tea-test-review --base origin/main --min-score N exits with a real code on a fixture PR; the generator and tests are deleted in the same story; Warden's gate verdict unchanged
- (capability) CAP-5 Manticore studio for Herald. intent: Herald renders a station video from a deck's speaker notes through manticore installed in a dedicated studio root outside this repo's _bmad/. success: the studio provisions via --custom-source with [modules.manticore] in the studio's own _bmad/custom/config.toml; one station .mp4 renders and is gitignored; this repo's _bmad/ is byte-identical before and after
- (capability) CAP-6 labs-skills by consent. intent: exactly the consented labs skills (mcp-builder -> Atlas, slides-generator -> Herald, multi-repo-git-ops -> Marshal, release-please -> Steward) are installed by name. success: each has a register row and lands via npx skills add bmad-labs/skills --skill <name>; no other labs skill is present in .claude/skills
- (capability) CAP-7 eval-quality pilot runs. intent: the designed twin-run contract measures the edge-case-hunter reviewer against a planted file:line defect under a per-trial budget ceiling. success: eval-quality-smoke, eval-quality-review-twin-run and eval-quality-review-replay exist as pixi tasks; one trial cites pkg/discount.py:17 on the mutated arm and not on the clean arm; none of the three joins detectors/detectors-ci (Story 45.2, spec-bmad-eval-quality CAP-2)
- (capability) CAP-8 Release cadence is one runbook. intent: every BMAD-METHOD release goes detect -> apply -> align -> refresh -> flip through release-cadence.md with named owners, and the @next prerelease is its optional rehearsal. success: the next release is processed with zero improvised steps against the runbook; the rehearsal recipe (throwaway worktree, --installer npx bmad-method@next, planted conflict, PATH without node) exists and has run once report-only
- (capability) CAP-9 BMAD-estate cutover readiness. intent: every prerequisite the python-foundry cutover assumes about _bmad/, .claude/skills/, _bmad-output/ and the loop homes is listed with an owner in cutover-readiness.md, the untracked gaps are relayed to the cutover chain by memlog, and the gate is every line green before Story 44.3 opens the foundry. success: the checklist reads green on P1-P17; G1-G11 each name a story; steward upgrade bmad-core preflight reports zero ungoverned local customizations
- (capability) CAP-10 Shim retirement (relay). intent: the 21 deprecated shims and every live caller are gone from this repo before the foundry opens. success: harness policy names bmad-build-auto, 8 loop homes re-rendered and validate clean, callers glossed, guard widened to the harness file, one --no-shims apply lands with installShims: false and 21 fewer skill dirs (era-alignment CAP-12 / marshal 30.5; core-upgrade CAP-9 / steward 14.9)
- (constraint) Provision by install class only: no hand copies into .claude/skills, never cleanup-legacy.py (would rmtree _bmad/core/config.yaml), never bmad-module-skill-forge uninstall (would delete every skill dir)
- (constraint) The manticore studio is a separate root; its upgrade ritual (wipe the studio's _bmad/ + _bmad-output/) never touches this repo's _bmad/
- (constraint) TEA and eval-quality verdicts are advisory (Warden) or review lenses (Marshal); Warden's gate stays the sole PR verdict
- (constraint) Customizations live in _bmad/custom/** or are re-applied by core-upgrade CAP-8; every in-place edit of an installer-owned file is under a spec surface (P7/P13)
- (constraint) Retired-name sweeps gloss shipped history (decks, docs/specs, pixi.toml comments); live docs and code lead with the live names (era-alignment convention kept)
- (constraint) Parallel agents address projects by physical path with BMAD_ACTIVE_PROJECT per invocation; sprint ledgers change only through sprint_plan.py generate + scoped sprint-ledger-sync; never bmad-switch, never an unscoped sync
- (constraint) Outward acts (upstream PRs, a new repo, budget-spending eval trials) need an explicit operator ask; the eval pilot's --max-budget-usd ceiling is per trial
- (note) Non-goals: bmad-method first-install (foundry's); suite conda-recipe refresh (CFE flow, Mason); mybmad-dashboard into the platform; module-template provisioning; the whole labs marketplace; upstream PRs (skf marketplace.json, sprint-plan key fix, skf uninstall bug); the cutover itself (Epic 44)
- (note) Decomposition targets (hand-authored, same PR): steward Story 14.9 (--no-shims), Epic 46 (CAP-1..8, 10 stories), Epic 47 (CAP-9, 5 stories); marshal Story 30.5 (harness + callers), Epic 31 (TEA replaces the generator, spec-surface widening, loop-home readiness, gh-triage/multi-repo routing); doctor Epic 20 (suite drift 7->13, render-HALT + frozen-path detectors, RCA routing, version-drift status); warden Epic 11; herald Epic 18; scribe Epic 7; atlas Epic 24; mason Epic 14 (eval-quality __win variant)
- (assumption) TEA's bmad-testarch-test-design / -framework outputs can cover what bmad_tea_playwright.py generates (epics + live test inventory -> test-architecture.md, deterministic, TBD = hard fail); the equivalence check in CAP-4 decides — if TEA cannot, the generator stays and CAP-4 narrows to the review lens
- (assumption) steward provision --module manticore cannot target a studio root today (CondaInstallBackend installs into the repo); CAP-5 uses the documented native path (npx bmad-method install --custom-source in the studio) until a --studio flag earns its keep
- (assumption) bmad-builder's five skills and skf's sixteen coexist without name collision (bmad-bmb-* / bmad-agent-builder vs skf-*); the collision check in steward provision decides at wire time
- (question) Where does Herald's manticore studio live — presentations/_studio/ (inside the repo, gitignored) or ~/pyforge-studio/ (outside)? CAP-5 needs one root; the studio's own _bmad/ must not sit under this repo's _bmad/
- (question) tea-test-review --min-score threshold for the Marshal lens / Warden advisory — start at upstream's example 80 or calibrate on the first ten PRs?
- (question) CAP-3 routing notes: persona skill AND the AGENTS.md block, or the block only (AGENTS.md is rewritten by skf-export at cutover — fnd:AD-6)?
- (question) eval-quality trial cost accounting: steward budget (Story 44.15 metering) or a local ledger under evals/ — carried from spec-bmad-eval-quality
- (event) Self-validate pass 1 (coherence): PASS — CAP-1..10 each carry intent+success; intents name outcomes (the steward verbs cited are the sanctioned interfaces, not prescriptions); 7 constraints each rule something out; 5 non-goals; success signal is demonstrable (pipeline-truth 13/13 + register agreement, --list-modules, a rendered mp4, a zero-improvisation release, checklist green, installShims false); IDs unique; three assumptions and four open questions recorded
- (event) Self-validate pass 2 (preservation): PASS — Dream § Grounding catalog + verdicts -> adoption-register.md; era tail + 73-item register -> open-items-register.md; cutover P1-P17 / G1-G11 -> cutover-readiness.md; § Decisions -> memlog decisions + SPEC constraints; § Whose job -> CAP-3 + register § 2 routing; § What it looks like -> CAP-1..10; § Constraints/Non-goals -> kernel. Wrapper-only content dropped: § Kinships (Dream-level cross-links) and § Realization log (process record)
- (event) Spec finalized 2026-09-06: status ready; companions adoption-register.md, release-cadence.md, cutover-readiness.md, open-items-register.md; next = bmad-prd create, bmad-architecture create (epic altitude), hand-authored epics (steward 46/47 + 14.9; marshal 30.5 + 31; doctor 20; warden 11; herald 18; scribe 7; atlas 24; mason 14), sprint ledgers, drain queue
- (decision) 2026-09-06 (spine reviewer gate, adversarial F-6): open question 1 closed — Herald's manticore studio root is OUTSIDE the repo, declared once per machine by PYFORGE_STUDIO_ROOT (default ~/pyforge-studio/), cited by the register cell and herald's CLI; mc-* never provisioned into the repo tree (lifecycle spine AD-3)
- (note) 2026-09-06 spine adopted by citation (architecture-bmad-suite-lifecycle-2026-09-06, AD-1..12): AD-9 fixes the provisioned-module roster to _bmad/custom/config.toml [modules.<code>]; AD-10 makes cross-station prerequisites blocked rows + in-story checks (Deps tokens stripped); AD-11/AD-12 surface ownership and skf's single root; the @next rehearsal is core-upgrade Story 14.10 and 46.10 is the runbook verification only
- (event) SPEC re-derived 2026-09-06 (surgical): open question 1 removed (decided), constraint added for the studio declaration; CAP text unchanged
- (decision) 2026-09-06 (spine AD-2, adversarial F-4): open question 'routing home' closed — the persona skill is the one durable home; AGENTS.md carries one pointer line to adoption-register.md § 2 placed once by bmad-project-context, never per-skill lines; a meta-test (46.1) proves one persona per skill dir
- (decision) 2026-09-06 (spine AD-1, adversarial F-11): CAP-6's writer is the steward-wrapped, pinned steward provision --plugin labs --skill <name>; a raw npx skills add beside the conda member is a second writer and a defect
- (event) SPEC re-derived 2026-09-06 (surgical, second pass): CAP-3 and CAP-6 success wording aligned to AD-2/AD-1, CAP-9 reads P1–P18, routing-home open question removed (decided); companions adoption-register.md § 2/§ 3 and cutover-readiness.md P18 updated
- (correction) 2026-09-06 CAP-10 spec correction (found during marshal Story 30.5, operator-directed "spec correction" over "new story"): the Success bar naming "the harness policy names bmad-build-auto, the 8 loop homes re-render and validate clean... the guard is widened to the harness file" is FALSE, verified against the installed bmad_loop 0.11.1 package source: DevPolicy.skill is a permanent internal adapter discriminator (DEV_SKILLS = {"bmad-dev-auto"}) that must read "bmad-dev-auto" forever; the skill actually invoked is resolved from disk at runtime, independent of this field. SPEC.md's CAP-10 corrected in place. Matching corrections: era-alignment CAP-12 (pyforge-marshal), core-upgrade CAP-9 (this project, spec-bmad-method-core-upgrade) — the latter's own false refusal check (_shim_retirement_blockers, plus the --loops-home CLI flag) is removed from upgrade.py in the same session, via a follow-up bmad-build-auto review pass on Story 14.9's own spec.
