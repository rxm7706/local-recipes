---
topic: mcp-host sidecar hosting only a generic identity stub per station instead of any station's real MCP tools
updated: 2026-09-12T15:40
---

- (decision) Owner-dream: docs/dreams/mcp-host-real-station-tools.md (status: dreamt). Kin to docs/dreams/mcp-era-isolation.md (spec-mcp-era-isolation, shipped slice 1) -- this Spec closes the gap slice 1 left open, not a rewrite of it.
- (decision) Owner: pyforge-steward (same surface as spec-mcp-era-isolation: mcp_http.py, mcp_dual_era.py, mcp_host/app.py, the platform chart's sidecar wiring).
- (capability) CAP-1 -- a station with a real in-process MCP app (e.g. django_marshal_portal.mcp_asgi) is reachable through the deployed mcp-host sidecar with its REAL tools, not the generic station_face() stub. Success: POST /stations/marshal/mcp with publish_loop_run on a real deployed cluster reaches publish_held_loop_bounded and returns a handle, not 'Unknown tool'.
- (capability) CAP-2 -- a station with no real in-process MCP app still gets the existing station_face() stub as a fallback, so today's slice-1 proof (the dual-era handshake) keeps passing unchanged for any station that never grows a real app. Success: spec-mcp-era-isolation's own CAP-1..3 acceptance criteria still hold after this Spec's change lands.
- (capability) CAP-3 -- the deployed proof: CAP-3's own CRC exercise (spec-run-state-one-publisher/verification-2026-09-12.md) is re-run and its 'NOT PROVEN' row (a live run appearing on /runs/, timing surviving workstation teardown) flips to PASS.
- (constraint) Must not change spec-mcp-era-isolation's shipped slices 1-3 sequencing or scope: the dual-era wire, the proxy dispatch, MCP_HOST_SIDECAR_BASE_URL wiring, CAP-4 cluster-required fail-loud, the stdio translator (slice 2), and the ImportError-skip retirement gate (slice 3) all stay exactly as they are.
- (constraint) A station's own portal package (django_<name>_portal) stays that station's surface; this Spec only owns how the sidecar discovers and mounts what already exists there -- it does not redesign any station's tool implementation.
- (non-goal) Not retiring the mcp 1.x/2.x ImportError skip in the web image (slice 3 -- gated on an upstream FastMCP 4 / unpinned-Langflow release, tracked in spec-mcp-era-isolation/retire-skip.md).
- (non-goal) Not the factory stdio wire-format translator (spec-mcp-factory-stdio-translator, slice 2) -- a different, already-specced problem (modern MCP clients vs FastMCP 3 stdio tools).
- (question) Open: how does the sidecar get Django ORM access for stations whose real app needs it (e.g. django_marshal_portal.mcp_asgi calling into django_pyforge.supervisor)? The sidecar image today runs no Django at all by design ('No Langflow. No FastMCP.'). Candidate shapes: (a) a minimal settings module registering only the portal AppConfigs + DB connectivity, no Langflow; (b) each station's real app builder takes its own ORM-free path; (c) something else. Not resolved by this Dream -- the Spec needs research before this can leave open_questions.
- (question) Open: does EVERY station with a django_<name>_portal package need this (herald, warden, etc.), or is marshal's held-loop tool surface the only one currently exercised in production -- i.e. should CAP-1 scope to marshal alone first and generalize later?
- (note) Found running the CAP-3 attended CRC exercise, 2026-09-12: verify_idp_bearer() and mint_assertion() both succeeded via real HTTP once their own bugs were fixed (PRs #1278-1280, #1282), but POST /stations/marshal/mcp publish_loop_run returned 'Unknown tool: publish_loop_run' from the real mcp library's own tool_manager -- confirming a real MCPServer answered but never had the tool registered. Traced to mcp_host/app.py's asgi_for_station() building a generic one-tool (station_face) MCPServer per station, and confirmed the web pod's own interpreter still cannot import mcp.server.mcpserver (spec-mcp-era-isolation's premise is still live).
- (event) Self-validate: Pass 1 (coherence) clean against Spec Law 1-6,8 -- all 3 capabilities carry intent+success, constraints bend design (slice 1-3 immutability, portal-package ownership), 2 non-goals present, success signal is concrete/testable, IDs fresh and unique, prose lean. Pass 2 (preservation): every memlog decision/capability/constraint/non-goal/question landed in SPEC.md frontmatter or a kernel field; the finding note folded into Why. No companion needed -- no diagrams, no multi-item catalog, nothing overflowing the kernel. status: draft (2 open_questions remain -- ready pends a human/research pass on ORM-in-sidecar shape and CAP-1's station scope).
- (decision) Open question 1 (ORM access shape) resolved by implementation: a minimal Django settings module (src/platform/mcp_host/settings.py) registers ONLY django_pyforge + django_marshal_portal in INSTALLED_APPS, real DATABASE_URL/DJANGO_SECRET_KEY, no CACHES config (the functions this sidecar calls never touch django.core.cache), REDIS_BROKER_URL/REDIS_CACHE_URL deliberately left unset so the best-effort run.started event publish silently no-ops rather than needing a redis client dependency at all. django.setup() wrapped in try/except in mcp_host/app.py -- any failure (bad settings, unreachable config) falls back to the generic stub for every station, never breaks a station that doesn't need Django (CAP-2's own guarantee, covered by a mocked-failure unit test).
- (decision) Open question 2 (station scope) resolved: CAP-1 scoped to marshal alone. Confirmed marshal's own PortalConfig.ready() is lean (only registers a runner callback, no pyforge.marshal import at all) unlike e.g. mason's portal (eagerly imports pyforge.mason.boot per config/settings/base.py's own comment) -- marshal was the safe, self-contained first candidate. Generalizing to other stations is future work, not blocked by anything this Spec found.
- (capability) CAP-1 PROVEN LIVE 2026-09-12: real bmad-loop-style run driven from a workstation-side HostPublisher against the deployed CRC cluster -- publish_loop_run returned a real handle, heartbeat_loop_run and complete_loop_run both succeeded, the publishing process exited, and a separate curl call to /runs/ showed the run under Completed timing with its real duration. A second, previously-hidden bug (HostPublisher's flat-kwargs call shape vs the real tool's required nested payload field) was found and fixed in the same pass -- it could not have been caught before CAP-1 shipped, since the tool was unreachable to validate against until then.
- (capability) CAP-2 PROVEN 2026-09-12: mcp-host's own boot logs show all 9 stations' session managers starting cleanly; a local smoke test (mcp-host pixi env, real settings module, no live DB) confirmed _apps contains all 9 default stations while _real_apps contains only {'marshal': ...} -- every other station keeps the identity-stub fallback unchanged.
- (capability) CAP-3 PROVEN 2026-09-12: spec-run-state-one-publisher's own verification-2026-09-12.md now records the live run + /runs/ proof directly; its NOT-PROVEN row flipped to PASS.
- (event) SHIPPED 2026-09-12. All three capabilities proven live on a real deployed CRC cluster in the same session that specced this Dream. Landed PR #1285 (pixi.toml mcp-host deps, mcp_host/settings.py, mcp_host/app.py real-app discovery, Containerfile django_marshal_portal COPY, chart platform.mcpHostEnv + postgres egress/ingress, django_marshal_portal/mcp_asgi.py + publisher_host.py payload-shape fix). Verified: pixi run -e local-recipes platform-ci-local -- --test (915 passed) + pixi run -e pyforge-marshal pyforge-marshal-test (7,915 passed) + a full live-cluster run.
