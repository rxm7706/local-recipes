---
topic: One `bmad-suite` metapackage installs every active suite member at upstream-aligned pins, generated from the canonical `suite-members.yaml` manifest
updated: 2026-09-09T06:10
---

# memlog — spec-bmad-suite-metapackage

- (event) 2026-09-01: CAP-1/2/3 landed on main (`e177de648e`) — `recipes/bmad-suite/suite-members.yaml`, `recipes/bmad-suite/recipe.yaml`, CFE `bmad_suite_metapackage.py` + `build_bmad_suite.py` (+ thin wrappers), pixi tasks `generate-bmad-suite` / `build-bmad-suite`, channel publish `bmad-suite 2026.9.1`. Epic 39 retroactive decomposition (39.1–39.3 done, 39.4 CAP-4 backlog). Doctor Story 19.1 consumes manifest read-only.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 2 governed path(s) drifted against this spec's recorded baseline (2 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `.claude/skills/conda-forge-expert/scripts/bmad_suite_metapackage.py` (changed)
  - `recipes/bmad-suite/recipe.yaml` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-bmad-suite-metapackage` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (change) **2026-09-05 suite refresh (Story 45.1 — eval-quality joins; WDS retired):** 2 governed path(s) changed against this spec's recorded baseline:
  - `recipes/bmad-suite/suite-members.yaml` (changed) — `bmad-method-wds-expansion` moved to `deprecated: true` (upstream `bmad-modules.yaml` `deprecated: true`, folded into BMM as the `bmad-ux` skill); `bmad-eval-quality` added as an active member (14th recipe; the metapackage roster stays 13)
  - `recipes/bmad-suite/recipe.yaml` (changed) — regenerated by `generate-bmad-suite`: version `2026.9.2` → `2026.9.5`, `bmad-method >=6.11.0` → `>=6.12.0`, `bmad-method-wds-expansion >=0.4.3` → `bmad-eval-quality >=0.2.0.dev0`
  Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-bmad-suite-metapackage`. Note: this memlog carries no frontmatter, so `memlog.py append` refuses it (`.memlog.md has no frontmatter`) — entry appended by hand in the file's own format.
- (event) **2026-09-06 SPEC re-derived:** status in-progress → shipped — Epic 39 (39.1–39.4) done (4/4, 2026-09-01); metapackage `2026.9.5` is on the channel (first governed refresh, 2026-09-05). Dream `docs/dreams/bmad-suite-metapackage.md` flipped dreamt → realized the same day; recorded late. Appended by hand (memlog carries no frontmatter, `memlog.py append` refuses it).
- (assumption) **2026-09-06 supersedes** the "WDS stays published but skip-wired" assumption: `bmad-method-wds-expansion` was retired 2026-09-05 (deprecated upstream in the 6.12.0 module registry, absorbed into `bmad-ux`; recipe kept as a catalog row only). The 13-member install-matrix remains the authoritative population with `bmad-eval-quality` seated in its place.
- (change by claude) 2026-09-09 channel naming (operator direction): every `SelfExplainML` reference INSIDE `recipes/bmad-suite/` now reads `PrivateChannel` — 3 comments + 1 member note in suite-members.yaml and the recipe's about.summary/description. DESCRIPTIVE ONLY: the functional channel resolution is deliberately untouched (pixi.toml:19 `channels`, the two `channel = "SelfExplainML"` selectors, the workflow upload target, the pixi-mirror URLs) because the live anaconda.org channel is still SelfExplainML and renaming those would break every solve and every upload. Also dropped bmad-method's redundant `notes: conda-forge canonical; ... refresh-parity optional` so its entry matches the other active members' description+urls shape (10 of 13 carry no notes). Generator dry-run unchanged; both files re-parse; no test asserts on this prose. ALSO: this memlog gained the frontmatter it never had, so `memlog.py append` accepts it — this entry is the first one written BY THE TOOL rather than by hand (three earlier entries record having to hand-append). RECONCILES: recipes/bmad-suite/suite-members.yaml, recipes/bmad-suite/recipe.yaml. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-bmad-suite-metapackage.
