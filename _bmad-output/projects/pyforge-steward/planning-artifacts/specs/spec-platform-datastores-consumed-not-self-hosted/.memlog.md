---
topic: PostgreSQL and Redis become consumed, not self-hosted (docs/dreams/platform-datastores-consumed-not-self-hosted.md)
updated: 2026-09-11T12:59
---

- (direction) Input: docs/dreams/platform-datastores-consumed-not-self-hosted.md + sprint-change-proposal-2026-09-11-ad-1-datastores-exception.md, both already-written, structured, pre-sorted -- direct extraction, express mode, mirroring sibling spec-platform-object-storage-kind's exact shape (same repo, one correct-course cycle earlier).
- (capability) CAP-1: the AD-1 exception itself. Intent: PostgreSQL and Redis permitted as consumed, never mandatorily self-hosted, backing services -- endpoint/credentials as configuration, existing self-hosted default preserved. Success: spec-pyforge-unifying-strategy/SPEC.md carries the dated 2026-09-11 exception bullet under AD-1 (second one, after object storage's 2026-09-10 bullet); the existing self-hosted default is not withdrawn. Landed directly by this correct-course pass, not a dispatched story.
- (capability) CAP-2: A BYO-external-PostgreSQL deployment overlay exists, additive to the self-hosted default. Intent: mirrors the existing OCP overlay's own additive shape (src/platform/deploy/overlays/ocp/core-overrides.yaml) -- a .Values.postgres.external.enabled toggle (default false) that when true deploys zero self-hosted Postgres resources and points DATABASE_URL/MIGRATION_DATABASE_URL at an externally-supplied endpoint+credential secret instead. Success: with the overlay applied, postgres-statefulset.yaml/postgres-service.yaml/postgres-backup-pvc.yaml all render empty and the app's DB URLs resolve externally; with the overlay NOT applied, a helm template render is byte-identical to today's output. (Story 51.1.)
- (capability) CAP-3: A BYO-external-Redis deployment overlay exists, additive to the self-hosted default. Intent: same pattern as CAP-2 applied to Redis -- a .Values.redis.external.enabled toggle, REDIS_URL/REDIS_BROKER_URL/REDIS_CACHE_URL point external when true. Success: with the overlay applied, redis-deployment.yaml/redis-service.yaml/redis-broker-pvc.yaml all render empty and the app's Redis URLs resolve externally; with the overlay NOT applied, a helm template render is byte-identical to today's output. (Story 51.2.)
- (capability) CAP-4: The backup/PITR handoff is explicit when the BYO-PostgreSQL overlay is active. Intent: postgres-backup-cronjob.yaml must not run a shadow backup of an instance pyforge doesn't own; the handoff to the enterprise's own database team must be documented, not silently assumed. Success: when CAP-2's overlay is active, the backup CronJob is not deployed, and deploy/README.md names the enterprise database team as the owner of backup/PITR for the BYO path; with the overlay NOT applied, the backup CronJob still deploys exactly as today. (Story 51.3.)
- (constraint) Local dev keeps self-hosting Postgres/Redis unconditionally -- this Spec is about the deployed platform's operational model only, not local dev ergonomics, matching object storage's own Silo/Garage precedent.
- (constraint) The shape is additive/pluggable, never a breaking cutover -- the self-hosted default must remain byte-identical (via helm template diff) when the BYO overlay is not applied, for both CAP-2 and CAP-3.
- (constraint) Endpoint and credentials are configuration only, never hardcoded -- the same DATABASE_URL/REDIS_URL seam already in settings/base.py must work unmodified whether it resolves to the self-hosted default or an external Enterprise instance.
- (constraint) Kubernetes/OCP's own treatment is out of scope -- already externally-provided via the existing OCP overlay, a different kind of thing than a backing service, not reopened here.
- (note) Non-goal: migrating any existing feature onto the BYO overlay in this chain -- the overlay exists and is proven; consumption is separate, future, story-by-story work, matching object storage's own CAP-4 precedent.
- (note) Non-goal: provisioning real Enterprise PostgreSQL/Redis credentials or endpoints -- an operator/ops action outside this repo's scope, the same boundary the IdP's and object storage's own credentials already sit behind.
- (note) Non-goal: changing Kubernetes/OCP's own already-external treatment -- unchanged by this chain.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6/8): PASS. CAP-1..4 each carry intent+success, WHAT not HOW; 4 constraints each rule something out; 3 non-goals explicit; success signal testable (helm template diffs, resource render checks); CAP IDs stable, mirroring the numbering convention (CAP-1 = landed exception, CAP-2..4 = the three stories) used by sibling spec-platform-object-storage-kind; prose lean.
- (event) Self-validate pass 2 (preservation): PASS. Every load-bearing claim from the Dream and the sprint-change-proposal lands: the AD-1 exception -> CAP-1, the three story bodies (51.1-51.3) -> CAP-2..4, the Dream's four Non-goals -> the 4 constraints + 3 non-goals, the operator-confirmed real deployment target -> Why section. No wrapper-only drops.
- (event) Spec finalized, status: ready. 4 capabilities (CAP-1 landed, CAP-2..4 backlog), 4 constraints, 3 non-goals, 0 open questions (both source documents already answered every field). Mirrors spec-platform-object-storage-kind's exact shape one correct-course cycle later.
- (decision) Topology refined (operator, 2026-09-11, pre-approval): all four Enterprise-managed pieces co-located at the same data center; pyforge ships as a podman/docker container into a namespace within the enterprise-managed OCP cluster -- a tenant workload, not the cluster owner/operator. Non-structural, folded into Why for accuracy; no capability/constraint change.
