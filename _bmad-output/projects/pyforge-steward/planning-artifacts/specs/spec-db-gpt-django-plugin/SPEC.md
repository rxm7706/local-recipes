---
spec: db-gpt-django-plugin
status: absorbed
owner-dream: docs/dreams/db-gpt-django-plugin.md
absorbed-into: spec-python-agent-platform
companions: []
sources:
  - ../../../../../../docs/dreams/db-gpt-django-plugin.md
---

# SPEC — DB-GPT joins Django as a pluggable app (ABSORBED)

This is a pointer spec, not a contract. The owner Dream self-closed on
2026-08-14 — its Realization log names **python-agent-platform** as the
subject project ("this dream's remaining role is the decision trail and its
named pattern contracts") — and the work shipped through
`spec-python-agent-platform`'s chain (steward Epics 10–12: the in-process
ASGI dispatcher behind one front door, Langflow Pattern A, DB-GPT Pattern B
sidecar with the AD-6 SQLite exception, the 12.1 chart). Authored 2026-08-22
during the local-ocp-hybrid-environment spec pass so INV-1 (every Dream has
a Spec) holds without re-minting a chain. Residual carried into the platform
orbit: thread-pool sizing under load (the asgi Dream's untested gap),
recorded in spec-local-ocp-hybrid-environment's reconciliation companion.
