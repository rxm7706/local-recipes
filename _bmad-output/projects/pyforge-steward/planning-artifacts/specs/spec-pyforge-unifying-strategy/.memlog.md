---
topic: The PyForge Canopy extension chain over src/platform (modular monolith; MCP on host ASGI)
updated: 2026-09-20T08:38
---

- (direction) Operator assigned the Dream to steward (was herald): the mass is platform hosting, deployment and estate custody. Charter 5 -- owning is the post, not the product.
- (decision) Chain is an EXTENSION binding spec-python-agent-platform as prior art, not a greenfield estate spec. Mints no CAP duplicating its CAP-1..6. Rationale: audit found src/platform shipped and steward epics 10/11/12/16 done.
- (decision) The Canopy is src/platform/ -- no ninth project, no Charter 5 governance act, roster stays at eight. pyforge_host and pyforge-agent-platform demoted to role names; no rename story minted.
- (constraint) Django >=5.2.15,<6 and Python 3.12.* are fixed by CAP-5 of spec-python-agent-platform. Django 6 is unavailable. conda-forge ships exactly one qualifying build (5.2.15), two patches behind upstream, zero headroom.
- (constraint) Every dependency MUST resolve from conda-forge. CAP-6's egress-blocked build is a gate, not a warning. A PyPI-only package is new feedstock work, not a preference.
- (constraint) Infrastructure stays exactly PostgreSQL + Redis + Kubernetes (inherited from spec-python-agent-platform). A component demanding a fourth piece has failed design review.
- (constraint) src/platform/ consumes the factory's published conda packages only and never imports pyforge.* code. The factory/platform boundary is an import rule.
- (decision) Steward owns cross-station slices outright, including marshal's pyforge.core.client and herald/marshal's Lane 1. Precedent: warden's compliance_face already lives in src/platform/.
- (event) Convergence pass complete: 14 surfaces covered-and-cited, 19 residual items evidence-confirmed absent, 5 Dream premises corrected.
- (note) Correction: Lane 1 is MARSHAL's console (docs/dashboard/, spec-factory-console shipped), not herald's. Herald's SPEC line 137 makes owning it an explicit non-goal. Wagtail work needs a negotiated handoff.
- (note) Correction: scribe is FlatFileGraphStore JSON, not SQLite. BS-1's premise and remediation must be rewritten as flat-file -> PostgreSQL/pgvector.
- (note) Correction: FastAPI already exists in-host at src/platform/config/fastapi_app.py. RFC-1 modifies an existing seam rather than introducing FastAPI.
- (note) Correction: pyforge-atlas already ships MCP (atlas/mcp/server.py build_server). Residual is the other seven stations plus transport, not the mechanism.
- (note) placeholder
- (note) VOID: the preceding 'placeholder' note was an accidental append from a line-count check. It carries no decision and supersedes nothing.
- (event) Phase-2 technical research complete. Four Dream directives invalidated against the live pins; operator ruled on all four 2026-08-24.
- (decision) RFC-5 revised from as-written to the least-bad form. As-written is not implementable: post_migrate is the only supported mechanism populating django_content_type/auth_permission/django_site, so migrate must keep running; create_test_db runs migrate so the test runner has no path off Django migrations.
- (decision) RFC-5 adopted shape: DDL revoked at the DATABASE ROLE (app role DML-only, separate migration role holds DDL -- the only auditor-verifiable control); Liquibase owns production DDL via a Helm pre-upgrade Job, NOT an init container (N replicas contend on DATABASECHANGELOGLOCK, default wait 5 min); Django migrations retained as the authoring surface with a CI sqlmigrate extraction gate; liquibase update then migrate --fake; test databases explicitly carved out.
- (decision) BS-2 rewritten to the current MCP spec. /mcp/sse is deprecated twice over: HTTP+SSE Deprecated since 2025-03-26, and 2026-07-28 removed the standalone GET stream and protocol-level sessions (compliant servers answer GET with 405). Adopted: single POST /mcp on official mcp 2.0.0 SDK, Tasks extension SEP-2663 for multi-minute work, keep-alive comment frames under 30s, X-Accel-Buffering no.
- (constraint) OpenShift timeout client stays at the cluster-wide 30s with NO per-route annotation. haproxy.router.openshift.io/timeout raises only timeout server. HAProxy governs SSE by timeout client/server, not timeout tunnel. Therefore keep-alive interval MUST be well under 30 seconds regardless of any route annotation.
- (decision) CodeRed CMS dropped; Wagtail 7.4.3 LTS alone carries Lane 1. CRX has no main commit since 2025-09-12, declares Wagtail 7.0-7.1 against a 7.4.3 LTS, and carries unfixed issue #710 (admin form submissions 404). Wagtail itself is conda-forge same-day with full closure, supports Django 5.2 + Python 3.12, and PostgreSQL FTS removes any Elasticsearch dependency.
- (decision) OpenFeature adopted via the flagd FILE resolver, accepting the packaging cost: FIVE new conda-forge recipes (openfeature-sdk, openfeature-flagd-api, openfeature-flagd-core, openfeature-provider-flagd, plus a cachebox 5.x build since conda-forge ships 6.2.5 and the provider pins <6). Per repo Rule 1 each of those stories MUST invoke conda-forge-expert.
- (decision) django-pyforge is OURS to build. django-lasuite is OIDC/DRF/malware plumbing with no UI layer and no app switcher; La Suite's La Gaufre is npm/React with a service-list endpoint unreachable air-gapped. No documented La Suite reusable-app pattern exists. Adopt django-lasuite for OIDC only.
- (decision) BS-4 gains a build task, not a dependency. PyBreaker's async support is Tornado-coroutine, not asyncio -- an httpx.AsyncClient coroutine passed to breaker.call() records a false success and the circuit never trips. Write the async wrapper over PyBreaker's own storage; reject aiocircuitbreaker (dormant since 2022) and purgatory (unpackaged).
- (assumption) PyBreaker's Redis state transitions use plain setnx/set/incr with no Lua or WATCH, so cross-replica transitions are not atomic. fail_max is treated as coarse protection, not exact-count semantics. This is read from source, not a documented upstream limitation.
- (question) Does air-gap policy accept a container as the delivery vehicle for Liquibase, a non-conda JVM tool, or must a conda-forge feedstock be authored first?
- (question) Is Liquibase issue #7791 (multi-schema default-schema-name regression on 5.0.3, runInTransaction false) fixed in 5.0.4? Must be verified before any multi-schema changeset lands.
- (question) Which MCP protocol revision do our agent clients actually speak? A 2026-07-28-only server rejects handshake-era clients and vice versa.
- (question) Does the official mcp Python SDK yet ship a server-side Tasks runtime? FastMCP's own PR notes no SDK shipped one at time of writing -- this is the largest gap between the recommended pattern and shippable code.
- (question) Marshal owns the Guildhall console (spec-factory-console, shipped). Does Wagtail Lane 1 supersede it, wrap it, or sit beside it? Requires a negotiated handoff, not an assumption.
- (capability) CAP-1 django-pyforge: one installable Django app supplying the shared chrome every station portal mounts -- app switcher, OIDC-aware base layout, Modernist theme assets, and the portal registration seam. Success: two portals render identical chrome from one package with zero duplicated template or static files.
- (capability) CAP-2 Lane 1 on Wagtail: a CMS-managed public front door at / on Wagtail 7.4.3, with the admin behind allauth OIDC via WAGTAILADMIN_LOGIN_URL and media on object storage. Success: an editor publishes a page without a deploy, and the admin is unreachable without an IdP-issued session.
- (capability) CAP-3 Eight portals mounted: every station reachable as a Lane 2 Django app under the one host, starting from compliance_face renamed warden_portal. Success: all eight portal URLs resolve behind one session, and a portal added or removed changes no host code outside its own registration.
- (capability) CAP-4 The services tier: per-station FastAPI services exposing MCP at a single POST /mcp on the official SDK, with operations over ~30s returning a Task handle. Success: an agent completes a multi-minute station operation across a simulated ingress disconnect without losing the result.
- (capability) CAP-5 One command grammar: pyforge <station> <noun> <verb> dispatching to the eight existing per-station CLIs without reimplementing them. Success: every verb reachable through both the unified entry point and its station binary, proven by a generated parity matrix.
- (capability) CAP-6 pyforge.core.client: the shared SDK every Django portal uses to reach its station service, carrying a signed internal JWT with a delegated_by claim. Success: a portal call arrives at the service with the end user's identity provable and independently verifiable, and no portal constructs a raw HTTP request.
- (capability) CAP-7 Lane 3 analytics behind the front door: atlas's Vizro boards proxied through the host with per-tenant row isolation enforced at the identity boundary, adopting spec-secure-live-dashboards rather than reinventing it. Success: two users with different roles receive provably different row sets from one board URL.
- (capability) CAP-8 The event fabric: a Redis Streams backbone carrying CloudEvents between stations, with consumer groups, a dead-letter queue and a loop-depth ceiling. Success: a poisoned event lands in the DLQ instead of retrying forever, and a cyclic publish chain halts at the declared depth.
- (capability) CAP-9 DDL governance: production schema change flows exclusively through Liquibase, enforced by database-role privilege rather than convention, with Django migrations retained as the authoring surface and a CI gate proving the extraction is not stale. Success: the application role provably cannot execute CREATE/ALTER/DROP, and a Django migration with no corresponding changeset fails the build.
- (capability) CAP-10 Runtime resilience invariants: circuit breaking on both sync and async portal-to-service calls, single-writer DuckDB discipline, inline form-error mapping, and idempotent startup reconciliation. Success: each invariant has a failing test that passes only once the invariant holds -- BS-4, BS-5, BS-7, BS-8 individually demonstrated.
- (capability) CAP-11 Resource-role separation: the Celery broker and the cache are distinct Redis instances with distinct eviction policies, and web and worker pools scale independently. Success: filling the cache to its eviction limit provably loses no queued task.
- (capability) CAP-12 Authorization and secrets: a Keycloak role matrix mapped onto Django groups so portal access is IdP-driven, with runtime secrets delivered from Vault rather than the environment. Success: revoking a role in the IdP removes portal access on the user's next request, and no long-lived secret appears in a pod spec.
- (capability) CAP-13 Feature-flag delivery: OpenFeature evaluation available to Django, FastAPI and CLI surfaces through the flagd FILE resolver, with every package sourced from conda-forge. Success: a flag flips behaviour across all three surfaces from one local flag file with no egress and no redeploy.
- (capability) CAP-14 Scribe durable storage: the knowledge graph moves from a single flat JSON file to PostgreSQL with pgvector behind the existing GraphStore port, keeping a local-development path. Success: the same graph operations pass against both drivers, and semantic recall returns results lexical recall cannot.
- (decision) Companions: convergence.md (chain boundary + residual), resilience-invariants.md (RFC-1..5 and BS-1..8 with revised forms), stack.md (the conda-forge gate), architecture-diagrams.md (mermaid -- Spec Law rule 2 keeps diagrams out of the kernel), plus the Phase-2 research file adopted by relative path.
- (event) Self-validate Pass 2 (Preservation) caught a gap: the Dream's 5-tier symmetry has five tiers (CLI, Web Portal, FastAPI/MCP Service, Domain Skill, Agent Persona) and CAP-1..14 covered only tiers 1-3. Tiers 4 and 5 were being silently dropped.
- (note) Tier-4/5 evidence: exactly ONE station domain skill exists (conda-forge-expert, mason's) of eight. ZERO station agent personas exist -- the five bmad-agent-* skills are BMAD roles (analyst/architect/dev/pm/ux-designer), not station personas. So tier 4 is 1/8 and tier 5 is 0/8.
- (capability) CAP-15 Station domain skills: each station carries an agent-invocable domain skill encoding its operating knowledge, following the shape conda-forge-expert already proves for mason. Success: an agent asked to perform a station's core task loads that station's skill and follows it, demonstrated for a station that has none today.
- (capability) CAP-16 Station agent personas: each station exposes an autonomous persona that can be addressed by name and acts through that station's own CLI and service surfaces rather than ad-hoc tool calls. Success: a persona completes a station task end to end using only CAP-5's grammar and CAP-4's service face, with the transcript showing no direct filesystem or ad-hoc HTTP access.
- (event) Self-validate Pass 1 (Coherence): Spec Law 1-6 and 8 pass. All 16 capabilities carry intent+success; intents kept to WHAT (CAP-2 says 'CMS-managed front door' not 'Wagtail', CAP-13 says 'vendor-neutral interface' not 'OpenFeature', CAP-14 says 'durable backing store' not 'PostgreSQL') with implementation held in stack.md and resilience-invariants.md; 12 constraints each rule something out; 7 non-goals; success signal is demonstrable; IDs CAP-1..16 unique and unreused.
- (event) Self-validate Pass 2 (Preservation) verdict after adding CAP-15/16: every load-bearing Dream claim now lands -- RFC-1..5 and BS-1..8 in resilience-invariants.md, the 5-tier matrix across CAP-3/4/5/15/16, the packaging gate in stack.md, topology in architecture-diagrams.md, and the covered/residual split in convergence.md. Wrapper-only content dropped: the press-release framing and the aspirational 'industry's first' prose.
- (decision) Operator ruling 1/5 -- tiers 4 and 5 are IN SCOPE. CAP-15 (station domain skills, 1 of 8 exist) and CAP-16 (station agent personas, 0 of 8 exist) stay bound. Adds a constraint: a station is never complete on fewer than five tiers, whatever its ledger says.
- (decision) Operator ruling 2/5 -- epic granularity is THEMATIC, roughly 7-9 epics grouped by delivery seam rather than 1:1 per capability. Steward runs to Epic 17, so this chain starts at Epic 18.
- (decision) Operator ruling 3/5 -- guildhall-handoff resolved as SUPERSEDE. Wagtail Lane 1 replaces Marshal's docs/dashboard console; spec-factory-console is retired via correct-course in Phase 5. CAP-2 rewritten from an addition into a replacement carrying a parity gate: every retired view reachable from the new front door, old build path REMOVED not merely unlinked. New open question console-parity-inventory replaces guildhall-handoff.
- (decision) Operator ruling 4/5 -- liquibase delivery vehicle goes to RESEARCH before deciding. New open question liquibase-airgap-policy: does this repo's air-gap policy govern container images at all, or only conda-sourced dependencies? CAP-9 cannot be scheduled until it lands. Repo-internal precedent research dispatched 2026-08-24.
- (decision) Operator ruling 5/5 -- Phase 5 correct-course scope is ALL EIGHT stations, one run per station, so every station spec records its Canopy obligations. Marshal's run additionally retires spec-factory-console per ruling 3. Each run pinned with BMAD_ACTIVE_PROJECT and written to physical paths, never through the shared symlink.
- (note) Convergence corrected: it still recorded RFC-5 as accepted 'as written', which the same-day least_bad revision superseded. Now states both, and adds the delivery-vehicle unknown.
- (event) Air-gap delivery research returned. ANSWERED liquibase-airgap-policy: the repo runs TWO boundaries. Conda channels govern the Python/pixi graph (enterprise-deployment.md, silent on images -- searched for container/image/OCI, not found). Container images are governed separately by spec-python-agent-platform CAP-6 ('internal-registry images ... any external reference is a FAILING check'), which already admits third-party non-conda images: postgres:17, redis:7 in the chart, quay.io/keycloak/keycloak:26.4.0 in compose.
- (note) DECISIVE FIND, larger than the policy answer: src/platform/deploy/charts/platform/templates/migrate-job.yaml is SHIPPED and is exactly the pre-upgrade hook RFC-5 asks for -- helm.sh/hook: post-install,pre-upgrade, hook-weight 0, running the platform image with args ['python','manage.py','migrate','--noinput']. So CAP-9 adds a Job at weight -1 and flips this one to migrate --fake. Story 12.1's chart contract stays intact; the 'chart rewrite' risk convergence flagged is retired.
- (note) Independent corroboration: migrate-job.yaml's header rejects initContainers because helm install --wait deadlocks migration-gated readiness (pods wait on migration, hook waits on pods). Phase-2 research rejected them because N replicas contend on DATABASECHANGELOGLOCK, default wait 5 min. Two unrelated arguments, same answer.
- (decision) SPEC constraint corrected: 'every dependency resolves from conda-forge' now reads 'every Python/pixi dependency', with the image boundary named explicitly. The unqualified form contradicted CAP-6, which the repo already relies on.
- (note) Vehicle options collapse: (A) conda feedstock -> platform pixi env -> second hook Job on the EXISTING image, no new image, no new supply path, apache-tika is the exact recipe shape and openjdk 25.0.2 clears the Java 17+ floor. (B) upstream liquibase image -> undocumented third-party mirroring path AND still builds a derived image because 5.x dropped the bundled JDBC driver. (C) self-built image -> needs the conda package first, so it is A plus redundant work. B's only advantage was avoiding feedstock work and the 5.x driver change removes it.
- (decision) Operator ruling -- liquibase-delivery-vehicle RESOLVED TO FEEDSTOCK. Sixth new recipe in this chain alongside CAP-13's five. openjdk 25.0.2 clears Java 17+, apache-tika is the recipe shape, PostgreSQL JDBC driver must be VENDORED into the recipe (5.0 Community dropped it, LPM fetches over network). Rule 1: that story's dev session invokes conda-forge-expert. Consequence: CAP-9 and CAP-13 both open with packaging work, not platform work -- the epic pass must honour that ordering.
- (decision) Operator ruling -- adjacent defects: fix the cheap ones now rather than filing them.
- (event) FIXED adjacent defect (a): air-gap-parity CI false-green. sidecar-deployment.yaml has NO enabled guard so helm install always renders the DB-GPT sidecar; the job never loaded platform-dbgpt-sidecar, waited only on job/platform-migrate, and curled the web Service via port-forward -- so the sidecar sat in ImagePullBackOff reaching for a registry and the job reported green, inside the gate whose criterion is 'any external reference is a FAILING check, not a warning'. Fix: build the sidecar in build-platform-image, export both images in the shared artifact, load+retag+kind load it, --set sidecar.image.tag, PLUS a class-level assertion step that no pod is waiting on an image pull (60s retry for cold-node ErrImagePull). Scoped to air-gap-parity only: a registry pull in gke/ocp portability smokes is not a criterion violation there.
- (note) SPEC surface gained recipes/liquibase/**; open_questions down to 4 (liquibase-7791-fixed, mcp-client-revision, mcp-tasks-runtime, console-parity-inventory).
- (event) FIXED adjacent defect (b): spec-12-3 recovered VERBATIM and promoted to planning-artifacts/specs/. Two byte-identical on-disk copies survived (md5 c3d6b466, 24886 bytes, 304 lines) in an un-torn-down bmad-loop worktree plus the session scratchpad, independently corroborated by replaying the original Write+Edit tool calls from the Claude Code transcript -- the replay reproduced the body exactly, diverging only on four frontmatter lines set by later status-flip edits. Never git-tracked, so this was a MISSED PROMOTION, not a deletion.
- (note) MUCH LARGER FINDING behind that one file: a sweep of every surviving Tier-3 scratch location found 94 distinct story specs on disk, of which 56 are still untracked in ANY project after promoting 12-3. Split by worktree symlink (definitive attribution): pyforge-steward 26 (epics 5,6,7,8,9,10,11) and pyforge-mason 30 (epics 2,3,4,5). Two un-torn-down worktrees hold them: agent-a5ff69efab2b6e64d -> projects/pyforge-steward/implementation-artifacts, agent-a5b67853df48d95b6 -> projects/pyforge-mason/implementation-artifacts.
- (note) Three specs have DIVERGENT copies across locations (spec-7-2-the-eligibility-union-carries-its-provenance, spec-8-1-the-convention-is-written-and-guarded, spec-9-1-the-repos-ci-is-consumable-via-workflow-call) -- those are already tracked somewhere, but any promotion sweep must reconcile rather than blind-copy. Also three scratchpad-only oddities with malformed names (spec-3-1.md, spec-16-1-work.md, spec-16-1-the-from-scratch-run-is-a-chartered-capability.md) needing triage, not bulk copy.
- (note) The worktrees are the ONLY surviving copy and are gitignored -- any cleanup that removes .claude/worktrees/ destroys 56 story specs permanently. This is the exact loss CLAUDE.md documents (pyforge-warden lost 13 of 31; pyforge-atlas 30 of 32 unrecoverable).
- (event) Promotion sweep widened and completed: 119 story specs promoted total. 56 from two un-torn-down worktrees (steward 26 epics 5-11, mason 30 epics 2-5) + 63 status:done specs from the projects' own gitignored implementation-artifacts/ (atlas 8, doctor 8, herald 8, marshal 39). All copied verbatim, md5-verified, frontmatter-validated. Held back: 2 marshal specs still status:in-review. Discarded as scratch intermediates: spec-3-1.md (12003B vs scribe's tracked 34162B), spec-16-1-work.md, spec-16-1-the-from-scratch-run... (16201B, an earlier draft of atlas's tracked spec-17-1 at 17088B, saved under a wrong 16-1 filename).
- (event) CONVERGENCE ERROR found and corrected: this chain asserted 'no Wagtail anywhere'. FALSE. Atlas owns docs/dreams/wagtail-corporate-brain.md (status: specified) and spec-wagtail-corporate-brain CAP-1..3, with Epic 16 stories 16.1/16.2 DONE -- pyforge/atlas/factory/lasuite.py ships LaSuiteConfig, resolve_lasuite_config, LaSuiteClient and WikiSyncer, plus tools/lasuite_bringup.py. The claim was true of src/platform/ and wrongly generalised to the repo; the original grep dismissed non-platform wagtail hits as warden test fixtures and atlas's client never matched the term.
- (note) Two live consequences, neither of which changes CAP-2's BUILD (no Wagtail server runs anywhere, nothing is mounted at /): (1) atlas's DW-H3 is an attended bring-up of a live La Suite/Wagtail instance -- CAP-2 may satisfy it outright instead of the estate running two; (2) LaSuiteClient froze a LA SUITE DOCS REST contract (POST /api/v1/documents/, PATCH/GET /api/v1/documents/{id}/, GET /api/v1/documents/all/, Bearer auth) which is NOT Wagtail's own API, so atlas's phrase 'Wagtail REST shape' must not be read as a guarantee that a Wagtail instance satisfies that client unmodified. New open question lane1-serves-dw-h3; new non-goal: this chain does not absorb spec-wagtail-corporate-brain. *(Footnote 2026-08-25: opened here; answered **no** — see decision below. Do not reopen.)*
- (note) Phase-2 research gap this exposes: the django-lasuite investigation went entirely to upstream sources and never checked the repo for existing La Suite prior art. Atlas had a frozen REST contract the whole time. Convergence-before-research, not after.
- (decision) 2026-08-24 operating-model Q2: 5-tier symmetry applies only to 03 capabilities. CAP-15/16 stay in scope for the eight stations; Never: restated from "a station is declared complete on fewer than five tiers" to "an 03 capability…". 01/02 may stay spec+script or spec+skill. Full PRD/epics correct-course not run in this pass.
- (decision) 2026-08-24 operating-model Q3: Steward AppConfig/discovery holds owner, backup, work_class, promotion date; SLA body stays in the 03 BMAD spec; no new SLA service; Marshal does not hold the book. Noted on CAP-1.
- (decision) 2026-08-24 operating-model Q4: traceability is spec_id + git sha + SBOM purl (+ optional work-item id). Jira is a steward adapter. Noted on CAP-8.
- (decision) 2026-08-24 operating-model Q5: measurement (Build League + Balanced Product Scorecard) is estate operating-model, not optional. Rules live in the Unifying Strategy Dream; the board is a sibling Dream (no CAP-18). WFT defined no metrics — residual is to write the measure set.
- (decision) 2026-08-24 operating-model Q6: Tachyon is a production LLM provider adapter (OpenAI-compatible), not Path B. Path B remains Agent Canopy + CAP-16 personas.
- (decision) 2026-08-24 operating-model Q7: HTMX portals, FastAPI station compute, DRF on Atlas/data-models only. No Lane 2 BFF. Noted on CAP-3.
- (decision) 2026-08-24 operating-model Q8: Warden is the sole PR gate; scanners are Kedro-shaped hook plugins. One verdict for the scorecard. SPEC Always/Never added. Q1–Q8 closed.
- (decision) 2026-08-24 hooks/plugins elevated to architecture principle (canopy:AD-21): as far as possible every layer is replaceable — process-owned hook specs, plugins without a fork. Kedro names the split; not a Kedro-project mandate. Q8 is the PR-gate instance. Contracts (Pixi names, Golden Path artifact, parent infra kinds, host import boundary, Warden verdict) are not plugin surfaces.
- (decision) 2026-08-24 Q5 residual: scorecard draft deferred; will cover human + agent + team metrics. Do not invent measures in this pass.
- (event) 2026-08-24 companions aligned to operating-model Q1–Q8: convergence residual 20 scoped to 03; architecture-diagrams CAP-15/16 + CAP-8 envelope + HTMX note; stack.md Lane-2-is-not-DRF. Steward sprint-change-proposal-2026-08-24-operating-model.md drafted; PRD/epics not edited pending approval.
- (event) 2026-08-24 operating-model SCP **approved**. PRD + epics 18/19/24/29 + spine note applied. Follow-on station correct-course: warden only.
- (decision) CAP-18 minted: one hook-spec + plugin-registration shape in pyforge-core. Not a scorecard. Warden owns PR-gate hook book; stations extract process plugins. Epics 18-30 must not violate; they do not implement this.
- (event) 2026-08-25 drain-bind correct-course (`sprint-change-proposal-2026-08-25-drain-bind.md`, approved and applied). SPEC status ready → in-progress. Residual 1–20 marked landed; no services/ farm; CAP-9 CRC holes + 12-7 /ht/ are the live leftover. Peers: one CAP-18 hook story. Parallel-agent / merge --merge Always lines added.
- (event) 2026-08-25 steward 12-7 closed on CRC: Helm `platform` deployed, Liquibase + migrate --fake Complete, `/ht/` 200. Open question `12-7-live-ht` answered. Verification: spec-12-1-…-verification-2026-08-25.md.
- (decision) 2026-08-25 lane1-serves-dw-h3 answered no: host Wagtail /cms/ is not LaSuiteClient POST /api/v1/documents/ REST. DW-H3 stays atlas attended bring-up. SPEC open_questions empty.
- (decision) 2026-08-25 Q5 measure set parked on docs/dreams/build-league-scorecard.md + spec-build-league-scorecard draft. Not an unbounded Canopy OQ. Never invent metrics.
- (note) 12-7 live /ht/ already answered; Why residual now pip-layer spec-platform-image-one-pixi-env, parked Q5, RFC-5 leftover DDL.
- (event) 2026-08-26 canopy closeout (`sprint-change-proposal-2026-08-26-canopy-closeout.md`, approved and applied). SPEC shipped; Dream realized; PRD shipped. CAP-2 `/` 200 + `/cms/` 302 to IdP on CRC; CAP-9 `platform_app` DML-only (`/api/health` 200, CREATE refused). `:17`–`:19` EXECUTED. Isolated `mfa` sqlmigrate stays the only RFC-5 fake. Query-plane CAP not minted. 12.9 ocp-portability-smoke named optional honesty gap (Actions minutes), not a stamp gate.
- (decision) 2026-08-26 operator: Unifying Strategy is evergreen. SPEC may return in-progress. Rebuild of shipped analytical stores is in scope. Do not mint spec-htap-query-plane.
- (capability) CAP-19 one HTAP query plane; stations are clients. Success: read-only attach, named Kedro Parquet, vss on the plane writer, agents not on OLTP DSN, Scribe canopy:FR-36 via GraphStore on the plane.
- (event) 2026-08-26 query-plane correct-course (`sprint-change-proposal-2026-08-26-query-plane.md`, approved and applied). SPEC/PRD in-progress; Dream specified; Epic 34; canopy:AD-22. Open: query-plane-face, query-plane-catalog, query-plane-scribe-cutover (defaults recorded). First dispatch S-34.1.
- (decision) 2026-08-26 django-lasuite retracted as Canopy dep. Host OIDC is django-allauth; chrome is django-pyforge. Feedstock/suite-* stay packaging. Atlas factory/lasuite.py is a remote Docs client, not this package.
- (decision) 2026-08-26 Kedro is optional for extracts; one Atlas Kedro home (canopy:AD-21). Vizro is Lane 3 over BSL after 34.2. vizro-ai deprecated. kedro-mcp wrapped never load-bearing.
- (decision) 2026-08-26 Scribe port is the store port (28.1/28.2 shipped). There is no GraphStore class in pyforge-scribe. CAP-19/34.5 language is the port, not a class name.
- (event) 2026-08-26 evergreen verification: CAP-1..18 match src/platform. CAP-19 bound not built. Living Dream How-to-read + lasuite removal applied. First dispatch 34.1.
- (event) Self-validate Pass 1+2 2026-08-26: CAP-19 intent+success hold; lasuite/Kedro/Vizro/store-port preservation landed in SPEC+stack+spine. OQs unchanged with defaults.
- (note) 2026-08-26 MCP cluster fail-loud is sibling spec-mcp-era-isolation CAP-4 / Epic 35. Not a unifying CAP. Dispatch after 34.1. Slice 3 still parked.
- (event) 2026-08-26 SPEC status ready. Readiness CONCERNS-proceed (implementation-readiness-report-2026-08-26.md). Ledger 34.1 ready-for-dev; 34.2-34.5 backlog. Next: bmad-build spec-34-1.
- (event) 2026-08-26 Epic 34 drained: 34.2 named `query_plane_cache` Parquet; 34.3 plane writer vectors; 34.4 estate DSN gate; 34.5 PlaneGraphStore on `atlas.duckdb`. Ledger epic-34 done. Parked: Mosaic face, Vizro Lane 3, slice 3, vss/postgres extension skipif when cache empty.
- (event) 2026-08-26 Epic 36.1: BSL `estate_cache` model + `load_estate_cache` over canopy:FR-47 Parquet. No new Vizro page (DW-D2). Parked: Vizro page, Mosaic, slice 3.
- (event) 2026-08-29 drift reconcile: `environment.yaml`/`pixi.toml`/`src/platform/{Containerfile,compose/dbgpt/Containerfile,compose/mcp-host/Containerfile}` changed by today's pixi 0.78.0 registry sync (`6ff2c008b4`, regenerates environment.yaml + bumps all 3 Containerfile builder pins), nodejs LTS-floor widen (`583bdf4eb5`, also regenerates environment.yaml), mcp ceiling `>=1.28.1`→`>=2.1.1` (`06fe64f603`, sibling spec-mcp-era-isolation's own subject; this spec's CAP-19/query-plane text unaffected), django/asgiref/channels_redis pin restore (`daa35ee171`). Routine platform-wide infra maintenance this spec's broad `src/platform/**` + pixi.toml + environment.yaml surface already anticipates; no CAP change. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy`.
- (event by claude) Surface reconcile 2026-08-30: 38 files across this spec's `pyforge-atlas/**` wildcard (+3 outside it) drifted since the entry above. All trace to exactly 5 already-landed commits, verified via `git log --oneline -2` per file:
  - `78ccbe4468` (atlas bootstrap-chain fix, not this session): admission.py, dashboard/data.py, datasets/vulnerability_feeds.py, semantic/{__init__,models,query_helpers}.py, tests/dashboard/test_dashboard_dryrun.py, tests/semantic/test_{bsl_metric_parity,maintainer_dimension}.py — typed-Kedro-dataset + Phase G' bootstrap fixes, no CAP-19/query-plane text change.
  - `c4aeaace30` (atlas Story 21.1, PR #932): conf/base/globals.yml, tests/catalog/{conftest,test_conventions,test_override_points}.py, pixi.toml — store-path relocation to `${paths.data_root}/stores/`, already this spec's own CAP-34/query-plane territory (no new CAP).
  - `cfadb82d93` (pyforge-core sole-ownership fix wave, PR #925): query_plane_boot.py, pyforge-core/tests/meta/test_process_sole_ownership.py, pyforge-scribe/src/pyforge/scribe/compile.py — subprocess-port migrations, no behavior change.
  - `a7721dd610` (SKF-marker governance fix, PR #924): tests/meta/test_skf_skill_and_persona.py.
  - Story 21.2 (PR #934, this session): conf/base/catalog.yml, README.md, datasets/{__init__,core_sources,request_datasets,vcs_sources}.py, orchestration/definitions.py, pipelines/{pypi_intelligence,vcs_health}/{nodes,pipeline}.py, tests/datasets/test_{core_sources,pypi_json_request_dataset,request_datasets,vcs_sources}.py, tests/parity/test_parity_complete.py, tests/pipelines/{test_dag_resolves,pypi_intelligence/test_nodes,vcs_health/test_nodes}.py, tools/bootstrap.py — removes the `cf_atlas.db` SQLite seed dependency from 5 dataset classes; full record in `spec-21-2-remove-cf-atlas-db-seeds-from-production-datasets.md`.
  None alter this spec's own CAP-19/query-plane/unifying-strategy text. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy`.
- (event by claude) Surface reconcile 2026-08-30, second pass: `pixi.toml` changed once more by `48ddb34b22` ("fix local recipes", already-landed) — routine dependency-pin bumps (python-crontab, asgiref, pyarrow, age) plus enabling `ppt-master`/`codegraph` recipe candidates. No CAP-19/query-plane touch. Scoped-stamped.
- (event by claude) Surface reconcile 2026-08-31: 65 drift findings (42 `pyforge-atlas/**`, 21 `pyforge-scribe/**`, 2 `pixi.toml`), verified via `git log --oneline 48ddb34b22..HEAD -- src/shared/packages/pyforge-atlas src/shared/packages/pyforge-scribe pixi.toml` (19 commits). All trace to already-decomposed, already-`done` story work, none altering this spec's own CAP-19/query-plane/unifying-strategy text:
  - **Atlas Epic 21 close-out** (Stories 21.4 Tier-1 catalog sources, 21.5 Tier-2 catalog sources, 21.6 identity-join-and-export-parquet, 21.8 end-to-end verification gate) — 21.6 landed this session via the rescued `dispatch/pyforge-atlas/21.6` branch, including this session's own merge-conflict resolution and the follow-up `9ee122a345` fix (`seed_root` calibration drift in `tests/catalog/conftest.py`, unrelated to any CAP here — a test-fixture path-resolution bug fix). Already reconciled in detail against `spec-atlas-kedro-catalog-expansion`'s own memlog (same date).
  - **Atlas Story 23.8** — `inventory_universe` catalog entry (CAP-8 there, already-anticipated).
  - **Scribe Epic 6** (Stories 6.1 graphify extra, 6.2 cocoindex extra, 6.3 graph-node staleness flag) — the `extras/`, `graph_store_pg.py`/`graph_store_plane.py`, `recall.py`, `compile.py`, `models.py`, `cli.py` changes plus their test files. Fully covered by this session's own `retro-pyforge-scribe-2026-08-31.md` (Epic 6 retrospective, verdict accepted, 313/1 tests) — not re-litigated here.
  - `pixi.toml`: `d230e7fce2` (token-economy dep activation, unrelated) and `5017329a9f` (already reconciled above under `spec-atlas-kedro-catalog-expansion` — the `artifactory_downloads` bootstrap-task addition, Story-21.6-caused).
  This spec's broad `pyforge-atlas/**`/`pyforge-scribe/**` wildcards mean it co-governs the same landed work several narrower specs already record in detail (`spec-atlas-kedro-catalog-expansion`, `spec-21-6-...`, scribe's own retro) — this entry cross-references rather than duplicating that detail. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy`.
- (event) Surface reconcile 2026-09-01: overnight drain landings for atlas Story 23.1 (PR #974, `de24e3961e`) and marshal Story 28.7 (PR #975, `86973a5a8d`). Seventeen governed paths under this spec's `pyforge-atlas/**` and `pixi.toml` wildcards drifted; all trace to already-landed, already-`done` story work — none alter this spec's own CAP-19/query-plane/unifying-strategy text:
  - **Atlas Story 23.1** (`spec-23-1-tier-3-bulk-os-indexes`, tier-3 bulk OS indexes): `src/shared/packages/pyforge-atlas/conf/base/catalog.yml`, `src/shared/packages/pyforge-atlas/conf/base/globals.yml`, `src/shared/packages/pyforge-atlas/conf/base/parameters.yml`, `src/shared/packages/pyforge-atlas/src/pyforge/atlas/datasets/__init__.py`, `src/shared/packages/pyforge-atlas/src/pyforge/atlas/datasets/tier3_sources.py` (added), `src/shared/packages/pyforge-atlas/src/pyforge/atlas/pipelines/pypi_intelligence/nodes.py`, `src/shared/packages/pyforge-atlas/src/pyforge/atlas/pipelines/pypi_intelligence/pipeline.py`, `src/shared/packages/pyforge-atlas/tests/catalog/conftest.py`, `src/shared/packages/pyforge-atlas/tests/catalog/test_override_points.py`, `src/shared/packages/pyforge-atlas/tests/catalog/test_scale_floors.py`, `src/shared/packages/pyforge-atlas/tests/datasets/test_tier3_sources.py` (added), `src/shared/packages/pyforge-atlas/tests/parity/fixtures/pypi_intelligence/flag_tier3_channels.json` (added), `src/shared/packages/pyforge-atlas/tests/parity/harness.py`, `src/shared/packages/pyforge-atlas/tests/parity/test_parity_complete.py`, `src/shared/packages/pyforge-atlas/tests/parity/test_parity_pypi_intelligence.py`, `src/shared/packages/pyforge-atlas/tests/pipelines/pypi_intelligence/test_nodes.py`, `src/shared/packages/pyforge-atlas/tests/pipelines/test_dag_resolves.py`.
  - **Marshal Story 28.7** (`spec-28-7-index-freshness-is-an-advisory-finding`, index freshness advisory): `pixi.toml` — new `[feature.local-recipes.tasks.index-freshness-check]` detector task only; `environment.yaml` re-exported and confirmed byte-identical. Cross-references marshal's own spec memlog for CAP-10 detail; this entry reconciles umbrella surface drift only. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy`.
- (event by claude) Surface reconcile 2026-09-01, second pass: `[feature.pyforge-ci.dependencies]` gained `"ruamel.yaml"` only — pyforge-ci test wiring, no unifying-strategy CAP touch. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 36 governed path(s) drifted against this spec's recorded baseline (12 added, 24 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `pixi.toml` (changed)
  - `recipes/openfeature-flagd-api/recipe.yaml` (changed)
  - `recipes/openfeature-flagd-core/recipe.yaml` (changed)
  - `recipes/openfeature-provider-flagd/recipe.yaml` (changed)
  - `recipes/openfeature-sdk/recipe.yaml` (changed)
  - `src/shared/packages/pyforge-atlas/README.md` (changed)
  - `src/shared/packages/pyforge-atlas/conf/base/catalog.yml` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/artifactory/__init__.py` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/artifactory/aql_adapter.py` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/dashboard/app.py` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/dashboard/data.py` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/dashboard/identity_gist.py` (added)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/orchestration/definitions.py` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/pipelines/artifactory_downloads/nodes.py` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/pipelines/artifactory_downloads/pipeline.py` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/pipelines/derived_artifacts/nodes.py` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/pipelines/derived_artifacts/pipeline.py` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/semantic/metrics.py` (changed)
  - `src/shared/packages/pyforge-atlas/src/pyforge/atlas/semantic/models.py` (changed)
  - `src/shared/packages/pyforge-atlas/tests/artifactory/test_aql_adapter.py` (changed)
  - `src/shared/packages/pyforge-atlas/tests/catalog/conftest.py` (changed)
  - `src/shared/packages/pyforge-atlas/tests/dashboard/conftest.py` (changed)
  - `src/shared/packages/pyforge-atlas/tests/dashboard/test_dashboard_dryrun.py` (changed)
  - `src/shared/packages/pyforge-atlas/tests/dashboard/test_identity_catalog_page.py` (added)
  - `src/shared/packages/pyforge-atlas/tests/dashboard/test_identity_gist_markdown.py` (added)
  - `src/shared/packages/pyforge-atlas/tests/dashboard/test_identity_ops_page.py` (added)
  - `src/shared/packages/pyforge-atlas/tests/dashboard/test_identity_parity.py` (added)
  - `src/shared/packages/pyforge-atlas/tests/dashboard/test_identity_workbook_page.py` (added)
  - `src/shared/packages/pyforge-atlas/tests/dashboard/test_zero_deferred_e2e_gate.py` (added)
  - `src/shared/packages/pyforge-atlas/tests/fixtures/inventory_identity/complete_export_expected.json` (added)
  - `src/shared/packages/pyforge-atlas/tests/pipelines/artifactory_downloads/test_nodes.py` (changed)
  - `src/shared/packages/pyforge-atlas/tests/pipelines/derived_artifacts/test_identity_complete_export.py` (added)
  - `src/shared/packages/pyforge-atlas/tests/pipelines/derived_artifacts/test_inventory_aoss_free_queue.py` (added)
  - `src/shared/packages/pyforge-atlas/tests/pipelines/derived_artifacts/test_inventory_priority_assignments.py` (added)
  - `src/shared/packages/pyforge-atlas/tests/pipelines/derived_artifacts/test_inventory_verified_packages.py` (added)
  - `src/shared/packages/pyforge-atlas/tests/pipelines/test_dag_resolves.py` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (change) **Story 42.2 — agent rate limits and run bounds** (CAP-11 / CAP-17, red-team A-6 / B-7 → directive R-8). Nothing rate-limited `POST /stations/<name>/mcp` or supervisor `start`, and `RunState` had no retention, so an agent loop was a platform-wide DoS with no backpressure. This entry records the surface this spec governs; the CAP text is unchanged (R-8 was already bound by CAP-11's correct-course of 2026-09-02). Governed paths:
  - `src/shared/packages/django-pyforge/src/django_pyforge/models.py` (changed) — `RunState.subject` + `celery_task_id`, a `cancelled` status, and two named bound indexes.
  - `src/shared/packages/django-pyforge/src/django_pyforge/supervisor.py` (changed) — three bounds in front of the write (per-subject token bucket, per-station queue-depth ceiling → 429, per-subject concurrent-run ceiling → 409), plus `revoke_subject` and `prune_run_state`. Still the only writer of `run_state`/`mcp_handles` (canopy:AD-12).
  - `src/shared/packages/django-pyforge/src/django_pyforge/tasks.py` (changed) — `enqueue_supervised_run` publishes with a pre-minted `task_id` and a `sub` message header, so a merely-queued task is revocable; `prune_run_state_task` is the retention sweep.
  - `src/shared/packages/django-warden/src/django_warden_fabric/views.py` (changed) — the portal `start` face refuses a run bound with the status the bound declares, rather than inventing its own.
  - `src/shared/packages/django-pyforge/src/django_pyforge/mcp_start_get.py` and `src/shared/packages/django-warden/src/django_warden_fabric/mcp_asgi.py` (changed, review pass) — both MCP `start` tools go through `start_bounded`, so a bound reaches an agent as the same projected refusal the portal renders. The SDK withholds the text of any exception that is not its own `ToolError`, so a bare `publish_start` here delivered `Error executing tool start_audit` and nothing else — no status, no `retry_after`, no `run_ids`.
  - `src/shared/packages/django-pyforge/src/django_pyforge/rate_limit.py` (added) — the token bucket. State is in `CACHES["default"]` (redis-cache, canopy:AD-10), never on the `noeviction` broker whose exhaustion the bound exists to prevent, and the limiter fails CLOSED with an `ERROR` log when that store does not answer.
  - `src/shared/packages/django-pyforge/src/django_pyforge/migrations/0004_run_bounds.py`, `management/commands/revoke_subject.py`, `management/commands/prune_run_state.py` (added).
  Also outside this spec's globs but part of the same change: `src/platform/db/changelog/changes/python-agent-platform-20-django-pyforge-0004-run-bounds.sql` + its `sqlmigrate-map.yaml` entry (CAP-9 / AD-9 — the extraction gate passes), the settings block in `config/settings/base.py`, and `pyforge steward revoke --sub <id>` in `pyforge-steward`. **Scoped stamp pending:** `--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy` must run from a CLEAN worktree after this story's commit lands, so the recorded hashes are committed content and not uncommitted working-tree bytes — it is deliberately not run from the dispatch worktree. **Read the residual honestly:** moving this memlog downgrades EVERY pending drift on this spec from `fail` to `drift-presumed: warn`, including ~60 paths this entry does not speak for — the already-landed Stories 40.1, 41.2–41.4 and 42.1 (`assertion/**`, `events/**`, `mcp_auth.py`, `mcp_http.py`, `config/**`, `deploy/**`), whose baseline lag predates this story and is unreconciled here. The stamp is therefore a deliberate operator act over that whole set, not something this entry has already justified.
- (change) **Story 42.3 — bus delivery semantics and a deployed consumer** (CAP-8 / canopy:FR-20, canopy:AD-8, RFC-4, red-team A-1 / A-2 / A-4 / A-5 → directive R-9). The fabric had a producer, one event type and no listener; `_apply` retried a failing well-formed event immediately and forever; `harvest_poison` quarantined only unparseable entries and its default `min_idle_time=0` stole live messages; the envelope carried no `traceparent`. This entry records the surface this spec governs; the CAP text is unchanged (R-9 was already bound by CAP-8's 2026-09-02 correct-course — "a well-formed failing event reaches the DLQ after N attempts with backoff; at least one consumer Deployment exists per subscribing station; `traceparent` rides the envelope"). Governed paths:
  - `src/shared/packages/django-pyforge/src/django_pyforge/events/fabric.py` (changed) — attempts are the XPENDING delivery counter (XCLAIM re-delivers, which is what advances it), exponential backoff `base·2^(k-1)` capped (1s/2s/4s/8s by default), DLQ after `EVENT_MAX_ATTEMPTS` with `reason`/`error`/`attempts`/`group`/`stream_id` and only then ACK; unparseable entries quarantined on first sight; `harvest_poison` defaults `min_idle_time` to the handler timeout, quarantines the unparseable and the exhausted, and leaves the rest pending under the harvester for its retry pass. The last handler error is recorded per stream id so a later harvest never writes a DLQ row without it.
  - `src/shared/packages/django-pyforge/src/django_pyforge/events/constants.py` (changed) — the Warden → Doctor → Mason vocabulary (`recipe.audit.failed`, `remedy.requested`, `recipe.rebuild.requested`, `remedy.completed`), each with a `dataschema` URN, the per-station `SUBSCRIPTIONS`, the `traceparent` extension, and the retry/backoff/harvest knobs (`DJANGO_PYFORGE_EVENT_*`).
  - `src/shared/packages/django-pyforge/src/django_pyforge/events/adapters.py` (changed) — one adapter per type, a registry (`register_adapter` / `adapter_for`), and `station_handler(station)`, which routes subscribed types and ACKs the rest untouched.
  - `src/shared/packages/django-pyforge/src/django_pyforge/events/tracing.py` (added) — W3C `traceparent` parse/carry: contextvar always, OpenTelemetry context and structlog contextvars when present; `trace_headers()` for Celery.
  - `src/shared/packages/django-pyforge/src/django_pyforge/events/memory.py` (changed) — the test double's pending idle times are real milliseconds plus `advance_ms`, `xpending_range` filters by consumer, `xclaim` takes a list (redis-py's shape; the old positional form raised `DataError` against a real server).
  - `src/shared/packages/django-pyforge/src/django_pyforge/management/commands/consume_events.py` (added) — the runner: `--station <token>` (= consumer group), `--once`, `--interval`, `--harvest-every`; SIGTERM finishes the pass.
  - `src/shared/packages/django-pyforge/src/django_pyforge/tasks.py` (changed) — `enqueue_supervised_run` adds the `traceparent` header next to `sub`; a task published with one re-binds it into structlog context on start (django-structlog's `bind_extra_task_metadata`).
  - `src/platform/deploy/charts/platform/templates/consume-events-deployment.yaml` (added), `values.yaml` `events:` block, `_helpers.tpl` (`platform.consumeEvents.fullname`), `redis-networkpolicy.yaml` (consumer components admitted) — one Deployment per station in `events.consumers` (default doctor, mason), platform image, the worker's env/contexts/volumes.
  - `src/platform/tests/test_cloudevents_redis_broker.py` (extended, incl. a real `redis-server` proof) and `src/platform/tests/test_chart_invariants.py` (consume-events proofs + ungated guard companion).
  **Scoped stamp pending:** `--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy` must run from a CLEAN worktree after this story lands, never from the dispatch worktree — same residual as Story 42.2's entry, whose ~60-path lag (40.1, 41.2–41.4, 42.1, 42.2) this entry also does not reconcile.
- (note by claude) 2026-09-05 drift-presumed reconciliation (branch maint/marshal-docs-reground-2026-09-05): the spec-surface detector reported these governed paths as changed/added while this memlog moved without naming them. Verified each against `git log -1` — every path was landed by a story commit on main within this spec's declared surface; none is an out-of-band edit. Named here so the presumption is discharged; scoped --write-baseline follows. Paths by landing commit — 3a9030c8bb (2026-09-02, steward: Story 41.4 follow-up review — whitespace, hostname checki): src/platform/config/broker_tls.py [added], src/platform/config/startup/stage_one.py [changed], src/platform/tests/test_startup_required_settings.py [changed] ; bb94a7138b (2026-09-02, steward: Story 41.4 — broker TLS is verified): src/platform/config/observability/__init__.py [changed], src/platform/config/startup/__init__.py [changed] ; 27d9e15747 (2026-09-02, steward: Story 41.4 review pass — capath, the composed URL, and a): src/platform/config/settings/production.py [changed] ; 3dbfeedfb5 (2026-09-02, steward: WIP 42.2 — rate limits, run bounds, and revoke (poll chec): src/platform/conftest.py [changed], src/shared/packages/django-pyforge/src/django_pyforge/management/commands/prune_run_state.py [added], src/shared/packages/django-pyforge/src/django_pyforge/management/commands/revoke_subject.py [added], src/shared/packages/django-pyforge/src/django_pyforge/mcp_auth.py [added] ; 797fde1f0b (2026-09-02, steward: Story 41.3 — scribe's DDL moves into the Liquibase change): src/platform/db/changelog/changes/pyforge-scribe-1-pgvector-extension.sql [added], src/platform/db/changelog/changes/pyforge-scribe-2-graph-nodes.sql [added], src/platform/db/changelog/changes/pyforge-scribe-3-app-role-grants.sql [added] ; c2ddf90422 (2026-09-02, steward: Story 41.3 follow-up review — tighten the governance gate): src/platform/db/README.md [added], src/platform/db/changelog/changes/pyforge-scribe-4-graph-nodes-stale-column.sql [added], src/platform/db/changelog/db.changelog-master.yaml [changed], src/platform/db/create_app_role.sql [changed], src/platform/db/sqlmigrate-map.yaml [changed], src/shared/packages/pyforge-scribe/src/pyforge/scribe/graph_store_pg.py [changed], src/shared/packages/pyforge-scribe/tests/unit/conftest.py [changed] ; 1cbcc78637 (2026-09-02, steward: Story 42.1 — MCP transport authorization and a streaming): src/platform/deploy/charts/platform/templates/mcp-host-networkpolicy.yaml [added], src/shared/packages/django-pyforge/src/django_pyforge/assertion/crypto.py [changed], src/shared/packages/django-pyforge/src/django_pyforge/assertion/verify.py [added] ; eb70f9d78e (2026-09-02, marshal: supervisor finalize (Story 28.24)): src/platform/deploy/DR.md [added], src/platform/deploy/charts/platform/templates/postgres-backup-cronjob.yaml [added], src/platform/deploy/charts/platform/templates/postgres-backup-pvc.yaml [added], src/platform/deploy/restore.md [added] ; c8988f5799 (2026-09-02, steward: fix Story 41.1 backup chart defects and close its ledger ro): src/platform/deploy/charts/platform/templates/postgres-statefulset.yaml [changed] ; 3b59d0ec38 (2026-09-02, Make redis-broker durable and bounded (Story 40.2).): src/platform/deploy/charts/platform/templates/redis-broker-pvc.yaml [added], src/platform/deploy/charts/platform/templates/redis-deployment.yaml [changed], src/platform/deploy/overlays/ocp/cluster-bringup.md [changed] ; ec5aed9bb8 (2026-09-02, steward: Story 42.2 follow-up review — off-loop limiter, terminal-): src/platform/tests/test_agent_rate_limits_and_run_bounds.py [added], src/platform/tests/test_start_get_survives_disconnect.py [changed], src/shared/packages/django-pyforge/src/django_pyforge/mcp_http.py [changed] ; 6247c168ee (2026-09-02, steward: Story 42.1 follow-up review — widen the sidecar hop's fai): src/platform/tests/test_mcp_transport_auth.py [added] ; 41a4bbffa8 (2026-09-02, marshal: supervisor finalize (Story 28.24)): src/shared/packages/django-pyforge/src/django_pyforge/assertion/exceptions.py [changed] ; 43915f49dc (2026-09-02, marshal: supervisor finalize (Story 28.24)): src/shared/packages/django-pyforge/src/django_pyforge/assertion/identity.py [changed], src/shared/packages/django-pyforge/src/django_pyforge/assertion/jwks.py [added] ; a7215e2706 (2026-09-02, Enforce query-plane process boundary (Story 41.2).): src/shared/packages/pyforge-atlas/src/pyforge/atlas/duckdb_writer.py [changed] ; f518ec9a50 (2026-09-02, steward(41.2): bmad-build-auto review pass 14 — strict read_only A): src/shared/packages/pyforge-atlas/tests/test_duckdb_boundary.py [added] ; 989df688c0 (2026-09-02, steward(41.2): tighten read-only plane open verification (review pas): src/shared/packages/pyforge-scribe/src/pyforge/scribe/graph_store_plane.py [changed], src/shared/packages/pyforge-scribe/tests/unit/test_graph_store_plane.py [changed] ; 3c249d6f0f (2026-09-02, steward: Story 41.3 review pass — the `stale` back-fill returns as): src/shared/packages/pyforge-scribe/tests/unit/test_graph_store_pg.py [changed] ; 0aedddcdfc (2026-09-03, steward: close Story 43.2 station API contract (review patches)): src/platform/config/asgi.py [changed], src/platform/tests/test_langflow_mount.py [changed] ; ed1cb49be3 (2026-09-03, steward: WIP 42.4 — Celery hardening, beat, and builds pool (poll): src/platform/deploy/charts/platform/templates/beat-deployment.yaml [added], src/platform/deploy/charts/platform/templates/redis-networkpolicy.yaml [changed], src/platform/deploy/charts/platform/templates/worker-builds-deployment.yaml [added], src/platform/deploy/charts/platform/templates/worker-deployment.yaml [changed] ; 5396c1917a (2026-09-03, feat(steward): golden-path CD by digest (story 43.4)): src/platform/deploy/charts/platform/templates/_helpers.tpl [changed] ; 328d870656 (2026-09-03, steward: in-process station port, no self-call (Story 43.3)): src/platform/platformapp/users/apps.py [changed], src/shared/packages/django-atlas/src/django_atlas_portal/views.py [changed], src/shared/packages/django-pyforge/src/django_pyforge/non_atomic.py [added], src/shared/packages/django-pyforge/src/django_pyforge/station_client.py [added], src/shared/packages/django-pyforge/src/django_pyforge/station_port.py [added], src/shared/packages/pyforge-core/src/pyforge/core/station_port.py [added], src/shared/packages/pyforge-core/tests/unit/test_station_port.py [added] ; f885eeef07 (2026-09-03, steward: close Story 42.4 — Celery hardening review pass): src/platform/tests/test_celery_hardening_and_builds_pool.py [added] ; 5af099d33c (2026-09-03, Story 42.5: prefixed role namespaces and tenant claim on runs/events): src/platform/tests/test_idp_revoke_next_request.py [changed], src/platform/tests/test_role_namespaces.py [added], src/platform/tests/test_validation_errors_render_inline.py [changed], src/shared/packages/django-atlas/src/django_atlas_portal/board.py [changed], src/shared/packages/django-pyforge/src/django_pyforge/assertion/views.py [changed], src/shared/packages/django-pyforge/src/django_pyforge/events/__init__.py [changed], src/shared/packages/django-pyforge/src/django_pyforge/migrations/0005_run_state_tenant.py [added], src/shared/packages/django-pyforge/src/django_pyforge/roles.py [changed] ; c7348a03d9 (2026-09-03, fix(story-43.2): follow-up review patches for station API contract): src/platform/tests/test_station_api_host_dispatch.py [added] ; 54a98242c3 (2026-09-03, steward: Story 42.3 — bus delivery semantics and a deployed consum): src/shared/packages/django-pyforge/src/django_pyforge/management/commands/list_event_dlq.py [changed] ; 4922e5f456 (2026-09-03, marshal: supervisor finalize (Story 28.24)): src/shared/packages/django-pyforge/src/django_pyforge/management/commands/sweep_lost_runs.py [added], src/shared/packages/django-pyforge/src/django_pyforge/queues.py [added] ; 40e8b578da (2026-09-03, steward: WIP 43.2 — station API contract seam and typed client): src/shared/packages/pyforge-core/src/pyforge/core/client.py [added], src/shared/packages/pyforge-core/tests/unit/test_client.py [added] ; 8aae8e2ef0 (2026-09-04, ci: fix every pre-existing Platform CI + Detectors failure the Actio): src/platform/config/settings/test.py [changed], src/platform/config/station_port.py [added], src/platform/db/changelog/changes/python-agent-platform-21-django-pyforge-0005-run-state-tenant.sql [added], src/platform/db/postgres_backup.py [added], src/platform/mcp_host/app.py [changed], src/platform/platformapp/front_door/lane1_seed.py [changed], src/platform/platformapp/front_door/runtime_catalog.py [changed], src/platform/platformapp/users/forms.py [changed], src/platform/tests/policy/test_liquibase_channel_policy.py [changed], src/platform/tests/policy/test_openfeature_channel_policy.py [changed], src/platform/tests/test_console_parity_homes.py [changed], src/platform/tests/test_estate_dsn_is_plane.py [changed], src/platform/tests/test_front_door_publish.py [changed], src/platform/tests/test_herald_portal_deck_status.py [changed], src/platform/tests/test_liquibase_update.py [changed], src/platform/tests/test_marshal_loop_homes_portal.py [changed], src/platform/tests/test_restarts_reconcile.py [changed], src/platform/tests/test_station_api_seam.py [added], src/platform/tests/test_station_portal_shells.py [changed], src/shared/packages/django-pyforge/src/django_pyforge/assertion/client.py [changed] ; c9bcbaf516 (2026-09-04, tests: consolidate the fleet's 23 whole-branch diff-guard bodies ont): src/platform/tests/test_warden_portal_audit_start_get.py [changed], src/shared/packages/pyforge-atlas/tests/meta/test_skf_skill_and_persona.py [changed], src/shared/packages/pyforge-scribe/tests/meta/test_first_portal_slice.py [changed], src/shared/packages/pyforge-scribe/tests/meta/test_skf_skill_ownership.py [changed], src/shared/packages/pyforge-scribe/tests/meta/test_station_persona.py [changed] ; 70a34b4ee6 (2026-09-05, fix(steward): stop .dockerignore's config.toml negation from forcing): src/platform/Containerfile [changed], src/platform/compose/dbgpt/Containerfile [changed], src/platform/compose/mcp-host/Containerfile [changed] ; 3435298657 (2026-09-05, ci(platform): gate ruff format --check in Platform CI's test job): src/platform/config/authorization/adapters.py [changed], src/platform/config/authorization/claims.py [changed], src/platform/config/authorization/mapper.py [changed], src/platform/config/local_dev/keys.py [changed], src/platform/config/local_dev/mint.py [changed], src/platform/config/local_dev/tokens.py [changed], src/platform/config/settings/base.py [changed], src/platform/config/settings/local.py [changed], src/platform/config/station_api.py [added], src/platform/config/urls.py [changed], src/platform/ingest/github_projects/graphql.py [changed], src/platform/ingest/github_projects/source.py [changed], src/platform/ingest/github_projects/test_github_metrics_dlt.py [changed], src/platform/manage.py [changed], src/platform/platformapp/front_door/views.py [changed], src/platform/platformapp/users/provisioning.py [changed], src/platform/platformapp/users/tests/test_adapters.py [changed], src/platform/platformapp/users/views.py [changed], src/platform/pyproject.toml [changed], src/platform/tests/policy/readers.py [changed], src/platform/tests/policy/test_coverage_policy.py [changed], src/platform/tests/policy/test_credential_surface.py [changed], src/platform/tests/policy/test_non_atomic_views.py [added], src/platform/tests/policy/test_typing_policy.py [changed], src/platform/tests/test_atlas_mcp_host.py [changed], src/platform/tests/test_atlas_portal_inventory_row.py [changed], src/platform/tests/test_django_pyforge_assertion.py [changed], src/platform/tests/test_django_pyforge_chrome.py [changed], src/platform/tests/test_host_board_row_isolation.py [changed], src/platform/tests/test_mason_portal_last_diagnose.py [changed], src/platform/tests/test_mcp_host_sidecar.py [changed], src/platform/tests/test_oidc_identity.py [changed], src/platform/tests/test_openfeature_file_flags.py [changed], src/platform/tests/test_postgres_backup.py [added], src/platform/tests/test_seven_mcp_faces.py [changed], src/platform/tests/test_station_port_no_loopback.py [added], src/platform/tests/test_warden_fabric_rename.py [changed] ; bd513e731c (2026-09-05, Merge branch 'refs-main-check' into fix/steward-changeset-gate-and-d): src/platform/db/sqlmigrate_extraction.py [changed], src/platform/tests/policy/test_sqlmigrate_extraction.py [changed] ; 1a78d0eda5 (2026-09-05, docs(steward): correct-course on parent AD-1 / canopy:AD-13 — re-a): src/platform/deploy/charts/platform/templates/NOTES.txt [changed], src/platform/deploy/charts/platform/values.yaml [changed] ; 045534f7a5 (2026-09-05, Merge origin/main into steward/land-followup-reviews-2026-09-05): src/platform/deploy/README.md [changed] ; aea6f5875a (2026-09-05, chore(steward): reconcile the follow-up-review landing — lint, def): src/platform/tests/policy/test_liquibase_ddl_governance.py [changed], src/platform/tests/test_broker_tls_verified.py [added]
- (note by claude) 2026-09-05 hygiene-2 PR (branch maint/hygiene-2-2026-09-05): pixi.toml changed (`lint` task -> CI-parity conda-smithy via pixi exec; no dependency/lock change). ALSO re-stamping five paths PR #1063 left stale on main: environment.yaml, pixi.toml, src/platform/Containerfile, src/platform/compose/dbgpt/Containerfile, src/platform/compose/mcp-host/Containerfile — #1063's scoped stamp for this spec was computed on a branch stacked on pre-#1061 main, then rebased; the merged baseline therefore recorded the pre-sweep bytes of those five files (all changed by #1061's pixi 0.79.0 bump: setup-pixi/Containerfile builder tags + the regenerated environment.yaml), and main's spec-surface verdict has been `drift: fail` on them since #1063 merged. Content-wise those changes are the #1061 sweep already named in this memlog's 2026-09-05 pixi-sweep entry. Scoped --write-baseline follows.
- (note by claude) 2026-09-05 hygiene-2 PR, second pixi.toml touch (same branch): the conda-smithy `lint` task now exports GH_TOKEN from `gh auth token` when unset (the CalVer linter's --conda-forge mode calls GitHub per recipe; anonymous is 60 req/h) and its description records the corpus runtime. No dependency, env, or lock change. Scoped --write-baseline follows.
- (note) Cross-station surface touch (marshal Story 30.2, 2026-09-06): src/shared/packages/pyforge-atlas/AGENTS.md (within this spec's broad surface glob) gained a bmad:context managed block migrating genuinely unique content out of the retired pyforge-atlas project-context.md rulebook (BMAD 6.12 persistent_facts=[] surface migration, D1) -- unrelated to the Django/platform cutover itself. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (event) Surface reconcile 2026-09-07 (fleet hygiene, pyforge-steward Story 46.3, spec-bmad-suite-lifecycle CAP-4 half): `pixi.toml` changed to add one `local-recipes` pixi task (`tea-test-review`, wrapping the real `tea-test-review` CLI with `--base origin/main --min-score 80` defaults) for TEA provisioning -- unrelated to this spec's own surface, but this spec's blanket `pixi.toml` glob makes it a co-governor of that one file. No product work here. Re-derived the governing set the same way Story 45.2/46.1 did (grep `pixi.toml` in every spec's `surface:` frontmatter) rather than assuming their own six-spec list still holds: it now numbers nine (this spec included), not six -- the surface set has grown since 45.2/46.1 landed. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy`.
- (note by claude) 2026-09-07 drift-presumed reconciliation (CI/hygiene sweep, DW-B2-3): this spec's broad `src/shared/packages/**` surface glob co-governs three pyforge-atlas vulnerability-pipeline files touched by DW-B2-3's fix (`vuln_kev_affecting_current` current-version scoping). Verified: the change is pyforge-atlas product work owned by `pyforge-atlas/spec-pyforge-atlas` (see that spec's own memlog for the substantive entry) and unrelated to this spec's Django/platform-cutover concerns -- an out-of-band edit would not be. Named here so the presumption is discharged. RECONCILES: src/shared/packages/pyforge-atlas/src/pyforge/atlas/pipelines/vulnerability/nodes.py, src/shared/packages/pyforge-atlas/src/pyforge/atlas/pipelines/vulnerability/pipeline.py, src/shared/packages/pyforge-atlas/tests/pipelines/vulnerability/test_nodes.py. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy`.
- (note by claude) Cross-station surface touch (CI/hygiene sweep 2026-09-07 follow-up): src/shared/packages/pyforge-atlas/tests/wasm/test_wasm_smoke.py's _chromium_executable() glob patterns updated to match the currently-pinned Playwright's actual per-arch download layout (see spec-pyforge-atlas's own memlog for the full note). Unrelated to the Unifying Strategy contract itself. Reconciled + baseline re-stamped per the foreign-spec-surface procedure.
- (note) Cross-station reconcile from pyforge-marshal Story 32.2 (spec-fleet-consistency-standard CAP-5): this spec's surface saw pixi.toml gain the pyforge-atlas-test task alias, and pyforge-core / pyforge-scribe pyproject.toml raise requires-python to ">=3.14" (marshal Story 32.2, CAP-5). No steward source change.
- (note) Story 32.5 (spec-fleet-consistency-standard CAP-2): the fleet converged on one test-suite vocabulary -- tests/unit/ + tests/integration/ + tests/meta/ (BMAD 6.12's Python default plus the one addition with 8/8 adoption), with fixtures/ and _support/ never collected. Eight suite names collapsed to three; the coverage gate's suite map now needs no per-station special case. No steward source change beyond the tests-tree move already noted on spec-pyforge-steward.
- (note) Same pixi.toml change as recorded on spec-pyforge-core: governance-currency task, pytest-cov into seven station features, seven per-station coverage tasks. No steward source change.
- (note) Story 32.8 (CI follow-up to 32.5, spec-fleet-consistency-standard CAP-2): atlas's dashboard/, publish/ and wasm/ moved from tests/unit/ to tests/integration/. All three are fail-loud gates needing provisioned prerequisites (Playwright's managed Chromium, the DuckDB httpfs extension, a built WASM artifact) that pyforge-station-tests.yml provisions for its whole-tree run but a coverage lane has no business installing. Integration weight by the same reasoning that placed warden's oracle gates there. Ten pixi tasks that still named pre-move atlas paths were repointed, and the eight test-architecture.md files regenerated from the live inventory. Also: pyforge-steward-five-tier.yml and pyforge-steward-fresh-clone.yml bumped python-version 3.12 -> 3.14. Those two lanes pip-install the pyforge packages OUTSIDE pixi, so CAP-5's requires-python raise made pip refuse -- they were the only thing in the repo still exercising 3.12, correcting CAP-5's own rationale.
- (change) Sweep follow-ups 2026-09-08 -- surface-touch note, no change to this spec own subject. pixi.toml gained one read-only detector task (`ledger-direction-check` -> `python -m pyforge.doctor.sources ledger-direction`); no dependency, channel, platform, environment or pin changed, and `pixi project export conda-environment -e build` is byte-identical to the committed environment.yaml. RECONCILES: pixi.toml. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy.
- (change) Fleet consistency pass 2026-09-08 -- surface-touch note, no change to this spec own subject. The atlas dashboard test named here is co-governed by this spec surface; the edit is an order-independence fix to a pre-existing intermittent assertion, reconciled in full on pyforge-atlas/spec-pyforge-atlas. No Canopy surface changed. RECONCILES: src/shared/packages/pyforge-atlas/tests/integration/dashboard/test_identity_catalog_page.py. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy.
- (change) Fleet consistency pass 2026-09-08 (round 2) -- surface-touch note, no change to this spec own subject. pixi.toml gained one read-only detector task (`ad-citation-check` -> `python scripts/ad_citation_check.py`, architecture-decision citation integrity); no dependency, pin, platform or environment changed, and `pixi project export conda-environment -e build` remains byte-identical to the committed environment.yaml. Reconciled in full on pyforge-marshal/spec-fleet-consistency-standard, which governs the detector. RECONCILES: pixi.toml. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy.
- (event) 2026-09-09 fleet-readiness decision batch approved (research/fleet-readiness-decision-batch-2026-09-09.md, operator AskUserQuestion): Class A+B blocks, C1-C17 station bundles, C4 intelligence-hub draft→ready, C6 Epic 49 widened to twelve station satellites, C7 Epic 48 additions minted. This entry captures the SPEC.md hand-edits that pass applied outside bmad-spec so Story 44.13 can retire the AGENTS.md never-re-derive exception.
- (decision) 2026-09-09 C3: django-accelerator-framework absorbed — daf:CAP-1 (accelerator contract) lands in this SPEC § Absorbed; daf:CAP-2 (templating engine) falsified and closed (nine Django faces reached via PortalConfig, not hand-copy). Owner Dream archived absorbed; mason spec frontmatter absorbed-into this folder.
- (decision) 2026-09-09 currency review (sprint-change-proposal-2026-09-09-currency-review.md): Epics 48–49 bound; CAP-17 seam → spec-marshal-token-economy CAP-18 via Epic 33 correct-course; every capability gains verified: line naming exercised vs fixture-only clauses (49.1).
- (constraint) 2026-09-09 constraints corrected in SPEC: Django >=5.2.17,<6 and Python 3.14.* (43.6; daa35ee171 bump); pap:CAP-5 table row corrected; GraphStore Protocol exists in pyforge-scribe (graph_store.py) — prior 'no GraphStore class' prose superseded in § Residual and CAP-14 notes.
- (decision) 2026-09-09 open_questions: realization-gate-home answered (hub spec-intelligence-hub draft→ready; Epic 49 re-home pending hub bmad-spec re-derive); single-spec-merge-timing answered — Story 48.8 before any 44.x flip; both remain in frontmatter until 48.8 lands done.
- (change) 2026-09-09 § Residual (2026-09-09) hand-edited into SPEC.md: R-18..R-22 → Epic 48 Stories 48.2–48.6; single-spec merge → 48.8; Story 43.7 backlog; six CAPs with unexercised criteria → Epic 49; five currency-review document-tier findings vessel to 48.7 / blocked 44.x / doctor D3; three live-code corrections (GraphStore, BS-5 read_only, BS-6 CloudEvents).
- (event) Story 44.13 memlog reconciliation 2026-09-10: 2026-09-09 fleet-readiness and currency-review hand-edits captured above; AGENTS.md never-re-derive exception retired; full bmad-spec byte-equivalence re-derive of this SPEC deferred to harness-driven batch (memlog now authoritative).
- (decision by claude) 2026-09-10 AD-1 GAINS A DATED, BOUNDED OBJECT-STORAGE EXCEPTION (`bmad-correct-course`, `sprint-change-proposal-2026-09-10-ad-1-object-storage-exception.md`, seeded from `docs/dreams/platform-object-storage-kind.md`). The 2026-09-05 pass (`sprint-change-proposal-2026-09-05-ad-1-reopen.md`) re-affirmed AD-1 for Lane 1 media and explicitly scoped object storage itself out ("Option B, not chosen"). That deferred scenario is now live: NetApp StorageGRID will exist as ops-provided, externally-operated air-gap infrastructure. `stack.md`'s "Never: a fourth backing service (Vault-in-pod, MinIO, extra bus)" was written to stop pyforge from becoming the OPERATOR of a fourth kind, not from ever consuming one it doesn't run itself -- the same distinction `canopy:AD-19` already draws for the external IdP ("no Vault HTTP from the platform image... stays outside the image"). SPEC.md's AD-1 Constraint bullet gains one dated exception bullet immediately after it (hand-edited, this pattern): object storage is permitted CONSUMED via an S3 client only, endpoint/credentials as configuration never hardcoded, never a service pyforge deploys inside its own platform image or Helm chart; Lane 1 media's own RWX-PVC answer is untouched; self-hosting MinIO/Silo/Garage/any object-store server in the deployed platform remains forbidden without a further, separately-justified exception. Decomposed into steward Epic 50 (Stories 50.1-50.3): the Silo (`pgsty/silo`, a maintained MinIO-codebase fork) conda-forge recipe, local-dev pixi tooling (Silo default, Garage alternative -- both verified live: conda-forge feedstock lookup for Garage, real GitHub release assets for Silo showing full Linux/macOS/Windows coverage vs. Garage's Linux/macOS-only), and a minimal S3-client seam proving the exception end-to-end. No consumer (Lane 1 media or otherwise) wired to it in this pass. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy`.
- (note) Cross-station incoming surface claim (marshal Story 33.12, dispatched 2026-09-12, run `pyforge-marshal-20260912T095631807Z-85c4ead2`, `spec-run-state-one-publisher` CAP-1/CAP-2/CAP-4/CAP-5): recorded ahead of landing, per the cross-station surface-claim rule Story 33.4/49.8 already used. Expected edits under this spec's own `django-pyforge/**` and `src/platform/**` surface globs: `src/shared/packages/django-pyforge/src/django_pyforge/supervisor.py` (a held-run publish/heartbeat/complete path alongside the existing Celery-backed `publish_start`, judged by heartbeat age only, never a second `run_state` writer -- CAP-1), whichever module registers the station's MCP tool table (adds `publish_loop_run`/`heartbeat_loop_run`/`complete_loop_run`, the three tools marshal's `HostPublisher` already calls by name since Story 33.4), `src/platform/compose/keycloak/realms/platform-realm.json` (adds one `pyforge:station:marshal` role/group entry -- there are zero `pyforge:station:*` entries in this file today, verified 2026-09-12), and `src/platform/config/local_dev/personas.py` (adds one local-dev persona carrying that group, for `pyforge login`'s local profile -- CAP-2/CAP-5). No change expected to `roles.py` itself (marshal already parses via the existing `STATION_TOKENS` set). This is marshal's own act on steward's surface, not a steward story -- see `spec-33-12-...md`'s own Boundaries & Constraints, which requires this exact note to land before its `django-pyforge/**` edits merge.
- (event by claude) **CAP-17 CLOSED 2026-09-12**: the incoming surface claim above landed as described (marshal Story 33.12, PR #1270) and steward Story 49.8 (PR #1273) confirmed it and reworded the doctor comment this CAP's own row named. CAP-17 is the last of the nine 2026-09-09 realization-gap rows -- full narrative and residual-open items (CAP-3's CRC exercise, CAP-5's deployed login) recorded on `spec-run-state-one-publisher/.memlog.md`, the Spec that actually binds this capability's engineering; this entry exists so a reader of this Spec's own memlog sees the closure without cross-referencing.
- (note) Cross-station incoming surface claim (marshal Story 33.13, dispatched 2026-09-12): recorded ahead of landing, per the cross-station surface-claim rule Stories 33.4/33.12/49.8 already used. Expected edits under this spec's own `src/platform/**` surface globs: `src/platform/tests/test_front_door_queries_supervisor.py` (CAP-3 mechanism-tier proofs — publish→exit→fresh-connection timing survives, socket guard on `/runs/` render), `src/platform/tests/test_chart_invariants.py` (hostPath chart invariant helper + guard-removed companion), and `src/platform/deploy/overlays/ocp/core-overrides.yaml` (OpenShift CoreDNS `networkPolicy.dns` override). No change expected to `django-pyforge/**` — this is marshal's CAP-3 automated half on steward's platform surface, not a steward story.
- (decision by claude) 2026-09-12: CAP-17's own `verified:` line in SPEC.md hand-edited (this spec's established pattern for out-of-band corrections) to drop the now-stale "marshal loop homes still filesystem-backed" clause and name what actually landed (33.4's publisher meta-test, 33.12's held-run + published-plane-first re-point). Stopped short of `stack.md`'s full CAP-17 `verified:` template (steward 49.8's nominal acceptance criterion per that companion) because it requires the mechanism-tier `platform-ci-local --test` proof and the deployed CRC exercise's own record file -- neither exists yet (CAP-3, open). Precedent for a partial `verified:` update pending a Spec's own unexercised sub-capability: CAP-15/CAP-16 rows above (49.5/49.6) also never completed their full rewrite, per stack.md's own parenthetical.
- (event) Story 33.14 incoming surface: src/platform/compose/keycloak/realms/platform-realm.json and src/platform/deploy/charts/platform/templates/keycloak-realm-configmap.yaml (pyforge-cli PKCE client + marshal station group for deployed profile).
- (note by claude) 2026-09-12, attended CRC exercise recovery: `django_pyforge/assertion/identity.py` and `jwks.py` gained a narrow, additive scheme allowance -- `http://` is now permitted for JWKS URLs ONLY when the host ends `.svc` or `.svc.cluster.local` (Kubernetes' own CoreDNS is the sole authority for those suffixes, so this is not a spoofable trust signal; a BYO/external issuer still needs `https://`/`file://`). Found live: the bundled OIDC profile's own in-cluster Keycloak JWKS call (`http://platform-keycloak:8080/...`) had never actually been able to verify a token since the bundled profile's own introduction -- verified end-to-end this session via a real Authorization-Code+PKCE login against a deployed Keycloak. `src/platform/deploy/charts/platform/templates/_helpers.tpl`'s JWKS URL also changed from the bare Service short name to the full `.svc.cluster.local` FQDN so the new check actually recognizes it. RECONCILES: `src/shared/packages/django-pyforge/src/django_pyforge/assertion/identity.py`, `src/shared/packages/django-pyforge/src/django_pyforge/assertion/jwks.py`, `src/platform/deploy/charts/platform/templates/_helpers.tpl`, `src/platform/tests/test_django_pyforge_assertion.py`. Scoped-stamped: `--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy`.
- (event) CAP-17's verified: line hand-edited again, 2026-09-12, completing the update the previous entry left partial: the deployed CRC exercise (spec-run-state-one-publisher's own open CAP-3) is now DONE, not open. A real HostPublisher process, run from a workstation against a real deployed CRC cluster with networkPolicy.enabled: true live, published a run through /stations/marshal/mcp, heartbeat'd and completed it, exited, and a separate process querying /runs/ afterward showed the run under Completed timing with real duration -- timing surviving the workstation, proven live. Getting there required closing a genuinely un-specced gap found along the way: the mcp-host sidecar had never hosted any station's real MCP tools. Per the operator's Dream-first ruling, this was seeded as docs/dreams/mcp-host-real-station-tools.md and derived to spec-mcp-host-real-station-tools under this same project, then implemented and shipped the same day (PR #1285) rather than left parked -- see that Spec's own memlog for the full engineering detail. Full narrative on spec-run-state-one-publisher/.memlog.md and its verification-2026-09-12.md, per this spec's own established cross-reference convention. CAP-17 is now fully verified, no open sub-items remaining.
- (event) 2026-09-13 overlapping surface from scribe Epic 8–12: SKILL.md, cli-runbooks.md, scribe {cli,compile,graph_store,recall,graphify}.py, tests, django_pyforge/assertion/client.py, pixi.toml. Accept; no unifying-strategy product change.
- (event) 2026-09-13 spec-surface: src/shared/packages/pyforge-scribe/src/pyforge/scribe/compile.py, src/shared/packages/pyforge-scribe/tests/unit/test_compile.py, src/shared/packages/pyforge-scribe/tests/unit/test_supersession.py — scribe Story 13.1 planning pointers + compiled_at pin on idempotency test. No unifying-strategy CAP change.
- (event) 2026-09-13 spec-surface: src/shared/packages/pyforge-scribe/src/pyforge/scribe/compile.py, src/shared/packages/pyforge-scribe/tests/unit/test_compile.py — Story 14.1 named docs extras (how-tos + library-catalog extract). This spec CAP unchanged.
- (event) 2026-09-13 spec-surface: src/shared/packages/pyforge-scribe/src/pyforge/scribe/extras/graphify.py, src/shared/packages/pyforge-scribe/src/pyforge/scribe/cli.py, src/shared/packages/pyforge-scribe/src/pyforge/scribe/compile.py, src/shared/packages/pyforge-scribe/tests/unit/test_extras_graphify.py, .claude/skills/pyforge-scribe/active/pyforge-scribe/SKILL.md — Story 15.1 named graphify target list. This spec CAP unchanged.
- (event) 2026-09-13 spec-surface: src/shared/packages/pyforge-scribe/src/pyforge/scribe/recall.py, src/shared/packages/pyforge-scribe/src/pyforge/scribe/cli.py, src/shared/packages/pyforge-scribe/tests/unit/test_recall.py, src/shared/packages/pyforge-scribe/tests/unit/test_cli.py, AGENTS.md, .claude/skills/pyforge-scribe/0.1.0/pyforge-scribe/SKILL.md — Story 16.1 --mode bags. This spec CAP unchanged.
- (event by claude) Surface reconcile 2026-09-14 (bulk, post-outage, 32 findings): this Spec's broad surface (src/platform, django-pyforge, several pyforge-* skill mirrors, pixi.toml) picked up drift from many independent, already-landed, already-reviewed stories during the ~5-day GitHub Actions billing outage. By commit: d021af5df0 (cutover flag reader + replay harness) -- src/platform/config/flags.json, django_pyforge/flags.py. f30fa3d846 (suite-skip authoring tool + consume sidecar, Story 52.1) -- src/platform/db/changelog/{changes/pyforge-mybmad-1-schema.sql, changes/pyforge-mybmad-2-app-role-grants.sql, db.changelog-master.yaml}, src/platform/db/sqlmigrate-map.yaml, django_pyforge/{context_processors.py, sidecars.py, templates/django_pyforge/switcher.html}. 797ceb0d01 (marshal retrieve wired to scribe --mode planning) -- django_pyforge/assertion/client.py, pyforge-scribe/tests/meta/test_first_portal_slice.py. 932d03da84 (Scribe Epic 17 / Hub CAP-1..4) -- pyforge-scribe/tests/unit/test_navigation_owner.py (already reconciled substantively in spec-pyforge-core's memlog; this is the same commit). c8f4eb44f8 (track.json assembly) -- .claude/skills/pyforge-marshal/0.1.0/pyforge-marshal/SKILL.md. 89fddf847e (Company + eight station Frames v0.2 preflight) -- .claude/skills/pyforge-scribe/0.1.0/pyforge-scribe/SKILL.md. 39ff0ad964 (Hub Guard library exposure) -- .claude/skills/pyforge-steward/0.1.0/pyforge-steward/SKILL.md. pyforge-core/cutover_root.py + its test already reconciled in spec-pyforge-core's memlog (same commit family). Local-recipes PR #1354 added: pixi.toml (silo dependency + pyforge-station-tests task), src/platform/compose/compose.yml (objectstorage service), src/platform/deploy/charts/platform/{templates/_helpers.tpl, templates/networkpolicy-dbgpt-ingress.yaml, templates/networkpolicy-egress.yaml, templates/sidecar-{deployment,pvc,service}.yaml, values.yaml} (sidecar.enabled toggle + OBJECT_STORAGE_* seam), src/platform/pyproject.toml (mypy override + dropped UP038), src/platform/tests/{policy/test_platform_dev_local_leaf.py, test_chart_invariants.py, test_story_52_1_mybmad_sidecar.py, policy/test_liquibase_ddl_governance.py unaffected content, only re-touched by an adjacent edit} (ruff/mypy fixes + dbgpt toggle tests). Full platform test suite verified green: 855 passed, 79 skipped against real Postgres 17 + Redis 7 matching CI's own service config. Scoped-stamped: --write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy.
- (event by claude) Surface reconcile 2026-09-14: `pixi.toml` changed for two reasons, neither of which touches this Spec's own contract. (1) The `frame-preflight` task description was restated for Frame Spec v0.3 (steward Story 53.6 moved Frame identity from `name` to the qualified-ref `identifier`). (2) `pyforge-station-tests` was REPAIRED: it landed on 2026-09-14 as `set -euo pipefail` plus a bash `for` loop, which pixi's task shell (deno_task_shell) cannot parse, so the task errored on its first token and had never run a single station despite CLAUDE.md documenting it as PR CI gate #3. Replaced with a declarative cross-environment `depends-on` over the eight `pyforge-<station>-test` tasks. Verified by running it: all 8 stations green, exit 0. `environment.yaml` re-exported and byte-identical (description/task-wiring edits only, no dependency change). Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (PR #1355 CI remediation): `pixi.toml` gained nine tasks and `board.py` gained coverage pragmas; neither touches this Spec's contract. (1) Eight `pyforge-<station>-coverage-gate` tasks plus a `pyforge-station-coverage-gates` aggregate now mirror `.github/workflows/coverage-gates.yml`'s `named-module-gates` matrix locally -- that gate measures only the modules a branch TOUCHES, so `pyforge.doctor.sources.board` had sat at 70.1% against an 80% floor since the Guildhall console was retired and only surfaced when an unrelated edit touched the file. (2) A `pr-preflight` aggregate runs the four lanes that actually red a PR (`detectors-ci`, `test-ci`, `pyforge-station-tests`, `pyforge-station-coverage-gates`), because two CI failures on #1355 had no local equivalent at all. (3) `board.py`'s retired-Guildhall machinery is marked `# pragma: no cover` with a block comment stating why -- it cannot execute while `retired-console-check` fails CI on reintroduction; module coverage went 72% -> 94% with no test or behaviour change, tracked for deletion as `DW-DASHBOARD-DEAD-CODE-1`. `environment.yaml` re-exported and byte-identical (task-only edits). Scoped-stamped.
- (correction by claude) 2026-09-14 — the (direction) entry above cites *"Charter 5 -- owning is the post, not the product."* **Charter §5 does not contain that sentence.** It is a collapse of §5's two separate rulings: *"the station is the post, not the **person**"* (`pyforge-charter.md:451` — a Smith can be swapped at a station, which is why model tiering is safe) and the 2026-07-28 amendment *"Owning is becoming — at the planning tier … It does not rename the package"* (`:474-485` — the planning home is not the package name). The ownership decision that entry records — steward, not herald, because the mass is platform hosting, deployment and estate custody — is correct and unaffected; only the citation is wrong. Corrected here by appending rather than editing, because this log is append-only. The same miscitation appeared in `docs/dreams/archive/pyforge-unifying-strategy-2026-08-23-topology.md:1519` and was repointed there, and the forked formula itself was corrected at `docs/dreams/README.md:110` and `docs/dreams/intelligence-hub.md:841` in the same pass. Tracked as `DW-VOCAB-2026-09-14-14`.
- (event) Surface reconcile 2026-09-16 PR 1378 CI: named test_marshal_loop_homes_portal.py / django_marshal_portal/mcp_asgi.py as applicable.
- (decision) 2026-09-17 fold-exemption: governance — survives the steward one-chain fold; not absorbed.
- (event by claude) Surface reconcile 2026-09-19: 23 governed paths reported [drift-presumed] (14 added, 9 changed); each traced to a landed story that already names it in its owning Spec's memlog — atlas Stories 14.1/14.2/14.3 (`src/shared/packages/pyforge-atlas/pixi.toml`, `pyproject.toml`, `src/pyforge/atlas/views/{__init__,asgi,cli_bridge,live,registry,render,widgets}.py`, `tests/unit/catalog/test_no_inline_io.py`, `tests/unit/singularity/test_duckdb_sole_engine.py`) plus the 2026-09-18 `tests/views -> tests/unit/views` move (`3c5b74dcbf`: `tests/unit/views/{__init__,conftest,test_cli_bridge,test_live,test_registry,test_render,test_widgets}.py`), named in spec-pyforge-atlas; marshal Story 50.4 (`src/shared/packages/pyforge-core/src/pyforge/core/landing_evidence.py`, `tests/unit/test_landing_evidence.py`) and the parity-matrix fix `1b5be7990e` (`src/pyforge/core/dispatch.py`, `tests/meta/test_cli_parity_matrix.py`), named in spec-pyforge-core; and the doctor 23.2 docs fold `eec68aede4` (`src/platform/deploy/overlays/eso/README.md`: one link repointed to docs/explanation/enterprise-deployment.md). None touches this Spec's contract; scoped-stamped (`--write-baseline --spec pyforge-steward/spec-pyforge-unifying-strategy`).
- (event by claude) Surface reconcile 2026-09-19 (marshal Story 52.1 landing, spec-pyforge-core:CAP-9): `src/shared/packages/pyforge-core/src/pyforge/core/errors.py` (docstring count 37 → 41) and `src/shared/packages/pyforge-core/tests/meta/test_process_sole_ownership.py` (testing-kit exemption + pins) — named in spec-pyforge-core's memlog; none touches this Spec's contract.
- (event by claude) Surface reconcile 2026-09-19 (marshal Story 52.2 landing, spec-pyforge-core:CAP-8): `src/shared/packages/pyforge-core/tests/meta/test_conformance_lane_wired.py` (new structural meta-test pinning the conformance lane's wiring) — named in spec-pyforge-core's memlog; no contract of this Spec moved.
- (event by claude) Surface reconcile 2026-09-19 (co-governor; scribe Story 19.1 / spec-pyforge-scribe CAP-27, PR #1513 review): src/shared/packages/pyforge-scribe/tests/meta/test_instruction_surface_parity.py added — 18 stdlib-only meta-tests over the repo's instruction files (CLAUDE.md @AGENTS.md import, .gemini/settings.json, .vscode/settings.json, per-tool pointer files' size and H2 disjointness, AGENTS.md's team-memory paths). Test-charter shape: tests/meta/ under the package, pytest, no network, no fixtures outside the tree. Owner: spec-pyforge-scribe.
- (event by claude) Surface reconcile 2026-09-19 (co-governor; scribe Story 19.2 / spec-pyforge-scribe CAP-28, PR #1513): src/shared/packages/pyforge-scribe/tests/meta/test_guild_env_membership.py added (8 stdlib-only meta-tests over pixi.toml's feature.pyforge-guild membership and the governance docs' scribe routing); pixi.toml [feature.pyforge-guild.dependencies] gains the pyforge-scribe core path dep (steward Story 63.1 / spec-pyforge-steward CAP-5 composition, one dep, no extra); pixi.lock re-locked. Owner: spec-pyforge-scribe.
- (event) Surface reconcile 2026-09-19: updated Diátaxis docs and package READMEs
- (event by claude) Surface reconcile 2026-09-19 (co-governor; doctor Story 30.1 / PR #1529): src/platform/requirements/README.md → docs/explanation/platform-requirements.md and src/platform/deploy/{README.md → docs/explanation/platform-deployment-architecture.md, DR.md → docs/how-to/disaster-recovery.md, restore.md → docs/how-to/restore-operations.md, overlays/ocp/cluster-bringup.md → docs/how-to/ocp-cluster-bringup.md} — git renames, content unchanged (the Story 22.5 / MAP.md § Per-file classification: src/platform direction). Owner: spec-pyforge-doctor CAP-83.
- (note by claude) Path manifest addendum for the 2026-09-19 reconcile (doctor Story 30.1 / PR #1529): src/shared/packages/pyforge-core/README.md, src/shared/packages/pyforge-marshal/README.md, src/shared/packages/pyforge-scribe/README.md, src/shared/packages/pyforge-steward/README.md, src/shared/packages/pyforge-warden/README.md, src/shared/packages/pyforge-testing-kit/README.md, src/shared/packages/pyforge-atlas/README.md, src/shared/packages/pyforge-herald/README.md, src/shared/packages/pyforge-mason/README.md, src/shared/packages/pyforge-doctor/README.md — one plain-text docs pointer line each, no content change otherwise.
- (event by claude) Surface reconcile 2026-09-20 (co-governor; marshal Story 51.7, spec-pyforge-marshal:CAP-255, PR #1536 merge 88d488d6f3): src/shared/packages/pyforge-core/src/pyforge/core/landing_evidence.py, src/shared/packages/pyforge-core/tests/unit/test_landing_evidence.py — the landing-evidence grammar is unchanged (no exact-match tightening); LandingEvidenceMatch exposes which branch shape a GitHub PR-merge subject matched through. Owner: spec-pyforge-marshal.
- (event by claude) Surface reconcile 2026-09-20 (co-governor; scribe Story 19.3, spec-pyforge-scribe:CAP-29): src/shared/packages/pyforge-scribe/tests/meta/test_instruction_surface_parity.py — the H2 duplication guard compares normalised headings; five new tests for Claude Code's agents-md mod facts. Owner: spec-pyforge-scribe.
- (event by claude) Surface reconcile 2026-09-20 (co-governor of pyforge-core files; steward Story 66.1, CAP-153, PR #1553): src/shared/packages/pyforge-core/** -- pyproject.toml gains [tool.ruff] / [tool.mypy] strict; report.py, atomic_write.py, client.py, assertion.py, landing_evidence.py typed for strict; every .py reformatted by ruff format. No kernel API moved.
