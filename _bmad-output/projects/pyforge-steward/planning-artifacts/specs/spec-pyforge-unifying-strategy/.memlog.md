---
topic: The PyForge Canopy extension chain: eight station portals, a services tier, event fabric and DDL governance over the already-shipped src/platform Django host
updated: 2026-08-24T05:33
---

- (direction) Operator assigned the Dream to steward (was herald): the mass is platform hosting, deployment and estate custody. Charter 5 -- owning is the post, not the product.
- (decision) Chain is an EXTENSION binding spec-python-agent-platform as prior art, not a greenfield estate spec. Mints no CAP duplicating its CAP-1..6. Rationale: audit found src/platform shipped and steward epics 10/11/12/16 done.
- (decision) The Canopy is src/platform/ -- no ninth project, no Charter 5 governance act, roster stays at eight. pyforge_host and pyforge-agent-platform demoted to role names; no rename story minted.
- (constraint) Django >=5.2.15,<6 and Python 3.12.* are fixed by CAP-5 of spec-python-agent-platform. Django 6 is unavailable. conda-forge ships exactly one qualifying build (5.2.15), two patches behind upstream, zero headroom.
- (constraint) Every dependency MUST resolve from conda-forge. CAP-6's egress-blocked build is a gate, not a warning. A PyPI-only package is new feedstock work, not a preference.
- (constraint) Infrastructure stays exactly PostgreSQL + Redis + Kubernetes (inherited from spec-python-agent-platform). A component demanding a fourth piece has failed design review.
- (constraint) src/platform/ consumes the factory's published conda packages only and never imports pyforge.* code. The factory/platform boundary is an import rule.
- (decision) Steward owns cross-station slices outright, including marshal's pyforge.core.client and herald/marshal's Lane 1. Precedent: warden's compliance_face already lives in src/platform/.
- (event) Convergence pass complete: 14 surfaces covered-and-cited, 19 residual items evidence-confirmed absent, 5 Dream premises corrected.
- (note) Correction: Lane 1 is MARSHAL's console (docs/dashboard/, spec-factory-console shipped), not herald's. Herald's SPEC line 137 makes owning it an explicit non-goal. Wagtail work needs a negotiated handoff.
- (note) Correction: scribe is FlatFileGraphStore JSON, not SQLite. BS-1's premise and remediation must be rewritten as flat-file -> PostgreSQL/pgvector.
- (note) Correction: FastAPI already exists in-host at src/platform/config/fastapi_app.py. RFC-1 modifies an existing seam rather than introducing FastAPI.
- (note) Correction: pyforge-atlas already ships MCP (atlas/mcp/server.py build_server). Residual is the other seven stations plus transport, not the mechanism.
- (note) placeholder
- (note) VOID: the preceding 'placeholder' note was an accidental append from a line-count check. It carries no decision and supersedes nothing.
- (event) Phase-2 technical research complete. Four Dream directives invalidated against the live pins; operator ruled on all four 2026-08-24.
- (decision) RFC-5 revised from as-written to the least-bad form. As-written is not implementable: post_migrate is the only supported mechanism populating django_content_type/auth_permission/django_site, so migrate must keep running; create_test_db runs migrate so the test runner has no path off Django migrations.
- (decision) RFC-5 adopted shape: DDL revoked at the DATABASE ROLE (app role DML-only, separate migration role holds DDL -- the only auditor-verifiable control); Liquibase owns production DDL via a Helm pre-upgrade Job, NOT an init container (N replicas contend on DATABASECHANGELOGLOCK, default wait 5 min); Django migrations retained as the authoring surface with a CI sqlmigrate extraction gate; liquibase update then migrate --fake; test databases explicitly carved out.
- (decision) BS-2 rewritten to the current MCP spec. /mcp/sse is deprecated twice over: HTTP+SSE Deprecated since 2025-03-26, and 2026-07-28 removed the standalone GET stream and protocol-level sessions (compliant servers answer GET with 405). Adopted: single POST /mcp on official mcp 2.0.0 SDK, Tasks extension SEP-2663 for multi-minute work, keep-alive comment frames under 30s, X-Accel-Buffering no.
- (constraint) OpenShift timeout client stays at the cluster-wide 30s with NO per-route annotation. haproxy.router.openshift.io/timeout raises only timeout server. HAProxy governs SSE by timeout client/server, not timeout tunnel. Therefore keep-alive interval MUST be well under 30 seconds regardless of any route annotation.
- (decision) CodeRed CMS dropped; Wagtail 7.4.3 LTS alone carries Lane 1. CRX has no main commit since 2025-09-12, declares Wagtail 7.0-7.1 against a 7.4.3 LTS, and carries unfixed issue #710 (admin form submissions 404). Wagtail itself is conda-forge same-day with full closure, supports Django 5.2 + Python 3.12, and PostgreSQL FTS removes any Elasticsearch dependency.
- (decision) OpenFeature adopted via the flagd FILE resolver, accepting the packaging cost: FIVE new conda-forge recipes (openfeature-sdk, openfeature-flagd-api, openfeature-flagd-core, openfeature-provider-flagd, plus a cachebox 5.x build since conda-forge ships 6.2.5 and the provider pins <6). Per repo Rule 1 each of those stories MUST invoke conda-forge-expert.
- (decision) django-pyforge is OURS to build. django-lasuite is OIDC/DRF/malware plumbing with no UI layer and no app switcher; La Suite's La Gaufre is npm/React with a service-list endpoint unreachable air-gapped. No documented La Suite reusable-app pattern exists. Adopt django-lasuite for OIDC only.
- (decision) BS-4 gains a build task, not a dependency. PyBreaker's async support is Tornado-coroutine, not asyncio -- an httpx.AsyncClient coroutine passed to breaker.call() records a false success and the circuit never trips. Write the async wrapper over PyBreaker's own storage; reject aiocircuitbreaker (dormant since 2022) and purgatory (unpackaged).
- (assumption) PyBreaker's Redis state transitions use plain setnx/set/incr with no Lua or WATCH, so cross-replica transitions are not atomic. fail_max is treated as coarse protection, not exact-count semantics. This is read from source, not a documented upstream limitation.
- (question) Does air-gap policy accept a container as the delivery vehicle for Liquibase, a non-conda JVM tool, or must a conda-forge feedstock be authored first?
- (question) Is Liquibase issue #7791 (multi-schema default-schema-name regression on 5.0.3, runInTransaction false) fixed in 5.0.4? Must be verified before any multi-schema changeset lands.
- (question) Which MCP protocol revision do our agent clients actually speak? A 2026-07-28-only server rejects handshake-era clients and vice versa.
- (question) Does the official mcp Python SDK yet ship a server-side Tasks runtime? FastMCP's own PR notes no SDK shipped one at time of writing -- this is the largest gap between the recommended pattern and shippable code.
- (question) Marshal owns the Guildhall console (spec-factory-console, shipped). Does Wagtail Lane 1 supersede it, wrap it, or sit beside it? Requires a negotiated handoff, not an assumption.
- (capability) CAP-1 django-pyforge: one installable Django app supplying the shared chrome every station portal mounts -- app switcher, OIDC-aware base layout, Modernist theme assets, and the portal registration seam. Success: two portals render identical chrome from one package with zero duplicated template or static files.
- (capability) CAP-2 Lane 1 on Wagtail: a CMS-managed public front door at / on Wagtail 7.4.3, with the admin behind allauth OIDC via WAGTAILADMIN_LOGIN_URL and media on object storage. Success: an editor publishes a page without a deploy, and the admin is unreachable without an IdP-issued session.
- (capability) CAP-3 Eight portals mounted: every station reachable as a Lane 2 Django app under the one host, starting from compliance_face renamed warden_portal. Success: all eight portal URLs resolve behind one session, and a portal added or removed changes no host code outside its own registration.
- (capability) CAP-4 The services tier: per-station FastAPI services exposing MCP at a single POST /mcp on the official SDK, with operations over ~30s returning a Task handle. Success: an agent completes a multi-minute station operation across a simulated ingress disconnect without losing the result.
- (capability) CAP-5 One command grammar: pyforge <station> <noun> <verb> dispatching to the eight existing per-station CLIs without reimplementing them. Success: every verb reachable through both the unified entry point and its station binary, proven by a generated parity matrix.
- (capability) CAP-6 pyforge.core.client: the shared SDK every Django portal uses to reach its station service, carrying a signed internal JWT with a delegated_by claim. Success: a portal call arrives at the service with the end user's identity provable and independently verifiable, and no portal constructs a raw HTTP request.
- (capability) CAP-7 Lane 3 analytics behind the front door: atlas's Vizro boards proxied through the host with per-tenant row isolation enforced at the identity boundary, adopting spec-secure-live-dashboards rather than reinventing it. Success: two users with different roles receive provably different row sets from one board URL.
- (capability) CAP-8 The event fabric: a Redis Streams backbone carrying CloudEvents between stations, with consumer groups, a dead-letter queue and a loop-depth ceiling. Success: a poisoned event lands in the DLQ instead of retrying forever, and a cyclic publish chain halts at the declared depth.
- (capability) CAP-9 DDL governance: production schema change flows exclusively through Liquibase, enforced by database-role privilege rather than convention, with Django migrations retained as the authoring surface and a CI gate proving the extraction is not stale. Success: the application role provably cannot execute CREATE/ALTER/DROP, and a Django migration with no corresponding changeset fails the build.
- (capability) CAP-10 Runtime resilience invariants: circuit breaking on both sync and async portal-to-service calls, single-writer DuckDB discipline, inline form-error mapping, and idempotent startup reconciliation. Success: each invariant has a failing test that passes only once the invariant holds -- BS-4, BS-5, BS-7, BS-8 individually demonstrated.
- (capability) CAP-11 Resource-role separation: the Celery broker and the cache are distinct Redis instances with distinct eviction policies, and web and worker pools scale independently. Success: filling the cache to its eviction limit provably loses no queued task.
- (capability) CAP-12 Authorization and secrets: a Keycloak role matrix mapped onto Django groups so portal access is IdP-driven, with runtime secrets delivered from Vault rather than the environment. Success: revoking a role in the IdP removes portal access on the user's next request, and no long-lived secret appears in a pod spec.
- (capability) CAP-13 Feature-flag delivery: OpenFeature evaluation available to Django, FastAPI and CLI surfaces through the flagd FILE resolver, with every package sourced from conda-forge. Success: a flag flips behaviour across all three surfaces from one local flag file with no egress and no redeploy.
- (capability) CAP-14 Scribe durable storage: the knowledge graph moves from a single flat JSON file to PostgreSQL with pgvector behind the existing GraphStore port, keeping a local-development path. Success: the same graph operations pass against both drivers, and semantic recall returns results lexical recall cannot.
- (decision) Companions: convergence.md (chain boundary + residual), resilience-invariants.md (RFC-1..5 and BS-1..8 with revised forms), stack.md (the conda-forge gate), architecture-diagrams.md (mermaid -- Spec Law rule 2 keeps diagrams out of the kernel), plus the Phase-2 research file adopted by relative path.
- (event) Self-validate Pass 2 (Preservation) caught a gap: the Dream's 5-tier symmetry has five tiers (CLI, Web Portal, FastAPI/MCP Service, Domain Skill, Agent Persona) and CAP-1..14 covered only tiers 1-3. Tiers 4 and 5 were being silently dropped.
- (note) Tier-4/5 evidence: exactly ONE station domain skill exists (conda-forge-expert, mason's) of eight. ZERO station agent personas exist -- the five bmad-agent-* skills are BMAD roles (analyst/architect/dev/pm/ux-designer), not station personas. So tier 4 is 1/8 and tier 5 is 0/8.
- (capability) CAP-15 Station domain skills: each station carries an agent-invocable domain skill encoding its operating knowledge, following the shape conda-forge-expert already proves for mason. Success: an agent asked to perform a station's core task loads that station's skill and follows it, demonstrated for a station that has none today.
- (capability) CAP-16 Station agent personas: each station exposes an autonomous persona that can be addressed by name and acts through that station's own CLI and service surfaces rather than ad-hoc tool calls. Success: a persona completes a station task end to end using only CAP-5's grammar and CAP-4's service face, with the transcript showing no direct filesystem or ad-hoc HTTP access.
- (event) Self-validate Pass 1 (Coherence): Spec Law 1-6 and 8 pass. All 16 capabilities carry intent+success; intents kept to WHAT (CAP-2 says 'CMS-managed front door' not 'Wagtail', CAP-13 says 'vendor-neutral interface' not 'OpenFeature', CAP-14 says 'durable backing store' not 'PostgreSQL') with implementation held in stack.md and resilience-invariants.md; 12 constraints each rule something out; 7 non-goals; success signal is demonstrable; IDs CAP-1..16 unique and unreused.
- (event) Self-validate Pass 2 (Preservation) verdict after adding CAP-15/16: every load-bearing Dream claim now lands -- RFC-1..5 and BS-1..8 in resilience-invariants.md, the 5-tier matrix across CAP-3/4/5/15/16, the packaging gate in stack.md, topology in architecture-diagrams.md, and the covered/residual split in convergence.md. Wrapper-only content dropped: the press-release framing and the aspirational 'industry's first' prose.
- (decision) Operator ruling 1/5 -- tiers 4 and 5 are IN SCOPE. CAP-15 (station domain skills, 1 of 8 exist) and CAP-16 (station agent personas, 0 of 8 exist) stay bound. Adds a constraint: a station is never complete on fewer than five tiers, whatever its ledger says.
- (decision) Operator ruling 2/5 -- epic granularity is THEMATIC, roughly 7-9 epics grouped by delivery seam rather than 1:1 per capability. Steward runs to Epic 17, so this chain starts at Epic 18.
- (decision) Operator ruling 3/5 -- guildhall-handoff resolved as SUPERSEDE. Wagtail Lane 1 replaces Marshal's docs/dashboard console; spec-factory-console is retired via correct-course in Phase 5. CAP-2 rewritten from an addition into a replacement carrying a parity gate: every retired view reachable from the new front door, old build path REMOVED not merely unlinked. New open question console-parity-inventory replaces guildhall-handoff.
- (decision) Operator ruling 4/5 -- liquibase delivery vehicle goes to RESEARCH before deciding. New open question liquibase-airgap-policy: does this repo's air-gap policy govern container images at all, or only conda-sourced dependencies? CAP-9 cannot be scheduled until it lands. Repo-internal precedent research dispatched 2026-08-24.
- (decision) Operator ruling 5/5 -- Phase 5 correct-course scope is ALL EIGHT stations, one run per station, so every station spec records its Canopy obligations. Marshal's run additionally retires spec-factory-console per ruling 3. Each run pinned with BMAD_ACTIVE_PROJECT and written to physical paths, never through the shared symlink.
- (note) Convergence corrected: it still recorded RFC-5 as accepted 'as written', which the same-day least_bad revision superseded. Now states both, and adds the delivery-vehicle unknown.
