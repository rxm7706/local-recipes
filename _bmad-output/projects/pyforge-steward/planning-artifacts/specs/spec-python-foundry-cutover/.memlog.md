---
topic: Cutover of the PyForge estate from rxm7706/local-recipes to python-foundry (Phases 0-6), derived from docs/dreams/pyforge-unifying-strategy.md § Cutover
updated: 2026-09-13T12:54
---

- (direction) Operator 2026-09-04: refactor/rewrite the repo per the Dream; the plan must live in the Dream itself (docs/dreams/pyforge-unifying-strategy.md § Cutover to python-foundry), then derive the contract.
- (event) Gate closed 2026-09-03: steward Epics 40-43 done, Mason 13.1/13.2 done, steward ledger 196/196; review: 'Then cutover Phase 1 may start.' Nothing downstream existed (phase table only in the archive under 'do not build'; no Spec CAP, epic, or story).
- (decision) New sibling Spec extends spec-pyforge-unifying-strategy; cite ids as fnd:CAP-N outside this file. Not CAP-20 in the evergreen Spec: its SPEC.md is hand-edited past its memlog and bmad-spec is the single writer, so a re-derive would drop content.
- (decision by user) Foundry genesis: fresh empty repo. History stays in archived local-recipes at a pinned SHA; the move-list manifest records source SHAs; the deferred secret-leak history rewrite is left behind by construction.
- (decision by user) Red-team MEDIUM band: R-17 (DW-RT-2026-09-02-1) is Phase 3; R-23/R-24/R-25 fold into the document-fixes story; R-18..R-22 stay open steward-owned ledger entries ('Epic 45 candidate') and never block Phase 1.
- (capability) CAP-1 (Phase 0, fnd:CAP-1): open rxm7706/python-foundry — workspace pyforge, no recipes, lean pixi, estate-only CI; done when the clone exists and CI is green on the empty estate.
- (capability) CAP-2 (Phase 1, fnd:CAP-2): move the estate — src/shared/packages -> src/packages, django pixi.toml per package, no sys.path, skills/ with IDE symlink adapters, BMAD + decks + dreams; done when station envs solve and the host boots in foundry. Package fold and skills/BMAD move are separate stories.
- (capability) CAP-3 (Phase 2, fnd:CAP-3): CFE comes home to skills/domain/conda-forge-expert; retros land in foundry; done when no MASON_CFE_ROOT resolves to local-recipes and the CFE surface/rebuild guards pass.
- (capability) CAP-4 (Phase 3, fnd:CAP-4, R-17): factory/ island with its own pixi.toml + lock, recipes-only CI on paths: factory/**; done when mason recipe build factory/recipes/<r> matches today's CFE wrap.
- (capability) CAP-5 (Phase 4, fnd:CAP-5): working set only — in-flight + sole-maintainer recipes; done when factory/recipes/ is the working set and the 7,855-dir universe was not copied (count ceiling asserted).
- (capability) CAP-6 (Phase 5, fnd:CAP-6): Mason submits to staged-recipes/bot fork and updates feedstocks by maintainer-edit from foundry; done when an agent PR never opens against local-recipes.
- (capability) CAP-7 (Phase 6, fnd:CAP-7): archive local-recipes — README superseded, Azure disabled, last SHA pinned, history kept, worktree residue retired; done when the default clone is foundry and .steward has one git root.
- (constraint) Contract before repo: no CAP-1 dispatch until this Spec is ready and Epic 44 exists; CAP-1, CAP-6, CAP-7 are outward and held blocked in the ledger until the operator flips — never auto-drained.
- (constraint) Move-list manifest (Story 44.1) is derived from the spec-surface map and precedes CAP-2; never a hand list. Graphify move-list and package fold never in one story.
- (constraint) Estate pixi.lock never absorbs the factory solver farm. No services/ or :800x tree, no root docker-compose.yml, no src/platform/compliance_face/ as the portal, no .claude/skills/pyforge-mason/.
- (constraint) CAP-3 and CAP-6 are Mason work under Rule 1 / Rule 2 (conda-forge-expert invoked; CFE retro lands). Marshal's Deps parser is station-local, so Mason gates are ledger state, as 43.6 was behind Mason 13.
- (constraint) Physical writes under _bmad-output/projects/pyforge-steward/ with BMAD_ACTIVE_PROJECT=pyforge-steward; no bmad-switch from parallel agents. Tier-3 feed repaired (sprint-ledger-sync --project steward --repair-feed, then story-status-check) before any ledger write.
- (assumption) gh auth for rxm7706 can create a private repository (CAP-1).
- (question) actions-minutes: CAP-1 'CI green on the empty estate' needs GitHub Actions minutes; rxm7706 was under an account-wide billing block on 2026-08-30 (Dream: 'Story 12.9 CI when Actions minutes return'). If still blocked at dispatch, what is CAP-1's CI evidence?
- (event) Express distill 2026-09-04 from the Dream § Cutover (rich input; no elicitation). Companion cutover.md carries the phase table, target tree, face map, do-not-carry list and dispatch order.
- (event) Self-validate pass 1 (coherence): CAP-1..7 each carry intent + success; intents state target state not mechanism; every constraint rules something out; six non-goals; success signal is a demonstrable clone-boot-build-submit-archive sequence; ids unique; one assumption on gh auth, one on no Dream line cap. Verdict: pass.
- (event) Self-validate pass 2 (preservation): Dream § Cutover ruling -> blockquote + Why; operator decisions -> Constraints/Non-goals + memlog; phase->story table, order, never-list -> cutover.md; archive target tree, five faces, do-not-carry list -> cutover.md. Wrapper-only content dropped: none. Verdict: pass. Open question actions-minutes carried, not answered.
- (direction by user) 2026-09-04: solutioning only (BMAD Phase 3) — the Spec, the cutover architecture spine (adopted as companion, fnd:AD-1..12) and Epic 44 are reviewed and refined in iterations before implementation; every 44.x held ledger blocked; no story spec until the operator flips a story. Rendered into SPEC.md Constraints + blockquote.
- (event) Cutover spine adopted as companion (fnd:AD-1..16; reviewer gate PASS-WITH-FIXES applied). Nine spine open questions bend ADs; SPEC open_questions keeps actions-minutes, the others live in the spine. Solutioning iteration 1 complete pending operator review.
- (note) Cross-station: marshal-policy.toml [epic_surfaces] "44" is governed by pyforge-marshal/spec-marshal-parallel-dispatch-fanout; its memlog carries the change entry and was scoped-stamped 2026-09-04 (auditable, reversible).
- (direction by user) Iteration 2 (2026-09-04): cutover is flag-gated and the plan is regenerable/appendable; local-recipes keeps evolving until the flip (after 44.5). New CAP-8.
- (capability) CAP-8 (fnd:CAP-8): replayable, flag-gated cutover — the operator can regenerate or append the cutover plan at any time and flip the root of record by a flag; success: steward cutover plan --regenerate and --append both yield a manifest preserving moved rows; flipping pyforge.cutover_root switches ledger of record, Mason targets and loop-home remotes without a redeploy, and flipping back restores.
- (decision by user) Answered: Windows native-estate/remote-host with generated junction/symlink links; per-machine tool-detected adapters; SKF writes into skills/; runtime state in gitignored var/.
- (direction by user) Iteration 3 (2026-09-04): Dreams only move; every capability is realized by rebuild or move per a capability ledger; evergreen, additive, regenerative.
- (capability) CAP-2 reworded: Realize the estate — each capability reaches foundry by rebuild or move per the capability ledger; success: every capability row is verified-in-foundry under its mode's gate; no src/shared/ exists; the host boots from workspace members.
- (capability) CAP-9 (fnd:CAP-9): capability ledger and rebuild harness — the operator sets a mode per capability; the ledger derives from the Dreams and the owner map; a rebuild re-derives Spec, spine and epics in foundry from the moved memlog and is drained by Marshal; success: steward cutover plan emits the ledger with modes, states and dependencies; a rebuilt capability passes the archived suite (oracle gate); a rebuilding capability's source is frozen and --append reports drift against it.
- (constraint) Seed is Dreams + memlogs only; rendered SPEC.md, spines, epics, stories are re-derived in foundry; memlog fidelity (44.13) precedes Phase 0. The oracle gate is non-negotiable. Mode is per capability; this is not a rebuild of everything.
- (decision by user) Answered: planning-history-scope (Dreams + memlogs + capability ledger; rest re-rendered or archived); ingest-keys-import (rebuild the ingest in pyforge-steward behind the station port).
- (event) Solutioning iteration 4 opened 2026-09-04 on the operator's answers to the last two open questions (repo-visibility, actions-minutes).
- (decision by user) repo-visibility answered: private, permanently. Pages stays on the paid plan that already serves the public site from the private local-recipes; the win-64 leg bills at 2x; Actions minutes are a standing budget, so the actions-minutes guards are load-bearing; nothing Mason submits carries a foundry URL. Spine fnd:AD-14 amended.
- (decision by user) actions-minutes answered, four parts: (1) CAP-1's CI evidence is a real green workflow run on a registered runner, GitHub-hosted first; (2) the fnd:AD-19 remote Linux dev host registered as a self-hosted runner is the fallback when hosted minutes are unavailable; (3) while both runner paths are unavailable a documented fresh-clone run of the estate gates on that host counts as provisional evidence only, never CAP-1's success; (4) with no evidence path at all 44.3 does not dispatch. Plus a steward budget metering story (44.15). Spine fnd:AD-23.
- (note) Live fact 2026-09-04: the account-wide billing block of 2026-08-30 had cleared; Detectors run 33899470280 executed 77 s on main with real findings, not the 2 s refusal. Its cause (private-repo CI volume against the plan's included minutes) is unchanged, so the guards stand. local-recipes is one of 3 private repos among 100 on the account.
- (capability) CAP-10 (fnd:CAP-10): metered minutes budget; steward budget knows the account's Actions-minutes spend and ceiling. Success: steward budget check returns a real under/over verdict against the plan's included minutes and the declared ceiling from a metering source (the Actions billing API through a user-scoped credential held in steward keys, never in the manifest); Marshal's foundry drains and the rebuild harness (44.14) read it as their ceiling; the honest-stub property is retired for this source only.
- (event) Iteration 4 rendered: open_questions empty; CAP-1 success cites the evidence ladder; CAP-10 added; Constraints gain private-permanently and evidence-is-a-real-run; Assumptions gain the paid plan; Story 44.15 minted (blocked) and 44.14 depends on it; Epic 44 = 15 stories, all blocked. No open question remains.
- (event) 2026-09-04: Phase 4 opened — the operator flipped 44-13-memlog-fidelity from blocked to backlog on PR #1043 after the ledger pre-flight (sprint-ledger-sync --repair-feed unchanged at 256 keys; story-status-check ok); every other 44.x stays blocked; no contract change.
- (note) 2026-09-06 relay from spec-bmad-suite-lifecycle CAP-9: BMAD-estate prerequisites tracked there — G1 `_bmad/**` missing from Epic 44 `[epic_surfaces]` and G8 PROJECTS.md cutover layout → steward Story 47.3; G9 foundry Stack `bmad-*` floor row → Story 47.4; G2/G3 Epic 44 gains dependencies on steward 14.9 + marshal 30.5 (shim retirement) and marshal 30.2 (rulebooks) → Story 47.5; G5 skf-export root proof → Story 47.2; G10 loop-home readiness → marshal Story 31.5; G7 render-HALT detector and G11 frozen-path-changed detector → doctor Stories 20.2/20.3
- (finding) 2026-09-07 relay from spec-bmad-suite-lifecycle Story 47.2 (G5 closed out): a real, headless `skf-export-skill` run in a scratch worktree confirms `skf-export` DOES accept `skills/stations/<x>/` as `skills_output_folder` (a real end-to-end run lands `skills/stations/pyforge-herald/0.1.0/pyforge-herald/SKILL.md` + `.export-manifest.json`), but confirms empirically — `git diff --stat -- .claude/skills/` byte-empty after the run — that **SKF itself has no link-generation step of any kind**: nothing in `package.md`/`generate-snippet.md`/`update-context.md` or any `_bmad/skf/shared/scripts/*.py` helper creates, symlinks, or touches a `.claude/skills/<x>` adapter. This repo's own target-tree diagram (cutover.md) labels `.claude/skills/ (generated per-machine links, gitignored, fnd:AD-19)` without naming a generator — that generator does not exist anywhere in SKF today and is this project's own gap to close (build a dedicated link-generator, or extend SKF with one) before any station's live IDE skill discovery can rely on a `skills/stations/` canonical root. Secondary, previously-unnamed finding from the same run: changing `skills_output_folder` alone does not migrate or discover already-exported packages living at the OLD root — SKF's read-side resolution has no cross-root fallback, so a real cutover needs an explicit move/re-forge step for every already-exported skill, not just the config-key flip fnd:AD-12 names. Also observed: with `snippet_skill_root_override` left in place (as this repo's config already has it), a re-exported skill's managed-section `root:` pointer keeps citing the OLD location even after its package physically moves to the new root — the override and the migration can silently drift apart with no warning from SKF. Full evidence in `spec-bmad-suite-lifecycle/.memlog.md` (Story 47.2 event) and `cutover-readiness.md` G5/P10.
- (note) 2026-09-07 Story 47.5: Story 44.13's acceptance gains "and every spine's own `.memlog.md` re-distills through `bmad-architecture` without loss" (closing `spec-bmad-suite-lifecycle` G6 — 44.13's own scope previously only exercised one re-derive, 44.14's), recorded here first per the AC's own ordering requirement, before the epics.md edit lands.
- (event) 2026-09-07 Story 47.5: fnd:AD-10 cross-station gate check for Story 44.5, run live against each project's own `sprint-status-ledger.yaml` (never via `bmad-switch`, never assumed) — steward `14-9-the-apply-retires-deprecation-shims-on-purpose-no-shims: done`, marshal `30-2-the-project-context-surface-follows-6-12-d1: done`, marshal `30-5-the-harness-and-every-live-caller-follow-the-shim-retirement: done`. 44.5's `Deps:` line gains `S-14.9` (same-station) with trailing prose "after marshal 30.5 and 30.2" (the two cross-station producers, fnd:AD-10 — never a foreign-station `Deps:` token) plus a new in-story acceptance check against `cutover-readiness.md` P11/P12 run at 44.5's own execution time. `44-5-move-the-estate` stays `backlog` in this project's ledger — gated only by its own still-open same-station deps (`S-44.4`, `S-44.11`), unrelated to this cross-station gate, which is already satisfied — so no `blocked`→`backlog` round-trip is manufactured for a gate that was never actually unmet during this pass. Closes `spec-bmad-suite-lifecycle` G2/G3 (G6 closed by the note above); the `cutover-readiness.md` P11/P12 state cells themselves stay untouched (their producers' own job, out of this story's Surface line — see the file's G3 row for the named staleness finding).
- (decision) D1 operator 2026-09-13 campaign Launch the Foundry: invent in local-recipes; foundry copies a working estate. fnd:CAP-1 / Story 44.3 success evidence is local detectors-ci + platform-ci-local, plus CRC IFF the empty repo already carries the estate Helm chart. GHA is a twin and may stay red; force-merge after local proof is the campaign gate. Fresh-clone local is AUTHORITATIVE, not provisional. Rewrite CAP-1 success, Constraints CI evidence (fnd:AD-23), and Story 44.3 AC that require a registered GHA runner or steward budget check at confirmation.
- (decision) D1b operator 2026-09-13: Story 44.15 / fnd:CAP-10 shrinks and is LATER, not Launch. Unhook from 44.3 confirmation. Keep CAP-10 in the Spec for when GHA twins or Marshal drains spend minutes.
- (decision) D2 operator 2026-09-13: Story 44.9 stays blocked for this whole campaign. No conda-forge / staged-recipes / feedstock PRs. Publish path is selfexplainml. Do not dispatch 44.9 when 44.3 flips. CAP-6 remains later Phase 5. Campaign Success signal is factory-island recipe-build + selfexplainml upload, not an agent-opened conda-forge PR from foundry. CAP-6 success wording may stay later-phase but must not be the campaign Success signal.
- (decision) D3 operator 2026-09-13 campaign four verbs (Launch the Foundry / Adopt Frames / Build the Intelligence Hub / Wire every BMAD-suite component): Launch = Stories 44.12 + 44.3-44.7. Later: 44.8, 44.14, 44.15. Out of campaign: 44.9, 44.10, 44.11. Drop Story 44.5 Deps on S-44.11 (win-64 out). Two git roots stay live (no archive in Launch). Do not flip 44.3 or any 44.x blocked key in this pass.
- (event) Self-validate pass 1 (coherence) 2026-09-13 D1-D3 re-derive: CAP-1..10 still each carry intent+success; CAP ids preserved; CAP-1 success and CI evidence constraint now name fresh-clone local detectors-ci + platform-ci-local as authoritative; CAP-10 unhooked from 44.3; Success signal is factory-island recipe-build + SelfExplainML upload; Launch/later/out slices named. Verdict: pass.
- (event) Self-validate pass 2 (preservation) 2026-09-13: D1/D1b/D2/D3 memlog decisions landed in CAP-1 success, CAP-6 later-phase note, CAP-10 later metering, Constraints CI evidence, Success signal, Open Questions, and cutover.md companion. Wrapper-only dropped: campaign verb names beyond Launch scope. Verdict: pass.
- (decision by user) 2026-09-13: operator flipped 44.3 — create private rxm7706/python-foundry now; protect main; trunk-based from commit one (PR + merge commits only, no squash/rebase). local-recipes stays cutover_root. Epoch 6e0607b530f5fa5db2faffd12cbc49da9d880083. 44.9 and 44.10 stay blocked.
