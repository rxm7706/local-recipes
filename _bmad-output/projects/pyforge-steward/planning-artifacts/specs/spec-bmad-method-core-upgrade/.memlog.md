---
topic: Steward-owned repeatable upgrade of the installed BMAD-METHOD core (_bmad/bmm+core) against new upstream releases
updated: 2026-09-06T05:43
---

- (direction) Operator (2026-08-21): spec this Dream now, grounding in today's live 6.10.0->6.11.0 upgrade session; then decompose into steward's chain
- (note) Source: docs/dreams/bmad-method-core-upgrade.md (owner steward, resolved 2026-08-15 two-pass investigation; marshal ruled out by installer-owned boundary, genesis verifies-never-installs, bmad-module-provisioning Constraints narrowed to first-install)
- (note) Grounding evidence: 2026-08-21 session performed the SECOND manual one-off (6.10.0->6.11.0, branch bmad-method-v6.11.0-update) after W1's 6.6.0->6.10.0 — every trap hit live is captured in companion failure-modes.md
- (decision) Detection stays doctor-owned (bmad-method-version-drift, SHIPPED as doctor 10.1/10.2) — this spec is the mutating apply half; its pre-flight diff is a precondition of safe apply, not ambient reporting
- (capability) CAP-1 pre-flight diff (report-only): given installed manifest + target release, report skill adds/removes/renames incl. shim disposition + removals.txt deletions, upstream-touched files the repo locally modified, _bmad/custom overrides that stop applying (legacy-name halt trap), new hard prerequisites (uv). Success: retrodicts the 2026-08-21 findings for the 6.10->6.11 pair
- (capability) CAP-2 deliberate reviewable apply: wrap 'bmad-method install --action update -y' non-interactively, branch/snapshot first, never silent overwrite of _bmad/custom/**. Success: clean run lands installer diff on a review branch with custom-surface checklist executed
- (capability) CAP-3 custom-surface reconciliation: post-apply detect clobbered repo-custom surfaces (resolve_config.py multi-project layers 5/6 — clobbered in BOTH manual upgrades) and re-apply or flag. Success: bmad-switch --current + BMAD_ACTIVE_PROJECT override resolve six layers post-apply; installer .bak accounted for
- (capability) CAP-4 downstream pin fan-out report (report-only for foreign surfaces): enumerate every pin site the release moves — root pixi.toml floors, marshal pyproject + package pixi.toml + HARNESS_VERSION_RANGE_TEXT + seed manifest/drift-test map, loop-home hook relays. Success: names exactly the sites the 2026-08-21 session had to touch, none discovered later by red tests
- (capability) CAP-5 post-apply verification gate: run the repo's own gates (bmad-drift-check integrity, CFE skill meta-tests, bmad-loop validate per loop home incl. init relay refresh) and report one verdict. Success: reproduces the 2026-08-21 checklist (8/8 homes validate clean) in one command
- (constraint) Never silently overwrite _bmad/custom/** (Dream's own top constraint — clobber-without-surfacing is worse than no upgrade path)
- (constraint) Multi-project marker/symlinks must survive an upgrade untouched; the tool never calls bmad-switch and verifies artifact placement by physical path
- (constraint) Refuse/warn before an unattended run when legacy-name customization files exist (_bmad/custom/bmad-dev-auto.toml etc.) — the 6.11 shim halts instead of forwarding on them
- (constraint) The upstream installer stays the only writer of _bmad/bmm/** and _bmad/core/** — steward wraps it, never reimplements it (the narrowed bmad-module-provisioning line)
- (constraint) Foreign-station surfaces (marshal pin sites, doctor detectors) are REPORTED, never edited — owners act on the report
- (constraint) Runs from the repo's pixi env (installer, uv, node prerequisites all resolve there; nothing global)
- (decision) Non-goals: ambient staleness (doctor 10.1/10.2, shipped); module keep/drop triage (one-front-door); bmad-suite conda-recipe refresh + SelfExplainML publishing (CFE factory flow — the 2026-08-21 suite bumps ran there); editing foreign pin sites; first-install of adjacent modules (bmad-module-provisioning, shipped)
- (decision) Companion failure-modes.md holds the two-upgrade trap catalog + the 2026-08-21 verification worked example (multi-item, table-shaped — outgrows the kernel per Spec Law 7/8)
- (assumption) conda-forge bmad-method pixi package remains the install channel (npx-era is over); installer --action update observed idempotent/safe to re-run on 2026-08-21
- (assumption) 6.11's four-layer TOML config is 'the migration, not the cutover' (release notes verbatim) — _bmad/bmm/config.yaml still ships and the symlink pattern still works; the TOML cutover lands in some future release and CAP-1 must flag it when it does
- (question) Verb naming/placement: extend 'steward provision' vs a new verb (e.g. 'steward upgrade bmad-core') — decide at epic time, mirrors the provisioning spec's flag-naming precedent
- (question) Should doctor's ambient drift extend to the SelfExplainML bmad-suite packages? bmad-loop lagged 0.9.0->0.11.0 with zero ambient signal until a human checked on 2026-08-21 — doctor-side scope, relay at decomposition
- (question) Prerelease/@next channel support in scope, or stable releases only for the first cut? Dream names @next; nothing today exercised it
- (note) Success signal: the FIRST post-6.11.0 release goes from doctor-reports-behind to installed-core-current-gates-green through this capability with no step improvised; CAP-1 retrodicts the 2026-08-21 session against the 6.10->6.11 pair
- (event) Self-validate pass 1 (coherence, rules 1-6+8): PASS — 5 CAPs intent+success, constraints all bend, 5 non-goals, testable success signal; CAP-2's installer mention accepted as WHAT via the only-writer constraint
- (event) Self-validate pass 2 (preservation): PASS — Dream walked claim-by-claim; ownership resolution, all three when-real bullets, both constraints, all non-goals/kinships, @next mention all landed in SPEC.md or failure-modes.md; no silent drops
- (event) Spec finalized 2026-08-21: status ready, 5 CAPs, 1 companion (failure-modes.md), 2 assumptions, 3 open questions; Dream frontmatter stamped specified
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event) First live steward-driven apply 2026-09-06: 6.11.0->6.12.0 via steward upgrade bmad-core --apply (catalog data/bmad_core_releases/6.12.0.yaml authored from the unpacked conda package); CAP-3 restored resolve_config.py from the installer .bak, six-layer probes green; installer diff 198 paths on branch steward/bmad-core-upgrade-6.12.0
- (correction) Assumption 1 falsified live (2026-09-06): 'install --action update -y is idempotent and safe to re-run' holds only for core/bmm. At 6.12 (a) -y does NOT skip the directory prompt (closed stdin -> clack cancel, exit 0, silent no-op apply; pass --directory); (b) -y neither selects nor preserves an installed custom module whose source is cached (~/.bmad/cache/custom-modules) and deletes it as deselected (skf: 335 module files + 16 skills gone; pass --modules core,bmm,skf); (c) the custom module's marketplace.json skill list is now honored (skf-campaign undeclared -> not installed) and its module config.yaml is regenerated from module.yaml defaults, dropping hand-set keys and writing a literal {project-root}/{value} for prompt:false keys. Recovery: skf's own installer (bmad-module-skill-forge update) restores a coherent 2.1.0 tree + all 16 skills; config.yaml restored by hand. Traps 12-15 in failure-modes.md
- (note) CAP-5 prove-landed gaps seen 2026-09-06: bmad-drift-integrity gate imports pyforge.doctor, absent from the pyforge-steward env (gate reports FAIL; bmad-drift-check from local-recipes is all-ok); native spot-check uv-from-git hardcodes 'uv tool install --dry-run' (no such flag in uv 0.12.10) and cites bmad-loop @v0.11.0 while the matrix says v0.11.1 (advisory only); marshal loop home reports 'worktree not clean' from a pre-existing untracked .bmad-loop/marshal-model-cost-catalog.json (2026-09-02, marshal-owned). Upstream 6.12 resolve_config.py gained only write_json_stdout (UTF-8 pin) — ported onto the multi-project copy, output parity verified
- (direction by user) Operator (2026-09-06): fix the Dream and the automation so the next upgrade does not hit traps 12-16; include the CAP-5 env/uv follow-ups now; deep-review the repo's bmad-method customizations (companion customization-inventory.md, 20 rows C1-C20 with keep/remove/add verdicts)
- (capability) CAP-6 — Non-interactive installer drive. Intent: the apply drives bmad-method on purpose, not by luck: --directory <repo> (the -y flag never skipped the directory prompt; a closed stdin cancels with exit 0), --modules <every module the installed manifest lists> (core, bmm and each source:custom module), stdin closed, node/bmad-method resolved from the repo pixi env when absent from PATH, and an exit-0 run that changed nothing is a refusal (ok=False, trap 12), never a green. Success: a re-run of the 2026-09-06 apply from a clean tree lands the 6.12 diff without a wrapper script.
- (capability) CAP-7 — Custom-module awareness. Intent: every source:custom module in _bmad/_config/manifest.yaml (today: skf) is selected for the core apply, its config files (from the release catalog: _bmad/skf/config.yaml) are snapshotted before and restored after, its own installer (catalog: bmad-module-skill-forge update) runs after the core apply so the module tree, undeclared skills (skf-campaign) and knowledge/shared land coherently, and an optional catalog pin (--pin skf=<tag>) stops the channel-next cache drift. Success: after apply the module's skill dirs equal the packaged source and its config.yaml equals the pre-apply bytes plus the installer's own appended keys; the report names each restored file.
- (capability) CAP-8 — Local-customization scan and re-apply. Intent: before apply, compare every installer-owned file (.claude/skills/<manifest skill>/** and _bmad/scripts/*) against the cached package of the INSTALLED version (~/.cache/rattler/cache/pkgs/bmad-method-<v>-*/lib/node_modules/bmad-method/src, or --installed-package-root) and list the customized files in the CAP-1 report (this is the Dream's own first when-real bullet, which CAP-1 had narrowed to marker-carrying scripts); after apply, 3-way merge each one (ours = pre-apply bytes, base = old upstream, theirs = new upstream) via git merge-file, write clean merges, and flag conflicts with the hunk saved beside the report — never leave a customization silently dropped. resolve_config.py takes the same path: replay the old->new upstream delta on the restored copy instead of a bare .bak restore. Success: the 2026-09-06 seven-file case re-applies with six clean merges and one flagged conflict (step-01 done routing).
- (decision) CAP-5 amendments (bug fixes to shipped 14.5/15.4, not new stories): the drift-integrity gate runs the repo task (pixi run -e local-recipes bmad-drift-check) when pyforge.doctor is not importable from the steward env; the uv-from-git spot-check drops the non-existent --dry-run flag (probe = uv tool install --help, citation carries the matrix's full native command at @v0.11.1); install-matrix.md line 65 moves with it. Companion registered: customization-inventory.md (the 2026-09-06 review).
- (decision) Decomposition: Epic 14 grows stories 14.6 (CAP-6), 14.7 (CAP-7), 14.8 (CAP-8); ledger keys backlog; spec status stays ready until they ship (14.1-14.5 done). Trap-12 as recorded in the 2026-09-05 plan (orphaned bmad-generate-project-context) was wrong — it ships as a lifecycle:shim under src/bmm-skills/plan/; trap 12 now names the directory-prompt no-op.
- (decision) Open question 1 (verb naming) resolved at epic time: dedicated 'steward upgrade bmad-core' duty (Stories 14.1-14.5, live 2026-09-06) — dropped from open_questions. New open questions from the review: which skf revision is truth (conda-packaged 2.1.0 == cached main today vs tag v2.1.0 vs next; --pin skf=<tag> exists); where bmad-method reads remembered [modules.skf] answers from; whether skf should stay a bmad-method custom module at all given the own-installer class (dropping loses bmad-help routing for 15 skills)
- (constraint) A local customization is never dropped silently: a 3-way conflict is flagged with its hunk, never resolved by taking upstream; an exit-0 apply that changed nothing is a refusal (both learned 2026-09-06)
- (event) Headless update 2026-09-06 (express): SPEC.md re-derived — CAP-1..5 unchanged, CAP-6/7/8 added, assumption 1 replaced by its correction, companion customization-inventory.md registered (spec-authored, sibling), success signal extended to the 6.12 run, surface stays [] (upgrade.py + catalogs are governed by spec-pyforge-steward; no double governance)
- (event) Self-validate pass 1 (coherence, rules 1-6+8) 2026-09-06: PASS — 8 CAPs each with intent+success; CAP-6's installer flags accepted as WHAT via the only-writer constraint (same ruling as CAP-2); the new constraint rules out take-upstream-on-conflict and zero-diff-as-green; 5 non-goals; success signal testable (CAP-1 retrodicts traps 12-16; next release with zero improvised steps); IDs 1-8 stable, none reused
- (event) Self-validate pass 2 (preservation) 2026-09-06: PASS — Dream realization-log entry walked: falsified assumption -> assumptions[0]; the narrowed when-real bullet -> CAP-8; CAP-6/7/8 direction -> Capabilities; inventory -> companions; CAP-5 amendments (memlog 40) live in failure-modes.md trap 15 and the CAP-5 story fixes, kernel text unchanged; wrapper-only content: none dropped
- (event by claude) 2026-09-06 Story 14.6 ships CAP-6 in steward (`src/pyforge/steward/upgrade.py`, governed by spec-pyforge-steward): the apply passes `--directory <repo> --modules <every manifest module, core first>` with stdin closed, resolves `node`/`bmad-method` from `.pixi/envs/local-recipes/bin` when off PATH, records the argv + resolution in the report, and refuses an exit-0 zero-diff run (`ApplyReport.zero_diff`, trap 12) and a manifest naming no modules (trap 13) before any branch exists. SPEC status ready -> in-progress (CAP-1..6 implemented; CAP-7/8 = Stories 14.7/14.8, backlog). CAP-6's live success criterion — a re-run of the 6.12 apply from a clean tree with no wrapper — is NOT yet exercised: the unit tests never run the real installer, so that re-run is a pending operator action. failure-modes.md traps 12/13 rows and customization-inventory.md § 3 step 2 now say CAP-6 closes them (the wrapper is retired). No CAP text change.
