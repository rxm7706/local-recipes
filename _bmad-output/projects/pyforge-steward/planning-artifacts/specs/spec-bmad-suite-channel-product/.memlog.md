---
topic: The SelfExplainML bmad-suite as a governed product: always latest, dual-path installable (pixi + native), modules provisioned, pipeline connected end-to-end
updated: 2026-09-06T11:12
---

- (direction) Operator (2026-08-22): run the spec pass; channel bmad-method decision = REFRESH to 6.11.0; STANDING POLICY: always upgrade and refresh any outdated component — never leave a stale copy because it's shadowed/harmless
- (note) Source: docs/dreams/bmad-suite-channel-product.md (owner steward). Grounded in the 2026-08-22 research pass: all 13 recipes verifiably current TODAY (5 tag-pinned at newest tag, 6 commit-pinned at literal HEAD); the gaps are pipeline connectivity and wiring, not versions. Native-method taxonomy (7 classes), npm hazards (7 invisible, 3 stale-with-GitHub-canonical, 3 name collisions) carried in the Dream verbatim and companion install-matrix.md
- (decision) Channel bmad-method: REFRESH to 6.11.0 (operator, this session — build from our recipe + upload executing now); conda-forge stays canonical for solves; the channel copy is listing-parity, refreshed on every core upgrade henceforth (joins steward Epic 14's CAP-5 gate orbit)
- (capability) CAP-1 pipeline-truth report: one command reports, for each of the 13 packages, upstream latest (npm AND GitHub per its class), recipe version, channel version, installed version, wired-or-not — drift named per stage. Success: run today it reports the exact 2026-08-22 research matrix (incl. the 6.3.0 relic until refreshed); offline it degrades fail-open per stage
- (capability) CAP-2 end-to-end advance: one command takes a stale package through autotick (tag-mode, or HEAD-advance mode for the six commit-pinned dev recipes — a new github_updater capability) -> build -> test -> publish -> listing verified, landing as a reviewable PR never an auto-merge. Success: replays the 2026-08-21 seven-stage hand ritual for one package with zero improvised steps
- (capability) CAP-3 module wiring via provision --module: _SUPPORTED_MODULES grows from {bmb} to {bmb, tea, cis, utility-skills, manticore}, each addition manifest-recorded, skill-name-collision-checked, retired-ID guard + integrity meta tests green, reproducible against a fresh clone. WDS is an explicit skip-decision (deprecated upstream, absorbing into bmad-ux). Success: all five wire-decided modules provision through the verb; .claude/skills grows the expected skill sets
- (capability) CAP-4 dual-path contract: the per-package install matrix (pixi command + native command with upstream citation + hazard flags) is a TRACKED artifact, and the upgrade verification gate spot-checks one native path per class rather than trusting docs. Success: the matrix file exists, cited, and a gate run exercises >=1 command per class
- (capability) CAP-5 ambient channel/recipe drift (RELAY to doctor — detection is doctor's per the version-drift split): channel-vs-recipe and recipe-vs-upstream join doctor's suite-drift surfaces, fail-open, warn-only; the GitHub-releases fallback (doctor DW-14-1-1) unblinds the 7 npm-invisible packages. Success: a fixture of the 6.3.0 relic fires channel-drift; the fallback names bmad-loop's version from GitHub
- (constraint) conda-forge stays canonical for bmad-method (feedstock current at 6.11.0 — the only suite package with one); the channel copy is refresh-parity, never a fork; no other suite package goes to conda-forge
- (constraint) Wiring is deliberate per-module triage (one-front-door posture) — never wire-everything; publish stays credential-gated via steward key discipline; publish-before-floor-bump ordering holds; commit-pinned dev recipes keep X.Y.Z.dev0@sha encoding + G109 re-derivation; autotick output lands as reviewable PRs, git review decides
- (decision) skf pixi pin (the one suite pixi-installability gap): add bmad-module-skill-forge to pixi.toml — folded into this chain's first landing, not a separate story
- (question) CAP-2 scheduling: manual-on-drift-signal only, or a scheduled sweep (Phase-K-style)? Decide at story time — the report (CAP-1) plus doctor's ambient findings may make a schedule redundant
- (note) Kin consuming/feeding: steward Epic 14 CAP-4/CAP-5 (upgrade gate consumes the pipeline), marshal Epic 25 (era alignment this pipeline preserves), the operator's incoming OCP hybrid-environment Dream (its BMAD-wiring phase consumes CAP-3 verbatim — its wiring table IS this chain's target state)
- (event) Self-validate pass 1 (coherence): PASS — 5 CAPs intent+success, constraints bend, non-goals in Dream carried, success signals testable
- (event) Self-validate pass 2 (preservation): PASS — Dream + research matrix + taxonomy + hazards + operator decisions walked into SPEC.md/install-matrix.md; refresh-now recorded as executed decision, not silently dropped
- (event) CAP-1..5 shipped: steward Epic 15 (15.1-15.4) done from 2026-08-22; doctor Epic 15 (15.1 GitHub-releases fallback, 15.2 channel/recipe staleness ambient) done; steward Epic 31 (install-class wiring, 31.1-31.3) done from 2026-08-24; recorded 2026-09-05 (owed at the time).
- (capability) CAP-6 suite metapackage — shipped as steward Epic 39 (39.1-39.4 done): recipes/bmad-suite/{recipe.yaml,suite-members.yaml}, CFE bmad_suite_metapackage.py + build_bmad_suite.py, pixi tasks generate-bmad-suite / build-bmad-suite, opt-in feature.bmad-suite-full; bmad-suite 2026.9.1 then 2026.9.5 on the channel; doctor Story 19.1 reads the manifest for its watched set.
- (note) 2026-09-05 re-verify via steward suite pipeline-truth after BMAD-METHOD 6.12.0 (released 2026-09-04): 13/13 recipe = channel = installed (method 6.12.0, loop 0.11.1, TEA 1.24.0, builder 2.2.2, CIS 0.3.2, skf 2.1.0, eval-quality 0.2.0.dev0, utility-skills 2.0.0, labs 1.0.0.dev0, template 0.1.0, manticore 3.1.0.dev0, dashboard 1.2.2.dev0, mybmad 0.1.0.dev0); drifts only wired (TEA / builder / utility-skills / manticore deliberately unprovisioned here) and npm/GitHub divergence (builder npm 1.1.0, CIS npm 0.1.9); the TEA v1.23.3 watch closed by 1.24.0.
- (decision) Roster 2026-09-05 (Story 45.1, PRs #1059/#1060): bmad-method-wds-expansion is deprecated — 6.12.0's bmad-modules.yaml marks it deprecated: true, folded into BMM as bmad-ux — removed from the metapackage run deps and the matrix, recipe kept as a suite-members.yaml catalog row; bmad-eval-quality (commit-pinned 0.2.0.dev0 @ 3172162f) took the seat; the roster stays 13 = 6 tag-pinned + 7 commit-pinned.
- (decision) Eighth native class recorded: a bare npm CLI with nothing to wire into _bmad (bmad-eval-quality; steward INSTALL_CLASS_CLI; eval-quality on PATH); gate spot-check candidate eval-quality --help. Hazard recount: npm-invisible ×6 (loop, utility-skills, labs-skills, module-template, manticore, mybmad), npm-stale-with-GitHub-canonical ×3 (builder, CIS, eval-quality), name-collisions ×3 unchanged, plus the G109 renumber hazard (manticore tag 1.0.1 vs 3.1.0.dev0; eval-quality tag 0.1.0 vs 0.2.0.dev0).
- (constraint) A member the upstream module registry marks deprecated: true (WDS since 6.12.0) stays in suite-members.yaml as a catalog row and never re-enters the metapackage run deps or the matrix; its recipe is kept, not deleted.
- (assumption) (supersedes the 2026-08-22 baseline) The 2026-09-05 state is the baseline: 6 tag-pinned recipes at newest tag, 7 commit-pinned at literal HEAD; TEA 1.24.0 released (watch closed); bmad-method 6.12.0 on conda-forge and the channel while the applied _bmad/ core is 6.11.0 pending steward Epic 14.
- (note) Open question CAP-2 scheduling: Story 15.2 did not decide it; the 2026-09-05 refresh was triggered by doctor's ambient drift plus the operator, not a schedule — the default stays manual-on-drift-signal until a schedule earns its keep; the question remains open.
- (note) Holes found 2026-09-05, relayed as deferred-work entries (steward ledger; doctor's own for #3) rather than new CAPs on a shipped spec: (1) the installer-tree installed stage reads the pixi env's conda-meta (bmad-method 6.12.0), not the applied _bmad/_config/manifest.yaml (6.11.0) — pipeline-truth reads all-green while the core is unapplied and doctor's core drift is the only signal; (2) bmad-eval-quality has only the __unix noarch variant on the channel — win-64 excluded until a Windows build; (3) doctor suite drift maps 7 of 13 members (commit-pinned unmapped); (4) anaconda upload stays an operator step by design.
- (note) First governed run = the 2026-09-05 refresh (PRs #1059/#1060): not zero improvisation — the eval-quality pin moved into the linux-64 / osx-arm64 target tables (unix-only variant) and the CFE-retro slices were re-mirrored with a CRLF stamp. Success-signal verdict: the pipeline shipped; 'never by hand again' holds for the seven stages except upload (by design).
- (event) Self-validate pass 1 (coherence): PASS — CAP-6 gains shipped evidence; constraints bend (deprecated-member rule); non-goals unchanged; success signal carries the shipped evidence and the relayed residue.
- (event) Self-validate pass 2 (preservation): PASS — Dream § Roster and version state (4 bullets) + § What is real walked into SPEC.md and install-matrix.md (versions, class cli, installed-stage caveat, metapackage >=6.12.0); nothing dropped.
- (event) SPEC re-derived 2026-09-05 (bmad-spec update, executed per the skill's Memory-and-derivation procedure): status shipped; Dream flipped to realized the same day; install-matrix.md re-verified against pipeline-truth.
- (decision) 2026-09-06: the 'wiring is deliberate per-module triage — never wire-everything' constraint is superseded: wiring follows the adoption register of spec-bmad-suite-lifecycle CAP-1 (operator decided TEA full adoption, BMB beside skf, utility-skills, manticore for Herald, labs skill-by-skill, eval-quality pilot); module-template stays scaffold-only and mybmad-dashboard stays out of the platform
- (note) 2026-09-06: the two relayed holes are now stories — pipeline-truth installed-stage reads `_bmad/_config/manifest.yaml` = steward Story 46.9; `bmad-eval-quality` `__win` variant = mason Story 14.1
