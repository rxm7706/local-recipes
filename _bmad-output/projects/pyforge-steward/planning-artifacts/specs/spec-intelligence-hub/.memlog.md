---
topic: PyForge speaks the Intelligence Hub vocabulary deliberately: which of the whitepaper's shapes (Frames, Cogs, Ops, Guards, Gates, Tracks, Organizational Memory) PyForge adopts, aligns to, or merely names
updated: 2026-09-06T03:32
---

- (event by claude) 2026-09-05 bmad-spec create, headless/express mode: input docs/dreams/intelligence-hub.md (rich, structured seed — whitepaper distillation, vocabulary map, five candidate shapes, six open questions, RB-1/RB-2 research backlog); project pyforge-steward (the Dream's owner). Trigger: dream-chain INV-1 dream-without-spec, the one pre-existing detectors-ci red left on main after PR #1071. Precedent followed: spec-golden-path-conda-blind-spot (draft seeded 2026-09-05, PR #1063, registered in doctor DEFERRED_SPECS).
- (direction by claude) Operator (2026-09-05): fix the pre-existing dream-without-spec finding. RB-1/RB-2 research still precedes the contract (PR #1069, operator-queued) — so this is a draft that establishes the chain, chooses nothing, and carries the research as blocking open questions.
- (direction by claude) Repo frontmatter convention (spec:/status:/owner-dream:/surface:/companions:/sources:/open_questions:) used instead of the template's id: line — the dream-chain and spec-surface detectors parse these keys.
- (decision by claude) Why = the Foundry is already an Intelligence Hub in miniature (owned factory on pixi + conda-forge; context in tracked files, workers as station skills, workflows as bmad-loop runs, checks as detectors + Warden, evidence in ledgers) that speaks the paper's vocabulary by accident; the Spec decides which shapes PyForge adopts, aligns to, or merely names. One tension carried, not assumed: PyForge rents the model and harness (Claude Code is on the paper's rented-black-box list) while owning context, workflows, checks and evidence. Owner steward (the estate) until the owner question is answered.
- (capability by claude) CAP-1 vocabulary alignment only: the Charter Lexicon + station roster map once to Frames/Cogs/Ops/Guards/Gates/Tracks/Organizational Memory in pyforge-charter.md or the Unifying Strategy, no code; success = every vocabulary-map.md row names its Charter term and gap, and the rented-model/owned-context tension is stated in the recording document.
- (capability by claude) CAP-2 Frames as first-class (gated by RB-1): the repo's own context (AGENTS.md block, station personas, recipe conventions) published as Frame-shaped artifacts with named accountable owners — a Community Frame for local-recipes; success = each artifact validates against the published protocol and names its owner; not specified before RB-1 records an open specification (URL, version, licence).
- (capability by claude) CAP-3 a declared validation strategy per run: a bmad-loop run or marshal factory spin declares Guards by stage, Gates as threshold rules, Track contents + retention, so the evidence files converge on one structured Track (landing-evidence grammar + durable-runs are the precedents); success = one run yields one structured Track record with enumerated fields and stated retention, Guards/Gates readable from the record not inferred from policy TOML.
- (capability by claude) CAP-4 Guards as a library: detectors, Warden axes and review lenses exposed as reusable Guards with a declared category from the paper's seven; success = a Spec can name which categories it lacks; Warden stays the sole PR verdict, doctor advisory — no Guard mints a second verdict.
- (capability by claude) CAP-5 package the Nebari/Nebi lineage where missing (gated by RB-2): a Mason / conda-forge-expert lane builds local recipes for nebari, nebari-infrastructure-core, nebi wherever RB-2 finds conda-forge and PyPI lacking; success = green local build ends the task; nothing packaged that RB-2 finds already on conda-forge; no external PR without an ask.
- (decision by claude) The five CAPs are CANDIDATE shapes (Dream § What it might look like when real — none chosen); selecting among them is the Spec's first decision, deferred behind the open questions; an unchosen shape becomes a recorded non-goal, never a silent drop.
- (constraint by claude) Dream-first: no code from the seed; the Spec chooses among CAP-1..5 before any code — and has not chosen yet (Dream).
- (constraint by claude) The whitepaper is the source of record: no claim the paper does not make; re-verify statistics/vendor facts at intake; re-fetch by URL + hash before citing a later revision (Dream).
- (constraint by claude) Copyright: distilled in our words with short attributed quotations; the PDF is not tracked and not to be committed (Dream).
- (constraint by claude) Foundry invariants stand: src/platform never imports pyforge.*; the infra-kinds lock and AD-1 (re-affirmed 2026-09-05) untouched; the paper's Organizational Memory menu is not a licence to add stores to the platform (Dream).
- (constraint by claude) One PR verdict: Warden stays the sole gate, doctor findings advisory; Guard language must not mint a second verdict (Dream).
- (constraint by claude) No implied platform adoption: naming Nebari/Nebi adopts nothing; the Unifying Strategy owns the platform shape (Dream).
- (constraint by claude) No new docs/specs/ file; no external PRs; no outward dispatch without operator confirmation (Dream).
- (decision by claude) Non-goals carried from the Dream: no marketplace or Desktop/Web Application; no replacing the Foundry with Nebari or conda-forge-expert with anything; no endorsing/marketing OpenTeams; no reproducing the whitepaper.
- (question by claude) Owner: steward (estate; owns Unifying Strategy, eval-quality, channel product) vs marshal (Ops, Gates) vs scribe (Frames, memory) — owning is the post, not the product.
- (question by claude) RB-1 — is the Frame protocol published, and where (nebari.dev, nebari-dev GitHub org, ownyourintelligence.ai, OpenTeams repos)? Gates CAP-2; closes by appending dated evidence to the Dream's research backlog.
- (question by claude) RB-2 — are nebari, nebari-infrastructure-core and nebi on conda-forge and PyPI (four-spelling rule; lookup_feedstock / get_conda_name / pypi_intelligence), under what licence, and can rxm7706 modify the feedstock? Gates CAP-5; Mason / CFE lane.
- (question by claude) Which of the seven Guard categories does the repo lack entirely, and is source-grounding of LLM output the first to add? Shapes CAP-4.
- (question by claude) Does a bmad-loop run already emit enough to constitute a Track, and what retention does the operator want? Shapes CAP-3.
- (question by claude) Does the repo publish its own context as Frames (a Community Frame for local-recipes)? Depends on RB-1.
- (assumption by claude) Owner steward stands until the owner question is answered; the Spec moves with the owner (dream-chain INV-2) if it changes.
- (assumption by claude) The candidate shapes are independent; more than one may be chosen, and CAP-1 may be chosen alone.
- (decision by claude) Companion vocabulary-map.md (spec-authored): the 12-row whitepaper→PyForge→gap matrix plus the rented/owned tension — a multi-item matrix outgrows the kernel (Spec Law 7/8). No adopted companions.
- (decision by claude) Status draft. Registered in pyforge-doctor sources/board.py DEFERRED_SPECS with the reason (chain link durable; CAPs are unchosen candidates behind RB-1/RB-2 + four operator questions) so chain-completeness does not demand steward epics for guesses — de-register when the questions clear, the operator picks the shape(s), and steward's epics carry the chosen CAPs. Dream status stays dreamt: docs/dreams/README.md requires a Spec at ready or beyond for specified.
- (event by claude) Self-validate pass 1 (coherence, Spec Law 1-6, 8): every CAP has intent + success; intents are WHAT (protocol/flag details deferred to questions); every constraint rules something out; four non-goals; the success signal is testable (choices as memlog decisions, RB-1/RB-2 closed with dated evidence, status ready, DEFERRED_SPECS exit); IDs unique; lean.
- (event by claude) Self-validate pass 2 (preservation): Dream sections walked — The Dream + the tension → Why; the five candidate shapes → CAP-1..5 with their RB gates; Constraints → seven constraints verbatim in gist; Non-goals → four; Open questions + RB-1/RB-2 → six open_questions; the vocabulary table → vocabulary-map.md; What-is-real ('nothing in the repo yet') → status draft + the Dream's own log entry. Nothing dropped.
- (event by claude) Spec finalized 2026-09-05: status draft; 5 candidate CAPs; 1 companion (vocabulary-map.md); 6 open questions (2 = the RB backlog); 2 assumptions; DEFERRED_SPECS entry lands in the same PR.
- (direction by claude) Operator (2026-09-06): run the RB-1 / RB-2 research now (Dream § Research backlog); findings close in the Dream with dated evidence and the open questions update — the shape decision stays the operator's.
- (event by claude) RB-1 run 2026-09-06 — CLOSED, answer YES: Frame Spec v0.2.0 is public at github.com/openteams-ai/frame-spec (repo created 2026-05-18, public via PR #23 on 2026-08-19; CHANGELOG dates the release 2026-08-18; normative frozen text spec/v0.2.md, 297 lines; working draft spec/frame-spec.md). Prescribes a single Markdown file + YAML frontmatter: required type (`frame` | `frame [0.2]`), name, description, visibility; recommended version, scope, maintainer, inherits; body = Markdown loaded as system context; explicit inheritance, child over parents, parents in order, transitive optional. Out of scope by design: package manifests, canonical identity, provenance, review workflows, registries, runtime management; directory form deferred. Tools: tools/validate_frames.py (+ GitHub Action), tools/frame-builder.html, two authoring prompts, examples/ incl. nebi-frame-package/frame/package.yaml. Caveats: the README's releases/tag/v0.2.0 link 404s — no git tag or release object exists; the repo has NO LICENSE file (reuse terms unstated).
- (event by claude) RB-1 reference implementation: github.com/nebari-dev/nebari-frames (Go, Apache-2.0, v0.1.7 on 2026-08-19, self-declared beta; backend + `frames` CLI + web app + MCP endpoint; Helm chart oci://quay.io/nebari/charts/nebari-frames; release binaries linux/darwin/windows amd64+arm64; frames.rb in nebari-dev/homebrew-tap; successor of skillsctl). Stores Frames in a ten-slot YAML schema (terminology, rules, skills, prompts, tool_specs, goals, style, norms, architecture, business_process) with `extends` inheritance + RBAC; imports/exports a conformant .frame.md (`type: frame [0.2]`, one ## per slot) that passes the spec validator; `visibility` is declared intent, not access control. Ecosystem per the spec's docs/ecosystem.md: no Cog spec exists; Ops renamed from Progs; Collab desktop client + private-invitation hub (ex-Nexus); Nebi does NOT define or ship Frame support (nebi-integration.md is exploratory). The ownyourintelligence.ai field guide (July 2026, Rev 7) saying 'no published spec' predates the release. Whitepaper canonical repo: github.com/openteams-ai/inthub-whitepaper (tag v9 = Revision 9, 2026-08-17, commit 9c2856923b; v8 = 2026-08-06; no licence file).
- (event by claude) RB-2 run 2026-09-06 — CLOSED: live channeldata.json + lookup_feedstock + PyPI JSON, four spellings each plus the python- prefix. nebari: conda-forge YES (nebari-feedstock, v0 meta.yaml, noarch python, __unix-only, 2025.10.1, BSD-3-Clause, maintainers marcelovilla/dcmcand/viniciusdc — rxm7706 NOT a maintainer; pushed 2026-04-22, no open PRs); PyPI YES (latest stable 2025.10.1 on 2025-11-04; 2026.3.1rc1/rc2 prereleases; the GitHub release 2026.3.1 of 2026-07-17 was never uploaded to PyPI → feedstock at PyPI-stable parity); nebari.dev labels it 'Nebari classic (sunsetting)', successor 'Nebari core' (NIC). nebari-infrastructure-core: conda-forge NO (all spellings, no feedstock), PyPI NO (404); Go CLI, Apache-2.0, v0.14.0 (2026-08-25), GitHub-release tarballs linux/darwin x86_64+arm64 + windows x86_64+arm64 with source tarball, SBOMs, sigstore checksums, plus a `nic` Homebrew cask; downloads its own OpenTofu at runtime (NIC_TOFU_PATH / tofu on PATH; conda-forge ships opentofu 1.12.6). nebi: conda-forge YES and current (nebi-feedstock, v1 recipe.yaml multi-output nebi-cli/nebi-desktop/nebi, 0.15 = upstream v0.15 of 2026-08-27, Apache-2.0, linux-64/osx-64/osx-arm64/win-64, Go + nodejs build, maintainers viniciusdc/Adam-D-Lewis/aktech/pmeier — rxm7706 NOT a maintainer); PyPI NO (Go). Also absent from conda-forge: the `frames` client (nebari-frames), skillsctl, nb-nebi-kernels.
- (note by claude) RB-2 tool notes: get_conda_name returned the identity name for all three via the metadata API — a fall-through, not evidence of existence (G10: existence needs channeldata/lookup_feedstock); pypi_intelligence is a ranked-candidate listing with no per-name filter, so it does not apply to a named lookup — the Dream's instruction to use it was a category error, recorded here so the next RB does not repeat it.
- (decision by claude) CAP-2 gate lifted: the 'no protocol exists' blocker is gone (RB-1); CAP-2's success now names the spec's own tools/validate_frames.py as the conformance check and the nebari-frames .frame.md round trip as the interoperability check if the registry is adopted. Selection of CAP-2 remains the operator's.
- (decision by claude) CAP-5 narrowed (RB-2): nebari and nebi are consumed from conda-forge as-is (re-packaging them is a non-goal); the lane's verified gap is nebari-infrastructure-core (Go, v0.14.0) plus the frames client (Go, v0.1.7) if the registry is adopted — the Go-source shape nebi-feedstock already uses; green local build ends the task; no external PR without an ask.
- (note by claude) RB-1 and RB-2 open questions CLOSED (answered in the Dream § Open questions + § Research backlog with dated evidence).
- (question by claude) Derived from RB-2: package nebari-infrastructure-core (Go CLI, GitHub-release binaries + Homebrew only, v0.14.0) and the frames registry client (Go, v0.1.7) as local recipes — is that wanted, now that CAP-5's verified gap is exactly those two?
- (question by claude) Derived from RB-1: author the first conformant .frame.md for local-recipes now (v0.2 is a single Markdown file with four required frontmatter fields; validator in-repo — cheap) or wait for the shape decision?
- (question by claude) (reworded) Does the repo publish its own context as Frames (a Community Frame for local-recipes)? RB-1 removed the protocol blocker; purely the operator's call on CAP-2 now.
- (assumption by claude) Frame Spec v0.2 stays single-file (directory form deferred) and its repository stays licence-less until upstream says otherwise; conformant documents can be authored regardless and the spec text is never vendored here.
- (assumption by claude) nebari-feedstock and nebi-feedstock stay maintained by nebari-dev people; rxm7706 is not a maintainer on either, so any bump there is a reviewed PR, never a push.
- (constraint by claude) (refined) Cite later whitepaper revisions by tag from openteams-ai/inthub-whitepaper (Revision 9 = v9), never by PDF URL alone — the repo is the canonical source of record the Dream's re-fetch rule needs.
- (decision by claude) Non-goal added: re-packaging nebari or nebi (both on conda-forge per RB-2; bumps there belong to their maintainers).
- (decision by claude) Status stays draft; DEFERRED_SPECS reason text in pyforge-doctor sources/board.py updated the same day to say RB-1/RB-2 are closed and only the operator's questions remain (exit condition unchanged: shapes picked + steward epics carry them).
- (event by claude) Self-validate pass 1 (coherence): PASS — CAP-2/CAP-5 intent+success refined not renumbered; constraints bend (tag-based citation); non-goals +1; success signal unchanged in shape with RB closure dated; six open questions (4 original-remaining + 2 derived); IDs stable.
- (event by claude) Self-validate pass 2 (preservation): PASS — both RB findings walked into the Dream (§ Source repo pointer, § candidate shapes 2 + 5 glosses, § What is real gloss, § Open questions answers, § Research backlog findings, Realization log) and into SPEC.md / vocabulary-map.md (Nebi + Frames rows); nothing dropped; RB-2's tool caveats kept as a note.
- (event by claude) SPEC re-derived 2026-09-06 (bmad-spec update, per the skill's Memory-and-derivation procedure): status draft; CAP-1..5 candidates; companion vocabulary-map.md updated; Dream status stays dreamt.
