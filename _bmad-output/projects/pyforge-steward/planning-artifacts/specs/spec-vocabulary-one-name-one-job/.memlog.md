---
topic: One name, one job — reconciling the estate's vocabularies (which word means what) and its identifier shapes (how each thing is written down)
updated: 2026-09-14T06:30
---

- (provenance by claude) THIS RECORD STARTS HERE, 2026-09-14. `SPEC.md` was authored earlier the same day from `docs/dreams/vocabulary-one-name-one-job.md` and shipped without a `.memlog.md`, which `memlog_fidelity_check` reports as `missing-memlog`. This file was created when the Dream was extended (see the next entry), by an agent that did **not** author the Spec. Nothing that happened during the Spec's own authoring was captured; do not read the absence of earlier entries as an absence of decisions — the authoring rationale is in the Spec's own frontmatter comment and in the Dream's Realization log entries for 2026-09-14. Same convention as `pyforge-marshal/spec-pyforge-core/.memlog.md`, created after the fact for the same reason.
- (event by claude) 2026-09-14, operator-directed Dream extension. A read-only identifier-shape sweep at `main` `445976e5be` measured nine naming surfaces across the estate and was recorded as `../research/technical-identifier-shapes-inventory-2026-09-14.md`. The operator directed that the result stay in **this** chain rather than become a sibling Dream ("keep it together"), on the reasoning that a reconciled status word is still unreachable if the id carrying it cannot be parsed. `docs/dreams/vocabulary-one-name-one-job.md` gained a `## The shapes` section, four constraints, four non-goals, open questions 12–20, and a third Realization-log entry. **`SPEC.md` was deliberately not edited** — `bmad-spec` derives the Spec from the Dream, and this Spec is `draft` pending eleven operator decisions that the nine new questions do not resolve.
- (decision by claude) Scope split recorded so the Spec does not have to re-derive it: the word side asks *which vocabulary does this term belong to*; the shape side asks *what form is the term written in*. They share the Lexicon's own rule — every noun does exactly one job — applied once to meaning and once to identifiers. Both halves are blocked on operator rulings, so they stay in one `draft` Spec rather than racing each other.
- (note by claude) The strongest finding for the Spec to build on is a positive one: the estate already holds two identifier shapes perfectly — `^### Story \d+\.\d+: .+$` at 952/952 across all eight stations, and 124/124 kebab-case doctor finding codes. Neither is declared anywhere; both are simply obeyed. That is the same "extend the asset, don't replace it" position the Dream already takes on the Dream status ladder.
- (note by claude) The sharpest defect is that one story is spelled three non-derivable ways 2760 times (`### Story 33.9:` / ledger `33-9-<slug>:` / `spec-33-9-<slug>.md`), and the slugify connecting them is not deterministic — 53 of 842 stories carry genuinely different slugs between ledger and spec file. Any CAP proposing a canonical spelling has to name the mint-time function, not just the target shape.
- (correction by claude) RETRACTED AND REPLACED, same session. This entry first claimed that `DW-VOCAB-2026-09-14-5` (steward `epic-18`) and `-6` (marshal `epic-29`) were "fixed in the working tree but still open in their ledgers". **The open/closed half was wrong.** It came from a `grep -A6` that stopped at `severity:` and never reached `status:` — an inference presented as a measurement, which is the exact failure the estate's own "your own prior claim is not evidence" rule names. Both entries read `status: closed`. Of the seven `DW-VOCAB-2026-09-14-*` entries, only these two are closed; `-1`, `-2`, `-3`, `-4` and `-7` are genuinely open.
- (correction by claude) The accurate finding, measured: both are a **complete loop** — raised, fixed, and closed in one changeset. The fixes are correct, not merely present: `## Epic 18` at steward `epics.md:1563` with Stories 18.1/18.2/18.3 following and `## Epic 19` after them; `## Epic 29` at marshal `epics.md:4685` with Stories 29.1/29.2 and `## Epic 30` after. Fleet-wide re-check: 214 epic ledger keys against 214 epic headings across all eight projects, zero mismatches either direction, no story orphaned or re-parented.
- (event by claude) Concurrent-session hazard observed and recorded, since it is the live risk to this whole chain. Between this memlog's creation and the correction above, the shared checkout changed underneath this session: `git status --porcelain` went 36 → 50 entries, most paths moved from unstaged (` M`) to **staged** (`M `), and files untouched at session start — `pixi.toml`, `docs/foundry/frames/**`, `steward/frames.py`, `doctor/sources/board.py` and their tests — appeared modified and staged. A concurrent session had run a broad `git add`, which also swept in this Spec, this memlog and the new research file (`A` entries). Last foreign write 06:35:25, index staged 06:35:03, quiet since; no `.git` lock, no in-flight merge or rebase, no `bmad-loop` process. **Nothing was committed and no index was touched from here** — the estate's rules forbid running git broadly in a shared checkout, and committing 46 files of another session's unreviewed work to `main` is not this session's call. `git show HEAD:…` returns 0 for every artifact in this chain: the Dream, this Spec, this memlog, the research, both epic headings, both ledger entries and both closures are reachable only from the working tree.
- (note by claude) Detector state at the time of writing, so a later reader can tell what this change did and did not cause: `bmad-drift` passes; `dream-chain` passes; the `missing-memlog` finding that prompted this file is now closed. Every other `detectors` finding in the same run (`ad_citation_check`, `chain_currency_sweep_check`, `governance_currency_check` — 3 dead refs, all in `AGENTS.md`/`CLAUDE.md` — `index_freshness_check`, `loop_stall_check`, `unpushed_work_check`, `chain-completeness`, `spec-surface`, `deferred-work`) is pre-existing and untouched by this change. No spec `surface:` governs `docs/dreams/vocabulary-one-name-one-job.md` or `planning-artifacts/research/`, so no baseline stamp was needed or taken.
- (decision by operator, 2026-09-14) **CAP-2 owns the exit-code lattices too** (`DW-VOCAB-2026-09-14-8`, the unification half). Asked "where does the work live" — its own Dream, this CAP, or leave as a ledger entry — the operator chose this CAP: the Dream already owns "one name, one job" for the estate's vocabulary, and a second Dream would split one contract across two chains. Declaration half already done (`docs/reference/judgement-vocabulary.md` + `tests/scripts/test_exit_code_domains_are_declared.py` pinning all four domains against the code); the unification half — one declared exit-code vocabulary that doctor, warden, marshal and `scripts/detectors.py` *read* — is now CAP-2's second success criterion, dispatched as a Story when this Spec flips to `ready`. Constraints carried in: Doctor⊂Warden stays a declared subset (operability, not policy); only the aggregator's `2` genuinely conflicts. SPEC.md CAP-2 amended in place with a dated scope-widening block; no other capability touched.
