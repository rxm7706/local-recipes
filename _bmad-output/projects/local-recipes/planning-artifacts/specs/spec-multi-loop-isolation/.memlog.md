---
topic: Multi-loop isolation harness: concurrent bmad-loop runs via per-loop git worktrees
updated: 2026-07-23T17:57
---

- (direction) User: fold the multi-loop-isolation story into regenerable-factory Wave 0; implement idea->code BEFORE the backfill program runs; idea folds into existing Dream pyforge-marshal (no new Dream)
- (decision) Two shared-state root causes identified: (1) gitignored active-project marker + _bmad-output symlinks are per-working-tree; (2) loop home's git HEAD. Both solved by one worktree per loop home; bmad-loop already worktrees per-story beneath it
- (decision) Canonical Tier-3 stays in the main checkout (NO migration); a loop worktree's projects/<slug>/implementation-artifacts becomes an absolute symlink back to it, keeping the repo-relative path identical for every consumer (generate.py, drift-check)
- (capability) CAP-1 worktree-aware bmad-switch: inside a linked worktree, a missing Tier-3 target auto-provisions as a backlink to the main checkout's canonical dir (mkdir canonical if absent); refuses to clobber a real non-empty local dir; marker+planning symlinks stay per-worktree
- (capability) CAP-2 bmad-loop-worktree provisioner: <slug> -> create/reuse worktree on branch loop/<slug>, run bmad-switch inside, verify, print launch line (cd + BMAD_ACTIVE_PROJECT=<slug> + loop cmd); --remove cleans up
- (capability) CAP-3 isolation verification: --verify asserts independent marker/symlinks per worktree, shared canonical Tier-3 (same realpath), main checkout untouched
- (constraint) Deterministic harness, not a skill (Marshal doctrine); stdlib-only Python in scripts/, matching bmad-switch style
- (constraint) Loop merges publish via git push origin HEAD:main (rebase/retry on non-FF); never checkout main in a second tree
- (constraint) BMAD_ACTIVE_PROJECT exported in each loop env as belt-and-suspenders (outranks marker; hard-coded planning_artifacts key still resolves via per-worktree symlinks)
- (assumption) bmad-loop resolves sprint feed + journals via repo-relative implementation-artifacts of its CWD (backlink makes that canonical); loop agents inherit env
- (note) Non-goals: upstream bmad-method planning_artifacts composition fix (filed as Herald/upstream scope); modifying bmad-loop itself; win-64 (loop is Linux/macOS)
- (event) Self-validate pass 1 (coherence): PASS — 3 caps with intent+success, constraints bend design, non-goals present, success signal testable (CAP-3 exit code), zero open questions
- (event) Self-validate pass 2 (preservation): PASS — every memlog claim landed in kernel (direction->frontmatter, decisions->Why/Constraints, assumption->frontmatter); no wrapper-only drops
