---
topic: herald CLI (pyforge.herald) formalizing the realized Design↔Code bridge — seed/pull/status/watch over the claude-design MCP round-trip
updated: 2026-07-23T12:40
---

- (direction) User: Genesis step 2 — first Dream-first bmad-spec run; distill docs/dreams/design-code-bridge.md into the herald CLI contract (seed/pull + watch + stale-mirror per Dream's Beyond-the-pilot)
- (decision) Slug inherited from the Dream (design-code-bridge); spec folder under active project pyforge-herald planning-artifacts/specs per Dream-first tier model
- (decision) Express distill (input rich: Dream + AGENTS.md bridge section + presentation-deck MCP-bridge playbook + 2026-07-23 pilot evidence); interactive session
- (capability) CAP-1 seed: create/refresh a Modernist-bound Design project for a deck slug with runtime + contract-proven starter prototype; success = seeded prototype passes repo extract+build; re-seed conflicts refused
- (capability) CAP-2 pull: land the Design prototype into presentations/<slug>/project/ and re-derive (extract, build, deck-export); unchanged = cheap etag no-op; success = post-edit pull yields green build + regenerated exports
- (capability) CAP-3 status: machine-readable per-deck bridge state (linked project, etags, unchanged/changed/conflict/unlinked) incl. stale hand-mirror detection (Design project mirroring a repo app tree)
- (capability) CAP-4 watch: poll-based continuous sync; a Design edit lands via auto-pull within one interval; unchanged polls write nothing
- (constraint) Only the prototype crosses the bridge — never a mirrored app tree (Local recipes repository connection is the cautionary tale)
- (constraint) Prove-before-cross: prototype must pass local extract+build before any seed write
- (constraint) Etag discipline both directions: if_match/if_none_match on every cross-surface op; unconditional writes forbidden; conflicts surface structurally, nothing clobbered
- (constraint) Design-side write protocol: get_claude_design_prompt gate + finalize_plan declared write-set before writes; entity-escaped read bodies decoded on pull
- (constraint) User-facing links are claude.ai/design URLs only; serve_url tokens never in output or files
- (constraint) pyforge persona decks bind the Modernist design system (fbc1d6c8-b35f-4df6-9044-a64d2675427b)
- (constraint) Acceptance criteria stay framework-agnostic + machine-checkable (AGENTS.md Portability contract); CLI exit codes + file artifacts are the oracle
- (constraint) Clear structured failure when the claude-design surface is unreachable — never a silent no-op
- (assumption) Pull leaves git commits to the operator by default (pilot committed via session); an opt-in --commit may exist
- (assumption) Watch is poll-based — no push/webhook channel exists on the Design surface
- (assumption) Deck slugs are presentations/<slug>/ dirs with the standard engine layout + README 'Design project' registry line (the pilot's convention)
- (question) Standalone auth/transport: how does the herald CLI reach the claude-design MCP surface OUTSIDE a Claude Code session (the pilot ran inside one)? Blocking for implementation, not for the contract
- (question) Watch cadence + rate limits: acceptable poll interval vs Design-surface quotas?
- (question) Marp/infographic source bridging (Dream's fuller scope): confirm v1 non-goal or pull them too?
- (note) Pilot evidence 2026-07-23: 4 decks seeded (marshal ad84d4f6, herald ff879a32, mason a7a2c3b1, doctor 46dbbdea), all extract 10/10 + build green; pull returned unchanged:true etag short-circuit; project IDs recorded in deck READMEs
- (event) Self-validate pass 1 (coherence, Spec Law 1-6+8): PASS — 4 capabilities all intent+success; intents WHAT-only (HOW isolated in bridge-protocol.md); all 8 constraints rule something out; 6 non-goals; success signal machine-checkable (exit codes + files); CAP IDs fresh CAP-1..4
- (event) Self-validate pass 2 (preservation): PASS — every load-bearing Dream claim landed (pain story->Why; loop->CAP-1/2+protocol; etag/only-prototype discipline->Constraints; watch+stale-mirror->CAP-3/4; marp bridging flagged as OQ+non-goal not dropped; pilot log->protocol evidence table). Wrapper-only content dropped: the Dream's narrative color (Herald's-first-organ framing) beyond what Why carries
- (event) Spec finalized (create): SPEC.md + bridge-protocol.md derived from memlog; adopted companion docs/specs/presentation-deck.md; source docs/dreams/design-code-bridge.md fully absorbed
