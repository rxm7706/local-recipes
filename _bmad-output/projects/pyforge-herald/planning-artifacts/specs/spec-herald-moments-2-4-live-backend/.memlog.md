---
topic: Herald Moments 2-4 as a genuinely live service (DB, webhook, scheduler) — the deferred contract behind the shipped CLI-triggered v1
updated: 2026-09-09T15:50
---

- (provenance) THIS RECORD STARTS HERE. Created 2026-08-09 by marshal S-13.5 (spec-surface-drift-reconciliation CAP-5 / FR-168): this Spec shipped declaring a `surface:` with no .memlog.md, so spec_surface_check's contract_hash() returned "" and the contract could never move — every governed change would have reported a hard [drift] whose printed remedy ("reconcile the spec") was unreachable, there being no contract to move. Nothing that happened before this entry was captured; do not read the absence of earlier entries as an absence of decisions. The baseline was scoped-stamped in the SAME change that created this file, because a new memlog moves the contract hash off "" and would otherwise downgrade this surface's next real drift from gating [drift] to informational [drift-presumed].
- (note) Status draft, DEFERRED AND UNBUILT by design. The Spec exists to satisfy INV-1 (every Dream carries a Spec) for a Dream that is real but deliberately not scheduled. Nothing in it is required for, or blocks, the v1 that shipped 2026-08-08. `status: draft` means authorized to be planned when prioritized.
- (capability) LB-1 DB-backed storage layer — replace .herald/{progress,claims,notices-index}.json with a real database carrying the same schemas, swapped BEHIND the existing function seam (progress.upsert, claims.create/publish, notices.author/publish/close are already pure (path, **fields) -> record functions), with the CLI/web-tab contract unchanged. Notices' git-tracked markdown stays the durable copy; only the index/cache layer moves.
- (capability) LB-2 Webhook endpoint — /api/herald/webhooks/on-ship and /on-pr-close that CI actually calls, creating progress records and success-claim drafts automatically rather than by operator memory; HMAC signature verification, retry/backoff, operator-alert delivery.
- (capability) LB-3 Cron scheduler — weekly progress aggregation and 7-day evidence re-validation actually running on a schedule, replacing the operator-remembered `herald progress --update`.
- (note) The v1 pivot traded away exactly the property this Spec preserves: an unrecorded ship is indistinguishable from no ship (technical research risk #2).
- (note) SURFACE CAVEAT, load-bearing for anyone reading the drift verdict: this Spec's `surface:` list is part real glob, part PROSE — three of its four entries describe systems that do not exist yet ("a real database ...", "a webhook HTTP endpoint ...", "a cron/scheduled-job runner ..."). Only src/shared/packages/pyforge-herald/src/pyforge/herald/** matches tracked files, and it alone accounts for all 18 governed files. The prose entries match nothing and are therefore silent — a glob matching nothing is not a finding.
- (question) OPEN, carried from the Spec: is there real pull for this at all (v1 has zero production-usage evidence)? Where would a persistent Herald backend run, and under whose operational ownership (likely a Herald x Steward estate question)? SQLite convergence vs per-file fcntl locking for the concurrency prerequisite? What triggers CI to call the webhook, with what authentication?
- (decision) ADOPTION (operator, 2026-08-09): this Spec ADOPTS steward spec-secure-live-dashboards rather than building its own backend. That answers open question 2 verbatim -- where a persistent Herald backend runs and under whose operational ownership -- with: STEWARD operational ownership, via that pattern CAP-6 deployment perimeter. Herald is its second named adopter after atlas. The Herald x Steward question the Spec anticipated is now decided, not merely flagged.
- (constraint) WHAT ADOPTION DOES AND DOES NOT RESOLVE, stated so it is not overclaimed. RESOLVED: hosting location and operational ownership (open question 2); Herald inherits identity (CAP-1), audit trail (CAP-4), role isolation (CAP-2/3), the WSGI/container/edge perimeter (CAP-6) and the non-vacuous security suite (CAP-7) instead of inventing them. NOT RESOLVED: LB-1 DB storage layer, LB-2 webhook receiver and LB-3 cron scheduler are still Herald work -- the pattern EMITS security webhooks, it does not RECEIVE them, and its audit store is not a general application store. Also NOT resolved: the concurrency-lock prerequisite, which self-blocks this Dream regardless of where it is hosted and still comes first.
- (note) Adoption forced two changes in the adopted pattern, recorded in its architecture spine: AD-8 (the pattern binds at the WSGI/request layer, not to a dashboard framework) exists because Herald is a stateless CLI plus a STATIC dashboard and this Spec refuses to commit a framework; AD-9 (machine callers authenticate by HMAC proof, never by ingress, and are never granted a human role) exists because LB-2 webhook receiver is an inbound non-human caller that the pattern AD-4 would otherwise refuse. Neither was visible while atlas was the only adopter.
- (event) DE-REGISTERED from DEFERRED_SPECS (2026-08-10, explicit operator confirmation 'de-register rebuild, herald-live-backend, secure-live-dashboards'). Decomposition landed first so INV-A holds on the real condition rather than on a suppression entry
- (decision) SERVERLESS INTERMEDIATES RESOLVED (Story 13.2, 2026-08-11): the Spec's three named
  cheaper steps are judged individually, not with one blanket verdict -- two dispositions across
  three items: (1) `herald snapshot` -- BUILD, tracked as a deferred-work entry (not gated on
  Epic 13; see deferred-work.md), not skipped. (2) Telemetry-derived defaults for `herald
  progress --update` -- SKIP. (3) Locking + hook-triggered CLI invocation -- SKIP. Current-state
  confirmed directly against the shipped code, not assumed from the 2026-08-08 research doc:
  none of the three exist today -- `herald snapshot` as a command, the other two as
  CLI/automation behaviors. Scope note: the research doc names five serverless-intermediate steps
  in priority order; epics.md's own Constraint 2 binds this story to only these first three --
  `herald notice reindex` and hosting the static bundle are real research-doc suggestions but
  outside this story's binding scope, not an oversight.

  (1) `herald snapshot` BUILD: export_web_snapshot.py's own docstring already designs itself as
  the shared exporter and explicitly anticipates "a future Epic 8/10 snapshot adds a sibling
  export_*_snapshot function here rather than a duplicate script"; export_notices_snapshot.py's
  docstring says a later story can fold all three in "once the shape each Moment needs is
  settled" -- that precondition is now met (all three exporters exist and ship stable output).
  This would close a real, currently-live risk (research risk #1 of 6, staleness with no
  freshness signal -- no JSON record anywhere carries a generated_at stamp; only
  sync-progress.mjs is wired into npm hooks) in the CURRENTLY-SHIPPED v1 dashboard, independent
  of whether/when the live backend (LB-1/2/3) lands -- so it is Epics 9/10 follow-up, not new
  Epic 13 scope, and does not need insertion into Epic 13's own story numbering. Filed as a
  deferred-work entry for pickup, rather than built inside this Effort:S decision-only story.

  (2) Telemetry-derived defaults SKIP: bounded value regardless of timing -- reduces keystrokes
  on an already-working, already-accepted 4-flag CLI flow, not a correctness or automation-
  completeness gap. Story 13.4 (Effort: L per epics.md, now clear to dispatch once 13.3 completes
  -- steward:S-9.1 landed 2026-08-11) is expected to substantially reduce or eliminate this need
  once its own still-open first AC (which CI events trigger it, what payload they carry) is
  resolved; building a partial auto-fill now duplicates logic the eventual webhook handler may
  own, for a benefit smaller than 13.3+13.4's combined committed cost (Effort L, L -- 13.5/13.6
  don't functionally overlap with this item, so aren't the relevant comparison).

  (3) Hook-triggered CLI SKIP: Story 13.1's lock (done) closes the core lost-update race for the
  write paths a hook would exercise (progress/claims/notices read-modify-write -- not state,
  which is scoped to deck-bridge commands only), proven by concurrency tests in
  tests/test_locking.py, tests/test_progress.py, tests/test_claims.py, tests/test_notices.py
  (tracked, merged at this story's baseline_revision). Its two pass-7 deferred items (one medium,
  one low; independently verifiable in the now-promoted spec-13-1-*.md) are both in claims.py's
  revalidate/revalidate_all path, not the progress/success/notice write paths a hook-triggered
  CLI would call, so they don't bear on this trigger's safety. One separate, real residual DOES
  sit in a hook-relevant path: notices._write_markdown is still a non-atomic write_text, so a
  concurrent reader can still observe a torn file even after this lock -- the lock closes
  writer-vs-writer races, not reader-vs-writer atomicity, and this safety claim is scoped to the
  former only. Safe enough to build for that narrower purpose, but not worth building: as a CI
  job it would be a straightforward duplicate of Story 13.4's webhook trigger surface; as a local
  git hook it is narrower (opt-in, single-operator) but still a second maintained path that
  doesn't cover the multi-operator/CI case 13.4 exists for. Either reading needs its own eventual
  deprecation once 13.4 (Effort: L) ships. This cost is sharper now, not softer: with
  steward:S-9.1 landed, the interim window either reading would fill is bounded, not open-ended
  -- Story 13.3+13.4 combined (Effort L, L; 13.5/13.6 aren't the relevant comparison here either)
  -- a throwaway duplicate trigger buys even less than when this was first drafted.

  Net: the operator's 2026-08-09 Steward-hosting adoption and 2026-08-10 funding go-ahead already
  settled WHETHER to fund Epic 13; that reprioritizes but does not itself answer each
  intermediate's standalone value, so each item was judged on its own cost/value against Epic
  13's committed LB-1/2/3 work (epics.md Effort sizes: 13.3 L, 13.4 L, 13.5 M, 13.6 M). Items (2)
  and (3) don't clear that bar; item (1) does, as non-Epic-13-gated follow-up. No Epic 13 stories
  inserted or renumbered; Epic 13 proceeds to Story 13.3 next, unmodified.
- (note) CLOSING two threads. First, a stale open-question clause: the "(question)" bullet's "is
  there real pull for this at all... zero production-usage evidence" clause was already answered
  by the operator's 2026-08-10 go (epics.md Epic 13 preamble: "Operator go: 2026-08-10...
  answering the Spec's own leading question"), left unmarked here until now -- CLOSED (for
  funding-readiness purposes, matching epics.md's own framing -- not independent evidence of
  operator demand for the automated-feel itself, which remains genuinely unmeasured). The same
  bullet's SQLite-vs-fcntl clause is ALSO closed, not open: Story 13.1 (done) answered it as its
  own first AC -- per-file advisory locking (fcntl/msvcrt), not SQLite, recorded in locking.py
  and DW-1-4-2's closure. Only the "what triggers the webhook, with what authentication" clause
  remains genuinely open, tracked as Story 13.4's own first AC. Second, ground truth as of
  2026-08-11 09:17: steward:S-9.1 (Story 13.4's cross-station dependency) is DONE, not backlog --
  landed after this story's own baseline_revision, confirmed via pyforge-steward's own
  implementation-artifacts/sprint-status.yaml. Its TRACKED twin,
  pyforge-steward/planning-artifacts/sprint-status-ledger.yaml, still reads 9-1: backlog as of
  its last regeneration (2026-08-10, before the landing) -- stale, the one artifact a reader
  confined to this repo without cross-project access can actually check, so don't be misled by
  it; the implementation-artifacts source is the current one. Herald's own sprint-status.yaml
  also still shows 13-4: blocked as of this writing; both stale lines should be corrected
  whenever Story 13.3/13.4 is next picked up (out of scope here -- this story's Never-list
  forbids editing sprint-status.yaml).
- (decision) DRIFT-PRESUMED RECONCILED, 2026-08-11. 5 files against this Spec's
  `src/shared/packages/pyforge-herald/src/pyforge/herald/**` surface flagged
  [drift-presumed] (the memlog had moved via the Story 13.2 entries above but never
  named these paths):
  `src/shared/packages/pyforge-herald/src/pyforge/herald/claims.py` (changed),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/locking.py` (added),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/notices.py` (changed),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/progress.py` (changed),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/state.py` (changed). All
  five are Story 13.1's own state-layer/concurrency-lock implementation -- exactly
  the "Story 13.1's lock (done)" work already discussed above -- landed in the single
  bmad-loop merge commit cfc951aac9 via PR #422 ("land herald 13-1 — the concurrency
  prerequisite, converged on its last review", merged 2026-08-11T04:52:59Z) and never
  reconciled at that landing. Verified per-path with `git log --follow -1` on each of
  the five: every one resolves to that same commit, nothing else is mixed in.
  Baseline scoped-stamped in this same change
  (`--write-baseline --spec pyforge-herald/spec-herald-moments-2-4-live-backend`).
- (decision) SURFACE DRIFT RECONCILED, 2026-08-13 (landed by hand after a cross-station deferral —
  see `pyforge-marshal/spec-pyforge-marshal`'s own memlog for the full account). The same six
  `src/shared/packages/pyforge-herald/src/pyforge/herald/**` paths as the 2026-08-11 entry above —
  `claims.py`, `deck_pipeline.py`, `notices.py`, `progress.py`, `registry.py`, `state.py` — changed
  again, this time by marshal Story 14.2 ("consolidate atomic-write into pyforge-core", commit
  `1d60da0193`): each file's own atomic-write-shaped write re-pointed at the new
  `pyforge.core.atomic_write` primitive. This Spec claims no capability here — CAP-2 ("atomic
  write has one implementation") is `pyforge-marshal/spec-pyforge-core`'s own contract; this entry
  reconciles surface/drift bookkeeping only, per the same convention the entry above already
  established. Also reconciled in the sibling `spec-pyforge-herald` (which additionally governs
  `pixi.toml`/`pyproject.toml`/`test_registry.py` for this same commit), each baseline-stamped
  independently via its own `--spec` scope.
- (decision) LB-1 IMPLEMENTED, Story 13.3 ("DB-backed storage behind the existing seam, with
  migrations"), landed 2026-08-13 in the single commit `20b88dd689`. Four of this Spec's governed
  `src/shared/packages/pyforge-herald/src/pyforge/herald/**` paths changed:
  `src/shared/packages/pyforge-herald/src/pyforge/herald/claims.py` (changed — internals swapped
  from JSON-file I/O to `db.py`, public API/error types unchanged, the unlocked-validate-then-
  recheck pattern preserved via `db.transaction`),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/db.py` (added — the new stdlib-sqlite3
  connection/migration/transaction module: WAL + busy_timeout, a version-tracked migration
  runner, a reentrant ambient-transaction context manager replacing `locking.locked()` for these
  three modules, and a one-time legacy-JSON import),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/notices.py` (changed — index-related
  functions swapped to `db.py`; `_write_markdown` and the markdown path untouched), and
  `src/shared/packages/pyforge-herald/src/pyforge/herald/progress.py` (changed — same swap,
  smallest schema). This is exactly the storage-layer swap this Spec's `surface:` list already
  authorized ("a real database ... replacing .herald/progress.json / claims.json /
  notices-index.json"): `.herald/herald.db` (stdlib `sqlite3`, one shared file for all three
  schemas) is the "SQLite-with-locking" option this Spec's surface named. LB-2 (webhook, Story
  13.4) and LB-3 (cron scheduler, Story 13.5) remain unbuilt. Also reconciled in the sibling
  `spec-pyforge-herald` (which additionally governs 14 more paths — docs, scripts, tests, and
  `web/package.json`/`sync-progress.mjs` — for this same commit), each baseline-stamped
  independently via its own `--spec` scope.
- (decision) LB-1 REVIEW-PASS HARDENING, Story 13.3, 2026-08-13 (second review pass on the
  already-`done` spec). One further governed path changed beyond the four named in the entry
  above: `src/shared/packages/pyforge-herald/src/pyforge/herald/cli.py` (changed — user-facing
  text only, no behavior: `--repo-root`'s `--help` string and three docstrings still named
  `.herald/claims.json`, a file this Spec's own storage swap had just deleted, so `herald success
  --help` contradicted the runbooks the same commit rewrote). The storage swap's public contract
  is unchanged by this pass; the code changes it did make are confined to `db.py`'s already-named
  connection/transaction seam (read-side error wrapping to `errors.HeraldError` for AD-6 parity,
  `STRICT` tables, side-effect-free reads, and a SQLITE_BUSY-only retry in `_set_wal_mode`). Also
  reconciled in the sibling `spec-pyforge-herald` (which additionally governs the two other
  exporter scripts and `web/README.md` for this same pass).
- (decision) LB-1 REVIEW-PASS HARDENING (third follow-up pass), Story 13.3, 2026-08-13. Two
  further governed paths changed beyond those named above, both stale-reference only:
  `src/shared/packages/pyforge-herald/src/pyforge/herald/errors.py` (changed -- `ClaimNotFoundError`'s
  docstring still said `claims.json`) and
  `src/shared/packages/pyforge-herald/src/pyforge/herald/locking.py` (changed -- its docstring
  still claimed scope over the three modules LB-1's storage swap moved onto `db.transaction`;
  `state.py` is its one remaining caller). The pass's behavior changes stay inside the already-named
  `db.py`/`claims.py`/`notices.py` seam and do not alter LB-1's public contract: the legacy import
  is now skipped per store when its table already holds rows (a database restored from a logical
  dump reran the v1 migration and silently re-imported every claim, since `claims.id` is
  deliberately not a key), `db.connection` translates the write paths' full exception set rather
  than `sqlite3.Error` alone, `claims`' three network-window writers wrap their in-transaction
  read the way `create` already did, and `author_notice` defers deleting a relocated notice's old
  markdown until after the commit so a rollback cannot leave the index pointing at a deleted file.
  Also reconciled in the sibling `spec-pyforge-herald` (which additionally governs the three web
  panels for this same pass), each baseline-stamped independently via its own `--spec` scope.
- (decision) SURFACE DRIFT RECONCILED, 2026-08-13 (dev-verify repair pass on marshal Story 14.3,
  "one lattice, one envelope, one exception root", commit `1bd8c8a2de`, not a herald-owned story):
  `src/shared/packages/pyforge-herald/src/pyforge/herald/errors.py` changed again — the one
  `HeraldError(Exception)` line became `HeraldError(PyforgeError)` (CAP-5's marker-base
  re-parenting) and `exit_code_for`'s hand-rolled most-specific-first isinstance loop now delegates
  to the new `pyforge.core.verdict.dispatch_exit_code(exc, table, default)` generic helper (CAP-3),
  same algorithm, extracted once; the `_EXIT_BY_ERROR` table itself is unchanged, still owned here.
  This Spec claims no capability for either change — CAP-3 ("the verdict lattice is declared once")
  and CAP-5 ("one exception root") are `pyforge-marshal/spec-pyforge-core`'s own contract; this
  entry reconciles surface/drift bookkeeping only, per the same convention the two entries above
  already established. Also reconciled in the sibling `spec-pyforge-herald`, each baseline-stamped
  independently via its own `--spec` scope. `pyforge-herald-test` green post-change (dispatch
  delegation + exception re-parent, per Story 14.3's own verification list).
- (decision) LB-1 IMPLEMENTED (scheduler entry point), Story 13.5 ("the scheduler enforces what was
  displayed"), landed 2026-08-13 in the single commit `18dca9e3ad`. Three of this Spec's governed
  `src/shared/packages/pyforge-herald/src/pyforge/herald/**` paths changed:
  `src/shared/packages/pyforge-herald/src/pyforge/herald/scheduler.py` (added -- composes
  `claims.revalidate_all` + `progress.write_snapshot` into one `herald scheduler run` invocation, no
  new scheduler dependency, driven by an operator-local crontab entry per this story's own Design
  Notes), `src/shared/packages/pyforge-herald/src/pyforge/herald/cli.py` (changed -- registers the
  verb), and `src/shared/packages/pyforge-herald/src/pyforge/herald/progress.py` (changed -- exposes
  the composed snapshot-export call). Also reconciled in the sibling `spec-pyforge-herald` (which
  additionally governs 7 more paths -- docs, scripts, and tests -- for this same commit), each
  baseline-stamped independently via its own `--spec` scope. Reconciled at landing time 2026-08-13
  (the loop's own dev-verify pass never reconciled either spec for this story).
- (change) S-13.4 the webhook endpoint CI calls (LB-2) merged via `bmad-loop/20260813-145413-7eb9/13-4-the-webhook-endpoint-ci-calls`, reconciled by name at landing time 2026-08-13 (the loop's own dev-verify pass never reconciled this Spec for the story). This is the deferred live-webhook contract this Spec exists to close: `src/shared/packages/pyforge-herald/src/pyforge/herald/webhook.py` (new) adds HMAC-verified, retry/backoff `on-ship`/`on-pr-close` handlers behind a raw ASGI3 boundary (AD-9, AD-8-shaped: no adopter framework coupling), and `cli.py` changes incidentally alongside it. Mounting the app into a live host and composing Steward's S-9.1 identity/trust boundary around it is Story 13.6's job, not this one's. Also reconciled in the sibling `spec-pyforge-herald` (which additionally governs 4 more paths — docs and the broader test files — for this same commit), each baseline-stamped independently via its own `--spec` scope.
- (event) Spec-surface reconciliation for the S-13.4 landing complete as of this entry; baseline re-stamped `--spec pyforge-herald/spec-herald-moments-2-4-live-backend` immediately after.
- (change) S-13.6 a ship records itself, end to end (LB-2 + LB-3 composed) merged via `bmad-loop/20260813-145413-7eb9/13-6-a-ship-records-itself-end-to-end`, reconciled by name at landing time 2026-08-13 (the loop's own dev-verify pass never reconciled either owning Spec for this story). This is the closing move of the deferred live-backend contract this Spec exists to close: `webhook_host.py` (new) is the actual daphne-pointable ASGI host that makes the webhook endpoint (13.4) and the scheduler (13.5) real, listening processes rather than fully-tested-but-never-run code -- a lazily-built `application` object (PEP 562 module `__getattr__`), a bounded request timeout, and a dedicated executor, all living in this new host-wiring module specifically so `webhook.py` itself stays a zero-framework-dependency ASGI3 callable per AD-8. `webhook.py` changes only incidentally alongside it. `.github/workflows/herald-live-demo.yml` and `test_webhook_live_smoke.py` are the two genuinely-live proofs (a real daphne subprocess over a real socket) that this contract's "genuinely live service" claim now holds, not just a fully-mocked test suite. Files under this Spec's own glob: src/shared/packages/pyforge-herald/src/pyforge/herald/webhook.py, src/shared/packages/pyforge-herald/src/pyforge/herald/webhook_host.py (new). Also reconciled in the sibling `spec-pyforge-herald` (which additionally governs 6 more paths — docs, `pyproject.toml`, and the broader test files — for this same commit), each baseline-stamped independently via its own `--spec` scope.
- (event) Spec-surface reconciliation for the S-13.6 landing complete as of this entry; baseline re-stamped `--spec pyforge-herald/spec-herald-moments-2-4-live-backend` immediately after.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Surface reconcile 2026-08-31: 4 drift-presumed paths (new files) since baseline at `18dca9e3ad` (S-13.6 landing). Ten commits, Epic 14-15 herald deck pipeline delivery (all done in ledger):
  - `c47a46d1b7` — Story 14.1 (done): `deck_qa.py` visual-QA gate report interface.
  - `9de381a01e` / `ef0b9482d2` — Story 14.2 (done): headless render gate hardening on deck_qa.py.
  - `3ab6509823` — Story 14.3 (done): image-slot scan gate on deck_qa.py.
  - `788423fc56` — Story 15.1 (done): template-parse-then-fill; `pptx_pipeline.py`, `templates/pyforge-deck-template.pptx` landed.
  - `0888084eff` / `ab1a988543` / `00d6ef4c91` — Story 15.2 (done): dense-content shapes + Pillow autofit; deck_qa.py + pptx_pipeline.py.
  - `a6288ed157` — Story 15.2 follow-up: `exporters.py` extracted as deck-export plugin surface.
  No CAP text change. RECONCILES: `deck_qa.py`, `exporters.py`, `pptx_pipeline.py`, `templates/pyforge-deck-template.pptx`. Scoped-stamped: `--write-baseline --spec pyforge-herald/spec-herald-moments-2-4-live-backend`.
- (decision by claude) 2026-09-09 "IS THERE REAL PULL FOR THIS AT ALL?" — ANSWERED IN THE NEGATIVE (operator, fleet-readiness-decision-batch-2026-09-09 rows C11 / herald-B1 / Class A herald-OQs). The evidence is now in and it is one-sided: `.github/workflows/herald-live-demo.yml` — the ONLY thing in the estate that ever starts a listening Herald process — is `disabled_manually`, its last 100 runs are 100 failures with zero successes, and its last run was 2026-08-24; its own header states it is "never a persistent, publicly-reachable deployment" and writes each job to a `${{ runner.temp }}` DB "discarded when the job ends", so even a green run would not record a ship; `webhook_host.py:1-8` states plainly that `webhook.py` and `scheduler.py` "were both built and fully unit-tested but never run as a real, listening process"; and DW-13-6-1 records that steward `deploy perimeter` cannot target an arbitrary ASGI callable — `deploy.py:484` renders only a hardcoded `myproject.asgi:application` with no `--asgi-application` flag — so the adopted "runs on Steward's perimeter" Constraint has no executable path today. Thirteen months after the 2026-08-09 hosting-half answer, no ship has ever recorded itself.
- (decision by claude) 2026-09-09 THE PULL QUESTION RE-OPENS AS A *HOSTING* DECISION, NOT A DEMAND QUESTION (operator, batch row C11). Deferred until the python-foundry cutover gives Herald a real perimeter; LB-2 (the webhook endpoint) and LB-3 (the scheduler) are re-scoped `foundry-side` — code-complete, unexercised, and not chargeable to this Spec until a perimeter exists to mount them on. LB-1 is the exception and is genuinely closed: `db.py` (667) + `locking.py` (208) shipped and the second-writer concurrency prerequisite holds. Until then `herald-live-demo.yml` stays disabled — re-enabling it would buy a green run against a throwaway store, which is not the success signal.
- (constraint by claude) 2026-09-09 THE ROUTE LITERALS VIOLATE THE STATION ROUTE CONTRACT AND MUST MOVE BEFORE ANY MOUNT (batch row C11). `webhook.ON_SHIP_PATH = "/api/herald/webhooks/on-ship"` / `ON_PR_CLOSE_PATH = "/api/herald/webhooks/on-pr-close"` (`webhook.py:183-184`) sit on the bare `/api/` namespace the platform FastAPI seam owns — `config/asgi.py:149-150` routes EVERY `/api/*` path to `fastapi_application`, and only `/stations/<name>/api/v<N>/` is diverted to a station sub-app (`config/asgi.py:132-144`, `config/station_api.py:24-32`) — so mounting `webhook_host:application` in the monolith today is a silent 404. `spec-pyforge-unifying-strategy` SPEC.md:497 carries this as an Always. Correct target: `/stations/herald/api/v1/webhooks/{on-ship,on-pr-close}`; only warden v1 is registered on that seam today (`station_api.py:146`). The Spec's `surface:` block enshrines the wrong path. Vessel: herald Epic 19 Story 19.1 (batch row C6).
- (note by claude) 2026-09-09 BODY RE-GROUNDING OWED ON RE-DERIVE (batch § 2.4 D1). Body § Constraints stale: says the backend "runs on Steward's perimeter, under Steward's operational ownership, by adopting spec-secure-live-dashboards" and that "adoption resolves hosting, not the build"; live is the reverse — `pyforge-herald/pyproject.toml:38-45` declares no `pyforge-steward` dependency and `webhook_host.py:77-89` imports only `webhook` + `errors`, mounting no Steward middleware, so AD-8/AD-9 bought a *design* constraint on `webhook.py` (framework-free ASGI3 callable; HMAC machine-caller proof — real and correctly honoured) and left hosting open. Body § surface stale: says the endpoint path is `/api/herald/webhooks/on-ship`; live contract is `/stations/herald/api/v1/…` (see the preceding constraint entry).
- (event by claude) status: ready -> in-progress — 2026-09-09, batch row § 2.2 herald (fleet-readiness-decision-batch-2026-09-09). Epic 13 is 6/6 `done` and LB-1 shipped, but LB-2/LB-3 have never run green in the estate and are re-scoped `foundry-side`; `ready` overstated a Spec whose decomposition is complete and whose criterion is unexercised.
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. LB-1/LB-2/LB-3 preserved with realization status recorded; status ready -> in-progress; surface webhook path corrected to /stations/herald/api/v1/webhooks/*; open_questions retired the pull question and the SQLite-vs-fcntl question (both answered, ANSWERED comments left in place) and now carries the re-opened hosting question plus the still-open CI-trigger question; route-contract constraint added; Steward-perimeter and adoption-resolves-hosting constraints re-grounded; herald-live-demo-stays-disabled constraint added.
