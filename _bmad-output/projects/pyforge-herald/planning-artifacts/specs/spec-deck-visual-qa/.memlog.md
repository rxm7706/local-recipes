---
topic: deck-visual-qa (genesis baseline bootstrapped 2026-08-22, marshal Story 25.3)
updated: 2026-09-09T15:51
---

- (event) Genesis baseline 2026-08-22 (marshal Story 25.3, spec-bmad-611-era-alignment CAP-3): .memlog.md bootstrapped so 6.11 bmad-spec updates succeed against this folder — a structural bootstrap only, per the S-13.7 pattern; no governed-file content is claimed reconciled by this entry
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (decision by claude) 2026-09-09 OQ-1 (ENTRYPOINT PLACEMENT) CLOSED — DECIDED IN SHIPPED CODE: the Epic-6 CLI dispatcher won; no per-deck `scripts/` step was added. `herald deck qa <slug> [--repo-root]` is registered at `cli.py:331-341` (parser), dispatched at `cli.py:764-765`, handled at `cli.py:982-995`. Batch row `herald-OQs` (fleet-readiness-decision-batch-2026-09-09 § 2.1, report § D F-8). Classification (a) answerable from evidence — retire from `open_questions:` on re-derive.
- (decision by claude) 2026-09-09 OQ-2 (SERVE MODE FOR THE HEADLESS RENDER) CLOSED — DECIDED IN SHIPPED CODE AGAINST `file://`: a throwaway loopback static server. `deck_qa.py:143-152` binds `socketserver.TCPServer(("127.0.0.1", 0), handler)` on a daemon thread, with the reason recorded inline — "Chromium needs a real HTTP origin, not `file://`: the deck is a Vite bundle with dynamically-imported JS chunks, and Chromium blocks cross-origin fetch/module-import under `file://`"; slides are driven at `http://127.0.0.1:{port}/#/{index+1}` (`deck_qa.py:341`). The spike the question asked for was run and settled. Batch row `herald-OQs` (§ 2.1, report § D F-9). Classification (a) — retire from `open_questions:` on re-derive.
- (note by claude) 2026-09-09 BUILT, NOT IN EFFECT — recorded under the realization gate (batch § 2.3 C6). Epic 14 shipped all three gates (14.1/14.2/14.3 `done`; `deck_qa.py`, 665 lines) and nothing calls them: no pixi task (`pixi.toml` mentions `deck_qa` only in the playwright dependency comment, line 2352), no CI job (`grep -rn "deck qa" .github/workflows/` empty), and `docs/specs/presentation-deck.md` — the workflow every deck build follows — carries zero reference to the gate, so its verify checklist is still purely run-shaped, which is the exact gap this Spec was written to close. No run artifact exists: `render_gate` writes `.herald/deck-qa/<slug>/render/` (`deck_qa.py:117`, `:245-250`) and `.herald/` does not exist in the tree. Vessel: herald Epic 19 Story 19.3.
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. CAP-1/CAP-2/CAP-3 preserved unchanged; status stays ready; both open questions retired (ANSWERED comments kept in frontmatter) and their answers promoted to Constraints — CLI-dispatcher entrypoint, loopback-HTTP-origin render; realization status 'built, not in effect' recorded under Success signal with vessel Epic 19 Story 19.3.
