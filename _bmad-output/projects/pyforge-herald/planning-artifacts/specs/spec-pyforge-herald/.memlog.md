---
topic: pyforge-herald
updated: 2026-09-20T07:50
---

- (event) Epic 1 stories 1.4 and 1.5 SHIPPED 2026-07-30/31 via the six-station fleet run on loop/pyforge-herald, unattended with the standing independent review. Merges bccda6dac0 (1.4 bridge-core skeleton, dev x1 / review x3, 13.79M weighted — the most expensive story of the run and the only one to converge ON the review cap rather than short of it) and 8857d98a3e (1.5 registry module, dev x1 / review x3, 6.55M). Landed bridge.py, registry.py, state.py and errors.py with cli.py wired to dispatch them, plus five test modules. 276 tests green.
- (note) Story 1.3 (fallback transport adapter) was DEFERRED after BOTH dev attempts crashed silently mid-thinking — no error text, logs ending on a spinner frame, 36,261 weighted tokens total. Ruled out by direct check: OOM (39 GiB free, no kernel records), session timeout (~6-7 min against a 180-min ceiling), environment fault (env_fault: false). Its branch carries ZERO commits: the crashes wrote nothing, so unlike the other five deferrals there is nothing to recover. Worth recording for the retry: attempt 2's log shows the session shelling out to a NESTED `claude -p` to reach the claude-design MCP, still reporting `Running...` at 6m22s under a `timeout 90` that never fired. Herald is the deck station and therefore the one station whose stories pull on Claude Design, so that pattern is specific to this line — it burns budget invisibly and outlived its own kill switch.
- (constraint) Story 1.3 (fallback transport adapter) AMENDED 2026-07-31 with a hard constraint: the nested agent process is NEVER actually spawned during development or in any test. `AgentSdkTransport` must take its process-launch seam as an injected dependency so every test drives it with a stub. Added after BOTH prior dev attempts (run 20260730-192235-062b) died mid-thinking with no error text — OOM, session timeout and env fault each ruled out by direct check — and attempt 2's log showed a real `claude -p` still `Running…` at 6m22s under a `timeout 90` that never fired.
- (note) The root of it is that this story's SUBJECT is a nested Claude Code invocation and its IMPLEMENTER is a Claude Code session. Asking an agent session to spawn an agent session is the one thing the story requires and the one thing that reliably kills the session doing the asking. Its branch carries ZERO commits across two attempts — the only deferral of the 2026-07-30 fleet run with genuinely nothing to recover, confirmed by `git fsck --no-reflogs` over the same window (every dangling commit there belongs to a story that already merged).
- (decision) Injecting the launch seam is not a workaround, it is the AD-4-shaped design — the impure edge belongs behind a port — so the constraint makes the story both survivable and better factored. Protocol conformance, the tool allowlist, the determinism boundary and `serve_url` stripping are all assertions about what the adapter SENDS and RETURNS, none of which needs a live subprocess. Live verification against a real nested agent is deferred to an operator-run integration check, outside the loop. Narrowing a story's surface is what marshal:AD-27 permits a spec to do.
- (event) Dream consolidated 2026-08-02: docs/dreams/herald-moments-2-4-missing-surface.md folded into docs/dreams/pyforge-herald.md (freshly rewritten, single current-state narrative). That Dream is now archived/absorbed; its own downstream chain (spec-herald-moments-2-4 + PRD/Architecture/Epics under planning-artifacts/) stays untouched and live as Herald's execution reference for Moments 2-4 — not duplicated into this spec.
- (note) Ground-truth verified 2026-08-02: HER-1's CLI foundation (SPEC-design-code-bridge CAP-1..5, FR-01-26, 17 stories) is 4/17 done (1-1 package scaffold, 1-2 MCP-transport spike, 1-4 bridge-core skeleton, 1-5 registry module -- see this spec folder's own 2026-07-28..07-31 entries above); loop/pyforge-herald is paused mid-story on 1-3 (fallback transport adapter). herald deck seed/pull/status/watch are not wired into cli.py yet -- only the empty deck subcommand group exists. Moment 1's deck family CONTENT (presentations/, Modernist system, deck-export) is production-ready independent of this CLI's completion; the CLI mechanizes what is today a manual seed/pull workflow.
- (event) SPEC.md re-derived 2026-08-02 from the fresh docs/dreams/pyforge-herald.md. Capability IDs HER-1/HER-2/HER-3 preserved unchanged (no new capability -- Moments 2-4 progress/success/notice capabilities live in the separate, untouched spec-herald-moments-2-4). Why/Success-signal reworded to state the CLI foundation's real in-progress status instead of implying HER-1 is fully realized. Self-validate pass 1 (coherence): rules 1-6/8 hold, no new gaps. Pass 2 (preservation): all load-bearing claims from the fresh Dream already covered by existing HER-1..3 + constraints; nothing new to preserve beyond the status correction above.
- (decision) From spec-herald-pitch: Dream consolidates Herald's Four Moments framework (Pitch, Progress, Success, Operations) as Tier 0 Vision, Tier 1 reusable capabilities (Design-Code-Bridge, Deckcraft, Video-Scripts, Modernist-Identity), and Tier 2 Moment 1 Feature Orchestration across 9 pyforge stations.
- (decision) From spec-herald-pitch: Use Layered Tier Model (Option 4) to separate Vision (overarching), Capabilities (reusable), and Features (orchestrations).
- (capability) HER-4 (was CAP-1) -- Design-Code-Bridge Framework -- Automate round-trip between Claude Design and Code with etagged conflict detection. Seed Design projects, allow human visual iteration, pull etagged updates to Code, zero manual file transfers.
- (capability) HER-5 (was CAP-2) -- Deckcraft Framework -- Generate editable PowerPoint from Markdown sources using python-pptx + design tokens. Decks are programmatically generated yet manually editable. Deliverable format: .pptx files tracked in src/pptx/.
- (capability) HER-6 (was CAP-3) -- Video-Scripts Framework -- Extract narration scripts mechanically from Design speaker notes, feed to bmad-manticore. Four hard gates: Outline -> Cut Plan -> Graphics Beats -> Final Render. No fabricated product demos.
- (capability) HER-7 (was CAP-4) -- Modernist-Identity Framework -- One visual language across all PyForge surfaces (decks, dashboards, docs, exports, videos). Design system: Archivo typography, palette (light #f3f2f2, ink #201e1d, red #ec3013/#c22a10), grid, zero corner radius, flush-left labels.
- (capability) HER-8 (was CAP-5) -- Six-Act Deck Framework -- Canonical structure for all 21 pitch decks. Acts: Cover (hook), I (friction), II (insight), III (solution/mechanics), IV (real-world location), V (future/vision), VI (action/CTA), Appendix (personas). ~28 slides per deck, 90KB+ class, inline SVGs, full depth.
- (capability) HER-9 (was CAP-6) -- Multi-Format Export -- Single Design prototype -> 6 artifact formats per station: Design protos (.dc.html), Markdown (marp), PPTX (editable), Narration scripts (.md), Infographics (SVG), Video scripts (.mp4 via manticore). Tracked + regenerable separation.
- (capability) HER-10 (was CAP-7) -- Station-Specific Customization -- 9 pyforge-station content set (Marshal, Warden, Atlas, Mason, Steward, Scribe, Genesis, Doctor, Herald). Per-station: Thesis, pain point, solution pillars, ecosystem vision, personas. Framework stays constant; content changes.
- (constraint) From spec-herald-pitch (HER-4..10): Single Design prototype per station is the immutable source of truth (etagged, tracked in git as .dc.html). All code-side artifacts derive from it.
- (constraint) From spec-herald-pitch (HER-4..10): Aggressive optimization: Track 144 files (design protos 3-4 each, markdown 5-8, PPTX 2-4, narration 1-2, infographics 0-1). Gitignore intermediate build artifacts (fragments.json, dist/, assets/, .mp4) as regenerable.
- (constraint) From spec-herald-pitch (HER-4..10): No fabricated demos. All screen recordings in video output are real, never AI-generated UI. Manticore receives real footage + real narration only.
- (constraint) From spec-herald-pitch (HER-4..10): Etagged safety on all Design <-> Code transfers. Conflicts detected, never silent overwrites. Design cloud + pull model, not live sync.
- (constraint) From spec-herald-pitch (HER-4..10): Narrator identity + voice bible derived from published transcripts (WPM, speech patterns). Blacklist enforced by script linter.
- (non-goal) From spec-herald-pitch: New deck-authoring infrastructure. Design-code-bridge already proven on 7 decks.
- (non-goal) From spec-herald-pitch: New export backends. Deckcraft handles PPTX; deck-export CLI handles HTML/others.
- (non-goal) From spec-herald-pitch: New video pipeline. Manticore is upstream; this effort feeds narration scripts to it.
- (non-goal) From spec-herald-pitch: Real-time collaboration. Design cloud handles iteration; pull model is explicit and conflict-safe.
- (success) From spec-herald-pitch (HER-4..10): 9 Design projects seeded (one per pyforge station) using Modernist identity tokens.
- (success) From spec-herald-pitch (HER-4..10): 9 prototypes authored and refined per six-act framework in Design layer.
- (success) From spec-herald-pitch (HER-4..10): 9 x 6 artifact sets exported and committed: design protos (.dc.html), markdown (marp), PPTX exports, narration scripts (.md), SVG infographics, built outputs (gitignored).
- (success) From spec-herald-pitch (HER-4..10): All HTML decks render without error; dashboard-check passes for all 9.
- (success) From spec-herald-pitch (HER-4..10): All PPTX files are editable in PowerPoint; formatting/layout choices preserved.
- (success) From spec-herald-pitch (HER-4..10): All infographics render as inline SVG (zero raster images).
- (success) From spec-herald-pitch (HER-4..10): All narration scripts extracted, available for video-scripts + manticore integration.
- (success) From spec-herald-pitch (HER-4..10): Zero manual file transfers between Design and Code; design-code-bridge automation end-to-end.
- (success) From spec-herald-pitch (HER-4..10): Total tracked footprint: ~144 files (9 stations x ~16 files per station) vs 270 unoptimized.
- (assumption) From spec-herald-pitch: Herald CLI (seed/pull/watch/stale-mirror/export) 5 capabilities are available and proven.
- (assumption) From spec-herald-pitch: Design-code-bridge etagged round-trip protocol is stable and working.
- (assumption) From spec-herald-pitch: Modernist design system is tokenized and ready for PPTX + video production binding.
- (assumption) From spec-herald-pitch: bmad-manticore (v2.0.0) is packaged and available as a conda-forge dependency.
- (assumption) From spec-herald-pitch: 9 pyforge station domains have sufficient visual metaphors available (warden: lattices/gates, marshal: policy composition, atlas: pipeline stages).
- (question) From spec-herald-pitch: Should narration scripts and SVG infographics be tracked or regenerated? (DECIDED: tracked for video pipeline readiness + source auditability)
- (question) From spec-herald-pitch: Should PPTX exports be tracked or regenerated on build? (DECIDED: tracked for deliverable readiness + formatting preservation)
- (question) From spec-herald-pitch: Video .mp4 output gitignored or tracked? (DECIDED: gitignored, regenerable on demand, expensive)
- (question) From spec-herald-pitch: Fragments.json and dist/ output gitignored? (DECIDED: yes, regenerable <15s)
- (event) From spec-herald-pitch: Dream intake completed; memlog initialized; Tier 0/1/2 consolidation accepted; workflow stages 1-4 architected.
- (event) From spec-herald-pitch: Self-validate sweep: Coherence pass complete -- all 7 capabilities have intent+success; constraints bend design; 4+ non-goals stated; success signals concrete/testable; Spec Law rules 1-8 satisfied.
- (event) From spec-herald-pitch: Preservation pass complete -- all load-bearing claims from dream routed to kernel or companion.
- (event) From spec-herald-pitch: Spec kernel and companions rendered; all capability IDs stable (originally CAP-1..7, renumbered HER-4..10 on 2026-08-02 fold-in).
- (event) From spec-herald-pitch: 2026-08-01 spec finalized; ready for PRD/Architecture decomposition.
- (capability) HER-11 (was CAP-1): Progress Visibility Surface -- Herald Moment 2. Surface showing what changed, what it cost (compute, tokens, time), what it unblocked. Triggered weekly/monthly or on shipping events. Automation: extract from sprint-status ledger, bmad-loop journals, and PRD updates.
- (capability) HER-12 (was CAP-2): Success Proclamation Surface -- Herald Moment 3. Surface claiming project completion with evidence: tests passing, metrics green, user adoption data. Triggered on shipping milestone. Automation: linked to PR close + test results + dashboard metrics.
- (capability) HER-13 (was CAP-3): Operations Proclamation Surface -- Herald Moment 4. Surface for deprecations, security fixes, end-of-life notices. Automation: triggered on manual notice authoring + archive/redirect rule generation. No fabricated claims; all notices backed by evidence.
- (constraint) From spec-herald-moments-2-4 (HER-11..13): Integration: All three Moments must integrate with Herald CLI and Herald web surface. No separate platforms; unified UI surface.
- (constraint) From spec-herald-moments-2-4 (HER-11..13): Automation triggers: Moment 2 weekly/on-ship, Moment 3 on-PR-close, Moment 4 on-notice-author. Each must have explicit automation rule.
- (constraint) From spec-herald-moments-2-4 (HER-11..13): Evidence requirement: Moment 3 and 4 claims must be backed by retrievable evidence. No claims without proof links.
- (decision) From spec-herald-moments-2-4: Scope decision: This spec addresses surface design only (Moment 2, 3, 4 entry points + automation rules). Implementation in separate stories.
- (question) From spec-herald-moments-2-4: Orchestration: Implement Moments 2, 3, 4 as separate stories within Herald, or as coordinated sub-stories within one epic? Decision needed before epics.
- (decision) From spec-herald-moments-2-4: DECISION: Coordinated epic approach. Moments 2-4 implemented as 6-7 coordinated sub-stories within one epic. Rationale: shared CLI/web/automation infrastructure, testing simplification, Herald precedent (Moment 1 was unified, not incremental), priority on integration correctness over velocity-to-first-value.
- (decision) From spec-herald-moments-2-4: DECISION: Moment 2 automation trigger = on-ship event + weekly fallback Thursday 2300 UTC. Ensures progress visible after shipping + guaranteed weekly cadence even if no shipping events.
- (decision) From spec-herald-moments-2-4: DECISION: Moment 3 automation = auto-extract on PR-close + passing gates. Herald extracts test results from CI, metrics from dashboard, adoption signals from downstream PRs. Operator reviews + approves before publish. Rationale: reproducibility, deterministic claim generation, operator review gates quality.
- (decision) From spec-herald-moments-2-4: DECISION: Moment 4 archive indexing = simple date/category index (YYYY-MM folders + category tags). No full-text search backend. Rationale: <100 notices expected; date/category is dominant query; manual search acceptable at scale; full-text can be added later without data migration.
- (note) Non-goal amended 2026-08-02: the prior Non-goals entry 'Re-specifying Moments 2-4' (this Spec references spec-herald-moments-2-4 without duplicating its capability breakdown) is superseded. Per explicit user override of the same-day keep-chains-separate convention, Moments 2-4's capability breakdown is now folded in inline as HER-11..13 above; the separate spec-herald-moments-2-4 file is archived, not deleted.
- (note) No contradicting constraints or non-goals found between spec-pyforge-herald's original two (process/ownership-scoped) and the folded-in spec-herald-pitch (6) and spec-herald-moments-2-4 (4): they operate in disjoint domains (station governance vs. deck-family product constraints vs. proclamation-surface automation constraints), so all are kept as complementary, non-overlapping entries -- no reconciliation choice was needed beyond the one non-goal amendment above.
- (event) 2026-08-02 (later same day): station-level Spec consolidation. Per explicit user override, spec-herald-pitch (CAP-1..7, now HER-4..10) and spec-herald-moments-2-4 (CAP-1..3, now HER-11..13) are folded into this Spec as the single canonical spec-pyforge-herald. Companions six-act-framework.md, station-roster.md, artifact-tracking-matrix.md, bridge-protocol.md, workflow-stages.md (from spec-herald-pitch) and epic-structure.md (from spec-herald-moments-2-4) are relocated here and listed in this Spec's companions: frontmatter. Both source spec folders are archived, unmodified, at archive/_bmad-output/projects/pyforge-herald/planning-artifacts/specs/.
- (decision) DRIFT-PRESUMED RECONCILED, 2026-08-09, marshal S-13.6 (spec-surface-drift-reconciliation CAP-6 / FR-169). This Spec carried 834 of the fleet's 994 [drift-presumed] entries — by far the largest set, and the one most at risk of being waved through for exactly that reason. Judged against THIS Spec's capabilities, not assumed. Partition: 819 added / 15 changed / 0 removed. 751 of the 834 are presentations/** across 15 clusters (agentic-sdlc 100; pyforge-marshal 67; pyforge-warden 65; pyforge-atlas 63; pyforge-genesis 50; deckcraft, presenton-pixi-image, pyforge-{doctor,herald,mason,scribe,steward}, unity-data-stack, wasm-analytics-stack 45 each; README 1). presentations/** is a DECLARED entry in this Spec's own surface, and HER-9 (Multi-Format Export) contracts precisely this corpus: six artifact formats per station, ~144 tracked files, an explicit tracked/gitignored split, with the rationale in the artifact-tracking-matrix companion. The commits that landed them are HER-4 / HER-7 / HER-8 / HER-9 delivery (deck-family builds, the Modernist identity refresh across 8 decks, the persona-deck scaffolds). The remaining 83 are src/shared/packages/pyforge-herald/** moved by this station's own stories — 6 by Story 3.1, 3 by Story 6.4, and singles from Stories 6.1, 6.3, 9.1, 12.1 — every one of which is `done` in the ledger. CONCLUSION: the code caught up to a contract that was already correct (the same finding S-13.4 reached for steward's 23-file cluster), so the contract does not move and the baseline is scoped-stamped with this reasoning recorded. Nothing here was moved by a story that is not done.
- (note) Two attribution traps found while judging this set, recorded so the next reader does not re-derive them. (1) A commit whose entire subject is `Atkas` — a typo for Atlas — owns 42 of these entries; it is the pyforge-atlas deck scaffold (47 files, 7,215 insertions, all under presentations/pyforge-atlas/), squarely inside HER-9. An unreadable subject is not an unaccounted change, but it looks like one until opened. (2) 68 entries appear to name a story `2026-07` under a naive id-regex, which is actually matching the DATE in the subject "marshal: apply 2026-07-31 rulings through the full chain + deck family rebuild + readiness gate" — deck-family work, also HER-9. Measure with the real key, not a regex that will match a date.
- (note) Audit Phase 2 landing (2026-08-10): station chain reconciled — see planning-artifacts/implementation-readiness-report-2026-08-10.md for the full finding set and per-path record; mechanical fixes (rollups/Status-lines/spec-status/promotions/README prose) landed via the audit's Phase 2 PR
- (decision) DRIFT-PRESUMED RECONCILED, 2026-08-11. 12 files against this Spec's
  surface flagged [drift-presumed]. 11 are Story 13.1's own state-layer/
  concurrency-lock implementation, landed in the single bmad-loop merge commit
  cfc951aac9 via PR #422 ("land herald 13-1 — the concurrency prerequisite,
  converged on its last review", merged 2026-08-11T04:52:59Z) and never
  reconciled at that landing:
  `src/shared/packages/pyforge-herald/src/pyforge/herald/claims.py` (changed),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/locking.py` (added),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/notices.py` (changed),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/progress.py` (changed),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/state.py` (changed),
  `src/shared/packages/pyforge-herald/tests/test_bridge.py` (changed -- registers
  the new `locking` module into the deterministic bridge-core module list, the
  AD-2 boundary test), `src/shared/packages/pyforge-herald/tests/test_claims.py`
  (changed), `src/shared/packages/pyforge-herald/tests/test_locking.py` (added),
  `src/shared/packages/pyforge-herald/tests/test_notices.py` (changed),
  `src/shared/packages/pyforge-herald/tests/test_progress.py` (changed),
  `src/shared/packages/pyforge-herald/tests/test_state.py` (changed). The 12th,
  `src/shared/packages/pyforge-herald/README.md` (changed), is unrelated to Story
  13.1 -- it is the Audit Phase 2 Status-line correction ("build skeleton" -> "all
  47 stories shipped (12 epics)"), landed via PR #404 and already summarized by
  this memlog's 2026-08-10 "Audit Phase 2 landing" note directly above, but never
  named there by path. Verified per-path with `git log --follow -1` on all twelve:
  the eleven resolve to the single 13.1 merge commit, README.md resolves to
  commit 28551a6b65 (PR #404); nothing else is mixed into either group. Baseline
  scoped-stamped in this same change
  (`--write-baseline --spec pyforge-herald/spec-pyforge-herald`).
- (decision) SURFACE DRIFT RECONCILED, 2026-08-13 (landed by hand after a cross-station deferral —
  see `pyforge-marshal/spec-pyforge-marshal`'s own memlog for the full account). Nine governed
  paths changed by marshal Story 14.2 ("consolidate atomic-write into pyforge-core", commit
  `1d60da0193`), a MARSHAL-owned story that retired 20 hand-written tmp-in-dir + `os.replace`
  copies across six stations including this one:
  `src/shared/packages/pyforge-herald/pixi.toml` and
  `src/shared/packages/pyforge-herald/pyproject.toml` (add `pyforge-core` as a real
  run-dependency), `src/shared/packages/pyforge-herald/src/pyforge/herald/claims.py`,
  `deck_pipeline.py`, `notices.py`, `progress.py`, `registry.py` and `state.py` (each own
  atomic-write-shaped write re-pointed at `pyforge.core.atomic_write`), and
  `src/shared/packages/pyforge-herald/tests/test_registry.py` (updated to match). This Spec claims
  no capability here — CAP-2 ("atomic write has one implementation") is
  `pyforge-marshal/spec-pyforge-core`'s own contract; this entry reconciles surface/drift
  bookkeeping only, for files landing under this Spec's own package-path glob, per the same
  convention the 2026-08-11 Story 13.1 entry above already established. Six of the nine paths
  (everything but pixi.toml/pyproject.toml/test_registry.py) are ALSO governed by the narrower
  sibling `spec-herald-moments-2-4-live-backend`; both specs are reconciled in this same pass,
  each baseline-stamped independently via its own `--spec` scope.
- (decision) SURFACE DRIFT RECONCILED, 2026-08-13, Story 13.3 ("DB-backed storage behind the
  existing seam, with migrations", LB-1). Eighteen governed paths changed in the single commit
  `20b88dd689`, which swaps progress.py/claims.py/notices.py's JSON-file backing for a shared
  stdlib-sqlite3 database behind the existing pure-function seam:
  `src/shared/packages/pyforge-herald/docs/automation-troubleshooting.md` (changed),
  `src/shared/packages/pyforge-herald/docs/cli-runbooks.md` (changed),
  `src/shared/packages/pyforge-herald/docs/operator-guide.md` (changed),
  `src/shared/packages/pyforge-herald/docs/web-ux-guide.md` (changed),
  `src/shared/packages/pyforge-herald/scripts/export_progress_snapshot.py` (added),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/claims.py` (changed),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/db.py` (added, the new
  connection/migration/transaction module),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/notices.py` (changed),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/progress.py` (changed),
  `src/shared/packages/pyforge-herald/tests/test_bridge.py` (changed — registers `db` into the
  deterministic bridge-core module list, the AD-2 boundary test),
  `src/shared/packages/pyforge-herald/tests/test_claims.py` (changed),
  `src/shared/packages/pyforge-herald/tests/test_db.py` (added),
  `src/shared/packages/pyforge-herald/tests/test_export_progress_snapshot.py` (added),
  `src/shared/packages/pyforge-herald/tests/test_integration_epic11.py` (changed),
  `src/shared/packages/pyforge-herald/tests/test_notices.py` (changed),
  `src/shared/packages/pyforge-herald/tests/test_progress.py` (changed),
  `src/shared/packages/pyforge-herald/web/package.json` (changed — `sync-progress` repointed at
  the new exporter script), and
  `src/shared/packages/pyforge-herald/web/scripts/sync-progress.mjs` (removed, superseded by
  `scripts/export_progress_snapshot.py`). This Spec's HER-11 (Progress Visibility Surface) is the
  capability this storage swap sits under; LB-1 itself is `spec-herald-moments-2-4-live-backend`'s
  own contract, not this Spec's — this entry reconciles surface/drift bookkeeping only, per the
  same convention the 2026-08-11/2026-08-13 entries above already established. Four of the
  eighteen paths (`claims.py`, `db.py`, `notices.py`, `progress.py`) are ALSO governed by the
  narrower sibling `spec-herald-moments-2-4-live-backend`; both specs are reconciled in this same
  pass, each baseline-stamped independently via its own `--spec` scope.
- (decision) SURFACE DRIFT RECONCILED (review pass), 2026-08-13, Story 13.3 — a second review
  pass over the already-`done` spec touched four governed paths beyond the eighteen named in the
  entry above, all of them stale-reference or hardening changes rather than new capability:
  `src/shared/packages/pyforge-herald/scripts/export_notices_snapshot.py` (changed — module
  docstring still said it reads `.herald/notices-index.json`),
  `src/shared/packages/pyforge-herald/scripts/export_web_snapshot.py` (changed — module
  docstring, `export_success_snapshot`'s docstring, and its user-facing `--repo-root` `--help`
  string still said `.herald/claims.json`),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/cli.py` (changed — same stale
  `.herald/claims.json` in `--help` plus three docstrings), and
  `src/shared/packages/pyforge-herald/web/README.md` (changed — still described the
  `sync-progress` hook as copying `.herald/progress.json` and pointed readers at
  `scripts/sync-progress.mjs`, the file Story 13.3 deleted). All four are the incomplete
  remainder of the same rename sweep the four `docs/*.md` files in the entry above already
  received: the JSON stores they name no longer exist. `db.py`/`progress.py`/`notices.py` and
  the three test modules also changed in this pass (read-path `errors.HeraldError` wrapping,
  `STRICT` tables, side-effect-free reads, SQLITE_BUSY-only WAL retry, plus eleven new tests)
  but are already named above. `cli.py` is ALSO governed by the narrower sibling
  `spec-herald-moments-2-4-live-backend`; both specs are reconciled in this same pass, each
  baseline-stamped independently via its own `--spec` scope.
- (decision) SURFACE DRIFT RECONCILED (third follow-up review pass), 2026-08-13, Story 13.3 --
  five governed paths changed beyond those named in the entries above, plus already-named ones.
  New this pass:
  `src/shared/packages/pyforge-herald/src/pyforge/herald/errors.py` (changed -- `ClaimNotFoundError`'s
  docstring still said a claim id "does not exist in `claims.json`"),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/locking.py` (changed -- its module
  docstring still claimed scope over `state.py`/`progress.py`/`claims.py`/`notices.py`, which
  Story 13.3 itself made false: three of those four moved onto `db.transaction`, leaving
  `state.py` as its one remaining caller), and the three web panels whose header comments named
  the deleted JSON stores as their snapshot source --
  `src/shared/packages/pyforge-herald/web/src/panels/ProgressPanel.jsx`,
  `src/shared/packages/pyforge-herald/web/src/panels/SuccessPanel.jsx`, and
  `src/shared/packages/pyforge-herald/web/src/panels/OperationsPanel.jsx` (all changed). These
  are the last of the rename sweep the four `docs/*.md` files and then `cli.py`/the two exporter
  scripts/`web/README.md` already received. `tests/test_bridge.py` (already named above) carried
  the same stale `locking.py` sentence and was corrected with it. Text only; no behavior.
  The pass's behavior changes are confined to already-named paths (`db.py`, `claims.py`,
  `notices.py`, `tests/test_db.py`, `tests/test_notices.py`,
  `docs/automation-troubleshooting.md`): legacy-import idempotency on a re-run over populated
  tables, AD-6 error translation for the read seam's non-`sqlite3` binding failures and for
  `claims`' in-transaction reads, deferring the stale-markdown unlink past the commit, and seven
  new regression tests. One further stale reference was found and deliberately NOT fixed here:
  `pixi.toml`'s `pyforge-herald-web-snapshot` task description still names `.herald/claims.json`,
  but `pixi.toml` is governed by `pyforge-marshal/spec-pyforge-core`, so correcting it belongs to
  that Spec's own surface rather than to a herald story. `errors.py`/`locking.py` are ALSO
  governed by the narrower sibling `spec-herald-moments-2-4-live-backend`; both specs are
  reconciled in this same pass, each baseline-stamped independently via its own `--spec` scope.
- (decision) SURFACE DRIFT RECONCILED, 2026-08-13 (dev-verify repair pass on marshal Story 14.3,
  "one lattice, one envelope, one exception root", commit `1bd8c8a2de`, not a herald-owned story):
  `src/shared/packages/pyforge-herald/src/pyforge/herald/errors.py` changed again —
  `HeraldError(Exception)` re-parented to `HeraldError(PyforgeError)` (CAP-5), and
  `exit_code_for`'s hand-rolled most-specific-first isinstance loop now delegates to the new
  `pyforge.core.verdict.dispatch_exit_code(exc, table, default)` generic helper (CAP-3), same
  algorithm, extracted once; the `_EXIT_BY_ERROR` table itself (station-specific data) stays here,
  unchanged. This Spec claims no capability for either change — CAP-3 and CAP-5 are
  `pyforge-marshal/spec-pyforge-core`'s own contract; this entry reconciles surface/drift
  bookkeeping only, per the same convention the entries above already established. Also reconciled
  in the narrower sibling `spec-herald-moments-2-4-live-backend`, each baseline-stamped
  independently via its own `--spec` scope. `pyforge-herald-test` green post-change.
- (decision) SURFACE DRIFT RECONCILED, 2026-08-13, Story 13.5 ("the scheduler enforces what was
  displayed", LB-1's `herald scheduler run` entry point). Ten governed paths changed in the single
  commit `18dca9e3ad`, which composes the two operations Stories 8.2/9.5 already scaled down to
  on-demand CLI verbs -- evidence revalidation (`claims.revalidate_all`) and the progress snapshot
  export (`progress.write_snapshot`) -- into one cron-invocable command, so a single crontab line
  keeps both AD-15's 7-day evidence-staleness window and the web dashboard's Progress tab current:
  `src/shared/packages/pyforge-herald/src/pyforge/herald/scheduler.py` (added -- the schedulable
  unit itself; no new scheduler dependency, per `evidence.py`'s own already-settled doctrine that
  "whatever already triggers periodic work in this repo" invokes it directly, never a GH Actions
  workflow since a hosted runner never has the gitignored `.herald/herald.db` to validate),
  `src/shared/packages/pyforge-herald/src/pyforge/herald/cli.py` (changed -- registers
  `herald scheduler run`), `src/shared/packages/pyforge-herald/src/pyforge/herald/progress.py`
  (changed -- exposes the snapshot-export entry point the scheduler composes),
  `src/shared/packages/pyforge-herald/docs/automation-troubleshooting.md`,
  `src/shared/packages/pyforge-herald/docs/cli-runbooks.md` (the documented crontab trigger),
  `src/shared/packages/pyforge-herald/docs/operator-guide.md` (all three changed -- operator-facing
  docs for the new verb), `src/shared/packages/pyforge-herald/scripts/export_progress_snapshot.py`
  (changed), `src/shared/packages/pyforge-herald/tests/test_bridge.py` (changed -- registers
  `scheduler` into the deterministic bridge-core module list, the AD-2 boundary test),
  `src/shared/packages/pyforge-herald/tests/test_cli_epic13.py` (added),
  `src/shared/packages/pyforge-herald/tests/test_scheduler.py` (added). Both jobs run
  unconditionally on every invocation -- neither filters by the 7-day window in code; the cron
  cadence is the whole enforcement mechanism, matching `evidence.schedule_async_validation`'s own
  established framing. Three of the ten paths (`cli.py`, `progress.py`, `scheduler.py`) are ALSO
  governed by the narrower sibling `spec-herald-moments-2-4-live-backend`; both specs are
  reconciled in this same pass, each baseline-stamped independently via its own `--spec` scope.
  Reconciled at landing time 2026-08-13 (not by the loop's own dev-verify pass, which never
  reconciled this Spec for the story -- same S-13.7-shaped gap as every prior occurrence).
  `pyforge-herald-test` 827 -> 851 passed, 2 skipped (net +24, the story's own coverage).
- (change) S-13.4 the webhook endpoint CI calls (LB-2) merged via `bmad-loop/20260813-145413-7eb9/13-4-the-webhook-endpoint-ci-calls`, reconciled by name at landing time 2026-08-13 (the loop's own dev-verify pass never reconciled this Spec for the story). Adds `webhook.py`: HMAC-SHA256-verified (AD-9, proof not ingress) sync handlers `handle_on_ship`/`handle_on_pr_close` plus a thin `async def app` ASGI3 boundary (`create_app`) that dispatches each matched handler via `asyncio.to_thread` so `_retry_with_backoff`'s blocking `time.sleep` cannot stall the whole worker. `on-ship` fires from a post-push-to-main CI step (this repo's own convention, not GitHub's native `pull_request closed+merged` event); `on-pr-close` additionally gates on the payload's own `merged`/`gates_passed` booleans, never trusting the HTTP event alone. Same-day `on-ship` deliveries REPLACE rather than merge — callers must send cumulative figures. `cli.py` changed only incidentally (shared helper reuse); wiring the real `.github/workflows/*.yml` step, mounting the ASGI app into a live host, and composing Steward's identity/trust boundary around it are explicitly deferred to Story 13.6. Docs (`automation-troubleshooting.md`, `cli-runbooks.md`, `operator-guide.md`) updated to describe the new endpoints. Files: src/shared/packages/pyforge-herald/docs/automation-troubleshooting.md, src/shared/packages/pyforge-herald/docs/cli-runbooks.md, src/shared/packages/pyforge-herald/docs/operator-guide.md, src/shared/packages/pyforge-herald/src/pyforge/herald/cli.py, src/shared/packages/pyforge-herald/src/pyforge/herald/webhook.py (new), src/shared/packages/pyforge-herald/tests/test_bridge.py, src/shared/packages/pyforge-herald/tests/test_webhook.py (new). Three of these paths (`cli.py`, `webhook.py`, plus the LB-2 surface itself) are ALSO governed by the narrower sibling `spec-herald-moments-2-4-live-backend`; both specs are reconciled in this same pass, each baseline-stamped independently via its own `--spec` scope. `pyforge-herald-test` 851 -> 984 passed, 2 skipped.
- (event) Spec-surface reconciliation for the S-13.4 landing complete as of this entry; baseline re-stamped `--spec pyforge-herald/spec-pyforge-herald` immediately after.
- (change) S-13.6 a ship records itself, end to end (LB-2 + LB-3 composed) merged via `bmad-loop/20260813-145413-7eb9/13-6-a-ship-records-itself-end-to-end`, reconciled by name at landing time 2026-08-13 (the loop's own dev-verify pass never reconciled either owning Spec for this story). Adds `webhook_host.py` (new): the ASGI host wiring `webhook.py` (13.4) and `scheduler.py` (13.5) never had -- a `daphne`-pointable `application` object built lazily via a module-level `__getattr__` (PEP 562, so a plain import never touches the environment or constructs anything, keeping `test_bridge.py`'s determinism sweep green with no secrets configured), plus a bounded 120s `asyncio.wait_for` request timeout and a dedicated 4-worker `ThreadPoolExecutor` wrapping the whole ASGI callable (narrows the previously-deferred DW-FU-13-4-3) rather than changing `webhook.py`'s own AD-8-governed zero-framework-dependency shape. `webhook.py` itself changes only incidentally (helper reuse). `pyproject.toml` gains the `webhook-host` optional extra (`daphne`), mirrored byte-identical into root `pixi.toml`'s `[feature.pyforge-herald.dependencies]` (reconciled independently in `pyforge-marshal/spec-pyforge-core`'s own memlog, since that surface also governs `pixi.toml`). `.github/workflows/herald-live-demo.yml` (new, not under this Spec's package-path glob) actually starts the daphne process in CI. `test_webhook_live_smoke.py` (new) is the one genuinely-live-socket proof -- a real daphne subprocess answering a real signed HTTP POST over loopback -- opt-in only (`HERALD_LIVE_WEBHOOK=1`), never in the default gate. Docs (`automation-troubleshooting.md`, `cli-runbooks.md`, `operator-guide.md`) updated for the now-real live deployment. Files: .github/workflows/herald-live-demo.yml, src/shared/packages/pyforge-herald/docs/automation-troubleshooting.md, src/shared/packages/pyforge-herald/docs/cli-runbooks.md, src/shared/packages/pyforge-herald/docs/operator-guide.md, src/shared/packages/pyforge-herald/pyproject.toml, src/shared/packages/pyforge-herald/src/pyforge/herald/webhook.py, src/shared/packages/pyforge-herald/src/pyforge/herald/webhook_host.py (new), src/shared/packages/pyforge-herald/tests/conftest.py, src/shared/packages/pyforge-herald/tests/test_bridge.py, src/shared/packages/pyforge-herald/tests/test_webhook.py, src/shared/packages/pyforge-herald/tests/test_webhook_host.py (new), src/shared/packages/pyforge-herald/tests/test_webhook_live_smoke.py (new). Six of these paths (`webhook.py`, `webhook_host.py`, plus the LB-2/LB-3 surface itself) are ALSO governed by the narrower sibling `spec-herald-moments-2-4-live-backend`; both specs are reconciled in this same pass, each baseline-stamped independently via its own `--spec` scope. `pyforge-herald-test` 984 -> 1036 passed, 4 skipped.
- (event) Spec-surface reconciliation for the S-13.6 landing complete as of this entry; baseline re-stamped `--spec pyforge-herald/spec-pyforge-herald` immediately after.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Surface reconcile 2026-08-31: 29 drift-presumed paths since baseline at `41ea84d1a8af` (S-13.6 landing). Sixteen commits, Epic 14-15 deck pipeline + station SKF/persona + unifying-strategy deck (all done in ledger):
  - `c659043efa3` — marshal PR #179: 2026-07-31 rulings + deck family rebuild.
  - Epic 14 (Stories 14.1-14.3): deck visual-QA gate, headless render gate, image-slot scan — deck_qa.py, tests, docs.
  - Epic 15 (Stories 15.1-15.2): template-parse-then-fill, dense-content Pillow autofit, deck-export plugins — exporters.py, pptx_pipeline.py, cli.py, pyproject.toml, tests.
  - `dbf53e2d75b` — herald station SKF skill + grammar-only persona.
  - `a1d0609c4f4` — one-slug deck status on /stations/herald/.
  - `336f4fd0ef0` — pyforge-unifying-strategy deck pilot (6-artifact family).
  - `a7721dd610f` — SKF-marker governance test fix + spec-surface drift hygiene.
  - `e07834580d` — steward Story 30.2: Guildhall generator deletion doc cleanup.
  No CAP text change. Scoped-stamped: `--write-baseline --spec pyforge-herald/spec-pyforge-herald`.
- (change by claude) Surface touch 2026-09-05 (PR #1045): tests/meta/test_skf_skill_and_persona.py's whole-branch diff-guard test bodies refactored to call the shared mechanics instead of duplicating git subprocess calls (retro-pyforge-steward-2026-09-04.md action item 11: the fleet's whole-branch diff-guard test mechanics (git subprocess calls computing files/commits changed since origin/main) were consolidated into a shared `pyforge.testing_kit.branch_diff_guard` module (PR #1045).); each guard's own enforced policy is unchanged. Scoped-stamped: `--write-baseline --spec pyforge-herald/spec-pyforge-herald`.
- (note) Cross-station surface touch (marshal Story 30.2 review pass, 2026-09-06): tests/meta/test_skf_skill_and_persona.py::test_context_files_not_hand_edited changed -- the prior unconditional `changed_paths_since(...)` "must not change" assertion on CLAUDE.md/AGENTS.md is replaced with a well-formedness check on the bmad:context markers (balanced, at most one block, AGENTS.md only -- CLAUDE.md carries no such marker), matching the SKF managed-section check above it. Root AGENTS.md legitimately gained a bmad:context block as part of Story 30.2's project-context-surface migration (BMAD 6.12 persistent_facts=[] surface migration, D1); the old check would have blocked that sanctioned edit. No herald skill/persona code changed. Scoped-stamped: `--write-baseline --spec pyforge-herald/spec-pyforge-herald`.
- (change by claude) CI/hygiene sweep 2026-09-07 follow-up: test_state.py's test_read_of_absurdly_nested_json_raises_herald_error was pinned to one specific HeraldError message ("could not be read", implying json.load hit RecursionError). PR #1081's own CI run showed the SAME 100k-deep nested-array input produces a DIFFERENT but equally-safe HeraldError on GitHub Actions' runner ("does not hold a JSON object at its top level" -- a clean parse followed by the top-level-type check failing, not a RecursionError). Broadened the regex to accept either message; both prove the real invariant (no raw exception leaks), only the specific failure path differs by interpreter/platform. No production code changed. RECONCILES: src/shared/packages/pyforge-herald/tests/test_state.py.
- (note) Cross-station reconcile from pyforge-marshal Story 32.2 (spec-fleet-consistency-standard CAP-5): this spec's pyproject.toml raised requires-python ">=3.12" -> ">=3.14". pixi.toml pins python ">=3.14.7,3.14.*" and every env-scoped pin is 3.14.*, so the 3.12 floor was a compatibility claim no environment in this repo has ever installed or exercised. No source change.
- (note) Story 32.5 (spec-fleet-consistency-standard CAP-2): the fleet converged on one test-suite vocabulary -- tests/unit/ + tests/integration/ + tests/meta/ (BMAD 6.12's Python default plus the one addition with 8/8 adoption), with fixtures/ and _support/ never collected. Eight suite names collapsed to three; the coverage gate's suite map now needs no per-station special case. This station: 43 of 47 test files sat loose at the tests root -- steward's concept with no directory at all -- and moved into tests/unit/. Five used Path(__file__).resolve().parents[1] to reach the package root and became parents[2]. conftest.py stays at the tests root. 1254 tests pass, 4 skipped.
- (question by claude) 2026-09-09 THE GUILDHALL NON-GOAL CITES A SUPERSEDED SPEC AND A RETIRED CONSOLE — REFERENT PENDING A CHARTER AMENDMENT (fleet-readiness-decision-batch-2026-09-09 § 2.3 C12; guild report F-9/E-4, herald report F-13/E-5). SPEC.md:137 reads "Owning the console. The Guildhall is Marshal's ([[factory-console]]); Herald supplies its conviction and its look, not its machinery." `factory-console`'s Spec is `superseded`, and the GitHub-Pages GuildHall console is retired-and-guarded (`retired-console-check`, `scripts/detectors.py:216`). The three live console surfaces are Atlas's Vizro/BSL fleet board, the Wagtail Lane-1 CMS and eight `django-*` station portals — none of them Marshal's. The non-goal's *substance* still holds (the hall is not Herald's) but its referent has fragmented. Guildhall is Lexicon §7, so the ruling is constitutional, not a station decision: it is raised as a Charter open question this same day (`docs/governance/spec-pyforge-charter/.memlog.md`, 2026-09-09) and will be decided with the Intelligence Hub's Track/Frame answers (batch C4). PENDING — do not re-word this non-goal here until the amendment lands; when it does, replace the `[[factory-console]]` referent in the same pass.
- (note by claude) 2026-09-09 BODY § SUCCESS SIGNAL IS STALE IN EVERY CLAUSE — RE-GROUND ON RE-DERIVE (batch § 2.2 herald/scribe body re-grounding; § 2.4 D1). SPEC.md:150-155 says "Not yet true end to end (2026-08-02): … the CLI that mechanizes seed/pull (HER-1 / HER-4) is mid-build; the notables (HER-2 / HER-11..13) have a full planning chain but no code." Live: Epics 1-5 are `done` and the seed/pull/status/watch CLI shipped (`cli.py:226-318`); Epics 8-10 shipped Moments 2-4 as 47 stories merged 2026-08-08; `cli.py` is 1764 lines wiring 26 subparsers. A `shipped` Spec whose prose describes a mid-build station is the worst class of stale — trusting the prose and trusting the status yield opposite conclusions. What is *still* not true end to end is narrower and should be stated honestly instead: the live-backend triggers have never run green (`spec-herald-moments-2-4-live-backend` memlog, 2026-09-09) and the deck-QA gate has no caller (`spec-deck-visual-qa` memlog, same date). The `claude-design` open drift is unchanged and stays.
- (note by claude) 2026-09-09 STATION-LEVEL EFFECT RESIDUE, VESSELLED (batch § 2.3 C6). Three of herald's five Dreams fail the realization gate — Epic 13's live backend has never run green, Epic 14's deck-QA gate is called by nothing, Epic 15's pptx pipeline has never rendered a real station deck. Herald Epic 19 ("Herald in effect") is minted this date as the vessel with one story per gap plus the route-contract fix; steward Epic 49 carries the index row. Also recorded for the record: `sprint-status-ledger.yaml:99` reads `epic-18: backlog` while 18-1/18-2/18-3 are each `done` — a roll-up defect, not outstanding work, folded into steward 48.1 (batch stA-D8); and the ledger header's `# stories: 100` mislabels 64 story keys + 36 epic/retro keys (fleet-wide generator defect).
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. All 13 HER ids, 6 companions, covers-dreams and surface preserved; status stays shipped; the Guildhall-referent question surfaced into frontmatter open_questions as PENDING and the Non-goal deliberately left un-reworded until the Charter amendment lands; body re-grounded where it contradicted status:shipped (Why/current state, HER-1 and HER-2 status clauses, Success signal); Epic 19 effect residue plus the epic-18 rollup and '# stories: 100' header defects recorded.
- (decision by claude) 2026-09-09 (chain-currency runbook § overtaken, after the fleet-readiness re-derive): the Guildhall-referent question is the CHARTER's open question (docs/governance/spec-pyforge-charter/SPEC.md open_questions, batch C12) and has ONE owner there; herald's SPEC does not carry it as its own open question. Render it instead as a dated DEPENDENCY in Assumptions/Residual: 'Non-goal :137 ("the Guildhall is Marshal's ([[factory-console]])") is PENDING the Charter amendment and re-words when it lands; until then the citation is known-stale, not a herald decision'. Frontmatter open_questions returns to [] so the station chain's coherence checkpoint reads current (runbook: resolve or empty; a duplicated question in two Specs is the fork the outcome/mechanism rule forbids).
- (event) (self-validate pass 1 -- coherence) 2026-09-09 second re-derive: PASS. open_questions returns to [] with a dated comment naming the Charter as the question's ONE owner; the question is re-rendered as a dated dependency in a new § Residual dependencies. The Non-goal at :137 is DELIBERATELY NOT re-worded -- only its inline pointer moves from 'the open question in frontmatter' to the new section. 13 HER ids, 6 companions, covers-dreams and both surface globs preserved; status stays shipped.
- (event) (self-validate pass 2 -- preservation) 2026-09-09 second re-derive: PASS. The closing 2026-09-09 entry landed in full -- the known-stale-not-a-herald-decision framing, the three live console surfaces, retired-console-check / scripts/detectors.py:216, Lexicon §7 as constitutional, the Charter memlog pointer, the batch C4 Track/Frame decision link and the one-owner-one-question rule. Only the frontmatter question line was removed; nothing else in the Spec changed.
- (event) 2026-09-13 presentations/README.md gained a pointer line to spec-deck-family-currency/infographic-standard.md (herald Story 20.1); the file is now also governed by that Spec's surface — recorded here because the detector checks each governing spec independently (spec-surface-overlap-tolerance still dreamt)
- (event) 2026-09-13 herald Story 20.2 added the per-deck fact ledgers presentations/pyforge-atlas/facts.yaml presentations/pyforge-doctor/facts.yaml presentations/pyforge-genesis/facts.yaml presentations/pyforge-herald/facts.yaml presentations/pyforge-marshal/facts.yaml presentations/pyforge-mason/facts.yaml presentations/pyforge-scribe/facts.yaml presentations/pyforge-steward/facts.yaml presentations/pyforge-unifying-strategy/facts.yaml presentations/pyforge-warden/facts.yaml under presentations/** (also governed by spec-deck-family-currency, whose memlog carries the change); recorded here because the detector checks each governing spec independently
- (decision) 2026-09-13 surface-drift-exclude gains the four deck-family surfaces spec-deck-family-currency owns (presentations/pyforge-*/facts.yaml, the ten posters, the deck READMEs, presentations/README.md): that Spec's memlog moves on every poster story while this kernel's does not, and facts.yaml headers carry the HEAD sha so they change on every re-derive after a commit — double-claiming them would red every Wave A PR with drift-presumed noise (same reasoning as the 2026-09-12 station_api.py entry); coverage unchanged
- (note) 2026-09-13 surface-drift-exclude entries rewritten as literal paths (31: ten facts.yaml, ten posters, ten deck READMEs, presentations/README.md) — the checker tests membership literally (sources/chain.py:1613), so the four glob entries added earlier today never matched and Wave A poster PRs still drifted this kernel
- (event) 2026-09-14 agentic-sdlc pulled from Design project f58c0f17 (pull only — nothing authored in-repo; DW-VOCAB-2026-09-14-3's deck half): presentations/agentic-sdlc/project/Agentic SDLC.dc.html (206,638 B, the 2026-09-09 v6.12 refresh), presentations/agentic-sdlc/project/Lexicon Poster.dc.html (12,213 B), presentations/agentic-sdlc/project/Lexicon to PyForge.dc.html (14,892 B) — each byte-exact against list_files via render_preview → curl → harness-block strip; presentations/agentic-sdlc/project/github.md mirrored verbatim (1,033 B); presentations/agentic-sdlc/README.md gained a 2026-09-14 ledger section (51 slides, what the refresh carried, what still needs a Design-side edit). Derived by `npm run extract` + `npm run build`: presentations/agentic-sdlc/src/slides/manifest.json and the whole presentations/agentic-sdlc/src/slides/fragments/ set (51 files: 06-what-is-ai-sdlc.html → 06-what-is-aidd.html; 39-workflow-matrix.html new, so 39..50 renumbered to 40..51). Spec-surface baseline NOT re-stamped for this key on purpose: the key also carries the recorded-but-unstamped deck-family drift-exclude (2026-09-13) and the Story 21.11 unifying-strategy pull, so a scoped stamp from an unrelated branch would accept those as this pass's; herald re-stamps once, after naming those paths here.
- (event) 2026-09-14 the regenerated fragment set, named literally for the checker (sources/chain.py tests membership by substring): presentations/agentic-sdlc/src/slides/fragments/01-title.html presentations/agentic-sdlc/src/slides/fragments/02-contents.html presentations/agentic-sdlc/src/slides/fragments/03-act-i.html presentations/agentic-sdlc/src/slides/fragments/04-the-shift.html presentations/agentic-sdlc/src/slides/fragments/05-the-productivity-paradox.html presentations/agentic-sdlc/src/slides/fragments/06-what-is-aidd.html presentations/agentic-sdlc/src/slides/fragments/07-maturity-spectrum.html presentations/agentic-sdlc/src/slides/fragments/08-core-idea.html presentations/agentic-sdlc/src/slides/fragments/09-act-ii.html presentations/agentic-sdlc/src/slides/fragments/10-what-is-sdd.html presentations/agentic-sdlc/src/slides/fragments/11-spec-rigor-on-a-spectrum.html presentations/agentic-sdlc/src/slides/fragments/12-spec-driven.html presentations/agentic-sdlc/src/slides/fragments/13-mental-model.html presentations/agentic-sdlc/src/slides/fragments/14-act-iii.html presentations/agentic-sdlc/src/slides/fragments/15-sdd-landscape.html presentations/agentic-sdlc/src/slides/fragments/16-method-vs-machinery.html presentations/agentic-sdlc/src/slides/fragments/17-why-bmad.html presentations/agentic-sdlc/src/slides/fragments/18-cover-framework.html presentations/agentic-sdlc/src/slides/fragments/19-the-crew.html presentations/agentic-sdlc/src/slides/fragments/20-why-named-agents.html presentations/agentic-sdlc/src/slides/fragments/21-party-mode.html presentations/agentic-sdlc/src/slides/fragments/22-execution-matrix.html presentations/agentic-sdlc/src/slides/fragments/23-skill-roster.html presentations/agentic-sdlc/src/slides/fragments/24-repo-artifacts.html presentations/agentic-sdlc/src/slides/fragments/25-cover-phases.html presentations/agentic-sdlc/src/slides/fragments/26-phase-map.html presentations/agentic-sdlc/src/slides/fragments/27-phase-1-analysis.html presentations/agentic-sdlc/src/slides/fragments/28-choosing-a-tool.html presentations/agentic-sdlc/src/slides/fragments/29-phase-2-planning.html presentations/agentic-sdlc/src/slides/fragments/30-phase-3-solutioning.html presentations/agentic-sdlc/src/slides/fragments/31-why-solutioning.html presentations/agentic-sdlc/src/slides/fragments/32-phase-4-implementation.html presentations/agentic-sdlc/src/slides/fragments/33-the-dev-loop.html presentations/agentic-sdlc/src/slides/fragments/34-cover-testing.html presentations/agentic-sdlc/src/slides/fragments/35-tea-testing.html presentations/agentic-sdlc/src/slides/fragments/36-cover-ecosystem.html presentations/agentic-sdlc/src/slides/fragments/37-scale-adaptive.html presentations/agentic-sdlc/src/slides/fragments/38-quick-flow.html presentations/agentic-sdlc/src/slides/fragments/39-workflow-matrix.html presentations/agentic-sdlc/src/slides/fragments/40-governance.html presentations/agentic-sdlc/src/slides/fragments/41-the-lexicon.html presentations/agentic-sdlc/src/slides/fragments/42-lexicon-to-pyforge.html presentations/agentic-sdlc/src/slides/fragments/43-build-for-agents.html presentations/agentic-sdlc/src/slides/fragments/44-governing-the-agent-not-just-the-code.html presentations/agentic-sdlc/src/slides/fragments/45-module-ecosystem.html presentations/agentic-sdlc/src/slides/fragments/46-bmad-builder.html presentations/agentic-sdlc/src/slides/fragments/47-plan-anywhere.html presentations/agentic-sdlc/src/slides/fragments/48-in-action.html presentations/agentic-sdlc/src/slides/fragments/49-get-started.html presentations/agentic-sdlc/src/slides/fragments/50-principles.html presentations/agentic-sdlc/src/slides/fragments/51-closing.html — and the thirteen the re-extract removed: presentations/agentic-sdlc/src/slides/fragments/06-what-is-ai-sdlc.html presentations/agentic-sdlc/src/slides/fragments/39-governance.html presentations/agentic-sdlc/src/slides/fragments/40-the-lexicon.html presentations/agentic-sdlc/src/slides/fragments/41-lexicon-to-pyforge.html presentations/agentic-sdlc/src/slides/fragments/42-build-for-agents.html presentations/agentic-sdlc/src/slides/fragments/43-governing-the-agent-not-just-the-code.html presentations/agentic-sdlc/src/slides/fragments/44-module-ecosystem.html presentations/agentic-sdlc/src/slides/fragments/45-bmad-builder.html presentations/agentic-sdlc/src/slides/fragments/46-plan-anywhere.html presentations/agentic-sdlc/src/slides/fragments/47-in-action.html presentations/agentic-sdlc/src/slides/fragments/48-get-started.html presentations/agentic-sdlc/src/slides/fragments/49-principles.html presentations/agentic-sdlc/src/slides/fragments/50-closing.html
- (event) 2026-09-14 spec-surface reconcile of this key's recorded-but-unstamped drift, named literally because the checker matches paths by substring (sources/chain.py). (1) The twenty paths the 2026-09-13 surface-drift-exclude decision above removed from this kernel's surface (ten posters + ten deck READMEs; the checker reported each as 'removed' against the pre-exclude baseline): presentations/pyforge-atlas/project/PyForge Atlas Infographic standalone.html presentations/pyforge-atlas/README.md presentations/pyforge-doctor/project/PyForge Doctor Infographic standalone.html presentations/pyforge-doctor/README.md presentations/pyforge-genesis/project/PyForge Genesis Infographic standalone.html presentations/pyforge-genesis/README.md presentations/pyforge-herald/project/PyForge Herald Infographic standalone.html presentations/pyforge-herald/README.md presentations/pyforge-marshal/project/PyForge Marshal Infographic standalone.html presentations/pyforge-marshal/README.md presentations/pyforge-mason/project/PyForge Mason Infographic standalone.html presentations/pyforge-mason/README.md presentations/pyforge-scribe/project/PyForge Scribe Infographic standalone.html presentations/pyforge-scribe/README.md presentations/pyforge-steward/project/PyForge Steward Infographic standalone.html presentations/pyforge-steward/README.md presentations/pyforge-unifying-strategy/project/PyForge Unifying Strategy Infographic standalone.html presentations/pyforge-unifying-strategy/README.md presentations/pyforge-warden/project/Warden Infographic standalone.html presentations/pyforge-warden/README.md. (2) The four unifying-strategy prototypes Story 21.11 pulled from Design in commit b8016a87e7 (the only commit touching them since the last stamp; the CAP-5 proof event in spec-deck-family-lockstep/.memlog.md records the pull): presentations/pyforge-unifying-strategy/project/PyForge Story.dc.html presentations/pyforge-unifying-strategy/project/PyForge Unifying Strategy - Executive Summary.dc.html presentations/pyforge-unifying-strategy/project/PyForge Unifying Strategy - Infographic.dc.html presentations/pyforge-unifying-strategy/project/PyForge Unifying Strategy - Infographic Deck.dc.html. Scoped stamp for this key follows; nothing outside these 24 paths differs from the baseline.
- (event) 2026-09-15 incoming surface (herald Epic 21 recover): ten-deck lockstep derive of Infographic heads/decks from standalones via deck-trio. Paths under presentations/pyforge-*/project/*Infographic*.
- (event by cursor) Story 21.5: ten PyForge exec summaries stamped from facts.yaml; dated 2026-09-15 marp sources + standalone HTML + both PPTX regenerated via deck-export. Residual unmarked historical tokens; 21.4 Design push still pending.
- (event by cursor) Wave C local prepare (Stories 21.6-21.9): derived facts.yaml and rebuilt four chain-deck standalones to infographic-standard (0 unmarked / 0 mismatch). Design push deferred. spec_status omitted because SPEC.md lives in archive folds.
- (event) 2026-09-17 one-chain fold: reminted 47 CAPs as CAP-1..47; absorbed 9 Spec folders (companions retained); covers-dreams 11; station Spec status ready; no blocked ledger keys flipped.
- (provenance) CAP remint table is in planning-artifacts/rekey-2026-09-17.md comments and SPEC.md CAP headings.
- (event by claude) 2026-09-18 recovered Story 23.3 (CAP-33, the `.potx` path) and Story 23.4 (CAP-34, PowerPoints push back) from the interrupted `bmad-loop` run `20260914-201759-bd47`'s stale loop home, whose stray per-story worktrees held complete, reviewed work never landed to main before the run was killed: the ledger already read Story 23.3 `done` (a false-done — the code was never on main) and Story 23.4 `backlog` despite finished code sitting unlanded. Three cherry-picks reconciled onto current main: `f909c5d726` (the `.potx` template-fill feature), `af5a9834d8` (its own six-verification-gap follow-up review pass), `3862fff87a` (the PPTX push-back + `--prove` read-back feature, which stacks on 23.3's stamps.py). Every "Story 23.5"/"Story 23.6" comment and docstring these commits carried from before the 2026-09-17 fold rekey (old numbering) was corrected to the current keys (23.3/23.4) while landing. Running the recovered Story 23.3 test suite against current main surfaced one real integration bug Story 21.5 introduced independently after the interrupted run branched off: `deck_export.py`'s `stamp_summary`/`stamp_marp` defaulted to true on ANY call without an explicit `--format`, including a bare positional-target call with no Executive Summary file — fixed to require `--format` explicitly for both. `pyforge-herald-test`: 1368 passed, 4 skipped (unchanged pass count from before this recovery). New file: `src/pyforge/herald/stamps.py` (write_stamp/read_stamp provenance sidecar) + `tests/unit/test_stamps.py`. Files: docs/specs/presentation-deck.md, scripts/deck_export.py, scripts/deck_trio.py, src/pyforge/herald/cli.py, src/pyforge/herald/deck_pipeline.py, src/pyforge/herald/errors.py, src/pyforge/herald/registry.py, src/pyforge/herald/stamps.py (new), src/pyforge/herald/transport/base.py, src/pyforge/herald/transport/mcp_transport.py, tests/unit/test_agent_sdk_transport.py, tests/unit/test_bridge.py, tests/unit/test_cli_push.py, tests/unit/test_deck_pipeline.py, tests/unit/test_mcp_transport.py, tests/unit/test_registry.py, tests/unit/test_stamps.py (new), tests/unit/test_transport_base.py, tests/scripts/test_deck_export.py, tests/scripts/test_deck_trio.py. `pixi.toml`'s `deck-export`/`deck-trio` PYTHONPATH env entries also changed (adds `pyforge-core/src` explicitly) but that surface is governed by `pyforge-marshal/spec-pyforge-core`, not this Spec.
- (event) Spec-surface reconciliation for this landing complete as of this entry; baseline re-stamped `--spec pyforge-herald/spec-pyforge-herald` immediately after.
- (event by claude) Story 21.10 landed 2026-09-18 (commit c1183e191a, CAP-4, "the registry sees all fourteen decks"): the three malformed chain-deck README registry sections (`wasm-analytics-stack`, `deckcraft`, `presenton-pixi-image` — each carried a multi-line body mixing the Modernist-binding clause, the "pull it with the MCP bridge" sentence, the Seeded table and, for wasm/deckcraft, a trailing note directly under the `## Design project (the bridge's far end)` heading, so `registry.read()` raised "expected exactly two body lines, found N") were fixed by inserting a `### Provenance` boundary right after the intended 2-line body first, moving the existing prose/table/note under it unchanged, THEN calling `registry.register(path, name, project_id, file_url)` — which rewrites only the span from the heading through that boundary, leaving `### Provenance` and everything below (Seeded tables, Ledger sections, corruption-incident writeups) byte-for-byte untouched. `unity-data-stack` needed no edit (Story 21.6 had already added its own `### Provenance` boundary 2026-09-17). `docs/specs/presentation-deck.md`'s bootstrap snippet was widened to sweep the four chain decks by explicit slug list (never a wider glob, so `agentic-sdlc` is never accidentally swept), hardened against a future malformed README (`try`/`except HeraldError`), a misleading partial `sorted()` call fixed, and its stale "the four chain decks sit there deliberately" sentence corrected to name `agentic-sdlc` (Story 23.2's) as the only deck deliberately left unregistered. Added `src/shared/packages/pyforge-herald/tests/meta/test_deck_registry_sections.py` as a regression guard over the real `presentations/*/README.md` tree. Verified: `pyforge-herald-test -k registry` 72/72; full suite 1369 passed, 4 skipped; `registry.read()` succeeds on all fourteen target decks and returns `None` for `agentic-sdlc`; a simulated fresh clone (`.herald/` removed) reproduces exactly fourteen `linked: true` entries via `herald deck status --repo-root .`. Files: `docs/specs/presentation-deck.md`, `presentations/deckcraft/README.md`, `presentations/presenton-pixi-image/README.md`, `presentations/wasm-analytics-stack/README.md`, `src/shared/packages/pyforge-herald/tests/meta/test_deck_registry_sections.py` (new). CORRECTION 2026-09-18: this landing's reconciliation entry was first written into the absorbed `spec-deck-family-lockstep/.memlog.md` (its own 2026-09-18 entry still stands as a duplicate historical record) instead of here — `spec-deck-family-lockstep`'s surface was reduced to `pixi.toml` alone by the 2026-09-17 one-chain fold, so none of these five paths are governed there any more; all five ARE declared in this kernel's own `surface:` (`docs/specs/presentation-deck.md`, the `presentations/{unity-data-stack,wasm-analytics-stack,deckcraft,presenton-pixi-image}/**` line, `src/shared/packages/pyforge-herald/**`), which is why `spec_surface_reconcile.py` flagged them here. Recorded properly in this entry; baseline scoped-stamped `--spec pyforge-herald/spec-pyforge-herald` immediately after.
- (event by claude) Surface reconcile 2026-09-18 (steward Story 59.2's own verify red on this repo-wide gate, unrelated to that story's own diff). Commit `97694e0b46` ("docs: sunset docs/specs/ by frontmatter status", doctor Story 23.3) relocated `docs/specs/presentation-deck.md`'s full body to `docs/how-to/presentation-deck.md`, leaving a short `status:-workflow` redirect stub at the old path (786 lines removed, 6 remaining) — a docs-tier relocation, not a change to this Spec's own contract; the file stays declared in this kernel's `surface:` because the redirect stub itself is still live. Already landed on `main`, unrelated to Story 59.2's own diff. Scoped-stamped: --write-baseline --spec pyforge-herald/spec-pyforge-herald.
- (event by claude) Story 23.1 implemented 2026-09-18 (`spec-design-sync-loop` CAP-1, "the account is enumerated and reconciled against the registry"): `DesignTransport` widened from 10 to 11 port methods with `list_projects` (`transport/base.py`'s `ProjectSummary` + Protocol method, implemented on both `McpTransport` and `AgentSdkTransport`; verified live 2026-09-18 that the deployed `claude-design` tool takes no arguments and answers a plain 20-entry JSON array with no pagination wrapper). `registry.py` gained `DESIGN_SYSTEM_PROJECT_NAMES` (Modernist/Broadsheet/Nocturne, matched by exact name) and `read_exclusions()` (parses a new `## Excluded projects (never twinned)` table in `presentations/README.md`, matched by exact name — never a heuristic, per the story's own Never boundary). `deck_pipeline.py` gained `AccountProjectStatus` + `account_status()`: enumerates `transport.list_projects()` and classifies each project `excluded` (exact match in `read_exclusions`) / `mirrored` (exact match in `DESIGN_SYSTEM_PROJECT_NAMES`) / `linked` (project id resolves through some local deck's own `registry.read()` — never `state.py`'s gitignored cache) / `untwinned` (none of the above); read-only throughout. `cli.py`'s `deck status` gained `--account` (refuses an explicit slug together with it). `presentations/README.md` gained the `## Excluded projects (never twinned)` table naming `REMOVED-PyForge Unifying Strategy` and `Local recipes repository connection` with their recorded reasons. Verified: `pyforge-herald-test` 1396 passed, 4 skipped (up from 1369); `pyforge-herald-coverage-gate` OK on all seven touched modules (80–99%); live `herald deck status --account --repo-root .` against the real signed-in account returned all 20 current projects with no project absent — 14 `linked`, Modernist `mirrored`, `Agentic AI SLDC deck`/`PyForge six-quarter roadmap`/`LLM Knowledge Bases` `untwinned`, the two retired projects `excluded` with their recorded reasons; confirmed `--account` together with a slug is refused and the pre-existing no-`--account` per-slug/all-decks JSON shape is byte-for-byte unchanged. Files: `presentations/README.md`, `src/shared/packages/pyforge-herald/src/pyforge/herald/cli.py`, `.../deck_pipeline.py`, `.../registry.py`, `.../transport/__init__.py`, `.../transport/agent_sdk_transport.py`, `.../transport/base.py`, `.../transport/mcp_transport.py`, `tests/unit/test_agent_sdk_transport.py`, `tests/unit/test_bridge.py`, `tests/unit/test_cli_status.py`, `tests/unit/test_deck_status.py`, `tests/unit/test_mcp_transport.py`, `tests/unit/test_registry.py`, `tests/unit/test_transport_base.py`. Not done: Story 23.2 (registering `agentic-sdlc` + the two brand-new twins + mirroring the design systems to `presentations/_design-systems/`) — out of this story's own scope, deps on it. Scoped-stamped: --write-baseline --spec pyforge-herald/spec-pyforge-herald.

- (event by claude) Surface re-stamp 2026-09-18 for Story 23.1's landing (PR #1459, `1dd017bc21 Merge pyforge-herald/23-1 into main`). The story's own reconcile above was stamped at 14:51Z, then bmad-build-auto's review pass patched `registry.py`, `cli.py`, `deck_pipeline.py`, `tests/unit/test_registry.py` and `tests/unit/test_deck_status.py` again (checkpoints `86fe4d482c`..`506fc1b9dd`, the 4-layer review triage applying its patches) — a stamp is only valid until the next edit of any file in the surface, so main landed with `spec-surface: drift` on five paths. No behaviour beyond what the entry above describes changed; the review patches tightened the same CAP-1 surface (status classification edge cases, README exclusion parsing, test coverage). Re-stamped scoped to this spec only.
- (event by claude) Story 23.2 landed 2026-09-18 (PR #1461, `489a2d2d19 Merge pyforge-herald/23-2 into main`; `spec-design-sync-loop` CAP-2, "every presentation has a local twin; design systems are mirrored as libraries"): the three Claude Design systems are mirrored byte-exact under `presentations/_design-systems/{broadsheet,modernist,nocturne}/**` (84 files: `_ds_bundle.js`, `_ds_manifest.json`, `components/*.html`, tokens, previews), two new presentation twins pulled byte-exact — `presentations/six-quarter-roadmap/{README.md,project/**}` and `presentations/llm-knowledge-bases/{README.md,project/**}` — each with a machine-owned registry section, and `presentations/agentic-sdlc/README.md` gained its registry section, so `registry.read` now resolves 17 surfaces. `herald/deck_pipeline.py` grew the adopt path (+294: windowed `read_file` pagination via `_windowed_read`, byte-exact twin/library pulls, an idempotent second pull that writes nothing) with `tests/unit/test_deck_pipeline.py` (+335) and `tests/meta/test_deck_registry_sections.py`; `tests/unit/test_fleet_scan_pitch_roster.py` covers the review-pass fix that excluded the two new non-pipeline twins from `scan_pitch()`. The dispatched session did not reconcile this memlog itself, so main landed with 102 `spec-surface: drift` rows on this spec; reconciled here and re-stamped scoped. Open follow-up ingested as DW-FU-23-2 (no non-advancing-window guard in `_windowed_read`).
- (event by claude) Story 23.5 landed 2026-09-18 (PR #1463, `f2e9ec9a69 Merge pyforge-herald/23-5 into main`; `spec-design-sync-loop` CAP-7 bound to `spec-pyforge-pages` CAP-1/CAP-2, "the family is browsable and downloadable on Pages"): `docsite/build.py` (+296) gains one family page per registered deck plus a Decks index — poster, Infographic Deck and Executive Summary in view, PPTX(s) and Marp sources as downloads deduplicated to the newest export per product, each stamped with its etag and tree, `check()` extended with size/unrendered-Jinja checks for family pages; new templates `page_family.html.j2`, `page_family_index.html.j2`, `shell_page.html.j2`, `page_gallery.html.j2` folded into the shell, `page_index.html.j2` gains the Decks landing card; `docsite/content/site.yml` and `docsite/assets/_site-chrome.css` extended; `docsite/README.md` outputs table updated. `dashboard.yml` stays the only `deploy-pages` caller and Kedro-Viz stays at `/kedro-viz/`. The dispatched session did not reconcile this memlog, so main landed with `spec-surface: drift` on the docsite paths; reconciled here and re-stamped scoped. Open follow-up ingested as DW-FU-23-5 (no PR-gating CI lane runs `docsite/build.py`/`site-check`; pre-existing across the whole docsite pipeline).
- (event by claude) Story 23.6 landed 2026-09-18 (PR #1465, `fbe1bffc92 Merge pyforge-herald/23-6 into main`; `spec-design-sync-loop` CAP-8 plus CAP-3's sweep half, "one command, idempotent, reported"): new `herald/sync_all.py` (+686) composes enumerate → pull every twin whose etag moved → refresh → derive → push → prove → publish, in order, for every registered deck or one `--slug`, printing a per-deck report (pulled / overwrote-local / overrode / derived / pushed / proven / published / unchanged); a second consecutive run reports every deck unchanged with zero writes, `--dry-run` previews read-only, and one deck's failure is isolated into its own report line. `herald/cli.py` gains `deck sync-all` (+73); `pixi.toml` gains the `deck-sync-all` task (`feature.pyforge-herald`, task-only — `environment.yaml` unchanged and verified in sync); `docs/how-to/presentation-deck.md` § The MCP bridge and `presentations/README.md` now point at the command rather than the steps. Tests: `tests/unit/test_sync_all.py` (+1115), `tests/unit/test_cli_sync_all.py` (+200), `tests/meta/test_deck_sync_all_pixi_task.py`, `tests/unit/test_bridge.py`. The dispatched session did not reconcile this memlog, so main landed with six `spec-surface: drift` rows; reconciled here and re-stamped scoped. Open follow-up ingested as DW-FU-23-6 (the idempotency AC is proven over fakes plus one live smoke of the skipped path only — settling it needs two `sync-all` runs against a seeded deck with live Design credentials). This closes Epic 23: 23.1/23.2/23.5/23.6 all landed 2026-09-18 via `marshal factory dispatch` on the Claude harness.
- (capability by claude) CAP-48 — the windowed Design read never loops on a stalled window. Intent: `_windowed_read` raises a typed pagination error when `last_line` does not advance between windows. Source: DW-FU-23-2 (Story 23.2's Edge Case Hunter finding, reachability unproven live).
- (capability by claude) CAP-49 — the docsite has a PR gate. Intent: a `pull_request` lane path-filtered to `docsite/**` runs `build.py --check` + `site-check`, mirrored into `pr-preflight`; `dashboard.yml` stays the only `deploy-pages` caller. Source: DW-FU-23-5 (Story 23.5's deferral: no PR-gating CI runs docsite; pre-existing for dossier/gallery/artifact).
- (capability by claude) CAP-50 — the second `sync-all` run is proven unchanged on a real deck. Intent: an opt-in, recorded, one-command live proof (`HERALD_LIVE_SYNC_PROOF=1`) against a seeded deck with real Design credentials, listed in doctor's live-proof-surfaces catalog. Source: DW-FU-23-6 (Story 23.6's deferral: idempotency proven over fakes plus one live smoke of the skipped path).
- (constraint by claude) CAP-50's live proof is opt-in and operator-run — never part of the default gate, never a second PR gate; the credentials only a person has are the reason the deferral existed, so the capability is the recorded act, not an unattended promise.
- (event by claude) Story-minted 2026-09-18 as Epic 24 (24.1..24.3, one per CAP); Epic 23 (23.1/23.2/23.3/23.4/23.5/23.6 all done today) flips `done` in the tracked ledger.
- (event by claude) Story 24.3 landed 2026-09-18 (`spec-pyforge-herald` CAP-50, "the second `sync-all` run is proven unchanged on a real deck"): `herald/sync_all.py` gains an opt-in `proof_dir` seam (`_report_to_dict`/`_write_proof`) that serializes each deck's `DeckSyncReport` plus a `stamps.write_stamp` sidecar under `<proof_dir>/<slug>/report-<timestamp>-<uuid8>.json`, isolated per-deck (an aggregated `HeraldError` only after every report has had its own write attempt) and using the module's existing `now` seam rather than a direct clock read. `herald/cli.py`'s `sync-all` verb gains `--proof-dir`, gated on `HERALD_LIVE_SYNC_PROOF=1` (refused before any transport is constructed) and prints the proof-dir path on success. `pixi.toml` gains the opt-in `deck-sync-proof` task (never in `pr-preflight`'s `depends-on`). `_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/spec-pyforge-doctor/live-proof-surfaces.md` gains the herald `sync-all` idempotency row. `deferred-work-ledger.md`'s `DW-FU-23-6` stays `status: open` — no live run has been recorded yet (no dispatch environment has live Claude Design credentials, per that entry's own `evidence:` field); its new `note:` field names the now-existing operator command. Review found 5 medium + 9 low findings across 4 layers; 8 patched (a raw-`OSError` leak in `_write_proof`, missing per-deck isolation in the proof-write loop, the bypassed `now` seam, a same-microsecond filename-collision guard, an unrecognized ledger field key, a missing CLI output-path print, an underspecified doc pointer, one missing test), 5 rejected false, 2 rejected low (both real but out of this story's single-`--slug` scope or in tension with the `published`-field accuracy the batched write design depends on — full reasoning in `spec-24-3-...md`'s own Review Triage Log). Tests: `tests/unit/test_sync_all.py`, `tests/unit/test_cli_sync_all.py`, `tests/meta/test_deck_sync_all_pixi_task.py`. The dispatched session reconciled this memlog itself (this entry) and re-stamps scoped immediately after.
- (event by claude) Epic 24 landed 2026-09-18: 24.1 (`fb2e983f96`, CAP-48), 24.2 (PR #1472, CAP-49 — the docsite lane proved itself live on the story's own PR), 24.3 (PR #1482, CAP-50 mechanism). The dispatched sessions reconciled this memlog themselves for all three (the 24.x fallout was one co-governor row on spec-pyforge-core). CAP-50's recorded live proof is operator-run and still pending — DW-FU-23-6 stays open until it exists; epic-24 flips done on the ledger because its three story keys are done, with that caveat recorded on spec-24-3.
- (event by claude) Live sync proof 2026-09-19 (DW-FU-23-6, operator-run with live Claude Design credentials): recorded at specs/spec-pyforge-herald/sync-proof-2026-09-19.md. pyforge-herald (never pulled) reaches only the skipped path; pyforge-warden (the one deck with pull etags) ran twice — both runs re-derived the stale deck and pushed both PPTX with identical read-back, and both refused the standalone HTML on read-back mismatch (deterministic → DW-FU-23-6-1, medium; DW-FU-23-6 closed as verified-with-finding). The re-derived deck is committed as the deck-currency refresh sync-all exists to produce (operator ruling): presentations/pyforge-warden/{README.md (push-and-prove ledger, deduped), facts.yaml (derived_at 2026-09-19; doctor 25/28, 108/114 …), project/Warden - Executive Summary.dc.html, project/Warden - Infographic Deck.dc.html (+.stamp.json), project/Warden - Infographic.dc.html (+.stamp.json), project/Warden Infographic standalone.html, src/marp/pyforge-warden-{deck,executive-summary,infographic}-2026-09-15.md, src/marp/pyforge-warden-infographic-standalone-2026-09-15.html (+.stamp.json), src/pptx/pyforge-warden-deck-2026-09-15.pptx (+.stamp.json), src/pptx/pyforge-warden_infographic_deck-2026-09-15.pptx (+.stamp.json)}; planning-artifacts/deferred-work-ledger.md (DW-FU-23-6 done, DW-FU-23-6-1 open). Scoped stamp for spec-pyforge-herald after this entry.
- (event by claude) Surface reconcile 2026-09-19 (co-governor; doctor Story 23.5, merge c83741220a): presentations/six-quarter-roadmap/project/github.md — a citation of an already-moved _bmad-output/ file repointed to its archive path; content otherwise unchanged. Owner: spec-pyforge-doctor.
- (event by claude) Surface reconcile 2026-09-19 (cross-station, doctor Story 23.5, spec-docs-shelf-alignment CAP-5): presentations/six-quarter-roadmap/project/github.md's Screen map rewrote two citations to point at the now-archived _bmad-output/{FLEET-READINESS-2026-08-08.md,DREAM-TRIAGE-2026-08-08.md} paths (both files already moved to archive/_bmad-output/ per prior story). Citation text only, no screen/deck content change. Doctor-owned edit; no herald contract moved. Accept the doctor-owned edit; scoped-stamped: --write-baseline --spec pyforge-herald/spec-pyforge-herald.
- (event) Surface reconcile 2026-09-19: updated Diátaxis docs and package READMEs
- (direction by claude) 2026-09-20 seed (operator ask 09:55Z): the docs site matches BMAD-METHOD's pattern (Astro+Starlight over docs/ via symlink, sidebar.order validators, one docs.yaml Pages deploy) so upstream's skills and workflows apply unchanged. Input: planning-artifacts/research/docs-site-bmad-method-pattern-2026-09-20.md (six decisions). docs/_STYLE_GUIDE.md vendored ahead (the bmad-os-diataxis skill reads it). Not yet a CAP — bmad-spec next session; Kinships: doctor 30.x (map.yaml ↔ sidebar), scribe 19.3.
- (capability by claude) CAP-51 — the deck pipeline runs from the Guild env. Intent: deck_pipeline.py's DeckExporter and sync_all.py's FactsRefresher / trio step shell pixi run -e pyforge-guild deck-export | deck-facts | deck-trio (the tasks registered in guild-tasks with their deps in pyforge-guild — steward 63.6), never -e local-recipes; the pipeline's fakes and the live sync proof keep their shapes. Success: no -e local-recipes string remains in pyforge-herald/src; test_deck_pipeline's argv assertions read the Guild env; deck-sync-proof's opt-in live run still passes; steward 63.6's meta-test lists no herald offender; pyforge-herald-test green.
- (event by claude) Mint 2026-09-20 (later): docs/dreams/pyforge-herald.md (2026-09-20 later entry), epics.md (Epic 25, Story 25.1 backlog), sprint-status-ledger.yaml (25-1 backlog, epic-25 backlog), specs/spec-25-1-*.md, docs/foundry/capability-ledger.yaml (CAP-51), this Spec's SPEC.md (CAP-51 rendered).
- (event by claude) Surface reconcile 2026-09-20 (Story 25.1, CAP-51 — the deck pipeline runs from the Guild env, hand-driven in PR #1551): src/shared/packages/pyforge-herald/src/pyforge/herald/deck_pipeline.py (DeckExporter and _PixiPartialDeckExporter argv, messages and docstring name -e pyforge-guild), sync_all.py (deck-facts / deck-trio steps), tests/unit/test_deck_pipeline.py (Guild argv assertions), planning-artifacts/specs/spec-25-1-*.md (status done, Auto Run Result), epics.md (Epic 25 / 25.1 Outcome), sprint-status-ledger.yaml (epic-25 and 25-1 done — the epic's only story landed with steward 63.6, which moved the tasks into guild-tasks and pyforge-herald into the Guild feature).
- (event by claude) Surface reconcile 2026-09-20 (fleet consistency pass before the foundry cutover; operator ruling: consistency even where historical records are rewritten): 57 tracked story specs under planning-artifacts/specs/ reconciled against sprint-status-ledger.yaml: frontmatter status equals the ledger row (done rows: done; blocked/backlog/in-progress rows: a status in that row's vocabulary), the Ledger-status line matches, and every done story carries an Auto Run Result (reconstructed from main's landing commits where none survived, marked reconstructed; no-evidence cases stated as such). Keys: 1-3, 1-6, 2-1, 2-2, 2-3, 2-4, 3-1, 3-2, 4-1, 4-2, 4-3, 5-1, 5-2, 6-1, 6-2, 6-3, 6-4, 6-5, 7-1, 7-2, 8-1, 8-2, 8-3, 8-4, 9-1, 9-2, 9-3, 9-4, 9-5, 10-1, 10-2, 10-3, 10-4, 10-5, 10-6, 11-1, 11-2, 11-3, 11-4, 12-1, 12-2, 12-3, 12-4, 15-2, 18-1, 21-1, 21-2, 21-3, 21-4, 21-5, 21-6, 21-7, 21-8, 21-9, 21-11, 23-3, 23-4. Also: spec-21-4 frontmatter made valid YAML (decks'' escaped); run-result Status on 18-1 / 21-6 -> done citing the promoting commits; 23-3 / 23-4 status ready -> done; sprint-status-ledger.yaml re-synced by sprint-ledger-sync (epic-21, epic-22 -> done: every story done).
- (event by claude) Surface reconcile 2026-09-20 (operator ruling: every station's PRD re-stamped in the fleet consistency pass, grace period or not): prds/prd-pyforge-herald-2026-08-01/prd.md, architecture/architecture-pyforge-herald-2026-08-01/ARCHITECTURE-SPINE.md, epics.md each gain a 'Currency reconciliation 2026-09-20 (fleet consistency pass)' note and updated: 2026-09-20 (spec->prd->arch->epics; bookkeeping, no requirement / AD / story moved).
