---
topic: pptx-deck-generation — parked with trigger
updated: 2026-09-09T15:51
---

- (event) Parked spec authored 2026-08-22 (operator: get all seeds ready-to-implement): the Dream's own constraints forbid building until the trigger fires; zero stories minted — parking IS the decomposition decision
- (note) Trigger half-PROVEN 2026-08-22: inspected the shipped exports (presentations/deckcraft/src/pptx/*.pptx, 10 slides) — ZERO text runs, content as background-image blips (a:blip in p:bgPr): Marp's .pptx export is structurally non-editable. The park now hinges ONLY on the audience question (does any deliverable audience need to EDIT — e.g. the 21-station deck program going to stakeholders?). Operator asked same day
- (decision) UNPARKED by operator 2026-08-22: audience NAMED — the 21-station deck program delivered to stakeholders who edit slides. With Marp inadequacy already proven (background-image slides, zero text runs), both trigger halves are satisfied; decomposed as herald Epic 15 (15.1), pptx-custom-shapes as its downstream (15.2)
- (decision by claude) 2026-09-09 THE ONE OPEN QUESTION (TEMPLATE PROVENANCE) IS CLOSED — ANSWERED AT 15.1 (2026-08-22) WITH A THIRD OPTION NEITHER BRANCH NAMED, and never propagated back. `scripts/generate_default_template.py:1-20` records that neither named branch is executable unattended — a hand-derived six-act `.potx` needs custom slide LAYOUTS, which python-pptx cannot author without raw-OOXML surgery (forbidden by this epic's own Boundaries), and an operator-supplied `.potx` needs a human who was not present in the run — so 15.1 adopted python-pptx's bundled default Office theme verbatim, committed as `src/pyforge/herald/templates/pyforge-deck-template.pptx` and regenerated (never hand-edited) by that script. `extract_spec` derives `spec.json` generically, so a future PyForge-branded `.potx` is a `--template` flag, not a code change. Batch row `herald-OQs` (fleet-readiness-decision-batch-2026-09-09 § 2.1; report § D F-10, classification (c) stale). The BRANDED-template decision stays genuinely open but is deferred work, not a contract gap — record it as a Residual, not an `open_questions:` entry.
- (note by claude) 2026-09-09 BUILT, NOT IN EFFECT — recorded under the realization gate (batch § 2.3 C6). Epic 15 is `done` (15.1 + 15.2) and `pptx_pipeline.py` (1057 lines) ships CAP-1 (`extract_spec`/`fill_template`) and CAP-2 (`add_card`/`add_metric_box`/`add_table`/`add_section_label` + Pillow `fit_text` autofit), CLI-reachable at `cli.py:342-386` / `:766-769`. The success signal — "a station deck ships as an editable `.pptx` a stakeholder modifies and sends back" — has never been exercised: no `content_plan.json` exists anywhere in the tree, and every `.pptx` under `presentations/*/src/pptx/` is a dated Marp export from 2026-07/08, not pipeline output. Vessel: herald Epic 19 Story 19.4.
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. CAP-1/CAP-2 preserved; status stays ready; the template-provenance open question retired (ANSWERED comment kept) and its answer promoted to a Constraint (generated-never-hand-edited default template + --template seam); the branded-template residual recorded as a Non-goal, not an open question; realization status 'built, not in effect' recorded under Success signal with vessel Epic 19 Story 19.4.
