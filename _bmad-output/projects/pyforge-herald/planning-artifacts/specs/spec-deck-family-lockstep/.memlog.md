---
topic: deck-family-lockstep — every surface of every deck derives from the same facts.yaml; the four chain decks join the standard and the registry; the Design side is used as a design surface
updated: 2026-09-15T04:15
---

- (event) create: derived from docs/dreams/deck-family-lockstep.md at Epic 20's closeout (2026-09-13); express mode — the parent Spec settled the standard, the ledger shape and the parallel-agent discipline, so only the new surfaces need a contract
- (note) measured on main 2026-09-13 after Epic 20 landed: ten deck READMEs say 'standalone ahead'; the Infographic heads are 14-48 KB against posters of 112-295 KB; every marp source and PPTX is dated 2026-07/08; four chain decks (unity-data-stack, wasm-analytics-stack, deckcraft, presenton-pixi-image) are 15-19 KB stubs with no facts.yaml and malformed registry sections; herald deck status reports 10 of 15 linked
- (decision) the head and Infographic Deck are DERIVED from the standalone, never re-authored: the trio's own definition says the head's body and the standalone's body are identical, differing only in the x-dc wrapper and where the styles sit — so a transform removes the whole class of divergence rather than re-checking it
- (capability) CAP-1 the trio derives from the standalone: a verb turns a current standalone into its Infographic head (.dc.html, x-dc wrapper, styles in the helmet) and its Infographic Deck (the same sections re-laid as 1920x1080 slides), byte-faithful in body; success: run over a rebuilt deck, the head's body matches the standalone's exactly (modulo the wrapper and style placement), the Infographic Deck carries one slide per numbered section, both render, and re-running changes nothing
- (capability) CAP-2 every marked surface of a deck refreshes together: deck-facts <slug> --refresh walks the poster, the head, the Infographic Deck, the exec summary and the marp sources, so one ledger move updates every surface; success: after a tracked ledger changes, a single --refresh leaves every surface of that deck at 0 mismatch, and --check reports per-surface
- (capability) CAP-3 the Standard export set is current in every format: the exec summary and marp sources carry marked facts and deck-export regenerates both PPTX from them; success: for every deck the six companions are regenerated from current sources, dated the rebuild day, and no README still reads 'standalone ahead'
- (capability) CAP-4 fourteen decks, not ten: the four chain decks are rebuilt to the standard from their own fact ledgers, registered through registry.register and linked; success: herald deck status reports every deck in presentations/ linked with its project id, and each of the four meets the infographic-standard floors with 0 unmarked / 0 mismatch
- (capability) CAP-5 the Design side is used as a design surface: at least one deck goes through a real visual pass in Claude Design and returns through a byte-exact pull; success: a dated Design-side edit is pulled to git, the read-back is byte-identical after the harness strip, the deck README records the etag, and the pull discipline (git is the archive of record) is demonstrated end to end
- (constraint) derive, never re-author: a hand-written divergence between the standalone and its head is the defect this Spec removes; the same rule that governs facts governs structure
- (constraint) the marked-fact contract is unchanged (every number a data-fact mark over a tracked facts.yaml row) and the three hazards --refresh cannot cover stay in force: hand-counted prose, swept SVG text labels, .tag{display:inline-flex} fusing a marked token to its neighbour
- (constraint) never restrict size at authoring; one PR per deck; worktrees and physical paths, never bmad-switch in a parallel agent; non-recipe PRs carry the maintenance label; checks stay advisory, never a second gate; deck_export.py's semantics, the React deck and the pptx pipeline are untouched
- (decision) non-goals: a new deck engine, a second PR gate, changing deck_export.py's semantics, re-authoring any poster Epic 20 already landed, and Design-side polish beyond the one proving pass CAP-5 names
- (decision) status ready, open_questions []: the parent Spec and Epic 20's retro settle every decision this one would otherwise have to ask
- (event) CAP-5 proved 2026-09-13 on pyforge-unifying-strategy (S-21.11, hand-driven, not via bmad-loop): a human visually improved the head/exec-summary/Infographic-Deck in a second, un-seeded Claude Design project (`13f845b0-4387-4a51-b1d4-304be61449a3`, missing the Modernist `_ds/` binding, `deck-stage.js` and the `reference/` exemplar) rather than the deck's own registered project (`1e4020bc`). Operator ruling: `1e4020bc` stays canonical; pulled the improved head/exec-summary/Infographic-Deck from `13f845b0` and landed them in `1e4020bc` and git instead of rebinding the registry. Standalone had no Design-side counterpart at all (13f845b0 never carried one) — mechanically derived it FROM the new head by hand (unwrap x-dc, move style into head, drop the dc-script marker), the reverse of the normal head-from-standalone direction S-21.1/21.2 will eventually mechanize. Pull recipe used render_preview -> curl -> strip; found and fixed a stripper bug leaving one spurious blank line after <head> (collapsed only the first newline pair of a three-newline gap, not the whole gap) before it reached git. Round-tripped byte-identical twice.
- (finding) the fresh content regressed deck-facts --check from 0 unmarked/facts 36/36 to 24 unmarked/facts 4/28 (masthead rebranded "Foundry Platform", was "The Canopy" — unreconciled in the README/deck-inventory display-brand fields, an operator call not made here). Hand-marked back down to 3 unmarked, following this deck's own pre-existing house style for exactly this situation (truncate x.y.z version floors to x.y, rewrite dated protocol-revision spans to month+year, drop narrative dates with no relative-day sequence to relabel to, rewrite bare N/M fractions as "N of M"). The remaining 3 (SPEC.md's AD-1 datastores exception 2026-09-11, stack.md's own updated: 2026-08-26, two dated filename/catalog citations at 2026-08-30) were each verified against their live cited source and are real values with no facts.yaml row yet -- same "no ledger row exists" category this README already documents for the pre-pull content. Turning them into tracked facts needs new derivation code in the shared deck_facts.py, not done here.
- (finding) an un-seeded, ad-hoc Claude Design project can exist alongside a deck's registered one with no detector noticing -- herald deck status / registry.read only ever look at the README's own pointer, never enumerate a user's whole Design account. Future CAP-5-style passes should list_projects first to catch this.
- (event) 2026-09-14 spec-surface reconcile: the four prototypes the CAP-5 proof event above pulled in commit b8016a87e7 (Story 21.11), named literally for the checker: presentations/pyforge-unifying-strategy/project/PyForge Story.dc.html presentations/pyforge-unifying-strategy/project/PyForge Unifying Strategy - Executive Summary.dc.html presentations/pyforge-unifying-strategy/project/PyForge Unifying Strategy - Infographic.dc.html presentations/pyforge-unifying-strategy/project/PyForge Unifying Strategy - Infographic Deck.dc.html. That commit is the only one touching them since this key's baseline; scoped stamp follows.
- (event by claude) Story 21.1 landed 2026-09-14 (commit b3b220b7f77b0d25cc2ac96e55897d7c95f30244): CAP-1's head half. New `scripts/deck_trio.py` (`deck-trio <slug> --head`) reads the deck's standalone poster, wraps its body in `<x-dc><helmet>`, relocates the poster's `<head><style>` block plus font/stylesheet `<link>` tags into that `<helmet>` verbatim, adds the `support.js` tag and a `data-dc-script`/`$preview` block sized from a headless-Chromium measurement of the standalone's rendered height, and writes the head -- refusing on a missing/ambiguous poster or an unlocatable `<style>` block, and never touching the standalone. Review pass (19 findings; 3 high, 6 medium, 7 low, 3 false) patched a purity-breaking bug (the height measurement wasn't waiting for fonts/network, so a rerun over an *unchanged* poster returned different heights -- fixed with `wait_until="networkidle"` plus `document.fonts.ready`) and an envelope-slice-vs-matched-spans bug in the helmet extraction; `pytest tests/scripts/test_deck_trio.py -v` 11/11 green, three consecutive real measurements against the unchanged Warden standalone now agree. `pixi.toml` gained `[feature.local-recipes.tasks.deck-trio]`; `docs/specs/presentation-deck.md` gained a pointer sentence under § Artifact dependency tree. CAP-1's deck (`--deck`, Infographic Deck derivation) is Story 21.2, not this one. Scoped-stamped: --write-baseline --spec pyforge-herald/spec-deck-family-lockstep.
- (event by claude) Story 21.1, second review pass 2026-09-14 (triggered by a `spec-surface` verification failure on the once-reviewed tree, not a fresh dispatch -- see the story spec's own Review Triage Log second entry for the full 22-finding breakdown): `scripts/deck_trio.py` patched again. The `measure_height`/`main()` failure paths were hardened to match `deck_qa.py`'s own established precedent -- both chromium-launch fallback `except Exception:` clauses broadened to `except (Exception, SystemExit):` (playwright's own internals have raised `SystemExit` live in this codebase before, per `deck_qa.py`'s own comment at the identical fallback point), `browser.new_page(...)` and `main()`'s `measure_height(poster)` call site both now convert failures into the tool's own clean `ap.error`/exit-2 refusal instead of an unhandled traceback, the helmet's joined spans now separate with `"\n"` (previously concatenated with no separator, silently dropping inter-tag whitespace), and the preview width `1240` was de-duplicated into one `PREVIEW_WIDTH` module constant. `tests/scripts/test_deck_trio.py` gained 6 tests (11 -> 17), three of them exercising the real `measure_height` (not monkeypatched) via a fake `playwright.sync_api` injected into `sys.modules` -- `tests/scripts/` runs in the stdlib-only `pyforge-ci` env, which has no real `playwright` install, so `test_deck_qa.py`'s patch-the-real-module precedent does not apply here. No output-bytes change for any real poster (`PREVIEW_WIDTH` is still 1240; the real-poster smoke check against the live, unchanged Warden standalone still returns 34641 across three calls). `pytest tests/scripts/test_deck_trio.py -v` 17/17 green; the full `pyforge-doctor-scripts-test` suite is 426 passed / 12 skipped (was 420/12). Scoped-stamped: --write-baseline --spec pyforge-herald/spec-deck-family-lockstep.
- (event by claude) Story 21.2 landed 2026-09-15 (commit 7a17d2b113a49710330fe4b6eb6ca8b1e2f20c4d): CAP-1's deck half. New `_DeckStructure` (a purpose-built `HTMLParser` subclass, not a `_PosterStructure` extension) walks `<body>` in document order and captures the masthead (content strictly before the first `.act`/`.sec` element, when present), `<div class="act">` bands, `<section class="sec">` numbered sections (sliced per direct child, not as one span, so overflow-split can pack children onto consecutive slides), and the closing band (content strictly after the last `.act`/`.sec`, when present). A new section-layout measurement function launches headless Chromium ONCE per `--deck` run (not per section, mirroring `deck_qa.py::render_gate`) and reads back each section's total height plus each direct child's effective height as an `offsetTop`-delta to its next sibling (not an isolated `getBoundingClientRect().height`, which excludes CSS margin-collapse and could understate a "fitting" group's real height) — same `networkidle`+`document.fonts.ready` determinism fix `measure_height` needed in Story 21.1. A greedy packer splits only a section that overflows its slide budget, at direct-child boundaries, never rewriting content; continuation slides keep the section's own `data-label` with a mechanical `(cont. i/n)` suffix. `render_deck` emits the family's established `<x-import ...deck-stage.js...><x-dc>` skeleton verbatim (reused, not reinvented) at `presentations/<slug>/project/<Persona> - Infographic Deck.dc.html`, with the same compare-before-write idempotency as `--head`. `main()` wiring: `--head`/`--deck` are now independently combinable (`--head`'s old `required=True` dropped), refusing exit 2 when neither is given, and both modes' preconditions validate before either file is written — no partial-completion state. First review pass (19 findings) surfaced two `[high]` `[intent_gap]` findings: the intent-contract never said whether the masthead/closing band belong in the derived deck at all, two defensible readings with real textual support on both sides, not mechanically resolvable — escalated to the operator rather than guessed. Operator ruling 2026-09-15: both ARE included, as fixed never-split `data-label="Cover"`/`"Close"` bookend slides, each only when present (`infographic-standard.md` mandates both structurally; all 14 pre-existing hand-authored decks already open/close on this content). The tree was reverted and re-implemented from scratch against the amended intent-contract (this run's worktree-isolation mode has no restore-supported re-drive), folding in seven other pre-verified-but-moot findings from the same pass: empty-`data-label` refusal, zero-direct-children-section refusal, a `measure_all_sections` length-mismatch guard, the head+deck validate-before-write-either ordering, a stale duplicate test's removal, a `--deck`-only-doesn't-also-write-head test, and the offsetTop-delta margin-collapse-safe measurement (already folded into the description above). Second review pass on the re-derived tree (27 findings: 0 high, 0 medium, 18 low, 9 false) found no gating defect — every `[false]` row was a refutation backed by reading the live Warden standalone's own CSS/markup or the spec's own pre-written Design Notes, and the `[low]``[patch]` rows actioned were cosmetic/doc-completeness (docstring + `pixi.toml` task description both gained the measured-vs-parsed section-count-mismatch refusal to their enumerated refusal lists; `measure_section`'s total now `round()`s instead of truncating, matching each child height's own rounding rule so a total within a fraction of a pixel of the budget boundary can't flip the fit-vs-split decision inconsistently; one new test for a single-child section whose sole child alone exceeds budget; one new test proving non-overflowing sections in a multi-section deck stay single, unsplit slides alongside a split one). Every `[low]``[reject]` row (12) was a real-but-zero-live-instance edge case (exact-string vs token-based class matching, a nested-`<span>`-inside-`.lbl` truncation edge, a bare text/void-element direct child, an act nested inside a section, an unclosed tag at EOF, a per-section child-height-list-length guard one level more granular than the intent asked for, cross-slide `data-label` uniqueness, `_escape_attr` not escaping `<`) — rejected per the family's own established low+zero-live-instance+complexity-adding-guard precedent from Story 21.1's review. `tests/scripts/test_deck_trio.py` grew from 11 (Story 21.1's count) to 50, covering every `--deck` I/O matrix row including the masthead/closing-band present/absent cases, the oversized-section split path (synthetic fixture, monkeypatched measurement, no live browser), and a `--deck`-only assertion the head artifact is not also derived; `test_head_flag_is_required` was removed, superseded by `test_neither_head_nor_deck_given_names_the_fix`. `python -m pytest tests/scripts/test_deck_trio.py -v` 50/50 green. `docs/specs/presentation-deck.md` § Artifact dependency tree and `pixi.toml`'s `deck-trio` task description both updated to describe `--deck` now that it exists (full reconcile in spec-deck-family-currency's own memlog, since neither file is in this Spec's own surface). No output-bytes change to any existing artifact — `--deck` is wholly new output; `--head`'s own bytes are unaffected (both modes validated, and `--head`'s own code path is untouched by this diff). Scoped-stamped: --write-baseline --spec pyforge-herald/spec-deck-family-lockstep.
