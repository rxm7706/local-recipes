---
topic: pyforge-pages — one dossier YAML, many renders; Pages landing; Kedro-Viz stays; foundry cargo after 54.5
updated: 2026-09-20T08:38
---

- (event) create: derived from docs/dreams/pyforge-pages.md and pyforge-pages.bundle (tip 5b6a540477, base 50504e52e); express mode — the bundle already implements the site
- (decision) owner is herald (public proclamation); steward owns only the Pages deploy duct; atlas owns kedro-viz/
- (capability) CAP-1 dossier YAML is the source of truth; site, artifact, and gallery are renders; success: edit dossier.yml, site-check rebuilds every page from it, no second hand-edited HTML copy
- (capability) CAP-2 Pages root is the site landing; Kedro-Viz stays at /kedro-viz/; success: dashboard.yml builds into docs/dashboard/ and remains the only deploy-pages caller
- (capability) CAP-3 local pixi site loop: site, site-check, site-verify, site-serve; success: those tasks exist under feature.site and environment.yaml -e build is byte-identical
- (capability) CAP-4 claim verify is advisory in CI; success: verify_claims.py annotates the Action and continue-on-error is set; local site-verify may exit 1
- (capability) CAP-5 Claude Artifact render has no wrapping html/body; success: build.py --check asserts the artifact shell shape
- (constraint) one Pages deployment; build.py never blanket-rmtree docs/dashboard; infographic sources unmodified except a back-link bar; maintenance label; not Lane 1; not a 44.4 fold
- (decision) non-goals: second deploy-pages workflow, making verify a merge gate, rewriting Kedro-Viz, replacing /console/, rsyncing this tree onto foundry as comments
- (decision) foundry cargo: after 54.5 B re-derives Dream/Frame/Spec and the docsite + feature.site + dashboard.yml build step from this contract; A pins the SHA; same CAP-N
- (decision) status ready, open_questions []; Story 22.1 realises CAP-1..5 in one landing PR from the rebased bundle
- (event by claude) Surface reconcile 2026-09-14: `pixi.toml` changed for two reasons, neither of which touches this Spec's own contract. (1) The `frame-preflight` task description was restated for Frame Spec v0.3 (steward Story 53.6 moved Frame identity from `name` to the qualified-ref `identifier`). (2) `pyforge-station-tests` was REPAIRED: it landed on 2026-09-14 as `set -euo pipefail` plus a bash `for` loop, which pixi's task shell (deno_task_shell) cannot parse, so the task errored on its first token and had never run a single station despite CLAUDE.md documenting it as PR CI gate #3. Replaced with a declarative cross-environment `depends-on` over the eight `pyforge-<station>-test` tasks. Verified by running it: all 8 stations green, exit 0. `environment.yaml` re-exported and byte-identical (description/task-wiring edits only, no dependency change). Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (PR #1355 CI remediation): `pixi.toml` gained nine tasks and `board.py` gained coverage pragmas; neither touches this Spec's contract. (1) Eight `pyforge-<station>-coverage-gate` tasks plus a `pyforge-station-coverage-gates` aggregate now mirror `.github/workflows/coverage-gates.yml`'s `named-module-gates` matrix locally -- that gate measures only the modules a branch TOUCHES, so `pyforge.doctor.sources.board` had sat at 70.1% against an 80% floor since the Guildhall console was retired and only surfaced when an unrelated edit touched the file. (2) A `pr-preflight` aggregate runs the four lanes that actually red a PR (`detectors-ci`, `test-ci`, `pyforge-station-tests`, `pyforge-station-coverage-gates`), because two CI failures on #1355 had no local equivalent at all. (3) `board.py`'s retired-Guildhall machinery is marked `# pragma: no cover` with a block comment stating why -- it cannot execute while `retired-console-check` fails CI on reintroduction; module coverage went 72% -> 94% with no test or behaviour change, tracked for deletion as `DW-DASHBOARD-DEAD-CODE-1`. `environment.yaml` re-exported and byte-identical (task-only edits). Scoped-stamped.
- (event) 2026-09-15 incoming surface (herald Epic 21 recover): pixi.toml gained a deck-trio task only. No product change on this spec. Paths: pixi.toml.
- (event by claude) Co-governed surface moved 2026-09-18 with herald Story 23.5's landing (PR #1463, `f2e9ec9a69 Merge pyforge-herald/23-5 into main`): `.gitignore` gains `docs/dashboard/decks/` beside the existing dossier/infographics/artifact render-output lines, because the family pages (CAP-1/CAP-2 realised by herald 23.5) render into `docs/dashboard/decks/`. One Pages deployment, `dashboard.yml` still the only `deploy-pages` caller. Re-stamped scoped to this spec only.
- (event by claude) Co-governor note 2026-09-18 for marshal Story 50.4's landing (`3e2859425c`): `.gitignore` gained two lines for the headroom MCP wrapper's per-process state (`.claude/.headroom_wrap_marker.json`, `.claude/.headroom_wrap_owners.json`, untracked in the same landing). Nothing pages-facing moved. RECONCILES: .gitignore. Re-stamped scoped to this spec only.
- (event) Surface reconcile 2026-09-20 (co-governor; doctor Story 30.2, `spec-pyforge-doctor:CAP-84`): .gitignore's repo-root Kedro-data rule anchored data/ -> /data/. Found live: the unanchored form matches at ANY depth and was silently swallowing every station's own package data/ dir (pyforge-doctor's new docs-map-schema.json existed on disk, invisible to git add -A, tracked nowhere). No other currently-untracked file surfaced by the anchor fix (verified via git status --short after re-adding). pixi.toml unaffected by this entry (governed separately, reconciled under its own co-governing specs by this same story).
- (event by claude) Surface reconcile 2026-09-20 (steward Story 66.1, spec-pyforge-steward:CAP-153, PR #1553): the one herald file this Spec co-governs reformatted by ruff format; no content change.
