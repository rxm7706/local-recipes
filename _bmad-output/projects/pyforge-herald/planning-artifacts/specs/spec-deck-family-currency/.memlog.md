---
topic: deck-family-currency — every PyForge-branded Infographic standalone.html re-derived to one codified standard from a per-deck fact ledger, pushed byte-exact to its Design project
updated: 2026-09-13T01:45
---

- (event) create: headless express derive from docs/dreams/deck-family-currency.md; slug deck-family-currency; owner-dream herald; mode express because the Dream pre-settles every decision (operator rulings 2026-09-13)
- (note) measured 2026-09-13: 14/15 deck folders carry a standalone poster; 11 are 2026-07-24/25 stubs 14-48 KB, 0 inline SVG, 0 act bands; exemplar-depth: warden 411764 B/18 sections/15 SVG/0 acts, marshal 91340 B/19/3/6 acts, unifying-strategy 128783 B/21/9/6 acts
- (note) measured 2026-09-13: marshal poster quotes bmad-method 6.10.0, bmad-loop 0.9.0, 128/333 fleet-wide, Herald 4/27, Mason 4/48, Doctor 5/18, Scribe 2/13, Steward 3/26; live: BMAD 6.12.0; ledgers marshal 248/249 herald 67/68 mason 66/66 doctor 95/95 scribe 20/20 steward 206/222 atlas 94/95 warden 49/49; fleet 846/865 stories 183/188 epics; 4414 commits on main since 2026-07-24
- (note) measured 2026-09-13 via DesignSync list_files/get_file: Unity Data Stack Infographic head + standalone in Design are the 2026-07-25 six-section stub, body-identical to disk; Warden project file set unchanged since July; herald deck status reports all 15 decks linked:false (no .herald/bridge-state.json; README sections predate canonical shape DW-1-5-1); pyforge-unifying-strategy has no Design project
- (direction) operator 2026-09-13 (1): the ten PyForge-branded decks first (8 stations + genesis + unifying-strategy); the four chain decks (unity, wasm, deckcraft, presenton) later; agentic-sdlc out (BMAD-branded, no standalone in project/)
- (direction) operator 2026-09-13 (2, confirmed after the Unity finding): Unifying Strategy is the structure/acts/length reference; Warden is the density/visual-form reference
- (direction) operator 2026-09-13 (3): the Infographic standalone.html leads this slice for every deck; trio head, Infographic Deck and Executive Summary re-derive in a later lockstep slice
- (direction) operator 2026-09-13 (4): repo-side authoring from the fact ledger, pushed with DesignSync (localPath); no Design-chat authoring and no visual polish in Design this session
- (direction) operator 2026-09-13 (5): tracked facts.yaml per deck, summarized in the deck README ledger
- (direction) operator 2026-09-13 (6): create the pyforge-unifying-strategy Design project during Wave B
- (direction) operator 2026-09-13 (7): four worktree agents per wave, one PR per deck
- (capability) CAP-1 one codified infographic standard (six-act arc with act bands, full-depth section set adapted per subject, inline-diagram floor, 90 KB+ class, source-cited facts; Unifying Strategy = structure, Warden = visual form) checkable per poster; success: infographic-standard.md is the single home, presentation-deck.md's verify checklist points to it, each README ledger records measured values against the floors, a poster below any floor is not marked current
- (capability) CAP-2 a per-deck fact ledger presentations/<slug>/facts.yaml — every count/version/status/date the poster shows has a row (id, value, source, method, derived_at) re-derivable from tracked live sources; success: ten ledgers exist, every fact token in the poster resolves to a row, re-derive on an unchanged tree is byte-identical, re-derive after a ledger change reports the changed rows
- (capability) CAP-3 every PyForge-branded poster rebuilt to the standard from its ledger, repo-side, full depth; success: ten posters meet CAP-1 floors and cite CAP-2 rows, each renders headless to a full-page PNG with no clipped/blank region (render date + page height recorded in the README ledger), each lands via its own PR
- (capability) CAP-4 the Design mirror holds the same bytes and the bridge knows it; success: each poster pushed via DesignSync finalize_plan+write_files(localPath), README ledger records etag + bytes, read-back is byte-identical (get_file <=256 KiB, else MCP render_preview->curl->strip), herald deck status reports the deck linked, unifying-strategy has a Modernist-bound Design project holding its poster
- (capability) CAP-5 poster staleness is detectable: an advisory check compares each poster's fact tokens with a fresh derivation and names stale posters/rows; success: after a tracked ledger changes a shown value the check names that poster and row, clean tree reports clean, exit 0 always, invocable as a pixi task
- (constraint) derive never declare: no number/version/status/date ships without a facts.yaml row; sources are tracked artifacts CI can see (fleet-picture, sprint-status-ledger.yaml, pyproject.toml, _bmad/_config/manifest.yaml, SPEC.md, bmad-groundtruth, SKILL.md frontmatter, cli.py subparsers, pytest --collect-only) — never gitignored Tier-3 files, never a prior poster, never memory; sprint status read with the real parser (parse_sprint_status), never a regex
- (constraint) never restrict size at authoring time (operator standing rule 2026-07-31); the 90 KB+ class is a floor, cutting is a later visual act
- (constraint) repo-side first: author in git, push with DesignSync finalize_plan -> write_files localPath so bytes never relay through model context; no Design-chat authoring or polish in this slice; any later Design edit session ends with a byte-exact pull (deck spec standing rule)
- (constraint) standalone leads — a dated exception to trio lockstep (operator 2026-09-13): each README ledger marks the Infographic head and Infographic Deck 'standalone ahead' until the lockstep slice re-derives them from the same facts.yaml
- (constraint) filename contract unchanged: '<Persona> Infographic standalone.html' at presentations/<slug>/project/ and at the Design project root (herald pull_standalone_bundle derives the remote name from the persona); display brand in content, slug in paths; Modernist tokens inlined in <head>, renders from disk, Google Fonts online with system fallback, inline SVG only, no external scripts
- (constraint) one PR per deck; non-recipe PRs carry the maintenance label; four worktree agents per wave; parallel agents address projects by physical path and never call bmad-switch
- (constraint) the facts check is advisory: exit 0, warn-only, never a PR gate unless a later Spec promotes it (no second gate)
- (constraint) wave order: Wave A = eight station decks (atlas doctor herald marshal mason scribe steward warden); Wave B = genesis + unifying-strategy; unifying-strategy's Design project is created via the claude-design MCP create_project bound to Modernist fbc1d6c8-b35f-4df6-9044-a64d2675427b (DesignSync create_project mints design-system-typed projects only)
- (constraint) posters above 256 KiB verify their Design mirror through the MCP render_preview -> curl -> strip path (DesignSync get_file caps at 256 KiB); marp-derived src/marp/*-infographic-standalone-*.html stay deck-export outputs, untouched
- (decision) non-goals: trio head / Infographic Deck / exec summary re-derivation (lockstep follow-up), the four chain decks and agentic-sdlc, Design-side visual polish, deck prototypes/React decks/pptx exports, a new deck engine, changing deck_export.py semantics, a second PR gate
- (assumption) the claude-design MCP will be reconnected (/mcp) before Wave B project creation and before any >256 KiB read-back; Wave A pushes proceed on DesignSync alone
- (assumption) Warden's Design-side standalone is unchanged since 2026-07-24 (file set unchanged; etag not re-verifiable without the MCP) — the disk copy 411764 B is the visual reference
- (decision) companions: infographic-standard.md (checklist + floors + measured exemplars + markup template), facts-ledger.md (facts.yaml schema + source catalog + derive/check behaviour), deck-inventory.md (14-deck table: Design ids, measured depth, wave)
- (decision) surface: presentations/pyforge-*/project/* Infographic standalone.html, presentations/pyforge-*/facts.yaml, presentations/pyforge-*/README.md, scripts/deck_facts.py, pixi.toml (deck-facts task), docs/specs/presentation-deck.md (allowlisted legacy tier; standard pointer)
- (decision) status ready, open_questions []: every gap the Dream could leave is settled by the seven operator rulings; two assumptions recorded
- (event) self-validate pass 1 (coherence): CAP-1..5 each carry intent+success; intents state WHAT (mechanisms live in Constraints and companions); every constraint rules something out (size floor, source classes, push path, wave order, advisory exit code); non-goals explicit (5); success signal testable (ten posters, ledger etags, deck status linked, check clean then names the first stale poster); ids unique; lean — verdict PASS, no fixes needed
- (event) self-validate pass 2 (preservation): Dream § Why-now rows -> SPEC Why + deck-inventory.md (all 8 rows landed); § What-it-looks-like bullets 1-5 -> CAP-1..5, bullet 6 (later slices) -> Non-goals; § Constraints (8) -> Constraints (9, split wave/MCP); § Realization-log rulings (7 + reference ruling) -> memlog directions + Constraints; kinships -> the binds blockquote (herald HER-1..3, deck-visual-qa CAP-1, presentation-deck.md) — chain-currency-sweep / exemplar-program / pyforge-charter / pptx-deck-generation kept as narrative rationale in the Dream. Wrapper-only content dropped: the pull-quote, kinship prose. Verdict PASS
- (event) spec finalized: status ready, open_questions [], assumptions 2, companions 3 (infographic-standard.md, facts-ledger.md, deck-inventory.md); next act: Epic 20 + ledger rows in pyforge-herald, then Story 20.1/20.2 before any file outside docs/dreams or this folder changes
- (event) Story 20.1 landed (CAP-1 pointer half): docs/specs/presentation-deck.md § Artifact dependency tree paragraph + verify step 5 poster sub-step, and presentations/README.md pointer line, now point at infographic-standard.md and name the floors; the standard itself unchanged; governed-file change accounted for here
- (decision) surface gains presentations/README.md: the family index carries this Spec's pointer line (Story 20.1) and is otherwise governed only by spec-pyforge-herald's presentations/** — claiming it here keeps the kernel spec's memlog out of pointer-line churn (spec-surface drift finding 2026-09-13)
