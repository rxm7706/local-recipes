---
topic: design-sync-loop — one command keeps every Design project's local twin and its whole deck family true: enumerate, pull (Design wins), refresh from the ledger, derive, push, prove, publish
updated: 2026-09-14T16:55
---

- (event) Create 2026-09-14 from docs/dreams/design-sync-loop.md (owner herald, dreamt). Headless, express mode: input is rich — the Dream carries the operator's four requirements verbatim, measured state of main at 168bbedb13, and the four open questions answered by the operator the same hour — so the contract derives at ready with no elicitation.
- (direction) Operator's four requirements, verbatim intent: (1) every Design project available locally in presentations/; (2) standalone HTML posters synced with Claude Design edits winning over local copies, then refreshed to the latest facts and repo state; (3) the remaining PowerPoint decks and Marp content (the deck family) in each Design project refreshed; (4) synced posters and PowerPoints browsable and downloadable on GitHub Pages.
- (decision) Scope ruling (operator, Q1): presentations AND design systems. Every presentation project gets a presentations/<slug>/ twin with a registry section — the 14 decks, agentic-sdlc (unregistered today), and new twins for 'PyForge six-quarter roadmap' and 'LLM Knowledge Bases'. The three design systems (Modernist, Broadsheet, Nocturne) are mirrored too, as libraries not decks: no poster, ledger or Pages page. 'REMOVED-PyForge Unifying Strategy' and 'Local recipes repository connection' are excluded BY NAME with the reason, listed as skipped never silently ignored.
- (decision) Conflict ruling (operator, Q2): Design wins, then the ledger re-applies. Pull wholesale every prototype/poster whose Design etag moved since the last pull (human visual/prose edits survive); then deck-facts --refresh rewrites every MARKED literal from the tree-derived ledger; the run report names every literal it overrode so an override is visible, never silent.
- (decision) PowerPoint ruling (operator, Q3): both, per deck. Default: Marp sources refreshed from the ledger, deck-export regenerates PPTX in-repo (marp CLI + Chrome on the loop's host). A deck that declares a .potx template is filled via pptx-spec/pptx-fill (Story 15.1) instead. The registry section records which path; both push to Design and both publish.
- (decision) Pages + cadence ruling (operator, Q4): extend the existing dossier site (spec-pyforge-pages; one Pages deployment, dashboard.yml, pgs:CAP-2) with a family page per deck — poster / Infographic Deck / Executive Summary to view, PowerPoints + Marp to download, each stamped with the Design etag and tree it derived at — plus an index. The loop runs on demand from an operator session (herald deck sync-all; herald deck watch for the pull half); unattended runs out of scope until a runner can hold a Design credential.
- (constraint) Git is the archive of record and the source of facts; Design is the editing surface. 'Design wins' governs prototype content only — facts.yaml is derived from the tree and is never pulled.
- (constraint) Binds, never re-mints: the loop sequences shipped mechanics — deck-facts (currency CAP-2/5/6), deck-export, herald deck seed/pull/push/status/watch (kernel), DesignSync finalize_plan → write_files (localPath), the CAP-4 read-back recipe, docsite/build.py (pages CAP-1) — and calls Epic 21's derivations (21.1–21.5, 21.10) where they exist rather than competing with them.
- (constraint) One Pages deployment (dashboard.yml); Kedro-Viz stays at /kedro-viz/ (pgs:CAP-2).
- (constraint) Every Design read/write rides the operator's claude.ai login; no runner credential exists — the loop is session-run, and must be idempotent because it will be re-run by hand.
- (constraint) Measure the artifact, never the container: a Design bundle (~205 KB of fonts + runtime) is not the poster; byte comparisons strip the injected harness (memory 2026-09-13).
- (capability) CAP-1 — the account is enumerated and reconciled: intent = every Design project the login can see is classified (presentation / design system / excluded-by-name) and mapped to a local twin and a registry section, or listed as excluded with a reason; success = herald deck status lists every project the account returns (paging past 20), presentations report linked, design systems report mirrored, the two retired projects report excluded with their recorded reason, and no project is absent from the report.
- (capability) CAP-2 — every presentation has a local twin: intent = a Design project that is a presentation exists under presentations/<slug>/ with its prototype pulled and a machine-owned registry section; success = the 14 decks, agentic-sdlc, six-quarter-roadmap and llm-knowledge-bases each resolve through registry.read; the three design systems live at the home the Spec fixes (presentations/_design-systems/<name>/) pulled byte-exact; a second run pulls nothing.
- (capability) CAP-3 — pull with Design winning: intent = for every twin whose Design etag moved since the last recorded pull, the loop takes Design's bytes wholesale; success = after a human edit in Design, one run leaves the repo copy byte-identical to Design (harness stripped) and records the new etag; a repo-side edit that Design has not seen is overwritten and reported, never merged.
- (capability) CAP-4 — refresh re-applies the ledger over the pulled surfaces: intent = after the pull, every marked literal on every fact-bearing surface (standalone poster first; trio, executive summary and Marp as Epic 21.3/21.5 make them markable) is rewritten from a facts.yaml re-derived at the current tree; success = deck-facts --check reads 0 drifted / 0 mismatch for every deck after a run, and the run report lists each literal the ledger overrode with its Design value.
- (capability) CAP-5 — the family derives and the PowerPoints regenerate: intent = the Infographic head, Infographic Deck, Executive Summary, Marp sources and PPTX for each deck are re-derived from the refreshed standalone and ledger; success = the trio derives via deck-trio (21.1/21.2), the export set via deck-export (Marp path) or pptx-fill (declared .potx path) per the registry, and every derived file's stamp names the tree and etag it derived at.
- (capability) CAP-6 — push what changed and prove the mirror: intent = only artifacts whose bytes changed are pushed to their Design project, and every pushed artifact is read back; success = read-back byte-identical for 100% of pushed files, the Design etag recorded in the README ledger and bridge state, and a second run pushes nothing.
- (capability) CAP-7 — the family is browsable and downloadable on Pages: intent = the dossier site gains one family page per deck and an index; success = for every registered deck, the family page shows the poster, Infographic Deck and Executive Summary in view, offers the PPTX(s) and Marp sources as downloads, stamps each with etag + tree, and is built by docsite/build.py in the one existing Pages deployment; site-check passes.
- (capability) CAP-8 — one command, idempotent, reported: intent = herald deck sync-all runs CAP-1..7 in order for every deck (or one --slug) and prints a per-deck report (pulled / overrode / derived / pushed / published / unchanged); success = running it twice in a row makes the second run report every deck unchanged with zero writes to git or Design, and a --dry-run prints the same report without writing.
- (assumption) The design systems' home is presentations/_design-systems/<name>/ — a sibling of the deck twins that no deck glob (presentations/pyforge-*/…, the four chain decks) matches, so no currency or Pages surface picks them up. If the Spec's downstream prefers a sibling top-level dir, the change is a one-line surface edit.
- (assumption) 'Every project the account returns' means paging past list_projects' 20-item first page; Scribe and Steward were absent from the first page on 2026-09-14.
- (assumption) PPTX regeneration needs Chrome on the loop's host (marp CLI); the loop reports a deck 'derive-skipped: no chrome' rather than failing the whole run.
- (note) Non-goals recorded: authoring new decks; changing the infographic standard; redesigning any poster; unattended/CI runs; a second Pages deployment; three-way merging of Design and repo edits (ruled out by Q2).
- (note) Wrapper-only content dropped from the Dream (Spec Law 7 — on the record, not silent): the 2026-09-14 sweep's step counts (nine prompts / nine pushes / nine read-backs) — narrative that motivated the Dream, carries no downstream decision; the list_projects page contents beyond the classification the Spec needs.
- (event) Self-validate pass 1 (coherence, rules 1-6 and 8): PASS. Eight capabilities each carry intent + success; intents are WHAT (mechanics named only in Constraints as the things bound to); every constraint rules something out (facts never pulled; one deployment; session-run; artifact-not-container); non-goals explicit; success signal is a demonstrable two-run property; IDs CAP-1..8 fresh; prose lean.
- (event) Self-validate pass 2 (preservation): PASS. Every load-bearing Dream claim landed — the four requirements (CAP-2/3/4, CAP-5, CAP-7), the four rulings (Constraints + CAP-1/4/5/7/8 success lines), the six measured facts under What is real (CAP-1 paging, registry 10/15, repo-side-first inversion, no family refresh path, Pages posters-only, no runner credential), the Kinships (Constraints bind-never-re-mint). Two wrapper-only drops logged above.
- (event) Spec finalized 2026-09-14 at status ready: SPEC.md rendered from this log (CAP-1..8, six constraints, five non-goals, two-run success signal, three assumptions, open_questions []). Companions: none — no catalog, diagram or long-form reference outgrew the kernel. Next: decompose into herald Epic 23 (one story per CAP) and the ledger before any code moves.
