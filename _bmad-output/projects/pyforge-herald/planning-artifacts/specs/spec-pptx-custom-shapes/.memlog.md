---
topic: pptx-custom-shapes — parked with trigger
updated: 2026-09-09T15:51
---

- (event) Parked spec authored 2026-08-22 (operator: get all seeds ready-to-implement): the Dream's own constraints forbid building until the trigger fires; zero stories minted — parking IS the decomposition decision
- (decision) UNPARKED with its parent 2026-08-22 (operator audience decision); decomposed as herald Epic 15 story 15.2, deps 15.1
- (note by claude) 2026-09-09 BUILT, UNEXERCISED — recorded under the realization gate (fleet-readiness-decision-batch-2026-09-09 § 2.3 C6). Story 15.2 shipped `add_card` / `add_metric_box` / `add_table` / `add_section_label` plus `fit_text`'s Pillow `ImageFont` autofit inside `pptx_pipeline.py`, reached from `content_plan.json`'s optional `"shapes"` list. No dense slide has been run through it — no `content_plan.json` exists anywhere in the tree — so the Dream's "densest six-act appendix slide" signal is unexercised. Vessel: herald Epic 19 Story 19.4 (the same real-deck render that exercises its parent). DW-FU-15-1-2 (`_resolve_layout` first-match on duplicate layout names) stays unreachable with the stock template's 11 distinct names and becomes live only when a branded `.potx` lands.
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. CAP-1 preserved; status stays ready; realization status 'built, unexercised' recorded under Success signal with vessel Epic 19 Story 19.4; DW-FU-15-1-2's reachability precondition recorded as an Assumption.
