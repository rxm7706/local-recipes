# pyforge-herald — planning artifacts

**Herald** — the factory's outward voice and design surface: pitch decks
(`herald deck`), and the Four Moments dashboard (Progress/Success/
Operations notices, CLI-triggered, local-storage). dist `pyforge-herald` /
module `pyforge.herald` / CLI `herald`.

BMAD Tier-2 output for this project (`research/`, `briefs/`, `prds/`,
`architecture/`, `epics.md`, `specs/`).
