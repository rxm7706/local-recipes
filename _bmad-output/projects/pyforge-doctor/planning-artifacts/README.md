# pyforge-doctor — planning artifacts

**Doctor** — ecosystem health & diagnostics CLI (`doctor check/monitor/diagnose`):
pre-flight toolchain verification, continuous fleet/feedstock pulse, ordered
prescriptions. Consolidates the atlas health/watch CLIs and Warden's
self-check. dist `pyforge-doctor` / module `pyforge.doctor` / CLI `doctor`.

BMAD Tier-2 output for this project (`research/`, `briefs/`, `prds/`,
`architecture/`, `epics.md`, `specs/`).
