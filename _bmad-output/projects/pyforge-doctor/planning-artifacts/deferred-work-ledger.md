---
doc_type: deferred-work-ledger
project: pyforge-doctor
date: 2026-07-29
status: promoted-verbatim
---

# pyforge-doctor — deferred-work ledger (TRACKED)

**Promoted verbatim from Tier-3 on 2026-07-29 to make it durable.**

`implementation-artifacts/deferred-work.md` is **gitignored**: it does not survive a
clone or a bmad-loop worktree teardown, and this repo has already lost data that way
(pyforge-atlas's live ledger is still truncated to 11 of 64 entries, collateral of the
2026-07-19 copy failure). Until today this project had **no tracked ledger at all**, so
its entire deferred-work record — 6 KB — existed only in
scratch space. Found by `scripts/deferred_work_check.py`.

**This is a COPY, not a curation.** Bodies are unedited; nothing has been given a
resolution, re-severitied, or reconciled against what has since shipped. Treat entry
*status* fields as of their authoring date, not as current. The one intentional edit is
id renaming, below.

Durability first; curation is owned follow-up work.

---

# Deferred Work

## DW-1-1-1 — The loop's exact `[verify]` command (`pixi run -e pyforge-doctor pyforge-doctor-test`, unfrozen)…

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-package-scaffold-frozen-finding-doctorreport-contract-exit-code-module.md`
  summary: The loop's exact `[verify]` command (`pixi run -e pyforge-doctor pyforge-doctor-test`, unfrozen) fails environmentally in every bmad-loop worktree — pixi-build-python 0.8.3 panics (`tools.rs:461` byte-index out-of-bounds) when the build `workDirectory` exceeds ~250 chars (this run's worktree root is 204 chars, over the threshold once nested build-metadata paths are appended). This is the same failure signature already recorded for `pyforge-warden`'s Story 1.1 (`_bmad-output/projects/pyforge-warden/implementation-artifacts/deferred-work.md`), with one added wrinkle: `pyforge-doctor` is a brand-new environment with zero existing `pixi.lock` entries, so `pixi run --frozen -e pyforge-doctor …` (the fix that worked for warden, which already had a solved lock) cannot bootstrap it either — `--frozen` refuses to add missing entries. The environment needs one successful *unfrozen* solve from a short-path checkout (e.g. the main `local-recipes` checkout, not a `.bmad-loop/runs/**/worktrees/**` path) to populate `pixi.lock` with `pyforge-doctor`'s entries; once committed, `--frozen` will work in future worktrees the same way it does for `pyforge-warden`/`pyforge-atlas` today. Recommend: (1) run `pixi install -e pyforge-doctor` (or any `pixi run -e pyforge-doctor …`) once from the main checkout to solve + commit the lock, then (2) switch `.bmad-loop/policy.toml` `[verify]` to `pixi run --frozen -e pyforge-doctor pyforge-doctor-test`.
  evidence: Reproduced on the untouched sibling `pyforge-warden` environment in this same worktree (`pixi run -e pyforge-warden pyforge-warden-test` → identical "the build backend (pixi-build-python) exited prematurely" panic), proving the failure is pre-existing/environmental, not caused by this story's changes — confirmed via `git status`/`git diff --stat` showing only `pixi.toml` (39 lines, no `pixi.lock` churn) and the new untracked `src/shared/packages/pyforge-doctor/` package. Story verification instead used three independent substitutes, all passing: `PYTHONPATH=src python3 -m pytest tests -q` → 35/35 passed; `python -m build --no-isolation --wheel` → clean wheel containing `data/report-schema.json`, empty `__init__.py`, and an `entry_points.txt` with `doctor = pyforge.doctor.__main__:main`; installing that wheel into a fresh venv and running the real console script (`doctor --version`/`--help`/no-args all exit 0 with correct output, `doctor --bogus` exits 2, no tracebacks).

  status: done 2026-07-30

  verified: 2026-07-30 — ALREADY RESOLVED — both halves of this entry's own two-step prescription have since landed. (1) The lock is bootstrapped: `pixi.lock:8279` carries a `pyforge-doctor:` environment block and three `conda_source: pyforge-doctor[…] @ src/shared/packages/pyforge-doctor` entries (`:8330`, `:8371`, `:8413`), so `--frozen` no longer has missing entries to refuse. (2) The verify command is frozen: `planning-artifacts/marshal-policy.toml:35` declares `"pixi run --frozen -e pyforge-doctor pyforge-doctor-test"`, and the live home `~/.bmad-loops/pyforge-doctor/.bmad-loop/policy.toml:32` carries the identical command. Resolved as a side effect of the Story 1.10 policy-rendering work (PR #139) plus the lock solve, which is why nobody marked it — the same pattern as warden's DW-1-1-1.

  verified: 2026-09-02 — done — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/3 present (absent: _bmad-output/implementation-artifacts/spec-1-1-package-scaffold-frozen-finding-doctorreport-contract-exit-code-module.md, _bmad-output/projects/pyforge-warden/implementation-artifacts/deferred-work.md); ledger status mapped to done; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

## DW-1-1-2 — The team's own auto-memory (`project_bmad_loop_worktree_path_length_limit.md`, updated 2026-07-2…

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-package-scaffold-frozen-finding-doctorreport-contract-exit-code-module.md`
  summary: The team's own auto-memory (`project_bmad_loop_worktree_path_length_limit.md`, updated 2026-07-25 by a concurrent pyforge-herald session) confirms bmad-loop's orchestrator classifies the above pixi-build-python panic as a *code* failure (not `env_fault`, since its return code isn't 126/127) and, with `scm.rollback_on_failure = true`, resets the story branch to `baseline_commit` — discarding a finished, reviewed, green story. This worktree's root path is 239 bytes, well over the ~173-byte panic threshold, so this story's own post-session `pixi run --frozen -e pyforge-doctor pyforge-doctor-test` re-check is expected to hit the identical panic and may trigger the same rollback a third time (attempts 1 and 2 of this exact story were already lost this way — see the Spec Change Log above). Recommend fixing `bmad_loop.verify.ENV_FAULT_RCS`/`verify_commands_outcome` (or the panic's root cause upstream in pixi-build-backends) so this whole panic signature is treated as `env_fault` rather than a per-story code failure.
  evidence: This story's own two prior attempts are direct evidence (dev pass 1 fully implemented + verified 35/35, per the note above, yet the worktree was clean-at-baseline with `sprint-status.yaml` still `backlog` when attempt 3 started — no commit, no `final_revision`, nothing to recover). Cross-confirmed independently by pyforge-herald story 1.2 hitting the same panic class the same day (per the cited memory). If this session's commit is likewise rolled back, recovery is cheap and already on record: `git merge --ff-only <final_revision>` (this commit's parent is the baseline) — see this spec's frontmatter `final_revision` once step 4 commits.

  status: open

  verified: 2026-07-30 — CONFIRMED STILL OPEN, and now locatable to the line. bmad-loop has moved 0.8.1 → **0.9.0** since this entry was written, and the classification is unchanged: `bmad_loop/verify.py:1474` still reads `ENV_FAULT_RCS = frozenset({126, 127})`, and `:1508` still gates solely on `if result.returncode in ENV_FAULT_RCS`. The pixi-build-python panic returns neither, so it is still scored a *code* failure and still triggers the rollback. Worth recording as intent rather than oversight: the comment at `:1469-1471` deliberately scopes the set to the sh-launcher convention (126 = found-but-not-executable, 127 = not-found), so widening it is a design change upstream, not a bug fix. Note the TRIGGER is now largely mitigated while the CLASSIFICATION is not — the fleet's homes moved to `~/.bmad-loops/` (worst-case story worktree 157–159 chars, under the ~173 panic threshold), so the panic rarely fires; any future long-path home would still lose a finished story.

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-1-1-package-scaffold-frozen-finding-doctorreport-contract-exit-code-module.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

## DW-1-1-3 — Three uncoordinated version constraints exist for the same `hatchling` build backend across the…

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-package-scaffold-frozen-finding-doctorreport-contract-exit-code-module.md`
  summary: Three uncoordinated version constraints exist for the same `hatchling` build backend across the whole `pyforge-*` package family (unconstrained in each package's `pyproject.toml [build-system] requires`, `>=1.31.0` in the root `pixi.toml`'s `[feature.pyforge-<pkg>.dependencies]`, and `"*"` in each package's own `pixi.toml [package.host-dependencies]`) — nothing ties these together, so the conda build and the wheel/sdist build can silently resolve to different hatchling versions.
  evidence: Pre-existing in `pyforge-warden`'s own three files (identical shape), faithfully mirrored — not introduced by this story. Cross-cutting across `pyforge-warden`/`pyforge-atlas`/`pyforge-doctor`; a fix belongs in a shared follow-up touching all three, not a one-off deviation in this story's scaffold (which is explicitly mandated to mirror warden's shape exactly).

  status: open

  verified: 2026-07-30 — CONFIRMED STILL OPEN, and WIDER than recorded — the entry described three packages (warden/atlas/doctor); the family is now **eight**. All three uncoordinated constraints are intact and measured: (a) unconstrained in every package's `pyproject.toml:3` — `requires = ["hatchling"]` in all 8 of atlas, doctor, herald, marshal, mason, scribe, steward, warden; (b) `hatchling = ">=1.31.0"` at 6 sites in the root `pixi.toml` (`:135`, `:164`, `:199`, `:267`, `:878`, `:1323`); (c) `hatchling = "*"` in all 8 packages' own `pixi.toml` `[package.host-dependencies]` (atlas `:23`, steward `:21`, warden `:24`, doctor `:25`, mason `:23`, herald `:25`, marshal `:26`, scribe `:25`). Nothing ties the three together, so the conda build and the wheel/sdist build can still resolve different hatchling versions.

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-1-1-package-scaffold-frozen-finding-doctorreport-contract-exit-code-module.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

## DW-1-1-4 — The AD-2 sole-ownership meta-test's AST exit-literal detector (mirroring `pyforge-warden/tests/m…

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-package-scaffold-frozen-finding-doctorreport-contract-exit-code-module.md`
  summary: The AD-2 sole-ownership meta-test's AST exit-literal detector (mirroring `pyforge-warden/tests/meta/test_verdict_sole_ownership.py`'s technique) only matches `ast.Call` nodes, so a bare `raise SystemExit` (or `raise SystemExit(2)` is caught, but a bare `raise SystemExit` with no parens/args is an `ast.Raise` wrapping a plain `Name`, never a `Call`) would evade the guard.
  evidence: Confirmed by direct inspection of the detector's `_is_exit_callable`/`_exit_literal_violations` logic (both here and in the exemplar `pyforge-warden` file it mirrors) — this is an inherited limitation of the exemplar technique itself, not something introduced by this story's implementation; the exemplar's own docstring already states "this is a best-effort STATIC check," so fixing it is a shared cross-package hardening task, not this story's problem.
  status: open

  verified: 2026-07-30 — CONFIRMED STILL OPEN — and the entry's parenthetical is exactly right, proven by execution rather than reading. `tests/meta/test_verdict_sole_ownership.py:151` still opens with `if not isinstance(node, ast.Call)`, so any non-`Call` node never reaches `_is_exit_callable`. Ran the detector directly against four synthetic sources: `raise SystemExit(2)` → `[2]` (caught), `sys.exit(2)` → `[3]` (caught), `e = SystemExit(2); raise e` → `[2]` (caught), and bare `raise SystemExit` → **`[]` — evades**. This is not a harmless gap: a bare `raise SystemExit` exits with code 0, and `GUARDED_EXIT_LITERALS` is `frozenset({0, 2, 130})`, so it slips a genuinely guarded literal past the gate. Still an inherited exemplar limitation shared with warden, whose copy carries the identical `ast.Call` gate at `pyforge-warden/tests/meta/test_verdict_sole_ownership.py:154` — so a fix belongs in both.

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-1-1-package-scaffold-frozen-finding-doctorreport-contract-exit-code-module.md, tests/meta/test_verdict_sole_ownership.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4: Follow-up review still recommended for 6-4-the-ledger-verdicts-come-home after the damping cap was spent

- source_spec: `spec-6-4-the-ledger-verdicts-come-home.md`
  summary: Follow-up review still recommended for 6-4-the-ledger-verdicts-come-home after the damping cap was spent
  evidence: The follow-up-review damping cap (limits.max_followup_reviews = 2) was spent with the story finalized (status: done, verify green) while the review pass still recommended an independent follow-up. The work was committed by bmad-loop run 20260808-210255-c88b; this entry preserves the lingering recommendation for a deliberate later review.
  promoted: 2026-08-09 — promoted from Tier-3 `implementation-artifacts/deferred-work.md` (id `DW-1` there) under the ledger's `DW-FU-<story>` convention, so the next damped story cannot collide with a generic `DW-1`. This promotion is the manual act Marshal FR-175 / Story 4.13 exists to make an obligation of the story itself.
  severity: low
  status: open

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-5: Follow-up review still recommended for 6-5-the-board-verdicts-come-home after the damping cap was spent

- source_spec: `spec-6-5-the-board-verdicts-come-home.md`
  summary: Follow-up review still recommended for 6-5-the-board-verdicts-come-home after the damping cap was spent
  evidence: The follow-up-review damping cap (limits.max_followup_reviews = 2) was spent with the story finalized (status: done, verify green) while the review pass still recommended an independent follow-up. The work was committed by bmad-loop run 20260808-210255-c88b; this entry preserves the lingering recommendation for a deliberate later review.
  promoted: 2026-08-09 — promoted from Tier-3 `implementation-artifacts/deferred-work.md` (id `DW-2` there) under the ledger's `DW-FU-<story>` convention, so the next damped story cannot collide with a generic `DW-2`. This promotion is the manual act Marshal FR-175 / Story 4.13 exists to make an obligation of the story itself.
  severity: low
  status: open

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-6: Follow-up review still recommended for 6-6-the-chain-verdicts-come-home after the damping cap was spent

- source_spec: `spec-6-6-the-chain-verdicts-come-home.md`
  summary: Follow-up review still recommended for 6-6-the-chain-verdicts-come-home after the damping cap was spent
  evidence: The follow-up-review damping cap (limits.max_followup_reviews = 2) was spent with the story finalized (status: done, verify green) while the review pass still recommended an independent follow-up. The work was committed by bmad-loop run 20260808-210255-c88b; this entry preserves the lingering recommendation for a deliberate later review.
  promoted: 2026-08-09 — promoted from Tier-3 (id `DW-3` there) under the ledger's
    `DW-FU-<story>` convention, at the wind-down landing of this run. Marshal FR-175 /
    Story 4.13 exists to make this promotion an obligation of the story rather than
    archaeology someone performs later.
  severity: low
  status: open

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-8: Follow-up review still recommended for 6-8-bmad_drift-comes-home-without-breaking-the-board after the damping cap was spent

- source_spec: `spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md`
  summary: Follow-up review still recommended for 6-8-bmad_drift-comes-home-without-breaking-the-board after the damping cap was spent
  evidence: The follow-up-review damping cap (limits.max_followup_reviews = 2) was spent with the
    story finalized (status: done, verify green) while the review pass still recommended an
    independent follow-up. The work was committed by bmad-loop run 20260809-195207-7471; this
    entry preserves the lingering recommendation for a deliberate later review.
  context: this story ran to every ceiling at once — dev attempt 2/2, review cycle 3/3,
    follow-ups 2/2 — and cleared its LAST review rather than escalating. It is also the
    largest single deliverable in Epic 6 (`sources/factory.py`, 1,197 lines, porting
    `scripts/bmad_drift_check.py`'s judgements), which is why an independent follow-up is
    worth more here than on a routine story.
  promoted: 2026-08-09 — promoted from Tier-3 `implementation-artifacts/deferred-work.md`
    (id `DW-4` there) under the ledger's `DW-FU-<story>` convention, so the next damped story
    cannot collide with a generic `DW-4`. Same act as DW-FU-6-5 / DW-FU-6-6; Marshal FR-175 /
    Story 4.13 exists to make this promotion an obligation of the story rather than
    archaeology someone performs later.
  severity: low
  status: open

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; cited paths 1/1 present; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-7-1: The Review Triage Log's `addressed_findings` never itemizes `defer` entries by the id they were just minted
- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md`
  summary: The Review Triage Log's `addressed_findings` never itemizes `defer` entries by the id they were just minted, unlike `patch`/`bad_spec`, so a review pass and the DW id(s) it produced aren't linked anywhere in the spec file itself.
  evidence: Found by review pass 1 (Blind Hunter, independent adversarial pass on this story's own diff). Confirmed by inspection of `step-04-review.md`'s Classify section (step 4): the triage-log template records only `intent_gap`/`bad_spec`/`patch`/`defer`/`reject` counts plus a free-text `addressed_findings` list, and only the `patch`/`bad_spec` triage branches (step 5) actually instruct listing specifics under `addressed_findings` — the `defer` branch never did, before or after this story's edit. Pre-existing (not introduced by this story's change to the `defer` bullet itself), but now more valuable to close since `defer` entries carry real, citable ids going forward. Deferred rather than patched in this pass: fixing it means extending the Classify section's shared triage-log format and step 5's `defer` branch — a change to a different part of the file than this story's own scoped edit — and deserves its own focused pass.
  severity: medium
  status: open
  promoted: 2026-08-11 (landing pass for doctor 7-1)

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-7-1-2: The `DW-FU-{story}` shape this story mints for non-mason stations already has an established, different meaning
- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md`
  summary: The `DW-FU-{story}` id shape this story mints for every non-mason station's generic `defer` findings is the exact same shape the pre-existing "follow-up review still recommended" promotion mechanism already uses exclusively, under a different field schema (`origin`/`severity`/`reason`/`status` there vs. `source_spec`/`summary`/`evidence` here) — collision-safe by suffix, but no longer distinguishable by id prefix alone.
  evidence: Found by review pass 1 (Blind Hunter). Confirmed by inspection of all four `### DW-FU-*` entries currently in `_bmad-output/planning-artifacts/deferred-work-ledger.md` (`DW-FU-6-4`, `DW-FU-6-5`, `DW-FU-6-6`, `DW-FU-6-8`) — every one carries `summary: Follow-up review still recommended for ...` and a `promoted:` annotation; none is a generic defer. This story's own convention (`DW-FU-{story}` for doctor/atlas/marshal/warden) is taken literally from the epic/SPEC text (`_bmad-output/planning-artifacts/specs/spec-deferred-work-visibility/SPEC.md`'s Constraints section), which does not address this overlap. Not patched in this pass: the epic's convention is explicit and was operator-confirmed the same day as this story, so resolving the overlap (e.g. giving generic defers a distinct discriminator) is a deliberate planning-level decision above this story's remit, not something an unattended pass should silently override.
  severity: medium
  status: open
  promoted: 2026-08-11 (landing pass for doctor 7-1)

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/3 present (absent: _bmad-output/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md, _bmad-output/planning-artifacts/deferred-work-ledger.md, _bmad-output/planning-artifacts/specs/spec-deferred-work-visibility/SPEC.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-7-1-3: `bmad-loop-sweep`'s canonical deferred-work format mandates a different id scheme and a dedupe check for the very file this emitter now mints station-convention ids into
- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md`
  summary: `.claude/skills/bmad-loop-sweep/deferred-work-format.md` declares itself the "Canonical entry format for `{implementation_artifacts}/deferred-work.md`" and mandates two rules the `step-04-review.md` defer bullet now contradicts: ids "numbered sequentially (`DW-1`, `DW-2`, …) by scanning the file for the highest existing number", and a mandatory "Before appending: dedupe check" that adds a `seen-again:` line to an existing entry rather than appending a duplicate. The defer bullet mints `DW-FU-<story>` / `DW-<story>-<n>` instead, and explicitly instructs the opposite on dedup ("Do not modify existing entries or look for duplicate content"). The two mechanisms need reconciling at planning level.
  evidence: Found by review pass 2 (Blind Hunter, independent adversarial pass). Confirmed by direct inspection of `deferred-work-format.md` lines 1-35: it names this exact file, explicitly covers "the inner dev path the bmad-dev-auto session appends its own flat entries (review defers, multi-goal splits, token splits)", and states both rules verbatim. Partially defused by that same doc — it says "the orchestrator owns the ledger and normalizes those flat entries into this canonical form on sweep", so a flat, non-canonical entry from bmad-dev-auto is anticipated by design; and `chain.py`'s `_GENERIC_RE = ^DW-\d+$` already flags sequential `DW-<n>` ids with the hint "Rename it to the ledger's DW-<story>-<n> convention on promotion, or the next damped story collides with it", so the fleet already treats sequential numbering as the shape needing correction. The residual, genuinely unreconciled half is the sweep's "highest existing number" scan, which is undefined in a file now holding both `DW-1`..`DW-4` and `DW-FU-7-1`..`DW-FU-7-1-4`, plus the flat contradiction on whether a dedupe check is required. Not patched in this pass: Story 7.1's Never clause explicitly scopes ledger-format normalization out ("that ledger normalization is a separate, later mechanism (`deferred-work-format.md`), not this story's job"), and changing either mechanism means deciding which document is authoritative for this file — a planning-level call above an unattended review pass.
  severity: medium
  status: open
  promoted: 2026-08-11 (landing pass, doctor 7-1 review)

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-7-1-4: Three stations fall into the emitter's "every other station" default whose tracked ledgers use mason's shape exclusively, and atlas uses neither shape
- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md`
  summary: CAP-1 assigns `DW-FU-<story>` to doctor/atlas/marshal/warden and `DW-<story>-<n>` to mason, naming five of the fleet's eight stations. The emitter's "every other station, including unknown" branch therefore silently imposes `DW-FU-<story>` on scribe, steward and herald — all three of whose tracked ledgers use mason's `DW-<story>-<n>` shape essentially exclusively. Atlas is a separate mismatch: it is explicitly assigned `DW-FU-<story>` but its 44 tracked entries use scope-coded shapes — the `DW-` prefix followed by a letter-coded scope (`B`, `D`, `F`, `G`, `H`) plus numbers — and not one uses the `FU` scope.
  evidence: Found by review pass 2 (Edge Case Hunter), then measured directly across all eight stations' `planning-artifacts/deferred-work-ledger.md`. Counts by normalized heading shape — scribe: 7/7 `DW-<n>-<n>-<n>`; steward: 23/23 `DW-<n>-<n>-<n>`; herald: 29/30 `DW-<n>-<n>-<n>` (1 `DW-<n>`); mason: 5/5 `DW-<n>-<n>-<n>`; warden: 39 `DW-<n>-<n>-<n>` vs 2 `DW-FU-<n>-<n>`; marshal: 39 `DW-<n>-<n>-<n>` vs 8 `DW-FU-<n>-<n>`; doctor: 5 `DW-FU-<n>-<n>` vs 4 `DW-<n>-<n>-<n>`; atlas: 0 `DW-FU-` of 44. So the shape the emitter defaults three unassigned stations onto is the one none of them has ever used, and for warden/marshal the assigned shape is the minority of their own precedent. Distinct from `DW-FU-7-1-2`, which is about `DW-FU-<story>` colliding with the *follow-up-review promotion* meaning rather than about which stations get which shape. Not patched in this pass: Story 7.1's Never clause forbids normalizing one station's shape onto another's, CAP-1's mapping is explicit and was operator-confirmed the same day, and extending the mapping to the three unnamed stations is a planning-level decision for the epic, not something an unattended review pass should decide.
  severity: medium
  status: open
  promoted: 2026-08-11 (landing pass, doctor 7-1 review)

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-7-1-5: Minting an id converts every new Tier-3 defer into a hard `tier3-only-deferral` FAIL of the always-on deferred-work gate, and no automatic promoter recognizes the minted shape
- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md`
  summary: Before Story 7.1 an anonymous defer bullet carried no id, so the deferred-work check never saw it. Now every minted entry is an id present in the Tier-3 file and absent from the tracked ledger, which is exactly the `tier3-only-deferral` FAIL condition -- so each defer reds an always-on gate until a human promotes it by hand. Nothing in the fleet promotes this shape automatically.
  evidence: Found by review pass 3 (Blind Hunter and Edge Case Hunter independently), then measured. `pyforge/doctor/sources/chain.py::_check_project_deferred_work` FAILs on every id in `_ids(tier3) - _ids(tracked)`. Running that logic against doctor right now returns exactly two such ids -- `DW-FU-7-1-3` and `DW-FU-7-1-4`, both minted by this story's own pass 2 (Blind Hunter reported four; the earlier two had already been promoted to the tracked ledger by hand, so its count was wrong). The auto-promoter in `pyforge/marshal/core/deferred_work.py` cannot close the gap: its `_HEADING_RE` is `^### (DW-\d+): ` (digits only) and `parse_followup_deferrals` additionally requires all five of `origin`/`source_spec`/`severity`/`reason`/`status` with `origin: review-budget-followup`, none of which the minted three-field entry has. Not patched in this pass: Story 7.1's Never clause explicitly scopes out the tracked ledger, the promotion mechanism, and the detector (Stories 7.2/7.3), so deciding whether minted defers should auto-promote, be exempted by the detector, or stay a deliberate manual gate is a planning-level call for the epic. Distinct from `DW-FU-7-1-3`, which concerns the sweep format's id-scheme and dedupe contradiction rather than the detector gate and the absent promoter.
  severity: medium
  status: open
  promoted: 2026-08-11 (landing pass, doctor 7-1 review)

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-7-1-6: The minted entry carries no `status:`, and giving it an id heading makes it look already-canonical to the one mechanism that would have supplied one
- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md`
  summary: `bmad-loop-sweep` defines an open deferral as an id'd block whose `status:` line reads `open`, so a three-field `source_spec`/`summary`/`evidence` entry is invisible to sweep triage no matter how well-formed its id is. Worse, the sweep's stated remedy is to normalize *flat* (un-id'd) entries into the canonical form on sweep -- so adding the heading makes a minted entry read as already-normalized, and the one mechanism that would have given it a `status:` is now the one most likely to skip it. The change can therefore leave these entries less likely to be triaged than the anonymous bullets it replaced.
  evidence: Found by review pass 3 (Blind Hunter, corroborated by Edge Case Hunter, which proposed adding `status: open` and `origin:` lines as the guard). Confirmed by direct inspection: `.claude/skills/bmad-loop-sweep/deferred-work-format.md` declares itself canonical for this exact file and requires `origin`/`location`/`severity`/`reason`/`status`, and that skill's SKILL.md Step 1 builds its open set from blocks whose `status:` is `open`. Not patched in this pass: Story 7.1's Always clause freezes the field schema ("Preserve the existing `source_spec`/`summary`/`evidence` field schema ... exactly as they are today") and its Never clause forbids rewriting entries into the `origin`/`location`/`severity`/`reason`/`status` shape, so adding `status:` here would directly violate the intent contract. Related to `DW-FU-7-1-3` but causally distinct: that entry records that two documents disagree about id numbering and dedupe; this one records that the id heading actively defeats the normalization pass, which `DW-FU-7-1-3` cites as the reason the conflict was tolerable.
  severity: medium
  status: open
  promoted: 2026-08-11 (landing pass, doctor 7-1 review)

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-5: Follow-up review still recommended for 7-1-the-emitter-mints-identity-at-defer-time after the damping cap was spent
origin: review-budget-followup
source_spec: `spec-7-1-the-emitter-mints-identity-at-defer-time.md`
severity: low
reason: The follow-up-review damping cap (limits.max_followup_reviews = 2) was spent with the story finalized (status: done, verify green) while the review pass still recommended an independent follow-up. The work was committed by bmad-loop run 20260810-192603-53da; this entry preserves the lingering recommendation for a deliberate later review.
status: open
  severity: medium
  status: open
  promoted: 2026-08-11 (landing pass, doctor 7-1 review)

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-7-1-7: marshal's promoter cannot suffix around an id collision, so a minted id that reuses a promoted-follow-up id silently cancels a real promotion
- source_spec: `{project-root}/_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-7-1-the-emitter-mints-identity-at-defer-time.md`
  summary: marshal's promoter cannot suffix around an id collision, so a minted id that reuses a promoted-follow-up id silently cancels a real promotion.
  evidence: Found by review pass 4 (Blind Hunter). Verified by direct inspection of `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/deferred_work.py`: `promoted_id()` is documented as having "no numeric counter" and returns the follow-up shape built from `render_filename_slug(story_key)` alone, while `deferrals_to_promote()` skips any candidate whose `promoted_id` already appears as a complete token in the tracked text (`_tracked_promoted_ids`, `_PROMOTED_ID_TOKEN_RE`). The emitter can suffix around a collision; the promoter cannot. So once an emitter-minted entry for a story is promoted into the tracked ledger, a genuine `review-budget-followup` deferral for that same story is dropped by `marshal land` with no error, and its Tier-3 generic id then reds the deferred-work gate with nothing able to close it. Live today for this very story: `DW-FU-7-1` is already in doctor's tracked ledger. This is causally distinct from `DW-FU-7-1-2`, and in fact falsifies that entry's stated premise -- it records the overlap as "collision-safe by suffix", which holds only on the emitter side. Not patched in this pass: Story 7.1's Never clause scopes the promotion mechanism out ("Never touch the tracked ledger, the promotion mechanism..."), and the shape mapping itself is operator-confirmed in the epic. Resolving it is a planning-level decision on `DW-FU-7-1-2`'s ground, now with the harder constraint that suffixing is not a workaround.
  severity: medium
  status: open
  promoted: 2026-08-11 (landing pass, doctor 7-1 review)

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/1 present; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-7-2: Whether the backlog's growth from 470 to 506 predates or postdates Story 7.1's merge is unverified, so this baseline neither confirms nor refutes whether 7.1 stopped new anonymous entries
- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-7-2-grandfather-the-470-at-a-dated-cut-off.md`
  summary: Whether the backlog's growth from 470 to 506 predates or postdates Story 7.1's merge is unverified, so this baseline neither confirms nor refutes whether 7.1 stopped new anonymous entries.
  evidence: Found by review pass 1 (Blind Hunter). The epic's 470 figure was measured 2026-08-10T19:15 (per spec-deferred-work-visibility's own `.memlog.md`); Story 7.1 ("the emitter mints identity at defer time") merged at 2026-08-10 21:06:04, after that measurement; this story's own stamp (2026-08-11) measures 506, a growth of 36 across the window. Story 7.2's own Design Notes already explain why 506 differs from 470 in general terms (the backlog is "a live, growing thing") and that explanation was already reviewed and accepted -- but it does not address WHEN the growth happened relative to 7.1's merge, which is the one fact that would show whether 7.1's fix is actually holding. Tier-3 entries carry no timestamp field, so this cannot be answered from the data alone without a dedicated investigation (e.g. diffing each project's Tier-3 file at the 7.1 merge commit against its current state). Not patched in this pass: this story's Never clause explicitly forbids triaging or wiring the backlog, and confirming 7.1's efficacy is a distinct question from stamping the current count -- but real and worth a focused follow-up, since a "yes, still growing after 7.1" answer would be a live regression in already-shipped work.
  severity: medium
  status: open
  promoted: 2026-08-11 (landing pass, doctor 7-2)

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-7-2-grandfather-the-470-at-a-dated-cut-off.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-7-2-2: A project whose Tier-3 file is deleted can never have its baseline entry lowered or zeroed via `--project`
- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-7-2-grandfather-the-470-at-a-dated-cut-off.md`
  summary: A project whose Tier-3 file is deleted can never have its baseline entry lowered or zeroed via `--project`, because the unknown-project validation only consults currently-discoverable projects, not the union of the committed baseline and the currently-discoverable set.
  evidence: Found by Blind Hunter and Edge Case Hunter independently (Blind Hunter's finding, Edge Case Hunter's related finding #6). Confirmed by reading `scripts/deferred_work_baseline.py::main`: `unknown = sorted(set(args.project) - set(current))` checks only `_live_state()`'s locally-discoverable projects; a project already present in the committed baseline but whose Tier-3 file no longer exists (backlog fully resolved and the file deleted, or a not-yet-backlinked worktree) is reported "unknown project(s)" and cannot be re-baselined at a lower or zero count -- its stamped count becomes a permanent ceiling. This is the mirror image of the already-fixed "bare mode silently drops an invisible project" defect (this story's own review pass 1 patched that one); the fix there was to always merge and never drop, but that same merge now means there is no path to deliberately LOWER a count once stamped, only to raise or leave it. Out of scope for this pass: this story's Never clause forbids triaging the backlog and this scenario (a known project's Tier-3 scratch disappearing entirely while the project itself remains active) has not occurred yet in this repo -- but it is a real gap Story 7.3 or a future maintenance pass should account for before relying on the baseline as authoritative for a shrinking count.
  severity: medium
  status: open
  promoted: 2026-08-11 (landing pass, doctor 7-2)

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-7-2-grandfather-the-470-at-a-dated-cut-off.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-6-11-1: No test in test_sources_factory.py asserts classify()'s literal return string for any rule, including the new spike-report one
- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-6-11-the-classifier-recognizes-a-spike-report.md`
  summary: No test in test_sources_factory.py asserts classify()'s literal return string for any rule, including the new spike-report one, so a typo in a returned label would sail through the whole file's coverage as long as it isn't literally "UNKNOWN".
  evidence: Found by review pass 1 (Blind Hunter). Verified by inspection: every test in test_sources_factory.py exercises classify() only indirectly through factory.gather()/check_coverage()'s check/status/evidence fields (e.g. check == "bmad-drift" and status is DoctorStatus.OK, or check == "uncovered" and status is DoctorStatus.FAIL) -- none calls factory.classify(path, target) directly and asserts its return string. This story's own three new tests (test_spike_report_is_classified_and_not_flagged_uncovered, test_a_second_spike_index_is_also_classified, test_spike_report_look_alike_without_a_numeric_index_still_hard_fails) follow that same pre-existing convention, so a typo in the new rule's return label (e.g. "archive:spike_report" for "archive:spike-report") would still pass every test in the file. Pre-existing across the whole file -- every prior classify() rule has the identical gap -- not a regression introduced by this story, and out of Story 6.11's Boundaries & Constraints, which scope the change to exactly one new rule using the file's existing test pattern. Worth a dedicated follow-up: add direct classify()-return-value assertions across the file's coverage tests.
  status: open
  promoted: 2026-08-15 — promoted from Tier-3 `implementation-artifacts/deferred-work.md` (id `DW-FU-6-11` there) during the pre-shutdown deferred-work audit.

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-6-11-the-classifier-recognizes-a-spike-report.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-7-3-1: The committed anonymous-Tier-3 baseline has no automated freshness check, only this landing's one-off manual verification
- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-7-3-the-detector-sees-anonymous-tier-3-entries.md`
  summary: The committed anonymous-Tier-3 baseline has no automated freshness check, only this landing's one-off manual verification that live counts still match the stamped counts.
  evidence: Found by Blind Hunter (review pass 1). This story's detector compares each project's live anonymous-Tier-3 entries against the count stamped in `scripts/.deferred-work-baseline.json` (Story 7.2), and CAP-3's "green on an unchanged repo" claim is confirmed in this landing only by a manual, one-time `pixi run -e pyforge-doctor python -m pyforge.doctor.sources deferred-work` inspection recorded in this Spec's `.memlog.md` -- not by any repeatable test or CI gate. If the fleet's live counts drift ahead of the stamped baseline over time (new anonymous entries accumulating faster than anyone re-runs `--write-baseline`), the first symptom is a bare `tier3-entry-unidentified` FAIL with no pointer back to this landing's context, and no automated signal warns that the baseline itself has gone stale. Related to `DW-FU-7-2-2` (a baseline count can never be LOWERED once a project's Tier-3 file disappears) but causally distinct: that entry is about the baseline being too CONSERVATIVE for a shrinking project; this one is about the baseline having no mechanism to detect it has fallen BEHIND a growing one. Not patched in this pass: this story's Never clause forbids triaging or wiring the backlog, and adding a staleness-detection mechanism for the baseline itself is a distinct capability outside CAP-2/CAP-3's stated scope -- worth a dedicated follow-up.
  status: open
  promoted: 2026-08-15 — promoted from Tier-3 `implementation-artifacts/deferred-work.md` (id `DW-FU-7-3` there) during the pre-shutdown deferred-work audit.

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-7-3-the-detector-sees-anonymous-tier-3-entries.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-CHAIN-COMPLETENESS-1: INV-A's spec-not-decomposed test is a bare substring match, so a Spec can grow capabilities with no stories and still read as fully decomposed
- source_spec: `_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/spec-deferred-work-visibility/SPEC.md`
  summary: `chain-completeness`'s INV-A (`sources/board.py::_check_project_chain_completeness`) decides "this open Spec is decomposed" by testing whether the Spec's slug appears **anywhere** in the concatenated prose of the project's PRDs and `epics*.md` (`if bare not in prose and slug not in prose`). It is a substring test over a blob, not a coverage test over capabilities — so once ANY epic mentions a Spec by name, that Spec reads as decomposed forever, no matter how many capabilities are later added to it with zero stories against them.
  evidence: Found by execution 2026-08-15, not inference. `spec-deferred-work-visibility` was extended the same day from 3 capabilities to 10 (CAP-4..10 added, PR #521). Epic 7's prose already contained the string `spec-deferred-work-visibility`, so `pixi run -e local-recipes python -m pyforge.doctor.sources chain-completeness` reported `ok -- every open Spec is decomposed, and epics, ledger and board agree` while **seven of its ten capabilities had no story anywhere in the fleet**. Confirmed by reading `board.py:463-477`: the loop is over `pa.glob("specs/spec-*/SPEC.md")`, the membership test is `bare not in prose and slug not in prose`, and nothing parses `CAP-` ids on either side. This is structurally the SAME defect class the owning Spec itself exists to fix -- a set/substring membership test that reports OK while missing most of what it claims to check (cf. that Spec's own `_DW_RE` two-set comparison, which passed while 470 anonymous deferrals were invisible). The gap was closed for THIS spec by hand (Epics 8 and 9, appended 2026-08-15), which is exactly the manual compensation that makes the detector's blind spot easy to keep not noticing.
  status: partially closed
  raised: 2026-08-15 — raised directly (not promoted from Tier-3) while answering that Spec's Q5, and deliberately NOT folded into it as CAP-11: it is a different detector (`sources/board.py`'s chain-completeness, not `sources/chain.py`'s deferred-work), and reopening a Spec at the moment its decomposition lands would defeat the purpose of decomposing it. Owner: doctor (owns `board.py`). A real fix needs INV-A to compare the Spec's declared `CAP-` ids against the CAP ids cited in the project's epics/stories prose, and to report the uncovered ones by id rather than answering a yes/no per Spec.
  closed: 2026-08-21 — Story 12.3 landed (Round 4 re-derive, after three bad_spec loopbacks on rounds 1-3), and closes the ORIGINAL defect this entry was raised for: `board.py` now parses each open Spec's declared `CAP-N` ids from its `## Capabilities` section and every `CAP-` citation actually present in the project's PRD/epics prose (bare `CAP-N`, inclusive range `CAP-N..M`, prefixed range `CAP-N..CAP-M`, slash-grouped `CAP-N/M/O`), scoped per-Spec via an any-slug-occurrence citation window (ended by the next Spec's own anchor or capped at the second `## ` heading past the anchor, whichever is nearer) — never pooled flat across a project, which would let two same-numbered Specs falsely cover each other (the Round 2 defect class). The uncovered ids are reported by name (e.g. `CAP-4..10`), not a single yes/no per Spec. A Spec with zero declared CAP ids keeps the original bare-substring fallback, byte-identical. Round 4's own contribution: each source file's own preamble (everything before THAT FILE'S first `## ` heading) is stripped at prose-build time, before concatenation — replacing Round 3's whole-blob, first-file-only exclusion, which only protected whichever file happened to sort first and left every later file's own preamble (e.g. an epics.md's own frontmatter `currency_review` field) still exploitable. Verified against both real live reproductions Round 3/4 review found (`pyforge-marshal/prd.md`'s changelog-block `CAP-9` mention and `pyforge-doctor/epics.md`'s own `currency_review` field, both before their file's first `## ` heading) — neither leaks after the fix, confirmed live and via a targeted redaction repro. `pixi run -e local-recipes python -m pyforge.doctor.sources chain-completeness` reports the same 7 genuine findings as before the fix (pyforge-marshal x6, pyforge-steward x1) — both repro fixes only removed latent/inert leaks, not real findings. **Marked "partially closed", not "closed"**: Round 4's own post-landing adversarial review (Blind Hunter) live-reproduced two further leaks in the SAME preamble-stripping heuristic — see `DW-CHAIN-COMPLETENESS-4` and `DW-CHAIN-COMPLETENESS-5`, minted the same day rather than triggering a fifth bad_spec loopback on a heuristic that has now needed narrowing four times running.

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec present; cited paths 1/1 present; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

  verified: 2026-09-07 — resolved — re-read against live code (agent judgment, not mechanical), resolving the self-contradiction between this entry's own `status: partially closed`/`closed: 2026-08-21` fields above and the two prior mechanical `verified: ... still-open` stamps. `board.py:156-193` (`_CAP_SECTION_HEADING_RE`, `_CAP_DECL_LINE_RE`, `_CAP_CITATION_RE`, `_parse_declared_cap_ids`) confirms Story 12.3's CAP-N-id comparison is genuinely landed exactly as this entry's own `closed:` note describes — the ORIGINAL defect this entry was raised for (a bare-substring "decomposed" test) is truly fixed, not merely claimed. The two `still-open` verified lines above are mechanical fleet-hygiene stamps, not a real re-read: both explicitly say "agent judgment not applied" and both derive their verdict from a closed vocabulary that has no "partially closed" member, so an unrecognized `status:` string defaults to `still-open` — they are not evidence of a real disagreement with this entry's own recorded resolution. `partially closed` is the semantically correct status: the residual gaps that keep this entry short of a full `closed` are two INDEPENDENTLY TRACKED follow-on defects, `DW-CHAIN-COMPLETENESS-4` and `DW-CHAIN-COMPLETENESS-5`, both re-confirmed still genuinely open in current code during this pass (`board.py`'s per-file preamble stripping keeps everything AFTER a file's own first `## ` heading un-stripped — the exact `## Changelog`-shaped leak DW-CHAIN-COMPLETENESS-4 describes — and a file with NO `## ` heading anywhere is still kept whole and unstripped, DW-CHAIN-COMPLETENESS-5). `DW-CHAIN-COMPLETENESS-3`'s exact-string `## Capabilities` heading match is also unchanged/still open. No code change made or owed for THIS entry specifically — its own status is accurate as written; the outstanding work lives entirely under -3/-4/-5's own entries.

  verified: 2026-09-09 — still-open — re-read against live code (agent judgment) during the fleet-readiness pass. A FOURTH route into this entry's defect class was found, and it is the one the fleet actually took: `board.py:715` reads `status = ...get("status", "")` and `:716` `continue`s when the value is not in `OPEN_SPEC_STATUSES`, so a SPEC.md with **no `status:` key at all** fails the membership test and is silently exempted from INV-A entirely — not a weak check, no check. **8 Specs fleet-wide** are in that state, **4 of them doctor's own**, and re-running INV-A's own `_parse_declared_cap_ids` / `_cited_cap_ids_by_spec` with the status gate lifted shows `spec-pixi-candidate-currency` with **all 5 declared CAPs uncovered** while `chain-completeness` reports `ok`. Note the asymmetry this closes: the Dream side already flags a missing status (`chain.py:993`). Vesselled 2026-09-09 as doctor **Story 21.2** (`spec-status-missing`, emit a finding, never guess an implied status — treating absence as `draft` would red `spec-pyforge-marshal` and `spec-pyforge-mason` for a bookkeeping omission); the four doctor Specs' own missing `status:` lines are corrected by the Class-B memlog entries of the same pass. Source: `fleet-readiness-decision-batch-2026-09-09.md` § 2.3 C8 / doctor-B5. `partially closed` remains the correct status for this entry; nothing about -3/-4/-5 changed.

### DW-CHAIN-COMPLETENESS-2: Story 12.3's newly-precise INV-A surfaces 7 real CAP-coverage gaps in pyforge-marshal and pyforge-steward that the old bare-substring check could never have caught
- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-12-3-chain-completeness-parses-capability-ids-not-a-bare-substring-match.md`
  summary: Now that `chain-completeness`'s INV-A compares a Spec's declared `CAP-N` ids against the ids actually cited for that Spec in its own project's PRD/epics prose (Story 12.3, closing `DW-CHAIN-COMPLETENESS-1`), a live run against this repo's real `_bmad-output/projects/` tree surfaces 7 genuine, previously-invisible capability-coverage gaps — none owned by `pyforge-doctor`, so none are this story's own to fix. `pyforge-marshal`: `spec-agent-tool-surface` (CAP-1..4), `spec-dashboard-project-path-derivation` (CAP-1..4), `spec-loop-home-fleet-refresh` (CAP-1..3), `spec-sprint-status-auto-promote` (CAP-1..4), `spec-pyforge-core` (CAP-1..7, ALL of it), and `spec-surface-drift-reconciliation` (CAP-7 only, a partial gap). `pyforge-steward`: `spec-jira-github-projects-sync` (CAP-2..5).
  evidence: Confirmed genuine for every entry, not a parsing artifact, by reading the underlying planning artifacts directly (independently re-verified across this story's Round 2, 3, and 4 review passes; the live finding set was identical across all of them, including after Round 3/4's preamble-leak fixes — proving the two fixed leaks were latent/inert and never the source of any of these 7 findings). Five of the six marshal Specs (`agent-tool-surface`, `dashboard-project-path-derivation`, `loop-home-fleet-refresh`, `sprint-status-auto-promote`, `pyforge-core`) are among the "8 previously-undecomposed Marshal Specs absorbed as FR-128..FR-163" named in `prd.md`'s own 2026-08-08 `currency_review` comment — their epics (where any exist at all; `spec-pyforge-core`'s Epic 14 cites only `FR-N`, never once repeating a `CAP-` id) predate or never adopted the `CAP-N` citation convention their own SPEC.md later gained. `spec-surface-drift-reconciliation`'s `prd.md` cites `CAP-1..CAP-6` explicitly but never `CAP-7`. `spec-jira-github-projects-sync`'s own `epics.md` Story 8.7 cites only "CAP-1 residual," matching its own `currency_review`'s "previously undecomposed" language for the rest. This is precisely the defect class `DW-CHAIN-COMPLETENESS-1` existed to surface — it was structurally impossible for the OLD bare-substring check to ever report any of these, since every one of these Specs' slugs already appears somewhere in its own project's prose. Reproducible: `pixi run -e local-recipes python -m pyforge.doctor.sources chain-completeness`.
  status: open
  severity: medium
  raised: 2026-08-21 — raised while running Story 12.3's own required live-sanity-check gate (its spec's Acceptance Criteria: inspect every new finding before closing `DW-CHAIN-COMPLETENESS-1`); first drafted during Round 2's review pass, then re-minted (identically) after Rounds 2 and 3's own code was separately reverted by later bad_spec loopbacks, landing here once Round 4's fix shipped with the same 7 findings unchanged throughout. Owner: pyforge-marshal (6 of 7 findings) and pyforge-steward (1 of 7) — decomposing these Specs' own remaining capabilities into their own epics/stories is planning work for those stations, not a `board.py` code change, and out of `board.py`'s own Never clause ("do not touch INV-B/C/D... same signature, same call site").

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-12-3-chain-completeness-parses-capability-ids-not-a-bare-substring-match.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

  verified: 2026-09-09 — still-open — re-measured against live artifacts during the fleet-readiness pass. The 7 findings this entry names are unchanged and still belong to marshal (6) and steward (1). What the pass adds is that the 7 are an **undercount by construction**: re-running INV-A's own parsers with the status gate lifted exposes a further 21 uncovered CAPs hidden behind Specs carrying no `status:` key — marshal `spec-pyforge-marshal` (17 of 18 uncovered), `spec-marshal-land-cross-project-story-key-collision` (2/2), `spec-marshal-status-harness-run-id-poisoning` (2/2); mason `spec-pyforge-mason` (7/7); doctor `spec-pixi-candidate-currency` (5/5). Doctor's own half is vesselled the same day (`spec-pixi-candidate-currency` gains `status: ready` + **Story 21.7** for CAP-4; `spec-backlog-intake-check` and `spec-fleet-hygiene-verification-exemplar-program` gain `status: shipped`, 0 uncovered each; `spec-deferred-work-resolution-sweep` gains `status: in-progress` + CAP-9 → **Story 21.8**). The marshal and mason halves stay with their stations, exactly as this entry's `raised:` note already scopes them — Class B of the batch flips those Specs' statuses, at which point INV-A will report their CAPs for the first time. Source: `fleet-readiness-decision-batch-2026-09-09.md` § 2.2 (doctor rows) + doctor report § E.

### DW-CHAIN-COMPLETENESS-3: `_parse_declared_cap_ids`'s `## Capabilities` heading match is exact-string, case-sensitive, no trailing text, so a SPEC.md using a different heading convention silently falls back to the (weaker) bare-substring check
- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-12-3-chain-completeness-parses-capability-ids-not-a-bare-substring-match.md`
  summary: `board.py::_parse_declared_cap_ids` recognizes only a literal `## Capabilities` heading (exact string, case-sensitive, no trailing text) as the start of a Spec's declared-CAP-ids section. A SPEC.md using any other heading for the same content — confirmed live: `spec-dream-to-code-model-self-verification`'s `SPEC.md` uses `## Scope (capabilities)` — parses to zero declared ids, which is INV-A's own signal to fall back to the original, weaker bare-substring check instead of being detected and flagged as a genuine coverage gap or a malformed-but-real Capabilities section.
  evidence: Found during Story 12.3's review pass 1 (adversarial review) via a live grep of every tracked `SPEC.md`'s section headings fleet-wide; confirmed by reading `spec-dream-to-code-model-self-verification/SPEC.md` directly. Deliberately not folded into Story 12.3's own fix (its Never clause scopes the story to the `board.py` INV-A comparison logic, not to auditing every SPEC.md heading-convention variant fleet-wide — a separate, larger scope question, possibly its own catalog item under `hygiene-gap-catalog.md` Category 1).
  status: open
  severity: low
  raised: 2026-08-21 — raised during Story 12.3's review pass 1, deferred rather than folded in to keep that story's diff bounded to its one scoped bug; minted here as promised in that pass's own triage log, once the story actually landed. Owner: doctor (owns `board.py`'s INV-A parsing) — a real fix widens `_CAP_SECTION_HEADING_RE` to tolerate case/trailing-text variance, or standardizes every SPEC.md onto one heading spelling (a `bmad-spec` template question, not purely a `board.py` one).

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-12-3-chain-completeness-parses-capability-ids-not-a-bare-substring-match.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-CHAIN-COMPLETENESS-4: Round 4's per-file preamble stripping only strips text BEFORE a file's own first `## ` heading — irrelevant content AFTER that heading (e.g. a `## Changelog` section) can still open a false citation window
- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-12-3-chain-completeness-parses-capability-ids-not-a-bare-substring-match.md`
  summary: `board.py`'s Round 4 fix slices each source file to start at its own first `## ` heading, discarding everything before it. It does not distinguish a decomposition-relevant heading from an irrelevant one — if the file's FIRST heading happens to be something like `## Changelog` and that section incidentally mentions a Spec's slug near an unrelated CAP id, that text is never stripped (it comes after the first heading) and can still open a false citation window, crediting a CAP id the Spec's own epics/stories never actually cite.
  evidence: Live-reproduced during Story 12.3's Round 4 review pass (Blind Hunter) against the actual merged `board.gather_chain_completeness`: a Spec declaring `CAP-1,2,3,9` with a legitimate `CAP-1..3` citation in `epics.md`, plus a `prd.md` whose first `## ` heading is `## Changelog` containing "spec-foo's CAP-9 was discussed and deferred" — reports `ok` instead of `FAIL: CAP-9 uncovered`. Same defect class as `DW-CHAIN-COMPLETENESS-1`'s original bug (a real, open gap silently reading as covered), recurring in a new position after four rounds of narrowing. Not folded into Story 12.3's own fix by operator decision (2026-08-21): the heuristic has already needed narrowing on rounds 2, 3, and 4, and a fifth loopback risks the same pattern recurring in yet another position rather than closing the underlying class. Landed with this residual documented instead.
  status: open
  severity: medium
  raised: 2026-08-21 — raised during Story 12.3's Round 4 review pass, landed as a documented residual rather than triggering a fifth bad_spec loopback. Owner: doctor (owns `board.py`'s INV-A parsing). A real fix likely needs to allowlist decomposition-relevant heading shapes (e.g. `## Epic N`, `### Story N.M`) rather than treating "any `## ` heading" as the boundary of relevant content — a larger redesign of the windowing model, not a narrow patch, given the heuristic's track record.

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-12-3-chain-completeness-parses-capability-ids-not-a-bare-substring-match.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-CHAIN-COMPLETENESS-5: a source file with NO `## ` heading anywhere is kept whole, unstripped — its entire text (including anything preamble-shaped) remains searchable for false citations
- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-12-3-chain-completeness-parses-capability-ids-not-a-bare-substring-match.md`
  summary: When a source file has no `## ` heading at all, Round 4's per-file preamble stripping has no signal to slice against and keeps the file's text whole, unchanged — so a slug+CAP-id adjacency anywhere in that file's prose (there being no "preamble" boundary to strip) can still open a false citation window, the same leak class as `DW-CHAIN-COMPLETENESS-4` triggered by a different shape.
  evidence: Confirmed directly in `board.py`'s own preamble-stripping code and comments ("a file with no `## ` heading anywhere is kept whole, unchanged -- there is no signal to slice against"), live-reproduced during Story 12.3's Round 4 review pass with a synthetic headerless `epics*.md`-shaped fixture. This exact edge was already identified and consciously deferred during Story 12.3's Review pass 3 ("a project with zero `## `-level headings anywhere would disable both the heading-cap and preamble-exclusion logic simultaneously... untested, not live-triggered by any of the 8 tracked projects today... revisit only if a real headerless project file surfaces") — re-confirmed here as still real and still not live-triggered (`grep -L '^## ' _bmad-output/projects/*/planning-artifacts/{prd.md,epics*.md}` finds none), so minted as its own tracked entry now rather than left as a one-off note buried in a landed story's Review Triage Log.
  status: open
  severity: low
  raised: 2026-08-21 — raised during Story 12.3's Review pass 3, minted as a proper ledger entry at landing time per that pass's own commitment. Owner: doctor (owns `board.py`'s INV-A parsing). No live occurrence today; revisit if a real headerless `prd.md`/`epics*.md` ever surfaces fleet-wide, or fold into `DW-CHAIN-COMPLETENESS-4`'s redesign if that work happens first.

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-12-3-chain-completeness-parses-capability-ids-not-a-bare-substring-match.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-11-8-1: CAP-8 (backlog-intake check) split out of Epic 11 — RESOLVED, follow-on Spec + Epic 13 now exist
- source_spec: `_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/spec-backlog-intake-check/SPEC.md`
  summary: former Story 11.8 ("backlog-intake surfaces deferred entries during story drafting," CAP-8) removed from Epic 11 2026-08-21. Its own Spec's Open Question — same story wave as CAP-1..7, or its own follow-on Spec, since it is write-adjacent to story-drafting rather than the read-only sweep the rest of the Spec is — resolved to "own follow-on Spec": Epic 11 shipped cleanly as Stories 11.1–11.7, a self-contained read-only pipeline, and bundling a write-adjacent capability into it after the fact would have muddied that result. `spec-deferred-work-resolution-sweep`'s own CAP-8 entry marked split-out (now covers CAP-1..7 only); the capability re-emerged the same day as its own Spec (`spec-backlog-intake-check`, CAP-1, via `bmad-spec`) and Epic 13 / Story 13.1 in `epics.md`, cleared to dispatch.
  evidence: `epics.md` Epic 11 (Story 11.8 removed, dated split-out note) and Epic 13 (Story 13.1, new); `spec-deferred-work-resolution-sweep/SPEC.md` (CAP-8 marked split-out); `spec-backlog-intake-check/SPEC.md` (the new companion Spec, CAP-1, `.memlog.md` records the full derivation); `docs/dreams/deferred-work-resolution-sweep.md` Realization log (both Specs' owner-dream).
  status: closed — Story 13.1 landed 2026-08-21: `pyforge.doctor.sources.backlog_intake` (`Source.BACKLOG_INTAKE`) + the `doctor backlog-intake <identifier>` CLI verb ship the fleet-wide, precisely-id-matched deferred-work surfacing this entry called for.
  raised: 2026-08-21 — raised directly while resolving Epic 11's own closeout; resolved same day once the operator directed the follow-on Spec be written.
  closed: 2026-08-21 — Story 13.1 (`spec-13-1-a-drafting-session-surfaces-matching-deferred-work-entries-for-an-epic.md`) landed.

  verified: 2026-08-26 — resolved — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to resolved

  verified: 2026-09-02 — closed — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec present; cited paths 2/2 present; ledger status mapped to closed; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

## DW-FU-12-4 — A glob-less, trailing-slash spec-surface entry that can never match (foreign defect, surfaced by Story 12.4's review)

`spec-dream-to-code-model-self-verification/SPEC.md:13` declares the surface entry
`.claude/skills/conda-forge-expert/tests/meta/` — glob-less with a trailing slash, which
`chain.py::_glob_to_re`'s exact-match rule can never match; the directory is actually
governed by pyforge-mason's `spec-packaging-factory` blanket glob. Pre-existing and
marshal-owned (foreign to doctor); surfaced incidentally by Story 12.4's pass-2 review and
deferred rather than fixed cross-station. Remedy: fix the entry in the owning spec (a real
glob or the file list), then re-stamp that spec's surface baseline. Severity: low. Status:
open. Relayed from the story worktree's ephemeral Tier-3 file at landing, 2026-08-21.

  status: open
  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): no source_spec; cited paths 1/1 present; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

## DW-FU-12-5 — Corrupt committed baseline dies with a raw JSONDecodeError on scoped stamps

Pre-existing in `scripts/spec_surface_check.py`: a corrupt `scripts/.spec-surface-baseline.json`
raises raw `json.JSONDecodeError` on a scoped `--write-baseline`. Remedy: a diagnostic naming
the file and the re-stamp recovery path — never an `except -> {}` fallback (rejected in Story
12.5's review as reintroducing the drop-every-other-spec hazard). Severity: low. Status: open.
Relayed from the story worktree's ephemeral Tier-3 at landing, 2026-08-21.

  status: open
  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): no source_spec; cited paths 2/2 present; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

## DW-FU-12-5-2 — Zero-discoverable-specs full stamp silently wipes the baseline to `{}` at exit 0

Pre-existing accept-everything semantics of the `--spec`-less full stamp, degenerate case: with
spec discovery returning nothing it replaces the whole baseline with `{}`, exit 0. Remedy:
refuse (or gate behind a flag) when the live snapshot is empty and the existing baseline is
not. Severity: low. Status: open. Relayed 2026-08-21.

  status: open
  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): no source_spec; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-14-1-1: GitHub-releases fallback for the 6 npm-invisible bmad-suite packages — RESOLVED
  origin: story-14-1 review (deferred, medium — carried in the story spec's 6.11-era frontmatter `deferred:` list)
  source_spec: `planning-artifacts/specs/spec-14-1-the-bmad-suite-is-compared-against-upstream-derived-not-declared.md`
  severity: medium
  reason: 6 of the 10 watched bmad-* pins (bmad-loop, bmad-labs-skills, bmad-manticore, bmad-method-wds-expansion, bmad-module-template, bmad-utility-skills) are GitHub-only and 404 on registry.npmjs.org, so CAP-4's live npm path is structurally blind to them — including bmad-loop, the package whose 0.9.0-vs-0.11.0 lag motivated CAP-4. The `packages_watched` (10) vs `packages_checked` (4) evidence keeps the gap operator-visible; the follow-on is a GitHub-releases fallback query through the same fail-open budget. Relayed here because the DW pipeline cannot yet read frontmatter `deferred:` lists (marshal DW-BL011-2).
  status: closed — Story 15.1 landed 2026-08-22: `_gather_suite_findings` (`sources/bmad_method.py`) now falls back to GitHub releases/tags, keyed by each package's own tracked `recipes/<name>/recipe.yaml` github mapping, whenever the npm fetch misses — the DW-14-1-1 fixture (bmad-loop named from GitHub) passes. `bmad-labs-skills`/`bmad-module-template`/`bmad-utility-skills` still go unchecked (neither Releases nor tags exist upstream today) — a real, documented limit of the source data, not this fallback.
  closed: 2026-08-22 — Story 15.1 (`spec-15-1-github-releases-unblind-the-npm-invisible-packages.md`) landed.

  verified: 2026-08-26 — resolved — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to resolved

  verified: 2026-09-02 — closed — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; cited paths 0/1 present (absent: recipes/<name>/recipe.yaml); ledger status mapped to closed; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-12-5-3: The spec-surface baseline write race RECURRED cross-process — scoped stamps from two live processes drop each other's entries
  origin: live recurrence 2026-08-22 (parent session + the Story 25.5 agent stamping concurrently)
  source_spec: `spec-12-5-*` orbit (Story 12.5 closed "the spec-surface baseline write race"; this is the cross-process shape)
  severity: medium
  reason: While the 25.5 agent ran scoped `--write-baseline --spec` stamps, the parent session's own scoped stamps interleaved — each full-file read-modify-write rewrote scripts/.spec-surface-baseline.json from its own read, and last-writer-wins dropped the other's entries (observed twice: 4 entries lost, re-stamped, then a DIFFERENT 8 lost — mason x2, scribe, steward x3, warden x2). Story 12.5's fix evidently does not cover two independent processes (or one path bypasses its lock). Remedy shape: file-lock (fcntl) around the read-modify-write, or merge-on-write keyed per spec (the file is per-spec keyed — a targeted upsert of only the stamped spec's entries would be race-immune by construction). Workaround until fixed: never stamp while another stamping process is live; do one consolidated pass on a quiet tree (this session's recovery).
  status: open

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-14-1: GitHub-releases fallback for the npm-invisible suite packages: 6 of the 10 live bmad-* pins (bmad-loop, bmad-labs-skills, bmad-manticore, bmad-method-wds-expansion, bmad-module-template, bmad-utility-skills) are GitHub-only local conda recipes that 404 on registry.npmjs.org, so the live CAP-4 path is structurally blind to them — including bmad-loop, the package whose 0.9.0-vs-0.11.0 lag motivated CAP-4.

- source_spec: `planning-artifacts/specs/spec-14-1-the-bmad-suite-is-compared-against-upstream-derived-not-declared.md`
  summary: GitHub-releases fallback for the npm-invisible suite packages: 6 of the 10 live bmad-* pins (bmad-loop, bmad-labs-skills, bmad-manticore, bmad-method-wds-expansion, bmad-module-template, bmad-utility-skills) are GitHub-only local conda recipes that 404 on registry.npmjs.org, so the live CAP-4 path is structurally blind to them — including bmad-loop, the package whose 0.9.0-vs-0.11.0 lag motivated CAP-4.
  evidence: Live probe 2026-08-21 during Story 14.1: only bmad-builder, bmad-creative-intelligence-suite, bmad-dashboard, and bmad-method-test-architecture-enterprise return 200 from https://registry.npmjs.org/{package}/latest. Recorded as a (note) in spec-bmad-method-version-drift/.memlog.md. The 404s fold to per-package silent skip exactly as the fail-open contract specifies, so no Finding can ever fire for these 6 on the live path; the fixture proof passes only via the stubbed fetch seam. Intent authorized "follow whatever bmad_method.py's existing upstream query does and generalize it" — npm-only — so this is a follow-on capability, not a defect in this story.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/bmad_method.py
  origin: spec-deferred 902f2339efff — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: medium
  promoted: 2026-08-23 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-14-1-2: Suite pin-floor-vs-installed (offline CAP-1 analog for the suite) is not covered by any capability: an installed suite package behind its own pixi.toml floor (the fixture's literal shape — bmad-loop 0.9.0 installed vs >=0.11.0 pinned) is provable with zero network but produces no signal; pins are used only as the watched-set roster.

- source_spec: `planning-artifacts/specs/spec-14-1-the-bmad-suite-is-compared-against-upstream-derived-not-declared.md`
  summary: Suite pin-floor-vs-installed (offline CAP-1 analog for the suite) is not covered by any capability: an installed suite package behind its own pixi.toml floor (the fixture's literal shape — bmad-loop 0.9.0 installed vs >=0.11.0 pinned) is provable with zero network but produces no signal; pins are used only as the watched-set roster.
  evidence: Blind Hunter review, Story 14.1: the design notes dismiss it as "pixi install hygiene, not upstream drift" but no other capability catches it either. Arguably pixi's own territory (the state means pixi install has not run since the pin bump); an operator decision on whether Doctor should ambient-report it.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/bmad_method.py
  origin: spec-deferred 4621b65f5ab5 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-08-23 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-15-1: _fetch_latest_github_release's /tags fallback only reads GitHub's first response page, never following pagination.

- source_spec: `planning-artifacts/specs/spec-15-1-github-releases-unblind-the-npm-invisible-packages.md`
  summary: _fetch_latest_github_release's /tags fallback only reads GitHub's first response page, never following pagination.
  evidence: GitHub's /tags endpoint paginates; a heavily-tagged repo's newest tag could sit beyond the first page, understating the "latest" comparison. No currently-watched package needs it live: of the 6 named npm-invisible packages, 3 have GitHub Releases (checked first, succeeds before /tags is ever reached) and the other 3 have zero tags at all. Found by Edge Case Hunter during Story 15.1 review.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/bmad_method.py:_fetch_latest_github_release
  origin: spec-deferred 14acbd8d5b93 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-08-23 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-15-1-2: No test in test_sources_bmad_method.py reads a real, live recipes/<name>/recipe.yaml directly to prove the GitHub fallback resolves the 6 actually-named npm-invisible packages.

- source_spec: `planning-artifacts/specs/spec-15-1-github-releases-unblind-the-npm-invisible-packages.md`
  summary: No test in test_sources_bmad_method.py reads a real, live recipes/<name>/recipe.yaml directly to prove the GitHub fallback resolves the 6 actually-named npm-invisible packages.
  evidence: Every test in this file (including the new Story 15.1 tests) uses synthetic tmp_path-scoped fixtures, matching this file's own established, pre-existing convention (no test reads the real pixi.toml or _bmad/_config/manifest.yaml either). The mechanism was independently hand-verified against the live recipes/*/recipe.yaml files for all 6 named packages during spec planning and again during review (Intent Alignment Auditor), but no automated test would catch a future drift in that file's extra.cfe-upstream-* shape. Found by Intent Alignment Auditor during Story 15.1 review.
  location: src/shared/packages/pyforge-doctor/tests/unit/test_sources_bmad_method.py
  origin: spec-deferred 17d79655759f — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-08-23 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-15-2: Story 15.2's two new checks fire only for suite packages already present in `.pixi/envs` (inherited from `_gather_suite_findings`'s pre-existing install-state gate), so on a fresh clone/CI with no local suite installs, neither new check fires for any suite package -- only the CORE (`bmad-method`) axis is unaffected, since it reads the tracked `manifest.yaml` instead.

- source_spec: `planning-artifacts/specs/spec-15-2-channel-and-recipe-staleness-are-ambient-findings.md`
  summary: Story 15.2's two new checks fire only for suite packages already present in `.pixi/envs` (inherited from `_gather_suite_findings`'s pre-existing install-state gate), so on a fresh clone/CI with no local suite installs, neither new check fires for any suite package -- only the CORE (`bmad-method`) axis is unaffected, since it reads the tracked `manifest.yaml` instead.
  evidence: Edge Case Hunter (Story 15.2 review): `_gather_suite_findings` returns `()` immediately when `_installed_suite_versions` is empty, and skips any package absent from `installed` inside the loop -- both pre-existing Story 14.1 gates that Story 15.2's new checks inherit by extension rather than by new design. The story's own named fixture (the 6.3.0 relic) is specifically about the CORE package, which is unaffected; the gap is narrower, scoped to suite packages only.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/bmad_method.py:_gather_suite_findings
  origin: spec-deferred 43ee95ec8fb5 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: medium
  promoted: 2026-08-23 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-15-2-2: No dedicated fleet-picture-layer meta-test names `bmad-channel-drift` or `bmad-recipe-upstream-drift` specifically -- coverage of that named Surface is structural/generic only.

- source_spec: `planning-artifacts/specs/spec-15-2-channel-and-recipe-staleness-are-ambient-findings.md`
  summary: No dedicated fleet-picture-layer meta-test names `bmad-channel-drift` or `bmad-recipe-upstream-drift` specifically -- coverage of that named Surface is structural/generic only.
  evidence: Blind Hunter and Intent Alignment Auditor (review pass 2, independently convergent): `.claude/skills/conda-forge-expert/tests/meta/ test_fleet_picture_bmad_core_drift.py`'s existing `test_both_cap1_and_cap2_warn_simultaneously` already proves the generic pass-through mechanism (any WARN Finding under `Source.BMAD_METHOD_VERSION_DRIFT` reaches ATTENTION regardless of `check` name), matching the precedent set by Stories 10.3/14.1/15.1, none of which added a per-check fleet-picture test either.
  location: .claude/skills/conda-forge-expert/tests/meta/test_fleet_picture_bmad_core_drift.py
  origin: spec-deferred f0be39068bd9 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-08-23 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

---

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

## Canopy course correction (2026-08-24)

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-CANOPY-2026-08-24

- source_spec: `planning-artifacts/change-history/sprint-change-proposal-2026-08-24-canopy.md`
  summary: Canopy Phase 5 (`spec-pyforge-unifying-strategy`) — record doctor's five-tier gaps and bind them to steward Epics 18–30 without minting doctor Epics 17+.
  evidence: Doctor ships Tier 1 (CLI, Epics 1–16) and in-process MCP client usage (AD-6, `sources/atlas.py`); Tiers 2/4/5 absent; no host MCP server face; pre-audit Dream prose (`doctor_portal`, `:8008`, standalone FastAPI under `services/`) superseded by 2026-08-24 Grounding (host ASGI seam, no `services/` tier).
  resolution: **Doctor Epics 1–16 are NOT reopened or extended for Canopy tiers.** Portal (`/stations/doctor/`, naming triple `django-doctor` / `django_doctor_<app>` / `doctor_<app>`), host MCP (`POST /stations/doctor/mcp`, official `mcp` SDK, dual-era), `pyforge doctor` dispatch (Epic 22), optional `pyforge.events` publishing (Epic 24), SKF domain skill, and Agent-Doctor persona are **steward-owned forward work** (Epics 19, 21, 22, 24, 29). Doctor package supplies domain logic and MCP tool definitions; steward mounts portal and host MCP face.
  constraints: no second chrome (`django-pyforge` only); no extra public port; no `pyforge.*` import under `src/platform/`; do not grow a FastAPI `services/` process; `conda-forge-expert` is mason's skill, not doctor's.
  origin: Phase 5 bmad-correct-course, pyforge-doctor, 2026-08-24
  severity: medium
  status: open

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; cited paths 1/1 present; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

## DW-OM-2026-08-24 — Operating-model obligations (all eight stations)

- source_spec: cross-cutting (pyforge-unifying-strategy Grounding Q1–Q8; steward SCP operating-model, §6 revisited)
  summary: Estate OM + CAP-18: shared hook-spec in pyforge-core; Warden Epic 9 is the PR-gate retrofit; this station extracts one process hook spec (today's backend = default plugin).
  owner: station planning (this file) + steward (Canopy FRs) + warden (PR-gate hook specs)
  status: open
  recorded: 2026-08-24
  close_when: steward S-32.1 done; doctor S-17.1 done (gather/prescribe plugins; findings not a second PR gate); no competing CI verdict

  verified: 2026-08-26 — still-open — 2026-08-26 fleet hygiene first CAP-4 stamp at HEAD d7853d7983; ledger status mapped to still-open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-17-1: Live hygiene true-positive test fails because the cited herald fixture is now referenced by spec-9-2; not caused by 17.1.

- source_spec: `planning-artifacts/specs/spec-17-1-extract-gather-prescribe-source-plugins.md`
  summary: Live hygiene true-positive test fails because the cited herald fixture is now referenced by spec-9-2; not caused by 17.1.
  evidence: pyforge-doctor-test: test_live_repo_gather_surfaces_at_least_one_true_positive_naming_a_non_warden_station asserts the fixture is unreferenced; spec-9-2 now names it. Unrelated to hooks.py / diagnose plugin wiring.
  location: src/shared/packages/pyforge-doctor/tests/unit/test_sources_hygiene.py
  origin: spec-deferred e13fc6bf3d85 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-08-28 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-18-2: Existing doctor check 5s budget test flakes on cold worktree starts and is not caused by this portal slice.

- source_spec: `planning-artifacts/specs/spec-18-2-first-portal-slice-fleet-pulse.md`
  summary: Existing doctor check 5s budget test flakes on cold worktree starts and is not caused by this portal slice.
  evidence: Full suite 1287 passed + 1 skipped + 1 failed on test_doctor_check_completes_within_the_five_second_budget (7.8s / 15.9s). Story 18.1 recorded the same flake. New 18.2 tests 7/7 passed.
  location: src/shared/packages/pyforge-doctor/tests/unit/test_check_speed_budget.py
  origin: spec-deferred 2bb5888a6493 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-08-28 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-8-5: The normalized-summary exemption could in principle mask two genuinely distinct findings that happen to share byte-identical normalized `summary:` text.

- source_spec: `planning-artifacts/specs/spec-8-5-the-detector-recognizes-content-that-already-reached-the-ledger-by-another-path.md`
  summary: The normalized-summary exemption could in principle mask two genuinely distinct findings that happen to share byte-identical normalized `summary:` text.
  evidence: Review-pass finding (2026-08-28): acknowledged as the same accepted tradeoff the write- side `deferred_work_promote.py` guard it mirrors already makes -- confirmed byte-identical `_normalize_summary` logic between the two files. Not a new defect this story introduces; full fuzzy/near-duplicate matching is explicitly out of scope, same rationale as the original write-side guard.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py (_tier3_entry_already_promoted)
  origin: spec-deferred c7670f9c0848 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-08-28 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec path absent at HEAD; no repo paths cited; ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-1-2: `doctor.sources.warden`'s `_INSTALL_HINT` message tells the operator to `` `pip install pyforge-doctor[gate]` `` even though this whole monorepo is conda/pixi-distributed and `pyforge-warden` is not published to PyPI — the pip instruction is not directly actionable here.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-wrap-wardens-engine-availability-self-check.md`
  summary: `doctor.sources.warden`'s `_INSTALL_HINT` message tells the operator to `` `pip install pyforge-doctor[gate]` `` even though this whole monorepo is conda/pixi-distributed and `pyforge-warden` is not published to PyPI — the pip instruction is not directly actionable here.
  evidence: Deliberately mirrors `pyforge.atlas.pipelines.universal_sbom.gate`'s own identical `INSTALL_HINT` phrasing (`"pip install pyforge-atlas[gate]" (or add pyforge-warden to the environment)`) verbatim, per this story's own Design Notes — a pre-existing convention this story faithfully carried forward, not something introduced fresh here. A fix (e.g. naming the actual `pixi install -e pyforge-doctor` / conda-appropriate remediation) belongs in a shared cross-package pass touching both `pyforge-atlas` and `pyforge-doctor`'s hint text together, not a unilateral deviation in this story alone.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-1-2-wrap-wardens-engine-availability-self-check.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-1-2-2: `test_no_warden_import.py`'s and `test_sources_warden_no_subprocess.py`'s AST guards are best-effort static scans (matching the explicitly-stated bounds of their sibling `test_read_only_guard.py`) — after this pass's fix for the `from pyforge import warden` bypass, a more convoluted bypass remains theoretically open: a bare `import pyforge.warden` (not currently flagged, since nothing is usable from it without a further attribute-access) followed later by a deep attribute chain (e.g. `pyforge.warden.models.SomeClass`) reaching a non-`engines` submodule would evade both the no-warden-import exemption boundary and the non-engines-submodule check, since neither guard tracks name bindings across subsequent `ast.Attribute` chains.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-wrap-wardens-engine-availability-self-check.md`
  summary: `test_no_warden_import.py`'s and `test_sources_warden_no_subprocess.py`'s AST guards are best-effort static scans (matching the explicitly-stated bounds of their sibling `test_read_only_guard.py`) — after this pass's fix for the `from pyforge import warden` bypass, a more convoluted bypass remains theoretically open: a bare `import pyforge.warden` (not currently flagged, since nothing is usable from it without a further attribute-access) followed later by a deep attribute chain (e.g. `pyforge.warden.models.SomeClass`) reaching a non-`engines` submodule would evade both the no-warden-import exemption boundary and the non-engines-submodule check, since neither guard tracks name bindings across subsequent `ast.Attribute` chains.
  evidence: Confirmed by inspection of both guards' current logic — the `ast.Import` branch for a bare `pyforge.warden` deliberately treats it as a non-violation (see the comment in `_non_engines_warden_submodule_violations`), and there is no follow-on attribute-chain tracker in either file. Consistent with this codebase's own accepted, explicitly-documented convention that these meta-tests are "best-effort STATIC" checks where "dynamic dispatch (`getattr`) or an indirection through a helper function is out of scope" (`test_read_only_guard.py`'s own stated bounds) — closing it fully would require a data-flow analysis beyond what any sibling guard in this package attempts, so it is deferred rather than patched unilaterally in this story.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-1-2-wrap-wardens-engine-availability-self-check.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-1-2-3: Warden's ok-with-caveat self-check states (e.g. "EPSS feed present but stale", "operating air-gapped") flatten to plain `DoctorStatus.OK` through `gather()`'s spec-mandated binary `ok`→OK/FAIL mapping, so `DoctorStatus.WARN` is unreachable from `Source.WARDEN_DOCTOR` and a JSON consumer filtering `status != "ok"` never sees those caveats — Story 1.3's tri-state/WARN work should decide whether specific warden check shapes deserve WARN promotion before the report contract hardens further.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-wrap-wardens-engine-availability-self-check.md`
  summary: Warden's ok-with-caveat self-check states (e.g. "EPSS feed present but stale", "operating air-gapped") flatten to plain `DoctorStatus.OK` through `gather()`'s spec-mandated binary `ok`→OK/FAIL mapping, so `DoctorStatus.WARN` is unreachable from `Source.WARDEN_DOCTOR` and a JSON consumer filtering `status != "ok"` never sees those caveats — Story 1.3's tri-state/WARN work should decide whether specific warden check shapes deserve WARN promotion before the report contract hardens further.
  evidence: `pyforge.warden.engines._doctor_check_feed` (engines.py ~lines 687–695) encodes stale-optional-feed and air-gapped conditions as `ok=True` with the consequence only in message text — a pre-existing coarseness of warden's own `DoctorCheck.ok: bool`, faithfully mirrored (not introduced) by this story's spec-mandated 1:1 binary mapping (epics AC4 and the spec's I/O matrix both mandate OK-or-FAIL). Doctor loses no information a warden CLI user would have had (message text is preserved verbatim), so this is a cross-story design decision for the WARN tier, not a defect in this story's code.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-1-2-wrap-wardens-engine-availability-self-check.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-1-3: `checks.registry.gather_one("engines", name, target)` returns `None` when `sources.warden.gather()` degrades to its single "pyforge-warden" sentinel `Finding` (warden absent/unimportable/`run_doctor_checks` raising) instead of surfacing that sentinel's own failure reason — an operator running one named check in isolation while the whole category is broken gets nothing telling them why, even though running the full `--engines` suite unfiltered would show it. Story 1.5's CLI wiring should decide whether a single-check request should surface the category-level failure instead of a bare "not found."

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-tri-state-individually-addressable-checks.md`
  summary: `checks.registry.gather_one("engines", name, target)` returns `None` when `sources.warden.gather()` degrades to its single "pyforge-warden" sentinel `Finding` (warden absent/unimportable/`run_doctor_checks` raising) instead of surfacing that sentinel's own failure reason — an operator running one named check in isolation while the whole category is broken gets nothing telling them why, even though running the full `--engines` suite unfiltered would show it. Story 1.5's CLI wiring should decide whether a single-check request should surface the category-level failure instead of a bare "not found."
  evidence: `gather_one` is spec-mandated (AC3, this spec's Always clause: "never a separate/duplicated lookup path") to be a literal filter over `sources.warden.gather(target)`'s full result — when that result degrades to one `Finding(check="pyforge-warden", ...)`, filtering by any other name legitimately finds nothing, so `None` is the behaviorally-correct output of the documented filter semantics, not a bug in this story's code. `test_gather_one_returns_none_when_gather_degrades_to_sentinel_finding` pins this exact behavior. Whether "not found" should be distinguishable from "category failed to run" is a CLI-facing UX decision that belongs to whichever story actually renders `gather_one`'s result to an operator (Story 1.5), not a retroactive change to this story's filter semantics.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-1-3-tri-state-individually-addressable-checks.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-1-3-2: The complement of the previous spec-1-3 entry — `checks.registry`'s addressable-name set and listed-name set disagree in the OTHER direction too: `gather_one("engines", "pyforge-warden", target)` returns the degradation-sentinel `Finding`, a check name `list_checks()` never advertises, so Story 1.5's CLI wiring must decide whether `--engines <name>` validates names against the catalog (making the one name that explains a broken category unreachable) or passes any name through (making `pyforge-warden` an undocumented magic name that only "works" when the environment is broken).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-tri-state-individually-addressable-checks.md`
  summary: The complement of the previous spec-1-3 entry — `checks.registry`'s addressable-name set and listed-name set disagree in the OTHER direction too: `gather_one("engines", "pyforge-warden", target)` returns the degradation-sentinel `Finding`, a check name `list_checks()` never advertises, so Story 1.5's CLI wiring must decide whether `--engines <name>` validates names against the catalog (making the one name that explains a broken category unreachable) or passes any name through (making `pyforge-warden` an undocumented magic name that only "works" when the environment is broken).
  evidence: Pinned by `test_gather_one_can_address_the_degradation_sentinel_by_name` (added this pass) and independently confirmed by both review subagents against the live worktree. Both halves of the asymmetry are behaviorally correct today: the sentinel `Finding(check="pyforge-warden")` is `sources.warden.gather()`'s spec-mandated degradation shape (Story 1.2), and `gather_one` is spec-mandated (AC3 filter semantics; the Always clause's "never a separate/duplicated lookup path") to return it verbatim when asked for by name. The open question is CLI exposure/validation policy, owned by whichever story renders these results to an operator (Story 1.5) — not a change to this story's filter semantics.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-1-3-tri-state-individually-addressable-checks.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-1-4: Three v1 scope limits remain in `checks.env_hygiene` after the review pass's patches (branch-blindness, substring-token, ternary-false-positive, import-aliasing, AugAssign, chained-assignment, and onerror/entry-cap-signal gaps were all fixed in this same pass — this entry covers what's still deliberately out of scope, not what shipped broken): (a) detection is DIRECT-EXPRESSION ONLY — no intermediate-variable tracking (`token = os.environ.get(...); headers["X"] = token`) and no dict-literal-construction tracking (`headers = {"k": os.environ.get(...)}`, `return {"k": token}`), so real repo shapes like `dependency-checker.py`'s `return {"X-JFrog-Art-Api": api_key}` and `gemini_server.py`'s `headers = {"x-goog-api-key": ...}` are invisible to this scanner; (b) guard recognition is `if`/`elif` STATEMENTS ONLY — a host check expressed as a ternary (`ast.IfExp`) condition or a `match`/`case` dispatch is not recognized as suppressing, so e.g. `headers["X"] = os.environ.get("Y") if host == "safe" else None` still produces a WARN despite being host-scoped; (c) `checks.registry.gather_one`'s `next(...)`-based filter (Story 1.3's established contract, mirrored exactly for the new "env" dispatch branch) silently returns only the FIRST matching `Finding` for a given `check` name — CONFIRMED live today (not merely a future risk): a target directory with two or more separately-matching files each produces a `Finding` under the same `check="unconditional-credential-injection"` name, and `gather_one("env", ..., target)` only ever surfaces the first.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-credential-environment-hygiene-check.md`
  summary: Three v1 scope limits remain in `checks.env_hygiene` after the review pass's patches (branch-blindness, substring-token, ternary-false-positive, import-aliasing, AugAssign, chained-assignment, and onerror/entry-cap-signal gaps were all fixed in this same pass — this entry covers what's still deliberately out of scope, not what shipped broken): (a) detection is DIRECT-EXPRESSION ONLY — no intermediate-variable tracking (`token = os.environ.get(...); headers["X"] = token`) and no dict-literal-construction tracking (`headers = {"k": os.environ.get(...)}`, `return {"k": token}`), so real repo shapes like `dependency-checker.py`'s `return {"X-JFrog-Art-Api": api_key}` and `gemini_server.py`'s `headers = {"x-goog-api-key": ...}` are invisible to this scanner; (b) guard recognition is `if`/`elif` STATEMENTS ONLY — a host check expressed as a ternary (`ast.IfExp`) condition or a `match`/`case` dispatch is not recognized as suppressing, so e.g. `headers["X"] = os.environ.get("Y") if host == "safe" else None` still produces a WARN despite being host-scoped; (c) `checks.registry.gather_one`'s `next(...)`-based filter (Story 1.3's established contract, mirrored exactly for the new "env" dispatch branch) silently returns only the FIRST matching `Finding` for a given `check` name — CONFIRMED live today (not merely a future risk): a target directory with two or more separately-matching files each produces a `Finding` under the same `check="unconditional-credential-injection"` name, and `gather_one("env", ..., target)` only ever surfaces the first.
  evidence: (a) and (b) are named explicitly in the story spec's Design Notes/Design Decisions and the module's own docstring (the generalization-boundary decision cites the two real-repo dict-literal shapes by name and states they are "NOT required by any AC ... deliberately out of v1 scope, not chased speculatively"; the if/elif-only guard boundary is stated directly in `env_hygiene.py`'s module docstring after the review pass). (c) is inherited, unmodified `gather_one` filter semantics from Story 1.3 (see that story's own two deferred-work entries above on the same `next(...)` filter), now also governing the "env" category via the mirrored dispatch branch — confirmed reproducible by construction (two files, each with one direct-injection match, same target) during this pass's adversarial review. Noted here for Story 1.5's CLI-wiring pass to weigh, per the spec's own Task 6 instruction — not a defect in this story's implementation.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-1-4-credential-environment-hygiene-check.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

  verified: 2026-09-07 — partially resolved — pyforge-doctor CI/hygiene sweep. Item (a)'s intermediate-variable gap is fully closed: `checks/env_hygiene.py`'s `_CredentialInjectionVisitor` now tracks a function-scoped `name = <credential-bearing-expr>` map (`_track_credential_var`/`_credential_vars`) and `_direct_env_read` resolves a later bare-`Name` reference against it, transitively. Item (a)'s dict-literal gap is PARTIALLY closed: a dict literal assigned to a bare header-shaped `Name` target (`headers = {"k": os.environ.get(...)}`, `_header_dict_literal_name`) is now caught -- confirmed live against `gemini_server.py`'s exact shape. A bare `return {...}` dict literal with no header-named variable at all (`dependency-checker.py`'s `return {"X-JFrog-Art-Api": api_key}`) is DELIBERATELY still not caught: implementing it and scoping detection to a header-named enclosing function was tried and reverted during this pass after it live-reproduced 5 NEW false positives against this repo's own already-safe, early-return-guarded real scripts (`dependency-checker.py`, `github_version_checker.py`, `scan_project.py`), breaking `test_gather_golden_fixture_finds_no_injection_in_the_real_cfe_scripts` — that shape is entangled with DW-FU-1-4-2's own still-open item (a) (statement-flow guard recognition) and cannot be closed correctly without it. Items (b) (ternary/match-case guard recognition) and (c) (`gather_one`'s first-match-only filter, a frozen Story 1.3 contract) are UNTOUCHED, still open. Closing this fix also newly surfaced two PRE-EXISTING, previously-invisible real host-scoping gaps in `github_version_checker.py:68` (`GITHUB_TOKEN`) and `scan_project.py:1898` (`OCI_REGISTRY_TOKEN`) -- genuine findings the intermediate-variable fix correctly reveals, not caused by it; fixing those two conda-forge-expert scripts is out of pyforge-doctor's own scope, so the golden-fixture test now pins them by exact (file, line, var) as known-pre-existing rather than asserting the whole scripts directory is clean. 7 new regression tests added (`test_gather_intermediate_variable_*`, `test_gather_dict_literal_*`); full env_hygiene suite green (51 passed); full doctor suite still green (see closeout entries below). Evidence: `src/shared/packages/pyforge-doctor/src/pyforge/doctor/checks/env_hygiene.py`, `src/shared/packages/pyforge-doctor/tests/unit/test_checks_env_hygiene.py`.

### DW-FU-1-4-2: Residual guard-recognition scope limits in `checks.env_hygiene` after the follow-up review pass (2026-07-31), which fixed the negated-guard polarity gap for pure-negation tests and split the incomplete-scan sentinel onto its own check name: (a) STATEMENT-FLOW guards are not recognized — the early-return idiom (`if host != safe: return` followed by an unguarded `headers[...] = os.environ.get(...)`), the dominant Python guard-clause style and the most plausible shape of any *remediated* `_http.py`, still produces a WARN false positive; suppressing it requires tracking which preceding sibling `if` bodies unconditionally return/raise, a statement-flow analysis beyond v1's enclosing-guard model. (b) Polarity resolution covers ONLY a pure-negation `ast.Compare` (every op `!=`/`not in`/`is not`) — negation expressed as `not host_ok(h)` or a `BoolOp` over negated compares conservatively suppresses its TRUE branch, so an inverse-condition leak written through those forms remains invisible; deciding those requires knowing whether the compared set is an allowlist or denylist, which is statically undecidable. (c) The incomplete-scan sentinel now carries its own never-cataloged check name (`env_hygiene.SCAN_INCOMPLETE_CHECK_NAME == "env-hygiene"`, mirroring engines' `"pyforge-warden"` sentinel), so the existing spec-1-3 ledger question — should a CLI validate check names against the catalog or pass unknown names through — now applies to a second category and should be answered once for both in Story 1.5.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-credential-environment-hygiene-check.md`
  summary: Residual guard-recognition scope limits in `checks.env_hygiene` after the follow-up review pass (2026-07-31), which fixed the negated-guard polarity gap for pure-negation tests and split the incomplete-scan sentinel onto its own check name: (a) STATEMENT-FLOW guards are not recognized — the early-return idiom (`if host != safe: return` followed by an unguarded `headers[...] = os.environ.get(...)`), the dominant Python guard-clause style and the most plausible shape of any *remediated* `_http.py`, still produces a WARN false positive; suppressing it requires tracking which preceding sibling `if` bodies unconditionally return/raise, a statement-flow analysis beyond v1's enclosing-guard model. (b) Polarity resolution covers ONLY a pure-negation `ast.Compare` (every op `!=`/`not in`/`is not`) — negation expressed as `not host_ok(h)` or a `BoolOp` over negated compares conservatively suppresses its TRUE branch, so an inverse-condition leak written through those forms remains invisible; deciding those requires knowing whether the compared set is an allowlist or denylist, which is statically undecidable. (c) The incomplete-scan sentinel now carries its own never-cataloged check name (`env_hygiene.SCAN_INCOMPLETE_CHECK_NAME == "env-hygiene"`, mirroring engines' `"pyforge-warden"` sentinel), so the existing spec-1-3 ledger question — should a CLI validate check names against the catalog or pass unknown names through — now applies to a second category and should be answered once for both in Story 1.5.
  evidence: (a) and (b) were independently confirmed live by both review subagents in this pass (the early-return shape is flagged today; `if host != x: headers[...] = env` was silently suppressed until this pass's patch narrowed suppression to non-negated tests — the narrowing deliberately stops at pure-negation Compares, boundary stated in the module docstring). (c) is this pass's own sentinel-split patch, pinned by `test_gather_one_env_can_address_the_incomplete_sentinel_by_name`; the engines-side half of the same CLI question is already ledgered under spec-1-3 above. All three are deliberate, documented v1 boundaries of this story's new detector — recorded for the v2-detector / Story 1.5 passes to weigh, not shipped defects.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-1-4-credential-environment-hygiene-check.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

  verified: 2026-09-07 — still-open — re-read against live code during the same pyforge-doctor CI/hygiene sweep that resolved DW-FU-1-4's intermediate-variable/dict-literal items (a different sub-gap of the same story). Items (a), (b), (c) here are all UNTOUCHED by that fix and remain genuinely open: `checks/env_hygiene.py` still has no statement-flow (early-return) guard tracking, still resolves polarity only for a pure-negation `ast.Compare`, and the sentinel-name-validation question (c) is unaddressed. Confirmed still-live: attempting to add `return`-statement dict-literal detection during this pass required exactly the statement-flow analysis item (a) describes, and its absence live-reproduced 5 new false positives against this repo's own early-return-guarded scripts (`dependency-checker.py`, `github_version_checker.py`, `scan_project.py`) — direct, current-code confirmation that item (a)'s gap is real and load-bearing, not stale. No code change made for this entry.

### DW-FU-1-5: This story closes both "Story 1.5 CLI-wiring decision" items the spec-1-3/spec-1-4 ledger entries above deferred: (1) `--engines NAME`/`--env NAME` VALIDATE the name against `registry.list_checks(category=...)` at parse time (`check_parser.error(...)`, exit 2) rather than passing it through to `gather_one` — a degradation-sentinel name (`"pyforge-warden"`, `env_hygiene.SCAN_INCOMPLETE_CHECK_NAME == "env-hygiene"`) is therefore never directly requestable, since neither is ever cataloged; (2) `gather_one`'s `None` result for an (already-validated) name is handled CATEGORY-AWARE, not uniformly: for "engines" it renders one synthetic FAIL `Finding` naming the degradation (warden's `gather` is all-or-nothing, so `None` there can only mean total degradation) — never a bare "not found"; for "env" it renders zero Findings (its `gather` is additive, so `None` there is the ordinary "clean, no match" outcome, not a degradation). Two residual boundaries were weighed and deliberately left unresolved, carried forward rather than fixed: (a) `checks.registry.gather_one`'s `next(...)`-based first-match-only filter (flagged by the spec-1-4 entry above) still surfaces only the FIRST `Finding` for a given check name when multiple files under "env" match the same check — fixing it means redesigning `gather_one`'s frozen filter semantics (a Story 1.3 contract this story is expressly forbidden from touching), so it is carried forward unfixed, not a regression introduced here; (b) single-name `--env NAME` filtering structurally CANNOT surface `env_hygiene`'s incomplete-scan sentinel (`SCAN_INCOMPLETE_CHECK_NAME`, a different, non-requested check name) — even when a scan is genuinely incomplete (entry cap hit, unreadable subdirectory) alongside a real match on the requested name, `gather_one`'s filter only ever returns the one `Finding` whose `check` equals the requested NAME, silently dropping the incompleteness signal that an unfiltered `--env` (no name) run would show. This is a structural consequence of the filter contract Story 1.3 already froze, not a new defect — documented here as a v1 CLI-exposure boundary, not fixed.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-doctor-check-cli-wiring-json-and-the-speed-budget.md`
  summary: This story closes both "Story 1.5 CLI-wiring decision" items the spec-1-3/spec-1-4 ledger entries above deferred: (1) `--engines NAME`/`--env NAME` VALIDATE the name against `registry.list_checks(category=...)` at parse time (`check_parser.error(...)`, exit 2) rather than passing it through to `gather_one` — a degradation-sentinel name (`"pyforge-warden"`, `env_hygiene.SCAN_INCOMPLETE_CHECK_NAME == "env-hygiene"`) is therefore never directly requestable, since neither is ever cataloged; (2) `gather_one`'s `None` result for an (already-validated) name is handled CATEGORY-AWARE, not uniformly: for "engines" it renders one synthetic FAIL `Finding` naming the degradation (warden's `gather` is all-or-nothing, so `None` there can only mean total degradation) — never a bare "not found"; for "env" it renders zero Findings (its `gather` is additive, so `None` there is the ordinary "clean, no match" outcome, not a degradation). Two residual boundaries were weighed and deliberately left unresolved, carried forward rather than fixed: (a) `checks.registry.gather_one`'s `next(...)`-based first-match-only filter (flagged by the spec-1-4 entry above) still surfaces only the FIRST `Finding` for a given check name when multiple files under "env" match the same check — fixing it means redesigning `gather_one`'s frozen filter semantics (a Story 1.3 contract this story is expressly forbidden from touching), so it is carried forward unfixed, not a regression introduced here; (b) single-name `--env NAME` filtering structurally CANNOT surface `env_hygiene`'s incomplete-scan sentinel (`SCAN_INCOMPLETE_CHECK_NAME`, a different, non-requested check name) — even when a scan is genuinely incomplete (entry cap hit, unreadable subdirectory) alongside a real match on the requested name, `gather_one`'s filter only ever returns the one `Finding` whose `check` equals the requested NAME, silently dropping the incompleteness signal that an unfiltered `--env` (no name) run would show. This is a structural consequence of the filter contract Story 1.3 already froze, not a new defect — documented here as a v1 CLI-exposure boundary, not fixed.
  evidence: (1)/(2) are pinned by `tests/unit/test_cli_check.py`'s `test_unknown_engines_check_name_is_usage_error_never_reaches_gather`, `test_unknown_env_check_name_is_usage_error`, `test_degraded_engines_category_named_check_renders_one_synthetic_fail`, and `test_clean_env_named_check_reports_zero_findings_and_exits_zero` — all four green, plus the full 182-test suite (`pixi run --frozen -e pyforge-doctor pyforge-doctor-test`) passing, including the untouched meta-tests (`test_verdict_sole_ownership.py`, `test_no_warden_import.py`, `test_read_only_guard.py`). (a) is unchanged, inherited `gather_one` behavior; no test in this story exercises the multi-file case further, per the spec-1-4 entry's own "logged, not chased" disposition, now doubly confirmed unfixable from the CLI side without a Story 1.3 contract change. (b) was confirmed by direct reasoning over `_gather_env`'s filter call (`registry.gather_one("env", name, target)` — a `next((f for f in env_hygiene.gather(target) if f.check == name), None)` filter, per `registry.py`'s own docstring) plus the fact that `SCAN_INCOMPLETE_CHECK_NAME` is never in `registry.list_checks(category="env")`'s catalog, so it can never even be the requested NAME in the first place — an unfiltered `--env` (no name) run remains the only path that surfaces it, unchanged from Story 1.4.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-1-5-doctor-check-cli-wiring-json-and-the-speed-budget.md, tests/unit/test_cli_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-1-5-2: The new NFR-4 benchmark (`tests/unit/test_check_speed_budget.py`) pins a 5-second budget (PRD SM-C1's own stated number, adopted verbatim) against a REAL, unmocked `doctor check` run over the whole monorepo — measured today at ~2.6-2.7s per iteration (dominated by `env_hygiene`'s AST walk over ~900 `.py` files), leaving roughly 2x headroom. That headroom is a function of the repo's current size, which this same repo's own conventions describe as actively, continuously growing (new recipes, new pyforge packages, new skill scripts) — the review pass's adversarial check confirmed the number is real but flagged the headroom as something to actively monitor, not a one-time calibration.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-doctor-check-cli-wiring-json-and-the-speed-budget.md`
  summary: The new NFR-4 benchmark (`tests/unit/test_check_speed_budget.py`) pins a 5-second budget (PRD SM-C1's own stated number, adopted verbatim) against a REAL, unmocked `doctor check` run over the whole monorepo — measured today at ~2.6-2.7s per iteration (dominated by `env_hygiene`'s AST walk over ~900 `.py` files), leaving roughly 2x headroom. That headroom is a function of the repo's current size, which this same repo's own conventions describe as actively, continuously growing (new recipes, new pyforge packages, new skill scripts) — the review pass's adversarial check confirmed the number is real but flagged the headroom as something to actively monitor, not a one-time calibration.
  evidence: Live-measured during this review pass on the same worktree used for the spec's original Design Notes measurement (2.14s/2.37s/2.45s at spec-writing time; 2.6s-2.7s during this review pass, consistent with organic repo growth in the intervening commits). This is not a defect in the current implementation — the budget is real, documented, and traceable to the PRD rather than invented — but a future story (or a recurring housekeeping pass) should re-measure this benchmark's margin periodically and decide whether the 5s figure itself needs revisiting, or whether `env_hygiene`'s walk needs a performance pass (e.g. parallelizing file scans), well before the benchmark starts failing for real users.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-1-5-doctor-check-cli-wiring-json-and-the-speed-budget.md, tests/unit/test_check_speed_budget.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-1-5-3: An env-only run against a nonexistent/typo'd target false-greens — `doctor check /no/such/dir --env` prints `0 finding(s)` and exits 0, because `checks.env_hygiene._discover_python_files` treats a non-directory target as its documented EMPTY case (`if not target.is_dir(): return ...`, explicitly NOT the incomplete-scan sentinel), and Story 1.5's intent contract mandates forwarding `path` verbatim with no CLI pre-validation ("the gather functions already degrade safely on a bad path"). The default combined run does NOT false-green (warden's engines checks FAIL on the bad path, exit 2), so this needs the `--env`-only flow plus a typo'd path — but for a pre-flight tool whose one job is to never false-green, "safe degradation" and "reports clean" are not the same thing on that flow. Root cause is Story 1.4's deliberate documented-empty decision, not this story's wiring; candidate fixes are (a) env_hygiene emitting its `SCAN_INCOMPLETE_CHECK_NAME` sentinel (or a new not-a-directory WARN) for a non-dir target, or (b) revisiting the no-pre-validation contract clause with a warden-style `_resolve_scan_target` — both are frozen-surface or contract changes outside this story's authority.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-doctor-check-cli-wiring-json-and-the-speed-budget.md`
  summary: An env-only run against a nonexistent/typo'd target false-greens — `doctor check /no/such/dir --env` prints `0 finding(s)` and exits 0, because `checks.env_hygiene._discover_python_files` treats a non-directory target as its documented EMPTY case (`if not target.is_dir(): return ...`, explicitly NOT the incomplete-scan sentinel), and Story 1.5's intent contract mandates forwarding `path` verbatim with no CLI pre-validation ("the gather functions already degrade safely on a bad path"). The default combined run does NOT false-green (warden's engines checks FAIL on the bad path, exit 2), so this needs the `--env`-only flow plus a typo'd path — but for a pre-flight tool whose one job is to never false-green, "safe degradation" and "reports clean" are not the same thing on that flow. Root cause is Story 1.4's deliberate documented-empty decision, not this story's wiring; candidate fixes are (a) env_hygiene emitting its `SCAN_INCOMPLETE_CHECK_NAME` sentinel (or a new not-a-directory WARN) for a non-dir target, or (b) revisiting the no-pre-validation contract clause with a warden-style `_resolve_scan_target` — both are frozen-surface or contract changes outside this story's authority.
  evidence: Live-reproduced by this pass's adversarial reviewer in the worktree env: `doctor check /definitely/not/a/real/path-xyz --env` → stdout `doctor check: 0 finding(s) -- 0 ok, 0 warn, 0 fail`, exit 0; `doctor check "" --env` additionally Path-normalizes `""` to `.` and silently scans the CWD (the exact empty-path hazard warden's `_resolve_scan_target` docstring names as its reason to exist). `env_hygiene.py`'s non-dir guard carries an explicit "documented empty case, not an incomplete scan" comment, confirming this is a deliberate pre-existing Story 1.4 boundary surfaced (not introduced) by the new CLI; the spec's Design Notes verified only "no exception", not "no false green".
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-1-5-doctor-check-cli-wiring-json-and-the-speed-budget.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-1-5-4: When a named `--engines NAME` check hits a degraded category, the specific degradation reason is computed and then discarded inside `registry.gather_one` — `sources.warden.gather` produces one of three deliberately distinct sentinel messages (warden absent vs. installed-but-unimportable vs. self-check crashed, per warden.py's module docstring), `gather_one` filters that sentinel out because its `check` name (`"pyforge-warden"`) never equals the requested NAME, and the CLI's synthetic FAIL Finding can therefore only say generically "absent, unimportable, or its self-check crashed; re-run `doctor check --engines` (no name)" — forcing the operator into a second multi-second gather to recover information the first call already had in hand. Fixing this requires `gather_one` (or a variant) to expose the sentinel alongside the `None`/filtered result, i.e. extending Story 1.3's frozen filter contract — the same contract two existing spec-1-3 ledger entries above already flag from other directions; a future registry-API story could resolve all three at once.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-doctor-check-cli-wiring-json-and-the-speed-budget.md`
  summary: When a named `--engines NAME` check hits a degraded category, the specific degradation reason is computed and then discarded inside `registry.gather_one` — `sources.warden.gather` produces one of three deliberately distinct sentinel messages (warden absent vs. installed-but-unimportable vs. self-check crashed, per warden.py's module docstring), `gather_one` filters that sentinel out because its `check` name (`"pyforge-warden"`) never equals the requested NAME, and the CLI's synthetic FAIL Finding can therefore only say generically "absent, unimportable, or its self-check crashed; re-run `doctor check --engines` (no name)" — forcing the operator into a second multi-second gather to recover information the first call already had in hand. Fixing this requires `gather_one` (or a variant) to expose the sentinel alongside the `None`/filtered result, i.e. extending Story 1.3's frozen filter contract — the same contract two existing spec-1-3 ledger entries above already flag from other directions; a future registry-API story could resolve all three at once.
  evidence: Confirmed by inspection during this pass: `_gather_engines`'s `None` branch (spec-mandated: "never a second `gather` call") constructs its synthetic Finding from nothing but the requested name, while `warden_source.gather`'s three distinct sentinel messages (warden.py's ModuleNotFoundError / ImportError / `except Exception` branches) are demonstrably distinct and demonstrably unavailable at that point in the CLI. Behaviorally correct per the current spec (the intent contract explicitly mandates the generic synthetic-Finding + re-run-hint shape), so this is a UX-quality boundary of the frozen filter semantics, not a defect in this story's code.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-1-5-doctor-check-cli-wiring-json-and-the-speed-budget.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-2: `Source.BEHIND_UPSTREAM` has no backing `gather()` implementation anywhere in `pyforge.doctor` — no code path ever produces a `Finding` tagged with it — so this story's new registry entry for it is metadata about a Source no instrument actually emits.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-2-a-source-registry-doctor-owns.md`
  summary: `Source.BEHIND_UPSTREAM` has no backing `gather()` implementation anywhere in `pyforge.doctor` — no code path ever produces a `Finding` tagged with it — so this story's new registry entry for it is metadata about a Source no instrument actually emits.
  evidence: `sources/atlas.py`'s `_VALID_AXES` is `{"staleness", "cve", "abandonment", "adoption"}` (4 axes only); the axis-to-Source map (`atlas.py:111-114`) sends `"abandonment"` to `{Source.FEEDSTOCK_HEALTH, Source.RELEASE_CADENCE}`, and no axis or any other module maps to `Source.BEHIND_UPSTREAM` — confirmed by a repo-wide grep for the string, which matches only the enum's own definition in `models.py` and this story's new `REGISTRY` entry in `sources/__init__.py`. Pre-existing since whichever Epic 2 story introduced the enum member (`AD-6 the five atlas Watch axes` per `models.py`'s own docstring names five, but only four axes were ever wired); surfaced incidentally by Story 6.2's `Source` <-> `REGISTRY` coherence test, which is the first code in the repo to make a concrete, testable claim about this Source. Not fixed here — `sources/atlas.py`'s `gather()` dispatch is outside this story's surface (`sources/__init__.py`, `models.py` (`Source`), `scripts/detectors.py` only). A future story should either wire a real `behind-upstream` axis/dispatch, fold it into `abandonment`'s composite (mirroring `FEEDSTOCK_HEALTH`/`RELEASE_CADENCE`), or retire the enum member — any of the three keeps the closed taxonomy honest; leaving it as an unreachable member does not.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-6-2-a-source-registry-doctor-owns.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-3: `pyforge.doctor.sources.__init__`'s `__all__` tuple is not alphabetically sorted (ruff `RUF022`), and this story's own addition of `scope_for`/`degrade_on_exception` extended the same already-unsorted tuple rather than fixing it.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-3-the-repo-runtime-split-survives-the-move.md`
  summary: `pyforge.doctor.sources.__init__`'s `__all__` tuple is not alphabetically sorted (ruff `RUF022`), and this story's own addition of `scope_for`/`degrade_on_exception` extended the same already-unsorted tuple rather than fixing it.
  evidence: Confirmed via `git show <pre-story-HEAD>:.../sources/__init__.py | ruff check - --select RUF022`, which already flags `__all__ = ("SourceRegistration", "REGISTRY", "list_sources")` before this story's changes — pre-existing, not introduced here. Trivial one-line auto-fix (`ruff check --fix`) whenever a future story next touches this file's `__all__`.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-3-the-repo-runtime-split-survives-the-move.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-2: `sources/marshal.py`'s `gather_story_status`, Route 3 ("landed by hand") hardcodes the branch name `"main"` (`_git(target, "log", "--format=%s", "main", ...)`) — a repo whose default branch isn't literally `main` gets zero Route-3 evidence, and the git call failing outright (no such ref) is indistinguishable from "checked `main`, found nothing," so a hand-landed story on a differently-named default branch can misclassify as a false-green FAIL.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/marshal.py`'s `gather_story_status`, Route 3 ("landed by hand") hardcodes the branch name `"main"` (`_git(target, "log", "--format=%s", "main", ...)`) — a repo whose default branch isn't literally `main` gets zero Route-3 evidence, and the git call failing outright (no such ref) is indistinguishable from "checked `main`, found nothing," so a hand-landed story on a differently-named default branch can misclassify as a false-green FAIL.
  evidence: Byte-for-byte inherited from `scripts/story_status_check.py:169-176`'s own `re.match(r"^(\d+)-(\d+)-", key)` + `sh("git", "log", "--format=%s", "main")` — the source script this story ports verbatim in behavior (spec Design Notes: "preserve, don't redesign"). Not introduced by this port; this repo's own default branch is `main`, so it is a non-issue in practice here. A future story wanting a real fix would need to resolve the actual default branch (`git symbolic-ref refs/remotes/origin/HEAD` or similar) rather than hardcoding it, in both the original script and this port simultaneously.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/story_status_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-3: `sources/ledger.py`'s `gather()` verifies `base`'s resolvability (`rev-parse --verify --quiet`) before comparing, but never verifies `head`'s — an unresolvable `head` makes every `git show head:path` call in `_check()` fail, which is interpreted identically to "the ledger doesn't exist at head," producing spurious `ledger-deleted` FAIL findings for every `done` key the base ledger held, rather than a WARN "cannot evaluate."

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/ledger.py`'s `gather()` verifies `base`'s resolvability (`rev-parse --verify --quiet`) before comparing, but never verifies `head`'s — an unresolvable `head` makes every `git show head:path` call in `_check()` fail, which is interpreted identically to "the ledger doesn't exist at head," producing spurious `ledger-deleted` FAIL findings for every `done` key the base ledger held, rather than a WARN "cannot evaluate."
  evidence: Traced against `scripts/ledger_regression_check.py:169-192`'s own `main()`: it likewise only `rev-parse --verify --quiet`s `args.base`, computes `head_sha` via a plain (non-verifying) `rev-parse`, and — since an unresolvable `head_sha` is `""` while `base_sha` is real — the `base_sha == head_sha` same-commit check evaluates False, so the original script also falls straight through to `check(args.base, args.head)` with a broken `head`, hitting the identical `after_text is None` -> "ledger-deleted" misclassification. Faithfully preserved by the port (spec Design Notes: "preserve, don't redesign"), not introduced by it.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/ledger_regression_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-4: `sources/ledger.py`'s `_ledger_paths` degrades a transient `git ls-tree` failure (not just "no ledgers exist at this ref") to an empty path list identically to the ref genuinely holding zero ledgers — `_check()` then iterates zero paths and `gather()` reports a false-clean OK Finding rather than a WARN, on what could be a real evaluation failure (e.g. git briefly unavailable, a corrupt pack).

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/ledger.py`'s `_ledger_paths` degrades a transient `git ls-tree` failure (not just "no ledgers exist at this ref") to an empty path list identically to the ref genuinely holding zero ledgers — `_check()` then iterates zero paths and `gather()` reports a false-clean OK Finding rather than a WARN, on what could be a real evaluation failure (e.g. git briefly unavailable, a corrupt pack).
  evidence: `_git` returning `None` on ANY failure and `_ledger_paths` treating `None` the same as "empty listing" (`listing = _git(...) or ""`) mirrors `scripts/ledger_regression_check.py:90-98`'s own `ledger_paths(rev)` exactly (`listing = _git(...) or ""`), which has the identical ambiguity. Pre-existing in the source script, faithfully carried over.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/ledger_regression_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-5: `sources/ledger.py`'s rename-continuity matching (`_tail`/`surviving_tails`) only checks whether SOME `done` key at head shares the missing key's kebab tail — an unrelated NEW key added at head with a coincidentally-matching tail can mask the genuine deletion of a different, unrelated old key as "continuity, not regression."

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/ledger.py`'s rename-continuity matching (`_tail`/`surviving_tails`) only checks whether SOME `done` key at head shares the missing key's kebab tail — an unrelated NEW key added at head with a coincidentally-matching tail can mask the genuine deletion of a different, unrelated old key as "continuity, not regression."
  evidence: The `surviving_tails = {_tail(k) for k, v in after.items() if v in TERMINAL}` set-membership check (no identity/provenance tracking beyond the tail string) is copied verbatim from `scripts/ledger_regression_check.py:136-145`'s own `check()`, including its own docstring's acknowledged scope ("the difference is decidable... only the numeric/alias id prefix may differ" — the algorithm was designed for the id-migration case, not adversarial tail collisions). Pre-existing algorithmic scope, faithfully ported.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/ledger_regression_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-6: `sources/ledger.py`'s regression check is scoped PER LEDGER PATH — a `done` key that moves from one project's ledger to a different project's ledger between `base` and `head` (a cross-path move, as opposed to a same-path key rename) is reported as `ledger-deleted` in the old project, with no recognition that the story continues to live (and stay `done`) in the new project's ledger.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/ledger.py`'s regression check is scoped PER LEDGER PATH — a `done` key that moves from one project's ledger to a different project's ledger between `base` and `head` (a cross-path move, as opposed to a same-path key rename) is reported as `ledger-deleted` in the old project, with no recognition that the story continues to live (and stay `done`) in the new project's ledger.
  evidence: `scripts/ledger_regression_check.py:101-155`'s own `check(base, head)` iterates `sorted(set(ledger_paths(base)) | set(ledger_paths(head)))` and compares before/after PER PATH, with the same-path rename-tail matching scoped to that one path's own before/after dicts — a key appearing in a DIFFERENT path's after-dict is never consulted. Pre-existing algorithmic scope (ledgers moving between projects was never a case the original script's design considered), faithfully ported.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/ledger_regression_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-7: `sources/ledger.py`'s and `sources/marshal.py`'s `_git` wrappers only catch `cli_bridge.CliBridgeError`; `cli_bridge.run_git`'s `subprocess.run(..., text=True)` can raise an uncaught `UnicodeDecodeError` on non-UTF-8 git output, which would escape both modules' documented "never raises" contract.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/ledger.py`'s and `sources/marshal.py`'s `_git` wrappers only catch `cli_bridge.CliBridgeError`; `cli_bridge.run_git`'s `subprocess.run(..., text=True)` can raise an uncaught `UnicodeDecodeError` on non-UTF-8 git output, which would escape both modules' documented "never raises" contract.
  evidence: `cli_bridge.py` itself is unmodified by this story — this is a pre-existing characteristic of `run_git` (Story 2.1) shared identically by every existing caller, including `sources/marshal.py`'s own already-shipped, unmodified `gather()`/`MARSHAL_DURABILITY` (Story 5.1/5.2). Surfaced incidentally by review of this story's two NEW call sites, not introduced or worsened by them. A real fix belongs in `cli_bridge.run_git` itself (catching/wrapping `UnicodeDecodeError` there once, for every caller), which is outside this story's Code Map.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-8: `sources/marshal.py`'s `gather_story_status`, Route 1 ("merge commit exists"): a `git log --grep` failure (git unavailable, timeout, corrupt repo) is indistinguishable from "no merge commit found" — both make `_git(...)` return a falsy value — so a transient git failure can misclassify an actually-landed story as a false-green FAIL rather than reporting "cannot evaluate."

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/marshal.py`'s `gather_story_status`, Route 1 ("merge commit exists"): a `git log --grep` failure (git unavailable, timeout, corrupt repo) is indistinguishable from "no merge commit found" — both make `_git(...)` return a falsy value — so a transient git failure can misclassify an actually-landed story as a false-green FAIL rather than reporting "cannot evaluate."
  evidence: Inherited from `scripts/story_status_check.py:89-94,138`'s own `sh()` helper, whose broad `except Exception: return ""` makes a subprocess failure indistinguishable from empty/no-match output in the original script too — the same ambiguity, faithfully carried into the port's `_git` wrapper (`sources/marshal.py`/`sources/ledger.py`, both catching only `CliBridgeError` and treating `None` uniformly as "no evidence").
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-9: `sources/marshal.py`'s `gather_story_status`, Route 3 regex (`^(\d+)-(\d+)-`) only recognizes canonical numeric story keys, unlike `sources/ledger.py`'s own alias-aware `_ID_PREFIX_RE` (`^(?:\d+-\d+[a-z]?|[a-z]+\d+)-`) — a legacy alias-form key (e.g. `a1-scaffold-the-kedro`) never gets Route 3 evaluated, even when a genuine hand-landed commit subject exists for it.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/marshal.py`'s `gather_story_status`, Route 3 regex (`^(\d+)-(\d+)-`) only recognizes canonical numeric story keys, unlike `sources/ledger.py`'s own alias-aware `_ID_PREFIX_RE` (`^(?:\d+-\d+[a-z]?|[a-z]+\d+)-`) — a legacy alias-form key (e.g. `a1-scaffold-the-kedro`) never gets Route 3 evaluated, even when a genuine hand-landed commit subject exists for it.
  evidence: `re.match(r"^(\d+)-(\d+)-", key)` is copied verbatim from `scripts/story_status_check.py:169`. The inconsistency with `ledger_regression_check.py`'s own alias-aware `_ID_PREFIX_RE` PRE-DATES this story — the two original scripts were never reconciled with each other before either was touched. Not introduced by the port; both new Doctor modules faithfully preserve their respective source script's own regex.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/story_status_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-10: `sources/marshal.py`'s `gather_story_status` uses `DONE_RE.findall(text)` without de-duplicating — a Tier-3 feed with a duplicate `key: done` line under `development_status:` (malformed input; the feed's own generator should never produce this) inflates the `audited` count and can produce duplicate FAIL findings for the same key.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/marshal.py`'s `gather_story_status` uses `DONE_RE.findall(text)` without de-duplicating — a Tier-3 feed with a duplicate `key: done` line under `development_status:` (malformed input; the feed's own generator should never produce this) inflates the `audited` count and can produce duplicate FAIL findings for the same key.
  evidence: `scripts/story_status_check.py:126`'s own `for key in DONE_RE.findall(feed.read_text()):` has the identical non-deduping behavior. Pre-existing, faithfully ported; the trigger condition (a malformed, self-contradictory feed file) is outside the scope of what this detector is designed to validate.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/story_status_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-11: The port silently drops the source script's defining "never green on can't-evaluate" guarantee. `scripts/ledger_regression_check.py` returns exit 2 for both cannot-evaluate branches (unresolvable base; base==head with no parent); `sources/ledger.py` maps both to a WARN `Finding`, and `verdict.exit_code_for` documents that "a `warn` finding never changes this" — so both branches now project to exit 0. This is invisible today (no production caller), but becomes live the moment Story 6.9 retires the script and CI consumes Doctor's verdict: an unresolvable `origin/main` (a normal `actions/checkout` shallow/detached shape) would flip a hard CI failure into a green run. The spec's own I/O matrix explicitly prescribes WARN for both rows, so this is a deliberate, spec-sanctioned mapping whose downstream consequence lands outside this story — recorded here so 6.9 decides consciously (either a third status, a FAIL for cannot-evaluate, or an explicit accepted-risk note) rather than inheriting it silently.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: The port silently drops the source script's defining "never green on can't-evaluate" guarantee. `scripts/ledger_regression_check.py` returns exit 2 for both cannot-evaluate branches (unresolvable base; base==head with no parent); `sources/ledger.py` maps both to a WARN `Finding`, and `verdict.exit_code_for` documents that "a `warn` finding never changes this" — so both branches now project to exit 0. This is invisible today (no production caller), but becomes live the moment Story 6.9 retires the script and CI consumes Doctor's verdict: an unresolvable `origin/main` (a normal `actions/checkout` shallow/detached shape) would flip a hard CI failure into a green run. The spec's own I/O matrix explicitly prescribes WARN for both rows, so this is a deliberate, spec-sanctioned mapping whose downstream consequence lands outside this story — recorded here so 6.9 decides consciously (either a third status, a FAIL for cannot-evaluate, or an explicit accepted-risk note) rather than inheriting it silently.
  evidence: `verdict.py:32-44` — `exit_code_for` returns `_EXIT_FAIL` only on a FAIL finding, `_EXIT_OK` otherwise. `scripts/ledger_regression_check.py:172` — `return 2  # never green on can't-evaluate`, with the matching `UNDETERMINED:` print at :171 and :186. Both new WARN branches verified live: a non-repo target returns exactly one WARN and no FAIL.
  resolution: Decided consciously by Story 6.9 (2026-08-09), not inherited silently — the two cannot-evaluate branches keep Story 6.4's own spec-sanctioned WARN mapping as-is: it is that story's already-shipped, deliberate choice (its own I/O matrix prescribes WARN for both rows), and 6.9's Boundaries explicitly accept it rather than reopen it. Recorded here as the accepted-risk note this entry asked for. See spec-6-9's Design Notes ("Ledger's cannot-evaluate WARN").
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/ledger_regression_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

  verified: 2026-09-07 — resolved — re-read against live code (agent judgment, not mechanical), during the pyforge-doctor CI/hygiene sweep. Do NOT flip the cannot-evaluate WARN to FAIL — this entry's own `resolution:` line above already records that Story 6.9 (2026-08-09) consciously decided to keep the WARN mapping, and that decision is still correct and still current: (1) `sources/ledger.py`'s own module docstring states outright, as current documented design: "Degrades, never crashes -- the house rule for every Doctor source. An unresolvable `base` ref, or `base`/`head` naming the same commit with no parent to fall back to, yields a WARN Finding rather than the original script's `exit 2`/raised error." (2) `tests/unit/test_sources_ledger.py` pins WARN for exactly these two branches (`test_unresolvable_base_ref_reports_warn` line ~226, `test_base_equals_head_with_no_parent_reports_warn` line ~245), plus a third cannot-evaluate test carrying an explicit spec-boundary comment (line ~383-394): "The spec's Always boundary is 'degrade to a WARN/OK Finding on any unreadable/missing input, never raise' ... FAIL is not an acceptable verdict; a cannot-evaluate WARN is." Changing WARN to FAIL would violate this documented spec boundary and break these pinned tests. (3) The entry's OWN "becomes live" precondition is still only half true: `scripts/ledger_regression_check.py` HAS been retired (confirmed absent from the repo, consistent with Story 6.9's own scope), but "CI consumes Doctor's verdict" has NOT happened — grepped every `.github/workflows/*.yml` (2026-09-07): none invoke `doctor check`/`doctor monitor` against `ledger-regression` as a gate; `pyforge-station-tests.yml` only runs `pyforge-doctor-test` (the unit-test suite). So the entry's own "invisible today" framing still holds. No code change made. This entry's `status: open` is stale ledger bookkeeping, not an unresolved bug — the question this entry raised WAS already decided (see `resolution:` above); it should read resolved/accepted-risk. If a future story ever wires a real CI gate onto Doctor's ledger-regression verdict, this WARN-vs-FAIL choice should be revisited consciously at that time, exactly as this entry's own resolution note anticipates — not reversed silently now.

### DW-FU-6-4-12: `gather_story_status` has no partial-failure signal: an individual feed that cannot be read (`except (OSError, UnicodeDecodeError): continue`) silently drops that ENTIRE station from the audit, while the returned OK Finding still asserts "every `done` story is backed by a merge commit or a recorded commit sha (N audited)" — a confident green produced by measuring less than it claims, in the detector whose sole purpose is catching confident greens. The `audited` total gives no hint that a station was skipped. (This story's review DID add a git-availability probe covering the all-or-nothing case — git absent / target not a repository now WARNs — so only the per-feed partial-read case remains.) A fix wants an `unreadable_feeds` count in evidence and a WARN when it is non-zero; that changes the OK Finding's evidence shape, which several tests assert by exact equality and the spec's I/O matrix pins for the zero-feed row.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `gather_story_status` has no partial-failure signal: an individual feed that cannot be read (`except (OSError, UnicodeDecodeError): continue`) silently drops that ENTIRE station from the audit, while the returned OK Finding still asserts "every `done` story is backed by a merge commit or a recorded commit sha (N audited)" — a confident green produced by measuring less than it claims, in the detector whose sole purpose is catching confident greens. The `audited` total gives no hint that a station was skipped. (This story's review DID add a git-availability probe covering the all-or-nothing case — git absent / target not a repository now WARNs — so only the per-feed partial-read case remains.) A fix wants an `unreadable_feeds` count in evidence and a WARN when it is non-zero; that changes the OK Finding's evidence shape, which several tests assert by exact equality and the spec's I/O matrix pins for the zero-feed row.
  evidence: `sources/marshal.py` — the per-feed `read_text` guard `continue`s without recording anything, and the terminal OK Finding's evidence is `{"audited": audited}` with no failure counter. Contrast the sibling `gather()` in the same module, which WARNs (`check="ledger-inventory"`) rather than returning OK when it cannot see the artifacts it judges.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-13: Adding `LEDGER_REGRESSION`/`STORY_STATUS` to the `Source` enum silently widened the CLI's `--source` filter to accept two values nothing can emit. `_validate_monitor_args` validates `--source` against `sorted(source.value for source in Source)`, so `doctor monitor --watch <axis> --source ledger-regression` now passes validation, filters the gathered findings to zero, and exits 0 — indistinguishable from "ran the check and it passed." Every other `--source` value names a source some gather actually produces; these two will not until a later story wires them in.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: Adding `LEDGER_REGRESSION`/`STORY_STATUS` to the `Source` enum silently widened the CLI's `--source` filter to accept two values nothing can emit. `_validate_monitor_args` validates `--source` against `sorted(source.value for source in Source)`, so `doctor monitor --watch <axis> --source ledger-regression` now passes validation, filters the gathered findings to zero, and exits 0 — indistinguishable from "ran the check and it passed." Every other `--source` value names a source some gather actually produces; these two will not until a later story wires them in.
  evidence: `__main__.py:407-412` validates against the full `Source` enum rather than the set of sources reachable from the requested axes. Caused by this story (the enum members are new here), but the fix belongs in `__main__.py`, which this story's spec explicitly lists under **Never** ("Wire either new source into `__main__.py`/`doctor check`") and which is absent from its Code Map — so it is deliberately left for the story that does own that surface (6.5/6.6/6.9).
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-14: `sources/ledger.py`'s `_check()` treats a `git show {head}:{path}` failure as proof of deliberate deletion: `after_text is None` unconditionally yields a `ledger-deleted` FAIL naming every `done` key the base ledger held. But the path being compared came from the union of `ls-tree` listings, so a path that IS present at head can still fail to read (transient git failure, timeout, corrupt pack, ENOMEM) and be reported as a destructive deletion. The information to distinguish them is already in hand — `_ledger_paths(target, head)` is computed — so a fix is cheap: treat "absent from head's listing" as deletion and "listed but unreadable" as a WARN.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/ledger.py`'s `_check()` treats a `git show {head}:{path}` failure as proof of deliberate deletion: `after_text is None` unconditionally yields a `ledger-deleted` FAIL naming every `done` key the base ledger held. But the path being compared came from the union of `ls-tree` listings, so a path that IS present at head can still fail to read (transient git failure, timeout, corrupt pack, ENOMEM) and be reported as a destructive deletion. The information to distinguish them is already in hand — `_ledger_paths(target, head)` is computed — so a fix is cheap: treat "absent from head's listing" as deletion and "listed but unreadable" as a WARN.
  evidence: `sources/ledger.py` `_check()` — the `if after_text is None:` branch does not consult head's own path listing. Faithfully ported: `scripts/ledger_regression_check.py`'s own `check()` has the identical unconditional mapping. Distinct from the already-recorded `_ledger_paths`/`ls-tree` entry, which concerns the LISTING failing rather than an individual `git show` failing on a listed path.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/ledger_regression_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-15: `sources/ledger.py`'s `gather()` returns an OK Finding ("no tracked ledger un-finishes a story between X and Y") when there are zero tracked ledgers at BOTH revisions — a green verdict over an empty measurement. The sibling `gather()` in `sources/marshal.py` treats the analogous condition as WARN (`check="ledger-inventory"`, "Marshal's durability guarantee cannot be evaluated here"), so the package now answers the same question two different ways depending on which guard is asked.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/ledger.py`'s `gather()` returns an OK Finding ("no tracked ledger un-finishes a story between X and Y") when there are zero tracked ledgers at BOTH revisions — a green verdict over an empty measurement. The sibling `gather()` in `sources/marshal.py` treats the analogous condition as WARN (`check="ledger-inventory"`, "Marshal's durability guarantee cannot be evaluated here"), so the package now answers the same question two different ways depending on which guard is asked.
  evidence: `sources/marshal.py` — `if not ledgers: return (Finding(..., status=DoctorStatus.WARN, check="ledger-inventory", ...),)`. `sources/ledger.py` — `if not raw_findings:` returns OK without ever checking whether any ledger path existed. Compounds with the `ls-tree`-transient-failure entry already recorded: that failure mode lands in exactly this branch and is reported as a clean pass.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-16: `_parse_statuses` (present in both `sources/ledger.py` and `sources/marshal.py`) ends the `development_status:` block on the FIRST line that does not start with a space or tab, and that dedent test runs BEFORE the comment test — so a column-0 comment inside the block (or any unindented line) silently truncates the parsed key set. Every story key after it is invisible: at `base` that hides real regressions, at `head` it fabricates them (surviving keys read as absent).

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `_parse_statuses` (present in both `sources/ledger.py` and `sources/marshal.py`) ends the `development_status:` block on the FIRST line that does not start with a space or tab, and that dedent test runs BEFORE the comment test — so a column-0 comment inside the block (or any unindented line) silently truncates the parsed key set. Every story key after it is invisible: at `base` that hides real regressions, at `head` it fabricates them (surviving keys read as absent).
  evidence: The parser is verbatim from `scripts/ledger_regression_check.py:65-88` (`if raw and not raw.startswith((" ", "\t")): break  # dedent ends the block`, with `line.startswith("#")` tested only afterwards, on the already-stripped line). Pre-existing in the source script, faithfully ported; the ledger's own generator does not currently emit column-0 comments, so there is no live impact today.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/ledger_regression_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-17: `_parse_statuses` stores the raw remainder after the first `:` without stripping YAML quoting or trailing inline comments, and membership is tested against the exact-match set `TERMINAL = frozenset({"done"})` — so `1-1-foo: done  # landed 2026-08-09` or `1-1-foo: 'done'` parses as a non-terminal value. At `base` such a key is skipped entirely; at `head` a previously-`done` key reads as regressed, producing a `done-key-regressed` FAIL for a story nobody touched (a purely cosmetic ledger edit becomes a CI failure).

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `_parse_statuses` stores the raw remainder after the first `:` without stripping YAML quoting or trailing inline comments, and membership is tested against the exact-match set `TERMINAL = frozenset({"done"})` — so `1-1-foo: done  # landed 2026-08-09` or `1-1-foo: 'done'` parses as a non-terminal value. At `base` such a key is skipped entirely; at `head` a previously-`done` key reads as regressed, producing a `done-key-regressed` FAIL for a story nobody touched (a purely cosmetic ledger edit becomes a CI failure).
  evidence: `key, sep, value = line.partition(":")` then `out[key.strip()] = value.strip()`, verbatim from `scripts/ledger_regression_check.py:84-86`. Pre-existing, faithfully ported. Both YAML forms are legal in a file the ledger's schema permits a human to hand-edit.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/ledger_regression_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-18: `_ledger_paths` parses `git ls-tree -r --name-only` output as plain newline-delimited paths, but git C-quotes any path containing non-ASCII bytes, a double-quote, or a backslash (`core.quotePath` defaults to true) — such a path arrives wrapped in quotes with escapes, so the `startswith(PROJECTS_PREFIX)` test fails, the ledger is never compared, and its lost `done` keys are reported clean. Fixable with `-c core.quotePath=false` or `-z` + NUL splitting.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `_ledger_paths` parses `git ls-tree -r --name-only` output as plain newline-delimited paths, but git C-quotes any path containing non-ASCII bytes, a double-quote, or a backslash (`core.quotePath` defaults to true) — such a path arrives wrapped in quotes with escapes, so the `startswith(PROJECTS_PREFIX)` test fails, the ledger is never compared, and its lost `done` keys are reported clean. Fixable with `-c core.quotePath=false` or `-z` + NUL splitting.
  evidence: `listing = _git(target, "ls-tree", "-r", "--name-only", rev) or ""` then `p.startswith(PROJECTS_PREFIX)`, verbatim from `scripts/ledger_regression_check.py:93`. Pre-existing, faithfully ported. No live impact: every project directory is an ASCII `pyforge-*` slug.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/ledger_regression_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-19: `gather_story_status` finds `done` stories with `DONE_RE = ^  ([a-z0-9][a-z0-9-]*): done$`, which demands exactly two spaces of indent, an unquoted lowercase key, and an unquoted lowercase `done` — while the same package's `_parse_statuses` accepts all of those variations. A feed entry outside that narrow shape is never audited at all, yet the returned OK Finding still claims every `done` story is backed, and the `audited` count silently under-reports. Reusing `_parse_statuses` + `TERMINAL` would make the two agree.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `gather_story_status` finds `done` stories with `DONE_RE = ^  ([a-z0-9][a-z0-9-]*): done$`, which demands exactly two spaces of indent, an unquoted lowercase key, and an unquoted lowercase `done` — while the same package's `_parse_statuses` accepts all of those variations. A feed entry outside that narrow shape is never audited at all, yet the returned OK Finding still claims every `done` story is backed, and the `audited` count silently under-reports. Reusing `_parse_statuses` + `TERMINAL` would make the two agree.
  evidence: `DONE_RE` is verbatim from `scripts/story_status_check.py:85`. Pre-existing, faithfully ported. Related to but distinct from the already-recorded Route-3 alias-regex entry (that one concerns which keys reach Route 3; this one concerns which keys are audited at all).
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/story_status_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-20: Two live implementations of the same two judgements now coexist — `scripts/ledger_regression_check.py`/`scripts/story_status_check.py` (what `pixi.toml` and `.github/workflows/detectors.yml` actually run today) and their Doctor ports — with nothing keeping them in step until Story 6.9 retires the originals. There is no parity test and no cross-reference in either script pointing at its port, so a fix applied to either original between now and 6.9 rots the copy silently, and the two could disagree on a live CI verdict without anything noticing.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: Two live implementations of the same two judgements now coexist — `scripts/ledger_regression_check.py`/`scripts/story_status_check.py` (what `pixi.toml` and `.github/workflows/detectors.yml` actually run today) and their Doctor ports — with nothing keeping them in step until Story 6.9 retires the originals. There is no parity test and no cross-reference in either script pointing at its port, so a fix applied to either original between now and 6.9 rots the copy silently, and the two could disagree on a live CI verdict without anything noticing.
  evidence: The spec's **Never** list explicitly forbids touching either script or the CI workflow (6.9's job), so the duplication window is intentional and correctly scoped — but it is a window with no tripwire. A cheap mitigation available before 6.9: a comment in each script naming its Doctor port, and/or a test asserting the two produce identical verdicts over a shared fixture.
  resolution: Resolved by deletion (Story 6.9, 2026-08-09) — `scripts/ledger_regression_check.py`/`scripts/story_status_check.py` are deleted; the "two live copies could disagree" premise no longer holds because only one copy exists. Live parity was verified BEFORE deletion (identical finding kinds/subjects/messages for both) as this story's own Verification step.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/4 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/ledger_regression_check.py, scripts/story_status_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-21: `check="ledger-regression"` is now emitted by two different sources — `Source.MARSHAL_DURABILITY`'s working-tree-vs-HEAD guard (pre-existing) and `Source.LEDGER_REGRESSION`'s revision-range guard (new here) — so the text render produces `[marshal-durability] ledger-regression:` and `[ledger-regression] ledger-regression:` for two structurally different checks. The `source` tag disambiguates them in both the text and JSON renders, so nothing is actually ambiguous to a machine, but the naming collision is exactly the confusion this story added a module-docstring paragraph to prevent in prose.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `check="ledger-regression"` is now emitted by two different sources — `Source.MARSHAL_DURABILITY`'s working-tree-vs-HEAD guard (pre-existing) and `Source.LEDGER_REGRESSION`'s revision-range guard (new here) — so the text render produces `[marshal-durability] ledger-regression:` and `[ledger-regression] ledger-regression:` for two structurally different checks. The `source` tag disambiguates them in both the text and JSON renders, so nothing is actually ambiguous to a machine, but the naming collision is exactly the confusion this story added a module-docstring paragraph to prevent in prose.
  evidence: `sources/marshal.py` emits `check="ledger-regression"` for `MARSHAL_DURABILITY`'s findings; `sources/ledger.py` emits the same string for `LEDGER_REGRESSION`'s OK/WARN findings. The spec's I/O matrix pins the two FAIL check names (`done-key-regressed`, `ledger-deleted`) but leaves the OK/WARN check name unspecified, so renaming it is unconstrained by the contract — deferred rather than patched because several existing tests assert the current string and check-name taxonomy is not this story's surface.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-22: `sources/marshal.py`'s `gather_story_status` Route 1 (`git log --oneline --all -F --grep=/<key> into`) spawns one full all-refs history walk PER candidate key, and unlike Route 3 it was never hoisted — so the more expensive of the two routes is the one that still scales O(keys x history), in the package carrying an explicit NFR-4 wall-clock budget test.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/marshal.py`'s `gather_story_status` Route 1 (`git log --oneline --all -F --grep=/<key> into`) spawns one full all-refs history walk PER candidate key, and unlike Route 3 it was never hoisted — so the more expensive of the two routes is the one that still scales O(keys x history), in the package carrying an explicit NFR-4 wall-clock budget test.
  evidence: Measured by instrumenting `run_git` over a 20-candidate-key fixture: 22 git calls total, of which 20 were Route 1 and exactly 1 was Route 3 (Route 3 was hoisted to one call per gather during this story's second review pass). Route 1 is strictly more expensive per call than Route 3 (`--all` walks every ref; Route 3 walks `main` only). NOT patched here because the hoist is not behavior-preserving the way Route 3's was: Route 3's query is literally loop-invariant, whereas Route 1's `--grep` is key-specific AND matches the FULL commit message, so collapsing it to one call requires fetching messages (`--format=%B` with a record separator, not `--oneline`, which prints subjects only) and re-implementing the fixed-string match in Python — a semantics-carrying restructure of the matching rule, outside a surgical review patch. Live cost is masked today because most real keys short-circuit on `commit_sha` before reaching Route 1 (measured on this repo: 27 audited, only 3 reach the git routes at all).
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-23: `sources/ledger.py`'s `gather()` silently requires `target` to be the repository ROOT — `git ls-tree`/`git show` run with `cwd=target`, so a `target` pointing at any SUBDIRECTORY of the repo makes ls-tree list only that subtree with subtree-relative paths, no path matches the `_bmad-output/projects/` prefix, and a real regression is reported as a clean OK. The original script could not hit this (it hardcoded `REPO_ROOT`); the `cwd=target` port introduced the exposure.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/ledger.py`'s `gather()` silently requires `target` to be the repository ROOT — `git ls-tree`/`git show` run with `cwd=target`, so a `target` pointing at any SUBDIRECTORY of the repo makes ls-tree list only that subtree with subtree-relative paths, no path matches the `_bmad-output/projects/` prefix, and a real regression is reported as a clean OK. The original script could not hit this (it hardcoded `REPO_ROOT`); the `cwd=target` port introduced the exposure.
  evidence: Reproduced by the review: `git ls-tree -r --name-only HEAD` run from `<repo>/sub` returned only that subtree's entries, and `ledger.gather(repo/"sub", ...)` returned OK with a genuine `done -> in-progress` regression present at the repo root. Distinct from — but compounding — the already-recorded "zero tracked ledgers reports OK rather than WARN" entry: that entry is the reporting decision, this one is a path-scoping bug that manufactures the zero-ledger state from a healthy repo. Two independent fixes are possible (normalize via `git rev-parse --show-toplevel` before listing, or reject/warn on a non-root target); both change what `target` MEANS for this source, and the same repo-root assumption is shared by the sibling `sources/marshal.py::gather` (which globs `target / "_bmad-output/projects"` directly), so the choice belongs to whichever story owns the target-resolution contract rather than to this review pass. No live impact today: no production caller exists, and `__main__.py`'s `--target` is not yet wired to either source.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-24: `sources/marshal.py`'s already-shipped `MARSHAL_DURABILITY` `gather()` reads each ledger's WORKING-TREE copy with `ledger.read_text(encoding="utf-8")` guarded by `except OSError` only — so a ledger holding a non-UTF-8 byte on disk raises `UnicodeDecodeError` straight out of the gather, contradicting the module docstring's own "Degrades, never crashes … A repo with no ledgers, no git, or an unreadable tree yields a WARN Finding, never a traceback." Every git-side read in the module is now guarded (`_git` catches `UnicodeDecodeError`); this one working-tree read is the last hazard treated differently, one line from a `ledger-unreadable` WARN the function already emits for the `OSError` case.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `sources/marshal.py`'s already-shipped `MARSHAL_DURABILITY` `gather()` reads each ledger's WORKING-TREE copy with `ledger.read_text(encoding="utf-8")` guarded by `except OSError` only — so a ledger holding a non-UTF-8 byte on disk raises `UnicodeDecodeError` straight out of the gather, contradicting the module docstring's own "Degrades, never crashes … A repo with no ledgers, no git, or an unreadable tree yields a WARN Finding, never a traceback." Every git-side read in the module is now guarded (`_git` catches `UnicodeDecodeError`); this one working-tree read is the last hazard treated differently, one line from a `ledger-unreadable` WARN the function already emits for the `OSError` case.
  evidence: Reproduced live — a `\xe9` byte written into a tracked ledger's working-tree copy raised `UnicodeDecodeError` out of `marshal.gather(repo)`. The line pre-dates Story 6.4 (Story 5.1/5.2) and is unmodified by it; the spec's **Never** list explicitly forbids changing `MARSHAL_DURABILITY`'s existing `gather()` behavior, and turning a raise into a WARN is a behavior change, so it is recorded rather than taken here. The gap is now named in the module docstring so the "never crashes" claim is not read as unqualified. Distinct from the already-recorded `cli_bridge.run_git` entry, which concerns git's *stdout* decoding rather than a direct filesystem read.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-4-25: `gather_story_status`'s `audited` / `no run record` counts treat every `  <key>: done` row in a Tier-3 feed as a STORY, but each station's feed also carries `epic-N: done` / `epic-N-retrospective: done` aggregate rows — which by construction can never have a harness run record, so they inflate both the denominator and the "no run record (unchecked)" caveat of the OK Finding's own breakdown.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md`
  summary: `gather_story_status`'s `audited` / `no run record` counts treat every `  <key>: done` row in a Tier-3 feed as a STORY, but each station's feed also carries `epic-N: done` / `epic-N-retrospective: done` aggregate rows — which by construction can never have a harness run record, so they inflate both the denominator and the "no run record (unchecked)" caveat of the OK Finding's own breakdown.
  evidence: Measured live against this repo's feeds: 27 audited, of which 5–6 are `epic-N` rows, all of which land in the no-record bucket — roughly 20% of the population the message describes as stories. `DONE_RE = ^  ([a-z0-9][a-z0-9-]*): done$` is verbatim from `scripts/story_status_check.py:85` (the source script counted nothing, so the inflation only became visible once this story's review added the breakdown to the message). Filtering to story-shaped keys means deciding the key grammar, which is the same decision the already-recorded Route-3-alias-regex and `DONE_RE`-narrow-shape entries turn on — all three want resolving together, by whichever story owns the key grammar, rather than three separate narrowings here. Directionally opposite to the `DONE_RE`-narrow-shape entry (that one under-counts real stories; this one over-counts non-stories).
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-4-the-ledger-verdicts-come-home.md, scripts/story_status_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-5-2: `sources/board.py`'s independence claim ("never imports `pyforge.marshal` or any other station package") stops at the Python-package boundary, not the file boundary — `gather_dashboard_drift`/`gather_check_layout` dynamically `exec_module()` two other Marshal-owned files (`docs/dashboard/generate.py`, `docs/dashboard/check_layout.py`) directly into Doctor's own process, so a broken or subtly-wrong `generate.py`/`check_layout.py` still executes as trusted code inside the judge that is supposed to be independent of Marshal.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md`
  summary: `sources/board.py`'s independence claim ("never imports `pyforge.marshal` or any other station package") stops at the Python-package boundary, not the file boundary — `gather_dashboard_drift`/`gather_check_layout` dynamically `exec_module()` two other Marshal-owned files (`docs/dashboard/generate.py`, `docs/dashboard/check_layout.py`) directly into Doctor's own process, so a broken or subtly-wrong `generate.py`/`check_layout.py` still executes as trusted code inside the judge that is supposed to be independent of Marshal.
  evidence: Identified by adversarial review. This is the SAME dynamic-load technique `scripts/dashboard_drift_check.py`'s own `_load_generate` already uses (and the story spec explicitly authorized reusing it, "never reimplemented, to avoid drift"), so it is an inherited, deliberate design tradeoff of the whole "reuse the original script's parsers" family, not a defect introduced by this port. Worth a future decision (parse the reused attributes without executing the whole module, or accept the tradeoff explicitly in the independence meta-test Story 6.10 builds) rather than a unilateral redesign here.
  resolution: HALF resolved by Story 6.9 (2026-08-09, coordinator-requested follow-up) — `gather_check_layout` no longer dynamically execs anything at all: `check_layout.py`'s own `check()`/`_rows()`/`_serve()`/module constants moved into `board.py` as permanent code (forced by 6.9 deleting the origin file; a dynamic load of a deleted file cannot merely be "a tradeoff", it is a permanent failure). `gather_dashboard_drift`'s dynamic exec of `docs/dashboard/generate.py` is UNCHANGED and unaffected — that file is not retired, is explicitly outside this story's Surface, and the independence-boundary concern this entry names remains fully live for it.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/4 present (absent: _bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md, docs/dashboard/check_layout.py, docs/dashboard/generate.py…); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-5-3: `gather_check_layout`'s browser orchestration (`_run_check_layout`) loses already-collected FAIL findings if `playwright` raises partway through the width/font-pressure sweep (e.g. a page crash on width N after widths 1..N-1 already produced real chip-overlap FAILs) — the whole result is discarded and replaced by one generic WARN via the `degrade_on_exception` wrap, rather than the partial results being preserved.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md`
  summary: `gather_check_layout`'s browser orchestration (`_run_check_layout`) loses already-collected FAIL findings if `playwright` raises partway through the width/font-pressure sweep (e.g. a page crash on width N after widths 1..N-1 already produced real chip-overlap FAILs) — the whole result is discarded and replaced by one generic WARN via the `degrade_on_exception` wrap, rather than the partial results being preserved.
  evidence: Identified by adversarial review; not reproduced live (no `playwright` in this package's own pixi env — see the story spec's Boundaries). Confirmed by code inspection: `_run_check_layout` accumulates `raw_findings`/`measured` in local variables with no incremental persistence, so a mid-loop exception unwinds past all of it into `degrade_on_exception`'s catch-all. Slightly BETTER than the original `docs/dashboard/check_layout.py::main()`, which has no top-level exception guard around the per-width loop at all and would let the same failure escape as an uncaught traceback — so this is a lower-priority refinement (preserve partial results), not a regression from the original's own behavior. No production caller exists yet for `gather_check_layout`.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-5-4: `sources/board.py` reads the SAME `sprint-status-ledger.yaml` with two different parsers in one module — `_ledger_rows` (a hand-rolled `line.partition(":")`, feeding INV-C) and `generate.py`'s real `parse_sprint_status` (a regex that strips inline comments and rejects quoted values, feeding `gather_dashboard_drift`) — so a single ledger row can be read two ways and the two gathers can contradict each other about the same file in one Doctor run.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md`
  summary: `sources/board.py` reads the SAME `sprint-status-ledger.yaml` with two different parsers in one module — `_ledger_rows` (a hand-rolled `line.partition(":")`, feeding INV-C) and `generate.py`'s real `parse_sprint_status` (a regex that strips inline comments and rejects quoted values, feeding `gather_dashboard_drift`) — so a single ledger row can be read two ways and the two gathers can contradict each other about the same file in one Doctor run.
  evidence: Identified by adversarial review; confirmed by inspection, latent today. A row `1-1-foo: done  # landed in PR #331` yields `"done  # landed in PR #331"` from `_ledger_rows`, so INV-C's `l_done` under-counts by one and fires a spurious `board-diverges-from-ledger` FAIL, while the sibling `gather_dashboard_drift` reads `done` correctly and reports clean. No live ledger carries an inline comment today (verified: 0 hits across all 8 project ledgers), so nothing is wrong now. The two parsers exist because each was preserved verbatim from its OWN source script, which this story's Boundaries explicitly required ("preserve, don't redesign") and whose rationale the module docstring already records under "Two independent `data.js` readers, deliberately not unified" — the same tradeoff, one level down. Unifying them means deciding which parser is canonical for the ledger, which is the same key/value-grammar decision the already-recorded `DONE_RE`/Route-3-alias entries turn on and belongs with them, not to this review pass. Related to this repo's own standing principle "measure with the real parser, never an ad-hoc regex" — `parse_sprint_status` is already imported in this module and would be the natural winner.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-5-5: `_run_check_layout` — the only genuinely NEW code in this port (the browser/HTTP orchestration; everything else reuses the original scripts' own functions) — has no effective automated coverage: no test names it, and the single end-to-end test that would exercise it is `playwright`-gated (always skipped in this package's pixi env) and asserts only tautologies.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md`
  summary: `_run_check_layout` — the only genuinely NEW code in this port (the browser/HTTP orchestration; everything else reuses the original scripts' own functions) — has no effective automated coverage: no test names it, and the single end-to-end test that would exercise it is `playwright`-gated (always skipped in this package's pixi env) and asserts only tautologies.
  evidence: Identified by adversarial review. `grep -rn "_run_check_layout"` returns hits only inside `board.py` itself. The gated test's three assertions are `assert findings`, `all(f.source is Source.CHECK_LAYOUT)` (guaranteed by construction) and `all(f.status in (OK, FAIL, WARN))` (tautological — `DoctorStatus` has exactly those three members), so it would pass whether the layout is clean, catastrophically broken, or unmeasurable. Two real defects in this exact uncovered region survived to the follow-up review and were fixed there (a narrow `except ImportError` around the playwright import letting an installed-but-broken playwright escape the gather; a `shutdown()` without `server_close()` leaking one listening socket per call), which is direct evidence the gap hides regressions. Not closable within this story: the Boundaries forbid adding `playwright` as a `pyforge-doctor` dependency, so real coverage needs either a fake-browser seam (inject the `sync_playwright` factory, which `_run_check_layout` already takes as a parameter — the cheap option) or an opt-in CI lane that installs playwright. The assertions were left as-is rather than tightened blind, since they cannot be executed in this environment to confirm a tightened form still passes.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-5-6: `_load_foreign_module` cleans up only its OWN `sys.modules` key, so the TRANSITIVE imports the exec'd file performs stay cached under their own names — `docs/dashboard/generate.py` does `from bmad_drift_check import GUILD_DREAMS, STATIONS`, so calling `gather_dashboard_drift` for two DIFFERENT targets in one process silently gives the second target the FIRST target's `bmad_drift_check`, and therefore a verdict computed from the wrong tree.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md`
  summary: `_load_foreign_module` cleans up only its OWN `sys.modules` key, so the TRANSITIVE imports the exec'd file performs stay cached under their own names — `docs/dashboard/generate.py` does `from bmad_drift_check import GUILD_DREAMS, STATIONS`, so calling `gather_dashboard_drift` for two DIFFERENT targets in one process silently gives the second target the FIRST target's `bmad_drift_check`, and therefore a verdict computed from the wrong tree.
  evidence: Reproduced live by adversarial review with two tmp targets carrying different `scripts/probe_mod.py` contents: `target A -> TARGET-A`, then `target B -> TARGET-A`. A previous review pass rejected the adjacent fixed-`sys.modules`-key finding on the (correct) grounds that each call's RETURN VALUE is the freshly-`exec_module`'d object and is never re-fetched from the cache — that reasoning holds for the loader's own key but does not cover transitive modules, which is where the contamination actually lives. Not fixed here because the honest fix is a design decision, not a patch: snapshotting and restoring the whole of `sys.modules` around an `exec_module` strands module objects and can break legitimately-shared stdlib/third-party caching, so the alternative (accept single-target-per-process, and say so) may well be the right answer. No production caller exists yet (this story wires nothing into `__main__.py`), and Doctor's CLI runs against one target per process, so nothing is wrong today. Belongs with the already-recorded "Doctor `exec_module`s two Marshal-owned files as trusted code" entry — the same "reuse the original script's parsers by executing them" tradeoff, one level deeper — and wants resolving together with it.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/3 present (absent: _bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md, docs/dashboard/generate.py, scripts/probe_mod.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-5-7: `Source.CHECK_LAYOUT` is registered `scope="repo"`, but `gather_check_layout` binds a loopback TCP listener and drives Chromium across every width x font-pressure combination — which is not what that field's own documented gloss ("reads only tracked files/git history, runs anywhere") describes, and cannot meet the 5-second NFR-4 budget `doctor check` holds.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md`
  summary: `Source.CHECK_LAYOUT` is registered `scope="repo"`, but `gather_check_layout` binds a loopback TCP listener and drives Chromium across every width x font-pressure combination — which is not what that field's own documented gloss ("reads only tracked files/git history, runs anywhere") describes, and cannot meet the 5-second NFR-4 budget `doctor check` holds.
  evidence: Identified by adversarial review; confirmed by inspection. The registration is faithful to this story's spec, which mandates "the scope each script's own current `DETECTOR` dict already declares (preserve, don't redesign)" and names `scope="repo"` for `CHECK_LAYOUT` in its Code Map, so changing it here would be an unauthorized redesign — it is recorded rather than corrected. Nothing dispatches the gather yet, so there is no live consequence today; the decision becomes load-bearing in Story 6.9, which wires these three sources into `__main__.py`, because `_category_in_scope` (`__main__.py:312`) reads `sources.scope_for` live and `tests/unit/test_check_speed_budget.py:42` enforces the 5s budget a browser sweep cannot meet. The three plausible resolutions (re-scope to `"runtime"`; add a third scope for "needs a browser"; keep `"repo"` and gate the gather behind an explicit opt-in flag) all belong to 6.9's own design, not to this port. A comment naming the tension was added to the registry row so 6.9 does not have to rediscover it.
  resolution: Decided consciously by Story 6.9 (2026-08-09), not inherited silently — kept `scope="repo"`, matching the existing `DETECTOR`/registry declaration and matching `.github/workflows/detectors.yml`'s own existing browser provisioning ("for the layout gate" under `--scope repo`), which is already built assuming this. No scope taxonomy change. This story's own dispatcher (`python -m pyforge.doctor.sources <name>`) is target-less and does NOT wire these sources into `__main__.py`'s `--engines`/`--env`/`--durability` categories at all (Never clause), so `_category_in_scope`/the NFR-4 budget test never actually reach `check-layout` — the load-bearing concern this entry named does not materialize under this story's design. See spec-6-9's Design Notes ("CHECK_LAYOUT's scope").
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/3 present (absent: _bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md, tests/unit/test_check_speed_budget.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-5-8: INV-A decides whether an open Spec has been decomposed with a bare SUBSTRING test (`if bare not in prose and slug not in prose`), so a Spec whose slug is a prefix of another slug mentioned anywhere in the PRD/epics prose is silently treated as decomposed — a false negative in the one check whose stated purpose is making "we chose not to" distinguishable from "nobody noticed".

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md`
  summary: INV-A decides whether an open Spec has been decomposed with a bare SUBSTRING test (`if bare not in prose and slug not in prose`), so a Spec whose slug is a prefix of another slug mentioned anywhere in the PRD/epics prose is silently treated as decomposed — a false negative in the one check whose stated purpose is making "we chose not to" distinguishable from "nobody noticed".
  evidence: Identified by adversarial review (edge-case walk); not reproduced against live data — no current slug pair in this repo collides. The test is verbatim from `scripts/chain_completeness_check.py`, unchanged by this port, so it is a pre-existing detector weakness surfaced incidentally rather than anything this story introduced, and the story's Boundaries require the invariants port with identical behavior. The fix is a word-boundary match (`re.search(rf"\b{re.escape(bare)}\b", prose)`), but it must land in the ORIGINAL script and the port together or the two detectors disagree — which makes it Story 6.9's business (that story owns reconciling the scripts with their Doctor ports), not this review pass's. Reachable in practice as soon as a station names a Spec `spec-foo` alongside a `spec-foo-bar`, a shape this repo already uses elsewhere.
  resolution: Resolved by deletion (Story 6.9, 2026-08-09) — `scripts/chain_completeness_check.py` is deleted; the "must land in both or they disagree" premise no longer holds. The inherited word-boundary gap itself is not patched (this story's Boundaries don't ask for it), only its two-copies blocking reason.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md, scripts/chain_completeness_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-5-9: The port MOVED the dynamic-import trust boundary: both original scripts resolve the file they `exec_module` from their OWN `__file__` (`REPO_ROOT = Path(__file__).resolve().parent.parent`, `HERE = Path(__file__).resolve().parent`), so they can only ever execute their own repo's `generate.py`/`check_layout.py`. `sources/board.py` resolves both from the caller-supplied `target`, so once Story 6.9 wires these gathers into `__main__.py` (whose `--target`/`path` argument becomes `Path(args.path)` from argv), `doctor check /some/untrusted/checkout` executes that checkout's two Python files with the Doctor process's full privileges before any read-only judgement runs.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md`
  summary: The port MOVED the dynamic-import trust boundary: both original scripts resolve the file they `exec_module` from their OWN `__file__` (`REPO_ROOT = Path(__file__).resolve().parent.parent`, `HERE = Path(__file__).resolve().parent`), so they can only ever execute their own repo's `generate.py`/`check_layout.py`. `sources/board.py` resolves both from the caller-supplied `target`, so once Story 6.9 wires these gathers into `__main__.py` (whose `--target`/`path` argument becomes `Path(args.path)` from argv), `doctor check /some/untrusted/checkout` executes that checkout's two Python files with the Doctor process's full privileges before any read-only judgement runs.
  evidence: Identified by adversarial review; the execution itself is demonstrated by the branch's own test `test_a_failed_generate_py_import_leaves_no_module_and_no_path_entry`, which writes `sys.path.insert(0, '/tmp/EVIL-GENERATE-PROBE')` into a tmp target's `generate.py` and observes it run. `_load_foreign_module`'s docstring frames its three hardenings (sys.path snapshot, sys.modules cleanup, SystemExit conversion) as "guarding the Doctor PROCESS from a file it does not own" — the right threat model, applied to everything except whether the file should be executed at all. DISTINCT from the already-recorded "Doctor `exec_module`s two Marshal-owned files as trusted code" entry: that one is about trusting Marshal's files inside the judge that grades Marshal, within one repo; this one is about the target becoming an arbitrary path. Not patchable here — the story's Boundaries mandate the dynamic-load technique ("preserve, don't redesign") and forbid touching `__main__.py`, and the resolutions (refuse a non-self target, parse the reused attributes without executing, or document `target` as trusted input) are all Story 6.9's design calls. No live exposure today: nothing dispatches these gathers. status_note: Story 6.9 (2026-08-09) did NOT trigger this — its own dispatcher (`python -m pyforge.doctor.sources <name>`) takes no `--target`/path argument at all (Boundaries: "always judges the repo it runs in"), and `__main__.py`'s verb dispatch is untouched (Never clause). The predicted trigger ("once Story 6.9 wires these gathers into `__main__.py`") did not happen. STILL OPEN for whichever future story adds a `doctor check --target <path>` wiring — recorded here, not closed, so that story inherits the risk consciously rather than rediscovering it.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-5-10: All three originals (`scripts/chain_completeness_check.py`, `scripts/dashboard_drift_check.py`, `docs/dashboard/check_layout.py`) still run as live fleet detectors alongside their Doctor ports, with nothing keeping the two copies in step — and `chain_completeness`'s `DEFERRED_SPECS` makes this sharper than the Story 6.4 case, because it is an operator-edited constant that a station updates when it records a Spec exemption.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md`
  summary: All three originals (`scripts/chain_completeness_check.py`, `scripts/dashboard_drift_check.py`, `docs/dashboard/check_layout.py`) still run as live fleet detectors alongside their Doctor ports, with nothing keeping the two copies in step — and `chain_completeness`'s `DEFERRED_SPECS` makes this sharper than the Story 6.4 case, because it is an operator-edited constant that a station updates when it records a Spec exemption.
  evidence: `scripts/detectors.py:63-66` globs `scripts/*_check.py` AND `docs/dashboard/check_*.py`, so all three originals still execute; `grep -rn "DEFERRED_SPECS\|OPEN_SPEC_STATUSES" tests/ src/` returns only comments and a docstring — no test compares the two implementations. Concrete failure: an operator adds a fourth entry to `DEFERRED_SPECS` in `scripts/chain_completeness_check.py` (the file the runbook names) and not in `board.py`; the repo-root detector reports clean while `doctor check` reports a `spec-not-decomposed` FAIL for a Spec whose exemption is on record — two Doctor-blessed detectors contradicting each other on the same tree. Behavioral parity was verified by hand during this review (both gathers reproduce their scripts' output exactly: 1 and 5 findings, identical text), which is precisely the check nothing automates. Same class as the already-recorded Story 6.4 entry for `ledger_regression_check.py`/`story_status_check.py`, and it wants the same resolution (Story 6.9 retires the originals); recorded separately because it covers three different scripts plus the hand-edited-constant hazard the 6.4 entry does not have. Cheap mitigations available before 6.9: a shared-fixture parity test, or a comment in each script naming its Doctor port.
  resolution: Resolved by deletion (Story 6.9, 2026-08-09) — this exact hazard had ALREADY happened, not merely predicted: `scripts/chain_completeness_check.py`'s `DEFERRED_SPECS` gained a fourth entry (`spec-secure-live-dashboards`, added the same day) that `board.py`'s copy lacked, reproduced live during this story's own pre-deletion parity verification (`chain-completeness` reported a false `spec-not-decomposed` FAIL for pyforge-steward that the origin script did not). Fixed by syncing `board.py`'s `DEFERRED_SPECS` before deletion, then deleting `scripts/chain_completeness_check.py`/`scripts/dashboard_drift_check.py`/`docs/dashboard/check_layout.py` — the "two live copies" premise no longer holds because only one copy exists.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/7 present (absent: _bmad-output/implementation-artifacts/spec-6-5-the-board-verdicts-come-home.md, docs/dashboard/check_*.py, docs/dashboard/check_layout.py…); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-6-2: `spec-pyforge-doctor`'s own `spec-surface` baseline (`scripts/.spec-surface-baseline.json`) has been drifting, unreconciled, since Story 6.2 — every file the ledger/board/chain ports have added (`sources/board.py`, `sources/ledger.py`, `sources/chain.py`, all their test files, this story's `pyproject.toml`/`__main__.py`/`models.py`/schema/`sources/__init__.py` edits) reports `[drift]` because `.memlog.md` never moved and `--write-baseline` was never re-run, and the epic's own Story 6.9 AC never actually schedules reconciling THIS spec's baseline (it only retires the `scripts/` shims and the two OTHER specs, `spec-regenerable-factory`/`spec-surface-drift-reconciliation`, that govern those retired files).

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md`
  summary: `spec-pyforge-doctor`'s own `spec-surface` baseline (`scripts/.spec-surface-baseline.json`) has been drifting, unreconciled, since Story 6.2 — every file the ledger/board/chain ports have added (`sources/board.py`, `sources/ledger.py`, `sources/chain.py`, all their test files, this story's `pyproject.toml`/`__main__.py`/`models.py`/schema/`sources/__init__.py` edits) reports `[drift]` because `.memlog.md` never moved and `--write-baseline` was never re-run, and the epic's own Story 6.9 AC never actually schedules reconciling THIS spec's baseline (it only retires the `scripts/` shims and the two OTHER specs, `spec-regenerable-factory`/`spec-surface-drift-reconciliation`, that govern those retired files).
  evidence: Running `python scripts/spec_surface_check.py --json` against the live repo (post-6.6) reports 23 `[drift]` findings against `pyforge-doctor/spec-pyforge-doctor`, including files added by 6.4 and 6.5 (`sources/board.py`, `sources/ledger.py`, `test_sources_board_*.py`, `test_sources_ledger*.py`) — confirming this predates 6.6 and is not caused by it; adopted by the review pass rather than reject-as-noise because the spec's own `.memlog.md` explicitly anticipated it ("Expect this baseline to lag again the moment that lands, and reconcile it as part of that epic") without naming which story actually owns running `--write-baseline --spec pyforge-doctor/spec-pyforge-doctor`.
  resolution: Resolved by Story 6.9 (2026-08-09), which took ownership despite its narrow AC text not scheduling it: named this story's own changed paths (`sources/factory.py`, `sources/board.py`, `sources/__main__.py`, `tests/unit/test_sources_dispatch.py`) in `spec-pyforge-doctor`'s memlog, then ran `--write-baseline --spec pyforge-doctor/spec-pyforge-doctor` (via the origin script, temporarily restored for this one operation since `--write-baseline` was deliberately never ported into the read-only Doctor library) in the same change. `python -m pyforge.doctor.sources spec-surface` now reports clean for this spec.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/3 present (absent: _bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md, tests/unit/test_sources_dispatch.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-6-3: `mcp` is declared in `pyproject.toml`'s `dependencies` and annotated in the root `pixi.toml` as "also a package run-dep", but was never added to `src/shared/packages/pyforge-doctor/pixi.toml`'s `[package.run-dependencies]` — so the built `.conda` does not depend on it and `import pyforge.doctor.sources.atlas` raises `ModuleNotFoundError` on a standalone conda install.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md`
  summary: `mcp` is declared in `pyproject.toml`'s `dependencies` and annotated in the root `pixi.toml` as "also a package run-dep", but was never added to `src/shared/packages/pyforge-doctor/pixi.toml`'s `[package.run-dependencies]` — so the built `.conda` does not depend on it and `import pyforge.doctor.sources.atlas` raises `ModuleNotFoundError` on a standalone conda install.
  evidence: Confirmed by inspection: `[package.run-dependencies]` carries only `python >=3.14` and `jsonschema` (plus `pyyaml`, added by this review pass), while `pyproject.toml` lists `jsonschema`, `mcp>=1.28.1`, `PyYAML>=6.0`. The root `pixi.toml` comment on the `mcp = ">=2.0.0"` line says "also a package run-dep", so the omission is an oversight rather than a deliberate exclusion — and the two pins disagree besides (`>=2.0.0` in the feature table vs `mcp>=1.28.1` in `pyproject.toml`). Pre-existing since Story 2.1 (`sources/atlas.py`), not introduced here; this pass fixed only the identical `pyyaml` defect it did introduce. Same failure class as pyforge-warden's own recorded `AUD-WARDEN-010`. The fix is one line plus a pin reconciliation, but choosing WHICH pin is authoritative is a decision this review pass should not make unilaterally.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-6-4: `_tracked_files` calls `git ls-files` without `-c core.quotePath=false`, so git C-quotes any tracked path containing a non-ASCII byte (e.g. `"docs/caf\303\251.md"`) — the quoted string matches no `surface:` glob and no allowlist pattern, producing a spurious `ungoverned` FAIL while the real file's drift goes unmeasured.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md`
  summary: `_tracked_files` calls `git ls-files` without `-c core.quotePath=false`, so git C-quotes any tracked path containing a non-ASCII byte (e.g. `"docs/caf\303\251.md"`) — the quoted string matches no `surface:` glob and no allowlist pattern, producing a spurious `ungoverned` FAIL while the real file's drift goes unmeasured.
  evidence: Identified by adversarial edge-case review; confirmed by inspection of both implementations. `scripts/spec_surface_check.py:127` runs `subprocess.run(["git", "-C", str(REPO_ROOT), "ls-files"])` with the same default quoting, so the port is FAITHFUL here and the defect is inherited, not introduced — which is why it is recorded rather than patched (this story's Boundaries mandate porting the comparison logic verbatim, and diverging in only one of the two live copies would make the repo-root detector and the Doctor port disagree on the same tree). Not reproduced against live data: no tracked path in this repo currently carries a non-ASCII byte. Wants fixing in the original and the port together — Story 6.9's business, which owns reconciling the scripts with their ports. Distinct from the already-fixed `UnicodeDecodeError` guard on the same function: that one covers git's output being undecodable, this one covers git deliberately escaping it into valid ASCII.
  resolution: Resolved by deletion (Story 6.9, 2026-08-09) — `scripts/spec_surface_check.py` is deleted; the "fixing in the original and the port together" premise no longer holds because only the port remains. The inherited `core.quotePath` gap itself is not patched (this story's Boundaries don't ask for it), only its two-copies blocking reason.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-6-5: All three source-independence tests (`test_sources_chain_independence.py`, `test_sources_ledger_independence.py`, `test_sources_board_independence.py`) skip every relative import (`if node.level: continue`) on the premise that a relative import is "never a station" — but these modules live at `pyforge.doctor.sources.*`, so a level-3 relative import resolves straight into the `pyforge` namespace and `from ...marshal import x` would pass all three tests.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md`
  summary: All three source-independence tests (`test_sources_chain_independence.py`, `test_sources_ledger_independence.py`, `test_sources_board_independence.py`) skip every relative import (`if node.level: continue`) on the premise that a relative import is "never a station" — but these modules live at `pyforge.doctor.sources.*`, so a level-3 relative import resolves straight into the `pyforge` namespace and `from ...marshal import x` would pass all three tests.
  evidence: Identified independently by adversarial review; confirmed by reading `test_sources_chain_independence.py:37` and its two siblings, which carry the identical line and the identical premise. The Charter §6 guard these files exist to enforce — "the Doctor holds the verdict on the Marshal's own conformance", which is only meaningful if Doctor's judgement never routes through Marshal's code — therefore has a real hole: the AST scan skips the import form, and the companion textual test only inspects string constants. Not exploited today (no relative import in any of the three modules reaches past `..`), and not caused by this story, which copied the established sibling pattern verbatim as its Boundaries require. Fixing it in chain's test alone would leave two of three guards holed, so it wants one change across all three files.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-6-6: `_spec_entry` iterates `covers-dreams:` with `for c in (fm.get("covers-dreams") or [])`, so a value written as a scalar string instead of a YAML block sequence is iterated CHARACTER BY CHARACTER — the consolidated Dream is still reported FAIL `dream-without-spec` (a false gating failure) while any one-character Dream slug would be silently marked covered.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md`
  summary: `_spec_entry` iterates `covers-dreams:` with `for c in (fm.get("covers-dreams") or [])`, so a value written as a scalar string instead of a YAML block sequence is iterated CHARACTER BY CHARACTER — the consolidated Dream is still reported FAIL `dream-without-spec` (a false gating failure) while any one-character Dream slug would be silently marked covered.
  evidence: Identified independently by both review lenses; confirmed by inspection. `covers-dreams: docs/dreams/foo.md` is a plausible authoring slip (the adjacent `owner-dream:` IS a scalar), and it parses CLEANLY through `yaml.safe_load`, so neither `_frontmatter`'s shape guard nor `_append_spec_entry`'s per-spec isolation fires — there is no WARN, only a wrong verdict. `scripts/dream_chain_check.py:137-140` and `:150-153` carry the identical expression, so the port is faithful and the defect is inherited; recorded rather than patched for the same reason as the `core.quotePath` entry — diverging in one of two live copies makes the two detectors disagree, so it belongs to Story 6.9's reconciliation. Fix is a two-line coercion (`raw = fm.get("covers-dreams") or []; raw = [raw] if isinstance(raw, str) else raw`) in both files.
  resolution: Resolved by deletion (Story 6.9, 2026-08-09) — `scripts/dream_chain_check.py` is deleted; the "diverging in one of two live copies" premise no longer holds because only the port remains. The inherited scalar-vs-list parsing gap itself is not patched (this story's Boundaries don't ask for it), only its two-copies blocking reason.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md, scripts/dream_chain_check.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-6-7: `gather_spec_surface` gives no honest answer when `target` is a SUBDIRECTORY of a repo rather than its root — `git ls-files` succeeds and returns subdirectory-relative paths, `target.glob(SPEC_GLOB)` finds no specs, and the result is a storm of false `ungoverned` FAILs plus `no-baseline`, with nothing saying "this is not a monorepo root".

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md`
  summary: `gather_spec_surface` gives no honest answer when `target` is a SUBDIRECTORY of a repo rather than its root — `git ls-files` succeeds and returns subdirectory-relative paths, `target.glob(SPEC_GLOB)` finds no specs, and the result is a storm of false `ungoverned` FAILs plus `no-baseline`, with nothing saying "this is not a monorepo root".
  evidence: Identified by adversarial review; the mechanism is confirmed by inspection (`_tracked_files` runs `run_git(target, ...)` with `cwd=target`, and both `SPEC_GLOB` and `ALLOWLIST_REL`/`BASELINE_REL` are resolved relative to `target`). This review pass FIXED the two sibling cases where the same root cause produced a SILENT false-clean verdict — `gather_dream_chain` and `gather_deferred_work` now WARN when their input trees are absent instead of reporting a confident OK — and deliberately left this one, because its failure mode is loud rather than silent: an operator sees dozens of FAILs, not a clean bill of health. It still deserves the same honest "not a monorepo root" WARN, but choosing the probe (`git rev-parse --show-toplevel` comparison vs. a marker-file check) is a design call shared with whatever Story 6.9 does when it wires these gathers behind `doctor check`'s own `path` argument. status_note: Story 6.9 (2026-08-09) did NOT wire these gathers behind `doctor check`'s `path` argument — its own dispatcher (`python -m pyforge.doctor.sources <name>`) takes no target argument at all and always judges the repo it runs in (Boundaries), and `__main__.py`'s verb dispatch is untouched (Never clause). STILL OPEN for whichever future story adds that wiring — recorded here, not closed.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-6-8: `tests/unit/test_check_speed_budget.py` is a load-sensitive gate that now fails far more often than it passes on this machine — identical, unmodified code measured 4.21s, 4.25s, 4.38s, 4.53s, 4.61s, 4.70s, 5.25s, 5.28s, 5.29s, 5.71s, 6.88s, 7.07s, 7.71s, 7.72s, 9.13s, 9.51s and 10.46s against a hard 5.0s budget.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md`
  summary: `tests/unit/test_check_speed_budget.py` is a load-sensitive gate that now fails far more often than it passes on this machine — identical, unmodified code measured 4.21s, 4.25s, 4.38s, 4.53s, 4.61s, 4.70s, 5.25s, 5.28s, 5.29s, 5.71s, 6.88s, 7.07s, 7.71s, 7.72s, 9.13s, 9.51s and 10.46s against a hard 5.0s budget.
  evidence: Observed while verifying this story: one full-suite run passed (672 passed) and the next six runs of the same test failed, with no code change between them. Not caused by this story — proven structurally, not just by timing: `doctor check` dispatches only the checks registered in `__main__.py`, `sources/__init__.py` has no `from . import chain`, and `grep chain src/.../doctor/__main__.py` is empty, so `sources/chain.py` is never imported during the measured run (this story deliberately wires nothing into `__main__.py`, per its own Boundaries). The spec's own Design Notes record the budget being set against a live measurement of "~2.1-2.5s combined ... ~2x headroom", so the environment is now running roughly 2-4x slower than when 5.0s was chosen. Left open rather than papered over with a raised constant: the honest options (re-measure and re-baseline the budget, switch from max to a median/percentile, or mark it as a benchmark excluded from the default gate) are a PRD SM-C1 decision, and silently loosening an NFR-4 gate is exactly the kind of change that should be deliberate.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md, tests/unit/test_check_speed_budget.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-6-9: `_parse_surface`'s hand-rolled frontmatter reader recognises exactly one `surface:` spelling (a block sequence indented `  - `), so three other spellings that are perfectly valid YAML — a quoted glob (`  - "src/**"`), a flow sequence (`surface: [a, b]`), and any other indentation — parse "successfully" to an EMPTY surface, silently un-governing every file the spec owns with no `spec-surface-unevaluable` WARN to say anything went wrong.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md`
  summary: `_parse_surface`'s hand-rolled frontmatter reader recognises exactly one `surface:` spelling (a block sequence indented `  - `), so three other spellings that are perfectly valid YAML — a quoted glob (`  - "src/**"`), a flow sequence (`surface: [a, b]`), and any other indentation — parse "successfully" to an EMPTY surface, silently un-governing every file the spec owns with no `spec-surface-unevaluable` WARN to say anything went wrong.
  evidence: Reproduced by adversarial edge-case review against live fixtures: a quoted glob yields the literal glob string `'"src/**"'` (matching nothing), and a flow sequence is skipped entirely, both leaving `globs == []`. The consequence is the same confidently-wrong pair the module's own `_parse_surface` docstring says it exists to prevent — every owned file reported FAIL `ungoverned` and every baselined one FAIL `drift ... removed` — except this path produces NO warning at all, because the parse did not raise. `scripts/spec_surface_check.py` carries the identical reader, so the port is faithful and the defect is inherited, not introduced; recorded rather than patched for the same reason as the `core.quotePath` and `covers-dreams` entries above (diverging in one of two live copies makes the repo-root detector and the Doctor port disagree on the same tree). No live SPEC.md currently uses any of the three unrecognised spellings, so nothing is mis-reported today. Belongs with Story 6.9's reconciliation; the fix is either a tolerant strip of surrounding quotes plus flow-sequence support, or a validation pass that raises (and therefore WARNs) on a `surface:` key that yields zero globs.
  resolution: Resolved by deletion (Story 6.9, 2026-08-09) — `scripts/spec_surface_check.py` is deleted; the "diverging in one of two live copies" premise no longer holds because only the port remains. The inherited quoted-glob/flow-sequence parsing gap itself is not patched (this story's Boundaries don't ask for it), only its two-copies blocking reason.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-6-10: Correction to the earlier `spec-pyforge-doctor` baseline-drift entry in this ledger (left unmodified, per the orchestrator's ownership of existing entries): its evidence line concludes the drift is "confirming this predates 6.6 and is not caused by it", which is true of the inherited findings but NOT of all of them — 9 of the live `[drift]` findings are Story 6.6's own files, so reconciling that baseline is partly this story's debt, not purely 6.2-6.5's.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md`
  summary: Correction to the earlier `spec-pyforge-doctor` baseline-drift entry in this ledger (left unmodified, per the orchestrator's ownership of existing entries): its evidence line concludes the drift is "confirming this predates 6.6 and is not caused by it", which is true of the inherited findings but NOT of all of them — 9 of the live `[drift]` findings are Story 6.6's own files, so reconciling that baseline is partly this story's debt, not purely 6.2-6.5's.
  evidence: Verified against the live repo after this review pass: `scripts/spec_surface_check.py` reports 26 `[drift]` findings against `pyforge-doctor/spec-pyforge-doctor`, and cross-referencing them with `git diff --name-only 5dcb6429..HEAD` shows 9 are introduced by this story — `sources/chain.py` and its four new test files (`added`), plus `pixi.toml`, `pyproject.toml`, `data/report-schema.json`, `models.py` and `sources/__init__.py` (`changed`). Neither commit on this branch touches `.memlog.md` or `.spec-surface-baseline.json`. This does not change the deferral decision (reconciling a baseline that spans four already-merged stories is repo-hygiene work outside this story's Surface, and the drift gates `spec-pyforge-doctor`'s own separate spec-surface check rather than anything this story's ACs or tests assert) — it corrects the RATIONALE, so whoever picks the entry up does not act on the premise that Story 6.6 contributed nothing to it.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-6-11: `score._is_gather_failure` is hardcoded to `finding.check == "doctor.sources.atlas" and not finding.evidence`, so a chain gather that fails WHOLESALE — the `degrade_on_exception` WARN, which carries `check="dream-chain"`/`"spec-surface"`/`"deferred-work"` and a non-empty `evidence={"exception": ...}` — is scored as an ordinary WARN rather than an unevaluable axis, grading the axis **C** instead of `INCOMPLETE`.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md`
  summary: `score._is_gather_failure` is hardcoded to `finding.check == "doctor.sources.atlas" and not finding.evidence`, so a chain gather that fails WHOLESALE — the `degrade_on_exception` WARN, which carries `check="dream-chain"`/`"spec-surface"`/`"deferred-work"` and a non-empty `evidence={"exception": ...}` — is scored as an ordinary WARN rather than an unevaluable axis, grading the axis **C** instead of `INCOMPLETE`.
  evidence: Reproduced directly: `score.grade((Finding(source=Source.DREAM_CHAIN, check="dream-chain", status=WARN, evidence={"exception": "OSError"}),))` returns overall grade `C` with `axis dream-chain ok/warn/fail 0 1 0 -> C`; `score._is_gather_failure` on that same finding returns `False`, failing BOTH halves of its condition (`_GATHER_FAILURE_CHECK` at `score.py:52` names only atlas, and the chain WARN's evidence is non-empty). Latent today rather than live: Story 6.6 deliberately wires none of the three gathers into `__main__.py`/`doctor check` (its own Boundaries), so nothing feeds these findings to `score.grade()` yet — which is exactly why it is recorded rather than patched here. It matters because the whole justification for this module degrading to WARN is that a WARN is the honest alternative to a confident OK; downstream, an axis that could not be evaluated AT ALL should poison the composite to `incomplete`, not contribute a middling letter. The same gap applies to `sources/board.py`'s and `sources/ledger.py`'s own `degrade_on_exception` labels, so the fix is one generalisation of the predicate (e.g. keying on a shared `*-unevaluable` convention or on `evidence["exception"]`), not a per-module patch — and it belongs with whichever story wires these gathers behind a CLI.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-6-12: Status update to the earlier relative-import independence-guard entry in this ledger (left unmodified, per the orchestrator's ownership of existing entries): its premise "fixing it in chain's test alone would leave two of three guards holed" has been overtaken — this review pass FIXED `test_sources_chain_independence.py`, so the remaining work is `test_sources_ledger_independence.py` and `test_sources_board_independence.py` only.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md`
  summary: Status update to the earlier relative-import independence-guard entry in this ledger (left unmodified, per the orchestrator's ownership of existing entries): its premise "fixing it in chain's test alone would leave two of three guards holed" has been overtaken — this review pass FIXED `test_sources_chain_independence.py`, so the remaining work is `test_sources_ledger_independence.py` and `test_sources_board_independence.py` only.
  evidence: `test_sources_chain_independence.py` now resolves relative imports against the module's own package (`_resolve_relative`) instead of skipping them, and carries a live regression test (`test_a_relative_import_of_a_station_is_resolved_not_waived`) plus an explicit `stations` tuple that excludes `doctor` itself; mutation-confirmed — restoring `if node.level: continue` fails the suite. It was patched rather than left because the hole is a real defect in the guard this story's OWN module depends on for its entire Charter §6 rationale, and it was verified exploitable first: inserting `from ...marshal import policy` into `sources/chain.py` passed all five of that file's tests beforehand. The two sibling files still carry the original `if node.level: continue` line verbatim and are outside this story's Surface.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-6-the-chain-verdicts-come-home.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-8-2: `sources/factory.py`'s `gather()` recomputes `_ground_truth()`/`_live_version()` independently up to four times per call (`check_pins`, `check_deferred_work`, `check_counts`, and again inside `check_baseline`'s `_fingerprint`), where the original script's `run_checks()` computed it once and threaded it into each check.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md`
  summary: `sources/factory.py`'s `gather()` recomputes `_ground_truth()`/`_live_version()` independently up to four times per call (`check_pins`, `check_deferred_work`, `check_counts`, and again inside `check_baseline`'s `_fingerprint`), where the original script's `run_checks()` computed it once and threaded it into each check.
  evidence: Confirmed by inspection — each of the four call sites calls `_ground_truth(target)`/`_live_version(target)` independently rather than receiving a shared value. No live consequence today: `Source.BMAD_DRIFT` is not wired into `__main__.py`/`doctor check` dispatch (this story's own Boundaries), and after this same review pass removed the dead `recipes_churny` computation, the remaining per-call cost is a handful of small-file reads (`CHANGELOG.md`, `conda_forge_atlas.py` x2, `SKILL.md`, `pixi.toml`), not a directory scan — so it is a real but currently minor inefficiency, not a budget threat (SM-C1). Worth threading through (matching the original's own data flow) when a future story wires this gather behind `doctor check`'s dispatch, at which point Story 6.1's per-gather cost attribution would surface it precisely.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-8-3: `check_coverage` and `check_stale_rules` walk the whole `pyforge-marshal` project tree via `Path.rglob`, which swallows `OSError` mid-traversal and silently yields nothing for an unreadable NESTED subdirectory (as opposed to the top-level project directory, which this review pass hardened via a raising `_is_dir` mirroring `sources/chain.py`'s own `_probe`/`_is_dir`) — so a permission-denied subtree several levels deep can still silently drop files from `check_coverage`'s `uncovered` HARD findings with no WARN naming the gap.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md`
  summary: `check_coverage` and `check_stale_rules` walk the whole `pyforge-marshal` project tree via `Path.rglob`, which swallows `OSError` mid-traversal and silently yields nothing for an unreadable NESTED subdirectory (as opposed to the top-level project directory, which this review pass hardened via a raising `_is_dir` mirroring `sources/chain.py`'s own `_probe`/`_is_dir`) — so a permission-denied subtree several levels deep can still silently drop files from `check_coverage`'s `uncovered` HARD findings with no WARN naming the gap.
  evidence: Reproduced structurally by inspection (same `Path.rglob`/`Path.glob` swallowing class `sources/chain.py`'s own Design Notes and review history document at length, closed there via `_listdir`/`_probe`/`_is_dir`/`_is_file` after three review passes). This story's own spec (Design Notes, "Inherited risk knowingly left to the review pass, not pre-solved") explicitly anticipated this exact question and left the file-walk-granularity decision to review; this pass closed the TOP-LEVEL form (the `_is_dir` gate checks, six call sites plus `_gather`'s own project-dir guard) but left the deeper mid-`rglob` case, since closing it fully would need a custom raising recursive walker (chain.py's own precedent took three passes to converge on its equivalent). Not reproduced against live data — no unreadable subdirectory exists in this repo's own `pyforge-marshal` tree today.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-8-4: `_read()` in `sources/factory.py` catches only `OSError`, not `UnicodeDecodeError`, so a tracked doc containing invalid UTF-8 raises past it into whichever `check_*` function called it — inherited verbatim from `scripts/bmad_drift_check.py`'s own identical `_read()`.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md`
  summary: `_read()` in `sources/factory.py` catches only `OSError`, not `UnicodeDecodeError`, so a tracked doc containing invalid UTF-8 raises past it into whichever `check_*` function called it — inherited verbatim from `scripts/bmad_drift_check.py`'s own identical `_read()`.
  evidence: Confirmed by direct comparison of both `_read()` implementations (both `except OSError: return ""`, and `UnicodeDecodeError` is a `ValueError` subclass, not an `OSError` subclass, so neither catches it). The port's own per-check isolation (this story's core AC) means the practical effect differs slightly from the original: in the port, the one check calling `_read` on the bad file degrades to a `bmad-drift-unevaluable` WARN naming that check (losing that check's OTHER findings for the same run) rather than the original's silent `""` treatment (which risks a different failure — e.g. a real pin silently reading as "missing" because the regex matched empty text). Not reproduced against live data — no tracked doc in this repo currently contains invalid UTF-8. Preserving the original's exact behavior here (rather than improving it unilaterally) matches this story's own Boundaries ("preserve every finding kind/message from the origin script verbatim in behavior"); fixing it well would need per-file rather than per-check granularity.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-8-5: `check_spec_status` treats any spec status matching neither `TERMINAL_STATUS` nor `NONTERMINAL_STATUS` (e.g. `"blocked"`, `"on-hold"`) as not-yet-terminal and skips the stale-status check entirely, even when a matching retro exists proving the spec actually shipped — inherited verbatim from the original's identical regex-driven skip condition.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md`
  summary: `check_spec_status` treats any spec status matching neither `TERMINAL_STATUS` nor `NONTERMINAL_STATUS` (e.g. `"blocked"`, `"on-hold"`) as not-yet-terminal and skips the stale-status check entirely, even when a matching retro exists proving the spec actually shipped — inherited verbatim from the original's identical regex-driven skip condition.
  evidence: Confirmed by direct comparison — both implementations share the identical `if not status or TERMINAL_STATUS.search(status) or not NONTERMINAL_STATUS.search(status): continue` line. A spec whose frontmatter status is a word outside both regexes' vocabularies silently escapes the `spec-status-stale` check regardless of retro evidence. Not reproduced against live data (no live spec currently carries such a status). Faithful port of a pre-existing origin-script gap; belongs with Story 6.9's reconciliation of the origin scripts themselves, per the same reasoning already recorded for `spec_surface_check.py`'s inherited gaps in the Story 6.6 entries above.
  resolution: Resolved by deletion (Story 6.9, 2026-08-09) — `scripts/bmad_drift_check.py` is deleted; there is no longer an "origin script" left to reconcile against. The inherited vocabulary gap itself is not patched (this story's Boundaries don't ask for it), only its two-copies blocking reason.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-8-6: A stray throwaway file (`.patch`/`.diff`/`.bak`/`.orig`/`.tmp`/`.rej`) nested in a subdirectory, or filed under `planning-artifacts/` instead of `implementation-artifacts/`, is invisible to the whole detector — `check_archive_hygiene` only scans `implementation-artifacts/`'s top level (`impl.iterdir()`, non-recursive), and `check_coverage` deliberately exempts any `STRAY_SUFFIXES` file from its `uncovered` HARD finding — inherited verbatim from the original.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md`
  summary: A stray throwaway file (`.patch`/`.diff`/`.bak`/`.orig`/`.tmp`/`.rej`) nested in a subdirectory, or filed under `planning-artifacts/` instead of `implementation-artifacts/`, is invisible to the whole detector — `check_archive_hygiene` only scans `implementation-artifacts/`'s top level (`impl.iterdir()`, non-recursive), and `check_coverage` deliberately exempts any `STRAY_SUFFIXES` file from its `uncovered` HARD finding — inherited verbatim from the original.
  evidence: Confirmed by direct comparison — both implementations share the identical non-recursive `impl.iterdir()` scan and the identical `path.suffix not in STRAY_SUFFIXES` coverage exemption (commented `# strays handled elsewhere` in both). Not reproduced against live data. Faithful port of a pre-existing origin-script gap.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-8-7: `check_dream_owners`'s `dream-unowned` finding message hardcodes the literal text "the eight Smiths" rather than deriving the station count from the `docs/governance/guild-roster.json` roster it now reads — inherited verbatim from the original, but notable because Part 1 of this same story (merged ahead of this branch) consolidated the roster specifically to stop this class of hand-mirrored-vocabulary drift, and the count is exactly the kind of fact that consolidation was meant to keep honest.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md`
  summary: `check_dream_owners`'s `dream-unowned` finding message hardcodes the literal text "the eight Smiths" rather than deriving the station count from the `docs/governance/guild-roster.json` roster it now reads — inherited verbatim from the original, but notable because Part 1 of this same story (merged ahead of this branch) consolidated the roster specifically to stop this class of hand-mirrored-vocabulary drift, and the count is exactly the kind of fact that consolidation was meant to keep honest.
  evidence: Confirmed present verbatim in both `scripts/bmad_drift_check.py` (`f"owner {owner!r} is not one of the eight Smiths"`) and the port. Not a live inconsistency today (the roster currently does hold eight stations), but if a station is ever added or removed, the message becomes self-contradictory without either script's own tests catching it (the port's own `test_dream_owned_by_an_unknown_station_reports_warn` intentionally uses a 2-station fixture roster and still asserts the literal "eight Smiths" substring, since the message is meant to be verbatim-identical to the origin regardless of the real roster — a deliberate parity choice, not an oversight, but one that means no test would notice the count drifting from reality either). status: open
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 2/3 present (absent: _bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-8-8: `check_counts`'s four ground-truth probes still degrade silently when their source file cannot be read: `_mcp_tool_count`/`_env_count` return `0` for an absent or unreadable `.claude/tools/conda_forge_server.py`/`pixi.toml` (so `int(stated) < 0` is never true and the MCP-tools and pixi-envs probes can never fire), while `_schema_version`/`_gotcha_max` return `None` and are skipped by the check's own `if live_val is None: continue` — in all four cases a doc stating a stale count reads as clean with no WARN naming the gap.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md`
  summary: `check_counts`'s four ground-truth probes still degrade silently when their source file cannot be read: `_mcp_tool_count`/`_env_count` return `0` for an absent or unreadable `.claude/tools/conda_forge_server.py`/`pixi.toml` (so `int(stated) < 0` is never true and the MCP-tools and pixi-envs probes can never fire), while `_schema_version`/`_gotcha_max` return `None` and are skipped by the check's own `if live_val is None: continue` — in all four cases a doc stating a stale count reads as clean with no WARN naming the gap.
  evidence: Confirmed by inspection of all four helpers plus `check_counts`'s probe loop. This is the same "ground truth that could not be read must not read as clean" class the follow-up review pass DID close for `_live_version` (a missing `CHANGELOG.md` silently erased all 17 `pin-behind` findings) and `_max_single_phase` (a missing atlas registry fabricated `omits phases through N`), both reproduced live and fixed in that pass. It is deferred rather than fixed with them for a real structural reason: `0` is a LEGITIMATE value for a count in a way `(0,0,0)` never is for a live version, so distinguishing "0 tools" from "file unreadable" requires changing the helpers' return contracts (`int` -> `int | None`) and then deciding, per probe, whether an unreadable source deserves a WARN even when no tracked doc states that count at all — the same lazy-evaluation judgement `check_phase_lists` needed. Inherited verbatim from `scripts/bmad_drift_check.py`, which had the same hole but printed all four values in its report header (`schema v29 | 46 MCP tools | 22 phases | 20 pixi envs | G1-G107`) where an operator would see a zero; this library module has no header. Not reproduced against live data — all four source files are present and readable in this repo.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 2/3 present (absent: _bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-8-9: The origin's `check_coverage()` returned `(findings, summary)` and its CLI printed `coverage: 195 files classified — tracked:spec=110 archive:research=14 …` on EVERY run including clean ones; the port drops `summary` entirely and emits no "how much did you measure?" signal, so an operator cannot tell a project whose files were all classified from one whose tree was mostly unwalkable.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md`
  summary: The origin's `check_coverage()` returned `(findings, summary)` and its CLI printed `coverage: 195 files classified — tracked:spec=110 archive:research=14 …` on EVERY run including clean ones; the port drops `summary` entirely and emits no "how much did you measure?" signal, so an operator cannot tell a project whose files were all classified from one whose tree was mostly unwalkable.
  evidence: Confirmed by direct comparison (`scripts/bmad_drift_check.py:590` returns the tuple, `:806` prints it unconditionally; `sources/factory.py`'s `check_coverage` returns findings only). This contradicts a convention Story 6.7 stated explicitly for the immediately-preceding port, recorded in this spec dir's own `.memlog.md`: "The coverage summary is likewise emitted unconditionally, including on a red run — 'how much did you measure?' matters most precisely when something failed" — and `sources/board.py` implements the same idea (`evidence={"measured": measured}`, plus an explicit refusal to "claim a clean grid that was not measured"). Deferred rather than patched because closing it means adding a NINETEENTH finding kind to a module whose own Boundaries mandate preserving the origin's 18 kinds verbatim, and because that new OK finding would make `_gather`'s `if not findings` aggregate-OK branch unreachable, changing this gather's whole return contract — a surface change that belongs with the CLI/dispatch-wiring story where a renderer for it exists, not in a review pass. Note the same reasoning already recorded in this module for dropping the origin's `recipes_churny` value ("this module has no printer to feed") applies, but is weaker here: `recipes_churny` fed only the header, whereas the coverage summary is a measurement-completeness signal the fleet convention says should survive.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-8-10: Nothing pins `sources/factory.py`'s ported `classify()` (143 lines, byte-identical to `scripts/bmad_drift_check.py`'s modulo the `_rel(path)` -> `_rel(path, target)` signature change) and its six copied constants (`TRACKED`, `CONFIG_FILES`, `STALE_RULE_PATTERNS`, `STRAY_SUFFIXES`, `_PIN_RE`, `FINGERPRINT_KEYS`) to their origin, so the next filing-convention change lands in the script — where `pixi run bmad-drift-check` and `test_bmad_artifacts_in_sync.py` red — and silently diverges the port, which no test would notice.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md`
  summary: Nothing pins `sources/factory.py`'s ported `classify()` (143 lines, byte-identical to `scripts/bmad_drift_check.py`'s modulo the `_rel(path)` -> `_rel(path, target)` signature change) and its six copied constants (`TRACKED`, `CONFIG_FILES`, `STALE_RULE_PATTERNS`, `STRAY_SUFFIXES`, `_PIN_RE`, `FINGERPRINT_KEYS`) to their origin, so the next filing-convention change lands in the script — where `pixi run bmad-drift-check` and `test_bmad_artifacts_in_sync.py` red — and silently diverges the port, which no test would notice.
  evidence: Confirmed by mechanical diff of both `classify()` bodies during the second follow-up review pass. Divergence is not hypothetical: `git log -- scripts/bmad_drift_check.py` shows the classification rules changed TWICE in the two weeks before this story (`9d3ea9e229` "11 unclassified files", `5648a8b8ff` "a drift-check coverage fix"), each adding rules for a new artifact shape. A silent divergence would make `check_coverage` emit `uncovered` HARD/FAIL findings against correctly-filed files. Verified deferrable rather than patchable in this pass on scope grounds: NO sibling port in Epic 6 has an origin-parity test either (`test_sources_chain_spec_surface.py`, `test_sources_board_dashboard_drift.py`, `test_sources_ledger.py` all test their module against tmp fixtures only), and this story's own Verification section prescribes a MANUAL parity diff — so adding one here would set an epic-wide convention from inside a single story's review. The cheap form is a meta-test that `importlib`-loads the origin script, retargets its module globals at a shared tmp fixture, and asserts `run_checks()`'s `(severity, kind, target, detail)` set equals `gather()`'s minus the aggregate/unevaluable rows; the reviewer built exactly that harness ad hoc (14 targeted scenarios + 80 randomized fuzz repos, ~1s, ZERO divergences today — the port is faithful as of this pass). Belongs with Story 6.9's reconciliation of the origin scripts, or Epic 6's closeout meta-test, where it can cover all ten ports at once.
  resolution: Resolved by deletion (Story 6.9, 2026-08-09) — `scripts/bmad_drift_check.py` is deleted; `classify()` can no longer silently diverge from an origin that no longer exists. `sources/factory.py::classify` is now the sole implementation. An origin-parity test was never built (still no sibling Epic 6 port has one, per this entry's own evidence) — that gap, if it matters, is a separate concern from the one this entry recorded.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-8-11: `check_spec_indexed` fabricates a `spec-unindexed` WARN for EVERY file in `docs/specs/` when `CLAUDE.md` is simply absent from the target — `_read` correctly answers `""` for a genuinely-absent file, but the check's membership test is `p.name not in claude`, and every name is "not in" the empty string.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md`
  summary: `check_spec_indexed` fabricates a `spec-unindexed` WARN for EVERY file in `docs/specs/` when `CLAUDE.md` is simply absent from the target — `_read` correctly answers `""` for a genuinely-absent file, but the check's membership test is `p.name not in claude`, and every name is "not in" the empty string.
  evidence: Confirmed by inspection and reproduced against a tmp fixture: a target with `docs/specs/alpha.md` + `beta.md` and no `CLAUDE.md` yields two `spec-unindexed` findings, each naming a spec that is not in fact unindexed — there is simply no index to check against. Inherited verbatim from `scripts/bmad_drift_check.py`, which reads `CLAUDE.md` through the identical `_read` and has the identical membership test; the origin was unreachable in practice because its module-level `REPO_ROOT` always named the repo it lives in, where `CLAUDE.md` exists by construction. The port generalizes to an arbitrary `gather(target)`, which is exactly what makes the case reachable. Distinct from the absent-vs-unreadable class the third review pass closed: `_read` is behaving correctly here (the file really is absent); the defect is the check treating "no index exists" as "the index omits everything" rather than as its own honest cannot-evaluate. Not reproduced against this repo (`CLAUDE.md` is present); the fix is a `_is_file` gate before the loop, which belongs with the same pass that decides whether an absent index is a WARN or a silent skip.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 2/4 present (absent: _bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md, docs/specs/alpha.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-8-12: `check_spec_status`'s shipped-detection guards the SPEC slug against emptiness but not the RETRO slug — `any(sslug and (sslug in rs or rs in sslug) for rs in retro_slugs)` — so a single retro file whose `_slug` reduces to the empty string (e.g. `retro-.md`) makes `rs in sslug` true for every spec, flagging every in-flight spec in the project as `spec-status-stale` ("a matching retro exists — it shipped").

- source_spec: `_bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md`
  summary: `check_spec_status`'s shipped-detection guards the SPEC slug against emptiness but not the RETRO slug — `any(sslug and (sslug in rs or rs in sslug) for rs in retro_slugs)` — so a single retro file whose `_slug` reduces to the empty string (e.g. `retro-.md`) makes `rs in sslug` true for every spec, flagging every in-flight spec in the project as `spec-status-stale` ("a matching retro exists — it shipped").
  evidence: Confirmed by direct comparison — `scripts/bmad_drift_check.py:365` carries the identical expression with the identical one-sided guard, so this is a faithful port of a pre-existing origin gap, not a porting error. `_slug` strips a leading `spec-`/`retro-` prefix and the extension, so `retro-.md` reduces to `""`; Python's `"" in anything` is unconditionally true. The blast radius is the whole check rather than one file: every non-terminal spec in `implementation-artifacts/` acquires a false DRIFT finding, and the finding's message actively asserts the spec shipped. Reproduced against a tmp fixture: one `retros/retro-.md` plus one wholly unrelated `spec-totally-unrelated.md` marked `status: in-progress` yields `status 'in-progress' but a matching retro exists — it shipped`. Not reproduced against live data (no such file exists in any project today), and the trigger requires a degenerate filename, which is why it is deferred rather than patched — but it is a one-token fix (`rs and`) whenever the origin scripts are next reconciled, and belongs with the other inherited-verbatim `check_spec_status` gap already recorded above.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-6-8-bmad-drift-comes-home-without-breaking-the-board.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-9: `docs/dashboard/generate.py`'s `_task_cmd()` classifies any pixi task whose `cmd` starts with the literal token `python` as directly runnable on the bare GitHub Pages build (no pixi, no `local-recipes` env, no `pyforge-doctor`). Before this story that was safe: `bmad_drift_check.py`/`spec_surface_check.py`/`llms_full_check.py` were genuinely stdlib-only. After this story, `bmad-drift-check`/`bmad-groundtruth`/`spec-surface-check`'s new `cmd`s (`python -m pyforge.doctor.sources ...`) still start with `python` but now need `pyforge-doctor` (and its `jsonschema`/`PyYAML`/`mcp` deps) — unavailable on the Pages runner. `subprocess.run` doesn't raise on a nonzero exit, so `_run_detector`'s pixi-fallback (which *would* correctly degrade to `state="unknown"`) never triggers; the traceback's exit code instead reads as `state="drift"` for those 2-3 dashboard rows.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-9-the-scripts-shims-retire.md`
  summary: `docs/dashboard/generate.py`'s `_task_cmd()` classifies any pixi task whose `cmd` starts with the literal token `python` as directly runnable on the bare GitHub Pages build (no pixi, no `local-recipes` env, no `pyforge-doctor`). Before this story that was safe: `bmad_drift_check.py`/`spec_surface_check.py`/`llms_full_check.py` were genuinely stdlib-only. After this story, `bmad-drift-check`/`bmad-groundtruth`/`spec-surface-check`'s new `cmd`s (`python -m pyforge.doctor.sources ...`) still start with `python` but now need `pyforge-doctor` (and its `jsonschema`/`PyYAML`/`mcp` deps) — unavailable on the Pages runner. `subprocess.run` doesn't raise on a nonzero exit, so `_run_detector`'s pixi-fallback (which *would* correctly degrade to `state="unknown"`) never triggers; the traceback's exit code instead reads as `state="drift"` for those 2-3 dashboard rows.
  evidence: Traced by inspection of `docs/dashboard/generate.py:_task_cmd`/`_run_detector` (unmodified by this story — `docs/dashboard/generate.py` is explicitly outside this story's Surface, epics.md does not name it). `_task_cmd` returns `[sys.executable, "-m", "pyforge.doctor.sources", "bmad-drift", "--groundtruth"]` for the new `bmad-groundtruth` cmd, which `_run_detector` then runs directly with the bare Pages interpreter — `ModuleNotFoundError`, non-zero exit, caught only by the generic `except Exception: continue` that also catches the intended pixi-fallback path, so the fallback (`pixi run --frozen -e local-recipes bmad-groundtruth`) is never reached either (no pixi on Pages). This is a **mislabel, not a false green** (`state="drift"` never satisfies `state == "green"`) — recorded per this story's own Design Notes ("Known, accepted consequence — not fixed here"), not patched here, since fixing it correctly belongs with whoever owns `generate.py`'s classifier contract.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/implementation-artifacts/spec-6-9-the-scripts-shims-retire.md, docs/dashboard/generate.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-9-2: `sources/board.py::gather_check_layout`'s geometry assertions (chip edges, no overlap, no clipping) become PERMANENTLY UNREACHABLE once `docs/dashboard/check_layout.py` is deleted — not merely degraded in one environment, but for every future invocation of the `check-layout` source, in every environment, forever. `_load_check_layout` dynamically `exec_module`s `target / "docs" / "dashboard" / "check_layout.py"` to REUSE (never reimplement) that script's own pure `check()`/`_rows()` functions — a Story 6.5 design choice ("preserve, don't redesign") made without anticipating that Story 6.9 would delete the very file this port depends on at every call. With the file gone, `gather_check_layout` always returns exactly one WARN ("console-bar layout could not be evaluated here — FileNotFoundError"), which is `check_layout.py`'s own documented, TESTED degrade path (`test_missing_check_layout_module_degrades_to_warn`, an existing pinned test written for a `tmp_path` fixture that happens to also describe the real repo's post-retirement state) — so this is a designed degradation, not a crash, but it is a silent, total loss of real coverage: the `dashboard-layout-check` pixi task and the `check-layout` Doctor source will never again report a real chip-overlap/clipping FAIL, only ever this one WARN.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-9-the-scripts-shims-retire.md`
  summary: `sources/board.py::gather_check_layout`'s geometry assertions (chip edges, no overlap, no clipping) become PERMANENTLY UNREACHABLE once `docs/dashboard/check_layout.py` is deleted — not merely degraded in one environment, but for every future invocation of the `check-layout` source, in every environment, forever. `_load_check_layout` dynamically `exec_module`s `target / "docs" / "dashboard" / "check_layout.py"` to REUSE (never reimplement) that script's own pure `check()`/`_rows()` functions — a Story 6.5 design choice ("preserve, don't redesign") made without anticipating that Story 6.9 would delete the very file this port depends on at every call. With the file gone, `gather_check_layout` always returns exactly one WARN ("console-bar layout could not be evaluated here — FileNotFoundError"), which is `check_layout.py`'s own documented, TESTED degrade path (`test_missing_check_layout_module_degrades_to_warn`, an existing pinned test written for a `tmp_path` fixture that happens to also describe the real repo's post-retirement state) — so this is a designed degradation, not a crash, but it is a silent, total loss of real coverage: the `dashboard-layout-check` pixi task and the `check-layout` Doctor source will never again report a real chip-overlap/clipping FAIL, only ever this one WARN.
  evidence: Reproduced live during this story's own AFTER-deletion verification: BEFORE deleting `docs/dashboard/check_layout.py`, `pixi run -e local-recipes dashboard-layout-check` (with `playwright` installed) reported `OK: console bar edges held, no overlap — 30 measurement(s): 6 width(s) x 5 font-pressure step(s)` — a real, working browser-driven geometry check. AFTER deletion, the identical command reports `console-bar-layout: warn -- console-bar layout could not be evaluated here — FileNotFoundError: docs/dashboard/check_layout.py not found`, unconditionally, regardless of `playwright`'s availability. `sources/board.py`'s own module docstring already names the tradeoff in the abstract ("the reuse the original script's parsers by executing them family") but nothing in Story 6.5's spec, Story 6.9's spec, or `sources/__init__.py`'s `CHECK_LAYOUT` registration comment anticipated the SPECIFIC consequence that Story 6.9 itself would delete the reused file. Not fixed here: Story 6.9's Boundaries mandate deleting `docs/dashboard/check_layout.py` as one of the ten origin files, and porting `check()`/`_rows()`'s actual geometry-assertion LOGIC directly into `board.py` (rather than dynamically loading it) would be a real redesign of a Story 6.5 artifact, outside this story's Surface. The fix belongs to whichever future story owns `sources/board.py`'s contract: inline `check()`/`_rows()` (and their probe constants) directly into `board.py`, closing the dependency on the file this story deletes.
  resolution: FIXED (2026-08-09, coordinator-requested follow-up, same session) — the initial pass's "not fixed here, outside this story's Surface" judgment was overridden: a verdict that can never again report anything but "could not evaluate" is a verdict that died, not one that came home, which is severe enough to belong to this story after all. `sources/board.py` gained `_check_layout_geometry`/`_layout_chip_rows`/`_serve_layout_dir`/`_LayoutQuietHandler` plus the `_LAYOUT_WIDE`/`_LAYOUT_NARROW`/`_LAYOUT_BREAKPOINT`/`_LAYOUT_CENTRE_TOL`/`_LAYOUT_CHIPS`/`_LAYOUT_EDGE_TOL`/`_LAYOUT_PRESSURES`/`_LAYOUT_DESIGN_SIZE`/`_LAYOUT_APPLY`/`_LAYOUT_PROBE` constants, ported verbatim in behavior from the origin's own `check()`/`_rows()`/`_serve()`/`_QuietHandler`/module constants (git history: `git show HEAD~1:docs/dashboard/check_layout.py` from this story's own deletion commit); `_load_check_layout` and its dynamic `exec_module` of `check_layout.py` are deleted entirely. Re-verified live post-fix: `python -m pyforge.doctor.sources check-layout` against this repo (the origin file genuinely absent from disk) now reports `console-bar-layout: ok -- console bar edges held, no overlap — 30 measurement(s): 6 width(s) x 5 font-pressure step(s)` — byte-identical to the pre-deletion output, confirmed via the dispatcher, the `dashboard-layout-check` pixi task, and `scripts/detectors.py`'s own registry. `pyforge-doctor-test` went from 817 passed/12 skipped to 824 passed/1 skipped (the 11 recovered skips were exactly this defect's own test-suite symptom: `test_sources_board_check_layout.py`'s `_HAVE_REAL_DASHBOARD` guard, which checked for `check_layout.py`'s presence). No narrower residual gap: the ported functions are now the SOLE implementation (no origin file survives to diverge from), and the orchestration (`_run_check_layout`) is unchanged in shape, only in where it reads its constants/assertion function from. This entry is kept rather than deleted per this story's own Boundaries ("Never delete a deferred-work.md entry outright... append, never erase").
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/3 present (absent: _bmad-output/implementation-artifacts/spec-6-9-the-scripts-shims-retire.md, docs/dashboard/check_layout.py); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-9-3: `scripts/detectors.py`'s `_DOCTOR_SOURCE_TASKS` is a second, hand-maintained, unvalidated name→pixi-task mapping for the ten ported Doctor sources. Unlike scanned `*_check.py` detectors — which get a free "declares scope but has no pixi task" check via `discover()`'s `_pixi_tasks()` cross-reference — nothing verifies these ten hardcoded task names actually exist in `pixi.toml`, or that the task's `cmd` really invokes `python -m pyforge.doctor.sources <name>`. This reintroduces, for the ported half of the registry, exactly the "hand-typed list silently drifts" failure mode `detectors.py`'s own opening docstring was written to eliminate (the 2026-07-31 incident it names: three disagreeing registries, `dream_chain_check` missing from two, `check_layout` from all three).

- source_spec: `_bmad-output/implementation-artifacts/spec-6-9-the-scripts-shims-retire.md`
  summary: `scripts/detectors.py`'s `_DOCTOR_SOURCE_TASKS` is a second, hand-maintained, unvalidated name→pixi-task mapping for the ten ported Doctor sources. Unlike scanned `*_check.py` detectors — which get a free "declares scope but has no pixi task" check via `discover()`'s `_pixi_tasks()` cross-reference — nothing verifies these ten hardcoded task names actually exist in `pixi.toml`, or that the task's `cmd` really invokes `python -m pyforge.doctor.sources <name>`. This reintroduces, for the ported half of the registry, exactly the "hand-typed list silently drifts" failure mode `detectors.py`'s own opening docstring was written to eliminate (the 2026-07-31 incident it names: three disagreeing registries, `dream_chain_check` missing from two, `check_layout` from all three).
  evidence: Found by review pass 2 (Blind Hunter, independent adversarial pass on the story's full diff). Confirmed by inspection: `_DOCTOR_SOURCE_TASKS` is a literal `dict[str, str]` in `detectors.py`, cross-referenced nowhere against `_pixi_tasks()`'s live `pixi.toml` parse the way scanned detectors are. Not reproduced as a live divergence today (all ten names currently match real tasks) — the risk is a FUTURE pixi task rename silently going unnoticed, the same shape of bug the module exists to prevent for the scanned half of the registry. Deferred rather than patched in this pass: closing it means either generalizing `_pixi_tasks()`'s cross-check to cover both scanned and ported entries, or adding a dedicated assertion, either of which touches `detectors.py`'s core discovery contract and deserves its own focused pass rather than a review-pass patch.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/implementation-artifacts/spec-6-9-the-scripts-shims-retire.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-6-9-4: Three of the eleven retired pixi tasks silently lost CLI-level scoping flags in the retarget to the dispatcher. `ledger-regression-check` used to expose `--base <rev>`/`--head <rev>`; `dream-chain-check` and `chain-completeness-check` used to expose `-- --inv INV-2`/`-- --inv INV-A` filtering. The underlying library gathers (`sources/ledger.py::gather(target, *, base=..., head=...)`, etc.) still accept these as keyword arguments, but `sources/__main__.py`'s `DISPATCH` only ever calls `DISPATCH[name](target)` with no passthrough, so the capability is unreachable from any CLI entrypoint — no flag exists anywhere to restore it — and neither `pixi.toml`'s new task descriptions nor any doc notes this is a deliberate capability trim rather than an oversight.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-9-the-scripts-shims-retire.md`
  summary: Three of the eleven retired pixi tasks silently lost CLI-level scoping flags in the retarget to the dispatcher. `ledger-regression-check` used to expose `--base <rev>`/`--head <rev>`; `dream-chain-check` and `chain-completeness-check` used to expose `-- --inv INV-2`/`-- --inv INV-A` filtering. The underlying library gathers (`sources/ledger.py::gather(target, *, base=..., head=...)`, etc.) still accept these as keyword arguments, but `sources/__main__.py`'s `DISPATCH` only ever calls `DISPATCH[name](target)` with no passthrough, so the capability is unreachable from any CLI entrypoint — no flag exists anywhere to restore it — and neither `pixi.toml`'s new task descriptions nor any doc notes this is a deliberate capability trim rather than an oversight.
  evidence: Found by review pass 2 (Blind Hunter). Confirmed by inspection: `sources/__main__.py`'s `_build_parser()` defines only `source`, `--json`, `--groundtruth` — no scoping flags for any of the ten dispatch entries. Cross-referenced against the pre-baseline `pixi.toml` (`git show 181db8e0ae:pixi.toml`), which did carry `--base`/`--head`/`-- --inv` on these three tasks' `cmd`s. No confirmed live caller uses these flags today (grepped this repo, `_bmad-output/`, and CI workflows for `ledger-regression-check -- --base`/`chain-completeness-check -- --inv`/`dream-chain-check -- --inv`: no hits). Deferred rather than patched: restoring the capability means extending `_build_parser()`/`main()` with per-source optional arguments the dispatcher's own "one thin, target-less CLI" design (see its module docstring) did not anticipate, which is a real design decision belonging with whoever next needs the scoping, not a review-pass patch.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (legacy legacy-flat entry, no prior id)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-6-9-the-scripts-shims-retire.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-8-1: `_anonymous()`'s state machine still swallows the next unrelated headerless bullet after an `IDENTIFIED_PLAIN`-shaped `### DW-<n>:` header, silently dropping it from the Tier-3-anonymous count -- this story's Never clause explicitly leaves `_anonymous()` itself unfixed, so the bug stays live in `gather_deferred_work`'s `tier3-entry-unidentified` check after this landing.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-8-1-the-parser-reads-every-legacy-tier-3-shape.md`
  summary: `_anonymous()`'s state machine still swallows the next unrelated headerless bullet after an `IDENTIFIED_PLAIN`-shaped `### DW-<n>:` header, silently dropping it from the Tier-3-anonymous count -- this story's Never clause explicitly leaves `_anonymous()` itself unfixed, so the bug stays live in `gather_deferred_work`'s `tier3-entry-unidentified` check after this landing.
  evidence: `_anonymous()` (chain.py) only sets `field_taken = True` on a bulleted `- source_spec:` line matching `_ANON_RE`; a header whose own body uses plain, non-bulleted `origin:`/`source_spec:`/`severity:`/`reason:`/`status:` keys (or has no recognizable field at all) never satisfies that regex, so `in_entry` stays `True` and `field_taken` stays `False` all the way to the FIRST later headerless `- source_spec:` bullet -- which then gets wrongly claimed (not counted anonymous), no matter how topically unrelated it is. This story's own `classify_tier3_entries` (added here) does not repeat the bug -- an `IDENTIFIED_PLAIN` header's claim ends the moment its own field block ends (a blank line, a heading, or a sibling bulleted line), never later in the file -- and a live diff of `classify_tier3_entries(path)` against `_anonymous(path)` over each project's real Tier-3 `deferred-work.md` proves the live defect at fleet scale: 38 `IDENTIFIED_PLAIN` headers exist across the 8 projects' Tier-3 files, 25 of which swallow a real, unrelated orphan this way (atlas 4/9, doctor 4/5, herald 5/6, marshal 9/9, mason 2/3, steward 1/4, warden 0/2, scribe 0/0) -- pinned permanently for marshal's 9/9 by this story's own `test_identified_plain_shape_and_marshal_swallow_regression_real_excerpt` and `test_live_marshal_file_all_nine_identified_plain_headers_do_not_swallow` in `test_sources_chain_deferred_work.py`. This measured 25/38 count supersedes this story's own Intent section's earlier "29 headers, 21 swallow" estimate (gathered before `classify_tier3_entries` existed): the gap is entirely headers whose body is prose with no plain `key: value` line at all (e.g. three of herald's identified-plain headers, each a follow-up-review recommendation for spec-13-4 and its two later review passes), which still trigger the identical mechanism but were missed by whatever narrower pattern the earlier pass matched on -- `classify_tier3_entries` is the more complete, re-runnable measurement and should be trusted over the earlier one. Not patched in this pass: this story's Never clause is explicit ("`_anonymous()`'s own `IDENTIFIED_PLAIN` swallow-bug stays live in the detector after this story ... do not fix `_anonymous()` itself") -- fixing it means either rewriting `_anonymous()`'s positional state machine (touching a function this story is forbidden to touch) or re-deriving the Tier-3-anonymous count from `classify_tier3_entries` itself (a `gather_deferred_work` behavior change this story's Boundaries also forbid, since it would move existing `tier3-entry-unidentified` finding counts). A future story can now do either safely, re-using `classify_tier3_entries` as its source of truth rather than re-deriving the swallow detection from scratch.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-8-1-the-parser-reads-every-legacy-tier-3-shape.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-8-4: A project promoted once via `--fix` can never promote a genuinely new orphan added later, because the whole-batch-abort collision guard aborts on the OLD, already-promoted orphan's now-expected collision against its own tracked twin -- there is currently no way to promote just the new entries while skipping an old, already-handled one that still collides.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-8-4-the-baseline-re-stamps-so-a-second-run-is-a-no-op.md`
  summary: A project promoted once via `--fix` can never promote a genuinely new orphan added later, because the whole-batch-abort collision guard aborts on the OLD, already-promoted orphan's now-expected collision against its own tracked twin -- there is currently no way to promote just the new entries while skipping an old, already-handled one that still collides.
  evidence: Live-verified by Edge Case Hunter during Story 8.4's own review (Review Triage Log 2026-08-15, `[high]` `[defer]` finding), and independently re-confirmed live against the real fleet while landing this story: `deferred_work_promote.py::_promote_project` re-classifies EVERY orphan in a project's Tier-3 file on each `--fix` run (Tier-3 is append-only and never mutated by this script, so an already-promoted orphan's Tier-3 bullet is unchanged and still classified as an orphan with `entry.id is None`), then `_validate_batch` correctly refuses to re-promote it because its (normalized) summary now matches its own tracked twin already in the ledger (`"... already exists in the tracked ledger"`). That guard is exactly right for a lone repeat run -- it is Story 8.3's own duplicate-summary protection, and the exact property Story 8.4 relies on for "a second `--fix` run is a true no-op" (see `test_second_fix_run_against_the_same_fixture_is_a_true_noop`). But `_promote_project`'s batch-abort semantics are WHOLE-PROJECT, not per-entry: `_validate_batch`'s `problems` list aborts the ENTIRE batch on ANY collision, so a project that gets even one genuinely NEW orphan added to its Tier-3 file after its first successful promotion can never promote that new orphan -- the batch containing it always also contains the old, already-promoted orphan, which always collides, which always aborts the whole batch, forever. This is not a hypothetical: re-running `--fix` against a tmp copy of every one of the fleet's 8 real, live Tier-3/tracked-ledger pairs (2026-08-15, during this story's own landing) showed ALL 8 projects (atlas, doctor, herald, marshal, mason, scribe, steward, warden) already have at least one already-tracked collision in their current backlog, so all 8 would ABORT outright today with no write at all if `--fix` were run against them unscoped; mason's own real backlog alone aborts on 4 already-tracked collisions out of 25 live orphans, and a live `--fix --project mason` run against a tmp copy of its real files reproduced exactly that abort. Not patched in this pass: fixing it needs a redesign of the batch-abort semantics into a per-entry skip/promote model (treat an old orphan's now-expected tracked-ledger collision as "already handled, skip", while still hard-aborting on a genuinely NEW collision) -- a real, separate future story's scope, not a patch to this one. This is Story 8.3's own collision-detection design (already merged, not introduced by Story 8.4); Story 8.4's own lifecycle testing (the first exercise of "run `--fix`, add new content, run `--fix` again" against this promoter) is simply what surfaced it live.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-8-4-the-baseline-re-stamps-so-a-second-run-is-a-no-op.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-9-2: `gather()`'s `projects_dir.iterdir()` call and each project's `(project_dir / "planning-artifacts").is_dir()` check run outside any try/except, so a `PermissionError` (or a directory vanishing mid-iteration) raises straight out of `gather()` and crashes the entire fleet-wide sweep, contradicting the module's own docstring claim that "degrades, never crashes" is "the house rule for every Doctor source" -- yet this is not a novel mistake: `sources/board.py::gather_chain_completeness` (Story 6.5, already shipped) has the byte-identical shape (`projects_dir.iterdir()` and the per-project `is_dir()` check both unguarded, only the inner `_check_project_chain_completeness` call wrapped), and the story spec's own Boundaries explicitly directed this story to mirror that exact function.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-9-2-the-sweep-runs-against-all-eight-stations.md`
  summary: `gather()`'s `projects_dir.iterdir()` call and each project's `(project_dir / "planning-artifacts").is_dir()` check run outside any try/except, so a `PermissionError` (or a directory vanishing mid-iteration) raises straight out of `gather()` and crashes the entire fleet-wide sweep, contradicting the module's own docstring claim that "degrades, never crashes" is "the house rule for every Doctor source" -- yet this is not a novel mistake: `sources/board.py::gather_chain_completeness` (Story 6.5, already shipped) has the byte-identical shape (`projects_dir.iterdir()` and the per-project `is_dir()` check both unguarded, only the inner `_check_project_chain_completeness` call wrapped), and the story spec's own Boundaries explicitly directed this story to mirror that exact function.
  evidence: Found independently by both Blind Hunter and Edge Case Hunter during Story 9.2's review pass (2026-08-15); Edge Case Hunter named the precise lines (`sources/hygiene.py`, the `projects_dir.iterdir()` call and the per-project `planning-artifacts` `is_dir()` check, both ahead of the per-station `try/except Exception` that starts one line later). Confirmed by direct inspection of `sources/board.py`'s `gather_chain_completeness`: `if not projects_dir.is_dir(): return findings` followed by `for project_dir in sorted(projects_dir.iterdir()): if not (project_dir / "planning-artifacts").is_dir(): continue`, with no try/except around either line -- the exact pattern `hygiene.py` was asked to mirror, carrying the same gap forward rather than introducing a new one. Not patched in this pass: fixing only `hygiene.py` would leave it inconsistent with the very precedent it claims to follow and would not close the same gap in `board.py`; a real fix needs to harden both files' top-level directory-listing step together (wrap the listing itself in a try/except that degrades to one WARN, same shape as the existing per-project/per-station catch), which is a cross-cutting hardening pass outside this story's Code Map (`board.py` is not a file Story 9.2 touches).
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-9-2-the-sweep-runs-against-all-eight-stations.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-9-2-2: `_evaluate_station` runs all five hygiene checks (`_check_dead_test_scaffolding`, `_check_hollow_sprint_status`, `_check_readme_placeholder`, `_check_stale_dream_status`, `_check_orphan_files`) sequentially, isolated only as one unit by `gather()`'s per-station `try/except`; if one check raises (e.g. a malformed `sprint-status.yaml`), every check ordered after it for that same station never runs, so a real, independent finding on that same station (e.g. a genuine README placeholder or orphan file) is silently dropped rather than degraded to its own WARN.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-9-2-the-sweep-runs-against-all-eight-stations.md`
  summary: `_evaluate_station` runs all five hygiene checks (`_check_dead_test_scaffolding`, `_check_hollow_sprint_status`, `_check_readme_placeholder`, `_check_stale_dream_status`, `_check_orphan_files`) sequentially, isolated only as one unit by `gather()`'s per-station `try/except`; if one check raises (e.g. a malformed `sprint-status.yaml`), every check ordered after it for that same station never runs, so a real, independent finding on that same station (e.g. a genuine README placeholder or orphan file) is silently dropped rather than degraded to its own WARN.
  evidence: Found by Blind Hunter during Story 9.2's review pass (2026-08-15). The story spec's own Boundaries scoped isolation to per-station granularity only ("mirrors `board.py::_check_chain_completeness`'s own per-project isolation"), and `board.py::_check_project_chain_completeness` has the identical shape: it runs its own several checks (INV-A/B/C/D) sequentially inside one function, isolated only at the whole-project level by its caller's single `try/except`, with no isolation between its own distinct invariant checks. `hygiene.py` faithfully mirrors that existing, already-shipped convention rather than inventing a new gap. Not patched in this pass: narrowing isolation to per-check granularity only in the new `hygiene.py` (while leaving `board.py`'s identical shape as-is) would fix this file inconsistently with the very precedent its own spec cites, and independent of the source of truth question -- whether finer-grained isolation should become the fleet's default here, or whether five separate try/except blocks per station is disproportionate for how rarely any of these five reads actually raises -- is a design-level call spanning at least two files, not a mechanical patch to this one story's diff.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-9-2-the-sweep-runs-against-all-eight-stations.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-9-2-3: `_has_inbound_references` (`sources/hygiene.py`) runs `git grep -l --fixed-strings -e <basename>` against the whole repo and treats ANY match (besides the candidate's own path) as an inbound reference -- a bare substring match, not path- or word-boundary-aware, and `git grep` without `--untracked`/`--no-index` only scans tracked content. Two independent latent false-negative shapes follow, both masking a real `ORPHAN_FILE` finding rather than crashing: (1) a genuinely orphaned candidate whose basename happens to be a substring mentioned elsewhere in the repo for an unrelated reason (prose, another file's name) reads as "referenced" even though nothing actually points at that specific file; (2) a candidate whose only real reference lives in a not-yet-`git add`ed file is invisible to `git grep`'s default tracked-only scan, so it reads as unreferenced when it is not. This is not a coding mistake against the story spec -- it is exactly the protocol the spec's own Design Notes mandate ("search the candidate's basename ... via `git grep -l --fixed-strings -e <basename>`") -- so it is a real, inherent limitation of the specified design, not a bug in its implementation.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-9-2-the-sweep-runs-against-all-eight-stations.md`
  summary: `_has_inbound_references` (`sources/hygiene.py`) runs `git grep -l --fixed-strings -e <basename>` against the whole repo and treats ANY match (besides the candidate's own path) as an inbound reference -- a bare substring match, not path- or word-boundary-aware, and `git grep` without `--untracked`/`--no-index` only scans tracked content. Two independent latent false-negative shapes follow, both masking a real `ORPHAN_FILE` finding rather than crashing: (1) a genuinely orphaned candidate whose basename happens to be a substring mentioned elsewhere in the repo for an unrelated reason (prose, another file's name) reads as "referenced" even though nothing actually points at that specific file; (2) a candidate whose only real reference lives in a not-yet-`git add`ed file is invisible to `git grep`'s default tracked-only scan, so it reads as unreferenced when it is not. This is not a coding mistake against the story spec -- it is exactly the protocol the spec's own Design Notes mandate ("search the candidate's basename ... via `git grep -l --fixed-strings -e <basename>`") -- so it is a real, inherent limitation of the specified design, not a bug in its implementation.
  evidence: Found independently by both Blind Hunter and Edge Case Hunter during this repair pass's re-review (2026-08-15), triggered while investigating the deterministic-verification failure this same pass fixed (a related but distinct self-reference hazard in `test_sources_hygiene.py`, already patched: see the pass's own triage log entry). Blind Hunter verified live that the literal filename `sprint-status.yaml` -- a real filename this module's OWN `_check_hollow_sprint_status` targets, not in `hygiene_definitions.py`'s `_CONVENTIONAL_FILENAMES` allowlist -- appears in 157 tracked files repo-wide, so any station's genuinely orphaned `planning-artifacts/sprint-status.yaml` would always resolve `has_inbound_references=True` and never surface, confirmed by direct inspection: `hygiene_definitions.py:134-143`'s `_CONVENTIONAL_FILENAMES` frozenset does not include `sprint-status.yaml` (only `sprint-status-ledger.yaml`), so such a file is not excluded by the pre-filter either. Edge Case Hunter independently named the same class of risk plus the untracked-file blind spot. No live false negative exists today (confirmed: no station currently has an unreferenced `sprint-status.yaml`), so this is latent, not currently manifesting, and does not affect this story's own acceptance criteria (all of which pass against the current live repo). Not patched in this pass: hardening the match (word/path-boundary awareness, excluding the searching module's own path, or including untracked content) would mean amending the Design Notes' own specified protocol, which is outside this repair pass's explicit charter (repair the deterministic-verification failure without touching the spec's frozen intent or expanding scope) -- the same reasoning `DW-FU-9-2` and `DW-FU-9-2-2` already recorded for this story's other precedent-mirroring gaps.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-9-2-the-sweep-runs-against-all-eight-stations.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-11-1: `_parse_verified_date` accepts any syntactically valid ISO date with no bound against `today`, so a hand-authored `verified:` line with a typo'd future year (e.g. `2126-07-15` instead of `2026-07-15`) parses cleanly, yields a large negative `days_stale`, and reads as permanently fresh -- silently and indefinitely exempting that entry from ever being selected again, the exact class of gap this epic exists to catch.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-11-1-due-for-verification-entries-are-selected-per-project.md`
  summary: `_parse_verified_date` accepts any syntactically valid ISO date with no bound against `today`, so a hand-authored `verified:` line with a typo'd future year (e.g. `2126-07-15` instead of `2026-07-15`) parses cleanly, yields a large negative `days_stale`, and reads as permanently fresh -- silently and indefinitely exempting that entry from ever being selected again, the exact class of gap this epic exists to catch.
  evidence: Found by Blind Hunter during Story 11.1's own review pass (2026-08-15). `chain._parse_verified_date` (added this story) only checks `date.fromisoformat(token)` for syntactic validity; `_check_project_due_for_verification` then computes `days_stale = (today - parsed).days` with no floor/ceiling check, so a future-dated `parsed` yields a negative `days_stale` that always fails the `> DUE_FOR_VERIFICATION_STALENESS_DAYS` test, i.e. always reads as fresh. Every one of the repo's ~400+ tracked entries is hand-authored prose, so a single transposed or extra digit in a `verified:` date is a plausible, ordinary human-error class, not an adversarial input. Not patched in this pass: the Spec's own I/O & Edge-Case Matrix only covers unparseable dates (explicitly resolved to "never-verified"), not out-of-range-but-parseable ones -- adding a sanity bound (e.g. reject/degrade any `verified:` date after `today`) is a new validation rule outside CAP-1's stated contract, and deciding the right bound (reject to never-verified? clamp? WARN separately?) is a design call for a follow-up story, not a mechanical patch to this one.
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-11-1-due-for-verification-entries-are-selected-per-project.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-9-3: `_check_stale_dream_status` sets `evidence["path"]` relative to the repo root (`docs/dreams/<slug>.md`, no `_bmad-output/projects/<slug>/` prefix), while the other 4 classes (`hollow_sprint_status`, `readme_placeholder`, `orphan_file`, and this story's own fix to `dead_test_scaffolding`) all set it relative to the station's own `project_dir` (e.g. `README.md`, `planning-artifacts/sprint-status.yaml`, `tests`) — a downstream consumer resolving `evidence["path"]` against one base directory gets it wrong for exactly the stale-dream-status class.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-9-3-hygiene-findings-report-and-never-mutate.md`
  summary: `_check_stale_dream_status` sets `evidence["path"]` relative to the repo root (`docs/dreams/<slug>.md`, no `_bmad-output/projects/<slug>/` prefix), while the other 4 classes (`hollow_sprint_status`, `readme_placeholder`, `orphan_file`, and this story's own fix to `dead_test_scaffolding`) all set it relative to the station's own `project_dir` (e.g. `README.md`, `planning-artifacts/sprint-status.yaml`, `tests`) — a downstream consumer resolving `evidence["path"]` against one base directory gets it wrong for exactly the stale-dream-status class.
  evidence: Found independently by both Blind Hunter and Edge Case Hunter during Story 9.3's review pass (2026-08-15). Confirmed by direct inspection of `hygiene.py`: `_check_stale_dream_status`'s `dream_path = target / "docs" / "dreams" / f"{project_dir.name}.md"` and its evidence line `f"docs/dreams/{project_dir.name}.md"` are both `target`-relative, while `_check_hollow_sprint_status`/`_check_readme_placeholder`/`_check_orphan_files` all build their evidence paths from `project_dir`-relative components. This predates Story 9.3 — Story 9.2 shipped 4 of the 5 evidence shapes, including the stale-dream-status outlier, unremarked at the time. Story 9.3 did not cause it but made it more consequential: before this story, only 4 of 5 classes had a `path` key at all (`dead_test_scaffolding` had none), so the split was less visible; now every class has one, so the inconsistent base is a real, load-bearing question for any future consumer. Not patched in this pass: renormalizing either shape is a design call (which base is "right," and whether to touch the other 4 already-shipped classes' evidence) explicitly outside this story's own Never clause ("no evidence key beyond `path`... the 4 already-shipped shapes are frozen; don't touch them").
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/3 present (absent: _bmad-output/projects/<slug>/, _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-9-3-hygiene-findings-report-and-never-mutate.md, docs/dreams/<slug>.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-11-2: `_churn_since`/`_authored_date` run fresh `git log` calls per ledger entry with no memoization across entries citing the same path, so a commonly-referenced file (e.g. `chain.py` itself, or `pixi.toml`) cited by many different due entries fleet-wide triggers duplicate, identical subprocess spawns once per citing entry — working directly against this story's own stated cost-reduction goal, though at a smaller order of magnitude than the agent-verification cost it exists to avoid.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-11-2-churn-based-cost-filtering-skips-entries-whose-code-has-not-moved.md`
  summary: `_churn_since`/`_authored_date` run fresh `git log` calls per ledger entry with no memoization across entries citing the same path, so a commonly-referenced file (e.g. `chain.py` itself, or `pixi.toml`) cited by many different due entries fleet-wide triggers duplicate, identical subprocess spawns once per citing entry — working directly against this story's own stated cost-reduction goal, though at a smaller order of magnitude than the agent-verification cost it exists to avoid.
  evidence: Found by Blind Hunter during Story 11.2's own review pass (2026-08-15). Confirmed by direct inspection: `_check_project_due_for_verification`'s per-entry loop calls `_attach_churn_skip` → `_churn_since`/`_authored_date` independently for every due entry, with no per-project or fleet-wide cache keyed by `(path, since)`. Not patched in this pass: correct memoization needs a `(path, since)` compound key (two entries citing the same path can have different churn-window start dates), threaded through the per-project loop without breaking the existing per-entry isolation contract — a genuine design addition beyond this story's spec-resolved scope, not a mechanical patch. Currently no measured performance problem: a live `pixi run -e local-recipes due-for-verification-check` against this repo's real ~400+ entries completes in a few seconds. Worth a dedicated follow-up once Story 11.3's usage pattern shows whether the duplicate-call volume actually matters at full fleet scale.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-11-2-churn-based-cost-filtering-skips-entries-whose-code-has-not-moved.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-11-2-2: `_churn_since` and `_authored_date` run `git log` with no `--all` or explicit ref, so they only see commits reachable from whatever branch `target` currently has checked out — a real change that landed on `main` but hasn't been merged/rebased into the worktree the sweep is run from is invisible to `--since`, so the entry can be wrongly tagged `skip_reason: no-churn`.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-11-2-churn-based-cost-filtering-skips-entries-whose-code-has-not-moved.md`
  summary: `_churn_since` and `_authored_date` run `git log` with no `--all` or explicit ref, so they only see commits reachable from whatever branch `target` currently has checked out — a real change that landed on `main` but hasn't been merged/rebased into the worktree the sweep is run from is invisible to `--since`, so the entry can be wrongly tagged `skip_reason: no-churn`.
  evidence: Found by Blind Hunter during Story 11.2's own review pass (2026-08-15). Confirmed by direct inspection: neither git call in `_churn_since` nor `_authored_date` passes `--all` or a ref argument, so both walk from `target`'s checked-out HEAD only — plain `git log`'s own default behavior. This is not unique to Story 11.2: every other git-based Doctor check in this file (`_tracked_files`'s `git ls-files`, `sources/marshal.py::gather_story_status`'s merge-commit-grep and main-subject-log) makes the identical ambient assumption that `target` reflects the state worth judging — Story 11.2 inherits this repo-wide convention rather than introducing a new one. Not patched in this pass: scoping to `--all` (or an explicit `origin/main` ref) is a cross-cutting design change spanning every git-based Doctor check, not a one-story fix, and the consequence here is bounded — `skip_reason` is an advisory, non-mutating, reversible annotation (Boundaries: this story makes zero ledger mutations) that the next sweep run from an up-to-date checkout self-corrects; worst case is one cost-optimization applied one run too early, never data loss or a false "resolved" verdict (Story 11.4's territory, not this one's).
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-11-2-churn-based-cost-filtering-skips-entries-whose-code-has-not-moved.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-10-1: `_declared_floors` walks every `dependencies`/`feature.*.dependencies`/target-scoped table in `pixi.toml` and `gather` compares the installed core against the MAXIMUM floor found across all of them, regardless of which pixi environment an operator actually resolves/activates -- if two features ever declare genuinely different `bmad-method` floors, an environment that only activates the lower-floor feature would be flagged WARN even though it satisfies every constraint that actually applies to it.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md`
  summary: `_declared_floors` walks every `dependencies`/`feature.*.dependencies`/target-scoped table in `pixi.toml` and `gather` compares the installed core against the MAXIMUM floor found across all of them, regardless of which pixi environment an operator actually resolves/activates -- if two features ever declare genuinely different `bmad-method` floors, an environment that only activates the lower-floor feature would be flagged WARN even though it satisfies every constraint that actually applies to it.
  evidence: Found by Blind Hunter during Story 10.1's own review pass (2026-08-15). This was a deliberate spec-level Boundaries decision ("read every occurrence and use the maximum declared floor, never assume exactly one line"), not an implementation slip, and is arguably the SAFER conservative default (an operator resolving any environment ends up with a core at least as new as the highest bar) -- but it does mean this source cannot currently express "declared floor per environment," only "the single highest floor anywhere in pixi.toml." Today's live `pixi.toml` declares `bmad-method >=6.11.0` identically in both `feature.python` and `feature.local-recipes` (verified via `python3 -c "import tomllib; ..."` during spec drafting), so this is not a live false-positive today -- it only bites if the two floors are ever allowed to diverge. Not patched in this pass: resolving this properly would require the Source to know which environment(s) an operator cares about (a new parameter/decision this Spec's CAP-1 scope never asked for), which is a design question for a follow-up, not a mechanical fix.
  severity: medium
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-10-1-2: `_FLOOR_RE`/`str(constraint)` only understands a bare `">=X.Y.Z"` string constraint. Pixi also supports an inline-table dependency spec (e.g. `bmad-method = { version = ">=6.11.0", channel = "..." }`) and a separate `[*.pypi-dependencies]` table shape; either form makes `str(constraint)` fail the regex, degrading to a generic "unrecognized constraint form" WARN with no actual version comparison performed.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md`
  summary: `_FLOOR_RE`/`str(constraint)` only understands a bare `">=X.Y.Z"` string constraint. Pixi also supports an inline-table dependency spec (e.g. `bmad-method = { version = ">=6.11.0", channel = "..." }`) and a separate `[*.pypi-dependencies]` table shape; either form makes `str(constraint)` fail the regex, degrading to a generic "unrecognized constraint form" WARN with no actual version comparison performed.
  evidence: Found independently by both Blind Hunter and Edge Case Hunter during Story 10.1's own review pass (2026-08-15). This IS the correct "cannot evaluate is never clean" degrade-never-crash behavior working as designed (a WARN naming the failure, never a silently-wrong verdict) -- not a live bug, since today's actual `pixi.toml` declares `bmad-method` as a plain string constraint in both occurrences (verified during spec drafting). Not patched in this pass: recognizing an inline-table spec's `version` key (and/or a `[pypi-dependencies]` table, though `bmad-method` is conda-only and would never realistically appear there) is a real but currently-hypothetical shape this repo's own `pixi.toml` doesn't use for this dependency -- worth adding if/when it does, not before.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-10-1-3: When two or more `pixi.toml` tables declare different `bmad-method` floors and the maximum is taken (`test_two_tables_different_floors_uses_the_maximum`'s own scenario), the Finding's `evidence` dict carries only `{"installed": ..., "declared_floor": ...}` -- nothing says WHICH table (`feature.python`? `feature.local-recipes`? a target-scoped one?) produced the winning value, leaving an operator who wants to act on the WARN no way to know where to actually bump the pin without grepping `pixi.toml` by hand.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md`
  summary: When two or more `pixi.toml` tables declare different `bmad-method` floors and the maximum is taken (`test_two_tables_different_floors_uses_the_maximum`'s own scenario), the Finding's `evidence` dict carries only `{"installed": ..., "declared_floor": ...}` -- nothing says WHICH table (`feature.python`? `feature.local-recipes`? a target-scoped one?) produced the winning value, leaving an operator who wants to act on the WARN no way to know where to actually bump the pin without grepping `pixi.toml` by hand.
  evidence: Found by Blind Hunter during Story 10.1's own review pass (2026-08-15). Confirmed by direct inspection of `_gather`'s `evidence` construction in `bmad_method.py` -- it only ever carries the two version strings, never the table/feature name(s) `_declared_floors` walked through. Not patched in this pass: the story's own Acceptance Criteria only require the message to name both versions (satisfied), not the source table -- adding table-name tracking means changing `_declared_floors`'s return shape from a plain list of version tuples to `(version, table_name)` pairs, a design change beyond this story's stated scope, not a mechanical one-line patch.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-10-1-4: `_declared_floors` raises on the FIRST unrecognized constraint form it encounters while walking `pixi.toml`'s dependency tables, discarding any valid floor already found in an earlier table -- if one table declared a plain `>=6.11.0` and a different table declared an unparseable `=="broken"`, the whole comparison degrades to one WARN "cannot evaluate" instead of comparing against the one perfectly good floor that was found.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md`
  summary: `_declared_floors` raises on the FIRST unrecognized constraint form it encounters while walking `pixi.toml`'s dependency tables, discarding any valid floor already found in an earlier table -- if one table declared a plain `>=6.11.0` and a different table declared an unparseable `=="broken"`, the whole comparison degrades to one WARN "cannot evaluate" instead of comparing against the one perfectly good floor that was found.
  evidence: Found by Edge Case Hunter during Story 10.1's own review pass (2026-08-15). Confirmed by direct inspection: the `raise ValueError` inside `_declared_floors`'s per-table loop fires on the first bad match, before the function returns anything, so an already-appended good floor from an earlier iteration is lost with it. This is a defensible "all or nothing" choice consistent with existing precedent elsewhere in this package (`sources/marshal.py`'s/`sources/warden.py`'s own documented all-or-nothing degrade contracts) rather than a design mistake -- unlike `sources/chain.py`'s per-check isolation (many independent checks, one bad unit shouldn't sink the others), this module has exactly ONE check with no natural per-table isolation boundary to preserve partial credit across. No live occurrence today: this repo's own `pixi.toml` declares `bmad-method` with the same well-formed constraint form everywhere. Not patched in this pass: choosing best-effort-partial vs. fail-whole-on-any-bad-table is a design call, not a mechanical fix.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-10-1-5: `sources/__init__.py`'s `degrade_on_exception` docstring claims it is "Deliberately NOT wired into today's three existing (repo-scope) dispatch calls." That claim was already false before this story (`board.py` x3, `factory.py` x1, `chain.py` x4 -- nine call sites, not zero of three) -- `bmad_method.py`'s own new call site (this story) is the fifth consecutive addition on top of an already-stale count, and `bmad_method.py`'s own docstring directly paraphrases the same (now further wrong) rationale.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md`
  summary: `sources/__init__.py`'s `degrade_on_exception` docstring claims it is "Deliberately NOT wired into today's three existing (repo-scope) dispatch calls." That claim was already false before this story (`board.py` x3, `factory.py` x1, `chain.py` x4 -- nine call sites, not zero of three) -- `bmad_method.py`'s own new call site (this story) is the fifth consecutive addition on top of an already-stale count, and `bmad_method.py`'s own docstring directly paraphrases the same (now further wrong) rationale.
  evidence: Found by Blind Hunter during Story 10.1's own review pass (2026-08-15). Confirmed by direct `grep -c` of `degrade_on_exception(` call sites across `sources/*.py`: 9 today, not the "zero of three" the docstring implies. Root cause predates this story -- `chain.py` (Story 11.x) already falsified the "three existing calls stay unwired" claim before Story 10.1 landed -- so this is a pre-existing documentation drift this story's own change surfaces incidentally, not a defect this story introduced. Not patched in this pass: the fix is a docstring rewrite in a shared module (`sources/__init__.py`) outside this story's own Code Map, and doing it here risks scope creep into files this story has no reason to touch.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-10-1-6: `_FLOOR_RE` (`^>=(\d+\.\d+\.\d+)$`) matches only a floor-only constraint with no leading/trailing whitespace and no upper bound. This repo's own `pixi.toml` already pins other dependencies with a compound range (`nodejs = ">=24.16.0,24.*,<25.0"`, `django = ">=5.2,<6"`, `conda-forge-ci-setup = ">=4.9.3,<5.0"`) and TOML permits internal whitespace after the operator (`">= 6.11.0"`) -- either shape on `bmad-method`'s own constraint would degrade to an uninformative "unrecognized constraint form" WARN instead of extracting the floor.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md`
  summary: `_FLOOR_RE` (`^>=(\d+\.\d+\.\d+)$`) matches only a floor-only constraint with no leading/trailing whitespace and no upper bound. This repo's own `pixi.toml` already pins other dependencies with a compound range (`nodejs = ">=24.16.0,24.*,<25.0"`, `django = ">=5.2,<6"`, `conda-forge-ci-setup = ">=4.9.3,<5.0"`) and TOML permits internal whitespace after the operator (`">= 6.11.0"`) -- either shape on `bmad-method`'s own constraint would degrade to an uninformative "unrecognized constraint form" WARN instead of extracting the floor.
  evidence: Found by Edge Case Hunter during Story 10.1's own review pass (2026-08-15); the compound-range sibling examples were grep-confirmed live in this repo's own `pixi.toml`. Not a live bug today -- `bmad-method`'s own two declarations are both the plain `>=X.Y.Z` form `_FLOOR_RE` expects (verified during spec drafting) -- but it is a real, currently-hypothetical shape one dependency-pin edit away from occurring, in the same "cannot evaluate degrades cleanly, never crashes or silently mis-parses" category as the already-tracked `DW-FU-10-1-2`. Not patched in this pass: extracting a floor from a compound range constraint is a small but real parsing-logic expansion beyond what this story's stated Boundaries ("no `packaging` dependency... a minimal tuple-of-ints parser suffices") scoped for.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-10-1-7: `_bmad/_config/manifest.yaml` carries both a top-level `installation.version` scalar AND a `modules` list where each entry (`core`, `bmm`, ...) has its own independent `version` field. `bmad_method.gather` reads only the top-level scalar -- if an upgrade updates `installation.version` but is interrupted before every module entry is rewritten (or vice versa), the two could diverge with no signal from this check.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md`
  summary: `_bmad/_config/manifest.yaml` carries both a top-level `installation.version` scalar AND a `modules` list where each entry (`core`, `bmm`, ...) has its own independent `version` field. `bmad_method.gather` reads only the top-level scalar -- if an upgrade updates `installation.version` but is interrupted before every module entry is rewritten (or vice versa), the two could diverge with no signal from this check.
  evidence: Found by Edge Case Hunter during Story 10.1's own review pass (2026-08-15). Confirmed by reading this repo's own live `_bmad/_config/manifest.yaml`: it has exactly this two-tier shape (`installation.version: 6.10.0` plus `modules: [{name: core, version: 6.10.0}, {name: bmm, version: 6.10.0}]`), currently in sync but structurally capable of diverging. Not a live bug today. Not patched in this pass: the story's own Intent names "the installed BMAD-METHOD framework version" as a single comparable value (the top-level scalar), and deciding whether/how to also reconcile per-module drift is a scope question for a follow-up, not a mechanical fix.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 1/2 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-1-the-declared-floor-and-the-installed-core-are-compared-and-reported.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-11-3: `_call_site_count` greps the WHOLE repo for a claimed symbol's bare name with no scoping to the entry's own cited paths, so two unrelated functions sharing a common/generic short name (e.g. `_run`, `_helper`) can attribute each other's usages to the wrong entry; separately, `git grep -w` matches any whole-word textual occurrence, so a comment or docstring mentioning the symbol (not an actual call) inflates the live count.

- source_spec: `_bmad-output/implementation-artifacts/spec-11-3-mechanically-checkable-claims-are-verified-without-an-agent.md`
  summary: `_call_site_count` greps the WHOLE repo for a claimed symbol's bare name with no scoping to the entry's own cited paths, so two unrelated functions sharing a common/generic short name (e.g. `_run`, `_helper`) can attribute each other's usages to the wrong entry; separately, `git grep -w` matches any whole-word textual occurrence, so a comment or docstring mentioning the symbol (not an actual call) inflates the live count.
  evidence: Found independently by both the Blind Hunter and Edge Case Hunter review passes on this story's diff (2026-08-15), confirmed by direct inspection of `_call_site_count`'s implementation (no pathspec beyond the ledger-exclusion, `git grep -w` with no AST awareness). This is a deliberate, spec-documented trade-off (Boundaries: "whole repo... via `git grep`", Design Notes: "intentionally simple... rather than an AST-based reference counter... matches this file's own established preference for a small duplicated shape over a heavier shared primitive"), not an oversight, and its failure direction is SAFE: both effects only ever push a genuinely-unused symbol toward `mechanical_verdict: escalate` (more findings deferred to Story 11.4's agent), never toward a false `still-open` confirm. Not patched in this pass: closing either gap (path-scoped grep, or AST-based call-site detection) is a real design expansion beyond CAP-3's own narrow scope, not a mechanical fix.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-11-3-mechanically-checkable-claims-are-verified-without-an-agent.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-11-3-2: `_entry_unused_symbol_claims` uses `_UNUSED_CLAIM_RE.search()`, which returns the FIRST match in document order. An entry mentioning two backtick-quoted identifiers where an "unused"-family phrase happens to sit close enough (within 80 chars, no intervening backtick) to the WRONG one -- e.g. "`` `_helper` `` unused? No, actually `` `_foo` `` is unused." -- would extract `_helper`, not the entry's actually-intended subject `_foo`.

- source_spec: `_bmad-output/implementation-artifacts/spec-11-3-mechanically-checkable-claims-are-verified-without-an-agent.md`
  summary: `_entry_unused_symbol_claims` uses `_UNUSED_CLAIM_RE.search()`, which returns the FIRST match in document order. An entry mentioning two backtick-quoted identifiers where an "unused"-family phrase happens to sit close enough (within 80 chars, no intervening backtick) to the WRONG one -- e.g. "`` `_helper` `` unused? No, actually `` `_foo` `` is unused." -- would extract `_helper`, not the entry's actually-intended subject `_foo`.
  evidence: Raised by Edge Case Hunter during this story's review pass (2026-08-15); verified by hand-tracing the regex against a constructed example where no backtick intervenes between the unintended symbol and the phrase. Consequence is bounded: Doctor is read-only and never mutates the ledger, so the worst outcome is a misleading `mechanical_symbol`/`mechanical_verdict` in the WARN report for that one entry, not a lost or corrupted claim -- a human or Story 11.4's agent still reads the entry's own prose before acting. Not patched in this pass: robustly identifying "the symbol the entry's unused-claim is actually about" out of free-form prose with multiple candidates is a real parsing-precision problem, not a mechanical fix, and no real fleet ledger entry observed during this story's development exhibits the pattern.
  severity: medium
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-11-3-mechanically-checkable-claims-are-verified-without-an-agent.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-11-3-3: `_call_site_count` parses each `git grep -n` output line via two chained `str.partition(":")` calls, assuming the format `path:lineno:content`. If the MATCHED FILE'S OWN PATH contains a colon, the first partition splits too early, shifting `lineno`/`content` and causing the declaration-line regex to fail to recognize a real `def`/`class`/`async def` line, inflating the call-site count.

- source_spec: `_bmad-output/implementation-artifacts/spec-11-3-mechanically-checkable-claims-are-verified-without-an-agent.md`
  summary: `_call_site_count` parses each `git grep -n` output line via two chained `str.partition(":")` calls, assuming the format `path:lineno:content`. If the MATCHED FILE'S OWN PATH contains a colon, the first partition splits too early, shifting `lineno`/`content` and causing the declaration-line regex to fail to recognize a real `def`/`class`/`async def` line, inflating the call-site count.
  evidence: Raised by Edge Case Hunter during this story's review pass (2026-08-15). Confirmed the parsing logic is exactly two sequential `partition(":")` calls with no defense against a colon in the PATH segment specifically (a colon inside the CONTENT segment is already handled correctly, since content is the unsplit remainder). One tracked path in this repo does contain a colon (`git ls-files | grep ':'` returns `docs/intake/gists/.../BMAD-METHOD SPEC: Enterprise Monorepo....md`, verified live -- an earlier draft of this note wrongly claimed zero, corrected after re-checking), so the parsing gap is not purely hypothetical; its practical bite is bounded, though, since that one file has zero `def`/`class` lines (`grep -c 'def \|class '` = 0), so a match inside it can never be miscategorized as a declaration -- worst case it is one more correctly-counted (if path-garbled) call site, same consequence as any other prose mention (see the sibling `DW-FU-11-3` entry). Not patched in this pass: a colon-safe parse (e.g. `git grep -z` for NUL-separated fields) is a real, narrow robustness improvement, not a mechanical fix warranted by this story's own scope given the bounded consequence.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-11-3-mechanically-checkable-claims-are-verified-without-an-agent.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-11-4: `apply_verification_verdicts.py`'s `_entry_spans` builds `{id: (start, end, fields)}` via a plain dict-building loop over `_ENTRY_RE` marks; if a tracked ledger's raw text happens to contain two entries sharing the identical `DW-` id (a malformed, invariant-violating ledger), the second occurrence's span silently overwrites the first in the dict, so a verdict applied against that id lands only on whichever occurrence happened to be scanned last, with no error surfaced and no indication which one was affected.

- source_spec: `_bmad-output/implementation-artifacts/spec-11-4-judgment-requiring-entries-get-an-evidence-grounded-verdict.md`
  summary: `apply_verification_verdicts.py`'s `_entry_spans` builds `{id: (start, end, fields)}` via a plain dict-building loop over `_ENTRY_RE` marks; if a tracked ledger's raw text happens to contain two entries sharing the identical `DW-` id (a malformed, invariant-violating ledger), the second occurrence's span silently overwrites the first in the dict, so a verdict applied against that id lands only on whichever occurrence happened to be scanned last, with no error surfaced and no indication which one was affected.
  evidence: Found by Edge Case Hunter during this story's review pass (2026-08-15), confirmed by direct inspection of `_entry_spans`'s dict-building loop (`spans[ident] = (pos, end, fields)`, unconditional overwrite on collision). Bounded and pre-existing in kind: this exact risk class is already explicitly accepted, for the SAME reason, in Story 11.1/11.2/11.3's own read-only `_check_project_due_for_verification` (its docstring: "a ledger with a duplicate (malformed, invariant-violating) id would silently collapse to one dict entry... positional pairing is correct regardless of whether ids repeat"), and the epic's own Spec Non-goals state this is "not a full ledger data-quality audit of pre-existing issues." A WRITE path silently picking the wrong duplicate is a strictly worse consequence than a READ-only tool's positional pairing, but still requires a pre-existing malformed ledger to trigger -- no real fleet ledger observed during this story's development carries a duplicate id. Not patched in this pass: detecting and refusing on a duplicate id is a real, narrow robustness improvement (matching this epic's own already-adjudicated precedent for how to treat this exact failure class), not a mechanical fix within CAP-4's own stated scope.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-11-4-judgment-requiring-entries-get-an-evidence-grounded-verdict.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-11-4-2: `apply_verification_verdicts.py` rejects a duplicate `(project, id)` pair WITHIN one verdicts-file batch, but nothing prevents the SAME verdicts-file from being applied twice across two separate invocations -- an operator re-running a script after an ambiguous/timed-out result, or a wrapper retrying on a transient failure, would silently append a second, byte-for-byte identical `verified: <date> — <verdict> — <evidence>` line to the same entry, with `exit 0` and no diagnostic distinguishing "a genuine re-check reached the same answer" from "this is an accidental duplicate."

- source_spec: `_bmad-output/implementation-artifacts/spec-11-4-judgment-requiring-entries-get-an-evidence-grounded-verdict.md`
  summary: `apply_verification_verdicts.py` rejects a duplicate `(project, id)` pair WITHIN one verdicts-file batch, but nothing prevents the SAME verdicts-file from being applied twice across two separate invocations -- an operator re-running a script after an ambiguous/timed-out result, or a wrapper retrying on a transient failure, would silently append a second, byte-for-byte identical `verified: <date> — <verdict> — <evidence>` line to the same entry, with `exit 0` and no diagnostic distinguishing "a genuine re-check reached the same answer" from "this is an accidental duplicate."
  evidence: Raised by Blind Hunter during this story's review pass (2026-08-15). Confirmed by direct inspection: the script's own design intentionally makes re-verification history purely additive (Boundaries: "NEVER remove or edit a prior verified: line... re-verification history is additive"), so a second `verified:` line for the same entry is the CORRECT shape when it reflects a genuine later re-check -- the gap is narrower: distinguishing an accidental exact-duplicate re-run from a deliberate identical-conclusion re-verification. Not patched in this pass: what counts as "the same verdict" (same date only? same date+verdict? same date+verdict+evidence, i.e. a full duplicate?) is a genuinely ambiguous scope decision this story's own Spec does not resolve, not a mechanical fix -- and the story's own explicit Boundaries ("Never: add flags... beyond what's needed") caution against expanding the CLI surface without a stated requirement.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/implementation-artifacts/spec-11-4-judgment-requiring-entries-get-an-evidence-grounded-verdict.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-10-2: `_gather` returns both the CAP-1 (declared-floor) and CAP-2 (upstream-npm) Findings under the same `Source.BMAD_METHOD_VERSION_DRIFT`. `score.grade()` groups Findings into axes by `Finding.source` alone (not `check`), so if this Source's Findings were ever fed into it, a CAP-1 `ok` + CAP-2 `warn` split (the story's own Problem-statement scenario: installed meets the declared floor but still lags the actual latest release) computes `warn/total = 0.5`, and `_axis_grade`'s own threshold (`Grade.C if warn/total > 0.5 else Grade.B`) is not satisfied -- so a real CAP-2 WARN that would grade `Grade.C` on its own is diluted to `Grade.B` by sharing an axis with CAP-1's independent `ok`.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-2-the-installed-core-is-compared-against-the-latest-upstream-release.md`
  summary: `_gather` returns both the CAP-1 (declared-floor) and CAP-2 (upstream-npm) Findings under the same `Source.BMAD_METHOD_VERSION_DRIFT`. `score.grade()` groups Findings into axes by `Finding.source` alone (not `check`), so if this Source's Findings were ever fed into it, a CAP-1 `ok` + CAP-2 `warn` split (the story's own Problem-statement scenario: installed meets the declared floor but still lags the actual latest release) computes `warn/total = 0.5`, and `_axis_grade`'s own threshold (`Grade.C if warn/total > 0.5 else Grade.B`) is not satisfied -- so a real CAP-2 WARN that would grade `Grade.C` on its own is diluted to `Grade.B` by sharing an axis with CAP-1's independent `ok`.
  evidence: station-unresolved: `_bmad/scripts/resolve_config.py --key project` reported `pyforge-mason` via a stray `BMAD_ACTIVE_PROJECT=pyforge-mason` environment variable present in this run's shell, while this run's own worktree-local `_bmad/custom/.active-project` marker and this very `implementation-artifacts/deferred-work.md` file's own resolved (`readlink -f`) path both independently agree on `pyforge-doctor` (confirmed by `readlink -f`) -- the env var is the outlier, not the marker, so this note is filed under `pyforge-doctor` per the marker/path agreement, with the disagreement recorded per the fleet's own "never mint station A's id shape into station B's ledger, say so instead" convention. Raised by Blind Hunter during this story's review pass (2026-08-20). Verified directly against `score.py`'s own `_axis_grade` (`if warn: return Grade.C if (warn / total) > 0.5 else Grade.B`) and `grade()`'s grouping (`by_source: dict[Source, list[Finding]]`, keyed on `Finding.source`). Confirmed the scenario is reachable via `bmad_method.gather`'s real live output whenever the installed core meets `pixi.toml`'s declared floor but lags the actual npm latest. Not live yet: `bmad-method-version-drift` is DISPATCH-only, not wired into `doctor check`/`monitor`'s composite `score.grade()` call today. Not patched in this pass: whether this Source's Findings ever feed `score.grade()` at all is undecided (Story 10.3's own scope, per the epic context, is Doctor's report/verdict output and `fleet-picture`'s ATTENTION block, not necessarily `score.grade()`); resolving the dilution -- grouping `score.grade()` by `(source, check)` instead of bare `source` (affecting every other multi-check Source too), or adding a second `Source` member for CAP-2 alone (this story's own spec explicitly rejected that, per `Finding.check`'s existing precedent for one Source backing multiple checks) -- is a real architecture decision for whoever wires this Source into `score.grade()`, out of this story's own scope (Boundaries: never wires into check/monitor).
  severity: medium
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/2 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-2-the-installed-core-is-compared-against-the-latest-upstream-release.md, _bmad/custom/.active-project); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-10-3: `bmad_core_drift_findings()` calls `subprocess.run(..., check=True)`, which only stays a clean "raise -> caller degrades to one watch line" story because `bmad_method.gather()`'s "always ok or warn, never fail" contract is asserted only in that module's own docstring, never pinned by a test anywhere in `test_sources_bmad_method.py`. If a future edit to `bmad_method.py` ever broke that promise, `doctor check --bmad-core` would still render the real FAIL finding faithfully (findings flow straight through `_gather_bmad_core` into the normal render/exit-code path), while `fleet_picture.py`'s `check=True` subprocess wrapper would instead collapse the same underlying data into the generic "could not check bmad-method core version drift" line -- the two surfaces this story wires up would disagree about what happened for identical input.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-3-the-drift-surfaces-ambiently-never-gates.md`
  summary: `bmad_core_drift_findings()` calls `subprocess.run(..., check=True)`, which only stays a clean "raise -> caller degrades to one watch line" story because `bmad_method.gather()`'s "always ok or warn, never fail" contract is asserted only in that module's own docstring, never pinned by a test anywhere in `test_sources_bmad_method.py`. If a future edit to `bmad_method.py` ever broke that promise, `doctor check --bmad-core` would still render the real FAIL finding faithfully (findings flow straight through `_gather_bmad_core` into the normal render/exit-code path), while `fleet_picture.py`'s `check=True` subprocess wrapper would instead collapse the same underlying data into the generic "could not check bmad-method core version drift" line -- the two surfaces this story wires up would disagree about what happened for identical input.
  evidence: Raised by Blind Hunter during this story's review pass (2026-08-20), verified directly against `exit_code_for` (`sources/__main__.py`'s CLI exits 2 only on a FAIL finding, which `check=True` would turn into a `CalledProcessError`) and `bmad_method.py`'s own docstring claim (never independently tested). Not live today: confirmed against this repo's own real, current drift (installed 6.10.0, both CAP-1/CAP-2 WARN) that the happy path works correctly end-to-end. Not patched in this pass: fixing it would mean either adding a dedicated `bmad_method.py` test pinning the never-FAIL contract, or reworking `fleet_picture.py`'s subprocess call to distinguish "genuine subprocess failure" from "the underlying source returned FAIL" -- both are reasonable but out of this story's own scope (Never: "changes bmad_method.py's own gather/_gather" is an explicit Boundary; and the fleet_picture.py behavior today is already safe, just imprecise, not a live bug).
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-3-the-drift-surfaces-ambiently-never-gates.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-10-3-2: Pre-existing tension from Story 10.2, already named in `sources/__init__.py`'s own `BMAD_METHOD_VERSION_DRIFT` REGISTRY comment (mirroring `CHECK_LAYOUT`'s loopback-socket/chromium precedent for the same field): CAP-2 makes a live, unauthenticated npm-registry GET despite being registered `scope="repo"`, the classification the rest of the codebase uses to mean "reads only tracked files/git history, runs anywhere" (CI-safe, offline-safe). Story 10.3 makes this concretely reachable and tested: `test_bmad_core_flag_matching_scope_repo_runs_fine` asserts `doctor check --bmad-core --scope repo` "runs fine," so an operator who picks `--scope repo` specifically to stay network-free during this story's own review pass would get a live call anyway, silently.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-3-the-drift-surfaces-ambiently-never-gates.md`
  summary: Pre-existing tension from Story 10.2, already named in `sources/__init__.py`'s own `BMAD_METHOD_VERSION_DRIFT` REGISTRY comment (mirroring `CHECK_LAYOUT`'s loopback-socket/chromium precedent for the same field): CAP-2 makes a live, unauthenticated npm-registry GET despite being registered `scope="repo"`, the classification the rest of the codebase uses to mean "reads only tracked files/git history, runs anywhere" (CI-safe, offline-safe). Story 10.3 makes this concretely reachable and tested: `test_bmad_core_flag_matching_scope_repo_runs_fine` asserts `doctor check --bmad-core --scope repo` "runs fine," so an operator who picks `--scope repo` specifically to stay network-free during this story's own review pass would get a live call anyway, silently.
  evidence: Raised by Blind Hunter during this story's review pass (2026-08-20). This story's own review pass already patched `--bmad-core`'s own `--help` text to name the exception inline (`registered scope='repo' despite that live call...`), so the risk is now discoverable by reading `--help`, but the underlying `scope="repo"` classification itself is unchanged and still not literally CI/air-gapped-safe for CAP-2. Not this story's problem to fix: the classification predates Story 10.3 (introduced whole by Story 10.2), and Story 10.3's own Boundaries explicitly forbid touching `bmad_method.py`'s gather logic or its `sources/__init__.py` REGISTRY entry's structural fields (only a comment touch-up was in scope). Resolving it for real would mean either a third scope value distinguishing "repo files but also one bounded network call" from plain "repo," or accepting the existing `CHECK_LAYOUT` precedent that "repo" already tolerates this class of exception -- an architecture decision for whoever next revisits `sources.REGISTRY`'s scope taxonomy.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-3-the-drift-surfaces-ambiently-never-gates.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-10-3-3: Only the extracted `bmad_core_drift_findings()` helper this story adds is unit-tested in isolation (mirroring `loop_home_staleness()`'s own existing extracted-and-tested shape exactly). Neither this new probe nor any of `main()`'s other ATTENTION probes (PR-list via `gh pr list`, baseline-drift-check, loop-home-staleness itself) has a test proving the `try/except`-wrapped call site inside `main()` actually threads its result into the printed `watch`/`needs` lists correctly -- a regression in the glue itself (a silently-mis-wired `watch.append`, an accidentally-broad `except` swallowing a real result) would not be caught by any test in this repo today, only by the kind of manual live run this story's own review had to perform to confirm the feature works end-to-end.

- source_spec: `_bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-3-the-drift-surfaces-ambiently-never-gates.md`
  summary: Only the extracted `bmad_core_drift_findings()` helper this story adds is unit-tested in isolation (mirroring `loop_home_staleness()`'s own existing extracted-and-tested shape exactly). Neither this new probe nor any of `main()`'s other ATTENTION probes (PR-list via `gh pr list`, baseline-drift-check, loop-home-staleness itself) has a test proving the `try/except`-wrapped call site inside `main()` actually threads its result into the printed `watch`/`needs` lists correctly -- a regression in the glue itself (a silently-mis-wired `watch.append`, an accidentally-broad `except` swallowing a real result) would not be caught by any test in this repo today, only by the kind of manual live run this story's own review had to perform to confirm the feature works end-to-end.
  evidence: Raised by Blind Hunter during this story's review pass (2026-08-20). Verified by inspection: `test_fleet_picture_loop_home_staleness.py` (the one existing meta-test file for this script) tests only `loop_home_staleness()` directly, never `main()`; `running_stations()`, the PR-list block, and the baseline-drift-check block all have zero test coverage of any kind. This is a pre-existing test-strategy gap for the whole file, surfaced incidentally while adding this story's own (correctly, narrowly unit-tested) addition -- not something this one story introduced or is scoped to fix alone, since closing it properly means one broader `main()`-level integration test covering every existing probe at once (mocking `marshal status`, `gh pr list`, and the baseline-drift-check subprocess together), not a narrow addition for bmad-method alone.
  severity: low
  promoted: 2026-08-28 — promoted from Tier-3 implementation-artifacts/deferred-work.md (already-identified identified-bulleted entry, never previously copied to the tracked ledger)
  status: open

  verified: 2026-09-02 — still-open — mechanical re-verification at HEAD 933039db67 (operator-directed fleet-wide refresh 2026-09-02): source_spec is Tier-3 (gitignored, not in clone); cited paths 0/1 present (absent: _bmad-output/projects/pyforge-doctor/implementation-artifacts/spec-10-3-the-drift-surfaces-ambiently-never-gates.md); ledger status mapped to still-open; agent judgment not applied — a re-read against live code is still owed where the claim is semantic

### DW-FU-20-1: epics.md Story 20.1's Surface line (naming suite-members.yaml and _manifest_suite_members/_pixi_suite_package_names) and its AC's 'packages_checked reads 13' wording do not precisely match the shipped implementation (per-recipe.yaml cfe-source-kind derivation; suite evidence reads 12, core excluded).

- source_spec: `planning-artifacts/specs/spec-20-1-suite-drift-maps-every-active-member-derived-from-the-roster.md`
  summary: epics.md Story 20.1's Surface line (naming suite-members.yaml and _manifest_suite_members/_pixi_suite_package_names) and its AC's 'packages_checked reads 13' wording do not precisely match the shipped implementation (per-recipe.yaml cfe-source-kind derivation; suite evidence reads 12, core excluded).
  evidence: suite-members.yaml carries no registry/probe-class field on any entry, so the literal Surface reading has no data to read; the recipe.yaml-based approach is the only functionally coherent one (matches Story 19.1 and steward's spec-bmad-suite-metapackage precedent) and is fully justified in this spec's own Design Notes ('Why 12, not 13'). Pre-existing imprecision in the epics.md text, not introduced by this diff.
  location: _bmad-output/projects/pyforge-doctor/planning-artifacts/epics.md (Story 20.1, Surface + AC lines)
  origin: spec-deferred 1ca276296ece — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-07 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

  verified: 2026-09-08 — resolved — CONFIRMED as stated, then FIXED in this sweep (the remedy was a doc edit, not code). Verified live: `python -m pyforge.doctor.sources bmad-method-version-drift --json` reports `packages_checked = 12`, while `epics.md:1539` still asserted 13; and `recipes/bmad-suite/suite-members.yaml` carries only `- name:` (plus an optional `notes:`) per member, with no registry/probe-class field anywhere in the file — so the literal Surface reading had, as the entry said, no data to read. Because the entry's own evidence establishes the recipe.yaml-derived implementation is the correct one, the imprecise text was the defect: `epics.md` Story 20.1's Surface, When and Then lines are reconciled to the shipped behaviour in this commit (12 not 13, per-`recipe.yaml` `cfe-source-kind` derivation, suite-members.yaml named as roster-only). Nothing in the code changed.

### DW-FU-20-2: pyforge-marshal/spec-pyforge-core's own declared surface glob (pyforge-{atlas,doctor,herald,marshal,mason,scribe,steward,warden}/src/**) never matches any real path, so changes under seven of the eight stations' src/** silently evade that spec's drift detection.

- source_spec: `planning-artifacts/specs/spec-20-2-the-render-halt-class-has-a-detector.md`
  summary: pyforge-marshal/spec-pyforge-core's own declared surface glob (pyforge-{atlas,doctor,herald,marshal,mason,scribe,steward,warden}/src/**) never matches any real path, so changes under seven of the eight stations' src/** silently evade that spec's drift detection.
  evidence: chain.py::_glob_to_re treats brace characters as literal regex-escaped text (verified directly against the glob-compiling code, no {a,b,c} expansion support exists), so the literal substring "pyforge-{atlas,doctor,herald,marshal,mason,scribe,steward,warden}" can never appear in any real file path. Discovered incidentally while reconciling this story's own pixi.toml touch against pyforge-marshal/spec-pyforge-core (a foreign spec); the fix (a real glob per station, or a spec-owned decision to only watch specific stations) is marshal's own spec-authoring call, not this story's to make.
  location: _bmad-output/projects/pyforge-marshal/planning-artifacts/specs/spec-pyforge-core/SPEC.md (surface: list)
  origin: spec-deferred 7e5ceba0b980 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: medium
  promoted: 2026-09-07 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

  verified: 2026-09-07 — resolved — pyforge-doctor CI/hygiene sweep. Replaced the single brace-expansion glob at `_bmad-output/projects/pyforge-marshal/planning-artifacts/specs/spec-pyforge-core/SPEC.md`'s `surface:` list with 8 real per-station globs (`src/shared/packages/pyforge-<station>/src/**` for each of atlas/doctor/herald/marshal/mason/scribe/steward/warden), verified each against the actual on-disk tree (`ls -d`, all 8 present). Re-ran `python -m pyforge.doctor.sources spec-surface`: the corrected glob immediately surfaced ~475 pre-existing files across all 8 stations' `src/**` trees entering this spec's tracked surface for the FIRST time (drift-presumed WARNs) — expected and reconciled, not new drift: `spec-pyforge-core` is still `status: draft` (the extraction has not started), so these files simply pre-date the spec and were never governed before because the glob could not match them. Documented as a first-time baseline capture in `spec-pyforge-core`'s own `.memlog.md` (mirroring that memlog's own 2026-08-09 "THIS RECORD STARTS HERE" provenance precedent, not a per-file drift review) and re-stamped: `python scripts/spec_surface_check.py --write-baseline --spec pyforge-marshal/spec-pyforge-core`. Re-verified clean: `python -m pyforge.doctor.sources spec-surface` now reports `ok -- every tracked file governed or allowlisted; no drift` (exit 0) fleet-wide. Evidence: `_bmad-output/projects/pyforge-marshal/planning-artifacts/specs/spec-pyforge-core/SPEC.md`, `.memlog.md`, `scripts/.spec-surface-baseline.json`.

### DW-FU-20-3: _changed_paths splits the diff --name-only output on newlines rather than using NUL-delimited (-z) output, so a changed path containing a literal embedded newline byte would be mis-split.

- source_spec: `planning-artifacts/specs/spec-20-3-frozen-path-changed-exists.md`
  summary: _changed_paths splits the diff --name-only output on newlines rather than using NUL-delimited (-z) output, so a changed path containing a literal embedded newline byte would be mis-split.
  evidence: This is the SAME pre-existing pattern already used in sources/ledger.py (unmodified by this story) -- not a new defect Story 20.3 introduces. Fixing it here without also fixing the inherited sibling would create inconsistency; fixing both is outside this story's own surface.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/frozen_path.py (_changed_paths); sources/ledger.py has the same pattern
  origin: spec-deferred aae0417acab3 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-07 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

  verified: 2026-09-08 — still-open — CONFIRMED against live code, both halves. `sources/frozen_path.py:146` still runs `git -c core.quotepath=false diff --name-only {base}..{head}` and splits with `output.splitlines()` (`:150`) — no `-z`, so a path containing a literal newline byte is still mis-split. The inherited sibling is also unchanged: `sources/ledger.py:141` runs `ls-tree -r --name-only` and `:144` splits with `.splitlines()`. Left open deliberately rather than fixed in this sweep: the entry's own reasoning still holds — fixing one without the other creates the inconsistency it warns about, and converting both to NUL-delimited output changes parsing in two modules with their own tests, which is a code change beyond a verification pass.

### DW-FU-20-4: No `## Allowed actions (CAP-16)` entry names the mechanism by which a doctor station task may consult `bmad-os-root-cause-analysis`.

- source_spec: `planning-artifacts/specs/spec-20-4-bmad-os-root-cause-analysis-is-doctor-wielded.md`
  summary: No `## Allowed actions (CAP-16)` entry names the mechanism by which a doctor station task may consult `bmad-os-root-cause-analysis`.
  evidence: Verified identical across all six wielding stations' persona files (warden, marshal, herald, steward, scribe, doctor) -- none of their `## Allowed actions (CAP-16)` sections name a mechanism for consulting their own routed `bmad-os-*` utility skill either; this is steward 46.2's own pre-existing pattern, not something this story introduces or worsens.
  origin: spec-deferred 70d56e4ced32 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-07 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  location: .claude/skills/bmad-agent-doctor/SKILL.md:67 (`## Allowed actions (CAP-16)`); the routing note that lacks a CAP-16 mechanism is at :14
  status: open

  verified: 2026-09-08 — still-open — CONFIRMED, and the entry's cross-station scope claim independently re-measured and upheld. `bmad-agent-doctor/SKILL.md:14` routes `bmad-os-root-cause-analysis` as advisory-only, but the `## Allowed actions (CAP-16)` section at `:67` names no mechanism for consulting it. Re-checked all six wielding personas mechanically (warden, marshal, herald, steward, scribe, doctor): every one mentions a routed `bmad-os-*` skill outside CAP-16, and **0 of 6** name one inside their CAP-16 section — so this is steward 46.2's fleet-wide pattern exactly as the entry claims, neither narrower nor wider. A `location:` was added to this entry in the same sweep: it had none, which is why the churn filter could never reach it.

### DW-FU-21-6: Refresh docs/dreams/README.md Phase-2b prose to describe file-driven reconciliation and the three new finding classes.

- source_spec: `planning-artifacts/specs/spec-21-6-dreams-hygiene-reconciles-every-dream-file-and-readme-71-is-enforced.md`
  summary: Refresh docs/dreams/README.md Phase-2b prose to describe file-driven reconciliation and the three new finding classes.
  evidence: README still describes hygiene as README-row-only reconciliation; behavior changed in chain.py but tier doc not updated in this story.
  location: docs/dreams/README.md
  origin: spec-deferred 36dd62536e4a — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-12 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-22-6: README Quick start, Common Commands, and Pixi tasks sections still carry full operational text until Story 22.5 populates docs/tutorials/ and docs/how-to/.

- source_spec: `planning-artifacts/specs/spec-22-6-readme-md-claude-md-and-agents-md-point-cleanly-into-the-reorganized-structure.md`
  summary: README Quick start, Common Commands, and Pixi tasks sections still carry full operational text until Story 22.5 populates docs/tutorials/ and docs/how-to/.
  evidence: docs/MAP.md lists those README sections as Story 22.5 relocation targets; the quadrant scaffolds are empty README stubs only. Replacing with pointers now would send newcomers to unpopulated homes.
  location: README.md
  origin: spec-deferred 9eea4f81fe33 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: medium (unverified)
  promoted: 2026-09-12 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-21-6-2: Wire the three new dreams-hygiene classes into detectors-ci after steward confirms live-tier volume is acceptable.

- source_spec: `planning-artifacts/specs/spec-21-6-dreams-hygiene-reconciles-every-dream-file-and-readme-71-is-enforced.md`
  summary: Wire the three new dreams-hygiene classes into detectors-ci after steward confirms live-tier volume is acceptable.
  evidence: Spec scopes measurement before joining detectors; checks run only via dream-chain --dreams and pyforge-doctor-test live-count tests today. The three classes live in `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py`; the CI gate list is `scripts/detectors.py`.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py
  origin: spec-deferred ac5c2efd1d11 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-12 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-CHAIN-COMPLETENESS-6: INV-A skipped every non-open Spec status, so a `shipped` Spec delivered with no epic and no ledger row was structurally invisible

- source_spec: `_bmad-output/projects/pyforge-steward/planning-artifacts/specs/spec-vocabulary-one-name-one-job/SPEC.md`
  summary: `board.py::_check_project_chain_completeness` gated INV-A on `if status not in OPEN_SPEC_STATUSES: continue` (`OPEN_SPEC_STATUSES = {"draft","ready","in-progress"}`). Every other status — `shipped`, `absorbed`, `archived`, `superseded`, `extension-point` — skipped the decomposition check entirely. That is correct for four of them and wrong for `shipped`: work really delivered, with no epic and no ledger row, leaves the station under-reporting what it shipped, and the dev/review loop, the retro triggers and the realization gate all key off that trail. The module's own comment (`board.py:84-88`) documented the exemption as deliberate — "When a Spec reaches a terminal status, `:716`'s first clause exempts it anyway" — so this was a design decision, not an oversight, which is why it survived five prior entries in this series.
  evidence: Found 2026-09-14 by a fleet-wide audit prompted by `spec-platform-image-one-pixi-env` — `status: shipped`, three CAPs each carrying a dated `verified: 2026-09-11` note, `59083b8391` (2026-08-25) in git, its three tests passing live — and **zero** Epic/Story anywhere in the fleet. `chain-completeness` reported clean. A first cut that added `shipped` to the existing per-CAP citation test flagged **34 Specs against 12 real ones**, because epics written before the cite-every-CAP-id convention (Story 12.3, DW-CHAIN-COMPLETENESS-1) name a Spec or its Dream without enumerating `CAP-n`; the delivered branch is therefore held to the WHOLE-SPEC standard only. Matching also had to accept an `owner-dream:` path, since steward Epic 51 claims `spec-platform-datastores-consumed-not-self-hosted` that way and a `spec-<slug>`-only test reported it as a false positive.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py
  severity: high
  status: closed
  raised: 2026-09-14 — raised and closed in the same change (operator-directed vocabulary/gap audit). Owner: doctor (owns `board.py`).
  closed: 2026-09-14 — new constants `DELIVERED_SPEC_STATUSES = {"shipped"}` and `DECOMPOSITION_OWED_STATUSES = OPEN_SPEC_STATUSES | DELIVERED_SPEC_STATUSES`; a `delivered` branch that runs the whole-spec reference test (slug, bare slug, `owner-dream:` path, or dream stem) and emits `delivered-spec-not-decomposed` with a remedy naming a RETROACTIVE epic (precedent Epics 38/39/57) and explicitly NOT `DEFERRED_SPECS`, which is for undone work. Open-Spec behaviour is byte-unchanged. Four tests replace the one that encoded the old exemption (`test_shipped_spec_is_not_open_and_reports_no_finding`): the new behaviour, two false-positive guards (epic-reference and dream-path), and a guard-removed companion asserting `absorbed`/`archived`/`superseded`/`extension-point` stay exempt. Full doctor suite 1646 passed. **Residue: the 7 real findings this now surfaces are NOT fixed** — tracked as DW-CHAIN-COMPLETENESS-8.

### DW-CHAIN-COMPLETENESS-7: INV-B discards every `epic-*` ledger key before comparing, so an epic row with no `## Epic N` heading is unreachable — and `fleet-picture` counts epics from exactly those keys

- source_spec: `_bmad-output/projects/pyforge-steward/planning-artifacts/specs/spec-vocabulary-one-name-one-job/SPEC.md`
  summary: INV-B compares `epics.md` `### Story` ids against `sprint-status-ledger.yaml` keys in both directions, but its first act is `story_rows = {k: v for k, v in rows.items() if not k.startswith("epic-")}` — every `epic-N` and `epic-N-retrospective` key is dropped before the comparison. Nothing anywhere checks that an `epic-N` key has a matching `## Epic N:` heading, or the reverse. This matters beyond bookkeeping: `scripts/fleet_picture.py` derives the fleet's headline epic counts from those same ledger keys, so a drifted key silently misreports fleet progress.
  evidence: Found 2026-09-14 by a purpose-written cross-check (not by any existing detector). Two live orphans: `pyforge-steward` has `epic-18: done` with **no `## Epic 18` heading** (stories 18.1–18.3 exist and are correctly keyed, so they render under Epic 17), and `pyforge-marshal` has `epic-29: done` with no `## Epic 29` heading (stories 29.1–29.2 likewise orphaned under Epic 28). Marshal's own `epics.md` frontmatter admits the second: *"183 (181 + Epic 29's two, never counted)"*. Net effect measured live: `fleet-picture` reports 57 epics for steward and 36 for marshal while their `epics.md` declare 56 and 35. Confirmed by parsing both files — headings `{16,17,19,...}` vs keys `{...,18,...}`.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py
  severity: medium
  status: closed
  raised: 2026-09-14 — Owner: doctor (owns `board.py`). A fix adds an epic-level arm to INV-B comparing `## Epic N` headings against `epic-N` keys both directions, with `epic-N-retrospective` treated as paired to its own `epic-N`. The two live orphans are data fixes in steward/marshal and are tracked separately so the detector work is not blocked on them.
  closed: 2026-09-14 — Fixed as described. `_epic_numbers_from_epics()` (regex `^##\s+Epic\s+(\d+)`) and `_ledger_epic_numbers()` (`re.fullmatch(r"epic-(\d+)")`, so `epic-N-retrospective` is deliberately not a second epic) feed a new INV-B arm emitting `epic-heading-without-ledger-key` and `ledger-epic-key-without-heading`, each naming the epic numbers in its detail string. Four tests cover both directions, agreement, and the guard-removed retrospective companion; `chain-completeness` reports INV-B clean on both arms fleet-wide.
    Both live orphans resolved, and each was a different defect than the entry above assumed — the entry predicted two missing headings:
    - **steward Epic 18 was mis-levelled, not missing.** `### Epic 18: Chrome and the trusted client` sat at H3 (line 1563) as the body heading while an identically-titled summary-list entry sat at line 1503. Promoted the body heading to `##`; the summary entry is untouched. Verified `count == 1` for the body form and `== 2` overall before editing, so the promotion could not hit the wrong one.
    - **marshal Epic 29 was genuinely absent at every heading level.** Inserted `## Epic 29: A harness halt of \`done\` ends the session, not the review` above its existing `**Goal:** FR-193 CAP-11` block, citing marshal's own frontmatter admission — *"183 (181 + Epic 29's two, never counted)"* — as the retroactive warrant.
    Fixture lesson worth keeping: the new arm initially reddened six existing tests, because `_write_epics_md` always emits `## Epic 1` while their ledgers carried no `epic-1` key. The fixtures were the unfaithful artifact — every one of the fleet's eight real ledgers pairs them — so `_write_ledger` now defaults `epic-1: done` when the caller supplies no `epic-*` key, rather than the detector being weakened to tolerate a shape that does not occur.

### DW-CHAIN-COMPLETENESS-8: seven `shipped` Specs are delivered with no epic anywhere — the backlog DW-CHAIN-COMPLETENESS-6 made visible

- source_spec: `_bmad-output/projects/pyforge-steward/planning-artifacts/specs/spec-vocabulary-one-name-one-job/SPEC.md`
  summary: With INV-A's delivered branch live, `chain-completeness` reports seven Specs whose `status: shipped` work has no FR or epic reference anywhere in the fleet. Each needs a RETROACTIVE epic documenting what already exists (stories at `done`) plus its ledger keys — the Epic 38/39/57 pattern — never a `DEFERRED_SPECS` entry, which is reserved for work deliberately not done.
  evidence: `pixi run -e local-recipes python -m pyforge.doctor.sources chain-completeness` at 2026-09-14 reports 7 `delivered-spec-not-decomposed` findings, including `pyforge-steward/spec-mcp-host-real-station-tools` (real surface: `mcp_host/app.py`, `settings.py`, its Containerfile, three helm templates, `django-marshal`'s `mcp_asgi.py`) and `pyforge-mason/spec-fleet-stewardship`. One instance of this class was already closed by hand the same day: `spec-platform-image-one-pixi-env` → retroactive steward **Epic 57** (3 stories at `done`, ledger keys added, `epics.md` currency_review stamped).
  severity: medium
  status: closed
  raised: 2026-09-14 — Owner: each Spec's own station. Sequenced deliberately after the detector change so the list is machine-derived rather than hand-assembled; the operator directed station-by-station clearing.
  closed: 2026-09-14 — All seven cleared station-by-station with retroactive epics, per the Epic 38/39/57 pattern and never a `DEFERRED_SPECS` entry. Eight epics minted across three stations, 30 stories, every one at `done` because the work was already delivered: steward **57** (3) and **58** (3); mason **17** (3); marshal **37** (6), **38** (4), **39** (2), **40** (5), **41** (4). Each verified to carry all three of a `## Epic N:` heading, an `epic-N` ledger key, and its `### Story N.M` stories — the same triple the INV-B epic arm added in DW-CHAIN-COMPLETENESS-7 now enforces, so this class cannot silently reopen. Marshal's `epics.md` frontmatter was re-stamped to `epicCount: 41` / `storyCount: 271` with a currency_review prepend. `chain-completeness` now reports zero `delivered-spec-not-decomposed` findings fleet-wide; the sole surviving finding is an open `draft` Spec in marshal, which is INV-A's original open-Spec arm and out of this entry's scope.

### DW-DASHBOARD-DEAD-CODE-1: board.py carries ~180 statements of retired-Guildhall machinery that cannot run in any supported state

- source_spec: `_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/spec-pyforge-doctor/SPEC.md`
  summary: `sources/board.py` still holds the whole Guildhall console subsystem — `_load_dashboard_generate`, `_load_data_js`, `_drift_epics_md_ids`, `_board_projects`, `_check_dashboard_drift`, `_check_project_dashboard_drift`, and the entire layout-check block (`_serve_layout_dir`, `_layout_chip_rows`, `_check_layout_geometry`, `_suppress_close`, `_run_check_layout`, and the `_LAYOUT_*` constants). None of it can execute: `docs/dashboard/generate.py`, `data.js` and `index.html` are all gone, and `retired-console-check` FAILS CI if any is reintroduced. The reachable entry points (`gather_dashboard_drift`, `gather_check_layout`, `_gather_chain_layers_audit`) now do nothing but assert the retirement holds.
  evidence: Found 2026-09-14 when `named-module-gates (doctor)` failed on PR #1355: `pyforge.doctor.sources.board` measured **70.1% against an 80% floor**, and the gate only measures modules a PR touches — so the shortfall had been latent since the console was retired and surfaced the moment board.py was edited for an unrelated reason (the INV-B epic arm). All 191 missing statements were in this subsystem. Marking it `# pragma: no cover` took the module to **94%** with zero test changes and zero behaviour change, which is itself the proof that the code is dead: excluding it cost no coverage of anything live.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py
  severity: low
  status: closed
  raised: 2026-09-14 — Owner: doctor. The pragma is the honest interim: it narrows measurement to live code, lowers no threshold and suppresses no finding, and the block carries a comment saying exactly why. Deletion is the real fix and was deliberately NOT done in the same pass — it removes two registered detector sources' internals plus their `Source` taxonomy members, `report-schema.json` entries and registrations, which is a behaviour change needing its own Dream/Spec under the repo's always-on Dream-first rule. Also worth settling in that effort: whether `gather_dashboard_drift` and `gather_check_layout` should survive at all as pure retirement assertions, given `retired-console-check` already enforces the same thing.

  closed: 2026-09-14 — **Deleted: 494 lines, ten functions and ten constants, zero behaviour change.** And the entry above was WRONG about the scope, which is why it had been deferred as needing its own Dream: it claimed deletion was *"a behaviour change to two registered detector sources plus their taxonomy and schema entries."* An AST call-graph pass over `board.py` showed otherwise — the dead code formed two closed **private orphan islands**: the layout chain (`_run_check_layout`, called by nothing, calling `_serve_layout_dir` / `_check_layout_geometry` / `_suppress_close`, with `_check_layout_geometry` calling `_layout_chip_rows`) and the drift chain (`_check_dashboard_drift`, called by nothing, calling `_check_project_dashboard_drift` → `_drift_epics_md_ids`), plus the standalone orphans `_load_data_js` and `_board_projects`. Ten of the eleven suspects had **no live caller at all**. Both gathers, their DISPATCH entries, their `Source` taxonomy members and `report-schema.json` were untouched, so this was hygiene rather than an effort and needed no Dream. Only `_load_dashboard_generate` survives — genuinely reachable from `_gather_chain_layers_audit`, and keeping its pragma because its reachable path is closed by policy (`retired-console-check` fails CI on reintroduction) rather than by structure.
    **Verified, not asserted:** `--json` findings for `dashboard-drift`, `check-layout`, `chain-completeness` and the `--layers` audit were captured before the deletion and byte-compared after — all four IDENTICAL. Full doctor suite green (1378 passed). Coverage went 72% (pre-pragma) → 94% (pragma) → **95% with the code actually gone**, which is the better outcome: the pragma had been narrowing measurement around a subsystem that should not have existed. The stale prose left behind — four docstring/comment references to deleted helpers, and a section header narrating a port whose ported code no longer exists — was rewritten rather than left pointing at nothing.
    **Lesson worth keeping:** the over-stated scope in this entry's own `raised:` line cost a deferral cycle. "Removes two registered sources' internals" was inferred from the code's *location* rather than measured from its *call graph*; one AST pass would have settled it when the entry was written.

### DW-FU-18-2-2: Existing doctor check 5s budget test flakes on cold worktree starts and is not caused by this portal slice.

- source_spec: `planning-artifacts/specs/spec-18-2-first-portal-slice-last-fleet-pulse.md`
  summary: Existing doctor check 5s budget test flakes on cold worktree starts and is not caused by this portal slice.
  evidence: Full suite 1287 passed + 1 skipped + 1 failed on test_doctor_check_completes_within_the_five_second_budget (7.8s / 15.9s). Story 18.1 recorded the same flake. New 18.2 tests 7/7 passed.
  location: src/shared/packages/pyforge-doctor/tests/unit/test_check_speed_budget.py
  origin: spec-deferred 2bb5888a6493 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-18 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-LEDGER-DIRECTION-REKEY-1: `ledger-direction`'s `gather_direction()` does not apply a fold PR's re-key map, so a rekeyed-but-still-done story reads as `landed-but-unpromoted` forever

- source_spec: `_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/spec-pyforge-doctor/SPEC.md`
  summary: The `ledger-direction` detector (`sources/ledger.py::gather_direction`, Story 15.2 / marshal FR-137/FR-138) fired three FAILs for pyforge-atlas — `13-5`, `14-4`, `15-3` "landed-but-unpromoted." Investigation found all three false positives, caused by the same fold PR (`86d1cdf3ee`, `rekey-2026-09-17.md`) that its sibling detector `ledger-regression` (`gather()`, same module) already handles: `gather()` calls `_new_rekey_maps()` and remaps the base-side keys before comparing (lines ~285-306, "Story 25.3: a fold PR renumbers every key and ships the map"). `gather_direction()` has no equivalent — it derives a bare `_story_id()` from the ledger's CURRENT keys and from merge-subject slugs, with no rekey substitution, so a merge subject citing the OLD id (`bmad-loop/…/13-5-downstream-handoff-to-mason`) never matches the ledger's post-rekey key (`12-5-downstream-handoff-to-mason-fr-68`), even though both the ledger (`done`) and that story's spec frontmatter (`status: shipped`) already agree the story shipped.
  evidence: |
    Verified 2026-09-18, all three genuinely merged and already correctly tracked under their post-rekey keys:
    - `13-5` → `12-5-downstream-handoff-to-mason-fr-68`: ledger says `done`; spec `spec-12-5-downstream-handoff-to-mason-fr-68.md` frontmatter `status: shipped`; merge commit `7156c2b66fd` (`bmad-loop/…/13-5-downstream-handoff-to-mason into loop/pyforge-atlas`) confirmed an ancestor of `origin/main` via `git merge-base --is-ancestor`.
    - `14-4` → `13-4-air-gap-asset-rewriting-cap-4`: ledger says `done`; spec `spec-13-4-air-gap-asset-rewriting-cap-4.md` frontmatter `status: 'done'`; merge commit `eee8e8b78d` confirmed an ancestor of `origin/main`.
    - `15-3` → `14-3-kedro-pipeline-surfacing-cap-4`: ledger says `done`; spec `spec-14-3-kedro-pipeline-surfacing-cap-4.md` frontmatter `status: 'done'`; merge commit `b3532d2536` confirmed an ancestor of `origin/main`.
    No ledger or spec edit was made for any of the three — both artifacts already state the true, current status. Re-adding the old keys to the GENERATED ledger (`sprint-status-ledger.yaml` header: "do not hand-edit") to silence the detector would introduce dead keys that `sprint-ledger-sync` does not itself write and would misreport the story's identity going forward.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/ledger.py (`gather_direction`, around line 589)
  severity: medium
  status: open
  raised: 2026-09-18 — Owner: doctor. The fix is mechanical (thread `_new_rekey_maps`/`RekeyMap` through `gather_direction` the same way `gather()` already does, remapping `merged_ids`/`done_ids` through the project's rekey map before diffing) but is a behaviour change to a Doctor detector source, which this repo's always-on Dream-first rule does not exempt even for a small, well-understood fix — needs a Dream seed + `bmad-spec` before implementation, not a same-pass hand-edit.

### DW-FU-28-1: status_body_consistency._parse_frontmatter is a verbatim copy of the pre-CAP-81 first-dashes-anywhere reader, so CAP-3 and CAP-81 now render contradictory verdicts on the same document

- source_spec: `planning-artifacts/specs/spec-28-1-a-frontmatter-reader-that-stops-at-the-fence-not-at-the-first-dashes.md`
  summary: status_body_consistency._parse_frontmatter is a verbatim copy of the pre-CAP-81 first-dashes-anywhere reader, so CAP-3 and CAP-81 now render contradictory verdicts on the same document
  evidence: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/status_body_consistency.py:219-236 still tests for the three-dash substring anywhere in the text and splits on its first occurrence; its `_body_after_frontmatter` (:239-246) finds the first newline-fence-newline marker (the three dashes are not spelled out here so no first-dashes reader truncates this very entry). After CAP-81 lands, a Dream with prose, a three-dash thematic break and no frontmatter is `({}, False)` -> `dream-without-spec` in chain.py but `({}, True)` -> an `unparseable` WARN in status_body_consistency.py, and the 34 glued-opener (three dashes fused to `title:`) archived Dreams are refused by chain.py but silently parsed by this copy. Review pass 1 (2026-09-19) verified the divergence by reading both readers; PR #1494's memlog entry already notes this copy "for CAP-81's story". Named blocker: CAP-81's intent names `chain.py::_frontmatter_parse` only, and a detector's own reader is chain work (Dream-first) — this needs its own CAP/story under spec-pyforge-doctor, not a hand-patch under Story 28.1.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/status_body_consistency.py::_parse_frontmatter
  origin: spec-deferred be6c272db6d7 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: medium
  promoted: 2026-09-19 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-28-1-2: factory.py's pin-scope extractor still takes the frontmatter as the text before the first three-dash substring, so a pin declared after an embedded three-dash run in a frontmatter scalar is missed

- source_spec: `planning-artifacts/specs/spec-28-1-a-frontmatter-reader-that-stops-at-the-fence-not-at-the-first-dashes.md`
  summary: factory.py's pin-scope extractor still takes the frontmatter as the text before the first three-dash substring, so a pin declared after an embedded three-dash run in a frontmatter scalar is missed
  evidence: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py:631 splits the text on its first three-dash substring; `scope = parts[1]` when the text starts with three dashes. A frontmatter value quoting three dashes before the pin key truncates `scope` and `_PIN_KEY_RE.search` misses the pin -> `pin-missing` on a doc that declares one. No live doc has the shape today (review pass 1 scan, 2026-09-19); same named blocker as the status_body_consistency entry — a separate capability's reader.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py::_doc_pin
  origin: spec-deferred 7dd30eafde52 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-19 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-28-1-3: 34 archived docs/dreams/*.md files carry a glued opener (three dashes fused to `title:` on line 1) and are now refused as unparseable-frontmatter, hiding 20 readme-table-drift and 2 kinship-wikilink-dead findings until the openers are repaired

- source_spec: `planning-artifacts/specs/spec-28-1-a-frontmatter-reader-that-stops-at-the-fence-not-at-the-first-dashes.md`
  summary: 34 archived docs/dreams/*.md files carry a glued opener (three dashes fused to `title:` on line 1) and are now refused as unparseable-frontmatter, hiding 20 readme-table-drift and 2 kinship-wikilink-dead findings until the openers are repaired
  evidence: The 2026-09-17 one-chain-per-station fold wrote the opener as three dashes fused to `title:`; `0b74756679` ("keep satellite Dream hashes") deliberately restored that shape on all 34 to hold spec-pyforge-steward's surface stable. Story 28.1's opener rule (an attempted-but-unbounded opener is refused, never parsed leniently or read as absent) turns them into 34 live `unparseable-frontmatter` WARNs (`gather_dreams_hygiene` 173 -> 185, `gather_dream_chain` 1 OK -> 34 WARN) and skips them before the readme-drift and Kinship scans (`readme-table-drift` 73 -> 53; `kinship-wikilink-dead` 33 -> 31, both from `enterprise-airgap.md`). Repair: insert a line break after the three dashes on line 1 of each file. Two of them -- `docs/dreams/mcp-era-isolation.md` and `docs/dreams/mcp-host-real-station-tools.md` -- are governed by `pyforge-steward/spec-mcp-era-isolation` / `spec-mcp-host-real-station-tools` (steward memlog event + scoped `--write-baseline` for each), and the same PR must re-measure `test_live_tree_kinship_wikilink_dead_count` 31 -> 33 and run `pyforge-doctor-test` locally, because a docs-only diff cannot fire the doctor station lane (the MRS-GATE-001 class). Named blocker: cross-station (steward- governed files) and outside Story 28.1's Surface; verified live 2026-09-19.
  location: docs/dreams/ (34 files whose first line begins with the three dashes fused to `title:`; the two steward-governed ones are named in the evidence)
  origin: spec-deferred a1a347e035e7 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: medium
  promoted: 2026-09-19 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-28-1-4: `unparseable-frontmatter` findings name no refusal cause, and `_unparseable_frontmatter_item`'s remedy text ("could not be parsed as a mapping ... fix the fenced YAML frontmatter block") is wrong for the attempted-but- unbounded opener that produces all 34 live hits

- source_spec: `planning-artifacts/specs/spec-28-1-a-frontmatter-reader-that-stops-at-the-fence-not-at-the-first-dashes.md`
  summary: `unparseable-frontmatter` findings name no refusal cause, and `_unparseable_frontmatter_item`'s remedy text ("could not be parsed as a mapping ... fix the fenced YAML frontmatter block") is wrong for the attempted-but- unbounded opener that produces all 34 live hits
  evidence: `chain.py::_frontmatter_parse` returns `(dict, bool)`; after Story 28.1 four distinct refusal causes (unclosed fence; attempted-but-unbounded opener; YAML error; non-mapping block) collapse into one bool, so `gather_dreams_hygiene` (the `unparseable-frontmatter` WARN, evidence `{"subject": slug}`) and `_unparseable_frontmatter_item` (the remedy line) cannot say WHICH line to fix -- for the 34 glued-opener Dreams the block IS a valid mapping and the only defect is line 1. Story 28.1's Never clause ("do not touch callers") excludes threading a reason through those call sites. Named blocker: needs a CAP that widens the reader's return contract (a reason enum) or adds a reason field to the finding, under spec-pyforge-doctor; review pass 2, 2026-09-19.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py::_unparseable_frontmatter_item
  origin: spec-deferred a8c284f7e5ef — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: medium
  promoted: 2026-09-19 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-28-1-5: Five other doctor readers close the frontmatter block on a stripped three-dash line, so an indented three-dash line inside a block scalar ends their block early with no error -- the class Story 28.1 fixed only in chain.py::_frontmatter_parse

- source_spec: `planning-artifacts/specs/spec-28-1-a-frontmatter-reader-that-stops-at-the-fence-not-at-the-first-dashes.md`
  summary: Five other doctor readers close the frontmatter block on a stripped three-dash line, so an indented three-dash line inside a block scalar ends their block early with no error -- the class Story 28.1 fixed only in chain.py::_frontmatter_parse
  evidence: `line.strip()` fence tests at `hygiene.py::_dream_frontmatter_status` (:269/:272), `sibling_dreams.py` (:82/:85), `status_body_consistency.py` (:518/:520 and :699/:702), `board.py` (:433) and `chain.py::_parse_surface` (:1615, the marshal-bound spec-surface contract). An `evidence: |` block scalar whose content has an indented three-dash line closes each of these early and drops every later key with no error (`_frontmatter_parse` now uses `rstrip`, column 0). 0 live Dreams/specs carry the shape (scan 2026-09-19). Named blocker: other capabilities' readers and a marshal-bound contract -- their own CAP/story, not Story 28.1.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/hygiene.py::_dream_frontmatter_status
  origin: spec-deferred a4e74eb1dcfa — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-19 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-OPS-2026-09-19-6: six folded-and-archived Dreams still live unfolded in the sibling repo — `sibling-dreams-drift` (CAP-71) reports status / content_hash / title divergence once a token reaches it

- source_spec: `_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/spec-pyforge-doctor/SPEC.md`
  summary: With `GH_TOKEN` exported (2026-09-19; `~/.bashrc` now sets it from `gh auth token`) the check reaches `OpenTeams-WFT-CDO/mgmt-wf-python-modernization` and finds `django-accelerator-framework`, `enterprise-data-models-and-apis`, `miniforge-installer`, `package-inventory-eligibility`, `pixi-container-image`, `reusable-cicd-workflows` diverging on status, content_hash and title. All six are `archived` here — folded into their station Dreams on 2026-09-17 (one-chain-per-station, e.g. `miniforge-installer` → `[[pyforge-mason]]`) — while the sibling still carries the pre-fold copies. Expected fallout of the fold, not a mistake here; needs a cross-repo decision: push the fold to the sibling (archive or repoint the six there), or declare the sibling a frozen snapshot and exempt them from CAP-71. Until decided the six warn on every run.
  evidence: `GH_TOKEN="$(gh auth token)" .pixi/envs/pyforge-guild/bin/python -m pyforge.doctor.sources sibling-dreams-drift` → six `sibling-dreams-drift: warn` rows, exit 0 (2026-09-19). Without the token the same run reports `sibling-dreams-unreachable` — the check fails open by design.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/sibling_dreams.py
  severity: low
  status: closed — Story 29.1 landed 2026-09-20: `sibling_dreams.py` honours a per-Dream `sibling-acknowledged: <hash>` frontmatter line (silent while it equals the sibling's current `content_hash`, re-fires naming both hashes the moment it does not) and `_SIBLING_OWNER` now points at `openteams-ai`. All six Dreams carry `sibling-acknowledged:` at their 2026-09-20 sibling hashes. Live re-verification: `GH_TOKEN="$(gh auth token)" pixi run --frozen -e pyforge-guild python -m pyforge.doctor.sources sibling-dreams-drift` → zero findings, exit 0.
  raised: 2026-09-19 — Owner: doctor (CAP-71); the fold decision belongs to the six Dreams' owners — mason (4: django-accelerator-framework, miniforge-installer, pixi-container-image, reusable-cicd-workflows), atlas (enterprise-data-models-and-apis) and warden (package-inventory-eligibility); corrected 2026-09-20 per DW-FU-29-1 (the clause used to read "mason's/steward's"; steward owns none). Operator-only for the sibling-side edit.
  update: 2026-09-19 (evening) — decision: ACKNOWLEDGE, not push. Live read: the sibling repo has moved (`OpenTeams-WFT-CDO/…` 301 → `openteams-ai/mgmt-wf-python-modernization`, private), last pushed 2026-08-24; all six Dreams there are `status: dreamt`, untouched since 2026-08-13 — a pre-fold snapshot nobody evolved. CAP-71's constraints (read-only, never a sync engine, unlicensed prose, per-Dream human reconciliation) rule out a sibling-side edit. Mechanism to record the acknowledgement + the corrected owner: Story 29.1 (CAP-82). This row closes when 29.1 lands with a live zero.

### DW-FU-23-5: No automated, repeatable check encodes "no live doc cites the old _bmad-output/ root path for the five archived files" as a re-checkable command anywhere in the repo.

- source_spec: `planning-artifacts/specs/spec-23-5-archive-citations-for-the-five-already-moved-_bmad-output-files.md`
  summary: No automated, repeatable check encodes "no live doc cites the old _bmad-output/ root path for the five archived files" as a re-checkable command anywhere in the repo.
  evidence: Confirmed independently by two review layers: grepped pyforge-doctor's hygiene definitions and tests for DREAM-TRIAGE, archive/_bmad-output, stale/dead/broken-link/resolve-citation patterns and found no matching detector or meta-test. This story's own bound Verification command (pyforge-doctor-test) is the station's generic suite and does not exercise this invariant, so a future re-introduction of a stale old-root citation would ship green. Already tracked separately as Story 23.7 ("A new Doctor source flags leftover shelf occupancy"), currently backlog — not this story's problem to build.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/
  origin: spec-deferred b39d4a218335 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-19 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-23-6: This story's own tracking artifacts (sprint-status-ledger.yaml key 23-6-…, epics.md's Story 23.6 section) still read backlog even as the spec advances through in-review to done.

- source_spec: `planning-artifacts/specs/spec-23-6-map-names-publish-roots-do-not-mint-an-empty-vizro-tree.md`
  summary: This story's own tracking artifacts (sprint-status-ledger.yaml key 23-6-…, epics.md's Story 23.6 section) still read backlog even as the spec advances through in-review to done.
  evidence: Real desync, but ledger/epics promotion is a distinct post-merge step (sprint-ledger-sync) that this build-auto workflow does not perform — it happens at landing time per this repo's own established convention (see git log: "marshal: promote sprint-status ledger ... -> done" commits following each merge).
  location: planning-artifacts/sprint-status-ledger.yaml, planning-artifacts/epics.md (Story 23.6)
  origin: spec-deferred 75105d6f468d — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: medium
  promoted: 2026-09-19 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-23-6-2: spec-pyforge-doctor/SPEC.md's CAP-53 annotation still reads "(ready 2026-09-17)" even as the underlying story spec advances.

- source_spec: `planning-artifacts/specs/spec-23-6-map-names-publish-roots-do-not-mint-an-empty-vizro-tree.md`
  summary: spec-pyforge-doctor/SPEC.md's CAP-53 annotation still reads "(ready 2026-09-17)" even as the underlying story spec advances.
  evidence: Confirmed pre-existing and systemic, not caused by this diff: every sibling CAP-48..54 annotation in the same block carries the identical stale "(ready 2026-09-17)" marker, including CAP-50 whose story (23.3) is already done. This diff doesn't touch SPEC.md; fixing the pattern is a separate, repo-wide reconciliation across all seven CAPs.
  location: _bmad-output/projects/pyforge-doctor/planning-artifacts/specs/spec-pyforge-doctor/SPEC.md:222
  origin: spec-deferred 9cc841072d32 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-19 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-23-6-3: Sibling story 23.5's three status trackers disagree with each other (spec still 'ready', epics.md still backlog, ledger already done).

- source_spec: `planning-artifacts/specs/spec-23-6-map-names-publish-roots-do-not-mint-an-empty-vizro-tree.md`
  summary: Sibling story 23.5's three status trackers disagree with each other (spec still 'ready', epics.md still backlog, ledger already done).
  evidence: Real, but about a different story entirely and outside this spec's declared Surface (docs/MAP.md, docs/dashboard/README.md). This diff's herald memlog entry only reconciles spec-surface hashes for 23.5's already-landed citation fix; it does not and should not touch 23.5's own status fields.
  location: _bmad-output/projects/pyforge-doctor/planning-artifacts/specs/spec-23-5-archive-citations-for-the-five-already-moved-_bmad-output-files.md
  origin: spec-deferred 781afe7e4bdd — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: medium
  promoted: 2026-09-19 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-30-1: `docs/how-to/github-actions-recipe-ci.md` (pre-existing, not one of the 14) claims '19 workflow files' — never re-counted; the count belongs to a generated page (Story 30.3's detector/workflow table) rather than prose.

- source_spec: `planning-artifacts/specs/spec-30-1-the-map-is-enforced-within-its-scope-the-stubs-are-gone-and-the-new-pages-are-true.md`
  summary: `docs/how-to/github-actions-recipe-ci.md` (pre-existing, not one of the 14) claims '19 workflow files' — never re-counted; the count belongs to a generated page (Story 30.3's detector/workflow table) rather than prose.
  evidence: Implementer sweep 2026-09-19 over every backticked path and pixi task in the 15 corrected pages found no other unverified claim; this count sits outside the story's pages.
  location: docs/how-to/github-actions-recipe-ci.md
  origin: spec-deferred 922f3cdd50a8 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-19 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-30-1-2: `docs/explanation/pyforge-estate-overview.md` uses Docusaurus-style `:::note` admonitions that GitHub does not render; left as authored (style, not fact) — a `docs-currency` style rule or the `bmad-os-diataxis` style guide should decide one admonition syntax for `docs/`.

- source_spec: `planning-artifacts/specs/spec-30-1-the-map-is-enforced-within-its-scope-the-stubs-are-gone-and-the-new-pages-are-true.md`
  summary: `docs/explanation/pyforge-estate-overview.md` uses Docusaurus-style `:::note` admonitions that GitHub does not render; left as authored (style, not fact) — a `docs-currency` style rule or the `bmad-os-diataxis` style guide should decide one admonition syntax for `docs/`.
  evidence: GitHub renders `> [!NOTE]`; `:::note` shows as literal text.
  location: docs/explanation/pyforge-estate-overview.md
  origin: spec-deferred 3bd6506da5c6 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-19 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-30-1-3: The unmapped-page class is `warn` (CAP-62 posture); nothing blocks a PR that adds a quadrant page without a MAP row until Story 30.2 promotes the class to `fail` once the registry exists.

- source_spec: `planning-artifacts/specs/spec-30-1-the-map-is-enforced-within-its-scope-the-stubs-are-gone-and-the-new-pages-are-true.md`
  summary: The unmapped-page class is `warn` (CAP-62 posture); nothing blocks a PR that adds a quadrant page without a MAP row until Story 30.2 promotes the class to `fail` once the registry exists.
  evidence: docs_map_hygiene.py `unmapped` → DoctorStatus.WARN by design of CAP-83.
  location: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/docs_map_hygiene.py
  origin: spec-deferred 20381fa0b4a5 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-19 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open

### DW-FU-29-1: `DW-OPS-2026-09-19-6`'s pre-existing `raised:` clause says "the fold decision is mason's/steward's" but the six Dreams' actual owners are mason (4), atlas (1), and warden (1) -- steward owns none of them.

- source_spec: `planning-artifacts/specs/spec-29-1-a-per-dream-acknowledgement-silences-exactly-one-sibling-hash-and-the-sibling-coordinates-are-current.md`
  summary: `DW-OPS-2026-09-19-6`'s pre-existing `raised:` clause says "the fold decision is mason's/steward's" but the six Dreams' actual owners are mason (4), atlas (1), and warden (1) -- steward owns none of them.
  evidence: Review pass 2026-09-20 (Blind Hunter): verified against the six Dreams' own `owner:` frontmatter. The inaccurate clause predates this story (authored 2026-09-19 when the DW entry was first raised) and this diff never touches that line, so it is a pre-existing inaccuracy, not one this change introduced.
  location: _bmad-output/projects/pyforge-doctor/planning-artifacts/deferred-work-ledger.md (DW-OPS-2026-09-19-6, `raised:` line)
  origin: spec-deferred 4030d94e19ad — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-20 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: done
  verified: 2026-09-20 — resolved: the `raised:` clause of DW-OPS-2026-09-19-6 now names the real owners (mason ×4, atlas ×1, warden ×1, from each Dream's `owner:` frontmatter); one-line factual fix in the same PR that ingested this entry.

### DW-FU-30-3: spec-pyforge-doctor/SPEC.md's `surface:` list does not yet list the five new docs-* generator scripts + shared helper (Story 30.2 precedent: scripts/docs_map_render.py was added there directly). SPEC.md is hook-blocked from a hand-edit in this session (AGENTS.md: re-derive via `bmad-spec`, never hand-edit) and a station-Spec `bmad-spec` re-derivation is out of scope for this one story's dispatch.

- source_spec: `planning-artifacts/specs/spec-30-3-the-reference-pages-are-generated-pixi-tasks-station-clis-detectors-skills-and-stamped.md`
  summary: spec-pyforge-doctor/SPEC.md's `surface:` list does not yet list the five new docs-* generator scripts + shared helper (Story 30.2 precedent: scripts/docs_map_render.py was added there directly). SPEC.md is hook-blocked from a hand-edit in this session (AGENTS.md: re-derive via `bmad-spec`, never hand-edit) and a station-Spec `bmad-spec` re-derivation is out of scope for this one story's dispatch.
  evidence: spec-pyforge-doctor/.memlog.md's 2026-09-24 entry already names the six new script paths under "Surface gains (surface: list): ...". Interim fix: allowlisted in scripts/spec_surface_allowlist.txt with a reason citing this story and the pending SPEC.md re-derivation. `python scripts/spec_surface_reconcile.py` reports OK with the allowlist entries in place.
  location: scripts/spec_surface_allowlist.txt
  origin: spec-deferred b232e01e8a92 — ingested from spec frontmatter `deferred:` (hand-driven build-auto; marshal Story 25.6)
  severity: low
  promoted: 2026-09-24 — ingested from spec frontmatter by scripts/deferred_work_intake.py
  status: open
