---
scope: pyforge-doctor v1 (Doctor CLI): check/monitor/diagnose
purpose: build-substrate
altitude: feature
updated: 2026-07-25T06:11
---

- (event) Headless run; inputs = PRD (prd-pyforge-doctor-2026-07-25, status final), brief, 2 research reports. Brownfield-in-monorepo: investigated pyforge-warden (engines.py, cli.py, models.py, verdict.py) and pyforge-atlas (pyproject.toml gate extra) as the two direct precedents before deciding.
- (constraint) v1 strictly read-only/non-mutating across check/monitor/diagnose -- no --fix actuator (PRD S5 non-goal, hard constraint).
- (decision) FR-1 mechanism: LIBRARY IMPORT of pyforge.warden.engines.run_doctor_checks, not subprocess. Precedent: pyforge-atlas already does exactly this shape -- pyproject.toml 'gate = ["pyforge-warden"]' optional-dependency extra, AC-8 'the ONLY cross-package code edge -- atlas -> warden', 'installed by default in the in-repo pyforge-atlas pixi env; external installs may omit it (gate node then fails with an install hint)'. pyforge-doctor mirrors this: doctor = ["pyforge-warden"] extra, default-installed in-repo.
- (decision) This does NOT violate warden's 'sole subprocess site' rule: that rule scopes engines.py as the only place WARDEN spawns a subprocess for ITS OWN scan (deptry/osv-scanner). pyforge-doctor calling run_doctor_checks() as a plain Python function call is not warden spawning a subprocess on doctor's behalf and not doctor spawning one either -- run_doctor_checks() itself does the (bounded, typed) subprocess work internally, unchanged. No new call site added anywhere.
- (decision) Exit-code composition: pyforge.doctor owns its OWN verdict/exit-code module (doctor.verdict), structurally mirroring warden's verdict.py's sole-owned-knob pattern but NOT importing or extending warden's enum. Doctor's exit-code domain is {0=all ok, 2=fail present, 130=SIGINT} -- deliberately a SUBSET of warden's {0,1,2,130}, permanently omitting warden's 1 (WARN_AS_ERROR / policy-gate rung), because Doctor has no policy-gate concept in v1 (PRD Glossary: 'Operability exit code... never policy'). Doctor consumes warden's own exit code (from run_doctor_checks) as DATA (mapped into a Doctor Finding's status), never re-exposes it as Doctor's own process exit code -- the two exit-code spaces never merge.
- (decision) Package layout mirrors pyforge-warden exactly: src/shared/packages/pyforge-doctor/{pyproject.toml,src/pyforge/doctor/,tests/{unit,meta,fixtures},scripts/}. Namespace package pyforge.doctor beside pyforge.warden and pyforge.atlas (both already namespace-package siblings per atlas's own pyproject.toml docstring).
- (decision) Doctor defines its own closed Finding/Source/Status taxonomy (doctor.models: DoctorStatus{ok,warn,fail}, Source enum naming each wrapped instrument, Finding dataclass) rather than importing warden's ErrorKind -- structurally mirrors warden's StrEnum + frozen-set + __post_init__ validation pattern (models.py) but semantically distinct scope (engine-missing / feedstock-stale / credential-hygiene, not warden's scan-engine-failure-scoped ErrorKind). Resolves PRD open question 5.
- (decision) JSON envelope (FR-9): one schema-validated DoctorReport document per invocation: {schema_version, verb, generated_at, findings: [Finding], prescriptions: [Prescription] (diagnose only)}. Finding = {source, check, status (ok/warn/fail), message, evidence (object, source-specific shape), }. Prescription = {finding_ref, partition (actionable/blocked/accepted-risk), rank (int, actionable only), rank_factors (object: kev, epss, blast_radius), action (patch/upgrade/retire/wait), root_cause}. schema_version starts at 1 (warden ComplianceReport precedent: schema-validated, versioned).
- (decision) monitor --fleet MCP/CLI mapping per watch axis: staleness -> staleness_report MCP tool / 'staleness-report' CLI. cve -> cve_watcher MCP tool / 'cve-watcher' CLI. abandonment -> composite of feedstock_health MCP tool (--filter stuck/bad) + release_cadence MCP tool (decelerating/silent labels) / 'feedstock-health'+'release-cadence' CLIs. Default axis set when --watch omitted: staleness+cve (resolves PRD open question 3, per Dependabot severity-first precedent in domain research).
- (decision) diagnose --target gathers Findings by calling: warden's run_doctor_checks (engine axis, if target implies env) + atlas MCP behind_upstream/cve_watcher/staleness_report for the named target -- no new gathering logic beyond composing FR-1/FR-4's existing calls; --prescribe is a pure function over the gathered Finding list (partition then rank), lives in doctor.prescribe, zero new subprocess/MCP calls of its own.
- (version) Runtime: Python >=3.14 (matches atlas's requires-python floor, both namespace-package siblings in the same pixi env family); hatchling build backend (matches warden+atlas); pytest >=9.1.1 test runner (matches warden's pinned floor in pixi.toml).
- (decision) FR-3 credential/env-hygiene check: a NEW, narrow static-scan check, read-only, pattern: unconditional-injection-shaped code (an env var read that gates an HTTP header attach with no accompanying host-scope conditional) -- implemented as a small AST-based scanner (ast.parse, no exec, no import of scanned code -- mirrors warden's extract/ subpackage's own 'no execution' meta-test discipline: tests/meta/test_extract_no_execution.py) scoped to files doctor is pointed at via --env's target path (default: repo root's .claude/**/*.py + src/**/*.py). Generalizes past JFROG_API_KEY by pattern-matching 'os.environ.get/os.getenv(<NAME>)' feeding an unconditional header/auth assignment, not a hardcoded name list.
- (decision) Subprocess/execution safety norms for anything doctor itself spawns (should be rare -- CLI-fallback path in FR-5 only): reuse warden's _engine_env() discipline verbatim as convention (argv list never shell string, NO_COLOR=1, stdin=DEVNULL, bounded timeout, typed failure) -- doctor.cli_bridge module, the one narrow subprocess site doctor owns for its own CLI-fallback calls to atlas CLIs when no MCP client is available.
- (decision) Packaging (documented decision, pixi.toml NOT edited this run): mirror [feature.pyforge-warden.*] block verbatim as [feature.pyforge-doctor.*] -- pyforge-doctor = { path = "src/shared/packages/pyforge-doctor" } path dependency, dedicated pixi env, tasks doctor-check (cmd="doctor"), pyforge-doctor-test, pyforge-doctor-build-conda/-dist/-build. pyproject.toml optional-dependencies: doctor's own extras include gate=["pyforge-warden"] (mirrors atlas's AC-8 shape) -- default-installed in-repo pixi env, external installs may omit with an install-hint failure on the engines check only.
- (question) Open: exact severity default (warn vs fail) for FR-3 findings, and whether MCP tool calls happen via the FastMCP client library directly or via a thinner doctor.atlas_bridge wrapper -- deferred to epics/stories, not architecture-blocking.
- (event) lint_spine.py: 0 findings. spine finalized: 6 ADs, 1 mermaid dependency-direction diagram, capability map covering all 9 FRs, 6 deferred items.
