---
topic: An ambient, warn-only doctor Source that reports a Dream's or Spec's prose contradicting its own status:
updated: 2026-09-09T15:42
---

- (note by claude) owner-dream: docs/dreams/status-body-consistency.md (owner: doctor, status: dreamt, seeded 2026-09-09).
- (decision by claude) Minted as a doctor Dream + Spec per fleet-readiness-decision-batch-2026-09-09 § 2.4 D1 (operator-approved § 0, C8 row: 'Approve all'). Raised independently by two station reports — guild § E-5 and scribe § E-3 — which is itself the evidence that no existing detector owns the axis: bmad-drift-check polices frontmatter vocabulary, counts and pin currency; dreams-hygiene polices presence (status, Realization log, Spec); nothing polices whether a document's prose still agrees with its own status.
- (decision by claude) The 2026-09-09 pass's six documents ARE the validation set, and each candidate signal must be proven against its named live case before it ships: (1) docs/dreams/pyforge-scribe.md status: realized with ':69 — 3 of 9 stories complete overall'; (2) spec-pyforge-scribe repeating the same stale figure under shipped; (3) docs/dreams/pyforge-herald.md realized over a mid-build body; (4) spec-pyforge-herald the same; (5) docs/governance/spec-pyforge-charter/SPEC.md:16 open_questions: [] while .memlog.md:9 carries an (open) 2026-07-31 question no later entry closes; (6) spec-unified-container shipped over a container the estate does not build (steward, stB-B3/B4).
- (capability by claude) CAP-1 — progress-phrase-under-terminal-status. Report a body matching an 'N of M stories/capabilities' (or 'N/M') phrase with N < M under status realized/shipped/done. Success: fires on the scribe Dream (:69) and its Spec, and stays silent across the rest of the tier; the finding names the line it read.
- (capability by claude) CAP-2 — open_questions: [] over a live memlog question. Report a Spec declaring no open questions while its companion .memlog.md's last (question)/(open) entry has no subsequent (decision) closing it. Success: fires on docs/governance/spec-pyforge-charter (SPEC.md:16 vs .memlog.md:9). NOT hygiene for this case: Charter CAP-2 makes the Lexicon's own integrity constitutional, so a hidden Lexicon question is a constitutional defect, not a formatting one.
- (capability by claude) CAP-3 — promissory language under a terminal status. Report a body whose section headings or lead sentences are still forward-looking ('will', 'is being built', 'queued', 'not yet') under realized/shipped. Success: fires on the herald Dream+Spec pair. This is the loosest of the signals and the one most likely to be narrowed or dropped once measured — if it cannot both fire on herald AND stay quiet elsewhere, it ships as measured-and-rejected with the number, not as a muted check.
- (capability by claude) CAP-4 — a status comment that contradicts the ledger it cites. Report a frontmatter status comment naming an epic/story whose live ledger row disagrees, e.g. the 'status: realized # … -> Epic 14 backlog' shape found on docs/dreams/bmad-method-version-drift.md while epic-14 reads done. Mechanical, and the cheapest of the four.
- (capability by claude) CAP-5 — it renders where the operator already looks: report-schema.json entry + detectors membership, so the finding appears beside dream-vocab and (once C8 lands) spec-status-missing in the doctor report and the fleet-picture ATTENTION block.
- (constraint by claude) Warn-only, fail-open, read-only. Never a second PR verdict (verdict.exit_code_for is sole owner, tests/meta/test_verdict_sole_ownership.py); never a mutation (doctor NFR-1); an unparseable document degrades to a named finding, never to silence.
- (constraint by claude) NO natural-language judgement. Every signal is a bounded textual pattern that can point at a line number. A check that cannot name the line it read is not shipped. This is not 'does the prose feel current'.
- (constraint by claude) False positives are the expensive failure mode here, not false negatives: a tier-wide narrative check that cries wolf gets muted, and a muted detector is worse than none. Each signal carries its measured precision over the live tier at the time it ships.
- (constraint by claude) Not a rewriter and not a replacement: it never edits prose, flips a status, or proposes replacement text (reconciliation is the chain-currency-sweep loop's job); vocabulary, counts and presence stay with bmad-drift-check and dreams-hygiene. This adds only the narrative axis none of them own.
- (assumption by claude) Assumes the six live cases are representative of the class rather than a one-off cluster. If the measured precision over the whole tier is poor for a signal, the honest outcome is to drop that signal with its number recorded — not to widen the threshold until it looks clean.
- (question by claude) Scope: Dreams and Specs only, or every tracked planning artifact (PRD, architecture spine, runbook)? Recommend Dreams + Specs for v1 — they are the two tiers with a machine-readable status: — and record the wider tier as a later capability if the first four signals earn it.
- (question by claude) Does a finding on a document another station owns need that station's acknowledgement before it renders? Doctor's Charter §6 answer is that the verdict on an artifact belongs to the station that does NOT produce it, which argues no — but five of the six live cases are foreign documents, so the question is worth an explicit ruling rather than an assumption.
- (event by claude) drafted 2026-09-09 from docs/dreams/status-body-consistency.md; SPEC.md to be derived by bmad-spec
- (event by claude) SPEC.md derived from this memlog 2026-09-09 (bmad-spec headless, create). Self-validate pass 1 coherence PASS (CAP-1..5 carry intent+success, 3 constraints bend design, 2 non-goals, the six-document validation set makes the success signal testable, lean prose), pass 2 preservation PASS (every Dream claim lands in the kernel; wrapper-only drops: Dream frontmatter and Kinships prose). status: draft; surface: [] until the module exists; both open questions carried into frontmatter.
- (event) 2026-09-17 absorbed into spec-pyforge-doctor (pointer + companions kept; derived SPEC body disposed).
