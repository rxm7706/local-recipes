---
topic: chain-currency-sweep — planning spines re-derived on trigger, never decayed (detector + runbook-driven reconciler sweep)
updated: 2026-09-04T13:47
---

- (direction) Create from docs/dreams/chain-currency-sweep.md under pyforge-doctor; headless express distill; physical path, no bmad-switch (branch sweep/run-1)
- (decision) Slug chain-currency-sweep inherited from the Dream filename; owner-dream frontmatter points at it, matching doctor spec-folder convention (spec/status/owner-dream/surface keys)
- (decision) CHAIN-CURRENCY-RUNBOOK.md adopted as companion (procedure of record, doctor-owned, referenced not duplicated); scripts/chain_currency_sweep_check.py cited in body as the shipped as-built detector, not a companion (code, not .md)
- (capability) CAP-1 detector: chain-currency-sweep-check sweeps the 8 station spines on the staleness+coherence checkpoints only; exit 0 current / 1 finding / 2 audit-could-not-run — SHIPPED as scripts/chain_currency_sweep_check.py + pixi task
- (capability) CAP-2 runbook is the procedure of record: audit mechanics (feeds order, 2-day grace, updated: precedence), finding→remedy map, dispatch+land rules — a sweep is dispatched from it, never reverse-engineered
- (capability) CAP-3 triggered sweep: research wave / spec re-stamp campaign / epic close landing substantial code / detector red → eight independent per-station cascades (brief→prd→arch→epics, +retro when code→retro fired), one commit per station, inside one grace window
- (capability) CAP-4 grounding triple per artifact touched: upstream artifact that fired (folded in) + spec-pyforge-unifying-strategy pack (hub-and-spoke role) + as-built src/ code (divergence written down); strategy spec overtaken → resolve first
- (capability) CAP-5 stamp integrity: every updated: bump pairs with a dated reconciliation addendum in the same file; a stamp without a genuine reconcile is forbidden
- (capability) CAP-6 Worked Examples accumulate per run in the runbook, timeless-workflow style; Run 1 = the 2026-08-26 26-finding clearance, doubling as the runbook validation pass
- (constraint) Findings stay advisory (doctor standing constraint) — the sweep is the remedy loop, never a second PR gate
- (constraint) Sweep exit code covers staleness+coherence only; layer gaps and orphans excluded (chain-completeness/deck-policy scope) to avoid the always-red trap
- (constraint) Dispatch: single-story per station, physical paths, BMAD_ACTIVE_PROJECT per invocation, never bmad-switch or a shared orchestrator; verify placement and frontmatter survival after every edit
- (decision) Non-goals: no second gate, no stamp-gaming, not replacing marshal SYNC-RUNBOOK (skill-facing volatile facts stay its scope), no layer-gap/orphan remediation
- (decision) Success signal binds to the machine check: pixi run -e local-recipes chain-currency-sweep-check exit 0 across all 8 stations after a sweep, plus addendum-per-stamp and a Worked Example appended
- (assumption) status: ready — detector half shipped, reconciler runbook tracked, Run 1 (the 2026-08-26 clearance) pending dispatch; Dream status flip handled by the parent session
- (decision) Preservation fix: Dream claim 'the sweep itself appears as a chain on the board it maintains' added to Success signal
- (event) Self-validate pass 1 (coherence, Spec Law 1-6+8): PASS — 6 CAPs intent+success, constraints bend design, 4 non-goals, machine-testable success signal, fresh unique IDs, lean prose, frontmatter YAML verified
- (event) Self-validate pass 2 (preservation): PASS after fix — Dream claims all land in SPEC or the adopted runbook companion; wrapper-only drops: Dream frontmatter (type/owner/status) and narrative color
- (event) Spec finalized: status ready; companions=[../../../CHAIN-CURRENCY-RUNBOOK.md adopted]; sources=[Dream]; detector half pre-shipped (scripts/chain_currency_sweep_check.py + pixi task chain-currency-sweep-check)
- (change) Declared this spec's real surface: scripts/chain_currency_sweep_check.py was implemented and shipped but never claimed in SPEC.md's surface list (was surface: []), leaving the tool ungoverned and the spec permanently no-baseline. Surface updated to name it; first baseline stamp follows this entry.
- (change) 2026-09-04 (PR #1043): companion `CHAIN-CURRENCY-RUNBOOK.md` rule 1 and cascade step 4 updated — `epics→sprint` is no longer a feeds edge (`scripts/fleet_scan.py::_FEEDS`, governed by marshal spec-factory-console); there is no daily ledger stamp, so the step now says: run `sprint-ledger-sync -- --project <slug> --repair-feed` and let INV-B judge the ledger. No CAP change; `scripts/chain_currency_sweep_check.py` untouched. Sweep after the change: all 8 station spines current. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-chain-currency-sweep`.
