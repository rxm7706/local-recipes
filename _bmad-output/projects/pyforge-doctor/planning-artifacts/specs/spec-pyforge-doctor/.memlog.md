---
topic: pyforge-doctor (Doctor) CLI — facade over pyforge-warden self-check + cf_atlas health/watch surfaces, three verbs: check/monitor/diagnose
updated: 2026-09-20T08:46
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pyforge-doctor`.

- (event) Surface reconcile 2026-09-19 (Story 23.4, `spec-23-4-empty-the-intake-inbox-per-its-own-readme`): `docs/MAP.md`'s `sync-jira-github-workflow-templates/` row was repointed from `docs/intake/jira-github-projects-sync/` to `archive/docs/intake/jira-github-projects-sync/` after 23.4 archived that intake dump (shipped steward Epic 8, 7/7 `done`) — a live-pointer fix caused by the intake move, not new doctor product work. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pyforge-doctor`.

- (direction) Distillation, not re-planning: source chain (dream, brief, prd, epics) is an already-approved, implemented-against artifact set; capability IDs map to PRD §4 feature groups, constraints carry architecture ADs 1-6 + NFRs, non-goals from PRD §5, success signal from PRD §7. Architecture + epics adopted/absorbed per Companions rules below.
- (capability) CAP-1 (doctor check, PRD §4.1/FR-1-3): intent - operator or Marshal runs 'doctor check --env --engines' for a fast, tri-state pre-flight wrapping warden's engine self-check and a new credential/env-hygiene detector. success - engine findings identical to warden's --doctor output; every check reports ok/warn/fail and is individually nameable/filterable via 'doctor check --list'; a JFROG_API_KEY-shaped unconditional-credential-injection pattern is caught by --env.
- (capability) CAP-2 (doctor monitor --fleet, PRD §4.2/FR-4-5): intent - operator runs 'doctor monitor --fleet --watch <axis>[,<axis>...]' to get one normalized, source-tagged fleet-pulse view over cf_atlas's staleness/cve/abandonment signals instead of reconciling five separate CLIs by hand. success - findings equivalent to the underlying atlas CLI/MCP call for the same scope, each tagged and filterable by originating Source; omitting --watch runs a documented default axis set (staleness+cve) rather than every axis unconditionally.
- (capability) CAP-3 (doctor diagnose --prescribe, PRD §4.3/FR-6-8): intent - operator triaging a target runs 'doctor diagnose --target <target> --prescribe' to get every gathered finding partitioned (actionable/blocked/accepted-risk, none silently dropped) and the actionable set ranked by severity x exploitability x blast-radius with root cause named. success - total findings across partitions equals findings gathered; a KEV-flagged finding outranks a non-KEV one of equal severity, higher-EPSS outranks lower at equal severity, smaller upgrade-lag outranks larger as tiebreaker; every Prescription shows its rank_factors (never a bare number) and a root_cause templated from the finding's own structured evidence (no new inference layer).
- (capability) CAP-4 (machine-consumer contract, PRD §4.4/FR-9): intent - every verb (check/monitor/diagnose) accepts a --json flag producing one schema-validated DoctorReport document with the same information as the human-readable output. success - --json output validates against a committed JSON Schema; no information present in the human-readable render is absent from --json (parity requirement).
- (constraint) AD-1: 'doctor check --engines' calls pyforge.warden.engines.run_doctor_checks as a library import, never a subprocess reimplementation; pyforge-doctor declares pyforge-warden as an optional 'gate' extra (mirrors pyforge-atlas's identical existing edge to warden). A meta-test asserts doctor.sources.warden contains no subprocess import/call.
- (constraint) AD-2: Doctor owns its own closed exit-code space {0 = all ok, 2 = a fail present, 130 = SIGINT} that never merges with or re-exposes warden's exit code, permanently omitting warden's policy-gate rung 1 -- Doctor reports operability, never policy. A warn-status finding never changes the exit code. Warden's own exit code is consumed only as data folded into a Finding.status.
- (constraint) AD-3: Doctor defines its own closed Finding/Source/DoctorStatus taxonomy (pyforge.doctor.models: DoctorStatus{ok,warn,fail}, a closed Source enum with one member per wrapped instrument, a Finding dataclass) structurally mirroring warden's models.py pattern but never importing warden's ErrorKind directly. Resolves PRD §8 open question 5.
- (constraint) AD-4: 'diagnose --prescribe' (pyforge.doctor.prescribe) is a pure function over an already-gathered list[Finding] -- zero subprocess or MCP calls of its own; a meta-test asserts no subprocess/MCP-client import exists in doctor.prescribe.
- (constraint) AD-5: exactly one narrow, typed subprocess site in the whole package -- pyforge.doctor.cli_bridge -- reserved for the CLI-fallback branch of AD-6; argv as a list (never a shell string), NO_COLOR-equivalent + stdin=DEVNULL discipline, bounded timeout, typed Finding(status=fail) on failure, never a raw traceback. A meta-test asserts cli_bridge is the only module containing a subprocess call.
- (constraint) AD-6: 'monitor --fleet' calls cf_atlas's MCP tools when an MCP client is available in-process (the expected Marshal/agent path); otherwise falls back to the equivalent CLI subprocess via AD-5's cli_bridge. Both paths must normalize to the identical Finding shape -- the caller cannot tell which path produced a given Finding except via its Source tag.
- (constraint) NFR-1: v1 is read-only/non-mutating across all three verbs -- no module under pyforge.doctor writes outside a tempfile-scoped path or mutates a scanned tree; no --fix/actuator exists.
- (constraint) NFR-4: doctor check's default run must stay within a documented speed budget (the five-second pre-flight promise, UJ-1) -- a benchmark test guards against future checks regressing this; check-suite runtime must not creep upward in pursuit of more findings (PRD SM-C1 counter-metric).
- (constraint) NFR-5: the DoctorReport JSON envelope ({schema_version, verb, generated_at, findings, prescriptions}) carries a schema_version field starting at 1, carrying warden's ComplianceReport precedent forward; 'prescriptions' is present only when verb=diagnose.
- (constraint) env-hygiene detection boundary (resolves PRD §8 open question 2, fixed by epics Story 1.4): the check fires on an env-var read (os.environ.get/os.getenv) feeding an HTTP-header/auth assignment with no accompanying host-scope conditional; a host-scoped credential attach (a conditional gating the header assignment on destination host) must NOT produce a finding. The scanner uses ast.parse only -- never exec/eval/dynamic-import of scanned code.
- (constraint) default Watch-axis set (resolves PRD §8 open question 3, fixed by epics Story 2.3): omitting --watch runs staleness+cve as the two highest-signal defaults, not every axis unconditionally.
- (constraint) check --list ships in v1 (resolves PRD §8 open question 6, fixed by epics Story 1.3): doctor check --list enumerates every named check without running them; running one named check in isolation matches that check's result within the full suite.
- (decision) Non-goal: no auto-remediation actuator in v1 -- check/monitor/diagnose are strictly read-only; no --fix, no PR-opening, no file mutation (PRD §5).
- (decision) Non-goal: no persistent fleet-health surface (dashboard/tracked-issue/status file) in v1 -- CLI + JSON output only; a Renovate-Dashboard-style persistent view is a possible v1.x addition, not a v1 commitment (PRD §5, §6.2).
- (decision) Non-goal: no new scanning engines or data sources -- Doctor consolidates warden + atlas only; the credential/env-hygiene check (FR-3) is the one new detection capability, not a precedent for adding others in v1 (PRD §5).
- (decision) Non-goal: no real dependency-graph topological resolver for --prescribe -- ranking + lag-magnitude tiebreaker only in v1; true multi-hop ordering is a possible v1.x addition (PRD §5, Architecture AD-4 Deferred).
- (decision) Non-goal: no waiver-authoring UI for the accepted-risk partition -- the partition exists for forward-compatibility; authoring/managing waivers is out of v1 (PRD §5).
- (decision) Non-goal: not a general-purpose OSS health-check tool -- scoped to this factory's own instruments (warden, atlas) and this repo's own fleet, not external conda-forge/staged-recipes contributors (PRD §2.2, §5).
- (decision) Success signal (PRD §7): SM-1 zero mid-build failures attributable to an environment/engine condition 'doctor check' could have caught, measured across factory runs after adoption. SM-2 'doctor diagnose --prescribe' ranking judged by the operator as at least as good as manually cross-referencing warden+atlas output for the same target. SM-3 'doctor monitor --fleet' replaces the operator's manual multi-CLI weekly habit in observed practice (used, not just built). SM-4 the credential/env-hygiene check (FR-3) is live and catches the JFROG_API_KEY unconditional-injection worked example by v1 ship. Counter-metrics (do not optimize): SM-C1 check-suite runtime must not creep upward chasing more findings; SM-C2 prescription ranking is not optimized for longest-list, a short correctly-partitioned worklist is the goal.
- (assumption) Marshal (the bmad-loop orchestrator) is the primary machine consumer of 'doctor check' as an unattended pre-flight gate; the repo maintainer is the primary human consumer running things by hand (brief, PRD §2.1 JTBD).
- (assumption) Success signals SM-2/SM-3 are qualitative and self-observed -- no analytics/telemetry infrastructure is built to measure them in v1, consistent with a single-operator internal tool (PRD §7, §9 Assumptions Index).
- (assumption) Agent-consumers (Marshal, other BMAD skills) are read-only callers of doctor's CLI + --json family; Doctor needs no agent-specific API surface beyond a well-behaved CLI, consistent with warden's own CLI-first design (brief §Who This Serves).
- (question) Open question 1 (PRD §8.1, unresolved through epics -- Story 1.4 AC1 literally punts with 'status=warn_or_fail'): exact severity default (warn vs fail) for the credential-hygiene check (FR-3) when a risky pattern like JFROG_API_KEY unconditional injection is detected.
- (question) Open question 2 (PRD §8.4, narrowed but not fully closed): exact schema-versioning POLICY for the DoctorReport envelope -- schema_version starts at 1 (NFR-5, fixed) but no rule is specified for what triggers a version bump (breaking vs additive change) or how consumers should react to one.
- (event) Chain-resolved PRD open questions folded into Constraints, not carried forward as spec-level open questions: §8.2 credential-hygiene generalization boundary (resolved by epics Story 1.4 host-scope-conditional rule), §8.3 default watch-axis set (resolved by epics Story 2.3: staleness+cve), §8.5 Doctor's own taxonomy vs warden's ErrorKind (resolved by Architecture AD-3), §8.6 check --list ships in v1 (resolved by epics Story 1.3).
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6+8): CLEAN. 4 capabilities each with intent+success (rule 1); intents are WHAT not HOW (rule 2); 11 constraints each rule something out -- AD-1..6 + NFR-1/4/5 + env-hygiene boundary + default-axis + check--list (rule 3); 6 explicit non-goals (rule 4); success signal ties to named, testable SM-1..4 + 2 counter-metrics (rule 5); CAP-1..4 IDs stable/unique, no reuse (rule 6); prose kept lean, no elicitation needed given rich structured input (rule 8). No fixes required.
- (event) Self-validate Pass 2 (preservation): walked PRD FR-1..9 (all 4 CAP entries + constraints cover all 9), NFR-1..5 (NFR-1/4/5 named directly, NFR-2/NFR-3 folded into AD-2/AD-5 which ARE those rules, not omitted), PRD §5 non-goals (6/6 present), PRD §7 success metrics SM-1..4 + counter-metrics C1/C2 (present in Success signal), PRD §8 open questions (2,3,5,6 resolved-down-chain -> folded into Constraints per the chain-resolution event entry; 1,4 genuinely still open -> Open Questions section), Dream's three verb-shaped claims (all 3 -> CAP-1/2/3), Architecture AD-1..6 (all 6 -> Constraints), Architecture diagram/Structural Seed/Consistency Conventions table (-> adopted companion ARCHITECTURE-SPINE.md, not duplicated in kernel per diagrams-always-a-companion rule). Wrapper-only content dropped (logged, not silent): PRD §0 Document Purpose, §3 Glossary term-definitions restating capability wording, §2.3 UJ-1..4 narrative (redundant with capability success criteria), full §2.1 JTBD/§2.2 non-users prose beyond the 3 Assumption-worthy claims, Architecture Stack version-pin table (implementation detail, not contract-load-bearing), epics.md's per-story Given/When/Then test scripts (decomposition detail already reflected at capability-success granularity). No load-bearing claim found undropped-and-unplaced.
- (note) Surface declared 2026-07-25: src/shared/packages/pyforge-doctor/** — the package this Spec builds. Previously undeclared; the files were masked by spec-pyforge-atlas's over-broad src/** claim until that was narrowed.
- (event) Epic 1 stories 1.2 and 1.3 SHIPPED 2026-07-30 via the six-station fleet run on loop/pyforge-doctor, unattended with the standing independent review. Merges 623deee254 (1.2 wrap warden's engine-availability self-check, dev x1 / review x2, 7.88M weighted) and 44a5026256 (1.3 tri-state individually-addressable checks, dev x1 / review x1, 4.93M). Landed checks/registry.py and sources/warden.py with models.py extended for the tri-state result, and two meta-tests that make AD-1's boundary structural rather than remembered: test_no_warden_import.py and test_sources_warden_no_subprocess.py. 119 tests green. The root pixi.toml gained pyforge-warden as an in-repo feature-level path dependency — deliberately NOT a pyforge-doctor package run-dep, so an external conda install stays warden-optional (mirrors the identical edge in [feature.pyforge-atlas.dependencies]).
- (note) Story 1.4 (credential-environment hygiene check) did NOT land and is NOT in this merge, despite reading `done` in the Tier-3 sprint feed. DEFERRED at dev x1 / review x3 — review budget spent with a follow-up still recommended. Work preserved on bmad-loop/20260730-192238-c553/1-4-credential-environment-hygiene-check (14 files / +2,863), pushed to origin. The feed's `done` was corrected to `backlog` on 2026-07-31; see the fleet run record's finding 7 for the pattern, which is that a dev pass self-marks done and a failed review does not roll it back.
- (event) Story 1.4 (credential/environment-hygiene check, FR-3) RECOVERED and landed 2026-07-31. It was DEFERRED on 2026-07-30 at dev x1 / review x3 — the review budget was spent with the reviewer still recommending a follow-up pass, and the feed then read `done` for hours although nothing had merged (fleet-run finding 7). Recovered by cherry-picking the story's own two commits (581dcf12c5 implementation, 7949053004 the follow-up-review hardening: guard polarity, crash containment, sentinel split) onto main — NOT by merging the branch, which shows phantom add/add conflicts because the branch carries pre-squash copies of 1.2/1.3. Clean pick, no conflicts. doctor's suite 119 -> 166.
- (constraint) This landed WITHOUT a converged review, and that is the honest status. What it has: a dev pass, one applied follow-up-review hardening commit, 166 green tests, and a full six-station + four-detector gate on the merge result. What it does NOT have: a reviewer that stopped recommending changes. The remaining recommendation is unread — the review session was cut by budget, not by agreement. Treat the delivered scope as sound and the polish as unfinished; the next reviewer of this file starts from the follow-up the budget cut off.
- (note) Bootstrap 2026-08-02: no prior .memlog.md existed (this Spec predates the memlog-driven convention). Backfilling CAP-1..CAP-4 verbatim from the existing SPEC.md (authored 2026-07-25, already implemented as Epic 1, 5/5 stories done) before appending new decisions, so nothing already-decided is lost on re-derive.
- (capability) CAP-1 (backfilled, unchanged): doctor check --env --engines fast tri-state pre-flight wrapping warden's engine-availability self-check + new credential/environment-hygiene detector. success: engine findings identical to warden's own --doctor output; every check ok/warn/fail, individually nameable/filterable incl. --list; JFROG_API_KEY-shaped unconditional injection caught by --env, host-scoped attach is not a false positive.
- (capability) CAP-2 (backfilled, unchanged): doctor monitor --fleet --watch <axis> one normalized source-tagged fleet-pulse view over cf_atlas staleness/cve/abandonment. success: findings equivalent to underlying atlas CLI/MCP call, tagged/filterable by Source; omitting --watch runs documented default (staleness, cve).
- (capability) CAP-3 (backfilled, unchanged): doctor diagnose --target <target> --prescribe partitions every finding (actionable/blocked/accepted-risk, none dropped), ranks actionable by severity x exploitability x blast-radius, names root cause. success: partition counts sum to gathered count; KEV outranks non-KEV at equal severity; higher EPSS outranks lower at equal severity; smaller upgrade-lag as tiebreaker; every Prescription shows rank_factors + templated root_cause.
- (capability) CAP-4 (backfilled, unchanged): --json on every verb (check/monitor/diagnose) returns one schema-validated DoctorReport. success: --json validates against committed JSON Schema; no information in human-readable render absent from --json (parity).
- (constraint) (backfilled) AD-1 library-not-subprocess: doctor check --engines calls pyforge.warden.engines.run_doctor_checks as library import, never subprocess; pyforge-warden is an optional gate extra.
- (constraint) (backfilled) AD-2 closed non-merging exit-code space: Doctor owns {0,2,130}, permanently omits warden's policy-gate rung 1 -- Doctor reports operability, never policy.
- (constraint) (backfilled) AD-6 MCP-first, CLI-fallback: monitor --fleet calls cf_atlas MCP tools when available in-process, else falls back to CLI subprocess via the one narrow typed subprocess site (AD-5 cli_bridge); both paths normalize to identical Finding shape.
- (non-goal) (backfilled) No auto-remediation actuator in v1 -- read-only across all three verbs, no --fix.
- (non-goal) (backfilled) No persistent fleet-health surface in v1 -- CLI + JSON only; explicitly named as a possible v1.x addition, not a v1 commitment (this is the opening CAP-6 below walks through).
- (non-goal) (backfilled) No new scanning engines or data sources beyond the one credential/env-hygiene check -- Doctor consolidates warden + atlas only.
- (non-goal) (backfilled) No real dependency-graph topological resolver for --prescribe -- ranking + lag-magnitude tiebreaker only in v1.
- (note) CORRECTION: the prior 'no .memlog.md existed' note was wrong -- it is a hidden dotfile, 'ls' without -a missed it. A real, rich memlog already existed (38 entries: full PRD/epic cross-references, Story 1.4's deferred/recovered operational history). The 4 backfilled CAP-1..4 entries just appended above are redundant with the real ones earlier in this log -- both say the same thing, harmless duplication, not a correction of content. Proceeding to the actual new work: CAP-5..8 from docs/dreams/pyforge-doctor.md's frontier section, per the operator-approved dream-consolidation pass.
- (capability) CAP-5 (health scoring, new -- dream 'The frontier' item 1): intent -- an operator can see a composite health grade (A-F) per dependency, synthesized from Doctor's own already-gathered Finding data across axes (age/staleness, CVE exposure, abandonment signal) -- not a new scanning engine, a new aggregation layer over existing sources. success -- the grade is a pure function over already-gathered Findings (no new subprocess/MCP call of its own, consistent with AD-4's precedent for prescribe); the same Finding set always produces the same grade (deterministic, no timestamp-in-logic-path); a grade is never computed from a partial/incomplete gather -- an incomplete axis set degrades the grade to explicitly 'incomplete', never a false A.
- (capability) CAP-6 (persistent fleet-health surface, new -- dream frontier item 2): intent -- an operator can see the fleet's health condition as a tracked, at-a-glance surface instead of reconstructing it from a point-in-time monitor --fleet snapshot. success -- the surface is derived output written from a monitor --fleet run (same Finding/Source shape CAP-2 already produces, never a second independent gather); regenerating from the same underlying findings is idempotent; the surface's own schema is versioned the same way DoctorReport is (NFR-5 precedent) so a consumer can detect a format change.
- (capability) CAP-7 (adoption-tracking watch axis, new -- dream frontier item 3): intent -- an operator can add 'adoption' to monitor --fleet's --watch set and get cf_atlas's adoption-stage and version-downloads signals normalized into the same Finding shape as the existing staleness/cve/abandonment axes. success -- follows AD-6 exactly (MCP-first, CLI-fallback via cli_bridge, both paths normalize identically); adoption is NOT in the default --watch set (CAP-2's default stays staleness+cve unless explicitly widened) so this is additive, not a default-behavior change; a Source enum member is added for it (AD-3's closed-taxonomy pattern extended, never an open/stringly-typed source).
- (capability) CAP-8 (safe upgrade-path recommendation, new -- dream frontier item 4): intent -- a Prescription from diagnose --prescribe can name a specific next-safe-version target, not just rank and explain. success -- the recommendation is single-hop only (this package's own next safe version, sourced from atlas's existing behind-upstream/version data) -- explicitly never a transitive multi-package resolution (the existing non-goal on a real dependency-graph resolver is reinforced, not reopened, by this capability); a Prescription with no confidently-known safe version states that plainly rather than guessing one; prescribe stays a pure function over already-gathered data (AD-4 preserved -- no new subprocess/MCP call added to prescribe itself, the version data arrives pre-gathered like every other Finding).
- (decision) Non-goals reinforced, not weakened, by CAP-5..8: 'no new scanning engines' now explicitly covers CAP-5 (health scoring is aggregation over existing sources, not a new instrument) and CAP-7 (adoption-tracking wires an EXISTING atlas source, doesn't add a new one); 'no real dependency-graph resolver' now explicitly bounds CAP-8 to single-hop only; 'no persistent fleet-health surface in v1' is the boundary CAP-6 crosses ON PURPOSE as the v1.x graduation the PRD itself already named as a candidate -- CAP-6 is that graduation, not a new promise.
- (event) Source: docs/dreams/pyforge-doctor.md (fresh 2026-08-02 rewrite, dream-consolidation pass). The sibling dream pyforge-doctor-dependency-health.md (bulk-commit origin, partially fabricated) is retired; its 4 real, verified items are CAP-5..8 above; its unverifiable precision claims (95% catch rate, 1000+ packages, 80% acceptance) do not carry forward -- no success criterion above cites them.
- (event) Self-validate Pass 1 (coherence): PASS. CAP-5..8 each carry intent+success (rule 1); intents WHAT-shaped (rule 2); non-goals reinforced not weakened (rule 3/4 -- 3 non-goals updated with explicit CAP-5/7/8 boundary language, 1 non-goal graduated to CAP-6 with the graduation stated explicitly); success signal extended with 4 new testable criteria (rule 5); CAP-5..8 are the next unused IDs, none reused (rule 6); prose lean, mirrors existing CAP-1..4 voice (rule 8).
- (event) Self-validate Pass 2 (preservation): PASS. All 4 dream frontier items -> CAP-5..8, 1:1. Existing CAP-1..4/AD-1..6/NFR-1/4/5/non-goals preserved verbatim, cross-referenced from new capabilities rather than restated. Fabricated dream's unverifiable precision numbers (1000+ packages, 95% catch rate, 80% acceptance) deliberately NOT preserved -- logged as wrapper-only/unfounded content, not silently dropped.
- (event) Spec updated 2026-08-02: 8 capabilities (4 existing + 4 new), companions/sources unchanged, 3 open questions (2 existing + 1 new on CAP-6's surface medium). Next: extend PRD (FR-10..13) and epics.md (new Epic 4) to decompose CAP-5..8, mirroring how CAP-1..4 already decompose into FR-1..9 and Epics 1-3.
- (decision) DRIFT-PRESUMED RECONCILED, 2026-08-09, marshal S-13.6 (CAP-6 / FR-169). 31 entries, all under src/shared/packages/pyforge-doctor/** — 22 added / 9 changed / 0 removed. Traced: Story 5.2 (render the marshal-durability verdict, and the schema gap it exposed) 4, Story 6.1 (the profile that found a truncated scan) 2, Stories 4.2 and 2.1 one each, plus within-epic review-finding fixes ("hold the verdict on Marshal's own row (Charter §6)" 4, "fix Epic 4 review findings" 4, "fix Epic 2+3 review findings" 2) and one chain-closing commit. Every referenced story is `done`; all fall inside capabilities this Spec already contracts. Contract unchanged; baseline scoped-stamped with the reasoning recorded.
- (note) Doctor's Epic 6 will ADD files to this surface as the four repo detectors move into src/shared/packages/pyforge-doctor/ (S-6.4..S-6.9). Expect this baseline to lag again the moment that lands, and reconcile it as part of that epic rather than treating the new entries as drift — they will be this station's own contracted delivery.
- (change) EPIC 6 STORIES 6.2/6.3/6.4 SHIPPED via bmad-loop run 20260808-210255-c88b, reconciled by name on landing (2026-08-09). S-6.2 (a source registry Doctor owns): src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py (the REGISTRY), src/shared/packages/pyforge-doctor/tests/unit/test_sources_registry.py, and scripts/detectors.py + tests/scripts/test_detectors_doctor_sources.py + pixi.toml (the `pyforge-doctor-scripts-test` task) for the repo-root detector CLI's integration — the AST-scan registry blocker Epic 6 recorded up front. S-6.3 (the repo/runtime split survives the move): src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/marshal.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_ledger_independence.py. S-6.4 (the ledger verdicts come home): src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/ledger.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_ledger.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_marshal_story_status.py, plus src/shared/packages/pyforge-doctor/src/pyforge/doctor/__main__.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/data/report-schema.json, src/shared/packages/pyforge-doctor/tests/unit/test_cli_check.py and src/shared/packages/pyforge-doctor/tests/unit/test_models.py for the new source wiring. Charter §6: Marshal keeps the operational guard, Doctor takes the verdict.
- (note) FIRST TIME the spec-surface gate fired on bmad-loop OUTPUT, and it is a standing gap, not a one-off: the loop does not reconcile the spec surface, so EVERY loop-produced story reds spec-surface-check until a human names the paths on landing. Marshal S-13.2's per-file rule is what makes that reconciliation honest rather than a blanket stamp — but nothing in the loop knows the rule exists. Reconciled here by naming all 14 paths; the general fix belongs to the loop's own dev/review contract and is NOT solved by this entry.
- (change) EPIC 6 STORY 6.5 SHIPPED via bmad-loop run 20260808-210255-c88b, reconciled by name on landing (2026-08-09). S-6.5 (the board verdicts come home) brings the dashboard-side verdicts under Doctor per Charter §6: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py (new, the board source), src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py (registry entry), src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py and src/shared/packages/pyforge-doctor/src/pyforge/doctor/data/report-schema.json (model/schema wiring), with tests src/shared/packages/pyforge-doctor/tests/unit/test_sources_board_chain_completeness.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_board_check_layout.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_board_dashboard_drift.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_board_independence.py and src/shared/packages/pyforge-doctor/tests/unit/test_models.py. Doctor's suite 502 -> 604.
- (note) SECOND occurrence of the S-13.7 gap, one landing later and exactly as predicted: the loop again produced governed changes without reconciling the spec surface, so all 9 paths were named by hand here. Recorded because the recurrence is the evidence — FR-174 / Story 13.7 is not a hypothetical tax, it is one paid at every single landing.
- (change) EPIC 6 STORY 6.6 SHIPPED via bmad-loop run 20260808-210255-c88b, reconciled by name at landing (2026-08-09). The chain verdicts come home (Charter §6): src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py (new — dream-chain, spec-surface and deferred-work verdicts), src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py (registry), src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/data/report-schema.json, src/shared/packages/pyforge-doctor/pixi.toml and src/shared/packages/pyforge-doctor/pyproject.toml, with tests src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_dream_chain.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_spec_surface.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_deferred_work.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_independence.py and src/shared/packages/pyforge-doctor/tests/unit/test_models.py. Root pixi.toml also changed. Doctor suite 604 -> 706.
- (note) 6.6 cost ~9.5 hours and crossed the 4M per-session token cap TWICE (dev-1 at 4.35M, review-3 at 4.05M) with mode=warn, so the cap warned and never enforced — the known max_tokens_per_story blind spot, observed at scale. It was the in-flight story when the graceful stop was issued and it completed through commit, exactly as `bmad-loop stop --graceful` promises.

## Surface reconcile 2026-08-09 — one path, via marshal 4-11's landing

- (event) `src/shared/packages/pyforge-doctor/pixi.toml` CHANGED — `[package.run-dependencies]` gains `mcp = ">=1.28.1"`. Doctor's own Story 2.1 imports the MCP SDK at module scope in `sources/atlas.py` for the atlas Watch-axis transport, but the floor was declared only in the root `pixi.toml`'s dev feature, never in the PACKAGE manifest — so the built `.conda` would install without it and the import would raise `ModuleNotFoundError` at runtime. Exactly the same defect class Story 6.6 fixed one line above for `pyyaml`, and it is fixed here the same way: package floor matches `pyproject.toml`'s own, while the root dev env stays pinned tighter at `>=2.0.0` (the identical split pyforge-herald uses).
- (event) The change arrived inside marshal story 4-11's landing rather than a doctor story — 4-11's second commit closed three pre-existing `pyforge-deps-test` failures that were blocking its own land, and this missing floor was one of them. Verified on the landing branch: `pyforge-deps-test` 60 passed. Recorded here rather than stamped silently because the path is Doctor's, not Marshal's. Baseline scoped-stamped to this spec.

## Story 6.7 — forward_dependency comes home, and the harness coupling is decided (2026-08-09)

- (decision) AD-13 RECORDED in the architecture spine, resolving Story 6.7's explicit "the harness coupling is decided" acceptance criterion. Operator chose Option B: `sources/deps.py` RESTATES `ACTIONABLE_STATUSES = frozenset({"backlog", "ready-for-dev"})` and imports nothing from `bmad_loop`. Rejected alternative — adding `bmad-loop` to `[feature.pyforge-doctor.dependencies]` — is recorded in full in AD-13 with its three grounds: it contradicts AD-11's own rationale (a verdict assembled from the judged station's machinery fails exactly when that machinery is what broke), it converts one check's dependency into a whole-CLI dependency in a 7-dep env whose contract is a five-second pre-flight (NFR-4/SM-C1), and it would need a second S-6.10 allowlist entry whose `sources/warden.py` justification does not transfer.
- (constraint) The "derived, never restated" invariant is NOT dropped, it MOVED — from runtime import to test-time equality. `.claude/skills/conda-forge-expert/tests/meta/test_actionable_statuses_conformance.py` imports the installed `bmad_loop` and asserts SET EQUALITY against the literal parsed out of `deps.py` by `ast` (never by importing `pyforge.doctor`, which is not installed in `local-recipes`). Three properties keep it honest and were each MUTATION-TESTED before landing: drift fails with a both-directions diff; a constant edited to a non-literal fails with the AD-13 reason rather than silently defeating the comparison; and an absent harness raises `ModuleNotFoundError` rather than skipping — a deliberate deviation from the sibling `test_forward_dependency_check.py`'s `pytest.importorskip`.
- (event) Seven governed paths moved under this Spec. ADDED `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/deps.py` — the port itself, reading only each project's tracked `epics*.md` + `sprint-status-ledger.yaml`. ADDED `tests/unit/test_sources_deps_forward_dependency.py` (behavioural: the two 2026-08-08 live defects, the whole-epic `S-<n>.*` grammar, all four coverage classes) and `tests/unit/test_sources_deps_independence.py` (bars `pyforge.marshal` AND `bmad_loop`, at module scope, lazily, and as a string constant). CHANGED `src/pyforge/doctor/models.py` (`Source.FORWARD_DEPENDENCY`), `src/pyforge/doctor/data/report-schema.json` (the same value in the envelope enum), `src/pyforge/doctor/sources/__init__.py` (the REGISTRY row, `subject_station="marshal"`), and `tests/unit/test_models.py` (the closed-taxonomy tripwire, which failed first and was updated deliberately — that is what it is for).
- (event) PARITY PROVEN against live data, not fixtures: the ported `gather_forward_dependency` reproduces `scripts/forward_dependency_check.py`'s verdict on the real repo exactly — 2 measured, 3 partial, 3 no-dispatch, 0 unmeasured, with identical per-station ratios (atlas 7/43, mason 7/30, steward 6/7) and zero forward-dep failures. `pyforge-doctor-test` 739 passed, 1 skipped.
- (change) The port is not a pure relocation in one further respect: the origin script COUNTED its no-dispatch stations but only printed them, and a naive port would have folded them into a number. They are now emitted as OK findings carrying the station and its ledger size, because a station that CANNOT suffer this defect must stay distinguishable from one that was never looked at. The coverage summary is likewise emitted unconditionally, including on a red run — "how much did you measure?" matters most precisely when something failed.
- (note) `pixi.toml` is UNTOUCHED, though Story 6.7's stated surface anticipated it. That is the visible signature of choosing Option B: the rejected alternative was the one that needed a manifest change. Recorded so the surface/diff mismatch does not later read as an omission.
- (note) DEFERRED, flagged not fixed: doctor's tracked `planning-artifacts/specs/` carries story specs for epics 1-4 only — epics 5 and 6 landed via bmad-loop without their specs being promoted from Tier-3, contrary to the durable-story-spec convention. Not introduced by this story and out of its scope; queued as immediate follow-up work at the operator's direction.

## Story 6.8 — bmad_drift comes home without breaking the board (2026-08-09)

- (change) EPIC 6 STORY 6.8 SHIPPED (commit `feee494db8`), reconciled by name at landing. The tenth and last of Epic 6's Charter §6 sweep: `scripts/bmad_drift_check.py`'s 13 check functions / 18 finding kinds ported into `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py` (new — single-`Source` module, `gather(target)`, mirrors `sources/ledger.py`'s naming convention since the origin is one cohesive detector, not several). CHANGED `src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py` (`Source.BMAD_DRIFT`), `src/shared/packages/pyforge-doctor/src/pyforge/doctor/data/report-schema.json` (the same value in the envelope enum), `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py` (the REGISTRY row, `subject_station="marshal"`), and `src/shared/packages/pyforge-doctor/tests/unit/test_models.py` (the closed-taxonomy tripwire). ADDED `src/shared/packages/pyforge-doctor/tests/unit/test_sources_factory.py` (all 18 finding kinds against tmp fixtures + a mutation test per check) and `src/shared/packages/pyforge-doctor/tests/unit/test_sources_factory_independence.py` (AST import scan, bars `pyforge.marshal`).
- (event) PARITY PROVEN against live data: `python3 scripts/bmad_drift_check.py` and `factory.gather(Path('.'))` produce the identical finding set (2 `pin-behind` INFO, both snapshot-tagged) on this repo. `pyforge-doctor-test` 782 passed, 1 skipped (739 -> 782 across this story). `dashboard-gen` succeeds unaffected, confirming Part 1's constant move (`docs/governance/guild-roster.json`, PR #360) and this port don't collide.
- (note) THIRD occurrence of the S-13.7 gap: the loop again produced governed changes without reconciling the spec surface (the dev-auto session halted with the review triage log complete and `final_revision` captured, but the spec-surface gate still reported 7 findings against a stale baseline). Reconciled here by naming all 7 paths in a dedicated repair pass, same as S-6.4/6.5/6.6/6.7 before it.
- (change) FOLLOW-UP REVIEW PASS (2026-08-09, `followup_review_recommended: true` from the first pass). Both hunters independently converged on ONE class: the first pass closed the false-clean-on-cannot-evaluate hole only at the top-level existence gates (`_probe`/`_is_dir`), leaving every ENUMERATION site (`Path.glob`/`rglob`) and the two `Path.is_file()` gates on the swallowing stdlib primitives. Four failures reproduced live, each producing NO finding at all before the fix: `chmod 000` on one nested `planning-artifacts/specs/` erased a real `uncovered` HARD and a real `stale-rule` WARN; `chmod 000` on `implementation-artifacts/` made three checks WARN while `check_deferred_work` reported clean; deleting the skill `CHANGELOG.md` erased all 17 `pin-behind` findings (`_parse_ver(None)` -> `(0,0,0)`, a version no pin can be behind); deleting the atlas registry fabricated `phase-list-stale: … omits phases through N` out of ground truth never read. CHANGED `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py` (added `_is_file`/`_listdir`/`_listdir_match`/`_walk` mirroring `sources/chain.py`'s converged trio, routed all 13 enumeration/existence sites through them, widened `_read` to decode non-UTF-8 bytes with U+FFFD instead of losing a whole check, made `_live_version`/`_max_single_phase` raise — LAZILY, so a repo with nothing to judge stays quiet and `check_pins` keeps its `pin-missing` half — and carried `target` on every `bmad-drift-unevaluable` finding per `sources/ledger.py`'s stated rule) and `src/shared/packages/pyforge-doctor/tests/unit/test_sources_factory.py` (+9 tests: the first permission-based tests in this file, the untested wrong-shape `baseline-corrupt` branch the FIRST pass added, non-UTF-8 sibling survival, unknown-live-version, lazy phase registry, uniform evidence keys, and finding-order stability; plus a `GIT_CEILING_DIRECTORIES` pin so the git-unavailable test stops depending on TMPDIR not being inside a checkout).
- (event) LIVE PARITY RE-PROVEN after the hardening: `python3 scripts/bmad_drift_check.py` and `factory.gather(Path('.'))` still produce the identical finding set (the same 2 snapshot-tagged `pin-behind` INFO findings, same messages) — the fixes change behavior only where the origin could not answer at all. `pyforge-doctor-test` 791 passed, 1 skipped (782 -> 791). `dashboard-gen` green.
- (note) Two entries this pass closed were already sitting OPEN in the Tier-3 deferred-work ledger from the first pass (the mid-`rglob` walk residual and `_read`'s `UnicodeDecodeError`); per the orchestrator's standing instruction the review session appends NEW ledger entries only and never rewrites existing ones, so those two remain open there for the orchestrator to resolve. Two genuinely new items were deferred: `check_counts`'s four probes still degrading silently to `0`/`None`, and the origin's unconditional coverage summary having no port (which would need a 19th finding kind and would make `_gather`'s aggregate-OK branch unreachable).
- (change) SECOND FOLLOW-UP REVIEW PASS (2026-08-09). Both hunters independently converged again, on the ONE member of the cannot-evaluate family the previous two passes never reached: `_read`. Existence (`_probe`/`_is_dir`/`_is_file`) and listing (`_listdir`/`_listdir_match`/`_walk`) had all been converted to raising counterparts, but the primitive that answers *what is in this file* — which every one of the 13 checks ultimately reads through — kept the origin's blanket `except OSError: return ""`. It failed in BOTH directions, each reproduced live: `chmod 000` on one project `.md` erased its real `stale-rule` WARN and left a confident aggregate OK, and `chmod 000` on `planning-artifacts/` manufactured FOURTEEN FALSE `pin-missing` HARD findings against docs whose pins were perfectly intact (`_doc_pin` reads `""` as "states no pin", so an unreadable doc is not merely lost, it is actively slandered). CHANGED `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py` (narrowed `_read` to answer `""` only for the genuinely-absent cases — `FileNotFoundError`, and `IsADirectoryError`/`NotADirectoryError` for a directory named `*.md`, which `check_stale_rules` reaches by suffix alone and the origin also read as empty — letting everything else land in `_gather`'s per-check net; and split `check_archive_hygiene`'s two INDEPENDENT trees into their own try/except scopes, the shape `check_pins` and `check_tier_alignment` already used, after `chmod 000` on `planning-artifacts/` was shown to discard a wholly readable `implementation-artifacts/`'s real `stray-file` and `archive-misplaced` HARD findings) and `src/shared/packages/pyforge-doctor/tests/unit/test_sources_factory.py` (+3 tests for those two fixes plus a repair to `test_every_unevaluable_finding_carries_the_same_evidence_keys`, which reached `_gather`'s missing-project guard TWICE — `repo / "nowhere"` has no project either — so `_unevaluable`, the producer it is named for, was never exercised by the one test that names it).
- (event) LIVE PARITY RE-PROVEN a third time: `python3 scripts/bmad_drift_check.py` and `factory.gather(Path('.'))` still produce the identical finding set (the same 2 snapshot-tagged `pin-behind` INFO findings, same messages). `pyforge-doctor-test` 794 passed, 1 skipped (791 -> 794). `dashboard-gen` green.
- (note) FOURTH occurrence of the S-13.7 gap, now within a review pass rather than a dev one: the patches above are governed paths, so `spec_surface_check.py` reported drift against the baseline the previous repair pass stamped. Reconciled here the same way — name the changed paths, re-stamp scoped to this spec.
- (change) THIRD FOLLOW-UP REVIEW PASS (2026-08-09). Both hunters converged again, this time on the COST of the previous pass's own fix: narrowing `_read` to raise was right, but it made two pre-existing catches wrong and one isolation boundary too coarse, each reproduced live before patching. (1) `_live_version` resolved the live skill version through `_ground_truth`, which eagerly reads all SIX surface facts — so `chmod 000` on `pixi.toml`, a file only `_env_count` reads, raised `PermissionError` past `check_pins`' `except ValueError` and erased a real `pin-missing` HARD finding, the exact outcome that check's own docstring promises cannot happen. (2) `check_baseline` kept the origin's `except (ValueError, OSError)` around `json.loads(_read(...))`, so an unreadable but byte-for-byte VALID baseline was reported as `baseline-corrupt` HARD — "cannot parse baseline JSON" about a file that was never parsed; "I could not read it" is not "it is corrupt". (3) `_gather`'s isolation boundary is the CHECK, but seven checks ITERATE over files, so one unreadable doc took its readable siblings' real findings with it — three docs each carrying the stale branch-naming rule produced three `stale-rule` findings, and `chmod 000` on the middle one produced ZERO. CHANGED `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py` (`_live_version` reads `_skill_version` directly; `check_pins` catches `(ValueError, OSError)`; `check_baseline` moves `_read` outside the try and catches `ValueError` alone; new `_read_item` helper narrows the blast radius to the one file and names it in the WARN, routed into `check_pins`/`check_counts`/`check_spec_status`/`check_stale_rules`/`check_phase_lists`/`check_dream_vocab`/`check_dream_owners`, with `_doc_pin` taking caller-supplied text so an unreadable doc reaches that helper instead of being read as `""` and slandered) and `src/shared/packages/pyforge-doctor/tests/unit/test_sources_factory.py` (+4 tests, each verified to FAIL against the pre-patch module and pass after).
- (event) LIVE PARITY RE-PROVEN a fourth time: `python3 scripts/bmad_drift_check.py` and `factory.gather(Path('.'))` still produce the identical finding set (the same 2 snapshot-tagged `pin-behind` INFO findings, same subjects, same messages). `pyforge-doctor-test` 798 passed, 1 skipped (794 -> 798). `dashboard-gen` green (its regenerated `docs/dashboard/data.js` reverted — a verification side-effect, not this story's surface).
- (note) FIFTH occurrence of the S-13.7 gap, second within a review pass. Same reconciliation: name the changed paths, re-stamp scoped to this spec.

## Story 6.10 — independence is structural, for every source (2026-08-10)

- (change) EPIC 6 STORY 6.10 SHIPPED (commits `0bd5ff9a8f`, `e4aa675f62`, `2b628bb950`). ADDED `src/shared/packages/pyforge-doctor/tests/meta/test_source_independence.py` — one meta-test, driven by `sources.REGISTRY`, replacing six near-identical AST-walk "must not import the judged station" tests with a single parametrized scan a new `Source` cannot escape (the gap that let `sources/atlas.py`, 6 of 19 `Source` members, carry zero independence coverage). Generalizes `deps.py`'s stricter `bmad_loop` bar (AD-13) to the fleet default per the pre-authored planning spec's own stated preference, making the meta-test a strict superset of all six old files including `deps.py`'s. REMOVED the six subsumed files: `src/shared/packages/pyforge-doctor/tests/unit/test_sources_marshal_independence.py`, `src/shared/packages/pyforge-doctor/tests/unit/test_sources_ledger_independence.py`, `src/shared/packages/pyforge-doctor/tests/unit/test_sources_board_independence.py`, `src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_independence.py` (its correct relative-import-resolving `_resolve_relative` lifted into the new file first, not the buggy skip-variant four of the other five used), `src/shared/packages/pyforge-doctor/tests/unit/test_sources_deps_independence.py`, and `src/shared/packages/pyforge-doctor/tests/unit/test_sources_factory_independence.py` (the undocumented 7th near-identical file, guarding `factory.py`/`BMAD_DRIFT`, that the pre-authored planning spec's "the six" count had missed).
- (change) FOLLOW-UP REVIEW PASS (commit `2b628bb950`). Blind Hunter + Edge Case Hunter converged on four live-verified defects in the new scan itself, each fixed: an absolute `from pyforge import marshal` named the judged station only via `node.names`, never combined with `node.module`, so it evaded detection entirely; the string-constant scan flagged any identifier merely starting with a forbidden dotted name (e.g. `pyforge.marshaling_utils`) until word-boundary matched; `_resolve_relative`'s bounds check let the level exactly one past the package root fall through to a bare, unprefixed name instead of `None` (`>` to `>=`); `SOURCES_DIR.glob` widened to `rglob` so a future `sources/<subpkg>/` file cannot hide from the unmapped-file exhaustiveness check. Six further findings were verified live and rejected as noise or already covered by the existing exhaustiveness tests (full triage recorded in the story spec).
- (event) `pyforge-doctor-test` 798 -> 816 passed, 1 skipped (net +18 across the consolidation: the meta-test's parametrized-per-source cases, exhaustiveness, construction-proof, and synthetic-fire/mutation-proof tests, less the six deleted files' assertions).
- (note) SIXTH occurrence of the S-13.7 gap. Same reconciliation: name the changed paths, re-stamp scoped to this spec.
- (constraint) SURFACE CHANGE 2026-08-10, one path: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py. WHAT CHANGED: its DEFERRED_SPECS dict was re-synced from scripts/chain_completeness_check.py, gaining two entries it was missing (spec-deferred-work-visibility, spec-secure-live-dashboards) and the narrowed reason text for the latter. No logic, no signature, no new check -- data only. WHY IT DRIFTED, which is the durable finding: DEFERRED_SPECS is DUPLICATED across the shim and this source, and only the shim is wired to the pixi task today, so an edit to the shim (PR #392, mine) left this copy silently stale and no gate could see it. Story 6-9 DELETES the shim, at which point this stale copy becomes the live one and chain_completeness_check would have fired on spec-deferred-work-visibility the moment 6-9 landed. A comment above the dict now names the duplication and the edit-both rule. Retiring the duplication belongs to 6-9's landing, not here.
- (change) Story 6.9 (`scripts/` shims retire): `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__main__.py` (NEW) — the target-less `python -m pyforge.doctor.sources <name> [--json] [--groundtruth]` CLI dispatcher every re-pointed pixi task now invokes, one entry per Story 6.4-6.8's ported source. `sources/factory.py` — added a public `ground_truth(target)` wrapping the existing private `_ground_truth`, so the `bmad-groundtruth` pixi task's replacement has a non-private entrypoint to call. `sources/board.py` — synced `DEFERRED_SPECS` with `scripts/chain_completeness_check.py`'s own copy (`spec-secure-live-dashboards`, added there the same day ahead of this story's deletion pass) so the two copies did not disagree during the pre-deletion parity window; the origin script is now deleted so this data lives only here going forward. `tests/unit/test_sources_dispatch.py` (NEW) — covers all ten dispatch entries, the unknown-name usage error, `--groundtruth` scoping, JSON output shape, exit-code mapping. `scripts/ledger_regression_check.py`/`story_status_check.py`/`chain_completeness_check.py`/`dashboard_drift_check.py`/`dream_chain_check.py`/`spec_surface_check.py`/`deferred_work_check.py`/`forward_dependency_check.py`/`bmad_drift_check.py`/`docs/dashboard/check_layout.py` are DELETED — outside this Spec's own surface (they belonged to `spec-regenerable-factory`/`spec-surface-drift-reconciliation`/`spec-bmad-loop-forward-dependency-blindness`/`spec-dream-to-code-model-self-verification`, each reconciled in its own memlog), named here only for cross-reference. Reconciled the same way as every prior S-13.7 occurrence: name the changed paths, re-stamp scoped to this spec.
- (event) Live parity re-confirmed before deletion: `pixi run -e local-recipes <task>` for all 11 re-pointed tasks vs. the origin `python scripts/<name>.py` — identical finding kinds/subjects/messages for all 11 (one real defect found and fixed in the process: `board.py`'s `DEFERRED_SPECS` was missing the `spec-secure-live-dashboards` entry `chain_completeness_check.py` had gained the same day, which without the fix would have produced a false `spec-not-decomposed` FAIL for pyforge-steward). After deletion, `python scripts/detectors.py --scope repo` reports 10 selected (matching the pre-story baseline of 10 repo-scope detectors exactly — 9 retiring + llms_full_check unchanged), and the "unimportable" mutation (both a bare `python3` run with no `pyforge.doctor` installed, and a dedicated pytest regression proven red against the pre-fix code) confirms all ten doctor-threaded rows report `unknown`/exit non-zero, never a silent 0.
- (change) Story 6.9 FOLLOW-UP FIX (2026-08-09, coordinator review): `gather_check_layout` reused `docs/dashboard/check_layout.py`'s own `check()`/`_rows()`/probe constants via a dynamic `exec_module` (`_load_check_layout`) rather than porting them in as permanent code — the one detector in this epic that missed the "port the real logic in" pattern every sibling source module followed, because it was reused by reference instead of duplicated logic. Once this story deletes the origin file, that dynamic load fails EVERY time, forever, in every environment: not an occasional degrade but a permanent, 100% loss of the real verdict, which the review correctly judged as "a verdict that died," not "came home." CHANGED `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py`: removed `_load_check_layout`; ported the five geometry assertions, the chip-grouping helper, the browser-probe JS strings, and the layout grid constants in as permanent module-level code (`_check_layout_geometry`/`_layout_chip_rows`/`_serve_layout_dir`/`_LayoutQuietHandler`/`_LAYOUT_WIDE`/`_LAYOUT_NARROW`/`_LAYOUT_BREAKPOINT`/`_LAYOUT_CENTRE_TOL`/`_LAYOUT_CHIPS`/`_LAYOUT_EDGE_TOL`/`_LAYOUT_PRESSURES`/`_LAYOUT_DESIGN_SIZE`/`_LAYOUT_APPLY`/`_LAYOUT_PROBE`), verbatim in behavior from the origin's own `check()`/`_rows()`/`_serve()`/`_QuietHandler`/module constants, renamed only to fit this module's existing `_check_<subject>` naming convention and avoid colliding with `_LAYOUT_CHECK` (the pre-existing finding-name string). `_run_check_layout` drops its `clm` parameter and reads these names directly, matching how every sibling `_check_*` function in this module already works; `gather_check_layout` no longer gates on loading a file that may not exist. `_load_foreign_module`'s own docstring updated (sole remaining caller is `_load_dashboard_generate`; `gather_dashboard_drift` still dynamically loads `generate.py`, which this story does not touch and which is unaffected). CHANGED `src/shared/packages/pyforge-doctor/tests/unit/test_sources_board_check_layout.py`: removed the dynamic-load-hygiene tests (`test_check_layout_loader_gets_the_same_hygiene_as_its_sibling`, `test_a_check_layout_that_exits_at_import_degrades_to_warn`, `test_real_check_layout_exposes_the_attribute_surface_the_orchestration_uses`) and the missing/broken-file WARN tests, since there is no dynamic load left to test; added `test_still_works_when_check_layout_py_is_absent_from_disk`, a direct regression pin for the exact defect just fixed; the geometry-function tests (`TestReusedGeometryFunctions` -> `TestGeometryFunctions`) lost their `_HAVE_REAL_DASHBOARD` skip guard, since the functions are now unconditionally present; the orchestration stub tests (`_StubLayoutModule`, `_Script`, etc.) moved from passing a `clm` module parameter to `monkeypatch.setattr(board, "_LAYOUT_WIDE", ...)`-style patching of the module's own names.
- (event) Verified live: `python -m pyforge.doctor.sources check-layout` against this repo (`docs/dashboard/check_layout.py` genuinely absent from disk) now reports `console-bar-layout: ok -- console bar edges held, no overlap — 30 measurement(s): 6 width(s) x 5 font-pressure step(s)` — byte-identical to what the origin script itself printed while it still existed, confirmed via all three invocation paths (the dispatcher directly, the `dashboard-layout-check` pixi task, and `scripts/detectors.py`'s own registry). `pyforge-doctor-test` 824 passed, 1 skipped (824 -> the one skip is the genuine no-playwright-installed smoke test; up from 817 passed / 12 skipped, 11 of which were exactly this defect's own test-suite symptom). Reconciled the same way as every prior S-13.7 occurrence: name the changed paths, re-stamp scoped to this spec.
- (constraint) SURFACE CHANGE 2026-08-10, story 6-9's LANDING (not its build): src/shared/packages/pyforge-doctor/tests/meta/test_source_independence.py. Naming it explicitly because spec-surface would have passed silently -- this spec's memlog moved for other reasons in the same commit, so the edit would have ridden along unnamed, which is exactly the laundering the name-then-stamp rule exists to stop. WHAT CHANGED: the two sources/ scans' hardcoded exclusion of __init__.py became a named NON_SOURCE_MODULES frozenset that also carries __main__.py. WHY: 6-9 adds sources/__main__.py, the dispatcher the retired shims now call; story 6.10's exhaustiveness meta-test landed AFTER 6-9 was built and requires every file in sources/ to have a SOURCE_MODULE entry, so the two stories are individually correct and collide only on landing. A dispatcher has no Source to map and imports EVERY station by design, so an independence scan over it is meaningless. The two exempt names are listed exhaustively rather than matched as a dunder pattern, so a third exemption is a deliberate edit and not a silent widening.
- (note) Audit Phase 2 landing (2026-08-10): station chain reconciled — see planning-artifacts/implementation-readiness-report-2026-08-10.md for the full finding set and per-path record; mechanical fixes (rollups/Status-lines/spec-status/promotions/README prose) landed via the audit's Phase 2 PR
- (note) Doctor Epic 7 confirmation landing (2026-08-10): src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py — spec-deferred-work-visibility de-registered from DEFERRED_SPECS (operator-confirmed) + the stale duplicated-dict NOTE fixed (the shim died with 6-9; this dict is the sole copy; de-registration protocol recorded); planning-artifacts/epics.md Epic-7 guard note lifted. Suite 842 green

## Story 7.3 — The detector sees anonymous Tier-3 entries (2026-08-13)

- (change) CHANGED `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py`: `gather_deferred_work`'s `_anonymous()` parser (verbatim, unchanged) now also runs against each project's gitignored Tier-3 file, not just the tracked ledger. Added `DEFERRED_WORK_BASELINE_REL` (`scripts/.deferred-work-baseline.json`, Story 7.2's committed grandfather baseline, read-only here) and `_load_deferred_work_baseline()`, mirroring `_drift_findings`'s own load-and-degrade shape (chain.py:968-1000): loaded ONCE by `_deferred_work_findings` before its per-project loop, degrading to exactly one `no-deferred-work-baseline` FAIL on missing/unreadable/wrong-shape rather than crashing or silently treating every project's count as 0. `_check_project_deferred_work` gained a `baseline: dict[str, int] | None` parameter and now emits one `tier3-entry-unidentified` FAIL per entry in `_anonymous(t3_path)[baseline.get(proj.name, 0):]` — the positional slice Story 7.2's Design Notes already settled (append-only Tier-3 discipline makes "beyond the stamped count" equivalent to "new"); when `baseline is None` the check is skipped for that project rather than guessing count 0. `_deferred_work_message` gained matching branches for both new finding kinds.
- (change) REVIEW PASS (2026-08-13), same day. Blind Hunter + Edge Case Hunter converged on the existence check: `_load_deferred_work_baseline` originally called stdlib `Path.is_file()` directly instead of this module's own `_is_file()`/`_probe()` convention, which exists specifically because stdlib `is_file()` swallows `PermissionError` on an unreadable ancestor directory and lies `False` — the identical failure class already documented and fixed elsewhere in this same function (`_check_project_deferred_work`'s own docstring: an unreadable `implementation-artifacts/` read as "this project defers nothing"). Fixed: routed through `_is_file()`, with any exception it raises caught locally so it still degrades to the same single `no-deferred-work-baseline` finding rather than propagating past `_deferred_work_findings`'s per-project isolation boundary via the outer `degrade_on_exception`. Both reviewers also independently found the shape guard's `isinstance(v, int)` accepts `bool` (a `bool` is an `int` subclass in Python) and negative integers — `{"proj": true}` would slice as `[1:]` and `{"proj": -1}` would slice as `[-1:]` (Python's negative-index semantics, not "everything past position N"), silently mis-slicing instead of being rejected as malformed. Fixed: guard now requires `isinstance(v, int) and not isinstance(v, bool) and v >= 0`. Blind Hunter additionally found the `no-deferred-work-baseline` branch of `_deferred_work_message` used a defensive `item.get("detail", <default>)` fallback that can never fire (every producing branch in `_load_deferred_work_baseline` always sets `detail`), inconsistent with every sibling branch's direct dict indexing — fixed to `item["detail"]`.
- (change) CHANGED `src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_deferred_work.py`: added a `_write_baseline()` fixture helper and 10 new tests covering every I/O matrix row — entries beyond/within the baseline count, a project absent from the baseline (treated as count 0), the CAP-2 mutation pair (a claimed `## DW-1` entry reports nothing; deleting its id heading reds the detector), a missing baseline (one `no-deferred-work-baseline` FAIL, no per-project `tier3-entry-unidentified` findings), a malformed baseline (invalid JSON and the wrong top-level shape, both degrading the same way), confirmation that a missing baseline does not suppress unrelated finding kinds (`tier3-only-deferral` still fires), and two review-pass additions: a baseline count larger than the live entry count (Python's slicing degrades gracefully; now pinned) and a `bool`/negative-valued baseline entry (both rejected by the hardened shape guard above). `_check_project_deferred_work`'s new `baseline` parameter is threaded through the existing per-project-isolation test's `_explode` monkeypatch wrapper; six pre-existing tests that assert an exact finding count/list gained a `_write_baseline(tmp_path, {})` call so the new `no-deferred-work-baseline` finding (fired whenever no baseline file exists in the fixture) does not change their unrelated assertions.
- (note) `scripts/.spec-surface-baseline.json`'s re-stamp for this spec also refreshed the committed hashes for `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py` and `src/shared/packages/pyforge-doctor/README.md` — INCIDENTAL to this story, named here so it does not read as unexplained. Neither file is touched by this story's diff; both had already drifted from their previously-stamped hash in earlier, already-landed commits (`board.py` via `7d3860cc90`/`80a1153e95`, both already described in this memlog's own "Doctor Epic 7 confirmation landing" and "chains: herald Epic 13 + steward Epic 9" history above; `README.md` via `28551a6b65`, the Phase-2 audit) whose own landings did not re-stamp this spec's baseline. `--write-baseline --spec` re-hashes the whole governed surface, not just the files a given story touches, so this run's re-stamp incidentally reconciled that pre-existing, unrelated drift as a side effect — flagged as a review finding, confirmed not caused by this story, and named here per the same "reconcile by naming the changed paths" convention rather than left silent.
- (event) Verified live: `pixi run -e pyforge-doctor python -m pyforge.doctor.sources deferred-work` against this repo reports only the pre-existing `tier3-only-deferral` FAIL for `pyforge-doctor/DW-FU-6-11` — no new `tier3-entry-unidentified` finding for pyforge-doctor (its live anonymous-Tier-3 count equals the baseline-stamped 72) or any other project. `pyforge-doctor-test` 842 -> 852 passed, 1 skipped (net +10, the Story 7.3 coverage incl. the review-pass additions). Reconciled the same way as every prior S-13.7 occurrence: name the changed paths, re-stamp scoped to this spec.

## Story 7.4 — One severity, both sides (2026-08-13)

- (note) INSPECTED `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py` (`_check_project_deferred_work`, lines 1485-1517, per this story's own Code Map): both anonymous-entry finding-building branches (`ledger-entry-unidentified` at 1494-1500, `tier3-entry-unidentified` at 1510-1517) already omit the `"warn"` key, and `_gather_deferred_work`'s single `DoctorStatus.WARN if item.get("warn") else DoctorStatus.FAIL` line is the only place severity is decided for every deferred-work finding kind — no production asymmetry existed, matching the story's own expectation. No change to `chain.py`. Review-pass correction: this same ternary is independently duplicated, unchanged, in `_gather_dream_chain` (chain.py:654) and `_gather_spec_surface` (chain.py:1302) — the claim above scopes to the deferred-work check only, not to `chain.py` as a whole; this story's test protects the deferred-work pair, not those other two gather functions.
- (change) CHANGED `src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_deferred_work.py`: added `test_ledger_and_tier3_anonymous_entries_carry_the_same_severity`, CAP-2's closing-AC proof — plants one anonymous entry on the tracked ledger (`_write_tracked`, the existing `test_anonymous_ledger_entry_reports_fail` fixture shape) and one anonymous entry on the Tier-3 file past the baseline-stamped count (`_write_tier3` + `_write_baseline(tmp_path, {"proj": 0})`, the existing `test_tier3_cap2_mutation_pair_identified_then_anonymous` fixture shape) for the SAME project in one `gather_deferred_work` call, then asserts both resulting findings' `.status` are `DoctorStatus.FAIL` and equal to each other. No new fixture machinery, per this story's own Boundaries.
- (event) `pyforge-doctor-test` 852 -> 853 passed, 1 skipped (net +1, exactly this story's one test). Reconciled the same way as every prior S-13.7 occurrence: name the changed paths, re-stamp scoped to this spec.

## Cross-package reconciliation for marshal Story 14.3 (2026-08-13)

- (change) SURFACE DRIFT RECONCILED, 2026-08-13 (hand-landing of marshal Story 14.3, "one
  lattice, one envelope, one exception root", commit `1bd8c8a2de`, a MARSHAL-owned story, not a
  doctor story — reconciled here at landing time since Doctor's own memlog did not move for it
  when marshal's branch was built, the same S-13.7 timing gap this spec has hit six times before).
  Seven governed paths changed: `src/shared/packages/pyforge-doctor/pixi.toml` and
  `src/shared/packages/pyforge-doctor/pyproject.toml` (both add `pyforge-core` as a real
  run-dependency, the 3-manifest pattern Story 14.2 already proved for other stations);
  `src/shared/packages/pyforge-doctor/src/pyforge/doctor/verdict.py` (adds a module-level
  `LATTICE = Lattice(...)` built from `pyforge.core.verdict.Lattice`, CAP-3 — turns the module's
  own long-standing docstring claim that Doctor's `{0, 2, 130}` exit-code domain is a *subset* of
  Warden's `{0, 1, 2, 130}` into a computed fact via `.exit_codes`, rather than an assertion; the
  pre-existing `exit_code_for` predicate itself is unchanged, not rank-based, per the story's own
  Design Notes); `src/shared/packages/pyforge-doctor/src/pyforge/doctor/__main__.py` (imports
  `pyforge.core.report.{BASE_ENVELOPE_SCHEMA, compose}`, CAP-4 — the `--format json` schema
  validation call site now validates against `compose(BASE_ENVELOPE_SCHEMA, _report_schema())`
  instead of `_report_schema()` alone, so Doctor's own envelope schema is checked as an extension
  of the shared base rather than independently); `tests/meta/test_verdict_narrows_warden.py` (new
  — proves `verdict.LATTICE.exit_codes` is a proper subset of warden's own frozen domain, the
  claim `verdict.py`'s docstring already made in prose); `tests/unit/test_models.py` (changed —
  covers the schema-composition path); `tests/fixtures/sample_report_with_finding.json` (new —
  fixture for the composed-schema test). This Spec claims no capability for any of the seven —
  CAP-3 ("the verdict lattice is declared once") and CAP-4 ("one report envelope") are
  `pyforge-marshal/spec-pyforge-core`'s own contract; this entry reconciles surface/drift
  bookkeeping only. `pyforge-doctor-test` 853 -> 856 passed, 1 skipped post-change (net +3, the two
  new files named above).
- (event) Story 9.1 landed (2026-08-15): src/shared/packages/pyforge-doctor/src/pyforge/doctor/hygiene_definitions.py (new) and src/shared/packages/pyforge-doctor/tests/unit/test_hygiene_definitions.py (new). This Spec claims no capability for either -- they belong to spec-deferred-work-visibility's CAP-8 (Epic 9); this entry reconciles surface/drift bookkeeping only, per this repo's spec-surface convention of naming every changed path in the owning umbrella spec's memlog even when the capability itself is owned elsewhere. pyforge-doctor-test 929 passed, 2 skipped post-change.

## Story 11.1 -- Due-for-verification entries are selected, per project (2026-08-15)

- (change) Story 11.1 landed (commit `d682235b1f`), decomposing
  `spec-deferred-work-resolution-sweep`'s CAP-1, not this Spec -- this entry reconciles
  surface/drift bookkeeping only, same convention as the Story 9.1 entry directly above. Nine
  governed paths changed: `src/pyforge/doctor/models.py` (adds `Source.DUE_FOR_VERIFICATION`,
  the closed taxonomy's first genuinely NEW, non-ported member); `src/pyforge/doctor/sources/
  chain.py` (adds `DUE_FOR_VERIFICATION_STALENESS_DAYS = 30`, `_VERIFIED_RE`, `_verification()`,
  `_due_for_verification_findings()`, `_due_for_verification_message()`, and
  `gather_due_for_verification()` -- mirrors `gather_deferred_work`'s per-project-isolation
  shape end to end; always WARN, never FAIL); `src/pyforge/doctor/sources/__init__.py` (one new
  `REGISTRY` row, `subject_station="marshal"`/`owning_station="doctor"`, same rationale as
  `DEFERRED_WORK`'s row); `src/pyforge/doctor/sources/__main__.py` (wires
  `due-for-verification` into `DISPATCH`, the first non-ported member there too; its module
  docstring's opening sentence drops "the ten ported Doctor sources" for "Doctor's ported
  sources", but a new second sentence still pins a live count -- "Eleven total today" -- so the
  count is relocated, not eliminated); `src/pyforge/doctor/data/
  report-schema.json` (adds `"due-for-verification"` to the `check` source enum);
  `tests/unit/test_sources_chain_due_for_verification.py` (new -- 22 tests, one per I/O matrix
  row plus a review-pass regression for the multi-`verified:`-line bug below);
  `tests/meta/test_source_independence.py`, `tests/unit/test_models.py`,
  `tests/unit/test_sources_dispatch.py` (all three changed -- the same closed-taxonomy
  invariant update every prior `Source` addition, 6.4-6.8 and the marshal 14.3 cross-package
  entry above, has required). Also changed in the same commit but governed elsewhere: `pixi.toml`
  (`pyforge-steward/spec-python-agent-platform`'s own surface -- reconciled in that spec's
  memlog, cross-referencing this entry, same pattern as the marshal Story 14.3 entry above).
  Review (Blind Hunter + Edge Case Hunter) caught a high-severity bug pre-landing: `_verification()`
  matched only the FIRST `verified:` line via `.search()`, pinning staleness to an entry's OLDEST
  re-check forever since reconciliation appends rather than replaces; fixed to the LAST match via
  `finditer()`, with a regression test. `pyforge-doctor-test` 933 -> 955 passed, 2 skipped
  post-change (net +22, the new file's own count).
- (event) Verified live: `pixi run -e local-recipes due-for-verification-check` and
  `pixi run --frozen -e pyforge-doctor pyforge-doctor-test` both green. Reconciled the same way
  as every prior S-13.7 occurrence: name the changed paths, re-stamp scoped to this spec.
- (change) REPAIR-PASS REVIEW (2026-08-15), same day, same repair pass as the entry above. Blind
  Hunter and Edge Case Hunter independently caught that this entry's own draft mischaracterized
  `sources/__main__.py`'s docstring fix as "correction from 'ten' to no-longer-pinning-a-number"
  -- corrected above in place (a relocated count, not an eliminated one). Blind Hunter separately
  caught that `pixi.toml`'s new `due-for-verification-check` task cited a nonexistent contract,
  `Contract: spec-11-1-due-for-verification-entries-are-selected-per-project` -- unlike every
  other `Contract:` line in `pixi.toml`, which names something that resolves on disk today, this
  one named the story's own Tier-3 `implementation-artifacts/` spec, not yet promoted into a
  tracked `planning-artifacts/specs/` directory per this repo's own promotion convention (CLAUDE.md
  § Spec-driven layout). Fixed to `Contract: spec-deferred-work-resolution-sweep CAP-1`, the real
  owning capability spec, matching the `spec-regenerable-factory CAP-3` precedent's naming shape.
  One reviewer finding (a claimed dangling `DW-FU-11-1` deferred-work id) was verified FALSE on
  direct inspection: the id exists, fully written, in this project's Tier-3
  `implementation-artifacts/deferred-work.md` -- the reviewer's search evidently missed it (most
  likely a non-dereferencing `grep -r` against the `implementation-artifacts` symlink, or scoped
  to the tracked ledger only); the previous session's landing was complete on this point. Six
  further findings (unverifiable cross-memlog references, subjective precedent-language framing,
  an already-independently-verified pixi.toml content claim, a misread "nine" undercount that
  actually matches this entry's own scoping, an unitemized test count, and a true-but-non-actionable
  observation that the mechanical gate does not vouch for prose accuracy) were rejected as noise.
  `pyforge-doctor-test` still 955 passed, 2 skipped after these prose-only fixes (no production
  code touched).

## Story 11.2 -- Churn-based cost filtering skips entries whose code has not moved (2026-08-15)

- (change) Story 11.2 landed (commit `34fcec5c6d`), decomposing `spec-deferred-work-resolution-
  sweep`'s CAP-2, not this Spec -- this entry reconciles surface/drift bookkeeping only, same
  convention as the Story 11.1 entry directly above. Two governed paths changed: `src/pyforge/
  doctor/sources/chain.py` (adds `_PATH_TOKEN_RE`, `_SOURCE_SPEC_LINE_RE`, `_entry_named_paths()`,
  `_authored_date()`, `_churn_since()`, `_attach_churn_skip()`; extends
  `_check_project_due_for_verification()` and `_due_for_verification_message()` to compute and
  surface a `skip_reason: "no-churn"` decision on the SAME Story 11.1 finding -- no new
  `Source`/REGISTRY/DISPATCH/schema entry, reuses `Source.DUE_FOR_VERIFICATION` entirely);
  `tests/unit/test_sources_chain_due_for_verification.py` (extended -- 11 new tests, one per I/O
  matrix row incl. an id-prefix-collision case for `_authored_date` and two message-text cases,
  real `git init`-ed fixtures with `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE`-pinned commits). Also
  changed in the same commit but governed elsewhere: `pixi.toml` (`pyforge-steward/spec-python-
  agent-platform`'s own surface -- reconciled in that spec's memlog, cross-referencing this entry,
  same pattern as the Story 11.1 entry above). `pyforge-doctor-test` 955 -> 966 passed, 2 skipped
  post-change (net +11, the new test cases' own count).
- (event) Verified live: `pixi run -e local-recipes due-for-verification-check` (rc=0, two real
  fleet entries show a live `skip_reason: no-churn` decision) and `pixi run --frozen -e
  pyforge-doctor pyforge-doctor-test` (966 passed, 2 skipped) both green. Reconciled the same way
  as every prior S-13.7 occurrence: name the changed paths, re-stamp scoped to this spec.

## Story 11.3 -- Mechanically-checkable claims are verified without an agent (2026-08-15)

- (change) Story 11.3 landed, decomposing `spec-deferred-work-resolution-sweep`'s CAP-3, not this
  Spec -- this entry reconciles surface/drift bookkeeping only, same convention as the Story
  11.1/11.2 entries directly above. Four governed paths changed: `src/pyforge/doctor/cli_bridge.py`
  (`run_git` gains `ok_exit_codes: frozenset[int] = frozenset({0})`, so a `git grep` call can
  accept exit code 1 -- "no matches," not an error -- without changing any other call site's
  default behavior); `src/pyforge/doctor/sources/chain.py` (adds `_UNUSED_CLAIM_RE`,
  `_entry_unused_symbol_claims()`, `_call_site_count()`, `_attach_mechanical_verdict()`; extends
  `_check_project_due_for_verification()` and `_due_for_verification_message()` to compute and
  surface a `mechanical_verdict: "still-open"`/`"escalate"` decision on the SAME Story 11.1/11.2
  finding for a due, non-churn-skipped entry claiming a bare identifier is unused -- no new
  `Source`/REGISTRY/DISPATCH/schema entry, reuses `Source.DUE_FOR_VERIFICATION` entirely);
  `tests/unit/test_sources_chain_due_for_verification.py` (extended -- 15 tests in the Story 11.3
  section, real `git init`-ed fixtures with COMMITTED ledgers, matching real production shape);
  `tests/unit/test_cli_bridge.py` (changed -- docstring only: Story 9.2's own `_isolate_git_env`/
  `_init_repo`/`ok_exit_codes` test trio, landed first via baseline-drift recovery PR #557, already
  covers the shared `run_git` primitive this story also exercises; 11.3's own duplicate pair of the
  identical primitive-level tests, and a second, differently-named fixture set, was dropped as
  redundant coverage during this cherry-pick, not landed -- the module docstring now credits both
  stories). Also changed in the same commit but governed elsewhere: `pixi.toml` (`pyforge-steward/
  spec-python-agent-platform`'s own surface -- reconciled in that spec's memlog, cross-referencing
  this entry, same pattern as the Story 11.1/11.2 entries above). Review (Blind Hunter + Edge Case
  Hunter, independently, same finding) caught a HIGH-severity bug pre-landing: `_call_site_count`
  grepped the whole repo with no exclusion of the tracked ledger itself, so an entry's own claim
  text (backtick-citing the very symbol under claim) was counted as a live call site against
  itself, making `mechanical_verdict: "still-open"` essentially unreachable for any real,
  committed claim -- fixed by excluding every `**/deferred-work-ledger.md` via a git pathspec.
  Three more correctness gaps (async def declarations not recognized; a variable/constant's
  assignment line never excluded from the count, systematically forcing `escalate`; `git grep`
  without `--untracked` missing a symbol wired up in an uncommitted file, risking a false
  `still-open` confirm) were patched in the same pass -- full triage in the story's own spec.
  `pyforge-doctor-test` 966 -> 981 passed, 2 skipped post-change (net +15).
- (event) Verified live: `pixi run -e local-recipes due-for-verification-check` (rc=0; a real
  fleet entry, `pyforge-atlas/DW-13-2-1`, shows a live `mechanical_verdict: escalate` decision) and
  `pixi run --frozen -e pyforge-doctor pyforge-doctor-test` (981 passed, 2 skipped) both green.
  Reconciled the same way as every prior S-13.7 occurrence: name the changed paths, re-stamp
  scoped to this spec.
- (change) RECONCILED BY NAME, 2026-08-20: `src/shared/packages/pyforge-doctor/tests/unit/test_checks_env_hygiene.py` changed by an unrelated station's work — pyforge-mason's Story 5.5 Rule-2 CFE retrospective (spec `spec-5-5-rule-2-conda-forge-expert-retrospective`, CFE v8.82.x). Not incidental drift, and not a Doctor decision that could have been taken here: Story 1.4's golden fixture was the live, unconditional `JFROG_API_KEY` injection in `.claude/skills/conda-forge-expert/scripts/_http.py` — the FR-3 detector's one non-synthetic positive, deliberately read from real code rather than copied into a string. That retro host-gated the injection (and its copy in `inventory_channel.py`), which is exactly what FR-3 exists to prompt, and in doing so removed the fixture: `test_gather_golden_fixture_finds_the_real_jfrog_api_key_injection` went red. Verified by A/B rather than inferred — `env_hygiene.gather` over the pre-retro revision of those scripts still returns both findings (`_http.py:215`, `inventory_channel.py:116`); over the post-retro tree it returns none. The test is inverted rather than deleted or skipped: it now asserts the real CFE scripts contain NO unconditional-credential-injection finding, keeping the property Story 1.4's spec actually cared about (the detector runs against real, unmodified code, read-only, never a synthetic string) and converting it into a live regression guard that the leak class stays closed. The detector-finds-such-an-injection property is unaffected — it is covered by the file's synthetic positive cases, which do not depend on another package shipping a real bug. No `env_hygiene` source change, no Doctor story moved, no FR re-scoped; `pyforge-doctor-test` unit suite green (44 passed in this module). Standing lesson worth carrying: a golden fixture that points at another component's live defect has a built-in expiry — it goes red precisely when the thing it documents gets fixed, and the station that fixes it is the one who discovers that. Baseline re-stamped scoped to this spec only.
- (change) RECONCILED BY NAME, 2026-08-20 (Story 5.5 FOURTH REVIEW PASS, mason's Rule-2 retro): the inverted golden-fixture test the third pass landed here was asserting an absence it could not distinguish from having scanned nothing. `gather()` over the real CFE scripts dir returns `[]`, and `gather()` over an EMPTY directory also returns `[]` — so if `_HTTP_PY_DIR` ever resolved to a wrong-but-existing directory, or the walker regressed to matching no files, the guard would pass green while proving nothing (reproduced by the reviewer against an empty dir). The `is_dir()` skip covers an absent directory but not an empty or mismatched one. The test now pins its own precondition first — the directory must actually contain `_http.py` and `inventory_channel.py` — and additionally asserts no `SCAN_INCOMPLETE_CHECK_NAME` finding, so a scan that bailed out cannot read as "no leak found" either. No `env_hygiene` source change, no Doctor story moved, no FR re-scoped; full `pyforge-doctor` suite 1031 passed / 2 skipped / 0 failed. Standing lesson, sibling to the one recorded in the entry above: **an absence assertion needs a positive precondition**, or it silently degrades from "we checked and it is clean" to "we checked nothing". Baseline re-stamped scoped to this spec only. File touched by this entry, named literally one per line because the drift-presumed test is a plain substring match:
  - `src/shared/packages/pyforge-doctor/tests/unit/test_checks_env_hygiene.py`

## Story 10.2 -- The installed core is compared against the latest upstream release (2026-08-20)

- (change) Story 10.2 landed (commit `3749c93afa`), decomposing `spec-bmad-method-version-drift`'s
  CAP-2, not this Spec -- this entry reconciles surface/drift bookkeeping only, same convention as
  the Story 11.1/11.2/11.3 entries above. `sources/bmad_method.py`'s existing CAP-1 `_gather`
  (Story 10.1) is extended with `_fetch_latest_upstream_version()` (stdlib `urllib.request`
  against npm's public registry, mirroring `pyforge-mason`'s `pypi_index.version_exists`) and a
  second, independent Finding comparing installed vs. latest-upstream, fail-open and silent on any
  fetch failure -- reuses `Source.BMAD_METHOD_VERSION_DRIFT` entirely, no new `Source` member, no
  schema change. Four governed paths changed: `src/pyforge/doctor/sources/bmad_method.py` (the
  fetch helper + `_gather` extension + docstring), `src/pyforge/doctor/sources/__init__.py`
  (comment-only touch-up on the `BMAD_METHOD_VERSION_DRIFT` registry entry), `src/pyforge/doctor/
  models.py` (the `Source.BMAD_METHOD_VERSION_DRIFT` per-member docstring, now naming both CAP-1
  and CAP-2), `tests/unit/test_sources_bmad_method.py` (net +17 tests: an autouse network-stub
  fixture keeping every CAP-1 test network-free unmodified, the CAP-2 I/O-matrix coverage, and
  direct `_fetch_latest_upstream_version` unit tests for every failure mode). Also changed in the
  same commit but governed elsewhere: `pixi.toml` (`pyforge-steward/spec-python-agent-platform`'s
  own surface -- reconciled in that spec's memlog, cross-referencing this entry, same pattern as
  the Story 11.1/11.2/11.3 entries above).
- (event) Review pass (Blind Hunter + Edge Case Hunter, independently) caught one HIGH finding
  pre-landing: `_fetch_latest_upstream_version`'s except clause missed `http.client.HTTPException`
  (a malformed/truncated HTTP response), which would have escaped the helper and let the outer
  `degrade_on_exception` discard CAP-1's own already-computed Finding too -- fixed by adding it to
  the except tuple. Five low findings (doc/comment accuracy, an end-to-end gather->fetch test gap,
  an unrealistic `HTTPError` test fixture) were also patched; one medium (`DW-FU-10-2`,
  `score.grade()` axis-sharing dilution once this Source is wired into that composite grader, not
  live today) deferred; three rejected as noise. Full triage in the story spec's own Review Triage
  Log.
- (event) Verified live: `pixi run --frozen -e pyforge-doctor pyforge-doctor-test` (1031 -> 1048
  passed, 2 skipped, net +17 matching the new test count) green. Reconciled the same way as every
  prior S-13.7 occurrence: name the changed paths, re-stamp scoped to this spec.

## Story 10.3 -- The drift surfaces ambiently, never gates (2026-08-20)

- (change) Story 10.3 landed (commit `1882877b91`), decomposing `spec-bmad-method-version-drift`'s
  CAP-3, not this Spec -- this entry reconciles surface/drift bookkeeping only, same convention as
  the Story 9.1/11.1/11.2/11.3/10.2 entries above. Wires the existing `bmad_method.gather()`
  (Story 10.1/10.2's CAP-1/CAP-2 Findings) into two consumer surfaces without touching
  `bmad_method.py`'s own gather logic. Five governed paths changed: `src/pyforge/doctor/
  __main__.py` (new opt-in `--bmad-core` argparse flag + help text naming the NFR-4/opt-in
  rationale, `_CATEGORY_SOURCE["bmad-core"] = Source.BMAD_METHOD_VERSION_DRIFT`,
  `_gather_bmad_core(target)` mirroring `_gather_durability`'s shape, and `_run_check` extended so
  `run_bmad_core` is computed OUTSIDE the "no flags -> all on" branch -- never part of the default
  trio, since CAP-2's live npm-registry fetch would put NFR-4's 5-second pre-flight budget at
  non-deterministic risk); `src/pyforge/doctor/models.py` (one-line `BMAD_METHOD_VERSION_DRIFT`
  docstring touch-up naming where the Source now surfaces); `src/pyforge/doctor/sources/
  __init__.py` (touch-up on the `BMAD_METHOD_VERSION_DRIFT` REGISTRY comment, "Story 10.3's job"
  now dispatched); `src/pyforge/doctor/sources/bmad_method.py` (touch-up on the
  `_UPSTREAM_FETCH_TIMEOUT_SECONDS` comment, "once Story 10.3 wires it in" now true -- no change to
  `gather`/`_gather`/`_fetch_latest_upstream_version` themselves); `tests/unit/test_cli_check.py`
  (new `--bmad-core` test section mirroring `--durability`'s: findings reach both text/`--json`
  renders, the default zero-flag run excludes it, a WARN never drives the exit code, and the
  `--scope` usage-error path). Also changed in the same commit but governed elsewhere:
  `scripts/fleet_picture.py` (new `bmad_core_drift_findings()` probe wired into `main()`'s
  ATTENTION `watch` block -- allowlisted in `scripts/spec_surface_allowlist.txt` as operator
  tooling, not governed by any spec's `surface:`; a follow-up review-pass correction of this
  entry's own first draft, which had wrongly attributed it to `spec-regenerable-factory`, a spec
  whose baseline entry tracks four unrelated detector scripts and does not mention
  `fleet_picture.py` at all) and `.claude/skills/conda-forge-expert/tests/meta/
  test_fleet_picture_bmad_core_drift.py` (new -- covered by the allowlist's blanket `.claude/**`
  entry) -- neither is part of this Spec's governed `src/shared/packages/pyforge-doctor/**` glob,
  so neither is named as drift here.
- (event) Review pass (Blind Hunter + Edge Case Hunter) caught one medium + one low pre-landing on
  the `fleet_picture.py` side (message newline-stripping/length-cap parity with
  `escalation_reason`'s own precedent, and a `--help` text gap on `--bmad-core`'s `scope="repo"`
  classification not actually meaning network-free for this one category); three low findings
  deferred (`DW-FU-10-3`, `DW-FU-10-3-2`, `DW-FU-10-3-3`); four rejected as noise or re-litigating
  already-justified Boundaries decisions. Full triage in the story spec's own Review Triage Log.
  None of the patched findings touch this Spec's own governed surface (`__main__.py`/`models.py`/
  `sources/__init__.py`/`sources/bmad_method.py`/`test_cli_check.py` were unaffected by the
  fleet_picture.py-side fixes).
- (event) Verified live: `pixi run -e pyforge-doctor pytest src/shared/packages/pyforge-doctor/
  tests/unit/test_cli_check.py src/shared/packages/pyforge-doctor/tests/unit/
  test_check_speed_budget.py -v` (42 passed -- note: run under `-e pyforge-doctor`, not the
  `-e local-recipes` the story spec's own Verification section names, since `test_cli_check.py`
  imports `pyforge.warden` directly and that package is only a run-dep of the dedicated
  `pyforge-doctor` env, per `[feature.pyforge-doctor.dependencies]`'s AD-1 `pyforge-warden` path
  edge -- `local-recipes` never carried it; confirmed pre-existing, not a Story 10.3 regression,
  the import predates this story). `pixi run --frozen -e pyforge-doctor pyforge-doctor-test` 1048
  -> 1054 passed, 2 skipped (net +6, the new `--bmad-core` test section). `pixi run -e
  local-recipes pytest .claude/skills/conda-forge-expert/tests/meta/
  test_fleet_picture_bmad_core_drift.py -v` (5 passed). Reconciled the same way as every prior
  S-13.7 occurrence: name the changed paths, re-stamp scoped to this spec.
- (change) FOLLOW-UP REVIEW PASS (2026-08-20), same day, a repair pass on top of the entry above
  (the previous session's landing had left `spec_surface_reconcile.py` red -- fixed by the entry
  above -- and a fresh Blind Hunter + Edge Case Hunter pass ran over the full story diff as part of
  that repair). Two low findings patched inside this Spec's own governed surface:
  `src/pyforge/doctor/__main__.py`'s `--bmad-core` `--help` text was trimmed to drop internal
  citations ("NFR-4", "review finding", "sources/__init__.py's own REGISTRY comment") that added no
  value for an end user reading `--help`, while keeping the substantive warning intact (the live
  npm-registry call, and that `--scope repo` does not mean network-free for this one category); no
  test asserts on the removed substrings, confirmed by grep before the edit. This entry's own first
  draft (directly above) also mis-cited `scripts/fleet_picture.py` as governed by
  `spec-regenerable-factory` -- corrected in place above to "allowlisted, not governed by any
  spec" after checking `scripts/spec_surface_allowlist.txt` directly (line 37) and confirming
  `spec-regenerable-factory`'s own baseline entry tracks four unrelated detector scripts, not this
  one. Two further low findings patched OUTSIDE this Spec's governed surface (recorded here for
  completeness, not because they move this Spec's own contract): `scripts/fleet_picture.py` gained
  a one-line comment on `bmad_core_drift_findings`'s `timeout=15` explaining the margin over the
  inner `_UPSTREAM_FETCH_TIMEOUT_SECONDS = 5.0` bound; `.claude/skills/conda-forge-expert/tests/
  meta/test_fleet_picture_bmad_core_drift.py` gained one new test,
  `test_both_cap1_and_cap2_warn_simultaneously`, covering the two-finding-at-once case the file's
  own existing tests had missed -- confirmed live via `pixi run -e local-recipes fleet-picture`
  that this is the REAL current state of this repo (both `bmad-method-version-drift` and
  `bmad-method-upstream-drift` warn today), not a hypothetical.
- (event) Six further findings across both reviewers were rejected, each verified before rejecting
  rather than taken on faith: the `--scope repo` network-I/O tension and the missing end-to-end
  `main()` ATTENTION-block test are the SAME two items already deferred in the prior pass as
  `DW-FU-10-3-2`/`DW-FU-10-3-3` (re-deferring would duplicate the ledger); a claim that no test
  proves the real subprocess call resolves was settled by live evidence -- `pixi run -e
  local-recipes fleet-picture` (rc=0) and `pixi run -e local-recipes python -m
  pyforge.doctor.__main__ check --bmad-core` (rc=0) both against this repo's real, un-mocked
  current drift, printing correctly labeled `bmad-method-version-drift`/`bmad-method-upstream-drift`
  lines in both surfaces; two findings proposing extra defensive code for a `None`-valued
  `Finding.message`/malformed non-list JSON shape were rejected because `pyforge.doctor.models.
  Finding.message: str` already makes the first case structurally unreachable from this source, and
  both cases are already caught by the SAME whole-probe `try/except Exception` idiom `main()` uses
  for every ATTENTION probe in this file, degrading to the one generic "could not check" line the
  story's own I/O matrix already specifies for "any failure mode" -- not a gap, the documented
  contract working as designed; a claim that the new meta-test file's placement under
  `.claude/skills/conda-forge-expert/` triggers CLAUDE.md's CFE-skill-invocation rule was rejected
  as a misapplication -- that directory already holds `test_fleet_picture_loop_home_staleness.py`,
  an established, unrelated precedent for exactly this kind of cross-station meta test having
  nothing to do with conda-forge recipe work.
- (event) Verified live after these patches: `pixi run -e local-recipes pytest .claude/skills/
  conda-forge-expert/tests/meta/test_fleet_picture_bmad_core_drift.py -v` (6 passed, net +1);
  `pixi run -e pyforge-doctor pytest src/shared/packages/pyforge-doctor/tests/unit/test_cli_check.py
  src/shared/packages/pyforge-doctor/tests/unit/test_check_speed_budget.py -q` (42 passed,
  unchanged); `pixi run --frozen -e pyforge-doctor pyforge-doctor-test` (1054 passed, 2 skipped,
  unchanged -- neither patch touched a file this suite collects). `python
  scripts/spec_surface_reconcile.py` exits 0. Reconciled the same way as every prior S-13.7
  occurrence: name the changed paths, re-stamp scoped to this spec.
- (event) Surface reconcile 2026-08-21, two parts. OURS (BMAD 6.11 branch): checks/env_hygiene.py prunes 'worktrees' + 'SDKs' — 18 stale harness worktrees (122,731 entries, 2.4x the 50k cap) plus the vendored macOS SDK truncated the walk before src/, exactly the Story-6.1 canary's predicted regression; also cured the 5s speed-budget miss. FOREIGN, documented not laundered: 15 sibling files (backlog_intake.py + tests, board/chain/models/__main__/report-schema, factory.py's 12.x deltas) drifted across doctor Epics 12/13 landings (PRs #601-#605 era) whose per-story specs moved but this umbrella spec's memlog did not — those landings are the record; this entry closes the umbrella gap
- (event) Story 14.1 (Epic 14/CAP-4, owning capability spec: spec-bmad-method-version-drift) landed on the governed package surface: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/bmad_method.py (suite pass: derived bmad-* watched set, conda-meta filename versions, generalized `package`-kwarg npm fetch seam, 5.0s shared suite deadline, entirely-fail-open `_gather_suite_findings`, check="bmad-suite-upstream-drift" appended to both `_gather` returns), src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py (BMAD_METHOD_VERSION_DRIFT keep-in-sync member comment extended -- comment-only), src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py (REGISTRY entry comment extended -- comment-only, no field/structure change), src/shared/packages/pyforge-doctor/tests/unit/test_sources_bmad_method.py (CAP-4 banner section, 61 tests pass). No new Source member, no report-schema/registry-structure change, no __main__.py/fleet_picture.py/check.py wiring change; full doctor suite 1163 passed / 1 skipped, meta tests (independence, sole-subprocess, taxonomy, schema) green unmodified.
- (event) Story 14.1 review-pass patches on the governed package surface: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/bmad_method.py (URL percent-encoding, suite-side lenient upstream parse, epoch tolerance, `packages_watched` evidence, two comment corrections) + src/shared/packages/pyforge-doctor/tests/unit/test_sources_bmad_method.py (mixed-outcome gather test, strengthened offline equality, conda-meta noise test, new direct tests). Full doctor suite 1166 passed / 1 skipped.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (change) Story 25.6 / CAP-6 (marshal cross-station): spec-frontmatter deferred intake in `chain.py` — `parse_spec_frontmatter_deferrals`, `discover_spec_frontmatter_deferrals`, `frontmatter_deferral_in_tracked`, `format_frontmatter_intake_entry`, `spec-frontmatter-only-deferral` finding; `_check_project_deferred_work` always scans specs (Tier-3 gated separately). RECONCILES: `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py`, `src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_deferred_work.py`.
- (change) Steward 30.2 / FR-7: Guildhall generator retired. RECONCILES `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py` (dashboard-drift / check-layout are reintroduction gates; chain-layers loads `scripts/fleet_scan.py`), `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py` (DASHBOARD_DRIFT scope=repo), `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__main__.py`, `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py`, `src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py`, `src/shared/packages/pyforge-doctor/tests/unit/test_sources_board_dashboard_drift.py`, `src/shared/packages/pyforge-doctor/tests/unit/test_sources_board_check_layout.py`, `src/shared/packages/pyforge-doctor/tests/unit/test_sources_board_chain_layers_audit.py`.
- (change) Doctor 8.5/8.6 (spec-deferred-work-visibility, PRs #906/#907): deferred-work-check content-comparison fix + fleet-wide promotion unblock. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_deferred_work.py
- (change) Doctor 20.11 (PR #904): closed story-status's own landing-evidence adoption gap (all 25 flagged findings confirmed false positives; fixed the detector's landing-evidence grammar). RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/marshal.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_marshal_story_status.py
- (change) PR #903: classify marshal dispatch-run records in bmad-drift-check (15 findings -> 0). RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py
- (change) Umbrella catch-up for three already-landed feature stories whose own per-story specs moved but this umbrella never named their test paths: fleet-pulse display on /stations/doctor/ (780d99096f), SKF skill + bmad-agent-doctor Wave A (f8cf4866b8), 18.1 review CAP-15 consult guard (0bef47407a). RECONCILES: src/shared/packages/pyforge-doctor/tests/meta/test_portal_fleet_pulse.py, src/shared/packages/pyforge-doctor/tests/meta/test_skf_domain_skill.py, src/shared/packages/pyforge-doctor/tests/meta/test_station_persona.py
- (change) chain-completeness's `DEFERRED_SPECS` dict gained two entries (spec-chain-currency-sweep, spec-build-league-scorecard) after operator sign-off on a fleet-wide chain-completeness triage. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py
- (change) Two real detector bug fixes, both against pre-existing FR-14/FR-15 detector infrastructure, no new capability: `factory.py`'s `_PIN_RE` couldn't parse a compound `source_pin` (BMAD-core + skill version), fixed with a two-phase parse that can't backtrack onto the wrong version number; `chain.py`'s `_tier3_entry_already_promoted` couldn't recognize an `IDENTIFIED_PLAIN` entry's truncated heading as already-promoted content under a fresh fingerprint, fixed with a length-gated prefix match. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_deferred_work.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_factory.py
- (change) 2026-08-29 fleet-hygiene pass (PR #924), two real bug fixes: `env_hygiene.py`'s discovery-walk pruning added `.worktrees` as a distinct basename from the already-pruned `worktrees` -- 18 stale hidden harness worktrees were bypassing the prune and blowing the 50k-entry walk cap before reaching `src/`; `test_skf_domain_skill.py` swapped a raw `"<!-- SKF:BEGIN" not in text` string check (which false-fails on any legitimate SKF-managed skill file) for the real `skf-rebuild-managed-sections.py check` invocation, asserting `markers_valid` when `has_managed_section`. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/checks/env_hygiene.py, src/shared/packages/pyforge-doctor/tests/meta/test_skf_domain_skill.py
- (change by claude) 2026-08-30 (PR #936): `factory.py::classify()`'s `uncovered` coverage check had rules for `implementation-artifacts/runs/*` and `dispatch-runs/*` but not the third `fleet-drain-runs/*` shape (`marshal factory drain` campaign records, `fleet-drain-supervisor.log`/`journal.jsonl`) -- own earlier-session drain campaigns were HARD-failing `bmad-drift`'s coverage check. Added the missing `re.fullmatch` rule (mirrors `dispatch-runs/` exactly, same `local:run-journal` classification) + a regression test (`test_fleet_drain_run_records_are_classified_and_not_flagged_uncovered`). RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_factory.py
- (change by claude) 2026-08-30: `chain.py::_call_site_count`'s mechanical-verdict git grep (`--untracked --no-exclude-standard`, needed to catch a symbol used only from an untracked file under a gitignored dir) un-ignores `.gitignore` ENTIRELY with no further exclusion -- against this repo's live `.pixi/` (measured 32GB), a single call did not return within 4 minutes, and `scripts/fleet_picture.py`'s `due-for-verification` subprocess (90s budget) hard-timed-out on every real invocation, degrading to "could not check verification staleness" instead of the per-station breakdown. Fixed by excluding `env_hygiene.py`'s own `_PRUNED_DIR_NAMES` (the SAME curated "never first-party source" list the discovery-walk pruning already trusts, not a second independently-maintained list) via additional git pathspecs -- re-measured the worst-case call at 7s (was 240s+/timeout). Also bumped `fleet_picture.py`'s `verification_staleness_findings` timeout 90s -> 180s to restore its own documented ~2x-margin design intent over the new, now-BOUNDED but fleet-grown (~600 tracked entries vs. the original 48s baseline's ~400) measured cost (~86s), rather than leaving a razor-thin margin. Regression test: `test_untracked_reference_inside_a_pruned_dir_does_not_force_escalate`. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_due_for_verification.py
- (event, 2026-08-30, atlas workbook-retirement course correction) `pyforge/doctor/sources/factory.py` classifier gains one rule: `planning-artifacts/fleet-drain-queue.yaml` → `tracked:marshal-queue` (marshal's optional fleet-drain order-override / skip-policy file, `pyforge.marshal.core.dispatch_fleet.QUEUE_CONFIG_FILENAME`, first instance minted for pyforge-atlas by `_bmad-output/projects/pyforge-atlas/planning-artifacts/change-history/sprint-change-proposal-2026-08-30.md`). Without it `bmad-drift` reported `uncovered: fail` for the new file. Mirrored into the mutation-only residual `scripts/bmad_drift_check.py`. Hand-authored, not pin-gated — same reasoning as the `marshal-policy.toml` rule directly above it. Doctor suite `-k "factory or classify or drift"`: 103 passed. `scripts/.spec-surface-baseline.json` re-stamped scoped to this spec.
- (change by claude) Surface reconcile 2026-09-01 (pre-drain hygiene, SM-C1/NFR-4): `checks/env_hygiene.py` regained sub-5.0s headroom for the default `doctor check` path without re-thresholding — extended Story 6.1's prune list with gitignored `output/` (736MB rattler-build tree), `archive/`, and `_skf-learn/`; added a whole-file prefilter before `ast.parse` (skip files lacking both a header-shaped target substring and an environ/getenv read). `test_doctor_check_completes_within_the_five_second_budget` green again. RECONCILES: `checks/env_hygiene.py`. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pyforge-doctor`.
- (change) Story 19.1 (Epic 19): manifest-driven suite watched set (`recipes/bmad-suite/suite-members.yaml` union pixi pins) + registry-aware `_resolve_upstream_latest` (github/npm/pypi authoritative; unknown → max of all resolved triples); suite budget 15s; `fleet_picture.py` timeout 30s. Live check now WARNs builder/CIS/TEA (not npm-stale blind). RECONCILES: `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/bmad_method.py`, `models.py`, `sources/__init__.py`, `tests/unit/test_sources_bmad_method.py`, `scripts/fleet_picture.py`.
- (change) 2026-09-04 (PR #1043): new unit test `tests/unit/test_fleet_scan_currency_feeds.py` pins the retired `epics→sprint` feeds edge in `scripts/fleet_scan.py` (marshal spec-factory-console) and that the contract edges still fire past the grace window; loads the real script the way `sources/board.py` does. No detector logic in this package changed. RECONCILES: `src/shared/packages/pyforge-doctor/tests/unit/test_fleet_scan_currency_feeds.py`. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pyforge-doctor`.
- (event) Surface reconcile 2026-09-04 (PR #1043, ninth pass — station meta guards): `tests/meta/test_portal_fleet_pulse.py::test_story_does_not_add_pyforge_under_src_platform` flags only pyforge / raw-HTTP imports the branch ADDED to a changed `src/platform` file (hits minus the file's hits at `origin/main`), not every import the file already carried — a Ruff fix on `config/station_port.py` had tripped it. The guard's intent ('does not add') is unchanged. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pyforge-doctor`.
- (change by claude) Surface touch 2026-09-05 (PR #1045): three meta-test files' whole-branch diff-guard bodies (test_portal_fleet_pulse.py, test_skf_domain_skill.py, test_station_persona.py) refactored onto the shared mechanics (retro-pyforge-steward-2026-09-04.md action item 11: the fleet's whole-branch diff-guard test mechanics (git subprocess calls computing files/commits changed since origin/main) were consolidated into a shared `pyforge.testing_kit.branch_diff_guard` module (PR #1045).); each guard's enforced policy is unchanged. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pyforge-doctor`.
- (change by claude) New source 2026-09-05: `platform-policy-suite` (retro action item 3 (retro-pyforge-steward-2026-09-04.md, 2026-09-05): a new pyforge-doctor repo-scope source, `platform-policy-suite`, judges src/platform's manifest-only tests/policy subset by shelling out to the platform-ci-test pixi env's own interpreter (cli_bridge.run_pytest, AD-5), so a pixi.toml regression like the openfeature-provider-flagd/cachebox ceiling drop is caught even when Platform CI itself is disabled. WARN when that env is not installed, FAIL only on an actual policy-suite failure.). Added `sources/platform_policy.py` + `cli_bridge.run_pytest` (the pytest-specific sanctioned subprocess call, mirroring `run_git`'s `ok_exit_codes` shape) + the `Source.PLATFORM_POLICY_SUITE` taxonomy member (models.py + report-schema.json) + its `SourceRegistration`/`DISPATCH` entry + unit/meta test updates (test_cli_bridge.py, test_sources_platform_policy.py new, test_models.py, test_sources_dispatch.py, test_source_independence.py's SOURCE_MODULE). Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pyforge-doctor`.
- (decision) 2026-09-05: `platform-policy-suite` stays a WARN-on-absent local/dev check, NOT wired into `detectors.yml` as a hard CI gate — operator call, after weighing it explicitly. Reasons: (1) `detectors.yml`'s repo-scope sweep is deliberately advisory-only except one dated, considered exception (`cfe_rebuild_guard_check`) — provisioning `platform-ci-test` there wouldn't make this a real gate without a second such carve-out, a bigger decision than "add an env"; (2) the `detectors` pixi env is deliberately kept minimal ("small on purpose... finishes in seconds" per its own workflow header) — `platform-ci-test` (full Django/pytest-django/langflow/dbgpt) directly fights that; (3) Platform CI's own `test` job already runs `tests/policy` as a real, non-skippable step on every triggering PR, so this source's unique value is narrow: catching the case where Platform CI's own workflow doesn't run at all. That scenario's actual root cause (the 2026-08-30→09-04 account-wide Actions-minutes billing block) is already root-caused and guarded (`fnd:AD-23` + Story 44.15) at the layer that matters, not per-workflow. Net: the residual risk this would close is real but narrow, and the cost is ongoing — not worth it today. Revisit if `fnd:AD-23`'s guard is ever found insufficient, or if Platform CI's own `paths:` filter is found to miss a real regression class.
- (note) Cross-station surface touch (marshal Story 30.2, 2026-09-06): `sources/factory.py`'s TRACKED tuple and classify() dropped the `('project-context.md','context')` entry / `tracked:context` branch, and `tests/unit/test_sources_board_chain_layers_audit.py` gained fixture/assertion updates for the same migration (BMAD 6.12 `persistent_facts=[]` retired the 8 per-station `project-context.md` rulebooks, D1; the surface is now the single AGENTS.md `bmad:context` block). No `platform-policy-suite` or other pyforge-doctor source behavior changed. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure (found during the story's follow-up review pass — this spec's own copies of both files' hashes had drifted un-reconciled alongside the sibling `spec-pixi-candidate-currency` entry, which was reconciled at the time).
- (note) Cross-station surface touch (marshal Story 30.5, 2026-09-06): `chain.py`'s `mint_id_for_entry` comment fixed a dead file-path citation (`.claude/skills/bmad-dev-auto/step-04-review.md` -> `.claude/skills/bmad-build-auto/step-04-review.md`, the skill's own directory moved under the 6.12 shim-roster rename). Comment-only, no behavior change. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (note by claude) Cross-station surface touch (marshal Story 31.1, 2026-09-07): sources/factory.py classify() and hygiene_definitions.py's _CONVENTIONAL_FILENAMES/_CONVENTIONAL_DIRECTORIES both gained rules for TEA's bmad-testarch-test-design output shape (test-design-architecture.md, test-design-qa.md, test-design-progress-system.md, the test-design/ and reviews/ directories) -- marshal's Story 31.1 TEA equivalence run against all 8 stations tripped both test_bmad_artifacts_integrity (HARD uncovered, factory.py) and test_live_repo_gather_reports_no_finding_naming_warden (orphan-file WARN, hygiene_definitions.py) for the new file shape. tests/unit/test_sources_factory.py and tests/unit/test_hygiene_definitions.py each gained one new test. No other pyforge-doctor source behavior changed. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (change) Story 20.1 (Epic 20, spec-bmad-method-version-drift CAP-4 residual): sources/bmad_method.py's suite pass now derives each watched package's own upstream probe from its recipe's tracked extra.cfe-source-kind (github-tag/npm-registry, unchanged; github-commit, new -- compares the recipe's own context.commit against the target repo's default-branch HEAD sha via one fail-open GET) instead of always going through releases/tags-or-npm. Every per-package bmad-suite-upstream-drift WARN Finding now names its probe class in evidence[probe_class]; the aggregate OK Finding's evidence shape is unchanged. No new Source member, no report-schema/registry-structure change. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/bmad_method.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_bmad_method.py.
- (change) Story 20.2 (Epic 20, spec-bmad-suite-lifecycle CAP-9 G7): new source bmad-render-config-ambiguity -- Doctor's verdict on whether _bmad/scripts/render_skill.py's own central-config merge (load_central_config()'s four _bmad/config*.toml / _bmad/custom/config*.toml layers) holds a bare key ambiguous across two dotted paths, the class that HALTs every rendering skill with RenderError: ambiguous config value (live 2026-09-06, fixed by 99e595cc6a). Independently reproduces the renderer's own merge/scan logic (never imports it), excludes the agents top-level namespace (BMAD's persona registry, not a settings namespace -- empirically verified this is what makes ok-on-todays-tree true), warn-only. Added sources/bmad_config.py + the Source.BMAD_RENDER_CONFIG_AMBIGUITY taxonomy member (models.py + report-schema.json) + its SourceRegistration (subject_station=steward) / DISPATCH entry + unit/meta test updates, mirroring platform-policy-suite's own prior landing shape exactly. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/bmad_config.py (new), src/shared/packages/pyforge-doctor/tests/unit/test_sources_bmad_config.py (new), src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__main__.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/data/report-schema.json, src/shared/packages/pyforge-doctor/tests/unit/test_models.py, src/shared/packages/pyforge-doctor/tests/meta/test_source_independence.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_dispatch.py. Full doctor suite: 1400 passed / 1 skipped. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (change by claude) Story 20.2 review-pass patches (4 findings): sources/bmad_config.py's _load_layer gained UnicodeDecodeError to its fail-open exception tuple (a non-UTF-8 layer file was escaping to the outer degrade_on_exception wrapper); the checked==0 OK message now says 0 checked -- no central config layers found instead of a bare 0 checked; models.py's BMAD_RENDER_CONFIG_AMBIGUITY comment fixed a copy-paste residue (Judges Marshal-governed factory apparatus) with an accurate subject_station=steward rationale citing customization-inventory.md C2; test_sources_bmad_config.py gained 2 tests (non-UTF-8 layer, list-of-tables dead end). Full doctor suite: 1402 passed / 1 skipped. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/bmad_config.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_bmad_config.py. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (change by claude) Story 20.3 (Epic 20, architecture spine fnd:AD-22/AD-11): new source frozen-path-changed -- reads a capability ledger at docs/foundry/manifest.{yaml,yml,json} (an [ASSUMPTION: schema] synthesis; no ledger exists at all until the python-foundry cutover begins) and, for every capability rebuilding/moving, diffs origin/main..HEAD via cli_bridge.run_git and FAILs one Finding per (capability, changed path) pair that falls under a frozen prefix. No ledger at all is OK -- true for every real invocation today. First FAIL-capable source Epic 20 has added, a deliberate departure from the epic's general warn-at-most framing, governed by this story's own specific AC. Added sources/frozen_path.py + the Source.FROZEN_PATH_CHANGED taxonomy member (models.py + report-schema.json) + its SourceRegistration (subject_station=steward) / DISPATCH entry + unit/meta test updates, mirroring bmad-render-config-ambiguity's own prior landing shape. A cross-station relay note was appended to spec-bmad-suite-lifecycle's own memlog so a later steward pass can flip cutover-readiness.md's G11 row. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/frozen_path.py (new), src/shared/packages/pyforge-doctor/tests/unit/test_sources_frozen_path.py (new), src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__main__.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/data/report-schema.json, src/shared/packages/pyforge-doctor/tests/unit/test_models.py, src/shared/packages/pyforge-doctor/tests/meta/test_source_independence.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_dispatch.py. Full doctor suite: 1437 passed / 1 skipped. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (change by claude) CI/hygiene sweep 2026-09-07: dream_chain's DEFERRED_SPECS dict gained one entry, `spec-bmad-cursor-interactive-routing` (owner marshal), registering a freshly-seeded stub Spec so dream-chain INV-1 (dream-without-spec) clears for the new docs/dreams/bmad-cursor-interactive-routing.md Dream. No other pyforge-doctor source behavior changed. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py.
- (change by claude) CI/hygiene sweep 2026-09-07 (DW-FU-21-8-6, atlas ledger item filed against this tool): `_flatten_deferred_scalar()` no longer silently hard-slices a scalar at its limit -- it still caps the surviving head at the same budget (bounded ledger growth unchanged), but now appends `... [truncated, N chars total]` when it actually truncates, so the cut is visible in every `deferred-work-ledger.md` entry this ingests and the original untruncated length is recoverable from the marker itself. Short values are returned unmarked, byte-for-byte. No schema/registry change; applies uniformly to `summary`/`evidence`/`location` (the one shared helper). RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_deferred_work.py. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (change by claude) CI/hygiene sweep 2026-09-07 (DW-FU-1-4/DW-FU-1-4-2): checks/env_hygiene.py's credential-hygiene scanner gained intermediate-variable tracking (name = os.environ.get(...); headers[...] = name, per-function-scoped, transitive) and dict-literal-construction tracking for a bare header-shaped Name target (headers = {"k": os.environ.get(...)}) -- closing the two real-repo-confirmed v1 gaps the ledger named (scan_project.py's tok = os.environ.get(...) feeding headers[...], and the headers = {...} shape exemplified by gemini_server.py). A bare 'return {...}' dict literal with no header-named variable (dependency-checker.py's return {"X-JFrog-Art-Api": api_key}) was deliberately NOT chased: live-confirmed during this pass that doing so regresses the golden fixture (5 new false positives against already-safe early-return-guarded code, the separately-tracked DW-FU-1-4-2 item (a) statement-flow gap). The golden-fixture test now pins two newly-surfaced, pre-existing, real host-scoping gaps in github_version_checker.py/scan_project.py by exact (file, line, var) rather than asserting the whole directory is clean -- those two files are conda-forge-expert's own surface to fix, out of this pass's scope. 7 new unit tests added; full env_hygiene suite green (51 passed). RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/checks/env_hygiene.py, src/shared/packages/pyforge-doctor/tests/unit/test_checks_env_hygiene.py.
- (change) Sweep follow-ups 2026-09-08: ledger-direction now decides "merged" by reading the tracked ledger as committed at base_ref (`_base_done_ids`), not by parsing merge subjects. Judged on subjects alone it reported 383 done-but-unmerged findings -- 53% of all 724 done stories -- because `_merged_ids_for_project` only counted branches shaped `<station>/...` and the fleet lands most work in batched chore/docs/dispatch/maintenance PRs that name no story. FR-138 holds: the oracle is still git, and a tracked blob at the base commit is stronger evidence than a heuristic over commit messages. A ledger absent at base_ref returns None (not an empty set) so a file added on the branch falls back to subjects instead of accusing every key in it. Live 383 -> 0 across 8 ledgers; proven non-vacuous by injecting a synthetic unmerged done key. The FAIL half (landed-but-unpromoted) is unchanged. Now wired into scripts/detectors.py _DOCTOR_SOURCE_TASKS with a ledger-direction-check pixi task, resolving herald/DW-FU-18-1 (which asked for triage-then-wire, not wire). RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/ledger.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_ledger_direction.py, scripts/detectors.py, pixi.toml. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (change) Fleet consistency pass 2026-09-08: an inline ledger FIXTURE inside tests/unit/test_sources_chain_deferred_work.py quoted `architecture-pyforge-marshal-2026-07-25/architecture.md`, which was renamed to ARCHITECTURE-SPINE.md in the same pass (upstream names the spine file that way -- bmad-architecture/customize.toml:43 -- and marshal and warden were the two stations still on the legacy filename). The fixture text was updated with the other 77 referencing files; it is prose inside a test fixture, not an assertion on the filename, so no doctor behavior changed. Full doctor suite green (1473 passed). RECONCILES: src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_deferred_work.py. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (note by claude) INCOMING SURFACE CLAIM (steward Story 49.2), recorded BEFORE any code lands so spec-surface-check does not read it as ungoverned drift: 49.2 writes a NEW module under src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/ plus a report-schema.json entry and detectors membership — all inside this Spec's declared surface (src/shared/packages/pyforge-doctor/**). Per fleet-readiness-decision-batch-2026-09-09 § 2.3 C8 (operator-approved), the CRITERION stays on spec-pyforge-unifying-strategy and the IMPLEMENTATION is relayed to a new doctor Dream + Spec: docs/dreams/capability-effect-check.md and specs/spec-capability-effect-check/ (both minted this pass). Doctor's precedent for a dedicated module rather than an extension of sources/factory.py is bmad_method.py:9-20's own docstring.
- (note by claude) INCOMING SURFACE CLAIM (steward Story 49.8), same reason: 49.8 names src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/marshal.py:544 verbatim in its Surface — the loop_root = Path.home() / '.bmad-loops' read that Unifying CAP-17 retires. Doctor mints NO story here: 49.8 already exists and is correctly blocked on marshal Epic 33's Track story (steward sprint-status-ledger.yaml). Doctor's only actions are this memlog line and a comment at sources/__init__.py:223 (the existing CHAIN_COMPLETENESS/story-status registration block that already documents the ~/.bmad-loops read as 'preserve, don't redesign') noting the read is scheduled for retirement under Unifying CAP-17, so the next reader does not fix it independently. That comment is a code edit and rides the C8 story. Cite: fleet-readiness-decision-batch-2026-09-09 doctor-B9.
- (decision by claude) spec-pyforge-charter is registered in DEFERRED_SPECS with the reason 'a constitutive Spec whose CAP-1/4/5/6/8 are document-integrity properties no story can pick up' (guild-E2: it is in-progress with eight capabilities, zero epics and zero ledger keys, and is registered nowhere today — so it is exempt from neither the decomposition expectation nor the documented-deferral list). The dict edit is code and rides the C8 story minted in doctor's epics.md this pass. Cite: fleet-readiness-decision-batch-2026-09-09 § 2.3 C8.
- (note by claude) bmad-drift's 17 live warns against marshal's artifacts (14 pin-behind 'conda-forge-expert v8.86.1 < v8.90.1' in pyforge-marshal/planning-artifacts/{architecture,index,deployment-guide,architecture-cf-atlas,development-guide}.md:5-7, 2 surface-changed for skill_version 8.87.1 -> 8.90.1 and gotcha_max 114 -> 117, 1 deferred-stale v8.86.2 < v8.90.1) are NOT doctor's to fix and no doctor story is minted for them: doctor is the detector reporting them, marshal's SYNC-RUNBOOK row 84 is the reconciler. Recorded because doctor's board carries the red. Related standing proposal (batch § 2.4 D6, marshal-owned): a DERIVED pin read from SKILL.md:10 instead of a stamped literal — CFE shipped five releases on 2026-09-09 alone, so a per-release pin bump is not sustainable.
- (note by claude) CORRECTION to the 49.8 entry above: the one-line comment at sources/__init__.py:223 is deliberately NOT given a doctor story in Epic 21 (minted this same pass). It belongs in steward Story 49.8's own diff — that is the change which retires the ~/.bmad-loops read, and a doctor story landing a comment about a retirement that has not happened would go stale before it merged. Doctor's obligation is discharged by the surface-claim memlog line above; the comment rides 49.8. Recorded so a later reader does not treat the missing story as an oversight.
- (event by claude) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS; no capability, constraint or status change — today's entries are notes/one decision, rendered as three Assumptions bullets (49.2/49.8 incoming surface claims, spec-pyforge-charter DEFERRED_SPECS registration, the marshal-owned bmad-drift warns).
- (note by claude) INCOMING SURFACE CLAIM (marshal Story 33.12), recorded BEFORE django-pyforge/** edits land: held-run lifecycle (`publish_held_run`, `heartbeat_held_run`, `complete_held_run`, `sweep_lost_runs` held-row judgment, loop-publish run-bound override), marshal MCP tools `publish_loop_run` / `heartbeat_loop_run` / `complete_loop_run`, and `platform-realm.json` `pyforge:station:marshal` group — all steward/host surface governed by django-pyforge and platform config, not this Spec's own surface. Doctor's `sources/marshal.py:544` read is re-pointed in the same story when the published payload carries per-story phase + commit; until then the filesystem fallback remains with CAP-17 debt tagging unchanged from the 49.8 claim above.
- (change by claude) Steward Story 49.8 CAP-17 landed 2026-09-12: reworded sources/__init__.py:223 comment to note gather_story_status now reads the published plane first (marshal Story 33.12 re-pointed sources/marshal.py _harness_tasks/gather_story_status) with ~/.bmad-loops fallback only when unreachable -- the Unifying CAP-17 retirement is landed, not scheduled. No behavior change; comment-only. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py.
- (event by claude) Surface reconcile 2026-09-14: commits c48a65e8ad ('Add a CAP extract detector to detectors-ci') and 9d7528297866 ('Require a 54.1 case-list id for verified-in-foundry') added a new capability-extraction source (sources/capability_ledger.py + tests/unit/test_capability_ledger.py), touched sources/__init__.py/__main__.py's registry wiring, data/report-schema.json, models.py, tests/unit/test_models.py, tests/meta/test_source_independence.py, and tests/unit/test_sources_dispatch.py to register and cover it. Already landed and its own new test file passes. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (event by claude) Surface reconcile 2026-09-14: local-recipes PR #1354 (commit 9cf304c62a) re-measured three live-repo snapshot tests against a ~5-day GitHub Actions billing outage's worth of accumulated content drift (see .claude/memory/ project notes) -- status_body_consistency.py, tests/unit/test_sources_chain_dreams_hygiene.py, test_sources_status_body_consistency.py, test_sources_status_body_promissory.py. All three are re-measurements of real, already-merged content changes elsewhere in the repo (kinship wikilink dead-count drifted 24->26; pyforge-scribe's Dream doc no longer contradicts its own shipped status; the herald Dream+Spec promissory-language pair dropped from 2/2 firing to 1/2 as the Spec side was cleaned up, so status_body_consistency.py's accepted invariant changed from == 2 to <= 2), not a change to any CAP's own contract text. Full pyforge-doctor suite verified green: 1643 passed. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (event by claude) Surface reconcile 2026-09-14: board.py's INV-A gained a delivered-Spec branch. It previously skipped EVERY non-open Spec status (`if status not in OPEN_SPEC_STATUSES: continue`), so a `shipped` Spec delivered with no epic and no ledger row was structurally invisible -- ten existed fleet-wide. New constants DELIVERED_SPEC_STATUSES/DECOMPOSITION_OWED_STATUSES add `shipped` only; `absorbed`/`archived`/`superseded`/`extension-point` stay exempt for stated reasons. The delivered branch is held to the WHOLE-SPEC standard (does any FR or epic reference it at all), never INV-A's per-CAP citation test -- per-CAP flagged 34 Specs against 12 real ones because epics predating the cite-every-CAP-id convention name the Spec or its Dream without enumerating CAP-n. Matching also accepts an owner-dream path, since Epic 51 claims its Spec that way. Open-Spec behaviour is byte-unchanged. Also registered spec-vocabulary-one-name-one-job in DEFERRED_SPECS (operator-owned open questions). Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (follow-up to the same day's board.py INV-A change): tests/unit/test_sources_board_chain_completeness.py replaced test_shipped_spec_is_not_open_and_reports_no_finding -- which encoded the now-corrected exemption -- with four tests: the new delivered-spec-not-decomposed behaviour, two false-positive guards (an epic referencing the Spec by slug, and an epic claiming it by owner-dream path), and a guard-removed companion asserting absorbed/archived/superseded/extension-point stay exempt. Full doctor suite 1646 passed. Tracked as DW-CHAIN-COMPLETENESS-6 (closed) and -7/-8 (open) in this project's deferred-work ledger. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (event by claude) Surface reconcile 2026-09-14 (DW-CHAIN-COMPLETENESS-7, closed): board.py's INV-B gained an EPIC arm. INV-B compared `### Story` ids against ledger keys in both directions, but its first act discarded every `epic-*` key (`story_rows = {k: v for k, v in rows.items() if not k.startswith("epic-")}`), so nothing anywhere checked that an `epic-N` ledger key has a matching `## Epic N:` heading or the reverse. This mattered beyond bookkeeping: scripts/fleet_picture.py derives the fleet's headline epic counts from exactly those keys, so a drifted key silently misreported fleet progress -- measured live at 57 vs 56 for steward and 36 vs 35 for marshal. New helpers `_epic_numbers_from_epics()` (regex `^##\s+Epic\s+(\d+)`, broad except -> empty set) and `_ledger_epic_numbers()` (`re.fullmatch(r"epic-(\d+)")`, so `epic-N-retrospective` is deliberately NOT counted as a second epic -- every station pairs the two, and a loose match would demand a heading per retrospective and red all eight ledgers at once). Emits `epic-heading-without-ledger-key` / `ledger-epic-key-without-heading`, each naming the epic numbers in its detail. Both live orphans were a different defect than predicted: steward Epic 18 was MIS-LEVELLED (an H3 body heading at line 1563 alongside an identically-titled summary entry at 1503 -- promoted the body form after verifying count==1 for it and ==2 overall), marshal Epic 29 genuinely ABSENT at every level (heading inserted above its existing `**Goal:** FR-193 CAP-11` block, warranted by marshal's own frontmatter admission "183 (181 + Epic 29's two, never counted)"). Four new tests cover both directions, agreement, and the guard-removed retrospective companion. The arm initially reddened six existing tests because `_write_epics_md` always emits `## Epic 1` while their ledgers carried no `epic-1` key; the FIXTURES were the unfaithful artifact (all eight real ledgers pair them), so `_write_ledger` now defaults `epic-1: done` when no `epic-*` key is supplied, rather than weakening the detector to tolerate a shape that does not occur. Full doctor suite green. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_board_chain_completeness.py. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (note by claude) INCOMING SURFACE CLAIM (marshal Epic 42, `spec-surface-overlap-tolerance` CAP-1..2), recorded 2026-09-14 BEFORE any code lands so spec-surface-check does not later read it as ungoverned drift: Stories 42.1/42.2 modify `sources/chain.py::_drift_findings` (chain.py:1662-1742) plus its unit fixtures -- inside this Spec's declared surface (`src/shared/packages/pyforge-doctor/**`). Doctor owns the module; marshal owns the contract, which is why the epic is minted there and not here, matching the 49.2/49.8/33.12 precedent. The change inverts `_drift_findings` from per-spec iteration to per-path grouping so a file governed by several specs is reported only when NO co-governing spec reconciled it. The per-spec bar itself does not change: `spec_moved` AND the memlog text naming the path stays exactly as it is -- that was the marshal Spec's one open question, answered 2026-09-14 by reading this module. Severity must be preserved on the residual: a co-governor that never moved yields `drift` (FAIL) and one that moved without naming the path yields `drift-presumed` (WARN), so a group with no clean co-governor keeps the STRONGEST severity present, never a FAIL silently downgraded to WARN by the arrival of a second governing spec. Doctor mints no story for this.
- (change by claude) Surface reconcile 2026-09-14 (post-merge of PR #1355): registered `spec-coverage-gate-independence` in `board.py`'s `DEFERRED_SPECS`. Seeding that Spec in #1355 reddened `chain-completeness` INV-A on main — doctor now owned an open `draft` Spec no FR or epic referenced. It is not decomposable today and the reason is structural, not scheduling: its five open questions are DESIGN decisions about where a fleet-wide blocking gate may live, and the obvious homes are already ruled out — `pyforge-core` and `pyforge-testing-kit` are both governed by marshal's own planning tree, so relocating there moves the violation one package over, and doctor cannot host a blocking gate because doctor is constitutionally advisory. The question is recursive (the gate judges all eight stations, so any station-owned home reproduces the defect), so there is nothing an epic could pick up. Same shape and same mechanism as `spec-vocabulary-one-name-one-job`, registered the same day: DEFERRED_SPECS is for undone work with a stated reason, never for delivered work. De-register once the operator picks a home — the remedy is mechanical after that. Full doctor suite green. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py. Scoped-stamped.
- (change by claude) Surface reconcile 2026-09-14: `board.py` lost **494 lines** of retired-Guildhall dead code (ten functions, ten `_LAYOUT_*` constants) with **zero behaviour change**, closing `DW-DASHBOARD-DEAD-CODE-1`. An AST call-graph pass proved they formed two closed PRIVATE ORPHAN islands -- the layout chain (`_run_check_layout`, called by nothing) and the drift chain (`_check_dashboard_drift`, called by nothing) plus two standalone orphans -- so ten of eleven suspects had no live caller at all. Both gathers, their DISPATCH entries, their `Source` taxonomy members and `report-schema.json` are untouched; the ledger entry had over-stated the scope as "a behaviour change to two registered sources plus taxonomy and schema", inferred from the code's location rather than measured from its call graph, which is why it had been deferred as needing a Dream when it was hygiene. Verified by byte-comparing `--json` findings for `dashboard-drift`, `check-layout`, `chain-completeness` and the `--layers` audit before and after: all four IDENTICAL. Full doctor suite green (1378 passed). Coverage 72% (pre-pragma) -> 94% (pragma) -> **95% with the code actually gone**. Only `_load_dashboard_generate` survives, genuinely reachable from `_gather_chain_layers_audit`; its pragma stays because its reachable path is closed by policy (`retired-console-check` fails CI on reintroduction), not by structure. Stale prose rewritten rather than left pointing at deleted helpers. Scoped-stamped.
- (change by claude) 2026-09-14: sources/board.py DEFERRED_SPECS gained spec-self-hosted-bmad-marketplace (steward, draft, seven open questions) so chain-completeness INV-A does not report spec-not-decomposed for a Spec seeded only to make the Dream's chain link durable. No detector logic changed. De-register once open_questions clear and steward epics carry the chosen CAPs. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py.
- (change) 2026-09-14: board.py DEFERRED_SPECS gained spec-work-passports-dated-extracts (steward, draft, six open questions) so chain-completeness INV-A does not report spec-not-decomposed. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py.
- (event) Surface reconcile 2026-09-14: scoped-stamped after DEFERRED_SPECS entries for spec-self-hosted-bmad-marketplace and spec-work-passports-dated-extracts. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (change by claude) Surface reconcile 2026-09-14 (later the same day, operator rulings pass): de-registered `spec-coverage-gate-independence` from `board.py`'s `DEFERRED_SPECS` — the entry registered this morning is now stale by its own invariant ("remove stale entries rather than leaving them inert"). The operator ruled the Spec's home the Guild (Charter §5 amendment), so it moved by `git mv` to `docs/governance/spec-coverage-gate-independence/` — outside `pa.glob("specs/spec-*")`, exactly as `spec-pyforge-charter` is — and its mechanism stories are doctor Epic 23 (23.1-23.3, `backlog`), which INV-A can see. `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py` is the one governed file that changed: the dict entry became a dated comment explaining the de-registration; no logic changed. Scoped stamp for this key follows (`--write-baseline --spec <key>`), never bare.
- (change) 2026-09-15: DEFERRED_SPECS dropped spec-vocabulary-one-name-one-job — Spec ready, steward Epic 59 cites CAP-1..8. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py.
- (change) 2026-09-15: DEFERRED_SPECS dropped spec-self-hosted-bmad-marketplace — Spec ready, steward Epic 60 cites CAP-1..7. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py.
- (change) 2026-09-15: DEFERRED_SPECS dropped spec-work-passports-dated-extracts — Spec ready, steward Epic 61 cites CAP-1..7. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py.
- (change) 2026-09-15: DEFERRED_SPECS dropped spec-build-league-scorecard — Spec ready, steward Epic 62 cites CAP-1..4. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py.
- (event) 2026-09-15 incoming surface (marshal Epic 45 / spec-bmad-cursor-interactive-routing): drop DEFERRED_SPECS entry for spec-bmad-cursor-interactive-routing after CAP-1 PASS. Paths: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_board.py. No product change on this spec.
- (event) Incoming marshal Epic 42 claim: pyforge.doctor.sources.chain._drift_findings now groups drift by path and ORs co-governors (spec-surface-overlap-tolerance). chain.py and test_sources_chain_spec_surface.py.
- (event) CI: test_live_tree_dream_readme_missing_count tracks 67 missing README rows. test_sources_chain_dreams_hygiene.py.
- (event) 2026-09-16 fleet-inbox-disposition: de-registered spec-golden-path-conda-blind-spot from DEFERRED_SPECS (warden Epic 12 done, CAP-1..5 cited). Charter and agentic-sdlc-autonomy stay. Record: steward research/fleet-inbox-disposition-2026-09-16.md.
- (decision by operator) 2026-09-16 board.py surface move: DEFERRED_SPECS gained spec-token-economy-claude-session-path (draft seed whose open questions were answered 2026-09-16 in its own memlog; decomposition mints with the re-derive, not by hand). Same entry recorded on spec-pixi-candidate-currency, the other surface governing board.py. Part of the operator-ruled token-savings Dream consolidation.
- (decision by operator) 2026-09-16 board.py surface move (second today): DE-registered spec-token-economy-claude-session-path from DEFERRED_SPECS — the operator ruled no satellite Specs, so it folded into spec-marshal-token-economy as marshal-token-economy:CAP-19..marshal-token-economy:CAP-24 (status superseded, terminal — not owed) with marshal Epic 46 (46.1-46.10) as the dispatch home. The morning registration comment stays as the record.
- (change) 2026-09-16 doctor Epic 25 (spec-one-chain-per-station CAP-2/CAP-5/CAP-3(g), guild outcome / doctor mechanism): new sources/one_chain.py (chain-sprawl, fr-without-cap) + rekey.py (the fold PR's re-key map parser, one grammar for three readers); Source enum gains CHAIN_SPRAWL and FR_WITHOUT_CAP (report-schema.json enum extended additively); __main__ DISPATCH + REGISTRY + scripts/detectors.py detectors-ci rows; sources/marshal.py story-status confirms a renumbered done story by evidence under any earlier spelling the maps record; tests: test_sources_one_chain.py (new), test_sources_ledger.py (five re-key cases), test_models/test_sources_dispatch/test_source_independence pins extended. Baselines: docs/governance/chain-sprawl-baseline.json (164 dreams, 164 spec folders at e630e43330) and fr-baseline.json (421 FR/NFR ids, 8 PRDs); scripts/chain_sprawl_baseline.py (--snapshot once / --prune only) and scripts/fr_baseline.py (--snapshot / --project). Stories 25.1-25.3.
- (change) 2026-09-16 tests/unit/test_sources_sibling_dreams.py: offline fetch-path and parser-edge tests (urlopen stubbed) lift sources/sibling_dreams.py from 57.7% to 96%. Cause: registering a new source touches sources/__init__.py, which coverage_gates_ci resolves to the whole pyforge.doctor.sources package (filter_percents prefix match), so every source module is measured on any registration PR; sibling_dreams was the one below the 80% floor. Fix-now rule. The gate's prefix behaviour on a package __init__ is marshal's (spec-pyforge-testing-charter) to decide on, not patched here.
- (change) 2026-09-16 Story 25.3 follow-up (found live on the marshal fold pilot, PR #1389): the first re-key map ever written, planning-artifacts/rekey-2026-09-16.md, reddened bmad-drift 'uncovered' -- 25.3 shipped the readers (pyforge.doctor.rekey, sprint-ledger-sync --rekey, ledger-regression / story-status) but no classify() rule for the file itself. Added the rule in BOTH copies (src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py classify(), scripts/bmad_drift_check.py classify() -- the mutation-only residual must stay in parity): planning-artifacts/rekey-YYYY-MM-DD.md -> tracked:plan (dated per AGENTS.md § Dates; undated rekey.md is not the shape). Test: src/shared/packages/pyforge-doctor/tests/unit/test_sources_factory.py::test_rekey_map_is_classified_and_not_flagged_uncovered. Doctor 1705 passed; scripts-suite 489 passed. The cloud agent correctly stopped at this red rather than editing doctor's tree from the marshal fold (pilot lesson 13).
- (event) 2026-09-17 one-chain doctor fold: 15 Dreams in covers-dreams; 15 Spec folders (1 living + absorbed pointers); CAP-1..76 reminted sequentially with provenance; companions kept; pixi.lock stays on spec-pixi-candidate-currency; station Spec status ready; station Dream specified.
- (capability by claude) CAP-77 — the map of what no agent can verify without a live proof. Intent: a fleet-wide, doctor-owned inventory of surfaces a dev/review pass structurally cannot verify from inside the repo alone (station, surface, what a static pass cannot see, how to prove it live, roughly how expensive) -- catalogued in a new companion live-proof-surfaces.md, cited by name from a Finding when a touched surface matches. Success: a story/PR touching a catalogued surface gets an advisory Doctor finding naming it (never gating, matching AD-2's operability-not-policy posture); the catalog is read from real, current mechanisms only (existing live-test scripts, opt-in env flags, service-up tasks), never invented. Motivating incident (2026-09-17/18): herald's mcp SDK transport broke across two 2.x changes (streamablehttp_client renamed to streamable_http_client with a different call signature; CallToolResult.isError renamed to .is_error), caught by neither review nor the test suite nor a dev pass's own self-report -- only a real live push-then-read-back against Claude Design surfaced it. Seeded from docs/dreams/pyforge-doctor.md's 2026-09-18 Realization-log entry (Dream-append-first; no new Dream file, doctor's canonical chain).
- (constraint by claude) CAP-77's catalog never invents a live-proof mechanism -- it names only what already exists somewhere in the fleet (a script, an opt-in env flag, a service-up task) and cites it by real path; a surface with no existing live-proof mechanism is recorded as a genuine gap, never papered over with a fabricated one. Extends AD-2's operability-not-policy posture: the finding is always advisory, never a second PR gate.
- (assumption by claude) live-proof-surfaces.md's guild-container/container (docker, podman) row was not named in the source Dream entry -- added from CLAUDE.md's own pr-preflight coverage note ('Not covered by it: container/guild-container (needs Docker/podman)...'), a different already-authoritative document, not invented. Flagged here per bmad-spec's own assumptions[] discipline rather than presented as pure distillation of the single cited source.
- (event by claude) Self-validate pass 1 (coherence) on CAP-77's addition: PASS. Intent describes WHAT (a fleet-wide inventory + advisory finding), not HOW; the new constraint actually bends a design decision (no invented live-proof mechanisms); success is testable (finding present, never gating); CAP-77 is a genuinely new unused id; prose density matches sibling CAP entries. No new non-goal needed -- existing 'no new scanning engines or data sources' non-goal already covers CAP-77 (a catalog + advisory finding over existing mechanisms, not a new instrument).
- (event by claude) Self-validate pass 2 (preservation) on CAP-77's addition: PASS with one logged assumption. Source docs/dreams/pyforge-doctor.md's 2026-09-18 Realization-log entry's load-bearing claims (the core problem, all five originally-named surfaces -- herald Design MCP, herald live webhook, scribe Postgres, atlas Chromium/DuckDB/WASM, warden live feeds -- and the motivating mcp SDK incident) all landed in CAP-77's intent/success or the live-proof-surfaces.md companion. The sixth companion row (guild-container) is an editorial addition from CLAUDE.md, logged as an assumption above, not a silent invention. Wrapper-only content: none dropped.
- (note by claude) 2026-09-18 cross-station spec-surface reconcile (herald Story 21.10's own dispatch verify red on this repo-wide gate, unrelated to that story's own diff -- reconciled here by name rather than left standing, per this repo's own no-pre-existing-exception doctrine for a repo-level gate). Seven governed paths, all `docs/**`, changed in three 2026-09-17 commits, none reconciled at landing time: `docs/MAP.md` (changed twice -- cd2372250d added the one-chain how-to/explanation index rows, ec9817b36f added the station-operations how-to series row), `docs/explanation/README.md` (changed, cd2372250d -- index row for the new one-chain-lock-and-mop explainer), `docs/explanation/one-chain-lock-and-mop.md` (added, cd2372250d, then changed d64e0324af -- dropped banned harness wording), `docs/how-to/README.md` (changed twice -- cd2372250d added the one-chain-station-ops row, ec9817b36f added the two new station-operations rows), `docs/how-to/detect-concurrent-agent-activity.md` (added, ec9817b36f), `docs/how-to/driving-a-pyforge-station-backlog.md` (added, ec9817b36f), `docs/how-to/one-chain-station-ops.md` (added, cd2372250d, then changed d64e0324af -- same banned-wording drop). All three commits (`cd2372250d8fde37d857ecf26ebebdf02181f49f` Cursor Agent, `d64e0324af52ba3d7de7216dcbd168937be05194` Cursor Agent, `ec9817b36f1f35cde7e7ad8a86e47c9c3f991858` rxm7706) are Diátaxis-shaped operator docs squarely inside this Spec's own declared `docs/how-to/**` + `docs/explanation/**` + `docs/MAP.md` surface -- no new capability, the docs caught up to a contract that already claimed them. Content not reviewed for substance here (out of scope for a herald dispatch); reconciliation is bookkeeping only. Baseline scoped-stamped `--spec pyforge-doctor/spec-pyforge-doctor` immediately after.
- (capability by claude) CAP-78 — a PR is judged at its merge-base, and a merge subject is attributed to the station it names. Intent: `ledger-regression` on a PR compares the ledgers at merge-base(base, head) vs head (push-to-main keeps the first-parent fallback); `ledger-direction`/`story-status` accept a templated merge subject only when its slug is the station's, reading each station's `merge_subject_template` from its marshal-policy.toml (TOML read, never a marshal import). Source: docs/dreams/pyforge-doctor.md 2026-09-18 (later) Realization-log entry — herald PR #1465's detectors lane redded 2 min after its unattended merge; atlas 13-5/14-4/15-3 landed-but-unpromoted all day from sibling stations' `Merge N-M into main`.
- (constraint by claude) CAP-78 does not soften the 2026-09-14 ruling: `ledger-regression` stays the one blocking Doctor step in CI; a branch that genuinely moves a `done` key still FAILs. It only changes WHICH base the question is asked against (the PR's merge-base) and WHOSE subjects count (the station's own).
- (assumption by claude) the legacy un-scoped `Merge {key} into main` shape is still honoured for a station whose policy declares that template (today: atlas, mason, scribe, steward, warden), so no historical `done` row loses its evidence when `spec-pyforge-marshal:CAP-247` later changes the repo default; the fixture over the eight tracked ledgers is the regression guard for that assumption.
- (event by claude) Self-validate pass 1 (coherence) on CAP-78: PASS — intent names WHAT (the base, the attribution), the constraint bends a real decision (blocking posture preserved), success is a replay of two real 2026-09-18 findings plus a negative case.
- (event by claude) Self-validate pass 2 (preservation) on CAP-78: PASS — both incidents in the Dream entry (PR #1465 lane at 18:17Z; atlas 13-5/14-4/15-3) appear in success verbatim; `spec-pyforge-marshal:CAP-247` is referenced as the sibling fix, not duplicated.
- (event by claude) Story-minted 2026-09-18 as Epic 27 / Story 27.1 (`27-1-a-pr-is-judged-at-its-merge-base-and-a-merge-subject-names-its-station`).
- (event by claude) CAP-78 success corrected 2026-09-18 after Story 27.1's dispatched session traced the atlas 13-5/14-4/15-3 rows to their real cause: atlas's own bmad-loop-scoped merges read with pre-rekey keys (rekey-2026-09-17.md), not sibling-station subjects. The station-attribution and merge-base halves landed as specified (PR #1471, 12 tests, ledger-regression `ok` on the PR #1465 shape); the atlas-rows claim is struck from CAP-78's success and re-minted as CAP-79.
- (capability by claude) CAP-79 — `ledger-direction` reads the station's rekey map. Intent: `gather_direction` applies the station's rekey-*.md maps (pyforge.doctor.rekey.parse_rekey, as gather() does since Story 25.3) before judging merge history against the ledger. Source: Story 27.1's Auto Run Result recommendation; the three live atlas rows are its fixture.
- (event by claude) Story-minted 2026-09-18 as Story 27.2 on Epic 27 (`27-2-ledger-direction-reads-the-stations-rekey-map`).
- (event by claude) CAP-78's recorded assumption ('the legacy un-scoped shape is still honoured only for a station whose policy declares that template') was wrong and Story 27.1 implemented it faithfully: after PR #1471, `story-status` on main reports marshal/34-3 as `done` with no merge commit anywhere — marshal landed it 2026-09-12 as `Merge 34-3 into main` under the then-default template, and marshal's policy now declares `Merge pyforge-marshal/{key} into main`. History was orphaned by the station's own template move. Corrected as CAP-80.
- (capability by claude) CAP-80 — a station's legacy-template history stays attributed after its template changes. Intent: the legacy `Merge {key} into main` form counts for a station only for keys its tracked ledger knows (Story 35.1's corroboration), so a template move orphans nothing and a sibling's bare subject still attributes nothing. Source: story-status finding on main after PR #1471 (marshal/34-3).
- (event by claude) Story-minted 2026-09-18 as Story 27.3 (`27-3-a-stations-legacy-template-history-stays-attributed`), dispatched ahead of 27.2 because it is a regression on main.
- (event by claude) Story 27.3's dispatched session found an intent gap in CAP-80's first Approach: "the querying station's ledger knows the key" reopens the cross-station collision whenever two stations know the same integer key -- with eight stations on one grammar, the common case. It reverted to baseline (patch preserved in the dispatch worktree), set `blocked`; marshal then landed the empty branch (PR #1476) and promoted 27-3 done -- recorded as a marshal gap. Operator-delegated ruling (2026-09-18, autonomous session under the operator's 'be autonomous'): attribute a bare-form merge by the station paths its first-parent diff touches -- git is the sole authority for merged facts (spec-pyforge-marshal Constraints). Verified on the real case: `dcda31b8cb Merge 34-3 into main` touches `_bmad-output/projects/pyforge-marshal/**` and `src/shared/packages/pyforge-marshal/**` only.
- (constraint by claude) CAP-80 attributes by diff paths, never by ledger membership alone: a bare-form merge whose diff touches no station path attributes to nobody; one touching several stations' paths attributes only where the querying station's paths appear AND its ledger knows the key (both, so a fleet-wide mop commit that grazes many packages cannot mint evidence).
- (event by claude) Story-minted 2026-09-18 as Story 27.4 (`27-4-a-bare-form-merge-is-attributed-by-the-paths-its-diff-touches`); 27.3's spec reads `done` (landed empty) with the truth recorded above its Auto Run Result.
- (event by claude) Story 27.4 re-keyed to 27.5 (2026-09-18): the mint PR's branch `doctor/27-4-mint` parses under the station-branch landing grammar, so `Merge pull request #1477 from rxm7706/doctor/27-4-mint` reads as 27.4 landed and the first dispatch of 27.4 short-circuited `story_merged_on_main` in one second (run pyforge-doctor-20260918T222825190Z-8c204792; orphan session killed at 44 s, nothing produced). 27.4 is a reserved hole; content unchanged as 27.5. Lesson recorded fleet-wide: never lead a station-prefixed branch name with a story key unless it IS that story's landing branch.
- (event by claude) Story 27.5 landed 2026-09-18 (PR #1484, `f33e9f0966 Merge pyforge-doctor/27-5 into main`; CAP-80 realized as ruled): new `src/pyforge/doctor/bare_merge.py` (`station_slugs_touched` memoized first-parent diff classifier, `known_story_keys` any-status ledger read, `attribute_bare_merge` — attributes a bare `Merge {key} into main` iff the diff touches the querying station's paths EXCLUSIVELY and its ledger knows the key); `sources/marshal.py` Routes 2/3 and `sources/ledger.py` `_merged_ids_for_project` both wired through it, diff-unreadable shas surface as standalone WARN findings. 13 new tests across `tests/unit/test_sources_marshal_story_status.py` / `test_sources_ledger_direction.py` (1744 passed). Verified on main after merge: story-status ok (marshal/34-3 no longer reported), ledger-direction ok (doctor 27-4 row cleared via rekey-2026-09-18.md). Review recommended no follow-up (exclusivity fix re-verified live + regression test). RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/bare_merge.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/marshal.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/ledger.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_marshal_story_status.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_ledger_direction.py. Scoped stamp follows: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (event by claude) Epic 27 closed 2026-09-18: 27.1 (PR #1471, CAP-78), 27.2 (CAP-79 rekey map), 27.3 (PR #1476, landed empty — truth recorded in its spec), 27.5 (PR #1484, CAP-80 as ruled) all done; 27.4 a reserved hole (rekey-2026-09-18.md). epic-27 backlog -> done. On main: story-status ok, ledger-direction ok, spec-surface ok. Scoped stamp follows: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (event by claude) Co-governor note 2026-09-18 for marshal Story 50.4's landing (PR #1487, `3e2859425c Merge pyforge-marshal/50-4 into main`; spec-pyforge-marshal:CAP-247): its review pass rewired this station's duplicated grammar readers — `sources/marshal.py` and `sources/ledger.py`'s `_MERGE_SUBJECT_TEMPLATE` fallback is now the `{slug}`-scoped repo default `Merge {slug}/{key} into main`, the templated parser runs unconditionally with project_slug (the pre-50.4 has_override guards are redundant: the default self-scopes, and a bare legacy subject can never match a `{slug}` template so it still reaches CAP-80's `attribute_bare_merge` fallback), and `bare_merge.py` calls the 3-arg form. The branch conflicted with 27.5 and was composed by hand (`1a5895317f`); both stories' tests kept (1748 passed). Named follow-up (50.4's own review): no drift check ties these two duplicated constants to marshal policy.py's default — the module-independence rule forbids a shared import, so a doctor-side drift detector is the shape. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/bare_merge.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/ledger.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/marshal.py, src/shared/packages/pyforge-doctor/tests/unit/test_landing_evidence_conformance.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_ledger_direction.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_marshal_story_status.py, _bmad-output/projects/pyforge-doctor/planning-artifacts/marshal-policy.toml (override retired). Scoped stamp follows: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (event by claude) Seed 2026-09-19 (no CAP minted; Dream-append on docs/dreams/pyforge-doctor.md): `sources/chain.py::_frontmatter_parse` splits on the first `---` anywhere in the file, not a line-anchored fence — marshal spec-50-5's quoted `"---"` inside a deferral's evidence truncated the YAML silently (one deferral seen where two exist; the seen one lost its location/fingerprint). Hand-reconciled in marshal's tracked ledger (DW-FU-50-5 rewritten, DW-FU-50-6 added). Waits for its CAP/Story; nothing in this spec's surface changed.
- (capability by claude) CAP-81 — a frontmatter reader that stops at the fence, not at the first dashes. Intent: `sources/chain.py::_frontmatter_parse` — and every doctor source that reads frontmatter through it (spec status, deferrals, surface, ownership) — bounds the YAML block by line-anchored `---` fences (opening fence on the first line, optionally after a `<!-- … -->` banner as marshal's post-CAP-248 readers allow; closing fence a line that is exactly `---`), so a quoted `---` inside a scalar or a `---` horizontal rule in prose never truncates or invents frontmatter, and a block that cannot be bounded is refused (`unparseable`) rather than degraded. Source: docs/dreams/pyforge-doctor.md 2026-09-18 (night) — marshal 50.5's tracked spec quotes a `"---"` fence inside its first deferral's `evidence:`, `text.split('---', 2)` cut the YAML mid-scalar, `yaml.safe_load` still returned a mapping, and the detector saw one deferral where two exist (the second severity high), while `deferred_work_intake.py` reported 'all 118 already in tracked ledger'; second defect in the same helper: `if '---' in text: return {}, True` marks any prose file containing a horizontal rule as unparseable frontmatter.
- (constraint by claude) CAP-81 keeps the helper's refusal semantics (Story 17-1 / FR-144): an unbounded or non-mapping block is still `({}, True)`, never silently `{}` — the fix narrows what counts as a fence, it does not widen what counts as parseable.
- (event by claude) Self-validate pass 1 (coherence) on CAP-81: PASS — intent states WHAT (the block is bounded by fences), success names the live fixture (50.5's tracked spec: two deferrals, fingerprints fdd6bce25c09 / 5434eca8c9e5), a prose-rule negative, an unclosed-fence negative, byte-identical verdicts across the existing caller fixtures, and a mutation check; ID 81 is fresh (80 was the last minted, 2026-09-18).
- (event by claude) Self-validate pass 2 (preservation) on CAP-81: PASS — every claim in the Dream's (night) entry (first-`---`-anywhere split, the hidden second deferral, the lost `location:`/fingerprint, the 'all 118' false report, marshal's line-anchored precedent, 'a reader that degrades silently is worse than one that refuses') lands in CAP-81's intent/success or the constraint; nothing dropped.
- (event by claude) Ready for decomposition 2026-09-19: CAP-81 → doctor Epic 28, Story 28.1 (single story; 27.4 stays a reserved hole per rekey-2026-09-18.md).
- (event by claude) Surface reconcile 2026-09-19 (red-main fallout, doctor dispatch 28.1 refused on MRS-GATE-001): `tests/unit/test_sources_status_body_promissory.py::test_gather_live_herald_pair_and_zero_false_positives` filtered promissory findings by `check` alone, so the moment the live scan became fully clean (PR #1493 quoted the unquoted `title:` in `docs/dreams/pyforge-mason-recipe-validator.md`, removing the last unparseable WARN) the source's clean-state OK summary — same check, `status: OK` — read as a hit. The test now filters on WARN status (the shape a real hit carries) and asserts the clean state explicitly. Source unchanged; no contract moved. Noted for CAP-81's story: `status_body_consistency._parse_frontmatter` is a second copy of the first-`---`-anywhere reader, and the docs-only PR that flipped this live test never fired the station lane (a live-repo test CI cannot see from its paths filter).
- (event by claude) CAP-81 realized 2026-09-19 (Story 28.1, re-derived after review pass 1's bad_spec): `sources/chain.py::_frontmatter_parse` now bounds the block by line-anchored fences — a `<!-- -->` banner skipped (`_skip_leading_banner`, the Story 50.5 port), the opening fence on line 1, the closing fence the first later line that is exactly `---` (`_is_fence`, `rstrip` so an indented `  ---` inside a block scalar stays content), one shared scan (`_split_fenced_block`) that `_dream_body_after_frontmatter` derives the Kinship body from too. Verdicts: unclosed fence and an attempted-but-unbounded opener (glued `---title:`, ` ---`, `--- # c`) stay `({}, True)`; a prose file with a `---` rule, a displaced block, or an unclosed banner is `({}, False)`. Moved: `sources/chain.py`; new `tests/unit/test_sources_chain_frontmatter_parse.py` (the primitive + body boundary) and the byte-verbatim snapshot `tests/fixtures/chain/spec-50-5-the-promoter-reads-a-spec-through-its-banner.md` (marshal's spec at `62c09c2e27`, not a live read); `tests/unit/test_sources_chain_deferred_work.py` (both 50.5 deferrals, fingerprints `3bc3d91bdf95` / `5434eca8c9e5`), `tests/unit/test_sources_chain_dream_chain.py` (the displaced-block fixture is absent, not unparseable — Story 28.1's Always clause supersedes the 17-1 pin for that shape), `tests/unit/test_sources_chain_dreams_hygiene.py` (two body-boundary fixtures; live kinship pin 33 → 31 because `enterprise-airgap.md`'s glued opener is now refused before the scan). Live: `gather_dreams_hygiene` 173 → 185 (+34 `unparseable-frontmatter`, kinship 33 → 31, `readme-table-drift` 73 → 53); `gather_dream_chain` goes from its single `dream-chain` OK summary to 34 `unparseable-frontmatter` WARNs on the same 34 glued-opener Dreams (exit domain unchanged: WARN projects to 0); marshal's tree now lists 119 deferrals with both fingerprints. `status_body_consistency._parse_frontmatter` and `factory.py:631` are the same reader in other capabilities — recorded in the story spec's `deferred:`, not touched here.
- (event by claude) Story 28.1 landed 2026-09-19T10:0xZ (PR #1500, `2348be5a95 Merge pyforge-doctor/28-1 into main`; finalize promoted the ledger as `fc07dd9922`; CAP-81 — a frontmatter reader that stops at the fence): `sources/chain.py::_frontmatter_parse` bounds the block by line-anchored fences and skips a leading banner; a prose file with a rule is `({}, False)`; marshal 50.5's tracked spec is the regression fixture (both deferrals visible). Tests: `tests/unit/test_sources_chain_frontmatter_parse.py` (+294), `test_sources_chain_deferred_work.py`, `test_sources_chain_dream_chain.py`, `test_sources_chain_dreams_hygiene.py`; `docs/detector-incident-log.md` entry. Five deferrals ingested by the session (DW-FU-28-1..-5: `status_body_consistency._parse_frontmatter` is a verbatim pre-CAP-81 copy; `factory.py`'s pin-scope extractor; 34 archived Dreams with a glued opener; unparseable findings name no cause; five other readers close on a stripped line). Landing needed two human acts: the first run was refused on MRS-GATE-001 by a test main itself had red (#1494), and the re-run's land was refused because a sibling landing had appended to the same memlog/baseline after this branch's baseline (spec-pyforge-marshal:CAP-249's case, 51.1 not yet landed) — resolved by merging main into the branch, union of the memlog tails, re-stamp, merge with the templated subject, `dispatch_land_finalize` replayed from the primary. Epic 28 done.
- (event by claude) Surface reconcile 2026-09-19 (marshal Story 52.2, spec-pyforge-core CAP-8 — the conformance suite is a PR gate; this spec co-governs `docs/how-to/**`): `docs/how-to/github-actions-recipe-ci.md` changed in ONE table cell — the Role column for `pyforge-station-tests.yml` now also names the new `core-test` conformance job (pyforge-core's whole suite, sole-ownership meta-tests included; fires on shared-surface OR any station change), which the row's old text 'PyForge station test matrix' omitted. Docs-only; no doctor contract moved. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pyforge-doctor`.
- (event by claude) Story 23.3 reconciled 2026-09-19 (landed 2026-09-18 as PR #1445, a8996d5abe, branch dispatch/pyforge-doctor/23.3 — 25 files: docs/specs/* sunset to archive/docs/specs/ by frontmatter status, bodies of the three workflow stubs moved to docs/how-to/, AGENTS.md / CLAUDE.md / docs/MAP.md / archive/docs/README.md / docs/how-to/README.md pointers): the ledger row stayed backlog and the Tier-3 twin was never promoted, so a 2026-09-19 re-dispatch found story_merged_on_main and a live session started re-doing the story (stopped by hand after 12 min; the merged-story + backlog-row trap). Paths this reconcile moves: planning-artifacts/sprint-status-ledger.yaml (23-3 backlog → done), planning-artifacts/epics.md (23.3 Status + Outcome), planning-artifacts/specs/spec-23-3-sunset-docs-specs-by-frontmatter-status.md (twin recovered from the session transcript — Write + 10 Edits replayed — title/type/Binding kept, Auto Run Result carries the provenance banner). Scoped stamp for spec-pyforge-doctor after this entry.
- (event by claude) Surface reconcile 2026-09-19 (fleet preflight; found when doctor 23.4 and steward 60.1 were refused at MRS-GATE-010 within minutes of each other): every queued pre-authored story spec under planning-artifacts/specs/ gains a '## Verification' → '**Commands:**' section bound to this station's marshal-policy verify_commands exactly (116 story specs on disk; touched: spec-23-4-empty-the-intake-inbox-per-its-own-readme.md spec-23-5-archive-citations-for-the-five-already-moved-_bmad-output-files.md spec-23-6-map-names-publish-roots-do-not-mint-an-empty-vizro-tree.md spec-23-7-a-new-doctor-source-flags-leftover-shelf-occupancy.md spec-24-1-the-evaluator-and-the-thresholds-move-out-of-marshal-s-package-together.md spec-24-2-an-import-linter-contract-catches-the-class-structurally.md spec-24-3-the-surfaces-are-reconciled-so-no-file-is-governed-twice-or-not-at-all.md spec-26-1-a-touched-live-proof-only-surface-gets-an-advisory-doctor-finding-naming-it.md ). The 2026-09-18 mint generated these specs from epics.md without the section, so gate.check_spec_binding saw declared_commands=None; marshal 47.x declared '-k recall' variants not in policy (MRS-GATE-011) — those move to a Manual checks list. Contract text untouched. Scoped stamp after this entry.
- (event by claude) Story 23.4 hand-landed 2026-09-19 (dispatch run pyforge-doctor-20260919T165948464Z-66b6f0c0 refused at MRS-GATE-010 on the mint's missing Verification section; #1517 added it; branch merged with main and landed by hand): docs/intake/{jira-github-projects-sync,secure-live-dashboards,local-ocp-hybrid-environment,gists}/ → archive/docs/intake/ (git mv, content unmodified); docs/intake/README.md dispositions; live pointers repointed in docs/reference/README.md, docs/MAP.md, docs/dreams/README.md, archive/docs/README.md; agentic-sdlc/ and external-repos-analysis-2026-08-22/ stay per the README's own gates. Ledger 23-4 done. Scoped stamp for spec-pyforge-doctor after this entry.
- (event by claude) Story 23.5 landed 2026-09-19 (dispatch run pyforge-doctor-20260919T193513493Z-3e0f163c, merge c83741220a, ledger promote b49ccf497b — the first fully autonomous doctor landing after #1517's binding fix; twin promoted by hand again, spec-pyforge-marshal:CAP-248 / spec-pyforge-marshal:CAP-250 collision): archive citations for the five already-moved _bmad-output/ files — paths: _bmad-output/projects/{pyforge-atlas,pyforge-herald,pyforge-marshal,pyforge-mason,pyforge-steward}/planning-artifacts/specs/README.md, _bmad-output/projects/pyforge-marshal/planning-artifacts/specs/spec-genesis-installer-name-retirement/citation-map.md, presentations/six-quarter-roadmap/project/github.md (herald co-governor). One deferral ingested (no repeatable check encodes 'no live doc cites the old path' — DW-FU-23-5). Same PR: DW-OPS-2026-09-19-6 appended (six sibling Dreams diverge under CAP-71 now that GH_TOKEN reaches the sibling; fold fallout, cross-repo decision pending). Scoped stamp after this entry.
- (direction by claude) 2026-09-19 (evening) update from docs/dreams/pyforge-doctor.md Realization-log entry 'the sibling drift check records a per-Dream human acknowledgement, and knows where the sibling went' — mint CAP-82 (extends CAP-71). Input: doctor DW-OPS-2026-09-19-6 (six folded Dreams diverge on the sibling), the live read of the sibling (renamed to openteams-ai/…, dormant since 2026-08-24, the six untouched since 2026-08-13, status dreamt).
- (capability by claude) CAP-82 — the sibling drift check honours a per-Dream acknowledgement and follows the sibling's rename. Intent: a local Dream may carry sibling-acknowledged: <sibling content_hash> (frontmatter); when the sibling's current content_hash equals it, CAP-71 reports nothing for that Dream; any other hash re-fires with both hashes named, so a genuine later change on the sibling still surfaces. A locally archived Dream with no acknowledgement is reported with its archived status in the message so the operator sees the fold, not a mystery. The sibling coordinates come from one place and read openteams-ai/mgmt-wf-python-modernization (the 301 target), with the old owner named in the docstring as history. Success: the six DW-OPS-2026-09-19-6 Dreams, once acknowledged at their current sibling hashes, produce zero findings on a live run; changing one acknowledged hash in a fixture re-fires exactly that Dream; the unreachable and unauthenticated paths are unchanged (warn, fail-open); tests/unit/test_sources_sibling_dreams.py covers ack-match, ack-mismatch, archived-without-ack, and the renamed owner.
- (constraint by claude) CAP-82 stays read-only and never writes to the sibling (CAP-71's constraint); the acknowledgement lives on the LOCAL Dream's frontmatter only. No content from the sibling is copied — only its content_hash is recorded.
- (event by claude) Self-validate 2026-09-19 (CAP-82): coherence — WHAT (record + honour an acknowledgement; correct coordinates), measurable success (six-Dream live zero, fixture re-fire, unchanged fail-open); preservation — both Dream-entry gaps land in CAP-82; wrapper-only content dropped: none.
- (event by claude) Epic 29 / Story 29.1 minted 2026-09-19 (evening) from CAP-82: planning-artifacts/epics.md (Epic 29), planning-artifacts/sprint-status-ledger.yaml (29-1 backlog, epic-29 backlog), planning-artifacts/specs/spec-29-1-a-per-dream-acknowledgement-silences-exactly-one-sibling-hash-and-the-sibling-coordinates-are-current.md (pre-authored; Verification bound to the station verify_commands per MRS-GATE-010), docs/foundry/capability-ledger.yaml (CAP-82 row), docs/dreams/pyforge-doctor.md (2026-09-19 evening entry), planning-artifacts/deferred-work-ledger.md (DW-OPS-2026-09-19-6 update: acknowledge, not push; sibling renamed and dormant). Scoped stamp after this entry.
- (event by claude) Surface reconcile 2026-09-19 (Story 23.6, spec-docs-shelf-alignment CAP-6): docs/MAP.md gains an 'Outside this map' row for docs/dashboard/ (publish root, one subfolder per published dashboard board; today only kedro-viz publishes, a vizro/ subfolder is minted only when that board actually publishes, never ahead of time). No CAP text change. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (event by claude) Story 23.6 landed 2026-09-19 (dispatch run pyforge-doctor-20260919T202708157Z-8883dc9e; PR #1527 refused by the engine on a memlog/baseline conflict with main after #1524/#1525/#1526, resolved by hand — union memlogs, main's baseline, scoped re-stamps): docs/MAP.md names the publish roots; docs/dashboard/README.md — no empty vizro/ tree is minted. Deferral(s) ingested by this landing. Scoped stamp after this entry.
- (event by claude) Story 23.7 realized 2026-09-19 (CAP-54; absorbed from spec-docs-shelf-alignment CAP-7 on 2026-09-17): new sources/docs_shelf.py -- find_bmad_output_root_leftovers (a non-allow-listed entry at _bmad-output/ root, e.g. a re-added dated campaign note) and find_extra_airgap_docs (an air-gap-named doc outside Story 23.2's two-file cluster, scanned across docs/MAP.md's four Diataxis quadrant dirs) -- both warn-only, fail-open, never FAIL; gather() falls back to one OK when clean. New Source.DOCS_SHELF_OCCUPANCY member + REGISTRY row (subject=fleet, owner=doctor, same rationale as GENERAL_DOCS_CONSISTENCY) + DISPATCH entry + report-schema.json enum entry. New pixi task docs-shelf-occupancy-check, wired into scripts/detectors.py's _DOCTOR_SOURCE_TASKS (outside this spec's own surface). 15 new tests in tests/unit/test_sources_docs_shelf.py (clean fixture, dirty tmp_path cases for both checks, live-repo proof) plus tests/fixtures/docs_shelf/clean/**; existing meta/closed-set tests updated: test_models.py's closed-set assertion, test_sources_dispatch.py's _EXPECTED_DISPATCH, test_source_independence.py's SOURCE_MODULE map. Verified: pyforge-doctor-test 1804 passed; live docs-shelf-occupancy-check reports ok on main's actual _bmad-output/ root and air-gap cluster; manual tmp_path probe fires both WARN findings with quoted paths as the spec's I/O matrix requires. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/docs_shelf.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__main__.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/data/report-schema.json, src/shared/packages/pyforge-doctor/tests/unit/test_models.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_dispatch.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_docs_shelf.py, src/shared/packages/pyforge-doctor/tests/meta/test_source_independence.py, src/shared/packages/pyforge-doctor/tests/fixtures/docs_shelf/clean/_bmad-output/PROJECTS.md, src/shared/packages/pyforge-doctor/tests/fixtures/docs_shelf/clean/docs/how-to/air-gapped-mirror-setup.md, src/shared/packages/pyforge-doctor/tests/fixtures/docs_shelf/clean/docs/explanation/airgap-distribution-contract.md. Scoped stamp follows: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (event) Surface reconcile 2026-09-19: updated Diátaxis docs and package READMEs
- (event) Surface reconcile 2026-09-19: added new Operator and Framework guides
- (direction by claude) 2026-09-19 (night) update from docs/dreams/pyforge-doctor.md Realization-log entry 'the documentation is right, and refreshing it is repeatable' (seeded at the review of PR #1529). Input: planning-artifacts/research/documentation-currency-and-repeatable-refresh-2026-09-19.md. Mint CAP-83 (this PR realizes it) and CAP-84 (ready; Stories 30.2–30.3). The PR's own two hand-appended '(event) Surface reconcile 2026-09-19: …' lines on this log named no path and were the basis of ten laundering re-stamps; superseded by the landing entry below, which names every path.
- (decision by claude) Operator rulings 2026-09-19 (night): (1) the 130 README.md stubs inside .claude/skills/*/ are removed — no harness reads a README in a skill dir, the BMAD installer regenerates and retires those dirs (71 deletions in the 6.10→6.11 upgrade), the SKF exporter regenerates pyforge-*; a human skills index is one generated page, not 130 stubs; (2) the documentation registry is docs/map.yaml, a machine twin of docs/MAP.md (MAP.md rendered from it so they cannot drift); (3) doctor owns the docs-currency chain end-to-end (it owns CAP-48..54 and CAP-60..65); station generators for a station's own surface are that station's, registered in the doctor registry; (4) the 14 authored pages PR #1529 added are kept, corrected now, and registered with sources:/verified: frontmatter.
- (capability by claude) CAP-83 — docs/MAP.md is enforced within its own scope. Intent: a Doctor source (docs-map-hygiene) reads docs/MAP.md and the four Diátaxis quadrant directories (docs/tutorials, docs/how-to, docs/reference, docs/explanation) — exactly the scope MAP.md § Outside this map leaves inside — and reports a MAP link to a page that does not exist (fail) and a quadrant page MAP does not list (warn; a section index README.md directly inside a quadrant is exempt). Nothing outside the quadrants (governance, intake, dashboard, foundry, station READMEs, skill dirs, Dreams, Tier-1/2/3) is scanned. Success: on main the source reports OK; adding an unmapped how-to reds a warn naming it; removing a mapped page reds a fail naming the dead link; a unit test covers each branch; the source is in detectors-ci through scripts/detectors.py; CAP-62's warn-first posture holds for the unmapped class until Story 30.2 promotes it.
- (capability by claude) CAP-84 — documentation currency: every mapped page is generated, authored-and-verified, or a pointer, and the change that invalidates a page reds the PR that made it. Intent: docs/map.yaml (machine twin of MAP.md; MAP.md rendered from it) records for every page inside the map's scope its quadrant, owner station, kind ∈ {generated, authored, pointer}, sources (files or commands it derives from or explains) and, for generated pages, a derived_at + tree stamp (herald's facts.yaml shape). Generated pages are never hand-edited: a docs-<name> pixi task regenerates each — the pixi-task reference from pixi.toml, the station CLI cheat sheet from each station CLI's --help, the detector table from scripts/detectors.py + doctor.sources registrations, the skills catalog from SKILL.md frontmatter, the environments table from [environments]. Authored pages carry sources: and verified: <date> <tree> frontmatter and are refreshed by the bmad-os-docs-audit → bmad-os-diataxis pass, triggered by a finding, not memory. One Doctor source docs-currency reds (a) a generated page whose source is newer than its stamp or whose regeneration differs, (b) an authored page whose named source moved past verified: or whose backticked path / pixi task / CLI grammar no longer resolves (governance-currency's rule applied to every mapped page), (c) a file outside the Agent Skills layout inside a managed skill dir. Success: map.yaml validates and MAP.md is byte-identical to its render; each generator is idempotent on an unchanged tree and its page carries a fresh stamp; on main docs-currency reports OK; editing pixi.toml's tasks without regenerating the task reference reds detectors-ci; the four checks start warn and are promoted to fail individually once green (the coverage-gate ratchet); llms-full-check keeps its own lane.
- (constraint by claude) Docs detectors keep CAP-62's posture — warn first, fail-open when a source cannot be read; promotion to fail is per check, recorded on this log. Generated pages are derived, never hand-edited (a hand edit is a finding, as for library-llms-full.md). No page under docs/ is copied into a per-tool instruction file (scribe CAP-27). Skill directories hold only the Agent Skills layout (SKILL.md, scripts/, references/, assets/).
- (event by claude) Self-validate 2026-09-19 (CAP-83, CAP-84): coherence — both intents are WHAT (enforced map; currency by construction), success signals name a source, a test and a live verdict; constraints rule out hand-editing generated pages, stray skill-dir files and per-tool copies; preservation — every load-bearing claim of the Dream entry and research § 3 lands in a CAP or the constraint; wrapper-only content dropped: PR #1529's 'Deterministic Enforceability' sketch (superseded by CAP-83) and its per-station 'updated Diátaxis docs' lines (named no path).
- (event by claude) Story 30.1 landed 2026-09-19 (PR #1529 reviewed and rebuilt; CAP-83 met): src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/docs_map_hygiene.py (rescoped: four quadrants, missing=fail, unmapped=warn, index exempt), models.py, sources/__init__.py, sources/__main__.py, data/report-schema.json, tests/unit/test_sources_docs_map_hygiene.py (added, 11), tests/unit/{test_models.py,test_sources_dispatch.py}, tests/meta/test_source_independence.py, tests/unit/test_sources_capability_effect_caller_reach.py (+3, coverage floor), scripts/detectors.py, pixi.toml (docs-map-hygiene-check description), docs/MAP.md (nine gaps mapped; docs/foundry/ outside the map), the fourteen pages docs/explanation/{pyforge-estate-overview,pyforge-ecosystem-architecture,the-detector-framework,the-tier-model-and-data-flow}.md, docs/how-to/{ai-engine-operations,manage-worktrees-with-bmad,monitor-the-fleet,reconcile-spec-surface,run-and-understand-detectors,station-cli-operations,troubleshoot-bmad-agent-loops}.md, docs/reference/{agent-memory-lifecycle,station-cheat-sheet}.md, docs/tutorials/local-platform-development.md (errors corrected; sources:/verified: frontmatter) plus docs/how-to/pixi-tasks.md (pre-existing, corrected), the five relocated platform runbooks docs/how-to/{disaster-recovery,ocp-cluster-bringup,restore-operations}.md and docs/explanation/{platform-deployment-architecture,platform-requirements}.md (moved by the PR from src/platform/deploy and requirements — the Story 22.5 direction), .claude/skills/*/README.md ×130 removed, .claude/skills/bmad-os-docs-audit/SKILL.md (added by the PR, kept), planning-artifacts/{epics.md (Epic 30), sprint-status-ledger.yaml (30-1 done, 30-2/30-3 backlog, epic-30 in-progress), specs/spec-30-1-*.md, specs/spec-30-2-*.md, specs/spec-30-3-*.md, research/documentation-currency-and-repeatable-refresh-2026-09-19.md}, docs/dreams/pyforge-doctor.md (2026-09-19 night entry), docs/foundry/capability-ledger.yaml (CAP-83/84). The PR's specs/spec-29-1-check-docs-map.md sketch removed. Same PR, Story 23.7's landing record (merge ca2077fa5e, ledger promote 76c33a409e; twin promoted by hand — the session left its tracked spec uncommitted): sources/docs_shelf.py (added), the docs_shelf fixtures under tests/fixtures/docs_shelf/, tests/meta/test_source_independence.py; Epic 23 complete. DW-FU-30-1, -2, -3 ingested. Scoped stamp after this entry.
- (note by claude) Story 30.1 path manifest (expanded for the spec-surface detector; same landing as the previous entry): docs/explanation/pyforge-estate-overview.md, docs/explanation/pyforge-ecosystem-architecture.md, docs/explanation/the-detector-framework.md, docs/explanation/the-tier-model-and-data-flow.md, docs/explanation/platform-deployment-architecture.md, docs/explanation/platform-requirements.md, docs/how-to/ai-engine-operations.md, docs/how-to/manage-worktrees-with-bmad.md, docs/how-to/monitor-the-fleet.md, docs/how-to/reconcile-spec-surface.md, docs/how-to/run-and-understand-detectors.md, docs/how-to/station-cli-operations.md, docs/how-to/troubleshoot-bmad-agent-loops.md, docs/how-to/disaster-recovery.md, docs/how-to/ocp-cluster-bringup.md, docs/how-to/restore-operations.md, docs/how-to/pixi-tasks.md, docs/reference/agent-memory-lifecycle.md, docs/reference/station-cheat-sheet.md, docs/tutorials/local-platform-development.md, docs/MAP.md, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/docs_map_hygiene.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_docs_map_hygiene.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_capability_effect_caller_reach.py, src/shared/packages/pyforge-doctor/tests/unit/test_models.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_dispatch.py, src/shared/packages/pyforge-doctor/tests/meta/test_source_independence.py, src/shared/packages/pyforge-doctor/data/report-schema.json, src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__main__.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/docs_shelf.py (23.7).
- (note by claude) Correction to the Story 30.1 landing entry: Story 23.7's tracked spec twin (Auto Run Result) was already in merge ca2077fa5e — main's copy and the hand-promoted one are identical; no hand-promotion was needed. The 23.7 code paths are reconciled by 23.7's own entry above; this branch merged origin/main at 76c33a409e so both docs-map-hygiene and docs-shelf-occupancy register (taxonomy, dispatch, schema, detectors.py unioned).
- (decision by claude) CAP-77 companion refined 2026-09-20 after dispatch run pyforge-doctor-20260919T233255320Z-8f2b958e (Story 26.1) halted on an intent gap found by four independent review layers: three catalog rows (scribe's Postgres cluster, herald's deck sync-all, guild-container) had no natural per-row path, the implementation fell back to bare-substring keywords ('cluster', 'deck', 'docker') and matched docs/how-to/ocp-cluster-bringup.md, docs/how-to/presentation-deck.md and recipes/docker/recipe.yaml — real false positives against the live tree. Decision (option b + a of the run's question 1; yes to question 2): live-proof-surfaces.md gains a hand-authored 'Surface globs' column on every row (herald transport/** + deck_pipeline.py; sync_all.py; webhook_host.py + herald-live-demo.yml; scripts/deck_export.py; scribe graph_store_pg.py + embeddings.py; atlas wasm/** + duckdb_writer.py + dashboard/**; warden feeds.py + vuln.py; the Containerfiles + .dockerignore + scripts/container-gates*); the source matches changed paths against that column and nothing else; a row with an empty cell is never diff-matched; ZERO false positives against the current catalog and the current tree is a hard, testable gate (a live-tree test walks git ls-files against every glob and asserts each hit belongs to the row's station). Story 26.1's tracked spec amended (Approach, Always, matrix, Binding surface now names the companion), status back to ready with the blocked run's triage log kept; ledger row stays backlog for re-dispatch. Saved attempt: implementation-artifacts/26-1-live-proof-surface-attempted-change.patch (primary's Tier-3) — its parser and wiring are reusable, its keyword fallback is not.
- (event) Story 26.1 implemented 2026-09-20 (fresh attempt off the 2026-09-20 amendment, all-glob-column resolution): new src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/live_proof_surfaces.py -- parse_catalog reads live-proof-surfaces.md's own Surface globs column (backtick-quoted tokens) only, _glob_to_re mirrors chain.py's spec-surface glob semantics (** spans separators, */? do not), _row_match matches real glob patterns against origin/main..HEAD changed paths (never a keyword/substring), gather() reports one WARN per matched row quoting its how_to_prove cell verbatim (zero branching for the atlas no-mechanism-yet row -- the catalog's own prose does the work), zero matches -> zero findings, malformed table or unresolvable git ref degrades to one generic WARN via degrade_on_exception. New Source.LIVE_PROOF_SURFACE member (models.py) + REGISTRY row (subject=fleet, owner=doctor, sources/__init__.py) + DISPATCH entry (sources/__main__.py) + report-schema.json enum entry. 22 new tests in tests/unit/test_sources_live_proof_surfaces.py: parse_catalog (header/separator/empty-glob-cell/malformed/table-end), _glob_to_re, _row_match (keyword-immunity, empty-globs, changed-paths order), gather() I/O matrix fixtures (match+quote, no-match, the three named false positives, empty-glob row, no-mechanism-yet row, malformed catalog, unresolvable ref, missing catalog, non-repo target), and the Boundaries' own hard gate test_zero_false_positives_against_the_live_tracked_tree (walks git ls-files against every REAL row's globs, cross-checks each hit against an independent pathlib.glob() oracle, pins the three named false positives absent) -- passed against the live tree. Existing closed-set/dispatch/independence tests updated: tests/unit/test_models.py, tests/unit/test_sources_dispatch.py, tests/meta/test_source_independence.py. New pixi task live-proof-surface-check + scripts/detectors.py _DOCTOR_SOURCE_TASKS row (outside this spec's own surface, same as docs-shelf-occupancy-check's precedent). Verified: pyforge-doctor-test 1853 passed, 1 pre-existing skip (up from the 2026-09-19 blocked attempt's 1825 baseline); environment.yaml unaffected by the pixi.toml task-only change. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/live_proof_surfaces.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__main__.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/data/report-schema.json, src/shared/packages/pyforge-doctor/tests/unit/test_models.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_dispatch.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_live_proof_surfaces.py, src/shared/packages/pyforge-doctor/tests/meta/test_source_independence.py. Scoped stamp follows: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (event) Story 26.1 review-pass patches 2026-09-20 (same landing as the implementation entry above): src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/live_proof_surfaces.py (added CatalogRow.cost + parse_catalog cost/blank-line fixes), src/shared/packages/pyforge-doctor/tests/unit/test_sources_live_proof_surfaces.py (cost-field assertions, station-attribution hard-gate assertion with a herald named-exception carve-out, one new multi-row/multi-glob combinatorial test) -- 4-layer review findings applied per the spec's own Review Triage Log. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pyforge-doctor.
- (note by claude) specs/spec-30-1-*.md final_revision set to f4d693ffe8 (the merge of PR #1529) — bookkeeping only.
- (event by claude) Story 30.2 landed 2026-09-20 (CAP-84 met): docs/map.yaml (new, 54-page registry) + JSON Schema src/shared/packages/pyforge-doctor/src/pyforge/doctor/data/docs-map-schema.json (new); new source docs_currency.py (map-render / authored-page-stale / skill-dir-hygiene, all WARN-only fail-open) + tests/unit/test_sources_docs_currency.py (new, 15 tests); docs_map_hygiene.py's unmapped class promoted WARN->FAIL (kept beside docs_currency.py, not retired) + tests/unit/test_sources_docs_map_hygiene.py updated; models.py (Source.DOCS_CURRENCY), sources/__init__.py (REGISTRY row), sources/__main__.py (DISPATCH entry), data/report-schema.json (source enum), scripts/detectors.py (docs-currency-check tuple entry), pixi.toml (docs-currency-check + docs-map-render tasks, docs-map-hygiene-check description refreshed), tests/meta/test_source_independence.py + tests/unit/test_models.py + tests/unit/test_sources_dispatch.py (closed-set/dispatch updates); new scripts/docs_map_render.py (write-side mutator, mirrors scripts/fr_baseline.py's sys.path pattern); docs/MAP.md gains the generated '## Page registry' section (docs-map:registry:begin/end markers) produced by running docs-map-render, not hand-typed. Real docs drift the new authored-page-stale check surfaced and fixed in the same landing: bmad-quick-dev -> bmad-build (feedstock-failure-remediation.md, feedstock-platform-expansion.md); src/platform/deploy/README.md -> docs/explanation/platform-deployment-architecture.md (ocp-cluster-bringup.md, enterprise-deployment.md); docs/dreams/design-code-bridge.md -> docs/dreams/pyforge-herald.md (presentation-deck.md, absorbed 2026-08-08); scripts/linter.py -> .github/workflows/scripts/linter.py (github-workflows.md, wrapped historical removed-file prose in governance-currency:ignore-start/end); presentation-deck.md's per-deck-relative src/... template paths wrapped in one governance-currency:ignore-start/end span (false-positive class the repo-root-anchored heuristic cannot resolve); reconcile-spec-surface.md/run-and-understand-detectors.md/the-detector-framework.md verified: bumped to 2026-09-20 (re-verified against this story's own scripts/detectors.py + sources/__init__.py edits). library-llms-full.md's stale/historical refs (docs/specs/pyforge-warden.md, docs/specs/bmad-loop-adoption.md -> archive/; bmad-dev-auto, bmad-method-wds-expansion, bmad-story-automator wrapped as historical) fixed under pyforge-marshal/spec-pyforge-marshal's own surface, reconciled separately. Design deviation recorded in docs_currency.py's module docstring: skill-dir-hygiene targets a stray README.md specifically, not the Code Map's literal {SKILL.md, scripts/, references/, assets/} allowlist -- that allowlist false-positives on the live .claude/skills/ tree (bmad-* carries customize.toml + extensive per-skill scaffolding as its norm; pyforge-* is the SKF-exported versioned/symlink layout with no top-level SKILL.md at all) and would violate this story's own AC2. Verified: pyforge-doctor-test 1885 passed/1 skipped; docs-currency-check + docs-map-hygiene-check report OK live on this tree; docs/map.yaml validates against its schema. Scoped stamp after this entry (surface: gains docs/map.yaml, scripts/docs_map_render.py).
- (event) Story 30.2 follow-up fix 2026-09-20: (1) src/shared/packages/pyforge-doctor/src/pyforge/doctor/data/docs-map-schema.json existed on disk but was invisible to git add -A -- an unanchored .gitignore data/ rule (repo root, governed by pyforge-herald/spec-pyforge-pages) matches at ANY depth and silently swallows every station's own package data/ dir; force-tracked here after anchoring the rule to /data/ (see that spec's own memlog entry). (2) docs-currency-check surfaced 7 genuine stale-source WARNs: docs/tutorials/local-platform-development.md, docs/how-to/ai-engine-operations.md, docs/how-to/monitor-the-fleet.md, docs/how-to/station-cli-operations.md, docs/reference/station-cheat-sheet.md, docs/explanation/pyforge-ecosystem-architecture.md, docs/explanation/the-tier-model-and-data-flow.md -- each cites pixi.toml as a source with verified: 2026-09-19, and this story's own pixi.toml edit (the two new guild-tasks entries) today postdates that stamp. Re-verified each page's claims about pixi.toml are unaffected by the purely-additive task change; bumped verified: to 2026-09-20 on all seven. docs-currency-check now reports OK on this branch, matching the story's own AC2.
- (event) Story 30.2 review pass 1 patches 2026-09-20 (spec-pyforge-doctor:CAP-84): scripts/docs_map_render.py now validates docs/map.yaml via docs_currency.load_map_yaml before rendering (invalid YAML / a page missing a required key / a missing pages key now return 1 with a clean message instead of crashing uncaught or silently writing an empty registry); its marker-detection/splice logic moved into a new shared pyforge.doctor.sources.docs_currency.splice_registry_section (docs_map_render.py no longer carries an independent copy of the begin/end constants and index arithmetic); docs_currency.py's _stale_sources now flags a sources: entry whose path was never committed to git (previously silently treated as current); new tests/scripts/test_docs_map_render.py (9 tests, pytest.importorskip("jsonschema") guarded since pyforge-ci lacks it — real coverage runs via the new docs-map-render-test guild-tasks entry, wired into pr-preflight); docs/how-to/run-and-understand-detectors.md gained a Docs currency (docs-currency-check) remediation subsection beside the existing Docs map hygiene one, and corrected its now-stale unmapped-is-warn wording to fail (Story 30.2's own promotion). Verified: pyforge-doctor-test 1887 passed/1 skipped; docs-map-render-test 9 passed (pyforge-guild); pyforge-doctor-scripts-test (pyforge-ci) 531 passed/5 pre-existing skipped + this file's 1 clean module-skip, exit 0 -- confirms the jsonschema-dependency regression this same pass introduced-then-fixed does not break the lean env's own lane. docs-currency-check + docs-map-hygiene-check both still report OK live on this tree.
- (event by claude) Story 29.1 landed 2026-09-20 (dispatch run pyforge-doctor-20260920T073621884Z-8d90cec5, merge 7c04dfb415, ledger promote fcf67e7a0d — autonomous; CAP-82 met, Epic 29 complete): src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/sibling_dreams.py (per-Dream acknowledgement silences exactly one sibling hash), src/shared/packages/pyforge-doctor/tests/unit/test_sources_sibling_dreams.py, the six acknowledged Dreams docs/dreams/django-accelerator-framework.md, docs/dreams/enterprise-data-models-and-apis.md, docs/dreams/miniforge-installer.md, docs/dreams/package-inventory-eligibility.md, docs/dreams/pixi-container-image.md, docs/dreams/reusable-cicd-workflows.md, and docs/dreams/pyforge-doctor.md, _bmad-output/projects/pyforge-doctor/planning-artifacts/specs/spec-29-1-*.md. The session reconciled no memlog; this entry is the landing ritual's.
- (event by claude) Surface reconcile 2026-09-20 (docs shelf co-governor): (a) scribe Story 19.3 (spec-pyforge-scribe:CAP-29) — docs/how-to/configure-your-coding-agent.md (added through bmad-os-diataxis, sources:/verified: 2026-09-20), docs/reference/agent-memory-lifecycle.md (one sentence + sources + verified: 2026-09-20), docs/MAP.md (the how-to row); docs-map-hygiene ok; (b) steward Story 65.2 (spec-pyforge-steward:CAP-150, merge 4c627a00a4) — docs/how-to/monitor-the-fleet.md (the one-command answer, sources: updated).
- (event by claude) Surface reconcile 2026-09-20: docs/_STYLE_GUIDE.md added (BMAD-METHOD's style guide vendored verbatim with provenance, MIT; the bmad-os-diataxis skill reads it — the shelf's writing rules, so this Spec governs it: surface: gains the path). Seeded on the herald Dream (the docs site matches BMAD-METHOD's pattern) with Kinship to this chain's 30.x registry; no doctor story yet.
- (event by claude) Surface reconcile 2026-09-20 (docs shelf, steward 63.6 in PR #1551): docs/how-to/presentation-deck.md (six pixi run sites: -e local-recipes deck-export / deck-facts / deck-trio -> -e pyforge-guild; verified: 2026-09-20), docs/how-to/recipe-testing-and-builds.md (four platform-ci-local sites -> -e pyforge-guild; verified: 2026-09-20). The tasks moved into [feature.guild-tasks.tasks] so both envs run them; the how-tos name the runtime one.
- (event by claude) Surface reconcile 2026-09-20 (pre-existing CAP-3 audit finding, fix-now): src/shared/packages/pyforge-doctor/tests/unit/test_sources_board_chain_layers_audit.py gains test_every_stage_glob_is_inside_the_git_date_index (every fleet_scan stage glob, for every live chain, sits under a _GIT_SCOPES prefix — the structural class) and test_root_agents_md_is_dated_by_the_git_index (live tree: AGENTS.md dates from the git index). The existing fixture test could not see the defect: a tmp_path is not a git repo and its files carry no dates, so no stage is reached, required collapses to [dream], and 'context not in gaps' held vacuously. Fix itself in scripts/fleet_scan.py (governed by spec-pyforge-marshal): root AGENTS.md added to _GIT_SCOPES.
- (event by claude) Merge reconcile 2026-09-20: origin/main (PR #1551, 5ff2a4805a) merged into dispatch/pyforge-doctor/30.2 before landing; this memlog's two branches unioned in order, main's updated: kept; scripts/.spec-surface-baseline.json taken from main and this Spec re-stamped scoped.
- (event by claude) Surface reconcile 2026-09-20 (30.2 landing, after the #1551 merge): docs/map.yaml gains how-to/configure-your-coding-agent.md (owner scribe, kind authored — the page #1551 landed while 30.2 was in flight; the registry must list every live quadrant page), docs/MAP.md's generated Page registry re-rendered (55 pages) with docs-map-render.
- (event by claude) Surface reconcile 2026-09-20 (30.2 status fallout): planning-artifacts/specs/spec-30-2-docs-map-yaml-is-the-registry-map-md-is-its-render-and-docs-currency-reds-a-stale-page.md frontmatter status in-review -> done, final_revision e2105fec02 (the PR #1550 merge); the ledger row was promoted to done by dispatch_land_finalize (21f9758741) while the spec's own status lagged.
- (event by claude) Surface reconcile 2026-09-20 (fleet consistency pass before the foundry cutover; operator ruling: consistency even where historical records are rewritten): 36 tracked story specs under planning-artifacts/specs/ reconciled against sprint-status-ledger.yaml: frontmatter status equals the ledger row (done rows: done; blocked/backlog/in-progress rows: a status in that row's vocabulary), the Ledger-status line matches, and every done story carries an Auto Run Result (reconstructed from main's landing commits where none survived, marked reconstructed; no-evidence cases stated as such). Keys: 10-1, 12-1, 12-2, 12-3, 13-1, 16-1, 19-1, 2-1, 2-2, 2-3, 23-1, 23-2, 24-1, 25-1, 25-2, 25-3, 27-1, 27-3, 3-1, 3-2, 3-3, 3-4, 30-2, 4-1, 4-2, 4-3, 4-4, 5-1, 5-2, 6-1, 6-10, 6-7, 6-8, 6-9, 8-6, 8-7. Also: spec-24-1 frontmatter made valid YAML (marshal's escaped); spec-27-1 / spec-27-3 run-result Status blocked -> done citing the promoting commits; spec-30-2 status in-review -> done (final e2105fec02); sprint-status-ledger.yaml re-synced by sprint-ledger-sync (epic-23, epic-26, epic-29 -> done: every story done).
- (event by claude) Surface reconcile 2026-09-20 (operator ruling: every station's PRD re-stamped in the fleet consistency pass, grace period or not): prds/prd-pyforge-doctor-2026-07-25/prd.md, architecture/architecture-pyforge-doctor-2026-07-25/ARCHITECTURE-SPINE.md, epics.md each gain a 'Currency reconciliation 2026-09-20 (fleet consistency pass)' note and updated: 2026-09-20 (spec->prd->arch->epics; bookkeeping, no requirement / AD / story moved).
- (event by claude) Surface reconcile 2026-09-20 (steward Story 66.1, spec-pyforge-steward:CAP-153, PR #1553): src/shared/packages/pyforge-doctor/** -- Surface reconcile 2026-09-20 (steward Story 66.1, spec-pyforge-steward spec-pyforge-steward:CAP-153 -- the ten-package lint / type gate landed in PR #1553): pyproject.toml gains [tool.ruff] (py314, line-length 120, E/F/W/I, E501 off) and [tool.mypy] (3.14, explicit_package_bases, import-untyped disabled; the 2026-09-20 baseline as [[tool.mypy.overrides]] per module, per error code); every .py under src/ and tests/ import-sorted (I001) and reformatted by `ruff format` -- semantics-preserving, verified by the station suite. Station-specific fix: src/pyforge/doctor/sources/pixi_currency.py (unused `end`). 10 modules baselined.
- (event by claude) Surface reconcile 2026-09-20 (code->retro currency edge after spec-pyforge-steward:CAP-153 landed, PR #1553): retros/retro-pyforge-doctor-2026-09-20.md (NEW) records what the fleet lint / type gate's first run found in this station -- its mypy baseline size, the lint findings fixed by hand, and the carry-forward (lint-types before pushing; drop unused-ignore comments in baselined modules).
- (event by claude) Surface reconcile 2026-09-24 (doctor Story 24.2, spec-coverage-gate-independence CAP-3): src/shared/packages/pyforge-doctor/tests/meta/test_coverage_gate_stays_outside_every_station.py (NEW) — the structural import-linter-class contract: scans every `pyforge-<station>` package's own `src/pyforge/<station>/` tree (all eight, doctor included) for a module that defines or imports the coverage-gate evaluator Story 24.1 (CAP-1/CAP-2) relocated to `scripts/coverage_gate.py`; fails on a planted `pyforge.<station>.coverage_gate` shim (parametrized across all eight stations, plus a PEP 420 namespace-package form and a dynamic `importlib.import_module`/`__import__` variant added in review), passes on the real tree; docstring cites Charter §5/§6 and this Spec. A CI-coverage-gap review finding (the doctor-test CI job only fires on doctor's own changed path, so a PR touching exactly one other non-marshal station would never run this check) is closed by a leaner companion copy in `pyforge-core/tests/meta/` (co-governed by spec-pyforge-steward/spec-pyforge-unifying-strategy, reconciled there separately) — `pyforge-core-test` already runs on any single-station change (Story 52.2). Scoped stamp for this key follows (`--write-baseline --spec pyforge-doctor/spec-pyforge-doctor`), never bare.
