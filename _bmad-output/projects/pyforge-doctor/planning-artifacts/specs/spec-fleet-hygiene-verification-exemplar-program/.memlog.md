---
topic: Fleet hygiene, verification & exemplar-standard catalog -- stop re-discovering the same shape of gap in isolated sessions
updated: 2026-08-22T05:38
---

- (direction) Source: docs/dreams/fleet-hygiene-verification-exemplar-program.md (owner: doctor, status: dreamt). UNUSUAL SHAPE: this Dream is explicitly a CATALOG, not a single buildable capability -- its own 'What this Dream is NOT' section states 'Not a commitment to build all of this. Not a spec -- none of these categories are sized, prioritized, or scoped for implementation here.' Distillation must honor that: extract only the catalog-maintenance meta-capability plus items explicitly flagged as concrete/ready, and route everything else to Non-goals/Open Questions rather than inventing scope the source declined to scope.
- (decision) Why: tonight's deferred-work audit found a real gap (58+ Tier-3 findings invisible to the detector meant to catch exactly that) AND a pattern -- this fleet has repeatedly needed a one-off, by-hand sweep to catch something no always-on gate watches for, and named tooling gaps go unfixed for six weeks until independently rediscovered (normalize_deferred_ledgers.py's heading-less blind spot: named 2026-07-30, hit again independently 2026-08-15). The goal is not to build every catalogued item -- it is to stop re-discovering the same shape of gap in isolated sessions by making the landscape visible in one place first.
- (decision) Convergence check against already-owned scope, done before drafting capabilities (this Spec's own Constraint): Category 2's 3 items already have their own Dream/Spec each -- deferred-work-audit-completeness (folded into spec-deferred-work-visibility CAP-4..7), deferred-work-resolution-sweep (sibling Spec authored this same session), bmad-loop-liveness-footgun (marshal-owned, not doctor's). Category 3's bmad-output-hygiene generalization = spec-deferred-work-visibility CAP-8/9 (Epic 9, Story 9.2/9.3, already in progress -- Story 9.1 is running as this Spec is being authored). Category 3/4's loop-home branch staleness = spec-deferred-work-visibility CAP-10 (Epic 9, Story 9.4). Category 4's deferred-work verification staleness = the sibling Spec's own CAP-7. None of that is this Spec's territory -- re-specifying it would duplicate Epic 9 and the sibling Spec.
- (capability) CAP-1 -- the catalog stays a maintained, current artifact. intent: an operator/agent facing a 'nothing actually checks for X' moment can check one document first and get one of three answers -- already catalogued and specced, catalogued but not yet built, or genuinely new -- rather than rediscovering it as a fresh, isolated finding six weeks later. success: given a newly-discovered hygiene/verification/audit gap, checking the catalog against its six categories returns a match or confirms genuine novelty; the catalog cross-references and stays in sync with which items have moved from cataloged to specced to shipped (e.g. this Spec's own existence should be reflected back onto the catalog's Category 5 entry once it lands).
- (capability) CAP-2 -- EXEMPLAR-STANDARD.md's conformance table is refreshed and re-verified. intent: the fleet's own designated reference for what done-and-clean means (_bmad-output/EXEMPLAR-STANDARD.md) stops reporting a stale, wrong table -- flagged directly in the source Dream as the single most concrete, immediately-actionable, ready-now item, 'arguably before anything else here, since it's the one place in Category 5 that already has teeth.' success: given the DW-ledger column currently shows only pyforge-atlas compliant while 7 other projects now carry real, git-tracked deferred-work-ledger.md files (warden 43, atlas 122, marshal 109, mason 48, steward 74, herald 45, doctor 20, scribe 7, as of this session), running the refresh corrects the table to the live state, and the other columns (companions, story specs, delivery records, README) are checked for the same staleness, not just this one row.
- (capability) CAP-3 -- chain-completeness's INV-A parses capability ids, not a bare substring match. intent: 'this open Spec is decomposed' is judged by whether the Spec's real CAP-N ids trace to actual stories, not by whether the Spec's slug merely appears somewhere in PRD/epics prose (board.py:463-477's current bare substring test). success: given a Spec that grew from 3 capabilities to 10 with only 3 ever decomposed into stories, the detector reports the real 7-capability gap instead of ok -- reproduces and fixes the exact DW-CHAIN-COMPLETENESS-1 finding logged against spec-deferred-work-visibility this session, found within hours of the sibling deferred-work blind spot as the same recurring defect shape.
- (capability) CAP-4 -- dream-chain's gap count surfaces in fleet-picture's ambient ATTENTION block. intent: an unspecced Dream (e.g. bmad-loop-liveness-footgun, sitting unspecced today) nags quietly in the report an operator already checks every landing pass, instead of waiting for a separate dream-chain detector run to be remembered and invoked. success: given N Dreams fleet-wide with no Spec, fleet-picture's ATTENTION block names the count without requiring a separate detector invocation -- same ambient-signal shape fleet-picture already uses for the baseline-drift check added this same session.
- (capability) CAP-5 -- spec_surface_check.py --write-baseline's read-modify-write race is closed. intent: two concurrent --write-baseline invocations against scripts/.spec-surface-baseline.json do not silently clobber each other's write -- a real risk given this repo's own documented history of concurrent-agent races on shared per-worktree state. success: given two concurrent --write-baseline calls, the race is closed (locked, atomic, or serialized) such that neither write is silently lost -- reproduces and fixes DW-13-5-2, named as a deferred finding by this session's atlas promotion.
- (constraint) Must not duplicate scope already claimed elsewhere (see the convergence-check decision above) -- this Spec's capabilities are deliberately the LEFTOVER items the source catalog names as concrete and not-yet-owned, never a re-specification of Epic 9 (CAP-8/9/10) or the sibling deferred-work-resolution-sweep Spec's CAP-7.
- (constraint) Category 5's exemplar-graduation question and Category 3's 'audit methodology -> repeatable check' question stay explicitly UNSCOPED here, per the source Dream's own 'not sized, prioritized, or scoped for implementation' disclaimer -- captured as Open Questions below, never invented as capabilities just to fill out the kernel.
- (non_goal) Not a commitment to build everything in Categories 1-6 -- the source Dream states this explicitly; this Spec extracts only the items not already claimed elsewhere and flagged concrete/ready by the source text itself.
- (non_goal) Not re-specifying deferred-work promotion, deferred-work resolution, the generalized hygiene sweep, loop-home branch staleness, or bmad-loop-liveness-footgun -- all already have their own Dream/Spec/Epic (see the convergence-check decision).
- (non_goal) Not deciding whether or how the purely-manual exemplars (six-act deck framework, warden-standalone infographic reference, recipe-domain patterns, CFE branch-naming convention) graduate to mechanical enforcement -- explicitly punted by the source Dream to be decided per-exemplar when this program is sized, via one of two proven shapes it names (bake-into-generator, per conda-forge.yml's pre-seed; or repeatable-apply-script, per Herald's TEA+Playwright rollout).
- (question) Should Categories 1-6's remaining not-yet-scoped items (repo-wide audit methodology -> repeatable check; which manual exemplars graduate to mechanical enforcement and via which shape) become their own future Dreams/Specs, or fold into a later revision of this one? The source Dream explicitly leaves this open -- operator call, not resolved here.
- (question) Where does the catalog itself live as a maintained artifact going forward -- does docs/dreams/fleet-hygiene-verification-exemplar-program.md remain the catalog, updated in place as CAP-1..5 ship, or does it graduate to a dedicated tracked location (mirroring EXEMPLAR-STANDARD.md's own graduation from convention to structured document) once enough items have a real status to track?
- (event) Self-validate pass 1 (coherence): 5 capabilities, each with intent+success; 2 constraints, each ruling something out (both anti-duplication, load-bearing given how much of the source text overlaps already-owned scope); 3 non-goals; success signal concrete. Rules 1-6,8 pass. Capability count is deliberately narrow (5, not one per catalog line item) because the source Dream itself explicitly declines to scope most of its own content -- inventing more capabilities here would violate the source's own stated intent, not fulfill it.
- (event) Self-validate pass 2 (preservation): walked docs/dreams/fleet-hygiene-verification-exemplar-program.md claim by claim. The full six-category catalog (52 named items/examples, the EXEMPLAR-STANDARD.md conformance table detail, the two exemplar-graduation case studies) is multi-item catalog content -- routed to a companion (hygiene-gap-catalog.md) per Spec Law's companion-spawn rule, cited by name from Why/Open Questions, rather than embedded in or dropped from the kernel. Items already owned by other specs are named in the convergence-check decision and Non-goals, not silently omitted. Nothing wrapper-only found -- every load-bearing claim landed in a capability, constraint, non-goal, open question, or the companion.
- (event) Spec finalized, status: dreamt -> ready for bmad-prd/bmad-architecture/bmad-create-epics-and-stories to decompose into pyforge-doctor's epics.md. Deliberately narrow scope (5 capabilities, all concrete/ready) reflects the source Dream's own explicit non-commitment to its remaining catalog content -- decomposition should not read this Spec as license to build all six categories.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
