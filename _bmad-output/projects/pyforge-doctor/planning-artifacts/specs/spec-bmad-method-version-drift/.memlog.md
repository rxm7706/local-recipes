---
topic: Doctor notices when BMAD-METHOD's own installed core falls behind upstream
updated: 2026-08-21T22:45
---

- (direction) Source: docs/dreams/bmad-method-version-drift.md (owner: doctor, status: dreamt), split 2026-08-15 from bmad-method-core-upgrade (now steward-owned) -- this Dream is the ambient, read-only detection half only.
- (decision) Why: Doctor already reports staleness for feedstocks/CVE-affected floors but has a blind spot for its own installed BMAD-METHOD core, a governance-layer dependency, not an ordinary pinned one. Confirmed live 2026-08-15: pixi.toml already declares bmad-method >=6.11.0, but _bmad/_config/manifest.yaml (the actually-installed core) still reports 6.10.0 -- undetected until a human happened to ask.
- (capability) CAP-1 -- declared-vs-installed drift. intent: an operator/agent sees when pixi.toml's declared bmad-method floor and the actually-installed _bmad/_config/manifest.yaml version disagree, without checking by hand. success: given today's real state (pixi.toml >=6.11.0, manifest.yaml 6.10.0), the new Source's Finding fires; given both agree, no Finding fires. Zero new infrastructure needed -- both are already-tracked repo files.
- (capability) CAP-2 -- installed-vs-upstream-latest drift. intent: an operator/agent sees when the installed core is behind the latest published upstream bmad-method release, not just behind the repo's own declared floor. success: given the installed manifest.yaml version is older than the latest upstream release, a Finding fires naming both versions. Data source undecided -- see Open Questions.
- (capability) CAP-3 -- surfaces as a warn, never a fail, through Doctor's existing report/verdict shape and fleet-picture's ATTENTION block. intent: the drift Finding appears where operators already look, and never blocks or fails a check-suite run on its own. success: fleet-picture's ATTENTION block names the drift when CAP-1/CAP-2 fire; the check-suite's exit code is unaffected by this Finding alone.
- (constraint) Read-only always -- this Dream/Spec never runs npx bmad-method install or writes to _bmad/**; applying an upgrade is bmad-method-core-upgrade's (owner: steward) territory entirely, never this one's.
- (constraint) New Source must fit Doctor's existing closed-taxonomy Source enum (PRD FR-2's convention, reused verbatim by FR-12's adoption-stage) -- never an open/stringly-typed source.
- (constraint) Must not duplicate bmad-method-core-upgrade's own pre-flight diff -- that Dream may run its own dry-run check immediately before an apply; this Spec's job is the AMBIENT, continuously-refreshed signal an operator sees without running anything.
- (non_goal) Not applying, merging, or reconciling anything -- steward's territory via bmad-method-core-upgrade.
- (non_goal) Not deciding which currently-unexercised BMAD modules to keep or drop (one-front-door's own open question).
- (non_goal) Not a general 'any dependency is behind' detector -- scoped specifically to BMAD-METHOD's own installed core, a governance layer with local customizations to protect, not an ordinary pinned dependency already covered by existing machinery.
- (question) How is CAP-2's 'latest upstream bmad-method release' sourced? No existing fleet infrastructure covers npm-package version lookups -- atlas's behind-upstream/version-downloads machinery (FR-12's own precedent) is conda-forge/PyPI-scoped only, not npm. Options: a live npm registry query at check time (tension with Marshal's own NFR-14 'no live query per home' discipline, which this fleet otherwise favors), a periodically-refreshed cached feed, or shipping CAP-1 alone first and deferring CAP-2 to a follow-on story. Operator call.
- (question) Which exact Source enum member / module in pyforge.doctor.sources.* does this plug into -- a new dedicated source, or an extension of an existing one (e.g. the bmad-drift source already used for pyforge-marshal artifact-vs-live-factory drift)? Architecture-level decision, not resolved here.
- (event) Self-validate pass 1 (coherence): 3 capabilities, each with intent+success; 3 constraints, each ruling something out; 3 non-goals; success signal concrete (fires on today's real pixi.toml/manifest.yaml drift). Rules 1-6,8 pass.
- (event) Self-validate pass 2 (preservation): walked docs/dreams/bmad-method-version-drift.md claim by claim -- The Dream, What is real, What it looks like when real, Constraints, Non-goals all landed in CAP-1..3/Constraints/Non-goals above. Kinships (bmad-method-core-upgrade, bmad-module-provisioning, pyforge-doctor) are process/lineage metadata, not load-bearing spec content -- correctly omitted from the kernel, dream file remains the source of record for lineage.
- (event) Spec finalized, status: dreamt -> ready for bmad-prd/bmad-architecture/bmad-create-epics-and-stories to decompose into pyforge-doctor's epics.md.
- (direction) Operator (2026-08-21): extend this spec with what today's live 6.10.0->6.11.0 upgrade session exposed — the SUITE is the blind spot, not the core
- (note) Evidence 2026-08-21: bmad-loop sat 0.9.0 vs upstream 0.11.0 (0.9.0 hardcodes /bmad-dev-auto and stalls every unattended session on BMAD >=6.11 — upstream shipped 0.9.1 as an emergency hotfix); TEA lagged 1.19.1 vs 1.23.2 (1.19.1's tea-test-review bin was published EMPTY); builder/CIS/skill-forge behind; three coordinated ecosystem waves (skill rename, uv-run conversion, persistent_facts/AGENTS.md) all invisible until a human checked
- (decision) SUPERSEDE the third non-goal: the bmad-SUITE joins scope as CAP-4; the non-goal narrows to non-BMAD dependencies (still no general any-dependency detector)
- (capability) CAP-4 suite-vs-upstream drift: watched set DERIVED from pixi.toml's bmad-* pins (derive-dont-declare — a hardcoded list omits exactly the newest tool), installed env versions compared against latest upstream releases via the same fail-open live-query exception CAP-2 already holds; warn-only ambient per CAP-3. Success: pointed at the 2026-08-21 pre-update state it names bmad-loop 0.9.0<0.11.0 and TEA 1.19.1<1.23.2; offline it degrades silently
- (event) Spec updated 2026-08-21: CAP-4 added, non-goal narrowed; preservation re-checked (CAP-1..3 untouched); decomposed as doctor Epic 14 Story 14.1
- (event) Story 14.1 implemented (CAP-4 suite pass): sources/bmad_method.py grew `_suite_packages` (watched set DERIVED from pixi.toml's bmad-* pins, core excluded), `_parse_release_triple` (lenient leading-X.Y.Z, tolerates .dev0/prerelease), `_installed_suite_versions` (`.pixi/envs/*/conda-meta/` FILENAME scan, max triple across envs), `_gather_suite_findings` (5.0s shared monotonic deadline, per-fetch min(remaining, 5.0), ENTIRELY fail-open with a documented last-resort except so CAP-1/2 outcomes are structurally untouched), and the fetch seam generalized in place (`_fetch_latest_upstream_version(*, package=DEPENDENCY_NAME, ...)`, `_NPM_LATEST_URL` -> `{package}` template) -- one WARN per package behind upstream (check="bmad-suite-upstream-drift", CAP-2's shape/wording + a "package" evidence key), exactly one OK naming the checked count when all checked are current, nothing when zero checked; appended to BOTH `_gather` success-path returns so the suite pass runs regardless of CAP-2's own fetch outcome. Changed files: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/bmad_method.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_bmad_method.py (CAP-4 banner section incl. test_2026_08_21_pre_update_fixture_names_bmad_loop_and_tea + every I/O row + direct helper/URL tests; 61 pass), src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py (BMAD_METHOD_VERSION_DRIFT member comment extended per the keep-in-sync contract), src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py (REGISTRY entry comment extended, comment-only). Zero changes to __main__.py/fleet_picture.py/check.py -- the suite findings ride Story 10.3's surfaces by status, not check. Full doctor suite 1163 passed / 1 skipped; live run: CAP-1/2 ok + one suite ok "(4 checked)", exit 0.
- (note) Live 2026-08-21 finding: only 4 of the 10 watched suite pins are npm-published (bmad-builder, bmad-creative-intelligence-suite, bmad-dashboard, TEA return 200; bmad-loop, bmad-labs-skills, bmad-manticore, bmad-method-wds-expansion, bmad-module-template, bmad-utility-skills 404 -- GitHub-only, packaged as local conda recipes). The 404s fold to per-package skip exactly as specced, but the package that MOTIVATED CAP-4 (bmad-loop) is invisible to the npm data source today; a GitHub-releases fallback would be a follow-on capability, not this story's.
- (event) Story 14.1 review pass (4-layer adversarial, 11 patch findings) landed same-tree: sources/bmad_method.py (percent-encoded package at the URL seam; lenient `_parse_release_triple` for SUITE upstream versions while the core keeps strict `_parse_version`; epoch-prefix tolerance `1!X.Y.Z`; OK-finding evidence gains `packages_watched`; budget comment softened to idle-timeout semantics; conda-name==npm-name assumption documented on `_SUITE_PREFIX`) + tests/unit/test_sources_bmad_method.py (mixed one-behind-one-current gather test closing a mutation-proven gap; full-equality offline assertion; conda-meta `history`/stray-entry noise test; epoch + prerelease-parse-split direct tests; evidence assertions updated; reconstruction note on `_PIXI_SUITE_PRE_UPDATE`). scripts/fleet_picture.py's 15s-timeout comment updated (comment-only, allowlisted). Suite: 1166 passed / 1 skipped; CFE meta test green.
