---
topic: An ambient, warn-only doctor Source that asks, for every declared capability in the fleet, whether anything actually exercises it
updated: 2026-09-09T15:42
---

- (note by claude) owner-dream: docs/dreams/capability-effect-check.md (owner: doctor, status: dreamt, seeded 2026-09-09). Companions: the criterion's home is spec-pyforge-unifying-strategy (steward Story 49.2); this Spec is the implementation half.
- (decision by claude) The criterion stays on the Unifying Spec; the IMPLEMENTATION is relayed here. Approved as fleet-readiness-decision-batch-2026-09-09 § 2.3 C8 ('Approve all, including the relay to a new doctor Dream + Spec', operator ruling § 0). Three grounds recorded there: (1) surface governance — spec-pyforge-doctor's surface: is src/shared/packages/pyforge-doctor/**, so a steward story writing a new module there is a foreign edit spec-surface-check reds at merge; (2) the re-home to hub:CAP-* is already scheduled, and a doctor-side Spec makes it a pointer change rather than a migration; (3) doctor's advisory doctrine is enforced by verdict.exit_code_for and tests/meta/test_verdict_sole_ownership.py, which a foreign author has no reason to know about.
- (decision by claude) A DEDICATED Source module, not an extension of sources/factory.py's BMAD_DRIFT. Doctor's own written precedent is bmad_method.py:9-20 — 'why this module exists, and why it is a dedicated file … a DIFFERENT artifact class entirely'. A capability's verified: line and its call sites are a different artifact class from a deferred-work ledger entry, a Dream, or a planning-spine feeds edge.
- (decision by claude) THE JOIN KEY is (spec-slug, CAP-N) — the same pair board.py's _parse_declared_cap_ids and _cited_cap_ids_by_spec already produce for chain-completeness INV-A. It reaches code through the citing story's Surface: line in the owning station's epics.md, and reaches evidence through an optional verified: line on the CAP in SPEC.md. Nothing new is parsed that the fleet does not already parse. Named up front deliberately: sibling-dreams-drift shipped against a join (title:) the estate does not share and has been structurally incapable of a finding since it merged (doctor-B4, measured 2026-09-09: 0 shared titles, 8 shared filenames).
- (capability by claude) CAP-1 — the caller-outside-its-own-tests check. For every declared capability whose citing story names Python surfaces, report whether any symbol those surfaces define is referenced from outside that module and outside tests. Success: run over every pyforge-* package in src/shared/packages/, it names risk-tiered-review-depth's classify_review_tier / resolve_review_cycles (zero callers outside core/gate.py and tests/unit/test_gate.py, measured 2026-09-09) as a live finding, and stays silent for a capability with real callers. Provenance: marshal-B report § E-6 — 'has a caller outside its own test file' is the cheapest first check, and three of marshal's four dormant capabilities were visible by no other means.
- (capability by claude) CAP-2 — the verified: line, rendered per CAP. A capability may carry verified: <date> — <what was exercised, where> on the CAP itself in SPEC.md. The check renders that line beside the capability and reports a shipped/realized capability that carries none. Success: the 'realized versus verified' column the Unifying Strategy's own 2026-09-09 review asked for is produced mechanically from SPEC.md, not hand-maintained; a CAP with a current verified: line produces no finding.
- (capability by claude) CAP-3 — it renders where the operator already looks. A report-schema.json entry for the new Source plus membership in the detectors task set, so the finding appears in the doctor report and the fleet-picture ATTENTION block beside story-status-check. Success: one detectors run answers both 'did the story land' and 'is the capability reached'; the two stay separate modules and separate check names.
- (constraint by claude) Advisory, always: warn at most, never a second PR verdict. pyforge.doctor.verdict.exit_code_for is the sole exit-code owner, pinned by tests/meta/test_verdict_sole_ownership.py. Read-only per doctor's NFR-1 — the check never authors a verified: line, flips a status, or moves a ledger row.
- (constraint by claude) Fail-open and SAY SO: an unreadable epics.md, an absent surface path, or a station with no ledger degrades to a named finding, never to silence. sibling-dreams-drift's second lesson — a bare return () renders 'could not look' as 'looked and agreed' (sibling_dreams.py:45,:48).
- (constraint by claude) THE JOIN MUST BE PROVEN ON LIVE DATA BEFORE THE FIRST STORY CLOSES. A fixture that constructs both sides from one synthetic value cannot reproduce a live failure — that is exactly how sibling-dreams-drift shipped silent (tests/unit/test_sources_sibling_dreams.py builds both trees from one _DECK_TITLE). At least one finding must be demonstrated against the real fleet, and the demonstration recorded.
- (constraint by claude) Not a coverage tool and not a call-graph resolver: 'has a caller outside its own test file' is a bounded, whole-word textual reach over declared surfaces — deliberately cheap, deliberately approximate, and honest about it in the finding text. Not a re-implementation of story-status-check either; that answers 'did it land', this answers 'is it reached', and they stay separate modules.
- (assumption by claude) Assumes the citing story's Surface: line in epics.md is a usable code pointer for most capabilities. Where a capability's surfaces are documents rather than modules, CAP-1 is not applicable and CAP-2's verified: line is the only signal — the check must say 'not applicable, document surface' rather than reporting a false 'no callers'.
- (question by claude) Where does this re-home when spec-intelligence-hub reaches ready? Epic 49's own binding note says the gate 're-homes to hub:CAP-* … once that Spec is ready'. Expected to be a pointer change (owner-dream / Kinship), not a migration — but the ruling belongs to steward's C4 bundle, and doctor should not pre-empt it.
- (event by claude) drafted 2026-09-09 from docs/dreams/capability-effect-check.md; SPEC.md to be derived by bmad-spec
- (event by claude) SPEC.md derived from this memlog 2026-09-09 (bmad-spec headless, create). Self-validate pass 1 coherence PASS (CAP-1..3 carry intent+success, 5 constraints bend design, 3 non-goals, machine-demonstrable success signal, lean prose), pass 2 preservation PASS (every Dream claim lands in the kernel; wrapper-only drops: Dream frontmatter and Kinships prose). status: draft; surface: [] until the module exists; companions=[spec-pyforge-unifying-strategy/SPEC.md adopted]; one open question (the hub re-home) carried into frontmatter.
