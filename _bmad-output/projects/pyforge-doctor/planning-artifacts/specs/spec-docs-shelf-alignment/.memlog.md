---
topic: One home for leftover docs — fold into Diátaxis (docs/dreams/docs-shelf-alignment.md)
updated: 2026-09-14T11:02
---

- (note by claude) THIS RECORD STARTS HERE, 2026-09-14. SPEC.md beside it was hand-seeded in the same pass rather than derived by bmad-spec, so this log does not begin at a derivation.
- (event by claude) Seeded 2026-09-14 from docs/dreams/docs-shelf-alignment.md at status: draft so dream-chain INV-1 has a durable Dream->Spec link. Five open questions, all design decisions; owning Dream stays dreamt.
- (decision by claude) Sibling of shipped spec-general-docs-consistency / realized general-docs-consistency, not a reopen. CAP-1..6 of the parent stay shipped. Residue is fold, sunset-by-status, dual-audience indexes, intake route, archive citations, publish-root subfolders, and a repeatable leftover-shelf check.
- (capability) CAP-1 candidate: one owner per fact in docs/; entry points are indexes. CAP-2 candidate: fold getting-started and air-gap cluster, stub old paths. CAP-3 candidate: sunset docs/specs/ by status (workflow->how-to; shipped/superseded->archive). CAP-4 candidate: empty intake per its README. CAP-5 candidate: retarget archive/_bmad-output/ citations. CAP-6 candidate: dashboard publish roots one subfolder per board; do not rename kedro-viz to vizro. CAP-7 candidate: warn-only Doctor check so the shelf does not grow back unmarked.
- (constraint) Do not unship parent CAP-1..6. Do not move Dreams, active Tier-2 Specs, or Tier-3 into Diataxis. Relocate-and-correct, do not regenerate. Doctor stays advisory. No Dream YAML on gist dumps.
- (question) Open: workflow specs stub vs move for bmad-drift --specs; brownfield extract vs keep binders; extend general_docs_consistency vs new source; doctor Epic 23 vs steward decompose; vizro/ now vs when secure-live-dashboards publishes.
- (decision by claude) Q1 workflow-specs: archive shipped/superseded fully to archive/docs/specs/. Keep in-progress files in docs/specs/ until those efforts close. For the three status:workflow files, MOVE the body to docs/how-to/ and leave a stub at docs/specs/<name>.md that keeps status: workflow plus a pointer — bmad_drift_check.py --specs only globs docs/specs/*.md for frontmatter and CLAUDE.md filename index; a full move would drop the detector and break the index. Stub is the sunset, not a second living copy.
- (decision by claude) Q2 brownfield: EXTRACT unique operational steps into the existing Diataxis files, then stub development-guide.md and deployment-guide.md to point at docs/ plus SYNC-RUNBOOK for CFE pin re-ground. Do not keep the 700-line mixed binders as a humans-vs-agents pair — that split is the CAP-2 failure mode Epic 22 already punished. The planning tree stays; only duplicate operational prose leaves it.
- (decision by claude) Q3 detector: NEW Doctor source (docs_shelf / general-docs-map occupancy), do not extend general_docs_consistency. That module is identity (README vs skill-brief vs AGENTS vs Dream) with frozen 22.3 fixtures. Shelf occupancy is a path-vs-MAP-allow-list check — a second job. Same family: warn-only, fail-open, never a second PR gate.
- (decision by claude) Q4 decompose: doctor Epic 23. Owner Dream is doctor, Epic 22 was doctor, the new source is a doctor module. Steward kinship stays for archive-path convention only — no steward epic for markdown moves.
- (decision by claude) Q5 vizro/: do NOT mint docs/dashboard/vizro/ now. Empty folders are leftover shelf. kedro-viz/ exists because viz-publish-stage uploads there. CAP-6 is the MAP/README rule (one subfolder per board, vizro when that board publishes). Mint the directory in the secure-live-dashboards / atlas publish story that first has an upload.
- (event by claude) Open questions cleared 2026-09-14 by analysis against live detectors (bmad_drift --specs glob, general_docs_consistency identity-only) and the kedro-viz publish tree. Spec re-derived to status: ready. CAP-1..7 remain the same IDs; candidates become commitments.
- (event by claude) Self-validate pass 1 (coherence): PASS. CAP-1..7 each have intent+success; constraints rule things out; non-goals explicit; success signal testable; CAP IDs stable; open_questions removed after 2026-09-14 decisions.
- (event by claude) Self-validate pass 2 (preservation): PASS. Five settled answers land in CAP-2/3/6/7 + Constraints. Dual-audience rule in CAP-1. Parent shipped CAPs untouched.
- (event by claude) Story Breakdown 2026-09-14: doctor Epic 23 Stories 23.1-23.7 (1:1 CAP-1..7), minted backlog via sprint_plan generate + sprint-ledger-sync. Dream status dreamt -> specified.
- (event) 2026-09-17 absorbed into spec-pyforge-doctor (pointer + companions kept; derived SPEC body disposed).
