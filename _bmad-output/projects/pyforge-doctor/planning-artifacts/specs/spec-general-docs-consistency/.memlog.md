---
topic: General documentation stops contradicting itself, and stays that way (docs/dreams/general-docs-consistency.md)
updated: 2026-09-11T13:18
---

- (direction) Input: docs/dreams/general-docs-consistency.md (status: dreamt, owner: doctor), a live two-pass audit this session with 6 independently-verified findings. Distilled headless/express per bmad-spec convention.
- (decision) General docs taxonomy (docs/reference, dreams, specs, governance, intake, archive) is already coherent and deliberate -- no restructuring. This Spec is content-correctness only, per the Dream's own Non-goals.
- (capability) CAP-1: fix the 2 identity contradictions at their source -- pyforge-doctor/README.md's 'its own exit-code gate' language (contradicts skill-brief.yaml/CLAUDE.md/AGENTS.md's 'advisory, not a second PR gate') and AGENTS.md's herald cross-agent-portability claim (contradicts herald's own Dream, which assigns it to marshal citing a dated ownership review). Success: every authoritative source about doctor agrees findings are advisory; AGENTS.md's herald claim matches herald's own Dream.
- (capability) CAP-2: fix the 4 cross-cutting decay findings in README.md/CLAUDE.md -- the GitHub Actions Workflows section (4 of 19 real workflows listed, false 'manual trigger only' claim), the project-structure tree's 2 broken paths (docs/bmad-setup-plan.md, docs/developer-guide.md -- real paths are archive/docs/bmad-setup-plan.md, docs/reference/developer-guide.md), the duplicated active-project-resolution-priority list (README.md vs CLAUDE.md, unequal copies), and CLAUDE.md's self-contradicting station count (8 stations named vs '7 skills' block omitting mason without explanation). Success: each of the 4 facts is accurate and, where duplicated, exists once with the other side pointing to it.
- (capability) CAP-3: a repeatable Doctor detector, extending the capability-effect-verified/status-body-consistency family, that cross-references a station's README/skill-brief.yaml/Dream/AGENTS.md claims against each other and flags a real divergence -- bounded, textual, explainable, warn-only, fail-open, matching the existing hygiene-detector discipline. Success: the detector exists, is tested against at least the herald/doctor cases that motivated this Spec, and is wired in beside its siblings (never a second PR gate itself).
- (constraint) No documentation restructuring -- the existing docs/ taxonomy stays exactly as-is; this Spec is content-correctness only.
- (constraint) Surgical fixes only -- no wholesale rewrite of README.md, CLAUDE.md, or AGENTS.md; touch only the lines the 6 confirmed findings name.
- (constraint) CAP-3's detector stays bounded/textual/explainable like Doctor's existing hygiene checks -- never a false green, never a heavier NLP/semantic tool than the problem needs, never a second PR gate.
- (constraint) CAP-3 findings must be evidence-based like capability-effect-verified's own discipline -- never invent or infer a contradiction the detector cannot literally quote from two sources.
- (note) Non-goal: a full editorial pass of every doc in the repo -- scope is the 6 confirmed findings plus the CAP-3 detector, not an open-ended sweep.
- (note) Non-goal: re-checking the 6 stations already found clean (atlas, marshal, mason, scribe, steward, warden) -- a light pass this session found nothing there.
- (note) Non-goal: refreshing docs/reference/*.md's 5 already-known self-flagged-stale files (github-workflows.md, station-verify-commands.md, enterprise-deployment.md, mcp-server-architecture.md, library-llms-full.md) -- separate, already-tracked issue, out of this Spec's scope.
- (assumption) Mason's own skill-directory omission is confirmed deliberate policy (AGENTS.md:31, governance-currency:ignore-wrapped) -- CAP-2's CLAUDE.md fix documents this rather than adding a skill for mason.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6/8): PASS. 3 capabilities each carry intent+success, WHAT not HOW; 4 constraints each rule something out; 3 non-goals explicit; success signal testable; CAP IDs stable; prose lean. No assumptions/open_questions needed beyond the 1 already logged.
- (event) Self-validate pass 2 (preservation): PASS. All 6 Dream findings map to CAP-1 (findings 1-2) or CAP-2 (findings 3-6); Dream's repeatable-detector ask maps to CAP-3; Dream's own non-goals map to Constraints + Non-goals. No wrapper-only drops.
- (event) Spec finalized, status: ready for the user's own Dream-seed edits before implementation proceeds -- operator explicitly asked to pause here.
- (direction) Input: docs/dreams/general-docs-consistency.md widened 2026-09-11 (operator-directed) -- content-correctness scope unchanged, PLUS a new Diataxis-adapted structural-adaptation ask for the general-facing docs layer only. Re-deriving per bmad-spec update convention; same slug, existing CAP-1..3 IDs preserved.
- (decision) Supersedes the 2026-09-11T04:17 'no restructuring' decision above: the general-facing documentation layer (README.md, docs/reference/, docs/intake/, scattered onboarding/operational content) is now in scope for a Diataxis-adapted reorganization. The BMAD spec-driven tier (docs/dreams/, docs/specs/ legacy, _bmad-output/*/planning-artifacts/, gitignored implementation-artifacts/) remains explicitly out of scope, unchanged from before.
- (capability) CAP-4: Design and adopt a Diataxis-adapted information architecture for the general-facing documentation layer (README.md, docs/reference/, docs/intake/, scattered onboarding/operational content), explicitly excluding the BMAD spec-driven tier. Success: a documented map exists naming which quadrant(s) apply and what lives where; docs/reference/'s existing content is preserved as the seed of the Reference quadrant (relocated/reorganized, not discarded and rebuilt); the BMAD spec-driven tier (docs/dreams/, docs/specs/ legacy, _bmad-output/*/planning-artifacts/, implementation-artifacts/) is untouched by the map.
- (capability) CAP-5: Give the general-facing docs layer a discoverable Tutorials/Getting-Started path and a discoverable How-to-Guides path -- both currently absent as named destinations, with their content scattered across README.md's Common Commands, docs/reference/developer-guide.md, and skill-scoped guides. Success: a newcomer with no prior context can find one place to get a working environment running (Tutorials/Getting Started) and one place to find task-oriented operational instructions (How-to Guides); the content populating both is relocated/corrected existing material, not rewritten from scratch.
- (capability) CAP-6: README.md, CLAUDE.md, and AGENTS.md point cleanly into the reorganized general-docs structure instead of duplicating or going stale against it. Success: no internal link into the reorganized structure is broken; no fact that lives in the new structure is also duplicated verbatim in README.md/CLAUDE.md/AGENTS.md without one side pointing to the other -- the same discipline CAP-2 already applies, now applied to the new structure.
- (constraint) CAP-4..6: the BMAD spec-driven tier (docs/dreams/, docs/specs/ legacy Tier 1, _bmad-output/*/planning-artifacts/, gitignored implementation-artifacts/) is out of scope, full stop -- not touched, moved, merged, or reorganized. It is a different layer (the spec-and-build pipeline) with its own already-coherent convention, documented in CLAUDE.md.
- (constraint) CAP-4..6: Diataxis is adapted, not imported wholesale -- no generic /docs/getting-started + /docs/guides + /docs/reference scaffold bolted on regardless of fit. The structure grows from what's already here; docs/reference/ survives as the Reference quadrant's home, sized to this repo's actual content volume.
- (constraint) CAP-4..6: content is relocated and corrected, not regenerated -- moving a file into the new structure and fixing a confirmed contradiction in it is in scope; rewriting accurate prose from scratch 'for consistency' is not. Same surgical discipline as CAP-1/CAP-2's constraints, now applied to files that also move.
- (assumption) The Dream names all 4 Diataxis quadrants (Tutorials, How-to, Reference, Explanation) but docs/reference/'s existing content mixes true reference material with architecture/rationale ('why') content -- whether Explanation becomes its own home or folds into Reference is CAP-4's own design decision (a WHAT-not-HOW deferral), not pre-committed by this Spec.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6/8) on the widened spec: PASS. CAP-1..6 each carry intent+success, WHAT not HOW (CAP-4/5/6 defer exact quadrant naming/placement to design time); 7 constraints each rule something out; 3 non-goals explicit (unchanged, still apply); success signal testable; all 6 CAP IDs stable, none renumbered/reused; prose lean.
- (event) Self-validate pass 2 (preservation) on the widened spec: PASS. Second Dream finding's load-bearing claims all land: operator's stated motivation -> Why section, Diataxis-adapted-not-imported -> constraint, docs/reference/ seeds Reference -> CAP-4 + constraint, BMAD tier out of scope -> constraint, CLAUDE.md/AGENTS.md corrected pointers -> CAP-6, scattered Tutorials/How-to content -> CAP-5, relocate-not-regenerate -> constraint, Explanation-quadrant ambiguity -> assumption. No wrapper-only drops.
- (event) Spec re-finalized against the widened Dream (2026-09-11). 6 capabilities (CAP-1..3 unchanged from the original narrower Spec, CAP-4..6 new for the structural-adaptation half), 7 constraints, 3 non-goals, 2 assumptions. Ready for the operator's next step (story breakdown or direct implementation dispatch).
- (decision) Resolves the Explanation-quadrant assumption (operator-directed, 2026-09-11): docs/reference/ splits into two homes, not one. True reference material (config/CLI/schema specs) stays in the Reference quadrant. Architecture-rationale / 'why this works this way' content (e.g. mcp-server-architecture.md's design reasoning, enterprise-deployment.md's rationale) moves to a new, dedicated Explanation quadrant. Files are relocated and split by kind, not rewritten.
- (capability) CAP-4 (refined success, same ID): intent unchanged (design and adopt a Diataxis-adapted IA for the general-facing docs layer, excluding the BMAD tier). Success, refined: a documented map exists naming four quadrants -- Tutorials/Getting-Started, How-to Guides, Reference, and Explanation. docs/reference/'s existing content is preserved but split by kind: true reference material stays in Reference; architecture-rationale/'why' content moves to the new Explanation quadrant. Nothing is discarded or rewritten from scratch -- files are relocated and split, not regenerated. The BMAD spec-driven tier (docs/dreams/, docs/specs/ legacy, _bmad-output/*/planning-artifacts/, implementation-artifacts/) remains untouched by the map.
- (event) Self-validate re-check after CAP-4 refinement: PASS (coherence + preservation). CAP-4 still WHAT-not-HOW (names four quadrants, doesn't prescribe folder paths); the Explanation-quadrant assumption is resolved and removed from Assumptions, replaced by the decision + refined CAP-4 success. Re-rendering SPEC.md.
- (decision) Frontmatter gained owner-dream: docs/dreams/general-docs-consistency.md and status: ready (both required by doctor's dream-chain-check/chain-completeness-check detectors, not part of bmad-spec's own generic frontmatter convention -- repo-local requirement, verified against sibling specs' own frontmatter shape before adding).
- (event) Story Breakdown (operator-directed, 2026-09-11): CAP-1..6 decomposed 1:1 into doctor Epic 22 (Stories 22.1-22.6), minted backlog. Dependencies: 22.5 depends on 22.4 (needs the IA map decided first), 22.6 depends on 22.4+22.5 (needs the final structure to exist before pointing to it); 22.1/22.2/22.3 have no dependencies. Dream status: dreamt -> specified.
