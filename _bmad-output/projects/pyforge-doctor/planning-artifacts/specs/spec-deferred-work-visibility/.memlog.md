---
topic: deferred_work_check cannot see 470 of the 503 Tier-3 deferrals it exists to guarantee; both the emitter and the detector must move, in that order
updated: 2026-08-22T05:38
---

- (note) Source: docs/dreams/deferred-work-visibility.md (owner doctor, seeded 2026-08-10). The finding originated in marshal story 4-14 review pass and was proven BY EXECUTION, not inference: grep -c on Tier-3 for spec-4-14 returns 11, grep -c on the tracked ledger returns 0, and deferred_work_check.py nonetheless prints OK and exits 0.
- (constraint) MEASUREMENT, taken with the detector own _anonymous() parser rather than a grep (a grep over source_spec: bullets overcounts by including fields inside identified entries): 470 anonymous vs 33 identified across eight stations -- marshal 202/12, doctor 72/4, steward 56/1, atlas 54/12, warden 41/2, herald 26/1, mason 13/1, scribe 6/0.
- (constraint) MECHANISM: _DW_RE at scripts/deferred_work_check.py:71 harvests DW-* ids from the Tier-3 file and the tracked ledger and compares those two SETS. bmad-dev-auto mandated defer format (.claude/skills/bmad-dev-auto/step-04-review.md) writes bare "- source_spec:" bullets with NO id, so anonymous entries are in neither set and are never compared; the identified ones all do have twins, so the comparison passes.
- (capability) CAP-1 a deferral carries identity from birth. intent: a review pass that defers something produces an entry bearing an id under the OWNING STATION own convention, not a fleet-wide imposition. success: a newly damped story leaves a Tier-3 entry whose id matches that station existing scheme -- DW-FU-<story> for doctor/atlas/marshal/warden, DW-<story>-<n> for mason -- and no new anonymous entry is created by any bmad-dev-auto run.
- (capability) CAP-2 the detector sees what it claims to check. intent: deferred_work_check reports anonymous Tier-3 entries instead of silently skipping them, so "every Tier-3 deferral has a tracked twin" means every deferral. success: deleting an id heading from a Tier-3 entry REDS the detector -- demonstrated by mutation, the same proof standard _anonymous() own docstring already records for the tracked side.
- (capability) CAP-3 the existing 470 do not red every landing pass on day one. intent: the backlog that accumulated while the detector was blind is baselined, and the gate bites on what is NEW. success: after the detector learns the anonymous shape, a landing pass is green on an unchanged repo, and adding one anonymous entry reds it.
- (decision) BOTH SIDES MOVE, AND THE ORDER IS LOAD-BEARING: emitter first (bmad-dev-auto writes ids -- stops producing invisible entries, reds nothing), detector second WITH a baseline. Rejected: detector-first, which turns one green check into 470 findings overnight; every landing pass fails and the realistic outcome is the finding gets suppressed -- strictly worse than today, because then it is suppressed AND invisible. Rejected: emitter-only, which stops the bleeding but leaves all 470 permanently unseeable.
- (constraint) HALF THE MACHINERY EXISTS AND IS ALREADY HARDENED. _anonymous() parses exactly this shape and its docstring records this same vacuity class being caught once before and fixed by mutation testing ("an earlier version latched seen_id true until the next heading ... proved by mutation: deleting an id heading did not red the detector"). It is called on the TRACKED ledger only, never on the Tier-3 file. The gap is one call plus a finding kind, not a new parser.
- (constraint) GRANDFATHERING PRECEDENT IS IN-HOUSE: spec-pyforge-warden already ships baseline-and-grandfathering for exactly this shape -- a real gate that would otherwise red on a large pre-existing backlog. Do not invent a mechanism; follow that one.
- (constraint) ORDERING CONSTRAINT THAT DECIDES WHERE THE CODE GOES: scripts/deferred_work_check.py is one of the eight shims doctor story 6-9 DELETES (confirmed against its branch: D scripts/deferred_work_check.py); its logic moves into pyforge.doctor.sources REGISTRY. A fix written against the shim is work thrown away. The detector half belongs in Doctor sources/, which places it after -- or deliberately alongside -- the 6-9 rewiring the operator has chosen to land on purpose rather than as a side effect.
- (question) OPEN: who owns which half? The detector half looks like Doctor -- Epic 6 spent six stories re-homing exactly these verdicts. The emitter half looks like Marshal -- it owns loop orchestration and the bmad-dev-auto skill. Splitting one effort across two stations needs deciding rather than assuming.
- (question) OPEN: are all 470 worth keeping? They accumulated unseen; some may be stale, duplicated, or already resolved. Grandfathering wholesale preserves noise as well as signal. Triage may be cheaper than it looks, or may be a second effort.
- (question) OPEN: does the emitter assign ids, or does promotion? Minting at defer time makes Tier-3 self-describing; minting at promotion keeps id-generation in one place. Marshal Story 4.13 already automated promotion, which argues for the second -- except 4.13 promotes BY ID, so it inherits the same blind spot.
- (question) OPEN: should the tracked ledger own anonymous entries red at the same severity? _anonymous() already reports them there as ledger-entry-unidentified. If Tier-3 gains the same check, the two sides should agree on severity rather than drift.
- (constraint) SURFACE IS EMPTY AND THAT IS HONEST, not a frontier claim: the files this would change (scripts/deferred_work_check.py, .claude/skills/bmad-dev-auto/step-04-review.md, pyforge/doctor/sources/) are governed by OTHER specs -- mason spec-fleet-stewardship covers .claude/**, and doctor own station spec covers its package tree. A glob matching nothing is silent by design; this Spec governs no files of its own.
- (event) SPEC-SURFACE BASELINE STAMPED 2026-08-10, first stamp. Changed paths: NONE -- this is a new Spec landed with an empty surface (frontier, no code exists; verified by grep before the Spec asserted it), so spec_surface_check reported [no-baseline] purely because no baseline ROW existed yet, not because a governed path drifted. Nothing is being laundered: an empty surface stamped against zero files is a no-op that records the starting point. The first real stamp with content will come when this Spec's implementation claims files, and that one must name them.
- (direction) OPERATOR 2026-08-10: proceed to decomposition. This Spec's only recorded blocker was doctor story 6-9, which LANDED today (PR #394) -- so the DEFERRED_SPECS reason that held it back has expired and the four open questions below are now answerable rather than deferred.
- (constraint) FACT CORRECTION, found while answering Q1 and load-bearing for the answer: this Spec's own frontmatter claimed '.claude/skills/bmad-dev-auto/** [is governed] by mason's spec-fleet-stewardship'. That is FALSE, verified two ways -- zero occurrences of 'bmad-dev-auto' anywhere in scripts/.spec-surface-baseline.json, and no Spec surface glob in any of the 8 projects matches .claude/skills/bmad-dev-auto/step-04-review.md. The path is ALLOWLISTED, not governed: scripts/spec_surface_allowlist.txt line 3, '.claude/** # non-CFE agent config'. So the emitter half is owned by NO station's surface today, and Q1 cannot be settled by pointing at existing governance.
- (decision) Q1 RESOLVED -- DOCTOR owns BOTH halves; the effort is not split. Three grounds. (1) The detector half is Doctor's beyond argument as of today: 6-9 landed and the check is now Source.DEFERRED_WORK registered in pyforge/doctor/sources/__init__.py, not a scripts/ shim. (2) The emitter half is not a change to loop ORCHESTRATION -- which would be Marshal's -- it is a one-template-line change making bmad-dev-auto COMPLY with a contract Doctor defines and validates. The station that defines what a valid deferral looks like should own making the emitter produce one. (3) Per the correction above the emitter path is allowlisted, so a Doctor-owned change invades no other station's surface. Rejected: splitting a 3-CAP effort across two stations for a single template edit, which buys a seam boundary and pays a coordination cost on every story.
- (decision) Q3 RESOLVED -- the EMITTER mints the id, at defer time, not promotion. The question is already settled by CAP-1's own words ('carries identity from birth'), and the alternative is self-defeating: an entry that reaches promotion without an id is ALREADY invisible to the thing that would promote it. Marshal Story 4.13 promotes BY ID and therefore inherits the exact blind spot this Spec exists to close, so routing id-minting through promotion would rebuild the defect one layer up. Rejected: minting at promotion to keep id-generation in one place -- the single place would be a place the anonymous entries never reach.
- (decision) Q2 RESOLVED -- grandfather all 470 wholesale at a dated cut-off; do NOT triage them in this effort. The Spec already lists triage as a Non-goal, and the warden precedent this Spec is told to follow does exactly this: baseline the pre-existing backlog, gate on what is new. Triage remains worth doing and is named as its own follow-on rather than smuggled into a gate story, because triaging 470 entries is judgement work per entry and would block a mechanical fix behind an open-ended review.
- (decision) Q4 RESOLVED -- yes, SAME severity on both sides. The tracked ledger's anonymous entries already report as ledger-entry-unidentified; the Tier-3 side gains the mirror finding at the same level. One invariant, two sides: different severities would let one side be silenced independently of the other, which is how the two drift apart -- and drift between the two sides is the shape of the original defect, not a new risk.
- (event) All four open questions resolved 2026-08-10; status draft -> ready. Ready for bmad-create-epics-and-stories.
- (event) DISPATCH CONFIRMED (2026-08-10, operator, in-session): Epic 7's four stories (7-1 emitter mints ids, 7-2 grandfather the 470, 7-3 Tier-3 anonymous detection with the mutation proof, 7-4 one severity) are cleared to run on the next doctor spin. DEFERRED_SPECS entry removed — its reason expired at 6-9's landing and this epic is the decomposition it awaited
- (direction) OPERATOR 2026-08-15: this Spec's own Q2 resolution named triage/promotion of the grandfathered backlog as 'its own follow-on'. Extend THIS Spec with that follow-on plus the structurally-identical fleet-hygiene generalization, rather than a second spec-kernel -- one big process/spec is preferred over several small ones. Convergence found while starting a separate Dream+Spec for the same problem (docs/dreams/deferred-work-audit-completeness.md, now folded in here) after a user challenge to a 2026-08-15 fleet-wide by-hand deferred-work audit that independently rediscovered this Spec's own CAP-1..3 shape from scratch.
- (constraint) STATUS CHECK 2026-08-15: CAP-1/2/3 are shipped (Epic 7, stories 7-1..7-4, verified live -- the emitter in .claude/skills/bmad-dev-auto/step-04-review.md mints ids at defer time in real production detail; the detector's tier3-entry-unidentified finding, this session's own tool all night, is CAP-2/CAP-3's grandfather-baseline). So the backlog is now BOUNDED and SHRINKING, not an ongoing leak -- every entry still anonymous predates PR #396 (2026-08-10). A 2026-08-15 by-hand fleet audit (before this convergence was found) promoted 144 tier3-only-deferral findings plus another ~72 previously-invisible ones across 6 of 8 projects, leaving 72 remaining (deferred-work-check's own tier3-entry-unidentified count as of 2026-08-15: marshal 30, steward 38, mason 2, herald 2) -- confirming the backlog is real, non-trivial, and exactly what Q2 deferred.
- (capability) CAP-4 -- an anonymous Tier-3 entry from BEFORE the CAP-1 emitter fix is still correctly parsed, whatever pre-fix shape it used. intent: the fix only prevents NEW anonymous entries; legacy ones use at least two distinct pre-fix shapes already found live -- a headerless flat -/source_spec:/summary:/evidence: bullet under a bmad-dev-auto step-04 marker comment (58 real findings in atlas alone, dating to July), and (separately, in pyforge-warden's Tier-3 file) an even older ## Deferred from: code review of <spec> (<date>) section-header convention that predates any DW-id scheme at all. success: a parser fixture built from real excerpts of both legacy shapes plus CAP-1's current shape and CAP-2's multi-bullet-per-header shape (## DW-<id> or ### DW-<id> with more than one - source_spec: bullet stacked under it) classifies every entry correctly -- these exact two shapes broke two different hand-rolled attempts on 2026-08-15 (a false-orphan duplication of already-headed content, and a 10x overcount from treating a header's second bullet as a new orphan).
- (capability) CAP-5 -- a legacy anonymous entry gets a collision-free id in the SAME per-station convention CAP-1 already established (DW-FU-<story> for doctor/atlas/marshal/warden, DW-<story>-<n> for mason), never a fleet-wide imposition. intent: promoting old backlog entries must not invent a second id scheme alongside the one CAP-1 just standardized going forward. success: minting against a tracked ledger that already has entries for a story picks the next free numeric suffix for THAT story (never restarts at 1 and collides with an existing id) -- verified against three real 2026-08-15 near-misses (DW-10-5-1, DW-10-6-1, DW-13-3-1 all attempted a duplicate mint before being caught and renumbered by hand).
- (capability) CAP-6 -- a --fix mode (mirrors scripts/spec_surface_check.py's --write-baseline pattern) promotes every genuine legacy-anonymous entry CAP-4 classifies as an orphan, mints via CAP-5, appends to the tracked ledger with a status: open line and a promoted: <date> provenance note, and refuses to write if the result would be a duplicate id or duplicate summary-text against the existing ledger. intent: replace the error-prone by-hand promotion this repo has now done twice (2026-07-29 for warden, 2026-08-15 fleet-wide) with a tool that cannot ship the two bugs that by-hand attempt actually shipped. success: running --fix against the live 72-entry backlog (marshal 30, steward 38, mason 2, herald 2 as of 2026-08-15) fully clears tier3-entry-unidentified for those with zero content duplication, and a manufactured collision fixture aborts the write with no partial output (mutation-tested).
- (capability) CAP-7 -- after a --fix run, the grandfather baseline (scripts/.deferred-work-baseline.json, the mechanism CAP-3 already ships) is re-stamped so newly-promoted entries are never re-flagged. intent: --fix and the baseline stay in lockstep, the way spec_surface_check.py's --write-baseline already keeps scripts/.spec-surface-baseline.json current. success: running --fix twice in a row is a no-op the second time -- 0 new writes, 0 findings delta.
- (capability) CAP-8 -- a fleet-wide hygiene sweep, generalizing the pyforge-warden-only 2026-08-14/15 bmad-output-hygiene precedent (dead test scaffolding archival, hollow sprint-status.yaml detection, orphan-file detection, README-placeholder detection, stale Dream-status detection) into a check runnable against all 8 stations rather than whichever one a human happened to audit by hand that night. intent: the same finding classes doctor/atlas/marshal/mason/scribe/steward/herald could equally carry are detected consistently, not left to chance. success: running the sweep against all 8 projects reproduces warden's own 5 finding classes as a fixture (zero false positives against warden post-sweep, since it is already clean) and surfaces at least one true positive on a station never audited this way.
- (capability) CAP-9 -- hygiene findings (CAP-8) are reported, never auto-applied. intent: a dead-scaffolding or orphan-file finding names the path and the evidence for why it is judged dead/orphaned, and the archive/delete action stays a separate, human-reviewed step -- matches this repo's own archive-don't-delete convention and the same review discipline CAP-6 keeps for deferred-work promotion. success: the sweep's own invocation never mutates a file; every finding it reports is actioned by a distinct, reviewable follow-up commit.
- (capability) CAP-10 -- loop-home branch staleness is visible without a human asking. intent: fleet_picture.py's ATTENTION block (already the ambient, always-run home for cross-cutting fleet signals -- it grew a baseline-drift line this same 2026-08-15 session) gains a line naming any loop/pyforge-<slug> branch that is N+ commits behind origin/main, the exact gap a 2026-08-15 session found by accident (all 4 active stations' branches 55-60 commits stale, which is how 3 separate stations independently rediscovered the SAME already-fixed spec-surface bug before their branches caught up). success: fleet-picture's ATTENTION block names a synthetically-staled loop-home branch without a separate manual check being run.
- (constraint) NEW non-goal: exemplar/quality-bar conformance (EXEMPLAR-STANDARD.md's own designated golden references, the six-act deck framework, canonical recipe patterns) is explicitly OUT of this Spec's capabilities -- it is judgment-based quality conformance, not a detectable/testable correctness class, and does not satisfy Spec Law rule 5 (a concrete pass/fail signal). Named in docs/dreams/fleet-hygiene-verification-exemplar-program.md Category 5 as a distinct, harder problem; left there as catalog/reference, not pulled into this Spec's capabilities.
- (constraint) NEW non-goal: code-verification of whether an ALREADY-TRACKED entry still describes true, live code (the 2026-07-30 145-entry fleet-wide campaign's own job) stays a distinct future effort, not folded in here -- CAP-4..7 promote a Tier-3 finding into durable storage verbatim, never curate/re-verify it, matching the 'promoted, not curated' convention every prior manual promotion in this fleet has already used. Tracked separately in docs/dreams/deferred-work-resolution-sweep.md pending its own convergence check against this Spec before being specced.
- (question) OPEN: CAP-4..10 substantially broaden this Spec past its original id-visibility scope. Does adding them to an already-shipped, status: ready Spec need a fresh self-validate pass + a return to draft before bmad-create-epics-and-stories runs against it, or can Epic 8+ be decomposed directly since CAP-1..3 are unaffected and already shipped? Operator call, not resolved by this memlog entry.
- (event) Memlog extended 2026-08-15 with CAP-4..10 (legacy-backlog triage + fleet hygiene sweep + loop-home staleness visibility), converged from docs/dreams/deferred-work-audit-completeness.md and docs/dreams/fleet-hygiene-verification-exemplar-program.md per explicit operator direction to extend this Spec rather than mint a new one. SPEC.md re-derive pending.
- (event) SPEC.md re-derived 2026-08-15. Self-validate Pass 1 (coherence, rules 1-6/8): CAP-4..10 each carry intent+success; success lines are concrete/testable (mutation-tested, specific counts, fixtures); 3 new constraints rule out real alternatives (mega-check merge, exemplar-conformance scope, curating-not-promoting); 3 new non-goals explicit; ids CAP-4..10 unique, no reuse. Pass 2 (preservation): docs/dreams/deferred-work-audit-completeness.md fully absorbed (added to sources:, its own frontmatter updated to status: specified pointing here) -- every load-bearing claim (2 detector blind spots, the Story 7.3 grandfather-baseline relationship, the two 2026-08-15 hand-rolled bugs, all 4 proposed capabilities) lands in CAP-4..7 or the Why section. docs/dreams/fleet-hygiene-verification-exemplar-program.md deliberately NOT added to sources: -- only its Category 3 hygiene-sweep item (CAP-8/9) and Category 4 loop-home-staleness item (CAP-10) converged here; Category 5 (exemplar/quality-bar conformance) stays out per Spec Law rule 5 and is recorded as a non-goal; the Dream itself remains a live catalog for its other, unconverged items and was cross-referenced back, not consumed. Wrapper-only content: none dropped -- both source Dreams' load-bearing claims either landed here or were deliberately left as open catalog items, recorded as such.
- (event) Added covers-dreams: docs/dreams/deferred-work-audit-completeness.md to frontmatter so INV-1 (dream-chain-check) sees it as covered rather than dream-without-spec -- verified: pyforge.doctor.sources dream-chain no longer flags it.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
