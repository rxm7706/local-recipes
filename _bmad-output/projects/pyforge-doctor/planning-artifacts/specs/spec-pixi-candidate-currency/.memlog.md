---
topic: pixi-candidate-currency: a durable, re-checkable dependency-currency ledger for pixi.toml (candidate disposition + version currency + channel-sourcing debt)
updated: 2026-09-14T12:09
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency`.
- (event) Surface reconcile 2026-09-07 (fleet hygiene, spec-45-2 CAP-2 pilot landing): `pixi.toml` changed to add three `local-recipes` pixi tasks (`eval-quality-smoke`, `eval-quality-review-twin-run`, `eval-quality-review-replay`) for pyforge-steward's `spec-bmad-eval-quality` CAP-2 pilot — unrelated to this spec's own surface, but this spec's blanket `pixi.toml` glob makes it a co-governor of that one file. No product work here. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency`.


- (direction) Headless invocation (unattended agent session); express distill chosen per protocol default. slug pixi-candidate-currency inherited from the Dream filename; title kept from the Dream (already declarative). Owner: pyforge-doctor per the Dream's own declared owner. This is the last of 3 orphan no-Spec Dreams found this session -- the other two (tree + fleet conventions) were later folded into `docs/dreams/pyforge-unifying-strategy.md` (§ One working tree, § Fleet conventions); this one was checked and does not fit there (zero subject-matter overlap -- repo-local pixi.toml hygiene vs estate/station architecture).
- (note) Input: docs/dreams/pixi-candidate-currency.md, ~560 lines, unusually complete -- three real audits, not hypotheses: a 44-package commented-out candidate ledger (each disposition cited to a real pixi install solve, a real local build, or a real repodata check), a 149-package active-dependency version-currency ledger (111 genuinely capped after a pixi update --dry-run re-solve, traced to terminal packages independently confirmed at their own latest), and a 30-package channel-sourcing debt ledger (every SelfExplainML-resolved package, verified against pixi.lock's own URLs, cross-checked against each recipe's cfe-on-conda-forge-status) plus 1 bare-PyPI dependency.
- (decision) Companion strategy: the Dream itself (docs/dreams/pixi-candidate-currency.md) is listed as an ADOPTED companion, not re-authored into new spec-owned files. Its three ledgers are live, ongoing, updated-in-place tables (not historical rationale that could move to sources:) -- downstream (the re-verification check, a future audit pass) reads them where they already live. Duplicating ~500 lines of table content into new companion files would be pure Spec-Law-rule-8 waste with no preservation benefit the adopted-companion pattern doesn't already give.
- (capability) CAP-1 candidate disposition contract: every commented-out pixi.toml candidate carries exactly one of five dispositions -- blocked (named re-check trigger), archived (upstream dead), stale (dead duplicate/superseded), ready (verified clean), unexamined (honest default). Success: every one of the Dream's 44 candidates already carries one; a newly-commented candidate with no disposition is the detectable gap this contract names.
- (capability) CAP-2 version-currency disposition contract: every active dependency below its conda-forge latest (after a pixi update --dry-run re-solve rules out stale-lock) carries one of blocked / platform-gap / deliberate-tradeoff / undiagnosed, naming the exact upstream package + depends/constrains spec read off an actually-resolved build -- never 'something conflicts'. Success: a shared root cause (e.g. one feedstock pin blocking N packages) is recorded once and every dependent package cites it, not re-derived per package.
- (capability) CAP-3 channel-sourcing disposition contract: every active dependency NOT resolved from conda-forge (SelfExplainML or bare PyPI) carries permanent-by-necessity / pending-conda-forge-submission / needs-recheck, cross-referenced against the owning recipes/<name>/'s own cfe-on-conda-forge-status where one exists. Success: a confirmed-on-conda-forge recipe whose package still locks from SelfExplainML (found live this session: fastmcp, slowapi) is flagged as needs-recheck, not silently accepted.
- (capability) CAP-4 durable re-verification: a doctor-owned advisory check (same station/gather pattern as chain_currency_sweep_check) reads each ledger's own recorded verification date and flags when it exceeds a declared staleness threshold -- advisory only, never a PR gate (Warden doctrine, fleet-wide). Success: a ledger last verified beyond the threshold produces a named finding; re-running the audit and updating the recorded date clears it; the threshold is a declared policy value, not a hardcoded magic number.
- (constraint) A blocked disposition must name what would unblock it (Dream's own constraint, carried forward) -- the difference between this ledger and the status quo is a re-check with a named trigger, not requiring someone to remember to look.
- (constraint) Don't re-derive a shared root cause per package (Dream's own constraint) -- one investigation, not N.
- (constraint) Trust local cfe-* metadata as a hint, not ground truth (Dream's own constraint, evidenced live: the langflow entry drifted stale within a single session) -- CAP-1..3 dispositions are always verified against pixi.lock/repodata/a real solve, never against the metadata field alone.
- (constraint) CAP-4's staleness check is advisory only, never a second PR gate -- matches the fleet-wide Warden doctrine every other doctor-owned check already follows.
- (note) Non-goals (from the Dream, carried forward): not a commitment to add any specific candidate or unblock any specific active dependency -- disposition is separate from remediation, a separate call per finding; not re-litigating conda-forge submission strategy for recipes already pending-approval/pending-submission (that's the recipe's own cfe-on-conda-forge-status lifecycle); not a commitment to actually fix any of the 33 version-currency terminal-package findings. New non-goal: not a merge of this environment-dependency ledger with the per-recipe cfe-on-conda-forge-status metadata -- they cross-reference (CAP-3) but stay two separate concerns (environment graph vs. submission status).
- (note) Operator direction (same session): added a fourth ledger to the Dream mid-spec-creation -- external-tracking gap (OpenTeams-WFT-CDO project board cross-reference, 197/335 untracked, 135 genuine candidates after excluding base infra/pyforge-own). Fold into the kernel as CAP-5, same disposition-contract shape as CAP-3.
- (capability) CAP-5 external-tracking disposition contract: every active dependency lacking a corresponding [Conda-Forge Packaging] <name>-shaped tracking item on OpenTeams-WFT-CDO's own project board carries visibility-gap-with-recipe (real packaging debt, invisible externally) or visibility-gap-unverified (no local recipe, plausibly already fine upstream) -- distinct from CAP-1..3's own disposition, which governs packaging/currency status, not external visibility. Success: base conda/pixi ecosystem infra and this repo's own pyforge-* packages are excluded by construction (never miscounted as candidates); a package's external-tracking disposition is never treated as a packaging verdict on its own.
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6+8): PASS -- 5 capabilities each with intent+success, IDs fresh CAP-1..CAP-5 (create operation), 4 constraints all rule something out, 4 non-goals, success signal concrete (cited-answer-in-seconds + CAP-4's staleness check as the currency proof), lean prose.
- (event) Self-validate Pass 2 (preservation vs docs/dreams/pixi-candidate-currency.md): PASS -- all 5 disposition taxonomies (candidate/currency/channel/tracking-gap) landed as CAP-1/2/3/5, the re-derive-don't-decay durability need landed as CAP-4 (mechanism left as an Assumption/Open Question since the Dream names the need, not the mechanism), all 4 Dream non-goals carried forward, the metadata-as-hint constraint carried forward. Companion strategy: the Dream itself adopted as companions: (live ledger data, not duplicated into new files). Wrapper-only content dropped: the Dream's narrative/rhetorical framing only -- no ledger data. Spec finalized, status ready.
- (event by claude) First-ever baseline stamp, 2026-08-31: this spec has never been stamped in `scripts/.spec-surface-baseline.json` (`no-baseline` finding, `spec-surface-check`). CAP-1..5 are planning-only so far — none of them are implemented in `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/` yet (no code references `pixi_candidate_currency`/`pixi-candidate-currency` anywhere under that surface), so this stamp establishes a fresh reference point going forward, not a retroactive claim about prior history. `pixi.toml`/`pixi.lock` carry no changes since this memlog's own last entry (2026-08-30T23:15) — verified via `git log --oneline` on both paths. `doctor/sources/**`'s recent commits (`6675de9670`, `11f8e63cbf`, `11780e08eb`) are pre-existing, unrelated doctor work (atlas epic course-correction, an unrelated `due-for-verification` timeout fix, atlas story-spec authoring) — none touch this spec's own subject. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency`.
- (event by claude) Surface reconcile 2026-09-01: `pixi.toml` changed once since the first baseline stamp by `af315d4bd7` (marshal Story 28.7, done) — new `[feature.local-recipes.tasks.index-freshness-check]` detector task only; no candidate-disposition or CAP-1..5 implementation touch. Bookkeeping under shared pixi.toml surface. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency`.
- (event by claude) Surface reconcile 2026-09-01, second pass: `[feature.pyforge-ci.dependencies]` gained `"ruamel.yaml"` so `tests/scripts/test_failure_catalog_check.py` can import `failure_catalog_generator` in the lean CI env; `pixi.lock` regenerated. No CAP-1..5 implementation touch. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): this spec's memlog HAD moved for the same pass, but did not NAME the 2 governed path(s) below, so spec-surface reported them `drift-presumed` (warn, non-gating). Naming them here closes that. All already landed on `main`; this records the baseline lag, not a re-review. No `SPEC.md` edit. Paths:
  - `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py` (changed)
  - `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/bmad_method.py` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency`.
- (event) Surface reconcile 2026-09-04 (fleet hygiene, PR #1043): PR #1040's `pixi lock` (64f3f8e584 — `pnpm >=12.3.1` → `>=12.3.2`, lock re-solved) moved this Spec's `pixi.toml` / `pixi.lock` rows without this memlog moving. Accept the current tree as the post-land surface; no contract change, no new product work. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency` (scoped, never a bare `--write-baseline`), from a clean tree so the recorded hashes are committed content.
- (event) Surface reconcile 2026-09-04 (PR #1043, second pass — the Detectors env): `pixi.toml` gains `[feature.detectors.dependencies]` (pyforge-doctor path dep, `ruamel.yaml`, `playwright-python`) and the `detectors` environment (no-default-feature; 86 packages) that `.github/workflows/detectors.yml` now installs pixi-first with `locked: true`, pip as the visible fallback; `pixi.lock` gains its block; `environment.yaml` (derived, `build` env) re-exported and caught up (python 3.14.6→3.14.7, nodejs 24.16→24.19 floors already in `pixi.toml`). Nothing this Spec governs changed in meaning; no contract change. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency` from a clean tree.
- (event) Surface reconcile 2026-09-04 (PR #1043, third pass): `pixi.toml` / `pixi.lock` rows moved for OTHER features (platform-ci-test gains the pyforge-steward path dep; dbgpt-sidecar gains pytz — Platform CI was red on both); nothing this Spec governs changed in meaning. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency`.
- (event) Surface reconcile 2026-09-04 (PR #1043, fourth pass — the fleet CI health pass): `pixi.toml` rows moved for the python-agent-platform feature only: the `rjsmin` / `rcssmin` `build = "py312*"` pins were dropped (they locked cp312 builds into the py3.14 image env, so the Containerfile's `compress` step was red) and the `cachebox <6` / `openfeature-provider-flagd <0.5.1` ceilings the platform policy suite guards were restored after commit 098f0f0672 had dropped them; `pixi.lock` re-solved by `pixi update rjsmin rcssmin` (rjsmin 1.2.5 / rcssmin 1.2.2 `py314` builds in the four envs that carry them; `pixi lock --check` clean, `environment.yaml` unchanged). Nothing this Spec governs changed in meaning. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency` (scoped, never a bare `--write-baseline`).
- (event) Surface reconcile 2026-09-04 (PR #1043, seventh pass — the local Platform CI replay): `pixi.toml` gains the `platform-ci-local` task under `[feature.local-recipes.tasks]` (`bash scripts/platform-ci-local.sh`): a local replay of `.github/workflows/platform-ci.yml` — the test job step for step, the three image builds, the container job's runtime smokes and golden-path-promotion + the deploy verifier — against an ephemeral PostgreSQL 17/Redis 7 from platform-dev, so a platform change is proven before it is pushed (operator ask, after four sequential Platform CI cycles for four one-line CI-only fixes). No dependency, environment or lock change; `pixi lock --check` clean, `environment.yaml` untouched. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency`.
- (change by claude) Surface touch 2026-09-05 (PR #1045): pixi.lock/pixi.toml re-solved for the new `pyforge-testing-kit` test-time dependency added to several stations' feature blocks (retro-pyforge-steward-2026-09-04.md action item 11: the fleet's whole-branch diff-guard test mechanics (git subprocess calls computing files/commits changed since origin/main) were consolidated into a shared `pyforge.testing_kit.branch_diff_guard` module (PR #1045).). No dependency floor/ceiling this spec tracks was touched. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency`.
- (change by claude) Surface touch 2026-09-05: pixi.toml gained the `platform-policy-suite-check` task (retro action item 3 (retro-pyforge-steward-2026-09-04.md, 2026-09-05): a new pyforge-doctor repo-scope source, `platform-policy-suite`, judges src/platform's manifest-only tests/policy subset by shelling out to the platform-ci-test pixi env's own interpreter (cli_bridge.run_pytest, AD-5), so a pixi.toml regression like the openfeature-provider-flagd/cachebox ceiling drop is caught even when Platform CI itself is disabled. WARN when that env is not installed, FAIL only on an actual policy-suite failure.); sources/__init__.py and sources/__main__.py registered/dispatched the new source; sources/platform_policy.py added. No dependency floor/ceiling this spec tracks was touched. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency`.
- (note by claude) 2026-09-05 (Story 45.1 pin PR, follows #1059): pixi.toml and pixi.lock changed — bmad-eval-quality >=0.2.0.dev0 added to feature.local-recipes' linux-64 and osx-arm64 target tables (unix-only variant on SelfExplainML), bmad-method-wds-expansion pin dropped (upstream deprecated), bmad-labs-skills comment 22 skills; lock re-solved for the pin swap only (no other package moved). Routine suite-pin upkeep this spec's surface already anticipates; no capability change against this spec.
- (note by claude) 2026-09-05 pixi upgrade sweep PR (branch maint/pixi-upgrade-sweep-2026-09-05): pixi.toml, pixi.lock changed — operator `pixi upgrade --pinning-strategy latest-up` pass landed as one maintenance PR: pixi 0.78.0->0.79.0 (requires-pixi + $schema + all 15 registered external sites via bump-pixi-version, environment.yaml regenerated), bmad-method >=6.12.0, bmad-suite >=2026.9.5, ~20 other floors bumped, lock re-solved; the cachebox <6 and openfeature-provider-flagd <0.5.1 ceilings the platform policy suite guards were restored after the sweep dropped them. No spec-content change; scoped --write-baseline follows this entry.
- (note by claude) 2026-09-05 hygiene-2 PR (branch maint/hygiene-2-2026-09-05): pixi.toml changed (`lint` task -> CI-parity conda-smithy via pixi exec; no dependency/lock change) and src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py changed (check_dream_vocab reads a `status:`/`type:` line with a trailing YAML comment instead of reporting 'no status: in frontmatter'; unit test added in tests/unit/test_sources_factory.py). Scoped --write-baseline follows.
- (note by claude) 2026-09-05 hygiene-2 PR, second pixi.toml touch (same branch): the conda-smithy `lint` task now exports GH_TOKEN from `gh auth token` when unset (the CalVer linter's --conda-forge mode calls GitHub per recipe; anonymous is 60 req/h) and its description records the corpus runtime. No dependency, env, or lock change. Scoped --write-baseline follows.
- (note by claude) 2026-09-05 hygiene: sources/board.py DEFERRED_SPECS gains spec-golden-path-conda-blind-spot (warden, draft, five open questions) so chain-completeness stops reporting spec-not-decomposed for a Spec that was seeded only to make the Dream's chain link durable; de-register once its open_questions clear and warden's epics carry CAP-1..5. No detector logic changed; 52 board tests green.
- (note by claude) 2026-09-05 fix/preexisting-detector-reds PR: sources/board.py DEFERRED_SPECS gains spec-intelligence-hub (steward, draft seeded the same day so dream-chain INV-1 clears; five CANDIDATE CAPs behind six open questions incl. the operator-queued RB-1/RB-2 research backlog) so chain-completeness stops reporting spec-not-decomposed for a Spec that exists only to make the Dream's chain link durable; de-register once RB-1/RB-2 close, the operator picks the shape(s), and steward's epics carry the chosen CAPs. No detector logic changed; 112 factory + board tests green. Scoped --write-baseline follows.
- (note by claude) 2026-09-05 same PR: sources/factory.py classify() gains the sweep-verdicts shape — implementation-artifacts/worktree-verdicts-<YYYY-MM-DD>.json -> local:sweep-verdicts (the dated JSON record scripts/worktree_sweep.py --format json emits; the first one, marshal's 2026-09-05 shutdown-sweep table, fell through to UNKNOWN and tripped a HARD uncovered on every local detectors-ci run). One dated re.fullmatch rule before the terminal return UNKNOWN, same convention as the prior spike-report / readiness-assessment shapes; check_coverage's fail-closed posture unchanged; new unit test mirrors the fleet-drain-runs one. Scoped --write-baseline follows.
- (note by claude) 2026-09-06 RB-1/RB-2 landing PR: sources/board.py DEFERRED_SPECS['spec-intelligence-hub'] reason text updated — RB-1 (Frame Spec v0.2.0 published) and RB-2 (nebari + nebi on conda-forge; NIC absent) closed the research half, so the entry now names only the operator's remaining questions; exit condition unchanged (shapes picked + steward epics carry them). String-only change, no detector logic; scoped --write-baseline follows.
- (note) Cross-station surface touch (marshal Story 30.2, 2026-09-06): sources/factory.py's TRACKED tuple and classify() dropped the ('project-context.md','context') entry / 'tracked:context' branch — BMAD 6.12 persistent_facts=[] retired the 8 per-station project-context.md rulebooks (D1); the surface is now the single AGENTS.md bmad:context block. No pixi.toml/pixi.lock/cf-atlas-data change. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (note) Cross-station surface touch (marshal Story 30.5, 2026-09-06): `chain.py`'s `mint_id_for_entry` comment fixed a dead file-path citation (`.claude/skills/bmad-dev-auto/step-04-review.md` -> `.claude/skills/bmad-build-auto/step-04-review.md`, the skill's own directory moved under the 6.12 shim-roster rename; the cited path no longer existed). Comment-only, no behavior change. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (event) Surface reconcile 2026-09-07 (fleet hygiene, pyforge-steward Story 46.3, spec-bmad-suite-lifecycle CAP-4 half): `pixi.toml` changed to add one `local-recipes` pixi task (`tea-test-review`, wrapping the real `tea-test-review` CLI with `--base origin/main --min-score 80` defaults) for TEA provisioning -- unrelated to this spec's own surface, but this spec's blanket `pixi.toml` glob makes it a co-governor of that one file. No product work here. Re-derived the governing set the same way Story 45.2/46.1 did (grep `pixi.toml` in every spec's `surface:` frontmatter) rather than assuming their own six-spec list still holds: it now numbers nine (this spec included), not six -- the surface set has grown since 45.2/46.1 landed. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency`.
- (note by claude) Cross-station surface touch (marshal Story 31.1, 2026-09-07): sources/factory.py classify() gained five new dated rules for TEA's bmad-testarch-test-design output shape (test-design-architecture.md, test-design-qa.md, test-design-progress-system.md, test-design/<slug>-handoff.md, planning-artifacts/reviews/*.md), tripped by marshal's Story 31.1 TEA equivalence run against all 8 stations. No pixi.toml/pixi.lock/cf-atlas-data change. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (note by claude) Surface touch 2026-09-06 (Story 20.1 + 20.2, Epic 20): sources/bmad_method.py's suite pass gained per-package probe-class routing (no new Source, no pixi.toml/pixi.lock change); sources/bmad_config.py added (new source bmad-render-config-ambiguity, Story 20.2) under this spec's sources/** surface; pixi.toml gained one new task (bmad-render-config-ambiguity-check) under the shared pixi.toml surface, no dependency/lock change. Nothing this spec's own CAP-1..5 (pixi candidate/currency dispositions) governs changed in meaning. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency.
- (note by claude) Surface touch 2026-09-07 (Story 20.2 review-pass patches): sources/bmad_config.py gained a fail-open UnicodeDecodeError guard and a sharpened zero-checked message; no dependency/lock or CAP-1..5 change. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency.
- (note by claude) Surface touch 2026-09-07 (Story 20.3, Epic 20): sources/frozen_path.py added (new source frozen-path-changed) under this spec's sources/** surface; sources/__init__.py and sources/__main__.py registered/dispatched the new source; pixi.toml gained one new task (frozen-path-changed-check), no dependency/lock change. Nothing this spec's own CAP-1..5 (pixi candidate/currency dispositions) governs changed in meaning. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency.
- (note by claude) 2026-09-06 mason Story 14.1: pixi.toml changed -- reworded both bmad-eval-quality pin comments (feature.local-recipes' linux-64/osx-arm64 target tables) to state the recipe is re-verified win-ready and only the __win artifact's Windows build/upload is outstanding, matching install-matrix.md's hazard-cell wording; no dependency, env, or lock change. Scoped --write-baseline follows.
- (change by claude) CI/hygiene sweep 2026-09-07: dream_chain's DEFERRED_SPECS dict gained one entry, `spec-bmad-cursor-interactive-routing` (owner marshal), registering a freshly-seeded stub Spec so dream-chain INV-1 (dream-without-spec) clears for the new docs/dreams/bmad-cursor-interactive-routing.md Dream. No other pyforge-doctor source behavior changed. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py.
- (note by claude) Surface touch 2026-09-07 (DW-FU-21-8-6, CI/hygiene sweep): sources/chain.py's `_flatten_deferred_scalar()` gained a truncation marker (no silent hard-slice; appends `... [truncated, N chars total]` when it actually cuts, original length recoverable) -- no new Source, no pixi.toml/pixi.lock/dependency change. Nothing this spec's own CAP-1..5 (pixi candidate/currency dispositions) governs changed in meaning. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency.
- (note) Story 32.4 (spec-fleet-consistency-standard CAP-3) retired epics-with-stories.md fleet-wide -- BMAD 6.12 produces it nowhere, six of eight copies were frozen at 2026-08-08 while epics.md moved to 2026-09-06, and a line-diff audit of all eight confirmed nothing normative lived only there (steward's test-tier mandate is at its own epics.md:74; mason's OQ-E2/OQ-E5 and steward's story-sizing convention are likewise in epics.md; herald's 30 stories are a SUBSET of the 64 in its epics.md). No source change for this spec; its surface intersects the retired artifact only through the shared planning-artifacts tree.
- (note) Same pixi.toml change as recorded on spec-pyforge-core. No doctor source change.
- (note) Story 32.8 (CI follow-up to 32.5, spec-fleet-consistency-standard CAP-2): atlas's dashboard/, publish/ and wasm/ moved from tests/unit/ to tests/integration/. All three are fail-loud gates needing provisioned prerequisites (Playwright's managed Chromium, the DuckDB httpfs extension, a built WASM artifact) that pyforge-station-tests.yml provisions for its whole-tree run but a coverage lane has no business installing. Integration weight by the same reasoning that placed warden's oracle gates there. Ten pixi tasks that still named pre-move atlas paths were repointed, and the eight test-architecture.md files regenerated from the live inventory. No doctor source change; pixi.toml task-path rewrites only.
- (note) Deferred-work sweep 2026-09-08 (spec-deferred-work-resolution-sweep CAP-4): this spec's surface saw two comment/vocabulary-only changes made while VERIFYING entries, not while building -- marshal cli/config.py's stale '9 keys' numeral removed (DW-FU-31-3-3), and doctor sources/chain.py's _KNOWN_FIELD_KEYS gained `note` so a real note: field stops being folded into the preceding evidence: block (DW-FU-31-6). No behaviour change; marshal 7530 and doctor 1450 tests pass.
- (change) Sweep follow-ups 2026-09-08 -- surface-touch note, no change to this spec own subject. pixi.toml gained one read-only detector task (`ledger-direction-check` -> `python -m pyforge.doctor.sources ledger-direction`); no dependency, channel, platform, environment or pin changed, and `pixi project export conda-environment -e build` is byte-identical to the committed environment.yaml. chain.py and ledger.py changed for the deferred-work sweep follow-ups (path-token precision + ledger-direction base_ref evidence), reconciled in full on pyforge-doctor/spec-pyforge-doctor and pyforge-doctor/spec-deferred-work-visibility. RECONCILES: pixi.toml, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/ledger.py. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency.
- (change) Fleet consistency pass 2026-09-08 (round 2) -- surface-touch note, no change to this spec own subject. pixi.toml gained one read-only detector task (`ad-citation-check` -> `python scripts/ad_citation_check.py`, architecture-decision citation integrity); no dependency, pin, platform or environment changed, and `pixi project export conda-environment -e build` remains byte-identical to the committed environment.yaml. Reconciled in full on pyforge-marshal/spec-fleet-consistency-standard, which governs the detector. RECONCILES: pixi.toml. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency.
- (change) Surface-touch note 2026-09-08: pixi.toml and pixi.lock gained the `scribe-pg` feature (postgresql + pgvector, linux-64/osx-arm64 only) and the composed `pyforge-scribe-pg` environment, giving pyforge-scribe durable GraphStore tests a pixi-provisioned database instead of a hand-rolled container. No existing environment, dependency or pin changed, and `pixi project export conda-environment -e build` remains byte-identical to the committed environment.yaml. Reconciled in full on pyforge-scribe/spec-pyforge-scribe. RECONCILES: pixi.toml, pixi.lock. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency.
- (change) Surface-touch note 2026-09-08 (follow-up): one comment line in pixi.toml qualified an unprefixed citation to pap:AD-16, the local-first / zero-containers decision, after the ad-citation detector flagged the same unprefixed form in scribe memlog. Text only -- no dependency, pin, platform or environment changed. RECONCILES: pixi.toml.
- (note by claude) Cross-station surface touch 2026-09-09 (foreign-spec reconcile): `pixi.toml` COMMENT-ONLY edits, no dependency, version, floor, task command, or env membership changed -- `pixi install --frozen -e local-recipes` still agrees with `pixi.lock`, and `pixi project export conda-environment -e build` is byte-identical, so `environment.yaml` needed no regeneration. Two comments were factually stale: (1) the `pr-artifacts` task description advertised `conda_pkgs_(linux|osx|win)`, which omitted `conda_pkgs_noarch` -- the ONLY artifact carrying a `noarch:` recipe's package -- a real defect fixed in CFE v8.88.1 that had made the task return 0 packages for every noarch recipe since v8.14.0; (2) both `bmad-eval-quality` pin sites (`[feature.local-recipes.target.{linux-64,osx-arm64}.dependencies]`) claimed the `__win` artifact's Windows build/upload was still outstanding, which stopped being true on 2026-09-09 when the `__win` noarch variant was built on a GH Actions windows-2022 runner and uploaded to SelfExplainML (a staged-recipes PR builds that variant, tests it green, then discards it -- CFE G115). The version FLOOR was deliberately left at `>=0.2.0.dev0` even though the recipe is now 1.3.0: the floor already admits 1.3.0, while raising it would make `pixi.lock` stale and red every `pixi run --frozen` lane until a full re-lock -- that is a separate change with its own blast radius, and the reason is now recorded inline so the next reader does not re-litigate it. RECONCILES: pixi.toml. Scoped-stamped: `--write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency`.
- (change by claude) 2026-09-09 pixi.toml/pixi.lock reconcile — operator-directed dependency refresh, three changes in one pass. (1) `pixi upgrade --pinning-strategy latest-up` with 19 excludes: 14 floors moved (gh, gitpython, spec-kit, crispy-bootstrap5 x2, django-anymail x2, djlint, sse-starlette, copier, pnpm) and the lock followed. (2) pixi 0.79.0 -> 0.80.0 across all 17 registry sites via `bump-pixi-version -- 0.80.0` (the operator self-updated the machine); pixi-version-check is clean and no 0.79.0 remains anywhere. (3) bmad-suite currency: TEA floor >=1.24.0 -> >=1.25.0 (published), and bmad-eval-quality left the linux-64/osx-arm64 target tables for the shared table at >=1.3.0 (mason Story 14.1 — both noarch variants are now published, so the win-64 solve resolves h2fd06db_0; the lock diff shows exactly that new win-64 entry). CORRECTION APPLIED IN THE SAME PASS: latest-up STRIPPED five deliberate upper bounds, which was a real regression, not a refresh — openfeature-provider-flagd `,<0.5.1` (its own comment says 0.5.2 needs protobuf 7 and cannot solve with langflow's a2a-sdk), cachebox `,<6` x2, and asgiref `,<4.0` + channels_redis `,<5.0` (both under a byte-identity contract with pyforge-steward's pyproject `[dashboard]` extra, enforced by tests/meta/test_invariants.py, which failed until restored). All five caps restored; the lock still satisfies them (pixi lock --check clean) and the invariant suite is 17/17. RECONCILES: pixi.toml, pixi.lock. Scoped-stamped.
- (change by claude) 2026-09-09 (same day, second lock movement): the operator ran `pixi update` + `pixi install -a` on their machine, re-resolving pixi.lock wholesale -- 1860 insertions / 2643 deletions, a NET REDUCTION of ~783 lines. `pixi.toml` was NOT touched by that run, so the six deliberate upper bounds restored earlier in this pass are intact in the manifest. Verified the new lock honours them rather than assuming: `pixi lock --check` is clean (which is the authoritative proof, since it re-validates every manifest constraint against the resolved set), and spot-checks agree -- cachebox 5.2.3 (<6), asgiref 3.12.1 (<4.0), coderedcms 6.0.0 (<7.0). openfeature-provider-flagd resolves BOTH 0.5.0 and 0.5.2 in the lock, which is correct and pre-existing, not a cap leak: two different tables pin it (line ~231 capped `>=0.5.0,<0.5.1` for the langflow/protobuf-6 constraint, line ~447 uncapped `>=0.5.2`). This pass's own pin work also survived the re-resolve: bmad-eval-quality 1.3.0 and bmad-method-test-architecture-enterprise 1.25.0. Post-update gates: environment.yaml still IN SYNC (export is manifest-derived), tests/packaging 173 passed / 2 skipped, pin-sync invariants 17/17. RECONCILES: pixi.lock. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency.
- (change by claude) 2026-09-09 (second pixi movement this pass): bmad-eval-quality floor >=1.3.0 -> >=1.4.1 and pixi.lock re-solved, now that BOTH noarch variants of 1.4.1 are published and indexed (`__unix` h07402fc_0 via macos-14, `__win` h2fd06db_0 via windows-2022). This is the floor rule stated in CFE G117 being exercised: the pin tracks the newest PUBLISHED build, never the newest the recipe builds -- it moved only once the artifacts existed. Lock resolves both variants; the one remaining 1.3.0 entry belongs to the bmad-suite-full env, which resolves through the PUBLISHED metapackage 2026.9.5 and so is expected. environment.yaml re-exported and unchanged. tests/packaging 173 passed / 2 skipped. RECONCILES: pixi.toml, pixi.lock. Scoped-stamped.
- (change by claude) 2026-09-09 (third pixi movement this pass): `networkx >=3.0` RESTORED to [feature.build.dependencies], so environment.yaml carries it again. Not a currency bump -- a missing runtime dep. `.ci_support/build_all.py` does `import networkx as nx` at line 8, and `.scripts/build_steps.sh` runs it from THIS env (it installs environment.yaml, exported from [feature.build]); the pin had been commented out, so every Linux Docker run died on `ModuleNotFoundError: No module named 'networkx'` before reaching a single recipe. That is a THIRD independent reason that lane could never build here, alongside the private-repo `git fetch` and the prune-everything-on-main selection -- all three pre-existing and all three found only by actually dispatching the lane. Restored WITHOUT the old `2.4.*` ceiling: it dates to 2019, its stated reason ("conda-build compat") no longer holds, and build_all.py uses only DiGraph/topological_sort. Strictly additive -- the pin was commented out, so nothing depended on it. Re-locked, environment.yaml re-exported (now carries `networkx >=3.0`); pixi lock --check, pixi-version-check, llms-full-check all clean and tests/packaging 173 passed / 2 skipped. RECONCILES: pixi.toml, pixi.lock. Scoped-stamped.
- (event by claude) status: (absent) -> ready — fleet-readiness-decision-batch-2026-09-09 doctor-B6 (operator-approved 2026-09-09). The Spec carries an id: and no status: key, which is the only reason chain-completeness reports ok on it: all 5 declared CAPs are uncovered by any doctor epic or FR (re-run of board.py's own _parse_declared_cap_ids / _cited_cap_ids_by_spec with the status gate lifted, 2026-09-09), and it would fire spec-not-decomposed today if it carried an open status.
- (decision by claude) CAP-1/CAP-2/CAP-3/CAP-5 are satisfied by the Dream's own four ledgers, not by code — reword them as such rather than decomposing them into stories. CAP-4 (the advisory ledger-staleness check) is the one code capability and gets exactly one doctor story: a new sources/pixi_currency.py, one entry in sources/__main__.py's DISPATCH, one detectors row in pixi.toml, and the staleness threshold as a declared policy value (never a magic number in the module). Cite: fleet-readiness-decision-batch-2026-09-09 doctor-B6.
- (note by claude) Live drift measured 2026-09-09, and it is exactly what CAP-4 exists to catch: the Dream audited 44 candidates over 64 commented dependency lines; pixi.toml now carries ~62 commented dependency lines (the readiness pass's count; a looser regex over the same file this session returns 66 — the exact figure is the story's to settle, the direction is not), with 45 commits to pixi.toml since the last currency pass. Without CAP-4 the Dream is a snapshot decaying the way it was written to prevent, and its own Success signal ('that answer is demonstrably still current … because CAP-4's staleness check would have flagged it otherwise') is false.
- (constraint by claude) CAP-4's staleness check stays advisory — warn at most, never a second PR gate (doctor doctrine: verdict.exit_code_for is the sole verdict owner, pinned by tests/meta/test_verdict_sole_ownership.py). Already stated in the Spec body; restated here because the story minted this pass is the first code to bind to it.
- (event by claude) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS; CAP-1..5 preserved (none retired), status: ready added, CAP-1/2/3/5 marked satisfied-by-ledgers, CAP-4 marked the single code capability, live-drift measurement and threshold-is-policy constraint recorded; the open question on per-finding age-checking stays open.
- (event) 2026-09-13 pixi.toml / pixi.lock overlap: graphifyy on feature.pyforge-scribe. No doctor currency product change.
- (event by claude) Surface reconcile 2026-09-14: commits c48a65e8ad ('Add a CAP extract detector to detectors-ci') and 9d7528297866 ('Require a 54.1 case-list id for verified-in-foundry') added sources/capability_ledger.py and touched sources/__init__.py for a new capability-extraction detector -- unrelated to this Spec's own pixi-candidate-currency reads, picked up only because both live under sources/. No pixi-currency behavior changed. Separately, local-recipes PR #1354 added a silo dependency to pixi.toml (commit 5dc73f0660, platform-dev feature) and a pyforge-station-tests task (commit a675e8b009, local-recipes feature, no lock impact) -- routine dependency/task additions, not a candidate-freshness change. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency.
- (event by claude) Surface reconcile 2026-09-14: board.py's INV-A gained a delivered-Spec branch. It previously skipped EVERY non-open Spec status (`if status not in OPEN_SPEC_STATUSES: continue`), so a `shipped` Spec delivered with no epic and no ledger row was structurally invisible -- ten existed fleet-wide. New constants DELIVERED_SPEC_STATUSES/DECOMPOSITION_OWED_STATUSES add `shipped` only; `absorbed`/`archived`/`superseded`/`extension-point` stay exempt for stated reasons. The delivered branch is held to the WHOLE-SPEC standard (does any FR or epic reference it at all), never INV-A's per-CAP citation test -- per-CAP flagged 34 Specs against 12 real ones because epics predating the cite-every-CAP-id convention name the Spec or its Dream without enumerating CAP-n. Matching also accepts an owner-dream path, since Epic 51 claims its Spec that way. Open-Spec behaviour is byte-unchanged. Also registered spec-vocabulary-one-name-one-job in DEFERRED_SPECS (operator-owned open questions). Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14: two foreign paths in this Spec's surface moved, neither changing its own contract. `board.py` gained INV-B's epic arm (doctor DW-CHAIN-COMPLETENESS-7 -- full entry in spec-pyforge-doctor's memlog): `^##\s+Epic\s+(\d+)` headings are now compared against `epic-N` ledger keys in both directions, closing a blind spot where `story_rows` discarded every `epic-*` key before comparing. `pixi.toml` changed for the `frame-preflight` description (steward 53.6) and to REPAIR `pyforge-station-tests`, which had shipped as a bash `for` loop pixi's task shell cannot parse and so had never run; it is now a declarative cross-environment `depends-on` and was verified green across all 8 stations. `environment.yaml` re-exported byte-identical. Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (PR #1355 CI remediation): `pixi.toml` gained nine tasks and `board.py` gained coverage pragmas; neither touches this Spec's contract. (1) Eight `pyforge-<station>-coverage-gate` tasks plus a `pyforge-station-coverage-gates` aggregate now mirror `.github/workflows/coverage-gates.yml`'s `named-module-gates` matrix locally -- that gate measures only the modules a branch TOUCHES, so `pyforge.doctor.sources.board` had sat at 70.1% against an 80% floor since the Guildhall console was retired and only surfaced when an unrelated edit touched the file. (2) A `pr-preflight` aggregate runs the four lanes that actually red a PR (`detectors-ci`, `test-ci`, `pyforge-station-tests`, `pyforge-station-coverage-gates`), because two CI failures on #1355 had no local equivalent at all. (3) `board.py`'s retired-Guildhall machinery is marked `# pragma: no cover` with a block comment stating why -- it cannot execute while `retired-console-check` fails CI on reintroduction; module coverage went 72% -> 94% with no test or behaviour change, tracked for deletion as `DW-DASHBOARD-DEAD-CODE-1`. `environment.yaml` re-exported and byte-identical (task-only edits). Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (post-merge of PR #1355): `board.py` registered `spec-coverage-gate-independence` in `DEFERRED_SPECS` — a one-key dict addition, no behaviour change to this Spec's contract. Seeding that Spec in #1355 reddened `chain-completeness` INV-A on main (doctor owned an open `draft` Spec no epic referenced); it is not decomposable because its five open questions are design decisions about where a fleet-wide blocking gate may live, and every obvious home is already ruled out. Full entry in spec-pyforge-doctor's memlog. Scoped-stamped.
- (change by claude) Surface reconcile 2026-09-14: `board.py` lost **494 lines** of retired-Guildhall dead code (ten functions, ten `_LAYOUT_*` constants) with **zero behaviour change**, closing `DW-DASHBOARD-DEAD-CODE-1`. An AST call-graph pass proved they formed two closed PRIVATE ORPHAN islands -- the layout chain (`_run_check_layout`, called by nothing) and the drift chain (`_check_dashboard_drift`, called by nothing) plus two standalone orphans -- so ten of eleven suspects had no live caller at all. Both gathers, their DISPATCH entries, their `Source` taxonomy members and `report-schema.json` are untouched; the ledger entry had over-stated the scope as "a behaviour change to two registered sources plus taxonomy and schema", inferred from the code's location rather than measured from its call graph, which is why it had been deferred as needing a Dream when it was hygiene. Verified by byte-comparing `--json` findings for `dashboard-drift`, `check-layout`, `chain-completeness` and the `--layers` audit before and after: all four IDENTICAL. Full doctor suite green (1378 passed). Coverage 72% (pre-pragma) -> 94% (pragma) -> **95% with the code actually gone**. Only `_load_dashboard_generate` survives, genuinely reachable from `_gather_chain_layers_audit`; its pragma stays because its reachable path is closed by policy (`retired-console-check` fails CI on reintroduction), not by structure. Stale prose rewritten rather than left pointing at deleted helpers. Scoped-stamped.
- (event) Surface reconcile 2026-09-14: board.py DEFERRED_SPECS gained marketplace and work-passport draft Specs (steward chain links). No CAP change on this Spec. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py. Scoped-stamped: --write-baseline --spec pyforge-doctor/spec-pixi-candidate-currency.
