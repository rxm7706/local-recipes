---
topic: pyforge.doctor.sources.factory::classify() has no rule yet for the spike-report artifact shape
updated: 2026-08-21T22:09
---

- (note) Governs pyforge.doctor.sources.factory::classify()/check_coverage (pyforge-doctor, 1 candidate file: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py). Source: docs/dreams/bmad-drift-new-artifact-shape.md, captured 2026-08-11.
- (capability) CAP-1 classify() recognizes the spike-report shape -- a file matching the 'design-spike PASS/FAIL report written to a project's planning-artifacts/ root' convention (e.g. spike-0-copier-api-fit-report.md) is classified rather than falling through to UNKNOWN, so check_coverage stops HARD-failing it as uncovered.
- (capability) CAP-2 the fix ships as one classification rule, in the same place and dated-comment convention as the prior 14-shape (2026-07-28) and 11-shape (2026-08-08) additions -- no change to check_coverage's fail-closed default for files the rule genuinely does not match.
- (constraint) check_coverage's HARD-by-default posture is unchanged: an unrecognized file is still a hole, not a pass. This widens the recognized set by exactly one shape; it does not add a general auto-accept path for arbitrary new files.
- (constraint) The classifier's independence rule (never importing pyforge.marshal or any station package it judges) and its per-check isolation model are out of scope and stay exactly as they are.
- (note) Non-goal (from the Dream): not a redesign of check_coverage's fail-closed philosophy, and not a general new-shape-tolerance mechanism -- one confirmed shape gets one confirmed rule, same as every prior incident.
- (question) Exact rule scope is undecided -- literally spike-*-report.md at planning-artifacts/ root, or a slightly wider pattern anticipating a future Spike-1/Spike-2 -- a story-level design decision, not resolved by the Dream.
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6+8): clean -- both capabilities carry intent+success, constraints each rule something out, one explicit non-goal, success signal is a concrete re-run of check_coverage against the live tree, capability IDs stable.
- (event) Self-validate Pass 2 (preservation): clean -- every load-bearing claim from the Dream (the concrete trigger, the historical 14-shape/11-shape precedent, the live-tree confirmation of exactly one uncovered file, the non-goal boundary) landed in SPEC.md; no wrapper-only drops.
- (question) Resolved by Story 6.11's own design decision: the wider pattern, generalized over the spike index and slug -- `planning-artifacts/spike-\d+-[a-z0-9-]+-report\.md` -- not a `spike-0`-only literal, so a future `spike-1`/`spike-2` report following the same convention stays covered without a repeat incident.
- (change) Story 6.11 SHIPPED, 2026-08-11 (FR-16, CAP-1, CAP-2). Reconciles by name: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py (`classify()` -- one new dated `re.fullmatch` rule immediately before the terminal `return "UNKNOWN"`, returning `archive:spike-report`) and src/shared/packages/pyforge-doctor/tests/unit/test_sources_factory.py (three new tests: the real shape is classified, a second spike index is also classified proving the rule isn't hard-coded to `spike-0`, and a look-alike missing the numeric index still HARD-fails `uncovered` -- proving CAP-2's fail-closed-default constraint held).
- (event) Success signal verified live: `check_coverage` against the real `pyforge-marshal` project tree returned exactly one `uncovered` finding (`planning-artifacts/spike-0-copier-api-fit-report.md`) before this change; after, it returns zero, and `pixi run -e local-recipes detectors-ci` reports clean for `bmad-drift`.
- (event) Surface reconcile 2026-08-21 (BMAD 6.11 upgrade branch): factory.py classify() gained archive:readiness-assessment (planning-artifacts/*-readiness-assessment.md) — marshal S-3.13's parallel-fan-out assessment tripped the HARD uncovered finding and redded the meta-test gate; fourth instance of the spike-report lesson, 54 factory tests green
