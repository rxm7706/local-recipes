# Marshal project-policy layer for pyforge-doctor.
#
# The MIDDLE layer of AD-16's fixed precedence: Marshal defaults -> THIS FILE ->
# invocation flags, last wins. Found by convention at
# `planning-artifacts/marshal-policy.toml`; `marshal config --project-policy PATH`
# still overrides the lookup.
#
# WHY THIS FILE IS TRACKED WHILE THE RENDERED ONE IS NOT. Story 1.10 correctly made
# `.bmad-loop/policy.toml` a derived artifact (AD-12/AD-35) and untracked it, closing
# the F-1 cross-project bleed. But nothing reachable RENDERED it: the writer's only
# callers were its own tests, `marshal config` merely printed, and no pixi task called
# either. Merging that story then DELETED the file from pyforge-marshal's own loop
# home, leaving it with no policy at all -- the exact state 1.10's SEQUENCING (hard)
# note forbade. A derived artifact needs a governed SOURCE; this is that source.
#
# Only keys that differ from Marshal's built-in defaults appear here. Deliberately
# ABSENT:
#   * `worktree_seed_paths` -- generated from the project slug, so restating it would
#     create a second truth that could drift.
#   (`model_tier_map` lives at file end — Story 33.6 / FR-51.)
#
# gate_mode: unattended (operator direction, 2026-07-26). This is the HUMAN approval
# gate ONLY -- the independent reviewer still runs on every story.
gate_mode = "none"

# harness_preference (Story 22.8, FR-193 CAP-8): cursor-only while Claude Code
# is spend-limited (2026-09-01 Epic 28 + atlas drain on composer-2.5-fast).
# Mirrors pyforge-marshal's/-atlas's/-warden's/-steward's own override -- this
# station never got it, so its dispatches inherited the repo-default
# `harness_preference` (claude-first) while still resolving `model_tier_map`'s
# composer-2.5-fast (a Cursor-only model name), a pairing Claude Code's CLI
# refuses outright. Found live 2026-09-10 when the drain-to-zero dispatch
# failed identically at launch.
# 2026-09-18 (operator ruling: Cursor is out of usage for the week) -- claude-only,
# mirroring pyforge-herald PR #1458. `--harness claude` alone cannot move a
# station whose tier map names an inline `{ harness = "cursor", ... }` dev entry
# (Story 28.11 leads the walk with it; spec-pyforge-marshal CAP-246 / Story 50.3
# is the fix). Flip this line and the tier map together when Cursor returns.
harness_preference = ["claude"]

# merge_subject_template (AD-24): station-scoped, NOT the repo default
# "Merge {key} into main" -- the default carries no station token and Story
# 35.1's known_keys corroboration only rejects keys this ledger does NOT know,
# so another station's "Merge N-M into main" merge reads as this station's N.M
# already merged (herald PR #1457's finding; spec-pyforge-marshal CAP-247 /
# Story 50.4 makes this the repo default). Rendered subjects still classify
# marshal-native.
merge_subject_template = "Merge pyforge-doctor/{key} into main"


# verify_commands: THIS station's own suite. Every home except atlas and warden was
# cloned from warden's and inherited `pyforge-warden-test`, so a story here would have
# gone green on warden's tests while this station's own never ran.
verify_commands = [
  "pixi run --frozen -e pyforge-doctor pyforge-doctor-test",
]


# epic_surfaces (AD-27): station-wide default for every currently-declared
# epic (2026-08-31, fleet-uniformity pass). Without an entry, effective_surface
# falls back to an EMPTY tuple -- MRS-GATE-007 fires on every changed path,
# unconditionally (see dispatch_verify.py's plain dict .get(epic, ()) lookup,
# no wildcard/default key support). This station had no [epic_surfaces] block
# at all until now. Same stopgap pattern as pyforge-atlas/-marshal/-scribe.
[epic_surfaces]
"1" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"2" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"3" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"4" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"5" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"6" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"7" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"8" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"9" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"10" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"11" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"12" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"13" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"14" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"15" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"16" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"17" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]
"18" = [
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
]

"20" = [  # whole-suite drift + two detectors + RCA routing (spec-bmad-suite-lifecycle doctor relays), added 2026-09-06
  "src/shared/packages/pyforge-doctor/**",
  "_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/**",
  "_bmad-output/projects/pyforge-doctor/implementation-artifacts/**",
  ".gitignore",
  "pixi.toml",
  "pixi.lock",
  "environment.yaml",
  "scripts/.spec-surface-baseline.json",
  ".claude/skills/bmad-agent-doctor/**",
  "AGENTS.md",
]

# model_tier_map (FR-51, Story 33.6): difficulty class -> {stage: model}.
# Must stay LAST in this file (TOML table scoping).
# context.planning-graph (Story 28.27, token-economy CAP-6/CAP-13): verified
# live 2026-09-10 against pyforge-marshal (the canary) -- grounded retrieval,
# real measured savings, zero further engineering needed. scribe recall's
# own project-scoping gap (a lexical query naming a project slug could still
# be outscored by another project's denser-vocabulary node) was found and
# fixed in the same pass -- see scribe Story marshal-28.27 / recall.py's own
# scope= parameter. wire/output/structure-graph/derived-context stay
# declared-off (undeclared = default off) pending Story 28.31 (dispatch
# cost spike) / 28.33 (spin reference wiring).
[context."planning-graph"]
enabled = true

# context.derived-context (Story 28.28/28.32, token-economy CAP-5): scribe's
# `index refresh --declare` shipped and the lean pyforge-scribe env's
# missing cocoindex dependency was fixed -- verified live 2026-09-10
# against pyforge-marshal, mode: incremental with zero findings.
[context."derived-context"]
enabled = true

# context.output (Story 28.30/28.32, token-economy CAP-3): caveman skill
# deployment now reaches dispatch worktrees too (not just loop homes), and
# bmad-build-auto's own step-01-clarify-and-route.md tells the agent to
# invoke it.
[context.output]
enabled = true

# 2026-09-18: back on Claude Code (see harness_preference above) -- the same
# sonnet-dev / opus-review pairing the plain [adapter]/[adapter.review]
# baseline uses, kept as explicit {harness, model} tables in the fails-safe
# shape Story 33.15 established (spec-cursor-native-tier-map CAP-1). The
# 2026-09-13 Cursor Ultra ladder (composer-2.5-fast / grok-4.6 / composer-2.5)
# is in git history for the week Cursor usage returns.
[model_tier_map.heavy]
dev = { harness = "claude", model = "sonnet" }
review = { harness = "claude", model = "opus" }

[model_tier_map.medium]
dev = { harness = "claude", model = "sonnet" }
review = { harness = "claude", model = "opus" }

[model_tier_map.easy]
dev = { harness = "claude", model = "sonnet" }
