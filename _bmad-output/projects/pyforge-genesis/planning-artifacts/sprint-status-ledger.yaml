# GENERATED — do not hand-edit. Regenerate with:
#     pixi run -e local-recipes sprint-ledger-sync
#
# RETIRED 2026-07-30. `pyforge-genesis` is the CONSTITUTIVE project: it records
# the operating model and ships nothing. Its `epics.md` has said so since the
# 2026-07-28 split ("No epics, and that is the contract"), and the installer's
# buildable half moved to pyforge-marshal as `epics-genesis-installer.md`.
#
# The split moved the epics and left the feed. Until today this file — and its
# Tier-3 source — still listed all 36 installer stories, and `bmad-loop` reads
# `[stories] source = "sprint-status"`. Launching this loop home would have
# dispatched `1-1-package-skeleton-as-a-pixi-workspace-member` and built a
# package for the project whose contract is that it ships nothing. genesis's own
# epics.md names the tripwire — "if epics reappear here, that is the signal to
# check whether buildable work has drifted back into the constitutive project"
# — and the feed WAS that drift; it simply never left.
#
# Those 36 stories now live in pyforge-marshal's feed as epics 7–12 (shifted +6:
# 33 of the 36 ids collided with marshal's own, and AD-23 requires `<epic>.<seq>`
# to be unique within a project).
#
# An empty feed is the correct steady state here, not a gap. A constitutive
# project has no stories to run, and this file states that rather than leaving
# it ambiguous — the same reasoning INV-3 applies to the empty epics tier.
#
# project: genesis
# stories: 0
development_status: {}
