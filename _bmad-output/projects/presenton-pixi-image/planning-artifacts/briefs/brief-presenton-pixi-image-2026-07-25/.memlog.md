---
topic: presenton-pixi-image
updated: 2026-07-25T06:31
---

- (event) Headless/express BMAD planning-chain run; brief created from pre-existing prd.md draft + two 2026-07-25 research reports (technical: Presenton stack/OCP/air-gap; domain: regulated-enterprise market)
- (override) Dropped template-style-extractor from v1 recipe scope: technical research found upstream Presenton already solves template import (DOCX/PPTX/XLSX/ODF) LibreOffice-free via stdlib zipfile+ElementTree, rejects legacy .doc/.ppt with the exact error the PRD proposed inventing; PDF text extraction already uses pdfplumber (MIT), no PyMuPDF. Reframed as net-new capability if style-preserving import is ever wanted, not a LibreOffice replacement.
- (decision) Added memory/RAG subsystem (mem0ai + fastembed-vectorstore) as a newly-discovered conda-forge gap, unconditional deps in current Presenton, neither on conda-forge; flagged as Phase-0 decision (add 2 more recipes vs. feature-drop) rather than silently expanding recipe count
- (override) Corrected Risk R3 trigger: PRD's named trigger (Microsoft Copilot IL5 GA) already happened without collapsing JTBD, because IL5 remains Azure Government cloud, not air-gapped. Real trigger is Microsoft's own disconnected stack (Azure Local disconnected ops + Microsoft 365 Local + Foundry Local), GA'd 2026-02-24; whether it includes Copilot-for-PowerPoint-equivalent deck generation is unresolved and now a Phase-0-blocking verification item, not a 12-24mo monitoring item
- (change) Re-pinned llmai reference version 0.2.2 -> 0.2.8 (PyPI verified); still Apache-2.0, still not on conda-forge, core PRD claim confirmed correct
- (assumption) psycopg (LGPL-3.0-only) flagged for buyer legal/compliance review alongside the now-resolved PyMuPDF/R7 risk; assuming LGPL is likely acceptable on most enterprise allowlists but not asserting this without buyer confirmation
- (decision) Named a Phase-0 spike for headless-Chromium-sandbox vs OpenShift restricted-v3 pod-level user-namespace isolation (hostUsers:false) interaction; --no-sandbox posture must be an explicit, buyer-documented security decision, not a silent default
- (decision) Carried forward unchanged from PRD: infrastructure/developer_tool classification, 9-persona user model, buyer-gate/user-gate JTBD, M365-Copilot-for-PowerPoint reference-product framing, Q1-Q4 decisions (image-overlay fidelity, dev/prod LLM tiering, full-air-gap posture), competitive table, six-recipe baseline minus template-style-extractor plus the two new memory-subsystem candidates
- (event) brief.md + addendum.md drafted; express mode skipped the parallel editorial-review-structure/prose subagent polish pass (effort budget for the full 5-stage headless chain) in favor of a self-reviewed single pass; flagged for optional follow-up polish
- (event) brief finalized, handing off to bmad-prd for PRD revision
