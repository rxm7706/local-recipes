---
topic: Upload a manifest, watch warden+atlas analyze it — a service face over existing engines
updated: 2026-09-09T15:44
---

- (capability) CAP-1 the service skeleton: manifest upload (safe-loader validation), async Celery execution calling the EXISTING warden/atlas engines, keys-not-blobs; CAP-2 results: SBOM + vuln/license/currency reports rendered from engine output, derived progress (FABRIC _phase_guard pattern — no phase chooses its own number). FABRIC is the Apache-2.0 shape reference; engines never reimplemented
- (event) Spec authored + decomposed 2026-08-22 as warden Epic 8 (2 stories)
- (note by claude) 2026-09-09 readiness pass, verified against live code: the DEPLOYED IMAGE CANNOT IMPORT pyforge.warden.cli. django_warden_fabric/tasks.py:97-102 lazy-imports it ('platform host must import without pyforge installed -- Story 10.1 boundary'), but pyforge-warden is absent from [feature.python-agent-platform.dependencies] in pixi.toml -- its sole path dep is pyforge-steward (pixi.toml:368, added 2026-09-04 for exactly this class of breakage) -- and src/platform/Containerfile installs that environment alone, so every uploaded manifest lands FAILED via tasks.py:89-94. The fix is minted as STEWARD Story 48.11 (add pyforge-warden = { path = "src/shared/packages/pyforge-warden" } to the platform env, add an import test, regenerate environment.yaml -- the sync check is ungated by the maintenance label); it is steward's surface, warden's capability. Until 48.11 merges, CAP-1's success line ('completes against the real engines') is unmet in the running estate. fleet-readiness-decision-batch-2026-09-09.md row warden-B7 / warden-E4, section 2.3 C7 new-48.11.
- (note by claude) 2026-09-09 readiness pass, second gap: the SBOM half of CAP-2 is a stub. _run_warden_engines returns sbom_json unconditionally as the literal "{}" (tasks.py:110-111), which tasks.py:80-82 persists to ComplianceJob.sbom_json, so the field is always empty. CAP-2 promises 'SBOM + vulnerability/license/currency reports rendered from engine output'; only the report half is rendered. The derived-progress half of CAP-2 IS real and correct -- phases.py computes progress = phase_index / len(PHASES) and callers advance only through advance().
- (decision by claude) RESIDUAL (2026-09-09): Stories 8.1/8.2 are marked done against their tests only -- the capability is realized-in-effect ONLY AFTER steward 48.11 merges and the deployed image can import the engines the face calls. Recorded as a named residual on this Spec rather than reopening the stories, because the missing piece is a one-line platform-env dependency on another station's surface, not warden work. Rejected alternative: re-scope CAP-1 to shell out to a warden binary in a sidecar (Pattern B) -- it preserves the 'host imports no pyforge' boundary but is a real architecture change for a Spec whose stories are already done, and the precedent for the chosen fix (pyforge-steward's own path dep) is already in the file. Operator-approved 2026-09-09, fleet-readiness-decision-batch-2026-09-09.md row warden-B7.
- (event by claude) status: ready -> in-progress -- Table A of the 2026-09-09 fleet readiness pass: 8.1/8.2 code landed and the app is mounted (django_warden_fabric registered through _STATION_PORTAL_PACKAGES in src/platform/config/settings/base.py; /compliance/ 308-redirects to /stations/warden/), but two gaps stand between the merged stories and CAP-1/CAP-2 being true in the estate (the unreachable engines, the stubbed sbom_json). Not shipped on paper alone. The one declared open question ('Host: a src/platform app vs standalone -- decide at 8.1; platform-app is the default lean answer') is ANSWERED BY DELIVERY at Story 8.1: it is a platform-mounted Django app, and the app relocated out of src/platform/compliance_face/ in 2394d850db. Operator-approved 2026-09-09, fleet-readiness-decision-batch-2026-09-09.md section 2.3 C9 bundle.
- (event by claude) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS; CAP-1/CAP-2 preserved unchanged, status ready -> in-progress, the host open question emptied as answered-by-delivery, and the two live gaps (platform env cannot import pyforge.warden.cli -> steward 48.11; sbom_json stubbed at literal '{}') recorded as a named Residuals section.
- (event) 2026-09-17 absorbed into spec-pyforge-warden (one-chain-per-station CAP-3 / warden fold). Companion documents stay as record.
