---
topic: golden-path-promotion Warden verdict: scan the shipped python-agent-platform closure, provision the offline OSV DB, keep deploy gated on clean
updated: 2026-09-05T12:55
---

- (event by claude) 2026-09-05 bmad-spec create, headless/express mode (no TTY; caller pre-supplied the Dream path + project pyforge-warden). Input: docs/dreams/golden-path-conda-blind-spot.md (rich, structured: Dream / What it looks like when real / What is real / Constraints / Non-goals / Realization log) plus the two designs it cites. Slug inherited from the Dream filename; the dream-chain detector expects spec-golden-path-conda-blind-spot.
- (direction by claude) Repo frontmatter convention (spec:/status:/owner-dream:/surface:/companions:/sources:/open_questions:) used instead of the template's id: line -- the dream-chain and spec-surface detectors parse those keys; sibling spec-compliance-factory-web-face is the shape reference.
- (decision by claude) Why = a pain + a fail-closed gate: golden-path-promotion's first run (2026-09-04) recorded warden_status indeterminate over 329 components with 0 assessed, structurally, because it scans a bare pixi.toml (PixiLockExtractor flattens every env/platform), no CI job provisions the offline OSV DB, and -- since 1016f4e763 (2026-09-04) -- platform-deploy refuses any non-clean verdict, so nothing can promote until the verdict is real.
- (note by claude) Correction to the Dream's What-is-real: scripts/platform-deploy-verify-promotion.py already refuses status != clean (commit 1016f4e763, after the Dream's measurement); the third structural gap is closed on main. Preserved as CAP-5 (regression guard), not new work.
- (capability by claude) CAP-1 environment-scoped lockfile extraction: Warden extracts exactly one named pixi environment for one platform from a multi-env pixi.lock; success = scoped scan of the root pixi.lock (python-agent-platform/linux-64) equals that env's packages.linux-64 list (count, names, versions) with no other env's exclusive packages; the unscoped call keeps today's union.
- (capability by claude) CAP-2 the promotion scans the shipped closure: golden-path-promotion scans the resolved python-agent-platform env out of the root pixi.lock, not a bare pixi.toml in a scratch dir; success = record components == the CAP-1 set, every component carries a locked version (no NO_VERSION from an unresolved manifest), no scratch staging.
- (capability by claude) CAP-3 the offline OSV DB is provisioned in CI per the accepted Story 1.4 decision record: OSV_SCANNER_LOCAL_DB_CACHE_DIRECTORY points at a populated <cache>/osv-scanner/PyPI/all.zip with recorded snapshot_at; success = vulnerability axis assessed > 0 in the record; a DB older than db-max-age (7 d default, strict) or provenance-less routes to indeterminate, never clean.
- (capability by claude) CAP-4 the verdict is honest and specific: clean only when the shipped closure's conda+pypi identity is scanned end to end; otherwise indeterminate/warn naming the unassessed components and the reason (UNMAPPED_ECOSYSTEM, stale DB, missing DB); success = a deliberately pinned vulnerable version flips the record to a failing rung naming the finding.
- (capability by claude) CAP-5 deploy gates on the verdict (already true on main, 1016f4e763 -- preserve): platform-deploy promotes a digest only on a recorded clean; success = the verifier refuses indeterminate/warn/fail and a missing record, accepts clean, names the driver finding id, and a test fails if the != clean refusal is removed.
- (constraint by claude) Never promote to clean by loosening what counts as scanned: a selector that cannot resolve a component's identity keeps withholding it (UNMAPPED_ECOSYSTEM), never drops it from the denominator (Dream).
- (constraint by claude) Reuse the two accepted designs -- spec-2-1's verified-confidence conda->pypi map and the Story 1.4 OSV-DB decision record (mechanism, 7-day strict staleness, unknown/future-dated provenance => indeterminate, trust anchor); do not re-derive matching or staleness semantics (Dream).
- (constraint by claude) Offline-first (NFR-S2): provisioning the DB is an explicit, non-default CI step; the scan itself never egresses silently (Dream).
- (constraint by claude) The two halves stay together: the verifier's clean-only gate (already landed) is never relaxed to make deploy pass; the real verdict lands under it (Dream, adjusted for 1016f4e763).
- (constraint by claude) Scope is pyforge-warden/extract/lockfiles.py, scripts/platform-golden-path-promotion.sh and the golden-path-promotion job in platform-ci.yml; the CFE atlas's own pixi.lock parser (scan_project) is untouched (Dream).
- (decision by claude) Non-goals carried from the Dream: no general multi-env/multi-platform selector beyond one named env + current platform; no new conda-native vulnerability source (OSV via the conda->pypi identity is v1); the atlas parser is out of scope. Added: no relaxation of the deploy verifier.
- (decision by claude) Success signal: the first golden-path-promotion run after landing records warden_status clean over the python-agent-platform closure with every component's vulnerability axis assessed and platform-deploy promotes that digest; a deliberately planted vulnerable pin in that env flips the next record to a failing rung that platform-deploy refuses.
- (assumption by claude) Target environment is python-agent-platform on linux-64 (the platform Containerfile pins --platform=linux/amd64); no other env is in scope.
- (assumption by claude) Warden's existing seven-rung lattice supplies every not-clean rung; no new rung or verdict field is introduced.
- (assumption by claude) Provisioning route defaults to osv-native --download-offline-databases on the connected CI runner with snapshot provenance recorded, unless a conda-packaged DB exists by story time (decision record section 1 order).
- (assumption by claude) The env/platform selector is an extractor option surfaced through the warden CLI; exact flag names are a story-time decision.
- (question by claude) Selector grammar: extractor constructor args plus which CLI flags (--pixi-environment / --pixi-platform?), and does the platform default to the host or must CI pass it explicitly?
- (question by claude) Provisioning: which decision-record route does CI take, where is the DB cached across runs (actions/cache keyed by snapshot date?), and who owns its refresh cadence?
- (question by claude) Coverage floor: does promotion also require 100 percent assessed coverage (--fail-under-coverage) or is a clean rung alone the gate?
- (question by claude) Is a warn verdict ever promotable with a recorded Story 3.2 waiver, or is clean the only promotable rung?
- (question by claude) Should the unscoped union extraction stay the default for other callers, or become an explicit error on a multi-environment lockfile?
- (decision by claude) Companions: brownfield.md (spec-authored: the live surfaces, line-cited); adopted: ../../osv-db-offline-provisioning-decision.md and ../spec-2-1-conda-pypi-map-the-ecosystem-identity-predicate.md. surface: [] until stories land (the paths are governed by steward/warden specs today; claiming them now would hide drift). status: draft.
- (event by claude) Self-validate pass 1 (coherence, Spec Law 1-6, 8): every CAP has intent+success; intents are WHAT (flag names deferred to a question); every constraint rules something out; 4 non-goals; success signal is a demonstrable run + planted-defect flip; CAP ids 1-5 fresh; prose lean. Verdict: PASS.
- (event by claude) Self-validate pass 2 (preservation): Dream sections walked claim by claim -- Dream/What-it-looks-like -> Why + CAP-1..5; What-is-real -> brownfield.md (with the 1016f4e763 correction); Constraints -> Constraints (the two-halves item adjusted); Non-goals -> Non-goals; Realization log -> wrapper-only (process record, dropped). Verdict: PASS; one source correction pending (Dream's verifier claim).
- (event by claude) spec finalized 2026-09-05 (status: draft; 5 open questions for the operator; Dream flipped dreamt -> specified with a realization-log entry recording the verifier correction).
