---
topic: One provenance trail and one eligibility answer per package, from N heterogeneous sources
updated: 2026-09-09T15:44
---

- (capability) CAP-1 SourceContract adapters (fetch/parse/validate/ingest; feeds.py's proven shape generalized) + the identity API (inventory.py's Component identity exposed adapter-facing); CAP-2 eligibility-union with ProvenanceEntry trails and the NEW vocabulary (eligible-union/observed-in-use/flagged-for-review — distinct from Status verdicts); CAP-3 CycloneDX output via sbom.py + FABRIC's 13x6 manifest corpus adopted as fixtures (Apache-2.0, notices kept). Sibling org's same-name dream = pattern reference only (unlicensed)
- (event) Spec authored + decomposed 2026-08-22 as warden Epic 7 (3 stories)
- (decision by claude) OPEN QUESTION ANSWERED + RESIDUAL ACCEPTED (2026-09-09): 'Required-authority policy: which source classes MUST agree for eligible-union -- config-driven; default at 7.2' is ANSWERED IN CODE at Story 7.2 -- eligibility.py:113-121 defaults required_authority_sources=None to 'every source name present in the full evidence tuple', i.e. unanimous consensus, and the docstring at eligibility.py:128-131 is explicit that the omission was deliberate ('this story deliberately ships no CLI flag or TOML config key to set it (cli.py/config.py are untouched); wiring a config-driven surface for it is a later story's job'). ACCEPT THE DEFERRAL: the config surface the question promised is recorded here as a NAMED RESIDUAL rather than left as an open question, and the config story is minted ONLY together with the missing CLI front door -- a settable policy no user can reach is unreachable, so the two belong in one story. Rejected alternative: mint the config story now, separately. Operator-approved 2026-09-09, fleet-readiness-decision-batch-2026-09-09.md row warden-B8 / section 2.3 C9.
- (note by claude) 2026-09-09 readiness pass, verified against live code: CAP-1..3 landed as real library modules -- sources.py (SourceContract + registry), eligibility.py (the union with ProvenanceEntry trails and the separate eligible-union/observed-in-use/flagged-for-review vocabulary), eligibility_sbom.py (CycloneDX through the existing renderer) -- and Stories 7.1-7.3 are all done with unit tests. But this Spec's Success signal is 'One CLI question, one CycloneDX answer' and THERE IS NO CLI QUESTION: grep for eligib in src/shared/packages/pyforge-warden/src/pyforge/warden/cli.py returns zero hits. The Dream's Constraint that this vocabulary stay separate from models.py's Status rungs HELD -- verified by reading, not assumed. This Spec is not shipped until warden grows the eligibility verb its Success signal describes.
- (event by claude) status: ready -> in-progress -- Table A of the 2026-09-09 fleet readiness pass: ready was stale in one direction (the library code landed) and optimistic in the other (no front door reaches it). open_questions is now empty; the one declared question is answered above and its config-surface half is carried as a named residual, not as an open question. Operator-approved 2026-09-09, fleet-readiness-decision-batch-2026-09-09.md section 2.3 C9 bundle.
- (event by claude) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS; CAP-1..3 preserved unchanged, status ready -> in-progress, the required-authority open question emptied (answered in code at 7.2) with its config surface carried as a named residual alongside the missing CLI verb.
