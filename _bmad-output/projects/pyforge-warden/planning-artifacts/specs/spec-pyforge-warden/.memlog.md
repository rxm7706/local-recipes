---
topic: Warden — the multi-axis Python dependency compliance gate that never false-greens: brownfield distillation of the SHIPPED product (31 stories / 6 epics merged) into the five-field Spec contract
updated: 2026-09-20T08:46
---

- (direction) Caller: derive the MISSING Spec for pyforge-warden. Per the Charter (Lexicon §2) the Spec is the unit of contract; the PRD/architecture/epics chain is its decomposition. This is a BROWNFIELD DISTILLATION: describe what Warden IS as contracted, not a plan for it. No re-planning, no invention.
- (decision) Headless + express distill. Inputs are rich and shipped: Dream + PRD (FR1-FR40, C0 + NFR-R/S/P/I/U/C) + architecture (7-stage pipeline, Gaps A/B/C, patterns) + epics (6 epics / 31 stories) + the legacy Tier-1 spec docs/specs/pyforge-warden.md (release map, decisions D1-D12, vision catalog, DoD) + the shipped package at src/shared/packages/pyforge-warden/.
- (decision) Ground the distillation in the SHIPPED code, not only the documents (brownfield rule): verified live 2026-07-25 — report.REPORT_SCHEMA_VERSION = 1.1.0; verdict.py holds _RUNG_ORDER + _EXIT_BY_STATUS + EXIT_SIGINT=130 as the sole owner; report._REPORT_AXES = (hygiene, vulnerability, license, currency) with unregistered-axis fail-loud; 24 shipped modules incl. license.py, currency.py, feeds.py, actuator.py, waiver.py; extract/ carries 7 format modules; data/ carries report-schema.json + conda_pypi_map.json + lts-registry.yaml.
- (decision) docs/specs/pyforge-warden.md goes in sources: with an explicit note that it is the LEGACY Tier-1 spec being superseded by this Tier-2 Spec — it is not adopted as a companion, because its content is fully absorbed here and its tier is being phased out.
- (decision) Three spec-authored companions, each a multi-item catalog that would bloat the kernel: verdict-contract.md (the frozen lattice + exit projection + osv exit-code reading + the C0 adversarial guard corpus), axes.md (the six-axis catalog, four live / two vision), extraction-contract.md (the six formats + supported-construct matrix + ecosystem-identity predicate + withhold reasons). Constraints carry the normative one-liners; the companions carry the matrices.
- (capability) CAP-1 one command, one verdict, one exit code: the whole multi-axis check runs as a single non-interactive invocation and gates on one exit code from the frozen enum. Success: N per-finding outcomes reduce to one status + one exit; the gate decides on report CONTENT + severity, never a subprocess return code; zero prompts in any mode incl. local workstation mode.
- (capability) CAP-2 source-manifest resolution, both ecosystems (the wedge): a project's declared dependency set resolves from its SOURCE manifests pre-build, with no resolved or installed environment. Success: six formats + lockfiles; PyPI inputs delegate to the engines' native parsers (no bespoke re-implementation); corpus conformance over ~1,950 real recipes = 0 uncaught exceptions + a ratcheted unparseable rate; a differential oracle asserts the extracted dep set is a superset of the authoritative renderer's, modulo name-only-marked.
- (capability) CAP-3 honest, split, per-axis coverage: a consumer can tell what was assessed from what was merely present. Success: coverage is per-axis with its own denominators + resolution_depth (direct-only vs locked-closure); a manifest can be 100% hygiene-covered and 0% vuln-covered and the two are asserted as DISTINCT fields; every withheld component states its reason and is always visible, never silently dropped.
- (capability) CAP-4 the axes of trust: every resolved component is assessed on each registered axis. Success: hygiene (deptry DEP001-005), security (osv-scanner + CISA-KEV + FIRST EPSS), license (SPDX via license-expression from conda about: + importlib.metadata), currency (bundled LTS registry -> endoflife.date -> N/N-1 -> unknown) each yield a verdict for 100% of resolved components; provenance and maintenance are named and unbuilt. Catalog: axes.md.
- (capability) CAP-5 configurable, two-mode policy: a team tunes what blocks without editing the tool. Success: [tool.pyforge-warden] in pyproject.toml and/or pixi.toml with deterministic per-key precedence and surfaced conflicts that never change the exit code; hygiene + security gate by default; license + currency gate when their policy flags are configured and, unconfigured, feed a visible warn rung — never a silent clean.
- (capability) CAP-6 auditable, expiring suppression: a team accepts a risk in the open and on a clock. Success: waivers and baselines are committed files the tool READS and never writes; suppression keys on the stable finding-ID grammar; every applied entry is echoed in the report; an expired entry re-blocks; wildcards are rejected (least-privilege); a bypassed run carries bypassed + review_required in the audit record even though its residual exit is 0.
- (capability) CAP-7 the machine contract: a pipeline or downstream estate consumes the run as data. Success: a schema-validated versioned ComplianceReport on stdout as a single valid document OR empty — never partial, never diagnostics-contaminated; a CycloneDX 1.6 SBOM with source-registry-correct purls and self-declared partiality; BOM component count == resolved-inventory count == report.inventory_count, post-merge, root project excluded.
- (capability) CAP-8 typed failure with a named owner: a fleet operator routes every failure without reading a log. Success: the typed error kinds (unparsable-manifest, engine-unavailable, engine-output-unrecognized, engine-output-unparseable, engine-execution-failed, engine-timeout, config-parse, config-validation, internal-error) each route to an owner (developer / platform / CLI maintainers); a missing, incompatible, crashed, timed-out or output-drifted engine is exit 2 and never a silent pass; absence of an expected field is an error, not a zero.
- (capability) CAP-9 adoption without a day-one red wall: a debt-laden repo turns the gate on without ripping it out. Success: --warn-only reports everything at exit 0; a committed baseline grandfathers existing findings so the gate blocks NEW findings only, with expiring entries that re-block; every non-zero exit names the offending package, the finding (advisory id + severity + fixed version, or the hygiene rule), the source manifest + location, and a remediation path.
- (capability) CAP-10 determinism and non-mutation at fleet scale: a run reproduces and leaves nothing behind. Success: decision-determinism by default (identical inputs + DB snapshot => identical exit code + findings set), byte-identical output in --deterministic over a pinned volatile-field set; a scan never mutates the scanned tree or host state and cleans up on success AND failure; per-invocation cost is O(project) independent of fleet size; all live axes run in parallel with no shared mutable state.
- (capability) CAP-11 offline-first with no silent egress: an air-gapped fleet runs the gate with no network. Success: the orchestrator's own process opens no socket (socket-guard test); the vuln DB and the KEV/EPSS/endoflife feeds are provisioned or cached, offline by default and opt-in online never silent; every verdict records its data source + snapshot timestamp; stale, empty, swapped or unverifiable data routes to indeterminate, never a confident clean.
- (capability) CAP-12 opt-in remediation that never touches the tree: findings become pull requests without the tool writing the scanned repo. Success: --open-fix-prs runs strictly post-verdict with environment-supplied credentials, opening upgrade/removal PRs via the forge API only; a failed PR-open never alters the verdict or exit code (it lands in the actuation report section, outside status/exit composition); --fix-prs-dry-run shares the code path to the egress seam and opens no sockets.
- (constraint) FROZEN exit-code contract: the enum {0, 1, 2, 130} is a CLOSED set — 0 clean/warn/not-applicable/bypassed, 1 policy-violation or indeterminate, 2 operational error, 130 SIGINT. Adding a code is a MAJOR change (a new code silently breaks every 'elif rc == 2' consumer) — the deliberate opposite of the additive-flag rule. Exit 2 stays RESERVED for operational error so fleet routing (rc==2 -> infra owner) holds. No abnormal exit returns 0: any signal, interrupt or uncaught internal error is non-zero and never a passing report.
- (constraint) The verdict lattice is frozen at 7 rungs, in this order: error > policy-violation > indeterminate > warn > bypassed > clean > not-applicable. Status severity and exit code are DELIBERATELY different orderings (bypassed sits above clean because a suppression is an audit-relevant event). verdict.py is the SOLE OWNER of both the lattice and the exit projection — no other module maps a status to an exit code, enforced by a sole-ownership guard test. Every non-clean status carries status.driver (axis + finding id): an exit that cannot say 'critical CVE' vs 'blocking DEP001' is an incoherent contract.
- (constraint) NEVER FALSE-GREEN is THE acceptance property (C0), not one reliability item among many. Its mechanism is the indeterminate doctrine: not-applicable = NOTHING existed to scan; indeterminate = SOMETHING existed we could not or would not scan. indeterminate sits ABOVE warn so a clean sibling axis can never mask 'we could not scan what existed', and it projects to exit 1 — never a silent 0. Any run that did not meaningfully scan (empty extraction, expected-but-missing manifest, crashed engine, skipped coverage) is never clean in the STATUS channel; only the sanctioned downgrades (--allow-empty, --warn-only) may lower the EXIT — the status stays honest. Absence of an expected field is an error, not a zero. Coverage improves ONLY by resolving (reading the lock) or name-level flagging — NEVER by assuming a version.
- (constraint) The ComplianceReport schema is versioned and its producer is CLOSED at 1.1.0: exactly ONE sanctioned amendment was paid (story 6.1) and no other story may widen the schema. A coverage claim for an axis not registered in report.py's _REPORT_AXES is a HARD ERROR, never a silent drop. The report and the CycloneDX SBOM version INDEPENDENTLY (SBOM = spec 1.6 + its own profile version, emitted experimental until a consumer ingests it).
- (constraint) Six axes of trust, four live: hygiene, security, license and currency are registered producers that report and gate in v1; PROVENANCE (Sigstore/SLSA, PEP 740 trusted publishing, in-toto/GUAC) and MAINTENANCE (OpenSSF Scorecard, criticality_score, sustainability) are named direction and remain UNBUILT — the Charter's six-axis identity is the destination, not the shipped surface. A new axis registers behind the existing Engine seam with an axis string; there is no Axis protocol.
- (constraint) One spine: a single Component + ResolvedInventory. Discovery/routing/extract PRODUCE it, axis producers ANNOTATE it, report/sbom/verdict READ it — no stage reaches around it and no stage redefines a parallel shape. Identity is (ecosystem, name, concrete_version); the purl is DERIVED and its non-identity qualifiers stripped before any comparison. A (name, None) entry merges into (name, version) only when exactly ONE concrete version exists — zero leaves one indeterminate, two or more keeps the bare entry a distinct indeterminate. Never guess-attribute a version.
- (constraint) No EXECUTION of untrusted input (the constraint was never 'stdlib-only'): extract/ is a no-execution zone importing no eval/exec/compile/__import__/os.system/subprocess and rendering no templates (no jinja2 import), yaml.safe_load only — enforced by an AST-denylist meta-test. Safe parsers are permitted because they do not execute input. engines.py is the ONLY module that spawns subprocesses, always through the _engine_env() helper (machine output forced to a system-temp file, NO_COLOR=1, stdin=DEVNULL, explicit utf-8 decode with undecodable output mapping to a typed error).
- (constraint) The tool NEVER writes the scanned repository. Waiver stanzas and baseline candidates are emitted to stdout for the human to commit; temp artifacts go to the system temp dir via mkstemp/mkdtemp at 0600/0700 (mkstemp IS the symlink/TOCTOU defense); the fix-PR actuator writes pull requests through the forge API, never the tree. Cleanup happens on success AND failure.
- (constraint) No SILENT egress: the orchestrator's own process opens no socket (socket-guard test) and all network is confined to the named engine subprocesses; air-gapped mode passes explicit offline/local-DB flags because osv-scanner defaults ONLINE (an assertion, not a hope); KEV, FIRST EPSS and endoflife.date are cached feeds under the same posture. actuator.py is the SOLE egress carve-out — scoped to that module under the real flag, never a global loosening.
- (constraint) Engines are CONSUMED, not authored: deptry and osv-scanner are conda run-deps provisioned from existing conda-forge feedstocks (never curl-fetched at runtime), pinned to a tested version RANGE rather than an exact pin, failing loud when out of range — the guard against silent upstream output-schema drift. That same version range is also the distribution gate. Where the local mirror recipes for the engines are touched, this repo's CFE Rule 1 (invoke the skill) and Rule 2 (closeout retro) apply.
- (constraint) osv exit codes are read as CONTENT, never as the gate: 0 = no vulns, 1 = vulns-found and EXPECTED, 127 is MULTIPLEXED (a DB-absent/empty/corrupt 127 is a coverage gap -> indeterminate; any other 127 -> error), 128 no-packages -> indeterminate, anything outside {0,1,127,128} -> error. The decisive case: a present-but-EMPTY database makes osv exit 0 with an empty body — a false-green neither the exit code nor a namelist count catches — so the loader runs a DB CONTENT pre-flight (parse + validate the advisory shape, count >= 1, at osv's case-sensitive PyPI segment) before trusting any clean; a provenance-less DB (snapshot_at = None) routes to indeterminate.
- (constraint) osv has NO conda ecosystem — it keys advisories over PyPI/npm/Go, so a conda dep is parsed as PyPI and a differently-named pinned package (pytorch -> torch, native openssl) would match the WRONG identity and silently false-green. Therefore vuln_matchable = (pypi_identity is not None) AND (version resolved to ==X.Y.Z), and pypi_identity resolves ONLY from trustworthy provenance: pixi.lock pypi: entries, explicit PyPI sections, or the bundled static conda->pypi map. Unmapped -> None. Conda '=1.2' is a PREFIX (1.2.*), not an exact match -> withheld as range-only.
- (constraint) Cross-ecosystem names are NEVER silently merged or deduped: per-ecosystem attribution is preserved and uncertainty is MARKED. A component's purl reflects the source registry of the manifest it came from (pkg:pypi/... vs pkg:conda/...?channel=) and an unresolved registry-of-truth is marked, never guessed into a purl. Full conda<->PyPI reconciliation is deferred.
- (constraint) Stream discipline: the report goes to stdout and ALL diagnostics, progress and warnings to stderr; in machine mode stdout is a single valid document OR empty, never partial and never contaminated. Human text output and stderr are EXPLICITLY unstable and may change any release — declaring non-contract is itself a stability statement.
- (constraint) Contract stability is taxonomized, not one rule: OUTPUT (the report, the SBOM) evolves by add-fields-and-bump-version; INPUT (config keys, the waiver/baseline file schema) evolves by accept-old-forever then deprecate -> warn -> remove, because those keys live in 20k repos, and an unknown or future waiver-file version is REJECTED with a typed error, never guessed; the exit enum is the frozen closed set. v1 ships the behavior, not the paperwork — no deprecation lifecycle machinery.
- (constraint) Determinism discipline: never iterate a set for output — sorted() before every emit, JSON with sort_keys and fixed separators; no unguarded datetime.now(); --deterministic pins the documented volatile-field set (report/SBOM timestamps, CycloneDX serialNumber and bom-ref derived from a content hash, set-iteration order, absolute paths -> repo-relative, DB version, and the actuation section).
- (constraint) Single-writer rules that bind independently-built stories: config.py alone computes the per-axis gating bool (producers and the report only READ it); axis producers never feed a rung above warn (producer meta-test) and one story solely owns the escalation mapping; suppression matching-by-finding-ID plus expiry is implemented ONCE in waiver.py for both the waiver and baseline formats (waiver wins where both match, echoed once); KEV/EPSS enrich findings at exactly ONE position — inside the vuln producer BEFORE policy dedup, never post-dedup.
- (constraint) The waiver file, the baseline file and the offline vuln DB are untrusted-or-verified INPUTS: schema-validated, expiry enforced against wall-clock, never executed. Authorship and integrity are explicitly delegated to code review + CODEOWNERS — the tool cannot verify a forge control at runtime and says so rather than pretending otherwise. The DB's trust anchor is conda package integrity plus build-date staleness.
- (constraint) Bundled data (the LTS registry, the conda->pypi map) carries a build-time snapshot_at, and EVERY verdict derived from it carries max_age_ok against a configurable max-age (default 180 days): a stale bundled registry can never silently report supported — stale under an active gate routes to indeterminate. Absent or stale KEV/EPSS feeds under an active policy do the same: the gate never silently no-ops, and kev: null (feed absent) stays distinguishable from 'assessed, not KEV-listed'.
- (constraint) Resource bounds are enforced mechanisms, not hopes: extraction is line-bounded with a per-line byte cap and a total manifest-size cap; no compiled pattern contains nested unbounded quantifiers (static assertion); DB extraction is decompression-bounded (zip-bomb) and zip-slip confined; every engine invocation carries a bounded, configurable timeout whose expiry maps to a typed error and the frozen exit code — never an indefinite hang, never scored as a pass.
- (constraint) Engine-input purity and output neutralization: the synthesized requirements projection is a PURE DATA projection — any line starting with '-', or carrying a URL, VCS ref, path or environment marker we did not author, is rejected or neutralized; manifest-derived values are NEVER passed as CLI flags; shell=True is banned. Every input-derived string is emitted only through a schema-aware encoder (never string concatenation), purls are canonically percent-encoded, and control/escape chars are stripped — so a malicious component string cannot make the tool a confused-deputy injection vector against a downstream SBOM or dashboard consumer.
- (constraint) Runtime shape: Python >= 3.12; argparse, not Click or Typer; stdlib-lean with a small set of targeted, conda-provisioned, safe-API-only runtime deps (PyYAML safe_load/safe_dump, packaging, cyclonedx-python-lib, jsonschema, license-expression) — 'lightweight' means runtime footprint, not total cost. pixi >= 0.72.2 is a build/dev-env floor: the tool NEVER invokes pixi at runtime (pixi.lock is safe_load-parsed). Scope is Python only — PyPI plus conda-forge.
- (constraint) Non-rendering two-pass extraction is the security posture made into a parsing strategy: v1 recipe.yaml is valid YAML and safe_loads directly; v0 meta.yaml is not, so selectors are captured as marks, {% ... %} statement lines stripped, and simple {{ VAR|filter }} substituted through OUR OWN safe-filter allowlist — never a Jinja engine — before safe_load. run_constrained / run_constraints entries are CONSTRAINTS, not dependencies: excluded from vuln matching and SBOM counts. Per-construct handling: extraction-contract.md.
- (note) Non-goals distilled from the legacy spec's Non-goals (v1) + the PRD's deferred list + the release map's v1.x/vision buckets + the PRD's explicit by-design non-capabilities (fleet aggregation by composition; retention is the CI system's; no telemetry; no waiver-authorship verification).
- (note) Success signal derived from what is DEMONSTRABLY shipped and gated, per the brownfield instruction: the C0 adversarial-fixture corpus (0 exit-0 outcomes), corpus conformance (0 uncaught exceptions over ~1,950 recipes + ratcheted rate), schema + CycloneDX 1.6 validation with BOM-count == inventory-count, and twice-run byte-identity under --deterministic. Shipped evidence: 31 stories / 6 epics merged (PR #110, 2026-07-25); report schema 1.1.0; four axes registered in _REPORT_AXES.
- (assumption) Warden's story set is COMPLETE (31/31 across 6 epics, merged to main via PR #110 on 2026-07-25) — so this Spec is written in the present tense as a standing contract, not a plan. The Dream's 'in-build 23/31' line and the legacy spec's status: in-progress both predate that completion.
- (assumption) The competitive wedge (pre-build source-manifest scanning + conda dependency-hygiene, where every incumbent requires an installed conda-meta/) is TIME-BOUND — grounded by a dated 2026-07-11 spike, to be re-run before any external release. The fallback if Trivy/Syft add source-manifest parsing: the hygiene+security unification and the honest-coverage contract survive, and conda dependency-hygiene still has zero incumbents.
- (assumption) The engines' output formats are a contract Warden is hostage to (an upstream dependency risk, not a persona): the tested version RANGE plus engine-output shape validation are the mitigations, so a working-but-drifted engine surfaces as a typed error rather than a false-green.
- (question) The legacy spec's v1 Definition of Done still carries unchecked release-level items even though all 31 stories merged: the CFE Rule-2 closeout retro (the engine recipes recipes/deptry and recipes/osv-scanner lack CHANGELOG entries) and the internal JFrog PyPI+conda publish behind the engine version-range gate. Is v1 'released' or 'story-complete'?
- (question) The legacy Tier-1 spec docs/specs/pyforge-warden.md still reads status: in-progress and its Goals block still describes the pre-D12 tiering (license/currency gates as v1.x). Does it get re-stamped shipped and marked superseded by this Spec, or frozen as-is as a historical record?
- (question) Two axes of the Charter's six-axis Warden identity (provenance, maintenance) are unbuilt and sit in the vision bucket with no owner or trigger. What promotes them out of vision — and until then, does the product describe itself as four-axis or six-axis?
- (event) Self-validate pass 1 (coherence, Spec Law 1-6+8): PASS — 12 capabilities all carry intent+success; intents are WHAT-only (mechanism isolated to Constraints and the three companions); every constraint rules something out; 18 non-goals; success signal machine-checkable (0 exit-0 across the adversarial corpus, 0 uncaught exceptions over the recipe corpus, schema + CycloneDX validation, twice-run byte-identity); CAP IDs fresh CAP-1..12, none reused.
- (event) Self-validate pass 2 (preservation): PASS after 7 repairs. Gaps found and closed: FR1's union-coverage / resolved-scan-set-as-denominator -> CAP-3; FR9 ([tool.deptry] ignores honored) + FR19 (coverage-floor gate, default off) -> CAP-5; FR29's scan --doctor (exit 0 healthy / 2 typed / never 1) -> CAP-8; NFR-P-warm p95 overhead + NFR-P-cold amortization -> CAP-10; the additive-enum-growth safety clause (unknown -> indeterminate, never clean) -> the never-false-green constraint; the Dream's three-rings depth model + D11's two-modes-one-identity -> new scope constraint; the legacy spec's naming contract (Warden / pyforge-warden / pyforge.warden / warden, product-name-not-dist-name intentional) -> new constraint.
- (event) Wrapper-only content dropped (on the record, not silent): the PRD's persona roster P1-P8/M1 and its ten user journeys (their contract content is fully carried by the capabilities and verdict-contract.md); the competitive landscape table and the innovation-area narrative (positioning, not contract — the time-bound-wedge conclusion IS kept, as an assumption); the architecture's module tree, data-flow diagram and epic-to-structure mapping, plus the epics' 6-epic/31-story decomposition and delivery order (the chain's job); the legacy spec's positioning-vs-in-house-gates section and its cross-spec sync obligations (process, not contract).
- (event) Spec finalized (create, brownfield distillation): SPEC.md + verdict-contract.md + axes.md + extraction-contract.md derived from this memlog. status: shipped; surface: src/shared/packages/pyforge-warden/**. sources: Dream + the LEGACY Tier-1 spec (noted as superseded) + PRD + architecture + epics, all fully absorbed. 3 open questions carried: released-vs-story-complete, the legacy spec's disposition, and what promotes the two vision axes.
- (event) AUD-WARDEN-010 closed on `main` (2026-07-29, Round-4 code-audit remediation). The finding was recorded as fixed on the abandoned PR #131 branch and was therefore still OPEN in the shipped tree: `pyproject.toml [project.dependencies]` declared `packageurl-python` but the member `pixi.toml [package.run-dependencies]` omitted it, so the built `.conda` received it only as `cyclonedx-python-lib`'s transitive and would have broken at import the moment cyclonedx re-bounded its own dependency. Added as `packageurl-python = "*"` matching the block's existing unpinned style (conda-forge ships 0.17.6, noarch). `pixi.lock` moved with it because the source package's run-dep set is part of its hash.
- (event) The finding is now mechanically guarded rather than re-audited: `tests/packaging/test_dependency_completeness.py` (`pixi run -e pyforge-ci pyforge-deps-test`) proves, for every `pyforge-*` package, that each unconditional module-level import is declared in `pyproject.toml` AND mirrored into the conda run-deps, in both directions. It is glob-discovered, so this package cannot fall out of coverage, and pure-stdlib, so it runs in an env carrying none of the dependencies it audits. Warden's two engines (`deptry`, `osv-scanner`) are recorded there as legitimately conda-only: they are external executables invoked by subprocess, not importable distributions, so they cannot be pip requirements — their version ranges stay guarded by this package's own `tests/meta/test_engine_version_range_sync.py`. Mutation-tested: re-removing `packageurl-python` reproduces the named failure.
- (note) Audit Phase 2 landing (2026-08-10): station chain reconciled — see planning-artifacts/implementation-readiness-report-2026-08-10.md for the full finding set and per-path record; mechanical fixes (rollups/Status-lines/spec-status/promotions/README prose) landed via the audit's Phase 2 PR
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Surface reconcile 2026-08-31: 111 drift-presumed paths since baseline at `3a83dd1c2681`. Eighteen commits, Epic 6-7 + Epic 10 warden delivery + pyforge-core integration + audit remediation (all done in ledger). Verified via git log per cluster:
  - Epic 6 (Stories 6.6-6.9): engine version-range pinning, baseline grandfathering, opt-in fix-PR actuator, fleet-scale validation corpus — scanners/, compliance/, cli.py, tests.
  - Epic 7 (Stories 7.1-7.3): SourceContract adapters, eligibility union provenance, CycloneDX + FABRIC corpus fixtures — eligibility/, tests/fixtures/.
  - Epic 10 (Story 10.1): station meta tests for SKF skill and persona.
  - pyforge-core integration: PR-gate hook book (`b5e34da5d7f`), optional scanner plugins (`4104636b341`), PortalClient audit (`51bbcd6d027`).
  - Cross-station: pyforge-core atomic-write consolidation (`1d60da0193a`, `5141daad88`), audit phase 2 reconciliation (`28551a6b65a`), audit remediation wave 2 (`864558e02d3`), SKF-marker fix (`a7721dd610f`), Checkmarx opt-out for default green scan (`ad4029fc3c7`).
  No CAP text change — all 31 stories shipped, code caught up to contract. Scoped-stamped: `--write-baseline --spec pyforge-warden/spec-pyforge-warden`.
- (change by claude) Surface touch 2026-09-05 (PR #1045): three meta-test files' whole-branch diff-guard bodies (test_portal_audit_start_get.py, test_skf_domain_skill.py, test_station_persona.py) refactored onto the shared mechanics (retro-pyforge-steward-2026-09-04.md action item 11: the fleet's whole-branch diff-guard test mechanics (git subprocess calls computing files/commits changed since origin/main) were consolidated into a shared `pyforge.testing_kit.branch_diff_guard` module (PR #1045).); each guard's enforced policy is unchanged. Scoped-stamped: `--write-baseline --spec pyforge-warden/spec-pyforge-warden`.
- (change by claude) DW-FU-11-2 resolved 2026-09-07: reconciled suite:AD-10 vs epics.md Story 11.2 GWT (a real conflict in the docs, not the doctor check, which already distinguished roster-vs-binary informationally). tea_advisory.py's run_tea_test_review now checks the suite:AD-9 roster (_bmad/custom/config.toml [modules.tea]) FIRST and raises new TeaRosterMissingError when tea has no roster entry at all (regardless of binary presence on PATH); TeaAdvisoryScanPlugin._contribute re-raises that one exception instead of swallowing it (every other runner/parse/binary problem still fails open, unchanged); cli.py's _run_scan catches it around invoke_pr_gate and records it via _record_error(kind=ErrorKind.CONFIG_VALIDATION, owner="tea-test-review", ...) -- mirroring the existing select_scanner_plugins/PluginError precedent one call site up -- a real ERROR rung, never suite:AD-4's advisory-lens exit-code exemption (that governs scored findings about the scanned code, not an unmet tool prerequisite). A roster entry that DOES name tea but whose binary is merely absent from PATH remains fail-open exactly as before. epics.md Story 11.2's GWT text updated to state the roster/binary distinction explicitly (suite:AD-10 itself needed no change). 5 new/updated tests in test_tea_advisory.py; full suite 2114 passed / 11 deselected (was 2109/11). RECONCILES: src/shared/packages/pyforge-warden/src/pyforge/warden/tea_advisory.py, src/shared/packages/pyforge-warden/src/pyforge/warden/cli.py, src/shared/packages/pyforge-warden/tests/unit/test_tea_advisory.py.
- (note) Cross-station reconcile from pyforge-marshal Story 32.2 (spec-fleet-consistency-standard CAP-5): this spec's pyproject.toml raised requires-python ">=3.12" -> ">=3.14", matching the interpreter every pixi env actually installs. No source change.
- (note) Story 32.5 (spec-fleet-consistency-standard CAP-2): the fleet converged on one test-suite vocabulary -- tests/unit/ + tests/integration/ + tests/meta/ (BMAD 6.12's Python default plus the one addition with 8/8 adoption), with fixtures/ and _support/ never collected. Eight suite names collapsed to three; the coverage gate's suite map now needs no per-station special case. This station: tests/conformance/ (19 oracle and engine gates -- dogfood, corpus determinism, perf, parallelism) folded into tests/integration/, which is their real weight; test_smoke.py moved to tests/unit/. Same directory name as steward's, two different concepts, which is why the fold is by LEVEL not by name. TESTS_ROOT-relative fixture lookups are depth-unchanged. 2114 tests pass.
- (note by claude) 2026-09-09 readiness pass, currency: this Spec's first Assumption still reads 'The story set is complete -- 31 stories across 6 epics, merged 2026-07-25' and cites 'The Dream's "23/31 in-build" line'. Both figures are v1-scope figures and are now stale as a description of the station: sprint-status-ledger.yaml carries 43 story keys, all done, across ELEVEN epics -- epics 7-11 post-date the original v1 scope (the eligibility union, the web face, the PR-gate hook book + scanner plugins, the skill/persona/portal slice, and the two advisory lenses). The Dream's '23/31' line was corrected to 43/43 in the same pass, so the citation now points at text that no longer exists. Re-derive the Assumption as: v1 was 31 stories across 6 epics, merged 2026-07-25; the station has since grown to 43 stories across 11 epics, all done, plus a twelfth epic minted 2026-09-09 for spec-golden-path-conda-blind-spot. status: shipped is unchanged and correct -- warden scan is the estate's sole PR verdict and scripts/platform-deploy-verify-promotion.py:31 refuses any digest whose recorded verdict is not clean, so the capability is realized AND in effect under the exercised-in-the-estate gate. fleet-readiness-decision-batch-2026-09-09.md Table A row pyforge-warden / finding warden-D-6.
- (event by claude) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS; only the stale first Assumption changed (31/6 v1 figures re-derived as 43 story keys across eleven epics, all done, plus the twelfth minted for spec-golden-path-conda-blind-spot; the dead '23/31 in-build' citation replaced). status stays shipped; all capabilities, constraints and companions unchanged.
- (event) 2026-09-11 frontmatter reconciliation (doctor status-body-consistency sweep, operator-directed CAP-effect-verified sweep companion pass): SPEC.md's frontmatter carried no open_questions: key at all, so gather_open_questions_reconcile treated it as empty even though this memlog carries three unclosed (question) entries (lines 51-53: v1 released-vs-story-complete; legacy Tier-1 spec re-stamp; four-vs-six-axis framing pending provenance/maintenance axis promotion). Re-derived SPEC.md's frontmatter to add an explicit open_questions: array naming all three, matching the repo-wide convention already used by 111/134 other SPEC.md files. No new question invented; none of the three resolved.
- (event) 2026-09-17 one-chain fold: status shipped → ready; covers-dreams lists all five owner:warden Dreams; reminted CAP-1..22 with ← spec-old CAP-m provenance; absorbed package-inventory-eligibility, compliance-factory-web-face, golden-path-conda-blind-spot (companions stay); pyforge-warden-compliance-gates remains archived duplicate. PRD FR ← CAP; epics sequential 1..12; rekey-2026-09-17.md feed keys only. pixi.lock stays off this surface (doctor).
- (event by claude) Co-governor note 2026-09-19 for marshal Story 52.1's landing (spec-pyforge-core:CAP-9 / CAP-5): `src/pyforge/warden/tea_advisory.py::TeaRosterMissingError` gains `PyforgeError` as first base with `RuntimeError` retained — every exact-class and `except RuntimeError` site behaves identically (`cli.py:1442`, `_contribute`'s deliberate re-raise); pin added in `tests/unit/test_tea_advisory.py`. No warden contract moved.
- (event) Surface reconcile 2026-09-19: updated Diátaxis docs and package READMEs
- (event by claude) Surface reconcile 2026-09-19 (doctor Story 30.1 / PR #1529): src/shared/packages/pyforge-warden/README.md — one plain-text docs pointer line. No warden code.
- (event by claude) Surface reconcile 2026-09-20 (pixi.toml [feature.pyforge-warden.dependencies]: py-rattler floor >=0.26.0 → >=0.25.0). The 0.26.0 floor came from a catalog-sync commit (8beb582d30), not an API need — the oracle tests use LockFile.from_path / CondaLockedPackage / PypiLockedPackage, present in 0.25; the pyforge-foundry-full union env (steward Story 63.5) cannot resolve conda's rattler-solver variant (py-rattler <0.26) beside a 0.26 floor. Warden's own env still resolves 0.26.0; pyforge-warden-test 2122 passed on the new lock.
- (event by claude) Surface reconcile 2026-09-20 (fleet consistency pass before the foundry cutover; operator ruling: consistency even where historical records are rewritten): 21 tracked story specs under planning-artifacts/specs/ reconciled against sprint-status-ledger.yaml: frontmatter status equals the ledger row (done rows: done; blocked/backlog/in-progress rows: a status in that row's vocabulary), the Ledger-status line matches, and every done story carries an Auto Run Result (reconstructed from main's landing commits where none survived, marked reconstructed; no-evidence cases stated as such). Keys: 1-2, 1-4, 1-8, 1-9, 2-1, 2-3, 2-4, 2-5, 2-6, 3-1, 3-2, 3-3, 4-1, 6-1, 6-2, 6-4, 6-9, 7-2, 7-3, 8-1, 8-2. Also: 15 draft/in-review frontmatters -> done.
- (event by claude) Surface reconcile 2026-09-20 (operator ruling: every station's PRD re-stamped in the fleet consistency pass, grace period or not): prds/prd-pyforge-warden-2026-07-14/prd.md, architecture/architecture-pyforge-warden-2026-07-14/ARCHITECTURE-SPINE.md, epics.md each gain a 'Currency reconciliation 2026-09-20 (fleet consistency pass)' note and updated: 2026-09-20 (spec->prd->arch->epics; bookkeeping, no requirement / AD / story moved).
- (event by claude) Surface reconcile 2026-09-20 (steward Story 66.1, spec-pyforge-steward:CAP-153, PR #1553): src/shared/packages/pyforge-warden/** -- Surface reconcile 2026-09-20 (steward Story 66.1, spec-pyforge-steward spec-pyforge-steward:CAP-153 -- the ten-package lint / type gate landed in PR #1553): pyproject.toml gains [tool.ruff] (py314, line-length 120, E/F/W/I, E501 off) and [tool.mypy] (3.14, explicit_package_bases, import-untyped disabled; the 2026-09-20 baseline as [[tool.mypy.overrides]] per module, per error code); every .py under src/ and tests/ import-sorted (I001) and reformatted by `ruff format` -- semantics-preserving, verified by the station suite. 7 modules baselined; no hand fixes needed.
- (event by claude) Surface reconcile 2026-09-20 (code->retro currency edge after spec-pyforge-steward:CAP-153 landed, PR #1553): retros/retro-pyforge-warden-2026-09-20.md (NEW) records what the fleet lint / type gate's first run found in this station -- its mypy baseline size, the lint findings fixed by hand, and the carry-forward (lint-types before pushing; drop unused-ignore comments in baselined modules).
