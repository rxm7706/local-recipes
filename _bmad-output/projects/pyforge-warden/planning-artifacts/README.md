# pyforge-warden — planning artifacts

**Warden** — non-interactive CI/CD dependency compliance gate: four axes of
trust (hygiene, security, license, currency) over Python/Conda/Pixi
manifests, one schema-validated `ComplianceReport` behind one exit code.
dist `pyforge-warden` / module `pyforge.warden` / CLI `warden`.

BMAD Tier-2 output for this project (`research/`, `briefs/`, `prds/`,
`architecture/`, `epics.md`, `specs/`, `retros/`).
