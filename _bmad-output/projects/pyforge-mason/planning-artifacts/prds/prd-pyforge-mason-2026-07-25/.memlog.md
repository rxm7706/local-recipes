---
topic: Mason PRD (pyforge-mason) — packaging CLI, wrap-vs-build resolution
updated: 2026-07-25T06:39
---

- (decision) D-1 wrap-vs-build RESOLVED: Option C seam-by-capability. Wrap CFE by subprocess for 'mason recipe'; build natively for 'mason package'/'mason environment'. A rejected (cannot deliver --ship pypi / environment lock — nothing to wrap); B rejected (Rule 1/2 make a fork adversarial; atlas precedent: ~29k LOC rebuilt, 8,902-LOC legacy still live; forks the 106-gotcha moat).
- (decision) D-1 tradeoff ACCEPTED and recorded: mason is NOT standalone — 'mason recipe' is inert without a discoverable CFE root. Paid deliberately; FR-5 degradation + FR-44 independence of the other two verb families.
- (decision) D-2 deployment target: v1 requires a co-located CFE root (T1/T2). No-CFE operation out of scope — recipes/ hardcoded in 17 scripts + no data-dir override = a CFE change Mason may not make.
- (decision) D-3 three ship targets: pypi (sync), channel:<name> (sync), conda-forge (ASYNC, returns PR ref). Receipts state terminal-vs-pending per target — a uniform 'success' would be a correctness bug.
- (decision) D-4 coexist with the ~105 pixi tasks; deprecate nothing in v1. Atlas failed by rebuilding without migration.
- (decision) D-5 token-based credentials v1; OIDC trusted publishing deferred to v2.
- (decision) D-6 requires-python >=3.12 matching warden (mason shells out so its floor is free; maximises co-installability).
- (decision) D-7 interpreter selection explicit: --cfe-python -> MASON_CFE_PYTHON -> sys.executable, with CFE import-floor probe. Lean no-default-feature envs cannot run CFE otherwise.
- (decision) D-8 no Mason MCP server in v1 — duplicating 46 tools is the atlas failure mode in miniature; fastmcp already a root dep so deferral is free.
- (decision) D-9 argparse, no click/typer — matches warden + all 60 CFE scripts, satisfies lean-dep doctrine.
- (change) 46 FRs / 16 NFRs / 9 D-records. FR-42..FR-46 are the seam-enforcement meta-tests; FR-42 (no recipe knowledge in pyforge.mason) designated the single most valuable test in the product.
- (change) Counter-metrics SM-C1 (Mason LOC), SM-C2 (recipe verb count), SM-C3 (adapter surface area) added to guard against seam erosion (R-1) — growth is a warning, not an achievement.
- (event) 7 open questions carried forward (verb set, channel upload engine, autotick ownership, conda-lock-vs-pixi.lock, --target shapes, competitive coverage risk, CFE min-version). 8 brief OQs resolved into D-records.
- (event) PRD finalized
- (event) Adversarial review run (review-adversarial.md): MAJOR REVISION REQUIRED — 3 critical + ~12 rework findings. All 3 criticals fixed in r2.
- (change) CRITICAL-1 FIXED: FR-45 forbade the very commit CLAUDE.md Rule 2 mandates. FR-45 rescoped to IMPLEMENTATION commits + one sanctioned retro exception (retro: subject + CFE CHANGELOG move, asserted used exactly once); new FR-47 owns the retro; SM-6 updated, SM-7 added.
- (change) CRITICAL-2 FIXED: UJ-1 promised a journey D-2 excluded and submit_pr.py cannot serve (it reads REPO_ROOT/recipes/<name> and writes to a staged-recipes fork clone). New D-10 states the boundary; UJ-1 rewritten with it; FR-23 rescoped + reported by doctor; added to 6.2 Out of Scope; R-11 logged.
- (change) CRITICAL-3 FIXED: no command shipped — FR-30 makes bare 'mason package' exit non-zero and FR-15 build uploads nothing. New D-12 + FR-16 rewritten: canonical 'mason package ship --to', charter form 'mason package --ship' kept as the single documented bare-noun alias; FR-30 exception recorded; new story S-3.9.
- (change) HIGH FIXED: FR-23 vs FR-44 — FR-44 now carries a named allow-list of exactly ONE entry, forbids a blanket 'except where CFE is needed' formulation, and asserts the excepted target fails for the RIGHT reason. FR-46 vs NFR-13 — FR-46 declared the sole NFR-13 exception, slow-marked, skips cleanly.
- (change) HIGH FIXED: 3 missing FR families added — FR-48 configuration surface (D-13: every knob is flag+env, no config file; FR-4's 'configurable timeout' now has a mechanism), FR-49 logging + child-output streaming (incl. the stream-vs-capture decision for multi-minute builds and the json-stdout-purity assertion), and cross-invocation idempotence via D-11 target interrogation (fork search on CFE's deterministic add-recipe-<name> branch) — a re-run no longer opens a second staged-recipes PR.
- (change) MEDIUM FIXED: SM-1 evidence caveat corrected (the repo dogfoods the dual-artifact BUILD, not the ship — neither sibling has a publish task); new FR-50 + pypi-test target so SM-1's first production upload is not its first attempt; R-10 logged.
- (change) MEDIUM FIXED: FR-42 would have shipped vacuous — now requires an enumerated deny-list citing its CFE sources, planted-violation fixtures per category (an empty deny-list is a FAILING test), and a rationale comment on every entry. R-9 logged; SM-2 updated.
- (change) FR-9 Docker bullet retargeted at CFE's Docker entry point (local_builder.py is Docker-less) and flagged OQ-8 for drop if no adapter-reachable entry exists.
- (change) Spine updated in step: AD-10 names the per-target interrogation method; AD-15 carries the sanctioned Rule-2 exception. lint_spine clean (0 findings).
- (event) r2 counts: 50 FRs / 16 NFRs / 13 D-records / 5 epics / 38 stories. Coverage re-verified 50/50.
