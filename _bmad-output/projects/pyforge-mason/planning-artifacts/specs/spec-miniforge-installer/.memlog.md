---
topic: miniforge-installer — parked with trigger
updated: 2026-09-17T11:11
---

- (event) Parked spec authored 2026-08-22 (operator: get all seeds ready-to-implement): the Dream's own constraints forbid building until the trigger fires; zero stories minted — parking IS the decomposition decision
- (note) Trigger bound to an owned checkpoint 2026-08-22: steward Story 12.3 (air-gap parity) closeout re-evaluates this park — if the air-gap run surfaces a need for an offline Python distributable beyond pixi+mirrored channels, unpark; else recommend archive-as-never-needed. A consult line added to 12.3's story text
- (decision) REFRAMED by operator 2026-08-22: parked -> EXTENSION-POINT. The fleet defines the air-gap distribution CONTRACT (what any distributable must provide: mirrored-channel set, pixi bootstrap, verification hooks) + an empty backend registry + a shape-validating test — so a Miniforge/constructor-based (or any) installer can be built as a SEPARATE deliverable satisfying the contract (the _SUPPORTED_MODULES one-entry-at-a-time precedent). Stub story mason 9.2, coordinated with steward 12.3's air-gap run; 12.3's consult line now reads against the contract
- (note by claude) Trigger ownership, 2026-09-09 (fleet-readiness-decision-batch-2026-09-09 D11 / mason-E7). This Spec stays status: extension-point and is CORRECT AS WRITTEN -- the socket is real and the capability is correctly absent: pyforge/mason/airgap_contract.py:54 declares SUPPORTED_DISTRIBUTABLES = {} empty by design. The point of this entry is ownership asymmetry: the declared trigger -- steward's enterprise-airgap/12.3 work surfaces a private-channel-locking need for a Python distributable -- is STEWARD-OWNED and UN-WATCHED. Nothing on the steward side points back at this extension-point, so the trigger can fire in steward's chain with no signal reaching mason. Requesting a RECIPROCAL POINTER in steward's enterprise-airgap chain naming spec-miniforge-installer as the extension-point that activates when 12.3 surfaces the need (cross-station -- mason cannot write it; routed to steward in the fleet-readiness apply report).
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. status stays extension-point, trigger key and extension contract unchanged; a dated Trigger ownership section records the Spec as correct as written (SUPPORTED_DISTRIBUTABLES empty by design) and names the ownership asymmetry — the steward-owned trigger is un-watched — requesting a reciprocal pointer in steward's enterprise-airgap chain.
- (event) 2026-09-17 absorbed into spec-pyforge-mason (one-chain mason fold). Pointer + memlog + companions retained.
