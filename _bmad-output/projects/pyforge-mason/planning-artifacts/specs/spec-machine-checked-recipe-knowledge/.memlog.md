---
topic: The CFE failure catalog generated from the skill spec, enforced_by lint-verified
updated: 2026-09-17T11:11
---

- (capability) CAP-1 catalog generation: failure-catalog.yaml DERIVED from SKILL.md's G-corpus (one row per gotcha class: greppable symptom_signature tokens, enforced_by pointer or explicit null); CAP-2 the lint + drift gate: enforced_by pointers resolve against the live CFE check/test surface, catalog-vs-SKILL.md drift fails CI, the null-rows report is the machine-check backlog. Pattern from auto-recipe (unlicensed — pattern only). Rules 1/2 apply (CFE surface); doctor supplies the detector convention
- (event) Spec authored + decomposed 2026-08-22 as mason Epic 7 (2 stories)
- (event by claude) status: ready -> shipped (2026-09-09). Operator ruling, fleet-readiness-decision-batch-2026-09-09 section 2.2 (Table A row machine-checked-recipe-knowledge). Both CAPs are live and green: config/failure-catalog.yaml (117 rows) derives from SKILL.md's gotcha corpus via scripts/failure_catalog_generator.py, freshness-guarded by tests/meta/test_failure_catalog_freshness.py, and independently re-resolved by scripts/failure_catalog_check.py (DETECTOR scope repo at :41, auto-discovered by scripts/detectors.py) -- pixi run -e local-recipes failure-catalog-check reports clean.
- (decision by claude) Open question CLOSED and surface DECLARED (2026-09-09, same ruling). The catalog-home question -- .claude/skills/conda-forge-expert/config/ vs data/ -- is settled exactly as the question predicted: it landed in config/, because the artifact is derived-AND-tracked. Clear open_questions on the re-derive. surface: [] is wrong for a shipped Spec that owns a detector: add scripts/failure_catalog_check.py (the Story 7.2 drift detector). Add a dated line under Success signal recording that coverage is a number the fleet can watch -- failure-catalog-check prints null_rows=<n> coverage=<pct> on every run, 115 of 117 rows carry enforced_by: null, coverage 1.7 percent as of 2026-09-09 -- and that raising it is the null-rows backlog's job, not this Spec's. DEFERRED, not applied here: deleting scripts/spec_surface_allowlist.txt:100 (the allowlist entry noting the detector is parked pending mason claiming it via a proper folder-format spec -- which this IS). That is a code edit under scripts/, outside this pass's write scope; it needs a story or a maintenance commit paired with a scoped --write-baseline stamp.
- (event by claude) Vessel found for the deferred allowlist cleanup (2026-09-09): the scripts/spec_surface_allowlist.txt:100 deletion is NOT a new story -- it is folded into mason Story 15.1 as a NAMED acceptance clause, because that story already has to re-stamp scoped surface baselines for every governed path it deletes or moves. Line 100 is the scripts/failure_catalog_check.py entry whose own comment reads allowlisted pending mason claiming it via a proper folder-format spec; THIS Spec is that folder-format spec, and its 2026-09-09 re-derive adds scripts/failure_catalog_check.py to surface:, so the allowlist entry becomes a governed path listed twice. Story 15.1's clause requires the stamp be SCOPED -- python scripts/spec_surface_check.py --write-baseline --spec pyforge-mason/spec-machine-checked-recipe-knowledge -- never a bare --write-baseline, because the baseline reads the working tree plus git ls-files and this repo's tree is routinely dirty with other stations' concurrent work; a bare stamp would bake their edits into mason's baseline. New files must be git add-ed BEFORE stamping.
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. CAP-1/CAP-2 preserved; status ready -> shipped; catalog-home open question retired (ANSWERED comment kept) and surface: [] replaced with scripts/failure_catalog_check.py; Success signal gains the dated coverage line (117 rows, 115 null, 1.7 pct) with the raising-it-is-the-backlog's-job boundary; the spec_surface_allowlist.txt:100 deletion recorded as an Assumption naming mason Story 15.1 as its vessel and the scoped --write-baseline requirement. No baseline stamped in this pass.
- (event by claude) 2026-09-09 (lead, after the re-derive): adding scripts/failure_catalog_check.py to surface: made the scripts/spec_surface_allowlist.txt entry for it match nothing (spec-surface stale-allowlist FAIL), so the lead removed that allowlist line and stamped a scoped baseline (--write-baseline --spec pyforge-mason/spec-machine-checked-recipe-knowledge) in the same change; Story 15.1's allowlist clause is therefore already satisfied and its AC should read as verified, not owed.
- (event) 2026-09-17 absorbed into spec-pyforge-mason (one-chain mason fold). Pointer + memlog + companions retained.
