---
topic: django-accelerator-framework — first baseline stamp (drift-blind landing gap)
updated: 2026-08-15T01:45
---

- (decision) This spec (surface `src/platform/**`, `src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/**`) landed on `main` at f150dce382 ("post-stop follow-ups: steward Epic 13 (scratch-worktree) + spec-django-accelerator-framework") without ever getting a `.memlog.md` or a `--write-baseline` stamp — a landing gap, not a reviewed drift. `spec_surface_reconcile.py`'s `[drift-blind]`/`[no-baseline]` findings (empty contract hash, 120 governed files) are the direct, expected consequence: the spec has been live on `main` with no baseline at all.
- (evidence) This was not cosmetic — it fleet-wide-blocked landing: herald's story `14-1-gate-report-interface` (run `20260814-202329-6e05`) deferred at attempt 2 on exactly this verify failure, and `S-13.7`'s reconcile command is appended into every station's rendered `policy.toml` (`pyforge.marshal.adapters.harness_bmadloop::render_policy_toml`), so every other running station (atlas, marshal, mason, steward) was heading for the identical defer the moment its own current dev attempt reached its verify step.
- (decision) Reconciling by naming the changed paths per S-13.2/S-13.7 convention: the governed surface is Story 10.1's `src/platform/` render (landed 2026-08-14, `config/settings/{base,local,production,test}.py`, `config/urls.py`, `config/asgi.py`, `config/celery_app.py`, plus the cookiecutter-django scaffold under it) and the pre-existing `pyforge-steward` dashboard package (`middleware.py`, `declarations.py`) the spec's own CAP-1 names as the second reference shape it documents, not new drift. Stamping the baseline now against the current on-disk state of both trees.
- (decision) Scoped stamp: `python scripts/spec_surface_check.py --write-baseline --spec pyforge-mason/spec-django-accelerator-framework` — this spec only, per S-13.2 (a full unscoped `--write-baseline` would accept every OTHER spec's pending drift as correct too, which this fix does not touch or intend to launder).
- (event) Marshal Story 10.4's own landing gate independently rediscovered this exact gap (`f150dce382` shipped no memlog/baseline) while reconciling its own verify run, and drafted a second, parallel fix (same diagnosis, same scoped-stamp remedy) that collided with this entry as an add/add merge conflict when 10.4 landed. Resolved by keeping this entry (already live on `main` and confirmed working since 2026-08-15T01:45) — the two fixes reached identical conclusions independently, so no reconciliation of substance was needed, only of the file itself. Marshal 10.4's own diff touches neither governed glob (`src/platform/**`, the steward dashboard), so its landing carries zero drift against this spec.
