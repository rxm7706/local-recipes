---
topic: django-accelerator-framework (legacy memlog; frontmatter added by the 2026-08-22 format migration, marshal Story 25.3 — body below is the untouched original)
updated: 2026-09-05T06:06
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-django-accelerator-framework`.


# .memlog.md — spec-django-accelerator-framework

## 2026-08-14 — First reconciliation (repair pass, found during Story 10.3 resume)

This spec's `SPEC.md` was created on 2026-08-14 (commit `f150dce382`, `status: draft`)
declaring `surface: [src/platform/**, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/**]`,
but no sibling `.memlog.md` was ever written for it and its baseline was never stamped in
`scripts/.spec-surface-baseline.json`. This is this spec's first-ever reconciliation.

The gap surfaced while resuming Story 10.3 ("One image, both engines," a pyforge-steward
story) under `bmad-loop`: the repo-wide `python scripts/spec_surface_reconcile.py` verify
gate that runs on every story failed with `[drift-blind]` / `[no-baseline]` findings for
this spec. The failure is pre-existing and independent of Story 10.3's own scope — it
reproduces identically on the parent commit `64e717d135` (before Story 10.3 started),
which reports 120 governed files instead of 122; the 2-file delta is exactly the two new
files Story 10.3 added inside this spec's surface, `src/platform/Containerfile` and
`src/platform/compose/compose.yml`, folded into this baseline like every other
already-tracked file under `src/platform/**`.

This spec's own CAP-1 documents a shape (cookiecutter-django + FastAPI accelerator
contract) that it asserts is already real across two surfaces — `src/platform/**`
(rendered by Stories 10.1/10.2/10.3, family-owned by `spec-python-agent-platform`) and the
Steward dashboard (`src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/**`,
pre-existing) — but CAP-1's own "success" clause admits no clause-by-clause conformance
audit against either surface has actually happened yet ("both existing surfaces verifiably
conform to the clauses applicable to each — or carry dated deviations" is stated as a goal,
not a completed check). This memlog and its accompanying baseline stamp do not perform that
audit; they adopt the current tracked content of both governed trees (109 files under
`src/platform/**` + 13 files under the dashboard tree = 122) as the initial reconciliation
baseline so the spec surface machinery has a starting point to diff future changes against.
Verifying actual clause-by-clause conformance to CAP-1 remains open work under this spec.

Baseline stamped for this spec only (`python scripts/spec_surface_check.py --write-baseline
--spec pyforge-mason/spec-django-accelerator-framework`) in the same change;
`python scripts/spec_surface_reconcile.py` confirmed `OK: every tracked file governed or
allowlisted; no drift.` (exit 0) afterward.

## 2026-08-15 — `compose.yml` changed within the same Story 10.3 repair session

`src/platform/compose/compose.yml` changed again, later in the same repair session that wrote
the entry above, before it ever landed: Story 10.3's own review pass (Blind Hunter + Edge Case
Hunter) found the `platform` service never ran `manage.py migrate`, so a fresh `docker compose
up` served a database with zero tables (`GET /admin/login/` 500'd,
`django.db.utils.ProgrammingError: relation "django_site" does not exist`, confirmed live). Fixed
by adding a `command:` override to the `platform` service that runs `manage.py migrate --noinput`
before `exec`-ing gunicorn — full evidence trail (before/after, live-reverified) is in
`spec-10-3-one-image-both-engines.md`'s own Verification section
("Repair-pass re-verification"). No other file under either governed tree changed in this same
pass. Baseline re-stamped for this spec only in the same change (see command below); no
clause-conformance implication for this spec's own CAP-1 — the change is inside
`spec-python-agent-platform`'s territory, this spec's surface just happens to overlap it.

## 2026-08-15 — Story 10.3 follow-up review pass (+ a correction to this file's first entry)

**Correction to the 2026-08-14 entry above.** That entry said "the 2-file delta is exactly
the two new files Story 10.3 added inside this spec's surface,
`src/platform/Containerfile` and `src/platform/compose/compose.yml`". The *count* delta
(120 → 122 governed files) was two, but the same commit also MODIFIED four already-tracked
files inside this spec's surface, which a file count cannot show and that entry did not
name: `src/platform/config/settings/base.py`, `src/platform/config/urls.py`,
`src/platform/platformapp/static/css/vendor/bootstrap.min.css`, and
`.../js/vendor/bootstrap.min.js`. They were folded into that first baseline stamp unnamed.
Naming them here is the point of the exercise — `spec_surface_reconcile.py`'s own contract
is "RECONCILE by naming the changed paths … never accept its own drift as correct", and a
stamp taken under an entry that names only additions launders every modification. Caught by
Story 10.3's follow-up review pass (Blind Hunter). All four changes belong to
`pyforge-steward/spec-python-agent-platform`, which owns this territory; they are described
in full in that spec's own `.memlog.md` entry of the same date, which this file does not
duplicate.

**This pass's own changes under this spec's surface** (Story 10.3's follow-up review, all
`pyforge-steward` work; this spec's surface merely overlaps it):

- `src/platform/Containerfile` — CHANGED. Pip layer stopped shadowing five conda-provided
  packages (`pillow`, `celery`, `uvicorn`, `gunicorn`, `argon2-cffi`), which in turn
  required raising `uvicorn-worker` to `>=0.4.0`; `$HOME` (`/app`) made group-0-writable so
  an arbitrary UID can actually write it; `EXPOSE 8000` declared; the secrets-scan gate
  extended to cover the shipped `shell-hook.sh`.
- `src/platform/compose/compose.yml` — CHANGED. The `platform` service gained its own
  `healthcheck:` (without one, "all services healthy" was unobservable and
  `compose up --wait` returned early) and `restart: on-failure`.

No clause-conformance implication for this spec's own CAP-1; the clause-by-clause audit that
this spec's first entry recorded as still-open remains open.

Baseline re-stamped for this spec only in the same change.

## 2026-08-15 — Story 10.3 follow-up review pass (second pass)

An independent follow-up review of Story 10.3 changed three files under this spec's surface. All
of it is `pyforge-steward` work under `spec-python-agent-platform`, which owns this territory;
this spec's surface merely overlaps it, and that spec's `.memlog.md` entry of the same date
carries the full reasoning and evidence rather than duplicating it here.

- `src/platform/Containerfile` — CHANGED. `$HOME` moved off `/app` to a dedicated `/app/.home`
  (a group-writable `/app` let the runtime user replace `/app/entrypoint.sh` — POSIX takes
  unlink permission from the directory, not the file); `STATIC_ROOT` tightened to group
  read-only; the nine explicitly-named pip extras pinned exactly rather than by floor, so the
  image is reproducible from a fixed commit + lock.
- `src/platform/config/urls.py` — CHANGED, comment only. Records that `/ht/` is a connectivity
  probe, not a schema or write check.
- `src/platform/tests/test_health_endpoint.py` — NEW. Regression-tests the `/ht/` route against
  the pip-pinned `django-health-check==3.24.0`, the only stack where the documented
  raw-class-vs-dotted-string trap bites and the one gate that can catch it (`manage.py check`
  demonstrably cannot).

No clause-conformance implication for this spec's own CAP-1; the clause-by-clause audit this
spec's first entry recorded as still-open remains open, and this entry does not advance it.

Baseline re-stamped for this spec only in the same change.

## 2026-08-15 — Story 10.3 (pyforge-steward), third follow-up review pass (surface reconcile)

This spec's `surface:` includes `src/platform/**`, so a steward story that touches the Django host
drifts it. Four governed paths changed; naming all four, per this reconciler's own contract that a
stamp names changed paths rather than folding them into a file count:

- `src/platform/Containerfile` — pip layer now fails the build if pip displaces a conda-provided
  package (`Attempting uninstall:` is fatal); `set -o pipefail` added so the new `| tee` cannot
  mask a failing `pip install`.
- `src/platform/config/urls.py` — comment only: the `/ht/` block stated 4.5.0's default `checks`
  list unqualified, in a comment whose subject is 3.24.0-vs-4.5.0 divergence. Both defaults now
  stated from the installed packages.
- `src/platform/tests/test_health_endpoint.py` — the route-shadowing test asserted
  `reverse("home")`, which cannot see `/ht/` being re-routed. Now asserts `resolve("/ht/")`.
- `src/platform/compose/compose.yml` — comment only: `restart: on-failure` is unbounded, not
  "a few attempts"; the terminal verdict comes from `healthcheck:`.

None of these touch the accelerator framework's own CAP clauses (no cookiecutter scaffolding, no
settings-module or app-layout change); the delta is a build-time guard, two comment corrections,
and one strengthened test assertion. Verification trail is in
`pyforge-steward/spec-python-agent-platform`'s memlog entry of the same date — 24/24 pytest on the
pip stack, and a green live container run (migrate, `/ht/`, `/admin/login/`, `/`, `/api/health`,
static assets, arbitrary-UID filesystem contract) on the conda stack.

Baseline stamped for this spec only (`python scripts/spec_surface_check.py --write-baseline
--spec pyforge-mason/spec-django-accelerator-framework`) in the same change.

## 2026-08-15 — CI lint + type fixes during landing (`tests/test_health_endpoint.py`)

`src/platform/tests/test_health_endpoint.py` — CHANGED twice (a `ruff --fix` import-sort, then
a `# type: ignore[attr-defined]` for a known django-stubs gap on `ResolverMatch.func.
view_class`), both no behavioral change. No clause-conformance implication for this spec's own
CAP-1. Full context in `pyforge-steward/spec-python-agent-platform`'s memlog entry of the same
date. Baseline re-stamped for this spec only in the same change.

## 2026-08-20 — Story 11.1 (pyforge-steward): Langflow joins as a pluggable app

Eleven governed paths changed under this spec's `src/platform/**` surface (commit `8cf4a13413`,
all `pyforge-steward` work under `spec-python-agent-platform`, which owns this territory — this
spec's surface merely overlaps it, and that spec's `.memlog.md` entry of the same date carries the
full reasoning and evidence rather than duplicating it here):

- `src/platform/config/asgi.py` — CHANGED. Dispatch extended to forward `/api/v1/*`, bare
  `/health`/`/health_check`, and prefix-stripped `/langflow/*` to Langflow's own FastAPI app; the
  lifespan handling now drives both sub-apps' startup/shutdown for the process lifetime.
- `src/platform/config/fastapi_app.py` — CHANGED, docstring only.
- `src/platform/config/settings/base.py` — CHANGED. Registers a new `langflow_integration` app and
  derives its database/cache/config-dir settings from the platform's existing `DATABASES`/
  `REDIS_URL`.
- `src/platform/langflow_integration/{__init__.py,apps.py,asgi.py,tests.py,
  migrations/{__init__.py,0001_create_langflow_schema.py}}` — ADDED. A new pluggable Django app
  (migration-only; provisions its own Postgres schema via `RunSQL`) plus its schema-isolation and
  end-to-end flow-execution tests.
- `src/platform/tests/test_asgi_seam.py` — CHANGED. `pytest.importorskip("langflow")` added so the
  pip-only CI `test` job (which never installs `langflow`) doesn't fail collection now that
  `config/asgi.py` transitively imports it.
- `src/platform/tests/test_langflow_mount.py` — ADDED. Dispatcher routing coverage.

No clause-conformance implication for this spec's own CAP-1 — this is a new pluggable Django app
mounted through the existing ASGI seam (the pattern CAP-1's contract exists to enable), not a
change to the accelerator's own render shape (no cookiecutter scaffolding, settings-module, or
static-asset change). The clause-by-clause audit this spec's first entry recorded as still-open
remains open, and this entry does not advance it.

Baseline stamped for this spec only (`python scripts/spec_surface_check.py --write-baseline
--spec pyforge-mason/spec-django-accelerator-framework`) in the same change.

## 2026-08-20 — Story 11.1 (pyforge-steward), review pass 3 (post-verify-repair)

Two further governed paths changed under this spec's surface, both `patch`-category fixes from
an adversarial review pass run after reconciling this spec's own surface drift (above) — all
`pyforge-steward` work under `spec-python-agent-platform`, which owns this territory:

- `src/platform/config/asgi.py` — CHANGED. `_strip_langflow_prefix`'s scope-rewrite corrected to
  extend `root_path` only (not slice `path`/`raw_path`), matching real Starlette `Mount`
  semantics — the previous shape silently dropped the `/langflow` alias from redirect Locations.
- `src/platform/tests/test_langflow_mount.py` — CHANGED + one test ADDED. Proves the fix above and
  closes a lifespan-rollback coverage gap.

No clause-conformance implication for this spec's own CAP-1. Full accounting in
`pyforge-steward/spec-python-agent-platform`'s own memlog entry of the same date.

Baseline stamped for this spec only (`python scripts/spec_surface_check.py --write-baseline
--spec pyforge-mason/spec-django-accelerator-framework`) in the same change.

## 2026-08-20 — Cross-package reconciliation for steward platform-ci follow-up

Three governed paths under this spec's `src/platform/**` surface changed by a
pyforge-steward follow-up (not a mason story) after Story 11.1's Langflow mount
red'd platform-ci on arbitrary-UID boot and ruff. Full accounting in
`pyforge-steward/spec-python-agent-platform`'s memlog of the same date.

- `src/platform/config/settings/base.py` -- trailing comma + comment naming the
  Containerfile coupling for `/app/.langflow`.
- `src/platform/tests/test_langflow_mount.py` -- ruff PLC0415/SLF001: public
  `application` lifespan entrypoint, module-level asgi import.
- `src/platform/langflow_integration/asgi.py` -- mypy narrowing on
  `_LifespanManager._task` awaits (`assert self._task is not None`).
- `src/platform/Containerfile` -- `/app/.langflow` added as the third
  group-writable runtime path (GID 0, `g+rwX`). `/app` itself stays 0755.

No clause-conformance implication for this spec's own CAP-1.

Baseline stamped for this spec only (`python scripts/spec_surface_check.py
--write-baseline --spec pyforge-mason/spec-django-accelerator-framework`) in the
same change.

## 2026-08-21 — Surface reconcile (foreign-drift documentation)

The `src/platform` tree (Containerfiles, compose, config, dbgpt_integration)
accumulated steward Epics 10–11 landings (DB-GPT sidecar image, Pattern-B
integration, PRs #595–#603 era) without this spec's memlog moving — those
story landings are the record; no code change rides this entry.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (change) `scripts/emit_wagtailcore_schema_delta.py` added to `surface:` (2026-08-29, spec-surface-drift-reconciliation ongoing practice) — it was tracked and ungoverned (a `[spec-surface] ungoverned` finding). It emits additive Liquibase SQL for CRC Wagtail schema drift against `src/platform`'s Django settings, which this Spec already governs via `src/platform/**`; the script itself just lives at repo-root `scripts/`, outside that glob. First `--write-baseline --spec` scoped stamp for this file.
- (event) 2026-08-29 drift reconcile: `src/platform/Containerfile`, `src/platform/compose/dbgpt/Containerfile`, `src/platform/compose/mcp-host/Containerfile` — pixi builder image bumped 0.77.0→0.78.0 by commit `6ff2c008b4` (fleet-wide `bump-pixi-version` registry sync, all 14 pin sites). Mechanical, tracked by `scripts/pixi_version_registry.py` / `pixi-version-check`; no CAP impact. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-django-accelerator-framework`.
- (event) Surface reconcile 2026-09-04 (PR #1043): `src/platform/Containerfile` runtime stage gains `COPY src/shared/packages/pyforge-core/src/pyforge/core /app/pyforge/core` beside the steward copy (Story 14.2 made steward depend on core; collectstatic died `ModuleNotFoundError: pyforge.core` in Platform CI). No contract change for this Spec. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-django-accelerator-framework`.
- (event) Surface reconcile 2026-09-04 (PR #1043, fourth pass — the fleet CI health pass): Platform CI health pass on `src/platform/**` after GitHub Actions' 2026-08-30 → 2026-09-04 dormancy: Ruff safe fixes (20 files) plus a dated per-file `[tool.ruff.lint.per-file-ignores]` debt block and dated per-module `[[tool.mypy.overrides]]` (`disable_error_code`, never `ignore_errors`) in `src/platform/pyproject.toml`; `config/settings/test.py` imports `django_pyforge` after `.base` (mypy's Django plugin crashed otherwise); the hand-inserted changeset `python-agent-platform:21-1` folded into 21; `test_openfeature_channel_policy.py` cachebox constant follows the raised floor; `lane1_seed.py` moves Wagtail's seeded default page aside and seeds the content-variant Locale (every DB-marked test errored at test-DB creation since 2026-08-26) with two regression tests; portal tests carry Story 42.5's prefixed group claims; the redis-broker test is DB-marked and registers its runner; the flags chart test pins images by digest; the boot guard admits `pyforge.core.errors` (Mason 14.3 taxonomy); `django_pyforge/assertion/client.py` drops the `Path.home()/.bmad-loops` fallback the laptop-state rule forbids (no-root regression test). No contract change — every edit makes an existing gate true again; verified locally in the CI job's step order (ruff, mypy, policy suite, sqlmigrate extraction, full suite against an ephemeral PostgreSQL + Redis). Full record: `_bmad-output/projects/pyforge-steward/planning-artifacts/retros/retro-pyforge-steward-2026-09-04.md`. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-django-accelerator-framework` (scoped, never a bare `--write-baseline`).
- (event) Surface reconcile 2026-09-04 (PR #1043, fifth pass): follow-up after the first CI run of the pass (Platform CI 33916796738): `src/platform/tests/test_warden_portal_audit_start_get.py::_platform_python_rels` skips loudly when `origin/main` is not resolvable (a depth-1 CI checkout crashed it with `fatal: bad revision`), and `.github/workflows/platform-ci.yml` gains a shallow `git fetch` of `origin/main` in the test job so the guard actually runs there, plus a free-runner-disk step before the image builds (rootless podman ran out of space committing the ~2 GB python-agent-platform layer). No contract change. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-django-accelerator-framework`.
- (change by claude) Surface touch 2026-09-05 (PR #1045): src/platform/tests/test_warden_portal_audit_start_get.py's `_platform_python_rels` gained a comment recording why it deliberately does NOT import `pyforge.testing_kit` for its whole-branch diff-guard mechanics, unlike every other station's copy of this guard (retro-pyforge-steward-2026-09-04.md action item 11: the fleet's whole-branch diff-guard test mechanics (git subprocess calls computing files/commits changed since origin/main) were consolidated into a shared `pyforge.testing_kit.branch_diff_guard` module (PR #1045).) — this file lives under the one tree these guards exist to keep `pyforge.*`-import-free (the `pap:AD-2` boundary), so importing even a test-only `pyforge.*` package here would trip the very guard it defines. No behavior change. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-django-accelerator-framework`.
- (change by claude) Surface touch 2026-09-05 (PR ci/gate-ruff-format, retro action item 4): `ruff format .` applied across src/platform (45 files reformatted) and a "Ruff format" step gates the Platform CI test job going forward. Ignored `COM812` (missing-trailing-comma) in `src/platform/pyproject.toml` -- ruff's own formatter warns this rule "may cause conflicts when used with the formatter," and the reformat put 53 spots at odds with it; the formatter's placement is authoritative once it's gated. Dropped the now-redundant `COM812` entries from the two `ingest/github_projects` per-file-ignores. No behavior change to django-accelerator-framework's own concern. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-django-accelerator-framework`.
- (change by claude) Surface touch 2026-09-05 (retro action items 5 + 9, fix/steward-changeset-gate-and-dockerignore): (5) `src/platform/db/sqlmigrate_extraction.py` gained `find_unexpected_changesets`/`expected_id_from_filename` -- every changelog file must carry exactly one `--changeset` line matching its own filename-derived id, closing the Row 8 gap (a hand-inserted second header was invisible to the old first-match-only parse); `src/platform/tests/policy/test_sqlmigrate_extraction.py` covers it. (9) The three platform Containerfiles (`src/platform/Containerfile`, `src/platform/compose/dbgpt/Containerfile`, `src/platform/compose/mcp-host/Containerfile`) now write `.pixi/config.toml`'s one setting directly via `RUN` instead of relying on `.dockerignore`'s `!.pixi/config.toml` negation, which forced a full walk of the multi-GB `.pixi/` tree on a raw `docker build .`. No behavior change to this spec's own concern. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-django-accelerator-framework`. Merged with `main` (which had independently landed the ruff-format entry above) — this branch's file-level changes merged clean; only the append-only memlogs and the JSON baseline needed manual reconciliation (baseline keys re-derived post-merge via a scoped `--write-baseline`, not hand-merged).
- (change by claude) Surface touch 2026-09-05 (landing the post-merge follow-up-review commits of Stories 41.3 / 41.4 / 42.1 / 42.2 / 43.2 on `steward/land-followup-reviews-2026-09-05` (cherry-picked from 2eaa1e0d61 / 97d3d5c1d9 / 482f47fa92 / 295257ecc8 / 7e6b6c4d18 — each was committed minutes after its PR merged and never landed)): 41.3 `src/platform/db/README.md`, `src/platform/db/changelog/changes/pyforge-scribe-4-graph-nodes-stale-column.sql`, `src/platform/db/changelog/db.changelog-master.yaml`, `src/platform/db/create_app_role.sql`, `src/platform/db/sqlmigrate-map.yaml`, `src/platform/tests/policy/test_liquibase_ddl_governance.py` (governance gates tightened; one E501 wrapped at landing); 41.4 `src/platform/config/broker_tls.py`, `src/platform/config/startup/stage_one.py`, `src/platform/deploy/README.md`, `src/platform/tests/test_broker_tls_verified.py` (ruff-formatted at landing), `src/platform/tests/test_startup_required_settings.py` (hostname checking, three unpinned guarantees); 42.1 `src/platform/tests/test_mcp_transport_auth.py` (sidecar-hop failure catch widened); 42.2 `src/platform/tests/test_agent_rate_limits_and_run_bounds.py`, `src/platform/tests/test_start_get_survives_disconnect.py` (off-loop limiter, terminal-wins revoke, retention chain); 43.2 `src/platform/tests/test_station_api_host_dispatch.py` (station API contract patches). No contract change for this Spec. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-django-accelerator-framework`.
