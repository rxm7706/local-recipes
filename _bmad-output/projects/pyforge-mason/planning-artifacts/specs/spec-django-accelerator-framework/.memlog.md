---
topic: Django accelerator framework: the named contract any PyForge Django surface renders to (CAP-1 real now, first realized by src/platform/; CAP-2 templating engine explicitly parked on the dream's third-surface trigger)
updated: 2026-08-15T00:00
---

- (event) This memlog is created by **marshal Story 10.4's landing gate**, not by mason work. The Spec landed in `f150dce382` (2026-08-14) declaring a `surface:` but shipping neither a `.memlog.md` nor an entry in `scripts/.spec-surface-baseline.json`, so `scripts/spec_surface_reconcile.py` reported it twice — `[drift-blind]` and `[no-baseline]`. The consequence was not cosmetic: with no memlog the contract hash was **empty**, so it could never move, and therefore no change under either governed glob was reconcilable — the detector was blind to exactly the 120 files the Spec claims to govern. Because that detector is repo-level and its verdict is a single exit code, the red was inherited by **every unrelated story in the fleet**; marshal 10.4 requires rc=0 from it as a deterministic verify gate, so the story that had to clear it is the one whose gate ran, not the one that owns the Spec. This is the same "lands here by location, not by ownership" shape the sibling `spec-packaging-factory/.memlog.md` already records for its cross-station meta-test entries.
- (change) First baseline stamp (2026-08-15): `python scripts/spec_surface_check.py --write-baseline --spec pyforge-mason/spec-django-accelerator-framework`, covering the 120 tracked files governed by `src/platform/**` and `src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/**`. Scoped, so it MERGES this one key and rewrites no other Spec's entry — 83 Specs were already stamped and this one was absent, making it a **first stamp, not a re-stamp**, so no prior recorded state is overwritten. `git diff f150dce382 HEAD` over both globs is **EMPTY**: those 120 files have not changed since the Spec landed, so the stamp records exactly the state the Spec was authored against and accepts **no** pending drift. Marshal 10.4's own diff touches only `src/shared/packages/pyforge-marshal/**` and `tests/packaging/test_dependency_completeness.py` — zero overlap with either glob — so none of that story's drift can be laundered through this stamp.
- (note) From here on the per-file rule applies normally to this Spec: any change under `src/platform/**` or `src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/**` must be named in this memlog in the same change, with the baseline re-stamped scoped to this Spec. The contract hash is no longer empty, so governed drift is now visible to the detector rather than invisible to it.
