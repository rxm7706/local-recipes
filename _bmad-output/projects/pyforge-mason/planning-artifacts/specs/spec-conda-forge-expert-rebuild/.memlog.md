---
topic: conda-forge-expert rebuilt Skill-Forge-authored, slice by slice, with parallel-run with a detector-enforced end cutover (topic updated 2026-08-10 with the entry of the same date)
updated: 2026-09-25T19:23
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild`.


- (provenance) THIS RECORD STARTS HERE. Created 2026-08-09 by marshal S-13.5 (spec-surface-drift-reconciliation CAP-5 / FR-168): this Spec shipped declaring a `surface:` with no .memlog.md, so spec_surface_check's contract_hash() returned "" and the contract could never move — every governed change would have reported a hard [drift] whose printed remedy ("reconcile the spec") was unreachable, there being no contract to move. Nothing that happened before this entry was captured; do not read the absence of earlier entries as an absence of decisions. The baseline was scoped-stamped in the SAME change that created this file, because a new memlog moves the contract hash off "" and would otherwise downgrade this surface's next real drift from gating [drift] to informational [drift-presumed].
- (note) Governs 370 tracked files — by far the largest governed surface in the fleet without a contract until now, and the reason CAP-5 of spec-surface-drift-reconciliation was treated as urgent rather than tidy. The surface is the live CFE skill tree, which changes on nearly every recipe effort.
- (capability) CAP-1 the slice map, derived not guessed — the decomposition comes from measuring the existing skill, not from an authored plan.
- (capability) CAP-2 first slice: recipe generation, built + cut over + retired in ONE epic — no long-lived parallel implementation.
- (capability) CAP-3 the anti-atlas guard: cutover enforced by a detector, not by discipline — named for the atlas migration, where the old path outlived the new one because nothing failed when both existed.
- (capability) CAP-4 campaign state: the sequence survives sessions.
- (constraint) Rule 1 of CLAUDE.md applies to every slice: any conda-forge work under this surface goes through the conda-forge-expert skill, and Rule 2 requires a CFE-skill retro at each effort's closeout. This Spec rebuilds the skill; it does not exempt work from it.
- (change) RECONCILED BY NAME, 2026-08-09: .claude/skills/conda-forge-expert/tests/meta/test_spec_surface_check.py changed under this surface, adding marshal S-13.5's six drift-blind tests to the shared meta-suite. Recorded here rather than stamped — and the distinction is the whole point. Creating this memlog moved the contract hash off "", which silently downgraded that file's real, gating [drift] to an informational [drift-presumed]; the before/after finding diff S-13.5's AC requires is what caught it. Naming the path clears it honestly under S-13.2's per-file rule.
- (change) RECONCILED BY NAME, 2026-08-09: .claude/skills/conda-forge-expert/tests/meta/test_unpushed_work_check.py added under this surface — marshal S-3.10's regression tests for `unpushed-work-check` (FR-171: durability is measured by TIP, never by branch name), landing in the shared meta-suite where the repo's other detector tests already live.
- (note) The file was nearly lost before it was ever committed: `/.claude/skills` had been re-added to .git/info/exclude (line 46, directly BELOW the comment block explaining why it must stay out), so `git add -A` silently added nothing and `git status` showed nothing — the rule hides only NEW files, which is precisely why it cannot be spotted with the tool you would use to spot it. Removed again. The designated guard, tests/meta/test_skill_files_tracked.py, is INTACT and was verified to fail on demand (both test_every_skill_source_file_is_tracked and test_git_info_exclude_does_not_suppress_the_skill_tree red with the rule present and an untracked skill file) — it walks the filesystem instead of asking git, so it survives the rule. It simply had not been re-run between creating the file and noticing by hand. Second recurrence of this rule after PR #324 removed it; worth asking what keeps re-adding it.
- (change) RECONCILES .claude/skills/conda-forge-expert/tests/meta/test_actionable_statuses_conformance.py (2026-08-09, ADDED). A Doctor Epic 6 artifact, not CFE skill guidance: Story 6.7 / AD-13 restates `bmad_loop.sprintstatus.ACTIONABLE_STATUSES` inside `pyforge.doctor.sources.deps` so Doctor's verdict stops depending on the machinery it judges, and this test holds the *derived, never restated* invariant instead — asserting set equality against the INSTALLED harness. It lives here because this meta-test tree is the only suite that runs in an environment carrying `bmad-loop`, so it falls inside this Spec's surface by location rather than by ownership. No rebuild slice moved, no CHANGELOG entry, no version bump. Naming the path is the reconciliation; no other file in this surface is touched by this entry.
- (note) Third sighting of the `/.claude/settings.json` family of `.git/info/exclude` rules, and the question above ("worth asking what keeps re-adding it") now has one concrete answer for a sibling entry: on 2026-08-09 `marshal preflight` flagged MRS-PREFLIGHT-013 on ALL EIGHT loop homes for `/.claude/settings.json` shadowing that TRACKED path. Removed (backup at `.git/info/exclude.bak-2026-08-09`), which cleared the finding fleet-wide. Recorded here because it is the same failure class this entry's predecessor documents — a repo-wide exclude that hides only NEW files and so cannot be spotted with `git status`.
- (change) RECONCILED BY NAME, 2026-08-09: .claude/skills/conda-forge-expert/tests/fixtures/error_logs/unmatched.log ADDED — and it is the FOURTH sighting of this failure class in a row, this time in `.gitignore` rather than `.git/info/exclude`. The VisualStudio template block contributes a blanket `*.log`, which meant `tests/fixtures/error_logs/` could never hold a fixture: `test_failure_analyzer.py`'s two unmatched-log cases had been running against a missing file since they were written, asserting on `"Log file not found: …"` instead of on the no-match contract. Narrowly negated for this fixture tree only — weakening `*.log` repo-wide would trade one silent hole for a larger one. The pattern across all four sightings is now unmistakable: every one was a broad ignore rule swallowing a file nobody could see was missing, and in every case the tool you would reach for (`git status`) is structurally incapable of telling you.
- (change) RECONCILES .claude/skills/conda-forge-expert/tests/meta/test_bmad_artifacts_in_sync.py, .claude/skills/conda-forge-expert/tests/meta/test_spec_surface_check.py and .claude/skills/conda-forge-expert/tests/meta/test_skill_md_consistency.py (2026-08-09, CHANGED, Doctor Story 6-9). Not CFE skill guidance: `scripts/bmad_drift_check.py` and `scripts/spec_surface_check.py` retired into `pyforge.doctor.sources` (Stories 6.6/6.8), so both meta-tests are re-targeted from subprocess-running the deleted scripts to calling `sources.factory.gather`/`sources.chain.gather_spec_surface` directly. `test_skill_md_consistency.py`'s file-existence allowlist gained one entry (`spec_surface_check.py`) so its historical v8.79.1 Version History citation of the now-deleted file still passes. Same "lands here by location, not by ownership" shape as this Spec's own `test_actionable_statuses_conformance.py` entry above. No CFE CHANGELOG entry, no version bump. No other file in this surface is touched by this entry.
- (note) Audit Phase 0B reconciliation (2026-08-10): 1 drift-presumed path — .claude/skills/conda-forge-expert/tests/meta/test_forward_dependency_check.py changed by c698d4b1ad (doctor 6-9 shim retirement, landed PR #394 chain): the meta-test was retargeted from the deleted scripts/forward_dependency_check.py to pyforge.doctor.sources.deps. Cross-station collateral of a done doctor story, not CFE-rebuild work; no unaccounted change. Scoped stamp follows
- (decision) Operator 2026-08-10 (in-session, mason correct-course): the rebuild is GO — 'the current conda-forge-expert skill is unsustainable; we rebuild and shift to using the mason rebuilt version.' Spec draft -> ready
- (decision) Operator 2026-08-10 (follow-up, concern raised and overridden): cutover shape = PARALLEL-RUN BOTH, CUT OVER AT THE END — explicitly chosen over hard-cutover-per-slice with the atlas precedent (~29K unused lines) on the table. Mitigations mandated so the atlas failure modes cannot recur: (1) per-slice EQUIVALENCE HARNESS — old and new run the same regression corpus continuously, divergence reds CI (kills silent divergence); (2) DUAL-LANDING rule — any Rule-2 retro knowledge lands in BOTH the live skill and the slice briefs while parallel; (3) the END CUTOVER is a detector-enforced campaign-terminal state (kills the no-forcing-function failure): the campaign cannot close, and the rebuild Dream cannot read realized, until every caller flips and legacy retires in one recorded endgame
- (event) SPEC re-derived 2026-08-10: CAP-2 pilot = build + parallel-validate (old stays authoritative); CAP-3 = divergence-and-endgame guard (was per-slice cutover enforcement); 'hard cutover per slice' constraint rewritten to record the override + mitigations; end-cutover terminal obligation added
- (event) DE-REGISTERED from DEFERRED_SPECS (2026-08-10, explicit operator confirmation 'de-register rebuild, herald-live-backend, secure-live-dashboards'). Decomposition landed first so INV-A holds on the real condition rather than on a suppression entry
- (change) RECONCILED BY NAME, 2026-08-20: `.claude/skills/conda-forge-expert/{SKILL.md,CHANGELOG.md,MANIFEST.yaml,config/skill-config.yaml,scripts/_http.py,scripts/bootstrap_data.py,scripts/feedstock_context.py,scripts/feedstock_lookup.py,scripts/recipe_optimizer.py,scripts/recipe_updater.py,scripts/dependency-checker.py,scripts/inventory_channel.py,tests/unit/test_http_resolvers.py,tests/unit/test_http_skip_auth.py,tests/unit/test_s3_resolver.py}` changed, and NEW `.claude/skills/conda-forge-expert/{scripts/_paths.py,tests/unit/test_paths.py,tests/unit/test_data_dir_consistency.py,tests/unit/test_recipe_optimizer_python_floor.py,tests/unit/test_recipe_updater_interpreter.py,tests/unit/test_http_jfrog_host_gate.py,tests/unit/test_dependency_checker_auth_host_gate.py,tests/unit/test_inventory_channel_auth_host_gate.py}` — pyforge-mason Story 5.5 (Rule-2 CFE retrospective, v8.81.0 → v8.82.0), the effort's one sanctioned commit permitted to touch the CFE surface (AD-15). Triaged the four upstream CFE defects `epic-5-context.md` flagged during planning: fixed a duplicated/divergent data-directory-resolution helper (2 scripts resolved the wrong path entirely), two independent repo-root parent-walk depth bugs (`bootstrap_data.py` too deep, `recipe_optimizer.py` too shallow), and the unconditional JFROG_API_KEY/JFROG_USERNAME+PASSWORD cross-resolver credential leak in `_http.py` (now host-gated). Also landed G108 (`recipe_updater.py`'s hardcoded bare `"python"`, closing `DW-2-10-2`). A same-pass adversarial review (Blind Hunter + Edge Case Hunter, independent, no shared context) then found the JFrog fix incomplete on its first landing: two SIBLING scripts (`dependency-checker.py`, `inventory_channel.py`'s no-`_http` fallback) had their own independent, still-unconditional copies of the identical leak that `_http.py`'s fix never touched; `_configured_enterprise_hosts()` missed pixi-config-derived hosts entirely (a functional auth regression for `docs/reference/pixi-config-jfrog.example.toml`'s documented, project-recommended pixi-only setup, not merely an unclosed leak); the JFrog/GitHub branches were a single if/elif chain, so a `*_BASE_URL` resolving to github.com could shadow GITHUB_TOKEN with a JFrog credential; and `_paths.py`'s `_REPO_ROOT` computed eagerly at import time with no guard, so the resolution-failure resilience `recipe_optimizer.py` specifically had before this refactor was lost. All four patched same-pass (host-gating extended to both sibling scripts; pixi-config hosts unioned in; github.com/api.github.com now always excluded from the JFrog gate; `_paths.get_repo_root()` made lazy + `None`-returning, `recipe_optimizer.py`'s fallback restored) with dedicated regression tests for each; CHANGELOG/SKILL.md corrected to describe the actual (now-verified-complete) fix scope rather than the first pass's overclaim. Not rebuild-slice work — the live skill is still the sole implementation; CAP-1's slice map (Epic 6) has not started, so the DUAL-LANDING mitigation (decision above) has no slice briefs to land into yet. No other file in this surface is touched by this entry.
- (change) RECONCILED BY NAME, 2026-08-20 (Story 5.5 FOLLOW-UP REVIEW PASS, v8.82.0 → v8.82.1): `.claude/skills/conda-forge-expert/{SKILL.md,CHANGELOG.md,MANIFEST.yaml,config/skill-config.yaml,scripts/_http.py,scripts/_paths.py,scripts/dependency-checker.py,scripts/inventory_channel.py,scripts/feedstock_context.py,scripts/feedstock_lookup.py,scripts/bootstrap_data.py,tests/unit/test_http_jfrog_host_gate.py,tests/unit/test_dependency_checker_auth_host_gate.py,tests/unit/test_inventory_channel_auth_host_gate.py,tests/unit/test_paths.py,tests/unit/test_recipe_updater_interpreter.py,tests/unit/test_http_skip_auth.py}` changed. An independent follow-up review (fresh Blind Hunter + Edge Case Hunter, no shared context, run against the full baseline→HEAD diff) found the entry above's landed host gate correct in shape but wrong in three places that decide, in OPPOSITE directions, whether a credential is withheld or handed over: (1) `_host_of()` parsed with `urlparse().netloc.split(":")[0]`, which returns the USERNAME for `https://svc:tok@artifactory.corp/...` — a routine Artifactory form, so the operator's real mirror never entered the allowlist and silently stopped receiving its own credential — and collapses EVERY IPv6 literal to `[2001`, so an unrelated same-prefix address matched and RECEIVED it (now `urlparse().hostname`; the same defect had been copied verbatim into `inventory_channel.py`'s fallback); (2) the env-var half did not subtract public default hosts although the pixi half did, so a merely redundant `PYPI_BASE_URL=https://pypi.org/simple` re-opened the leak against a public host (now `_public_default_hosts()`, derived from `_http`'s own `_DEFAULT_*` globals so a new resolver is covered on declaration, subtracted from both halves); (3) `dependency-checker.py` takes its channel from `--channel`/`CONDA_CHANNEL_URL`/enterprise config, NONE of which is a `*_BASE_URL`, so gating on the shared allowlist alone withheld the credential from the operator's own Artifactory channel and left `CONDA_TOKEN` reaching nothing at all (now unions `_EXPLICIT_CHANNEL_HOSTS`, recorded on explicit branches only, never on the step-4 public fallback; its `_http`-unimportable path no longer degrades to "attach unconditionally", which had reinstated the leak for anyone in that state). Also: the gate is skipped entirely when no JFrog credential is set (it gates nothing else and cost an `os.environ` walk plus a stat + TOML parse of the pixi config chain on a path every `make_request()` takes); `_pixi_configured_hosts()` no longer lets `read_pixi_config()`'s `Path.home()` RuntimeError raise through every outbound request; npm's own `npm_config_registry`/`NPM_CONFIG_REGISTRY` join the scan (`resolve_npm_urls` honours them); `dependency-checker.py` stopped appending to `sys.path` once per call; and THREE `_paths` callers were still crashing on v8.82.0's own `None` contract — `feedstock_context.py`/`feedstock_lookup.py`/`bootstrap_data.py` bound `get_data_dir()` straight into a module-scope `_DIR / "name"`, a `TypeError` at import, the same crash the lazy contract exists to avoid one file over — now degrading (optional cache disabled) or exiting with a diagnostic. `inventory_channel.py`'s own hand-rolled `_get_data_dir()` gained the `.resolve()` the new constraint requires. Two doc overclaims corrected: SKILL.md's constraint called the two sibling copies "still-unpatched" three lines above saying both were fixed, and both docs claimed `mason_cfe_surface_check.py` "proves" the AD-15 exception was used exactly once — verified against the detector's own code and a live run: it enumerates only commits touching `src/shared/packages/pyforge-mason`, so a CFE-only commit (this one included) never enters its scan and its `exception-reused` check cannot fire for it. Four tests were fixed rather than trusted (the JFrog fixture read the developer's real pixi config chain — green here, red on an enterprise machine; two tests could not fail as written; one had an unguarded `ruamel.yaml` dependency the module treats as optional). Unit suite 1352 → 1377 passed, 0 failed. No other file in this surface is touched by this entry.
- (correction) The 2026-08-20 entry above (the v8.82.0 landing) closed with "No other file in this surface is touched by this entry", and that sentence was accurate about what the COMMIT changed but not about what its scoped `--write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild` re-stamp ABSORBED. A scoped restamp rewrites every hash in the spec's surface, not only the paths the commit touched, so four files that had changed out-of-band under earlier, unrelated commits were silently brought current with no reconciliation of their own: `.claude/skills/conda-forge-expert/scripts/feedstock_enrich.py` and `scripts/recipe-generator.py` (both last changed by the conda-forge packaging-inventory commits `329003b14d`/`d204da00fd`), and two ADDED paths, `tests/meta/test_dashboard_resolve_project.py` and `tests/meta/test_fleet_picture_loop_home_staleness.py` (marshal/doctor bmad-loop collateral). Named here so the record matches the baseline: their drift is accounted for, not laundered. Standing lesson for this surface — 370 tracked files, changing on nearly every effort — a scoped restamp is only as honest as the memlog entry authorising it, so diff the baseline JSON itself (`git diff -- scripts/.spec-surface-baseline.json`) before writing the entry, rather than listing the paths you happen to have edited.
- (note) Paths named literally, one per line, for the two entries above — because `pyforge.doctor.sources.chain`'s drift-presumed check is a plain substring test (`elif f not in named`) against the memlog text, so the brace-expansion shorthand this file has used for path lists (`.claude/skills/conda-forge-expert/{SKILL.md,CHANGELOG.md,...}`) names NOTHING as far as the detector is concerned. Earlier entries got away with it only because their scoped restamp made `old == new` before the naming test was ever reached; the moment a restamp is deferred or partial, every path silently reads as unnamed. Write full paths, one per line, in any entry that intends to clear drift:
  - `.claude/skills/conda-forge-expert/SKILL.md`
  - `.claude/skills/conda-forge-expert/CHANGELOG.md`
  - `.claude/skills/conda-forge-expert/MANIFEST.yaml`
  - `.claude/skills/conda-forge-expert/config/skill-config.yaml`
  - `.claude/skills/conda-forge-expert/scripts/_http.py`
  - `.claude/skills/conda-forge-expert/scripts/_paths.py`
  - `.claude/skills/conda-forge-expert/scripts/dependency-checker.py`
  - `.claude/skills/conda-forge-expert/scripts/inventory_channel.py`
  - `.claude/skills/conda-forge-expert/scripts/feedstock_context.py`
  - `.claude/skills/conda-forge-expert/scripts/feedstock_lookup.py`
  - `.claude/skills/conda-forge-expert/scripts/bootstrap_data.py`
  - `.claude/skills/conda-forge-expert/scripts/recipe_optimizer.py`
  - `.claude/skills/conda-forge-expert/scripts/recipe_updater.py`
  - `.claude/skills/conda-forge-expert/scripts/feedstock_enrich.py`
  - `.claude/skills/conda-forge-expert/scripts/recipe-generator.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_http_jfrog_host_gate.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_dependency_checker_auth_host_gate.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_inventory_channel_auth_host_gate.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_paths.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_data_dir_consistency.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_recipe_optimizer_python_floor.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_recipe_updater_interpreter.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_http_skip_auth.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_http_resolvers.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_s3_resolver.py`
  - `.claude/skills/conda-forge-expert/tests/meta/test_dashboard_resolve_project.py`
  - `.claude/skills/conda-forge-expert/tests/meta/test_fleet_picture_loop_home_staleness.py`
- (change) RECONCILED BY NAME, 2026-08-20 (Story 5.5 THIRD REVIEW PASS, v8.82.1 → v8.82.2). A second independent follow-up review (fresh Blind Hunter + Edge Case Hunter, no shared context, run against the full baseline→HEAD range) found the host gate still wrong in three ways, each reproduced by executing the code before triage rather than accepted from the reviewers. (1) **The gate answered only half the question.** `dependency-checker.py` deliberately authorizes the channel the operator NAMED, and that channel is routinely public — `--channel https://conda.anaconda.org/myprivateorg` is a supported setup — so host-level authorization alone still sent `JFROG_API_KEY` to `conda.anaconda.org`, and, the JFrog branch being checked first, it also shadowed the `CONDA_TOKEN` the previous pass had just fixed, whenever both were set. Now split by credential KIND: a named public host gets its channel token and nothing else; a non-public configured host keeps the original priority. Separately `_EXPLICIT_CHANNEL_HOSTS` only ever grew — a module-level set assembled from caller arguments, so in the long-lived MCP server a host named by one request's `--channel` stayed credential-authorized for every later request (reproduced) — now cleared per `get_configured_channels` call. (2) **The copy was wider than the original.** `inventory_channel.py`'s no-`_http` fallback never received the previous pass's public-default subtraction, making it *wider* than `_http` in exactly the leaking direction while its docstring claimed it was deliberately narrower; verified side by side, the same env that made `_http.auth_headers_for` return `{}` for anaconda.org made the fallback attach `X-JFrog-Art-Api` to it. (3) **The third host-parse.** `netrc_credentials` was still `urlparse().netloc.split(":")[0]` — the exact form the previous pass's own new constraint bans, in the same file, left behind only because it is not on the allowlist path; a userinfo-bearing Artifactory URL matched no `machine` line and fell through to any `default` entry, sending an unrelated credential to the mirror the operator had a correct entry for. Also in this pass: **G108 was documented backwards and implemented backwards.** `CONDA_PYTHON_EXE` is not the activated environment's interpreter — conda writes `sys.executable` into it beside `_CONDA_ROOT` and `conda activate <env>` never re-exports it, so on any multi-env install it names BASE python — yet all four call sites preferred it over `sys.executable`, running the child under a python that need not carry the caller's dependencies (live failure: a misleading "ruamel.yaml is not installed" on a machine where it plainly is). Corrected at all four sites, with the drift guard widened from the two files the original finding named to all four. The four `_paths` callers added last pass each inserted into `sys.path` unguarded — the same defect that pass fixed in `dependency-checker.py`, reintroduced four times over; now guarded. Three tests that could not fail were repaired: two `recipe_optimizer` floor tests asserting a default that equals the real pinning file's value (so they passed with the monkeypatch deleted), and the GitHub-shadow guard whose premise `_public_default_hosts()` had already made unreachable. `_paths.py`'s docstring claim of "correct-but-duplicated" copies corrected — ~35 files under `scripts/` carry an un-`.resolve()`d walk. Unit suite 1377 → 1386 passed, 0 failed. One file outside this surface changed in the same commit and is reconciled in its own spec's memlog: `src/shared/packages/pyforge-doctor/tests/unit/test_checks_env_hygiene.py` (see `pyforge-doctor/spec-pyforge-doctor`). Paths named literally, one per line, per the convention recorded in the note above:
  - `.claude/skills/conda-forge-expert/SKILL.md`
  - `.claude/skills/conda-forge-expert/CHANGELOG.md`
  - `.claude/skills/conda-forge-expert/MANIFEST.yaml`
  - `.claude/skills/conda-forge-expert/config/skill-config.yaml`
  - `.claude/skills/conda-forge-expert/scripts/_http.py`
  - `.claude/skills/conda-forge-expert/scripts/_paths.py`
  - `.claude/skills/conda-forge-expert/scripts/dependency-checker.py`
  - `.claude/skills/conda-forge-expert/scripts/inventory_channel.py`
  - `.claude/skills/conda-forge-expert/scripts/feedstock_context.py`
  - `.claude/skills/conda-forge-expert/scripts/feedstock_lookup.py`
  - `.claude/skills/conda-forge-expert/scripts/bootstrap_data.py`
  - `.claude/skills/conda-forge-expert/scripts/recipe_optimizer.py`
  - `.claude/skills/conda-forge-expert/scripts/recipe_updater.py`
  - `.claude/skills/conda-forge-expert/scripts/github_updater.py`
  - `.claude/skills/conda-forge-expert/scripts/npm_updater.py`
  - `.claude/skills/conda-forge-expert/scripts/recipe-generator.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_http_jfrog_host_gate.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_dependency_checker_auth_host_gate.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_inventory_channel_auth_host_gate.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_paths.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_recipe_optimizer_python_floor.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_recipe_updater_interpreter.py`
- (change) RECONCILED BY NAME, 2026-08-20 (same Story 5.5 third-pass commit, follow-on edit to `.claude/skills/conda-forge-expert/SKILL.md`): the JFrog constraint claimed `_public_default_hosts()` covers "a newly-added resolver the moment it is declared". That is true only for a resolver that declares its fallbacks in a `_DEFAULT_*` global. `resolve_anaconda_channel_urls` inlines its two public fallbacks as f-string literals in its own body, so it is covered today only because `repo.prefix.dev` and `conda.anaconda.org` happen to appear in `_DEFAULT_CONDA_FORGE_FALLBACKS` — coincidence, not mechanism, and the next resolver written that way gets no coverage while SKILL.md tells its author the protection is automatic. Reworded to state the actual precondition and to instruct authors to declare fallbacks in a `_DEFAULT_*` global. No code change; the derivation itself is correct for what it can see.
  - `.claude/skills/conda-forge-expert/SKILL.md`
- (change) RECONCILED BY NAME, 2026-08-20 (Story 5.5 FOURTH REVIEW PASS, v8.82.2 → v8.82.3). A third independent follow-up review (fresh Blind Hunter + Edge Case Hunter, no shared context, full baseline→HEAD range) found five real defects in the previous two passes' own fixes, each reproduced by executing the code before triage. (1) **The derivation could not see every public host.** `_public_default_hosts()` derives from this module's `_DEFAULT_*` globals, which is correct for what it can see but blind to a public host reached from an inline-built URL (`anaconda.org`), as a CDN/redirect target (`files.pythonhosted.org`), or under a vendor's second domain (`repo.anaconda.com`) — plus `dev.azure.com`. Each was reproduced receiving `JFROG_API_KEY` under a `*_BASE_URL` naming it: the exact public-host leak the previous pass added the subtraction to close, arriving through the hosts that subtraction could not enumerate. `_PUBLIC_HOST_FLOOR` now sits beneath the derivation (a floor, never a replacement), and an EMPTY result is no longer cached — the set is subtracted from the allowlist, so freezing an empty one silently re-opens the gate for the process's life. (2) **The canonical host-parse broke netrc matching.** Moving `netrc_credentials` to `_host_of` last pass was right about userinfo and wrong about case: `_host_of` is `urlparse().hostname` and LOWERCASES, while `netrc.authenticators` is an exact dict lookup over the `machine` tokens as spelled and folds nothing. A legal `machine ARTIFACTORY.CORP.COM` stopped matching and fell through to `default` — reproduced returning the `default` entry, i.e. the very wrong-credential-to-the-mirror failure that migration was made to fix, re-entered by the fix. Now matched case-insensitively against `nrc.hosts`, with `default` still applying when nothing matches. (3) **A newly-reachable branch could take down all HTTP.** `netrc_credentials` builds its path with `Path.home()` outside the `try`; that raises `RuntimeError` (not `OSError`, so the existing handlers miss it) when neither HOME nor a passwd entry resolves. v8.82.0 gating the JFrog branch handed this branch the traffic, putting the raise on every request to an unconfigured host whenever a JFrog credential was set — reproduced. Guarded, exactly as `read_pixi_config` was guarded one pass earlier for the identical reason; this is the second time the same call has bitten this feature. (4) **The hand-kept copy drifted toward the leak again.** `inventory_channel.py`'s no-`_http` floor held 9 hosts against `_http`'s 18, so it stayed WIDER than `_http` in the leaking direction for 11 of them (crates.io, rubygems.org, gitlab.com, codeberg.org, endoflife.date, api.nuget.org, search.maven.org, luarocks.org, crandb.r-pkg.org, fastapi.metacpan.org, the anaconda S3 bucket) while its own docstring claimed it was never wider — the same defect the previous pass fixed, one list short. Brought to parity and pinned by a containment test, so the next divergence reds instead of leaking. (5) **The channel token went to the wrong vendors.** The previous pass's credential-KIND split was right in shape but treated every public host as interchangeable: a named `--channel https://repo.prefix.dev/myorg` or `https://pypi.org/simple` was handed `CONDA_TOKEN`, an anaconda.org credential, where it can never authenticate (reproduced on both). Restricted to the anaconda.org family. **Also — a rationale that was never traced to its entry point.** The previous pass justified its `_EXPLICIT_CHANNEL_HOSTS` clear and its `sys.path` guard with a "long-lived MCP server accumulates across requests" failure mode, asserted as reproduced, in SKILL.md, the CHANGELOG and this memlog. It is not reachable there: `conda_forge_server.py::_run_script` is `subprocess.run([sys.executable, script, *args])`, so every MCP tool call gets a fresh interpreter and no module-level state survives it. Both fixes are correct in-process and stay; the claims are corrected wherever they were written, and SKILL.md gains the general rule — when you justify a fix with a failure mode, name the entry point you traced it through. Two tests were also repaired rather than accepted: `test_http_skip_auth.py`'s "configured host" example was `anaconda.org` until the third pass repointed it at `dev.azure.com`, which is public too, so this pass's floor correctly stopped its baseline firing — repointed at a genuinely enterprise host; and `test_data_dir_consistency.py`'s equality assertions were near-tautological (verified by mutation: `_paths` at `parents[3]` left them passing), now backed by independent on-disk anchors. Unit suite 1386 → 1399 passed, 0 failed. Meta failing set returned to the same 8 pre-existing tests. `pyforge-doctor` 1031 passed, 0 failed. Files touched by this entry, named literally one per line because `pyforge.doctor.sources.chain`'s drift-presumed test is a plain substring match and this file's brace-expansion shorthand names nothing to it:
  - `.claude/skills/conda-forge-expert/SKILL.md`
  - `.claude/skills/conda-forge-expert/CHANGELOG.md`
  - `.claude/skills/conda-forge-expert/MANIFEST.yaml`
  - `.claude/skills/conda-forge-expert/config/skill-config.yaml`
  - `.claude/skills/conda-forge-expert/scripts/_http.py`
  - `.claude/skills/conda-forge-expert/scripts/dependency-checker.py`
  - `.claude/skills/conda-forge-expert/scripts/inventory_channel.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_http_jfrog_host_gate.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_http_skip_auth.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_dependency_checker_auth_host_gate.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_inventory_channel_auth_host_gate.py`
  - `.claude/skills/conda-forge-expert/tests/unit/test_data_dir_consistency.py`
- (change) RECONCILED BY NAME, 2026-08-20: a file was added to this Spec's governed surface by an
  unrelated station — pyforge-doctor's Story 10.3 ("the drift surfaces ambiently, never gates"),
  which wired `bmad_method.gather()` into `scripts/fleet_picture.py` and added a meta-test covering
  that probe. No CFE change of substance: the test asserts fleet_picture's own new bmad-core-drift
  block, imports nothing from the skill, and no skill file moved with it — this entry is
  surface/drift bookkeeping only, the capability belonging to
  `pyforge-doctor/spec-bmad-method-version-drift`'s CAP-3. Worth recording because doctor's own
  Story 10.3 entry reasoned the file was "covered by the allowlist's blanket `.claude/**` entry"
  and so named no drift for it. That is the wrong precedence: this Spec's `surface:` claims the
  path, and a declared surface outranks an allowlist exemption, so the file was governed and
  undeclared until now — caught by `spec_surface_reconcile.py` at mason's own landing, three merges
  later. Standing lesson: an allowlist entry is not a defence against a Spec that claims the same
  path; look for an owning `surface:` before concluding a new file is exempt. File touched by this
  entry, named literally one per line for the same substring-match reason as the entries above:
  - `.claude/skills/conda-forge-expert/tests/meta/test_fleet_picture_bmad_core_drift.py`

## 2026-08-21 — surface/drift bookkeeping: five landed sibling changes reconciled at the doctor-12.5 landing

Five files under this Spec's declared CFE test surface changed or appeared through
already-landed, already-reviewed sibling work whose own landings never moved this memlog:
mason Story 6.3's slice-equivalence proof (conftest hook + the integration test), doctor
Story 11.7's fleet-picture staleness test (merged 2026-08-21 AM), doctor Story 12.4's
dream-chain-gap test (PR #602), and doctor Story 12.5's four S-12.5 lock tests appended to
the existing spec-surface harness (PR #604). No CFE capability of substance changed in any
of them from this Spec's perspective — every file either tests fleet_picture's consumer-side
probes (doctor-owned capabilities) or proves slice equivalence against the unchanged CFE
surface; this entry is drift bookkeeping so the baseline can be re-stamped honestly, per the
reconcile-then-scoped-stamp procedure. Files, named literally one per line:
- `.claude/skills/conda-forge-expert/tests/conftest.py`
- `.claude/skills/conda-forge-expert/tests/integration/test_slice1_equivalence.py`
- `.claude/skills/conda-forge-expert/tests/meta/test_fleet_picture_dream_chain_gap.py`
- `.claude/skills/conda-forge-expert/tests/meta/test_fleet_picture_verification_staleness.py`
- `.claude/skills/conda-forge-expert/tests/meta/test_spec_surface_check.py`
- (event) Surface reconcile 2026-08-21: CFE v8.83.0 Rule-2 retro (BMAD 6.11 suite refresh) — SKILL.md gains G109 (dev-snapshot renumbering) + G110 (npm bin mutability), CHANGELOG/config/MANIFEST version bump, cheatsheet gains the SelfExplainML publish flow; organic MINOR produced by the bmad-suite recipe effort, not by mason's chain (relevant to DW-5-4-1's re-verify condition)
- (event by claude) RECONCILED-BY-NAME 2026-08-22: new meta-test .claude/skills/conda-forge-expert/tests/meta/test_no_retired_bmad_skill_ids.py lands under this spec's surface glob but belongs to marshal Story 25.1 (spec-bmad-611-era-alignment CAP-1) — a regression guard that reds any bare reintroduction of the 20 retired BMAD v6 skill IDs on live surfaces (seed templates, fleet epics/PRD, AGENTS.md, CLAUDE.md) before the v7 shim removal; one line-based allow rule (6.11|retired|forwarder|shim|deprecated). No CFE rebuild-slice behavior touched.
- (note by claude) 2026-08-22 review pass on Story 25.1's guard test (test_no_retired_bmad_skill_ids.py, RECONCILED-BY-NAME above): allow markers anchored (6.11 not inside v8.6.11/16.11/6.111; whole-word retired/forwarder(s)/shim(s)/deprecated), ID regex gains left boundary + IGNORECASE + hyphen-admitting right boundary so compounds fire the base ID, per-glob rot floors replace the single total, portable **/* glob, adjacent-line + lookalike negative tests. Marshal-owned change riding this spec's surface glob; no CFE rebuild-slice behavior touched.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event) Surface reconcile 2026-08-22 (Story 25.5 post-merge completion): two new CFE meta tests (test_fleet_picture_awaiting_operator.py, test_loop_stall_check_awaiting_operator.py) join their four siblings under this spec's surface — the 25.5 landing left them unclassified in the baseline (its review flagged it); classified here with the scoped re-stamp
- (event by claude) Surface reconcile 2026-08-31: 17 drift-presumed paths since baseline at `2ea9c3930933`. Fifty commits spanning CFE Waves A-E (cyclonedx-universe-inventory), v8.71-8.76 seed-gap suggesters, v8.83.0 Rule-2 retro, mason 12.7 compiled slice, audit remediation, MCP >2.0, and cross-station doc/test churn (all done in ledger). Representative story clusters verified:
  - CFE Waves A-E: export-purls, mapping-gap, universe-sbom, inventory-match, add-handoff, library-futures, recommend-2027 — scripts + tests under `.claude/skills/conda-forge-expert/`.
  - Seed-gap suggesters (v8.74-8.76): lts-registry-gap, cwe-seed-gap, spdx-schema-gap, license-map-gap.
  - `864558e02d3` — audit remediation wave 2 (AUD-CFE-* findings).
  - `6ace3fd6ab2` — v8.83.0 Rule-2 retro for bmad-suite refresh.
  - `c117ce842f1` — mason 7.1: failure-catalog.yaml generation.
  - `8c35423d58d` / `c46ac684af4` — mason 12.7 compiled slice adversarial-review patches.
  - `06fe64f6037` — MCP SDK >2.0 (conda_forge_server.py API compatibility).
  - Marshal 25.1/25.5 meta-tests + doctor/marshal/dashboard cross-station doc updates.
  No CAP text change — organic CFE skill evolution within contracted rebuild surface. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 18 governed path(s) drifted against this spec's recorded baseline (6 added, 11 changed, 1 removed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `.claude/scripts/conda-forge-expert/bmad_suite_metapackage.py` (added)
  - `.claude/scripts/conda-forge-expert/build_bmad_suite.py` (added)
  - `.claude/skills/conda-forge-expert/CHANGELOG.md` (changed)
  - `.claude/skills/conda-forge-expert/MANIFEST.yaml` (changed)
  - `.claude/skills/conda-forge-expert/SKILL.md` (changed)
  - `.claude/skills/conda-forge-expert/config/skill-config.yaml` (changed)
  - `.claude/skills/conda-forge-expert/scripts/bmad_suite_metapackage.py` (added)
  - `.claude/skills/conda-forge-expert/scripts/build_bmad_suite.py` (added)
  - `.claude/skills/conda-forge-expert/scripts/recipe-generator.py` (changed)
  - `.claude/skills/conda-forge-expert/scripts/recipe_optimizer.py` (changed)
  - `.claude/skills/conda-forge-expert/tests/meta/test_all_scripts_runnable.py` (changed)
  - `.claude/skills/conda-forge-expert/tests/meta/test_dashboard_renders.py` (removed)
  - `.claude/skills/conda-forge-expert/tests/meta/test_fleet_picture_awaiting_operator.py` (changed)
  - `.claude/skills/conda-forge-expert/tests/meta/test_fleet_picture_dispatch_phase.py` (added)
  - `.claude/skills/conda-forge-expert/tests/meta/test_skill_md_consistency.py` (changed)
  - `.claude/skills/conda-forge-expert/tests/unit/test_bmad_suite_metapackage_rewrite.py` (added)
  - `.claude/skills/conda-forge-expert/tests/unit/test_recipe_optimizer.py` (changed)
  - `.claude/tools/conda_forge_server.py` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (change) 2026-09-04 (PR #1043): CFE v8.85.2 retro `1da5fd0213` landed on the governed surface with `CHANGELOG.md` moving — `_paths.get_repo_root()` is a marker walk (nearest ancestor with `pixi.toml` + `.claude/`) and `gen_yml_reference.py`, `_path_guard.py`, `mapping_manager.py`, `vulnerability_scanner.py` resolve through it; the four (+ `_paths.py`) mirrored into the compiled `cfe-recipe-lifecycle` slice and `github_updater.py` re-ported into `cfe-recipe-generation` (v8.84.0 `--head`); `campaign-state.yaml` pointers advanced to `1da5fd0213`, both `equivalence` fields restored to green with the stale records kept as history; `cfe_rebuild_guard_check` + both slice harnesses green. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild`.
- (event by claude) CFE skill v8.86.0 (2026-09-05, bmad-suite 2026.9.5 refresh Rule-2 retro): .claude/skills/conda-forge-expert/SKILL.md (version 8.85.2->8.86.0, G111 noarch_platforms required for selector-carrying noarch recipes, G112 npm-from-commit-archive build + clean prod reinstall, G113 same-version change bumps build.number, CFE-metadata rule cfe-upstream-registry must match the publishing registry, history line), .claude/skills/conda-forge-expert/CHANGELOG.md (v8.86.0 entry), .claude/skills/conda-forge-expert/MANIFEST.yaml + .claude/skills/conda-forge-expert/config/skill-config.yaml (8.86.0), .claude/skills/conda-forge-expert/quickref/commands-cheatsheet.md (repo-wide lint aborts at empty recipe-maintainers; lint touched recipes by name), .claude/skills/conda-forge-expert/scripts/github_updater.py (_get_build_number; --head increments build.number instead of resetting), .claude/skills/conda-forge-expert/tests/unit/test_github_updater.py (G113 regression tests).
- (note by claude) v8.86.0 follow-through: .claude/skills/conda-forge-expert/config/failure-catalog.yaml regenerated (generate-failure-catalog) so the derived catalog carries G111-G113; tests/meta/test_failure_catalog_freshness.py guards this.
- (event by claude) 2026-09-05 guard follow-through for retro 4825cc426b (PR #1059 CI red: cfe-rebuild-guard unmirrored-retro on both compiled slices): .claude/skills/cfe-recipe-generation/1.0.0/cfe-recipe-generation/scripts/github_updater.py and .claude/skills/cfe-recipe-lifecycle/1.0.0/cfe-recipe-lifecycle/scripts/github_updater.py re-ported byte-for-byte (the --head build.number increment); recipe-generator.py in both slices caught up to live (5-line docstring lag since f45a9209d1, 2026-09-02, behavior identical); campaign-state.yaml brief_mirrored_through -> 4825cc426b on slices 1 and 2 with dated ADVANCED notes and RE-VERIFIED equivalence lines; slice-1 Tier-3 skill-brief.yaml gained the retro-mirror amendment; slice-2 brief file is absent on this machine (Tier-3 loss) -- debt recorded in campaign-state. Guard clean locally (9 qualifying retros in range).
- (note by claude) 2026-09-05 CFE v8.86.1 PATCH (branch maint/recipe-maintainers-sweep-2026-09-05, commit 879dbebf2f): .claude/skills/conda-forge-expert/SKILL.md (G26 marker-split extension re-landed from orphaned Story 13.1 commit 74bc80fe61; version 8.86.1; history entry), CHANGELOG.md (v8.86.1 TL;DR), MANIFEST.yaml + config/skill-config.yaml (8.86.0 -> 8.86.1), tests/meta/test_recipe_maintainers_nonempty.py (new -- guards empty/missing recipe-maintainers across recipes/*, the shape that crashed the repo-wide lint at Flake8-pyproject) changed; campaign-state.yaml brief_mirrored_through re-pointed x2 to 879dbebf2f (commit e83486e5f9). No shipped script changed, so neither compiled slice needs a re-port; scripts/cfe_rebuild_guard_check.py clean. Scoped --write-baseline follows.
- (note by claude) 2026-09-05 (same branch, follows 879dbebf2f): .claude/skills/conda-forge-expert/config/failure-catalog.yaml regenerated via generate-failure-catalog -- the G26 extension paragraph shifted the catalog's SKILL.md pointers (failure_catalog_check detector + test_failure_catalog_freshness were red). No CHANGELOG touch, so the rebuild-guard pointer stays at 879dbebf2f. Scoped --write-baseline follows.
- (note by claude) 2026-09-05 hygiene-2 PR (branch maint/hygiene-2-2026-09-05): CFE v8.86.2 PATCH (commit a5f63c95fb): .claude/skills/conda-forge-expert/SKILL.md (version, history), CHANGELOG.md (v8.86.2 TL;DR), MANIFEST.yaml + config/skill-config.yaml (8.86.1 -> 8.86.2), tests/meta/test_recipe_yaml_parse_audit.py (new strict duplicate-key test) changed; campaign-state.yaml brief_mirrored_through re-pointed x2 to a5f63c95fb (commit 05c3ed8720); config/failure-catalog.yaml regenerated with zero diff. No shipped script changed, no re-port; scripts/cfe_rebuild_guard_check.py clean. Scoped --write-baseline follows.
- (note by claude) 2026-09-06 review pass on Story 30.1's guard test (test_no_retired_bmad_skill_ids.py, RECONCILED-BY-NAME 2026-08-22 above): 6.12.0 renamed bmad-checkpoint-preview to bmad-walkthrough, so RETIRED_SKILL_IDS gains it as the 21st guarded id (dated comment) plus a dated clarifying comment on bmad-generate-project-context (6.12 ships it as neither a new shim nor a removals entry, not orphaned); new test_catalog_renames_are_guarded + its fixture-based red-on-plant counterpart assert steward's bmad_core_releases/*.yaml skill_renames[].from ids stay a one-directional subset of the guard tuple, plus a planted-fixture test for the new id specifically. Marshal-owned change (spec-bmad-611-era-alignment CAP-8, Epic 30 Story 30.1) riding this spec's surface glob; no CFE rebuild-slice behavior touched.
- (note by claude) 2026-09-06 same-day follow-up (Story 30.1 independent review pass, patch findings): bumped the "20-shim list" header comment above RETIRED_SKILL_IDS to note the 21-guarded-id total, and merged the standalone `test_planted_checkpoint_preview_id_is_detected` into `test_planted_bare_id_is_detected`'s fixture (one scanner-detection test covering old and new guarded ids together, removing duplicate coverage). Same file, same story, no new behavior.
- (event) 2026-09-06 CFE v8.86.3 fleet-hygiene retro (commit `647fda3ed3`, PATCH): split out of Story 30.1's marshal commit per the station CFE-surface guards' "a station story never touches the surface" rule -- landed as its own `retro:`-subject commit touching only `SKILL.md` (version, history), `config/skill-config.yaml` + `MANIFEST.yaml` (8.86.2 -> 8.86.3), `CHANGELOG.md`, and `tests/meta/test_no_retired_bmad_skill_ids.py` (already reconciled above). No shipped script changed; `cfe_rebuild_guard_check.py`'s `brief_mirrored_through` advanced to `647fda3ed3` for both compiled slices (campaign-state.yaml, same commit). Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild`.
- (note by claude) 2026-09-06 marshal Story 30.4 (spec-bmad-611-era-alignment CAP-11, Epic 30) adds `.claude/skills/conda-forge-expert/tests/meta/test_bmad_loop_skills_match_installed.py` -- a regression guard proving the repo's three `bmad-loop-*` skill dirs (`-setup`/`-sweep`/`-resolve`) never silently drift from the installed `bmad_loop` conda package's own `data/skills/` canon (located via `importlib.util.find_spec`, never `importlib.metadata`), with a `tmp_path`-based red-on-plant proof of the diff logic and a clean `pytest.skip` when `bmad_loop` is not importable. Lands under this spec's surface glob (`.claude/skills/conda-forge-expert/**`) by location, not by CFE-skill ownership -- no `SKILL.md`/`CHANGELOG.md` change, no version bump, no CFE rebuild-slice behavior touched. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild`.
- (note by claude) 2026-09-06 same-day follow-up (Story 30.4 independent review pass, patch findings): the per-skill loop now collects a missing installed/repo dir as a diff entry instead of asserting inline (so one skill's missing dir never hides another's already-collected divergence), the failure message names the concrete refresh command (`bmad-loop init --project <root> --cli claude --force-skills`), the module docstring's "verified...before writing this test" line was reworded to point at the checkable unskipped-pass fact instead of an unfalsifiable claim, and a new test exercises `find_installed_package_dir`'s `spec.origin`-parent fallback branch (previously uncovered). Same file, same story, no new external behavior. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild`.
- (event) 2026-09-06 CFE v8.86.4 fleet-hygiene retro (commit `a10a8c7d00`, PATCH): `tests/meta/test_bmad_loop_skills_match_installed.py` (new) + `SKILL.md`/`CHANGELOG.md`/`MANIFEST.yaml`/`config/skill-config.yaml` (8.86.3 -> 8.86.4), landed via pyforge-marshal Story 30.4. No shipped script changed; `cfe_rebuild_guard_check.py`'s `brief_mirrored_through` advanced to `a10a8c7d00` for both compiled slices (campaign-state.yaml, same commit). Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild`.
- (event by claude) CFE skill v8.87.0 (2026-09-06, Mason Story 14.1 retro, commit c4f699a280): bmad-eval-quality re-verified Windows-ready -- build.number 0 -> 1, recipe-build green on linux-64. SKILL.md gains G114 (local_builder.py::build_all_platforms() short-circuits every noarch recipe to its native platform, so a green recipe-build for an if: win/if: unix noarch recipe only ever proves the native leg, never the other-OS branch); CHANGELOG.md, MANIFEST.yaml, config/skill-config.yaml (8.86.4 -> 8.87.0). No shipped script changed; campaign-state.yaml brief_mirrored_through advanced to c4f699a280 for both compiled slices. Scoped-stamped: --write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild.
- (note by claude) 2026-09-06 CFE v8.87.0 follow-through: .claude/skills/conda-forge-expert/config/failure-catalog.yaml regenerated (generate-failure-catalog) so the derived catalog carries G114 (114 rows total). tests/meta/test_failure_catalog_freshness.py guards this.
- (event by claude) CFE v8.87.0 follow-through retro (commit b87a402abd): CHANGELOG.md gains a one-line note that config/failure-catalog.yaml was regenerated for G114 (114 rows); no SKILL.md/version change beyond what v8.87.0 already recorded. campaign-state.yaml brief_mirrored_through advanced to b87a402abd for both compiled slices -- no shipped script changed, nothing to re-port. Scoped-stamped: --write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild.
- (event by claude) 2026-09-06 cfe-rebuild-guard-check follow-up: commit 068b84c673 (the v8.86.5 -> v8.87.0 renumber retro) touched CFE surface + CHANGELOG.md but its campaign-state.yaml mirror was missed in the same PR. campaign-state.yaml brief_mirrored_through advanced to 068b84c673 for both compiled slices -- pure version-number/text renumbering, no new gotcha/script/behavior content, nothing to re-port. Scoped-stamped: --write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild.
- (event by claude) CI/hygiene sweep 2026-09-07, CFE v8.87.0 -> v8.87.1 retro (PATCH): G101's segfault diagnosis corrected (rattler-build's binary relocation, not the vendor Bun binary, corrupted the packaged ccusage binary -- `dynamic_linking: binary_relocation: false` fixes it; recipes/ccusage/recipe.yaml's own dated context comment carries the full byte-diff evidence). Separately, validate_recipe.py's compiler-without-stdlib check gained the go-nocgo/go exemption STD-001 already carried (see spec-packaging-factory's memlog for the full script-change note -- surface overlap, both specs declare the identical .claude/skills/conda-forge-expert/** glob). config/failure-catalog.yaml regenerated (generate-failure-catalog) -- title-sync only for G101, still 114 rows. campaign-state.yaml's brief_mirrored_through re-point for both compiled slices follows in a separate commit once this one's SHA is known (established two-step pattern). RECONCILES: .claude/skills/conda-forge-expert/CHANGELOG.md, .claude/skills/conda-forge-expert/MANIFEST.yaml, .claude/skills/conda-forge-expert/SKILL.md, .claude/skills/conda-forge-expert/config/failure-catalog.yaml, .claude/skills/conda-forge-expert/config/skill-config.yaml, .claude/skills/conda-forge-expert/scripts/validate_recipe.py, .claude/skills/conda-forge-expert/tests/fixtures/recipes/v1-go-nocgo/recipe.yaml, .claude/skills/conda-forge-expert/tests/unit/test_validate_recipe.py.
- (event by claude) 2026-09-07 cfe-rebuild-guard-check follow-up: commit 5fdccf4a36 (the CFE v8.87.0 -> v8.87.1 G101-correction + validate_recipe go-nocgo retro) touched CFE surface + CHANGELOG.md. campaign-state.yaml brief_mirrored_through advanced to 5fdccf4a36 for both compiled slices -- validate_recipe.py (shipped only in cfe-recipe-lifecycle) was byte-re-ported and verified identical before advancing; cfe-recipe-generation has nothing to re-port (it doesn't ship that script).
- (change by claude) 2026-09-09 mirror advance: both briefed slices' `brief_mirrored_through` moved 02b104e573 -> 9427a5ff5c, the G117 CFE Rule-2 retro (v8.89.0 -> v8.90.0, MINOR) closing out the bmad-suite currency effort. G117: `pixi upgrade --pinning-strategy latest-up` silently DELETES deliberate upper bounds -- six caps dropped in one refresh that exited 0 and re-solved cleanly, only one of them machine-caught (asgiref, via pyforge-steward's pin-sync test). DOCS + derived config only -- SKILL.md, CHANGELOG.md, MANIFEST.yaml, skill-config.yaml, failure-catalog.yaml (regenerated, 116 -> 117 rows) and one meta test (test_skill_md_consistency gains station-package suites in `existing`, per that file's own convention that `project_level` is for filenames with NO file in this repo). NOTHING under scripts/ changed, so neither slice needs a byte re-port -- verified, not assumed. `cfe-rebuild-guard-check` clean (22 qualifying retro commits in range); the three unbriefed slices correctly stay null. RECONCILES: _bmad-output/projects/pyforge-mason/planning-artifacts/specs/spec-conda-forge-expert-rebuild/campaign-state.yaml. Scoped-stamped.
- (event by claude) CFE v8.90.0 -> v8.90.1 retro (PATCH), 2026-09-09: G47 gains its THIRD failure mode -- a stale recipe-dir `conda_build_config.yaml` does not merely supply stale values, its `zip_keys` HIJACKS the merge. `.ci_support/build_all.py` runs `conda_build.variants.combine_specs`, which resolves `zip_keys` from the LAST spec that declares it and re-validates EVERY spec against that group, so a pre-2026 pinning snapshot's ['python','numpy','python_impl','is_python_min'] group is imposed on the container's current pinning (numpy 1 entry vs python 4) and the merge raises before any recipe renders -- naming /opt/conda/conda_build_config.yaml, the innocent file, which is why this read for months as an upstream conda-build compat problem rather than a repo defect. Two facts the old G47 text lacked: a CBC at the recipes/ ROOT breaks EVERY recipe (build_all.py loads it ahead of the per-recipe glob and build_steps.sh's TEST_RECIPE prune removes only sibling DIRECTORIES), and `channel_sources` is read from the recipe's CBC, not from conda-forge.yml. New meta test tests/meta/test_recipe_cbc_merges_with_pinning.py replays the real merge over every recipes/*/conda_build_config.yaml and asserts no root-level one exists; A/B-verified (2 failed with two deleted files restored, 33 pass without). config/failure-catalog.yaml regenerated -- title/body sync for G47 only, still 117 rows. DOCS + one meta test + derived config; NOTHING under scripts/ changed, so neither compiled slice needs a byte re-port (verified, not assumed) -- the campaign-state.yaml brief_mirrored_through advance follows in a separate commit once this one's SHA is known (the established two-step). The 15 stale CBC deletions and recipes/bmad-suite/conda_build_config.yaml land in a companion commit under recipes/**, outside this surface. RECONCILES: .claude/skills/conda-forge-expert/CHANGELOG.md, .claude/skills/conda-forge-expert/MANIFEST.yaml, .claude/skills/conda-forge-expert/SKILL.md, .claude/skills/conda-forge-expert/config/failure-catalog.yaml, .claude/skills/conda-forge-expert/config/skill-config.yaml, .claude/skills/conda-forge-expert/tests/meta/test_recipe_cbc_merges_with_pinning.py. RECONCILED BY NAME, not stamped -- naming every changed path in this entry clears the drift under S-13.2's per-file rule, and `pyforge.doctor.sources spec-surface` reports no finding against this spec afterwards, so there is nothing a --write-baseline would add.
- (change by claude) 2026-09-09 mirror advance: both briefed slices' `brief_mirrored_through` moved 9427a5ff5c -> 9b92661d4d, the CFE v8.90.0 -> v8.90.1 retro (PATCH) closing out the Linux-lane fifth-blocker fix. DOCS + one meta test + derived config only -- SKILL.md (G47's third failure mode), CHANGELOG.md, MANIFEST.yaml, skill-config.yaml, failure-catalog.yaml (regenerated, title/body sync for G47, still 117 rows) and tests/meta/test_recipe_cbc_merges_with_pinning.py. NOTHING under scripts/ changed, so neither slice needs a byte re-port -- verified, not assumed. RECONCILES: _bmad-output/projects/pyforge-mason/planning-artifacts/specs/spec-conda-forge-expert-rebuild/campaign-state.yaml.
- (decision by claude) OQ3 (does full scope survive the first slice's measured cost?) -- NO: full scope does not survive the measured cost. Operator ruling 2026-09-09, fleet-readiness-decision-batch-2026-09-09 row mason-B1 / section 2.3 C2. The endgame is declared over slices 1-2 ONLY. The cost is recurring, not one-time: every CFE release is a byte-re-port obligation into two mirrors (23 qualifying retro commits in the campaign range; both re-scope gates returned adjust, never go), for zero behavioural change (campaign.callers empty, both slices still status compiled). Slice 3 (atlas-owned under Story 12.5's ruling, 12.3x slice 1) is NOT briefed; slice 4 is NOT briefed. Callers are cut over to slices 1-2, or the mirrors are retired, by a closing story.
- (decision by claude) OQ2 (can skf-create-skill actually carry ~41K LOC of implementation?) -- NO: Skill Forge compiles the KNOWLEDGE layer; the code is mirrored byte-for-byte, not generated. Operator ruling 2026-09-09, fleet-readiness-decision-batch-2026-09-09 row mason-B2 / section 2.1. Evidence: slice 1 -- all 5 scripts sha256-identical to live (campaign-state.yaml:524-527); slice 2 -- 25 scripts sha256-verified byte-identical to source (:638-642); every drift event was closed by porting live source into the copy, never by re-compiling (:633). Consequence the operator weighed: the Spec's headline Skill-Forge-authored is not what shipped -- what shipped is a Forge-authored brief plus audit harness around an unmodified code copy. This is the strongest single input to OQ3.
- (decision by claude) OQ4 (deprecation posture for the long tail) -- DELETE-ON-CUTOVER, no stubs, as the campaign-wide default. Operator ruling 2026-09-09, fleet-readiness-decision-batch-2026-09-09 row mason-B3 / section 2.3 C2. The kinder-to-out-of-repo-callers premise does not hold: Mason reaches CFE by FILESYSTEM PATH, not by import -- resolve.py's chain is flag, then MASON_CFE_ROOT, then a cwd walk, and mason doctor reports cfe_root_step cwd-walk -- so a Python stub module cannot serve a path resolver; an out-of-repo Mason gets a missing root, which cfe.py's CAP-5 degradation path already handles (mason doctor exits 0 with unavailable_verbs recipe). Guard clause (c) becomes trivially satisfiable instead of needing marker machinery, and steward S-44.10 archives this repo anyway.
- (constraint by claude) Rule-2 retros land in the LEGACY skill ONLY, and dual-land in the same PR (settled 2026-09-09 from practice, ratifying OQ5; fleet-readiness-decision-batch-2026-09-09 row mason-B4 / section 2.1). A retro edits .claude/skills/conda-forge-expert/**; any mirrored script it touches is byte-re-ported into the affected compiled slice and that slice's brief_mirrored_through is advanced, both in the same PR. Precedent: CFE v8.88.1 (pr_artifacts.py re-ported, cmp clean) and commit 4b04cce30e. CAP-3 clause (b) is the machine enforcement of this rule. On the next re-derive this MOVES OUT of Open Questions and INTO Constraints -- it is a decision, not a question.
- (event by claude) campaign endgame declared 2026-09-09; closing story minted (mason Epic 15, Story 15.1). Spec status stays in-progress until that story lands -- the flip to a closing status is a separate memlog event once the cut-over-or-retire work merges. campaign-state.yaml campaign.endgame_declared flipped false to true with a dated reason and the operator citation, and campaign.next_story was rewritten to the endgame. campaign.re_scope_gate_2's two open pre-conditions are now MOOT (they gate a slice-3/4 briefing that will never happen) and were deliberately left unedited rather than re-worded. Cite: fleet-readiness-decision-batch-2026-09-09 section 2.3 C2.
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. CAP-1..CAP-4 preserved, none retired; status stays in-progress per the endgame entry; updated: 2026-08-28 -> 2026-09-09; OQ2/OQ3/OQ4 recorded as dated Resolved blocks under their original question text (nothing deleted), OQ5 struck through and moved into Constraints as the legacy-only dual-land rule; new closing section 'Endgame declared - 2026-09-09' records slices 1-2 only, Epic 15 Story 15.1, the campaign-state flip, the moot re_scope_gate_2 pre-conditions left unedited, and the close-before-S-44.6 sequencing. campaign-state.yaml not touched by this derive.
- (event by claude) CAMPAIGN CLOSED 2026-09-10 (mason Story 15.1, the closing story minted by the 2026-09-09 endgame declaration). Disposition: RETIRE BOTH compiled mirrors, not cut over (OQ4) -- `.claude/skills/cfe-recipe-generation/` and `.claude/skills/cfe-recipe-lifecycle/` deleted outright (whole tree, incl. the `active` marker), no dated stub. Rationale: Mason's sole caller (`cfe.py`) resolves the CFE root via `resolve.py::resolve_cfe_root`, whose walk hard-codes the marker `.claude/scripts/conda-forge-expert` and joins `_CFE_SCRIPTS[key]` under it -- Mason never touches either compiled mirror's directory today. Cutting over would mean rearchitecting that filesystem-marker contract to additionally resolve two of ten `_CFE_SCRIPTS` entries into a different directory shape -- a real, non-trivial Mason architectural change (AD-3/AD-5) for zero behavioral gain, since both mirrors are byte-identical to what Mason already invokes via the wrapper today. `campaign-state.yaml`: both `slice-1-recipe-generation` and `slice-2-recipe-lifecycle` set `status: "retired"` (dated comment naming this story), `brief_path`/`brief_mirrored_through` nulled with the prior values preserved in adjacent comments (this permanently silences guard clause (b) for both slices); `equivalence` fields left untouched as historical record; `campaign.current_focus` -> `"campaign-closed"`, `campaign.next_story` rewritten to record the closure, and a new dated closure note added under `campaign` (after `callers`) citing this story; `campaign.callers` stays `[]` (already trivially satisfies clause (c) -- retiring needs no caller-resolution entries). Deleted the now-pointless equivalence-harness tests `tests/integration/test_slice1_equivalence.py` and `test_slice2_equivalence.py` (their sole purpose was proving the now-deleted mirrors stayed byte-identical). Updated `.github/workflows/cfe-regression-net.yml`: removed `.claude/skills/cfe-recipe-generation/**` from both `pull_request.paths` and `push.paths`, renamed the workflow to "CFE regression net", and rewrote the header comment to describe its one remaining job (CI coverage for the legacy CFE skill's own regression suite, `test_cve_manager.py`'s `@pytest.mark.slow` still needing `test-ci`'s `not network` selection) rather than a divergence guard that no longer applies; the workflow and its `test-ci` pixi task are NOT deleted. Reworded `pixi.toml`'s `test-ci` task description and preceding comment to stop naming the retired slice-equivalence harness. Confirmed `scripts/spec_surface_allowlist.txt` already carries no `failure_catalog_check.py` entry (removed on `main` by commit `2a63b00c27`, pre-existing, not touched by this story). CFE Rule-2 retro (PATCH, no new gotcha): `CHANGELOG.md`, `SKILL.md` (frontmatter version + Version History bullet), `MANIFEST.yaml`, `config/skill-config.yaml` all bumped `8.90.1` -> `8.90.2`, recording this campaign-closure PATCH. `python3 scripts/cfe_rebuild_guard_check.py` runs clean after all edits. Files touched by this story: `.claude/skills/cfe-recipe-generation/**` (deleted), `.claude/skills/cfe-recipe-lifecycle/**` (deleted), `.claude/skills/conda-forge-expert/tests/integration/test_slice1_equivalence.py` (deleted), `.claude/skills/conda-forge-expert/tests/integration/test_slice2_equivalence.py` (deleted), `.claude/skills/conda-forge-expert/CHANGELOG.md`, `.claude/skills/conda-forge-expert/SKILL.md`, `.claude/skills/conda-forge-expert/MANIFEST.yaml`, `.claude/skills/conda-forge-expert/config/skill-config.yaml`, `.github/workflows/cfe-regression-net.yml`, `pixi.toml`, `campaign-state.yaml`, this `.memlog.md`, and `SPEC.md` (next). `SPEC.md`'s `status:` flips `in-progress` -> `shipped` in the edit immediately following this entry, per this file's own convention of appending the memlog event before the SPEC.md edit.
- (note by claude) RECOVERY CORRECTION 2026-09-12: the entry above describes a 2026-09-10 dispatch attempt whose changes never reached `main` -- verify failed on `python scripts/spec_surface_reconcile.py -core` (a repo-wide gate, unrelated to this story's own diff; 68 pre-existing findings across other stations' specs), and the loop preserved the diff as `changes.patch` under the dead run's `failed/` directory rather than landing it. Recovered 2026-09-12 after that gate was reconciled fleet-wide (see `pyforge-marshal/spec-pyforge-core`'s own memlog, same date). The prior entry's version bump text ("8.90.1 -> 8.90.2") is stale: by 2026-09-12, mason Stories 15.2/16.1/16.3 had already independently claimed `v8.90.2`/`v8.90.3`/`v8.90.4` from the same `v8.90.1` base while this patch sat unrecovered, so the recovered CFE retro landed as **`v8.90.5`** instead (CHANGELOG.md + SKILL.md Version History carry the renumbering note). `campaign-state.yaml`'s `brief_path`/`brief_mirrored_through` nulling hunks did not apply cleanly against current `main` (the file had moved on -- `brief_mirrored_through` had advanced past the patch's recorded value via an unrelated 2026-09-11 follow-up retro) and were manually re-applied preserving the then-current values in the same "was:" comment style. Everything else in this story's diff (deletions, `campaign.status`/`current_focus`/`next_story`, workflow/pixi.toml rewording) applied unchanged from the original 2026-09-10 patch.
- (event by claude) Surface reconcile 2026-09-14: commit f180624fd8 (Story 44.7, 'Wire mason to the foundry factory island') changed .claude/scripts/conda-forge-expert/native-build.sh to read MASON_FACTORY_ROOT from its environment and resolve ISLAND_ROOT from it when set, falling back to its prior cwd-walk behavior otherwise -- an additive, backward-compatible env-var opt-in, not a rewrite of the script's CFE-facing contract this Spec governs. Already landed and covered by mason's own tests/unit/test_factory_island.py. Scoped-stamped: --write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild.
- (event) 2026-09-17 absorbed into spec-pyforge-mason (one-chain mason fold). Pointer + memlog + companions retained.
- (event) 2026-09-17 pointer keep: restored `surface-drift-exclude` for the five MCP/parity files also governed by packaging-factory.
- (event by claude) Surface reconcile 2026-09-18 (steward Story 59.2's own verify red on this repo-wide gate, unrelated to that story's own diff -- reconciled here by name per this Spec's own established precedent for this exact failure class, see the 2026-09-12 RECOVERY CORRECTION entry above). Commit `3aadb22e32` ("Prefer marshal factory dispatch and point fleet-picture at marshal watch") changed `.claude/skills/conda-forge-expert/tests/meta/test_fleet_picture_awaiting_operator.py`: stopped/idle ATTENTION remedies and the state column now steer operators to `bmad-build-auto` dispatch and `marshal watch --fleet` instead of `factory spin`, and the fixture assertions were updated to match. Already landed on `main`, unrelated to this story's own diff. Scoped-stamped: --write-baseline --spec pyforge-mason/spec-conda-forge-expert-rebuild.
- (event) Surface reconcile 2026-09-25: .claude/skills/conda-forge-expert/scripts/github_version_checker.py, .claude/skills/conda-forge-expert/tests/unit/test_github_version_checker.py — CFE v8.90.6 (Rule-2 retro of the bmad-suite advance): extract_github_repo now reads the v1 about.repository / about.homepage (+ dev_url) beside the v0 about.home it read alone, so a v1 recipe with a non-GitHub source URL (npm tarball recipes/bmad-module-skill-forge) is auto-detected; four unit cases added. No slice mirror to re-port — both Skill-Forge mirrors were retired in v8.90.5. No CAP change.
