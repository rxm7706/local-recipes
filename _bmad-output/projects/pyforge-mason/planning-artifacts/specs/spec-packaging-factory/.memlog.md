---
topic: Packaging factory (CFE skill + recipes machinery): brownfield backfill kernel (regenerable-factory Wave 4, Rule 1 invoked)
updated: 2026-09-25T19:24
---

- (decision) The CFE skill's own docs (SKILL.md v8.79.0 + reference/ + guides/ + CHANGELOG) ARE the living architecture documentation, maintained by Rule-2 retros + the bmad sync loop - adopt as companions, do not duplicate (bmad-document-project would re-document what the retro loop already grounds)
- (decision) Drift discipline: skill-surface changes are contractual when CHANGELOG moves (Rule 2 mandates it) - spec declares surface-drift: sentinel:.claude/skills/conda-forge-expert/CHANGELOG.md so a governed edit without a CHANGELOG move reds the checker
- (capability) CAP-1 recipe lifecycle machinery (10-step loop, generators, validators, build, submit); CAP-2 atlas intelligence (cf_atlas.db, 15 phases, 17 CLIs, offline-safe reads); CAP-3 MCP surface (conda_forge_server.py, 30+ tools); CAP-4 self-improvement loop (Rule 2 retros land as skill edits + CHANGELOG semver)
- (event) Self-validate 1+2 PASS + sentinel live-tested: v8.79.1 retro edit moved SKILL.md+CHANGELOG together -> spec_surface_check stayed green with NO restamp (CAP-4 proven); bmad-drift-check caught the version pin and was re-stamped per runbook
- (direction) Terminology (2026-07-25, Charter amendment): this artifact is 'the Spec' — retire 'kernel' (bmad-spec tool-jargon) on next re-derive. Also: the eight are 'Smiths', not 'Forgemasters'.
- (decision) Meta-test test_skill_md_consistency.py: repo-root scripts/*.py now count as 'existing' rather than needing an entry in the project_level allowlist — that allowlist is for EXTERNAL/illustrative filenames with no file in this repo, so putting a real governance script (spec_surface_check.py) there mislabels it. Same edit derived PROJECT_ROOT from SKILL_DIR.parents[2], removing a machine-absolute path that made the suite unrunnable in any other clone (AGENTS.md portability contract). Recurrence of the known 'new CFE script touches three places' trap.
- (event) Pre-existing red in the meta suite cleared (2026-07-25): recipes/boring-semantic-layer/recipe.yaml was missing the mandatory yaml-language-server schema-header line (SCHEMA-001 / repo convention); header restored. Meta suite 1013 passed / 0 failed / 2 skipped.
- (change) 2026-08-08 — new meta-test under this surface: `.claude/skills/conda-forge-expert/tests/meta/test_forward_dependency_check.py` (26 cases). It pins `forward-dependency-check`'s coverage honesty after two defects were found in the detector's own bookkeeping: atlas's alias-first headings were invisible (0 of 46 stories parsed) and mason reported MEASURED-and-clean with **0 of 30 dependency declarations machine-readable**. The tests assert the canonical heading resolves, that the retired alias shape is **no longer accepted** (deliberately — re-adding it would reinstate the fourth private accommodation EXEMPLAR-STANDARD INV-5 forbids), that an explicit `—`/`none (…)` reads as no-dependency, that the last stdout line carries the coverage breakdown (`scripts/detectors.py` lifts `tail[-1][:200]` as the registry summary), and that marshal's three original forward deps (2-3→3, 2-7→4, 8-5→10) still detect. Mason's own Deps declarations remain prose and its station therefore reports `partial`, not `measured` — the 23-declaration migration is tracked separately and is hand-authored, because token substitution inverts prose negations.
- (change) RECONCILES .claude/skills/conda-forge-expert/tests/meta/test_spec_surface_check.py (2026-08-09). Marshal's S-13.1/S-13.2 added six tests for the REPO-LEVEL spec-surface detector; the file lives in the CFE meta-test tree (beside the other detector meta-tests) and so falls inside this Spec's surface, but the change is Marshal Epic 13 work, not CFE skill guidance. Deliberately NO CFE CHANGELOG entry and NO version bump: the sentinel exists to make Rule-2 skill changes visible, and stamping a release for a detector test would misreport what shipped. Naming the path here reconciles it under the per-file rule S-13.2 introduces. No other file in this surface is touched or reconciled by this entry.
- (change) RECONCILES .claude/skills/conda-forge-expert/tests/meta/test_actionable_statuses_conformance.py (2026-08-09, ADDED). Same shape as the entry above, and handled the same way: Doctor's Story 6.7 / AD-13 needs a conformance test that compares Doctor's restated `ACTIONABLE_STATUSES` against the INSTALLED `bmad_loop.sprintstatus`, and this meta-test tree is the only suite that runs in an environment carrying `bmad-loop` (`pixi run -e local-recipes test`). It therefore lands inside this Spec's surface while being Doctor Epic 6 work, not CFE skill guidance. Deliberately NO CFE CHANGELOG entry and NO version bump — the sentinel exists to make Rule-2 skill changes visible, and stamping a release for a cross-station conformance test would misreport what shipped. No other file in this surface is touched or reconciled by this entry.
- (change) RECONCILES .claude/skills/conda-forge-expert/tests/fixtures/error_logs/unmatched.log (2026-08-09, ADDED) — a genuine CFE fix, unlike the two entries above. `test_failure_analyzer.py`'s two unmatched-log cases had NEVER run against their fixture: `.gitignore`'s VisualStudio template block contributes a blanket `*.log`, so `tests/fixtures/error_logs/` sat empty and the file could not be added. Both tests were asserting against `"Log file not found: …"` rather than against the no-match contract they were written for, and one of them passed anyway because it only requires valid JSON. Fixed by NARROWLY negating the rule for this fixture tree (never by weakening `*.log`) and authoring the fixture. This is the third instance this year of the same failure class recorded in this Spec's sibling memlog — a broad ignore rule hides only NEW files, so `git status` can never show what it is costing.
- (note) The fixture carries an explicit instruction in its own body: if a future ERROR_LIBRARY pattern ever starts matching it, make the fixture blander rather than relaxing the assertion — a fixture that quietly starts matching turns both tests green while testing nothing.
- (change) RECONCILES .claude/skills/conda-forge-expert/tests/meta/test_bmad_artifacts_in_sync.py, .claude/skills/conda-forge-expert/tests/meta/test_spec_surface_check.py and .claude/skills/conda-forge-expert/tests/meta/test_skill_md_consistency.py (2026-08-09, CHANGED, Doctor Story 6-9). Not CFE skill guidance: `scripts/bmad_drift_check.py` and `scripts/spec_surface_check.py` retired into `pyforge.doctor.sources` (Stories 6.6/6.8), so both meta-tests are re-targeted from subprocess-running the deleted scripts to calling `sources.factory.gather`/`sources.chain.gather_spec_surface` directly. `test_skill_md_consistency.py`'s file-existence allowlist gained one entry (`spec_surface_check.py`) so its historical v8.79.1 Version History citation of the now-deleted file still passes. Same "lands here by location, not by ownership" shape as this Spec's own `test_actionable_statuses_conformance.py` entry above. No CFE CHANGELOG entry, no version bump. No other file in this surface is touched by this entry.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Surface reconcile 2026-08-31: 51 drift-presumed paths since baseline at `1172dc17ab56`. Two-hundred-twenty-two commits spanning the full factory surface — CFE skill evolution, mason Epic 5-12, doctor cross-station detectors, packaging inventory, and infra churn (all done in ledger). Verified via git log sampling per cluster rather than per-file (same disposition as marshal spec-pyforge-marshal 2026-08-29 precedent):
  - CFE skill + scripts: Waves A-E cyclonedx-universe-inventory, seed-gap suggesters (v8.74-8.76), v8.83 Rule-2 retro, audit remediation (`864558e02d3`), MCP >2.0 (`06fe64f6037`), marshal 25.1 retired-skill-ID guard, doctor 6.9 meta-test retargeting.
  - Mason Epic 5-7: CFE independence/delegation tests, failure-catalog generation (7.1), packaging inventory operations (6.3), Rule-2 retrospective (5.5).
  - Mason Epic 10-12: drift surfaces ambiently (doctor 10.3), conda-forge packaging inventory reconciliation, mason 12.7 compiled slice.
  - Cross-station: steward Guildhall deletion (30.2), marshal 23.1-23.3 dashboard timing, atlas bootstrap chain fix (`78ccbe4468a`), doctor dream-chain/fleet-picture gates.
  No CAP text change — organic factory evolution within contracted packaging-factory surface. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-packaging-factory`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): this spec's memlog HAD moved for the same pass, but did not NAME the 16 governed path(s) below, so spec-surface reported them `drift-presumed` (warn, non-gating). Naming them here closes that. All already landed on `main`; this records the baseline lag, not a re-review. No `SPEC.md` edit. Paths:
  - `.claude/scripts/conda-forge-expert/bmad_suite_metapackage.py` (added)
  - `.claude/scripts/conda-forge-expert/build_bmad_suite.py` (added)
  - `.claude/skills/conda-forge-expert/MANIFEST.yaml` (changed)
  - `.claude/skills/conda-forge-expert/SKILL.md` (changed)
  - `.claude/skills/conda-forge-expert/config/skill-config.yaml` (changed)
  - `.claude/skills/conda-forge-expert/scripts/bmad_suite_metapackage.py` (added)
  - `.claude/skills/conda-forge-expert/scripts/build_bmad_suite.py` (added)
  - `.claude/skills/conda-forge-expert/scripts/recipe-generator.py` (changed)
  - `.claude/skills/conda-forge-expert/scripts/recipe_optimizer.py` (changed)
  - `.claude/skills/conda-forge-expert/tests/meta/test_all_scripts_runnable.py` (changed)
  - `.claude/skills/conda-forge-expert/tests/meta/test_dashboard_renders.py` (removed)
  - `.claude/skills/conda-forge-expert/tests/meta/test_fleet_picture_awaiting_operator.py` (changed)
  - `.claude/skills/conda-forge-expert/tests/meta/test_fleet_picture_dispatch_phase.py` (added)
  - `.claude/skills/conda-forge-expert/tests/unit/test_bmad_suite_metapackage_rewrite.py` (added)
  - `.claude/skills/conda-forge-expert/tests/unit/test_recipe_optimizer.py` (changed)
  - `.claude/tools/conda_forge_server.py` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-packaging-factory`.
- (note by claude) 2026-09-05 drift-presumed reconciliation (branch maint/marshal-docs-reground-2026-09-05): the spec-surface detector reported these governed paths as changed/added while this memlog moved without naming them. Verified each against `git log -1` — every path was landed by a story commit on main within this spec's declared surface; none is an out-of-band edit. Named here so the presumption is discharged; scoped --write-baseline follows. Paths by landing commit — ecfb778a67 (2026-09-03, langlow cleanup): .claude/skills/conda-forge-expert/reference/conda-forge-yml-reference.md [changed] ; 1da5fd0213 (2026-09-04, retro: CFE v8.85.2 — relocation-aware repo root; both compiled sli): .claude/skills/conda-forge-expert/scripts/_path_guard.py [changed], .claude/skills/conda-forge-expert/scripts/_paths.py [changed], .claude/skills/conda-forge-expert/scripts/gen_yml_reference.py [changed], .claude/skills/conda-forge-expert/scripts/mapping_manager.py [changed], .claude/skills/conda-forge-expert/scripts/vulnerability_scanner.py [changed] ; 4825cc426b (2026-09-05, cfe: v8.86.0 Rule-2 retro -- G111-G113, github_updater --head increm): .claude/skills/conda-forge-expert/config/failure-catalog.yaml [changed], .claude/skills/conda-forge-expert/quickref/commands-cheatsheet.md [changed], .claude/skills/conda-forge-expert/scripts/github_updater.py [changed], .claude/skills/conda-forge-expert/tests/unit/test_github_updater.py [changed]
- (note by claude) 2026-09-05 hygiene-2 PR (branch maint/hygiene-2-2026-09-05): drift-presumed reconciliation: .claude/skills/conda-forge-expert/tests/meta/test_recipe_yaml_parse_audit.py changed (CFE v8.86.2: test_no_duplicate_mapping_keys_anywhere, the strict duplicate-key audit that found 31 half-migrated recipes) — within this spec's surface, landed by the CFE retro commit a5f63c95fb. Scoped --write-baseline follows.
- (note by claude) 2026-09-06: marshal Story 30.1 (spec-bmad-611-era-alignment CAP-8, Epic 30) edited .claude/skills/conda-forge-expert/tests/meta/test_no_retired_bmad_skill_ids.py -- adds bmad-checkpoint-preview as the 21st RETIRED_SKILL_IDS entry (6.12.0 renamed it to bmad-walkthrough) with a dated comment, a dated clarifying comment on the pre-existing bmad-generate-project-context entry (6.12 ships it as neither a new shim nor a removals entry, not orphaned), and new catalog-consistency tests (test_catalog_renames_are_guarded + its fixture-based red-on-plant counterpart) asserting steward's bmad_core_releases/*.yaml skill_renames[].from ids stay a one-directional subset of the guard tuple, plus a planted-fixture test for the new id. Lands under this spec's surface glob (.claude/skills/conda-forge-expert/**) by location, not by CFE-skill ownership -- no SKILL.md/CHANGELOG change, no version bump. No other file in this surface is touched by this entry.
- (note by claude) 2026-09-06 same-day follow-up (Story 30.1 independent review pass, patch findings): bumped the "20-shim list" header comment to note the 21-guarded-id total, and merged the standalone `test_planted_checkpoint_preview_id_is_detected` into `test_planted_bare_id_is_detected`'s fixture. Same file, same story, no new behavior.
- (note by claude) 2026-09-06 marshal Story 30.4 (spec-bmad-611-era-alignment CAP-11, Epic 30) adds `.claude/skills/conda-forge-expert/tests/meta/test_bmad_loop_skills_match_installed.py` -- a regression guard proving the repo's three `bmad-loop-*` skill dirs (`-setup`/`-sweep`/`-resolve`) never silently drift from the installed `bmad_loop` conda package's own `data/skills/` canon (located via `importlib.util.find_spec`, never `importlib.metadata`), with a `tmp_path`-based red-on-plant proof of the diff logic and a clean `pytest.skip` when `bmad_loop` is not importable. Lands under this spec's surface glob (`.claude/skills/conda-forge-expert/**`) by location, not by CFE-skill ownership -- no `SKILL.md`/`CHANGELOG.md` change, no version bump from this story.
- (note by claude) 2026-09-06 same-day correction: the write-baseline this story ran also re-stamped `CHANGELOG.md`/`MANIFEST.yaml`/`SKILL.md`/`config/skill-config.yaml` under THIS spec's key -- those four were stale leftovers from the CFE v8.86.3 fleet-hygiene retro (commit `647fda3ed3`), which Story 30.1's cleanup only scoped-stamped against the sibling `spec-conda-forge-expert-rebuild` and missed this spec (both declare the identical `.claude/skills/conda-forge-expert/**` surface glob). The prior entry's "no other file in this surface is touched" line was wrong for that reason; corrected here rather than edited in place, per this memlog's append-only convention. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-packaging-factory`.
- (note by claude) 2026-09-06 same-day follow-up (Story 30.4 independent review pass, patch findings on `test_bmad_loop_skills_match_installed.py`): soft-collect a missing skill dir as a diff entry instead of asserting inline, name the concrete refresh command in the failure message, reword an unfalsifiable docstring claim, and add a test for the `spec.origin`-parent fallback branch. No new external behavior. Scoped-stamped: `--write-baseline --spec pyforge-mason/spec-packaging-factory`.
- (change by claude) CI/hygiene sweep 2026-09-07, CFE retro v8.87.0 -> v8.87.1 (PATCH): scripts/validate_recipe.py's compiler-without-stdlib check (v1 structured path + v0 meta.yaml text-scan fallback) gains the go-nocgo/go exemption recipe_optimizer.py's STD-001 already carried -- a pure-Go recipe using only compiler("go-nocgo") false-positived as "Missing stdlib('c')". New fixture tests/fixtures/recipes/v1-go-nocgo + test_v1_go_nocgo_passes_without_stdlib cover the v1 path. Byte-mirrored into the cfe-recipe-lifecycle compiled slice (validate_recipe.py ships in that slice, not cfe-recipe-generation). RECONCILES: .claude/skills/conda-forge-expert/scripts/validate_recipe.py, .claude/skills/conda-forge-expert/tests/unit/test_validate_recipe.py, .claude/skills/conda-forge-expert/tests/fixtures/recipes/v1-go-nocgo/recipe.yaml.
- (change by claude) CI/hygiene sweep 2026-09-07: DW-12-2-1/DW-12-2-3 fixed directly (not via mason -- the entry's own evidence notes mason is barred from this surface by mason-cfe-surface-check). `test_github_updater_gap_closed` (test_slice1_equivalence.py) makes a real live GitHub API call but was marked only `@pytest.mark.slow`, so `test-ci`'s `-m 'not network'` selection didn't exclude it -- the exact live mechanism behind cfe-regression-net's own same-day "one failure out of 9096" report. Added `@pytest.mark.network` alongside the existing `@pytest.mark.slow`; verified via `--collect-only` that `-m "not network"` now deselects it and `-m "network"` selects only it. RECONCILES: .claude/skills/conda-forge-expert/tests/integration/test_slice1_equivalence.py.
- (change by claude) 2026-09-09, Linux-lane fifth-blocker fix (CFE retro v8.90.0 -> v8.90.1, PATCH): new meta test `tests/meta/test_recipe_cbc_merges_with_pinning.py` replays `.ci_support/build_all.py`'s own `conda_build.variants.combine_specs` merge over every `recipes/*/conda_build_config.yaml` and asserts no root-level `recipes/conda_build_config.yaml` exists -- a stale pinning snapshot's `zip_keys` is imposed on the container's CURRENT pinning and the merge raises before any recipe renders (G47's new third failure mode). Recorded here because this surface declares the same `.claude/skills/conda-forge-expert/**` glob as spec-conda-forge-expert-rebuild, which carries the full retro note; nothing under scripts/ changed, so neither compiled slice needs a re-port. RECONCILES: .claude/skills/conda-forge-expert/tests/meta/test_recipe_cbc_merges_with_pinning.py. Not stamped -- naming the path is the reconciliation, and the two `pr_artifacts.py` drift-presumed warns already standing against this spec are another effort's to clear, not this one's to absorb into a baseline.
- (note by claude) Currency re-read 2026-09-09 (fleet-readiness-decision-batch-2026-09-09, Table A row packaging-factory; Rule 1 -- conda-forge-expert was invoked to read the skill tree). Status stays shipped and the Dream stays realized; what moved is the FACTS both quote. Verified live against .claude/skills/conda-forge-expert/: SKILL.md frontmatter version is 8.90.1 (five releases landed on 2026-09-09 alone), the gotcha corpus is G1-G117 (117 sections), and .claude/tools/conda_forge_server.py registers 46 MCP tools. The Dream body was eleven minor versions stale (v8.79.x, about 90 gotchas, 30-plus tools) and has been corrected in place. This Spec's own companions block still annotates SKILL.md with (v8.79.0) at SPEC.md:12 -- update that parenthetical to (v8.90.1) on the re-derive. NOTE for whoever maintains it: a stamped literal version in a companion comment is a standing drift source against a skill that ships several releases a day; the same problem is recorded fleet-wide as decision-batch D6 (propose a DERIVED pin that reads SKILL.md:10 rather than a stamped literal). No contract change.
- (note by claude) Cross-station trigger, 2026-09-09 (decision-batch D11 / mason-E1): steward Epic 44 carries TWO Mason capabilities with no mason counterpart -- fnd:CAP-3 -> S-44.6 (CFE comes home; moves the whole CFE cell to skills/domain/conda-forge-expert/) and fnd:CAP-6 -> S-44.9 (Mason submits to conda-forge; marked outward, dispatched only on explicit operator confirmation). Both read blocked in steward's ledger and both carry CLAUDE.md Rule 1 / Rule 2 (Mason) in their own AC lines, so they are Mason work carried on steward's chain. A Kinship line pointing at them was added to docs/dreams/packaging-factory.md so the practice Dream names them rather than leaving mason's decomposition silently incomplete against the greenfield spine. Sequencing that matters for this Spec's surface: the CFE-rebuild campaign closes BEFORE 44.6 (see spec-conda-forge-expert-rebuild's 2026-09-09 endgame entries), otherwise 44.6 has to re-point a live campaign's surface and slice map as well as move the cell.
- (gap by claude) CLI-tool parity is UNGATED for this Spec's MCP surface (2026-09-09; marshal C10 routing, fleet-readiness-decision-batch-2026-09-09 section 3 routing table). Surface ownership checked before filing: .claude/tools/conda_forge_server.py is declared by TWO mason Specs -- this one and spec-conda-forge-expert-rebuild -- and it is filed HERE because the parity gate is standing factory machinery, not campaign work, and the rebuild Spec is closing (endgame declared the same day). The gap: parity between a station's CLI verbs and its MCP tools is a GATED NUMBER for marshal only -- src/shared/packages/pyforge-marshal/tests/meta/test_cli_tool_parity.py drives pyforge.marshal.mcp.parity (assert_cli_tool_parity, discover_cli_verbs, parity_findings, tools_by_cli_verb, with explicit CLI_ONLY_VERBS / TOOL_ONLY_NAMES allowlists) against live TOOL_SPECS, per FR-155 / Story 18.2 -- while this Spec's 46 @mcp.tool registrations have NO parity gate at all, so a CLI wrapper can gain or lose a verb with no tool counterpart and nothing reds. Vessel: mason Story 16.3, minted 2026-09-09. Design constraint carried into its ACs: EXTEND marshal's primitive, do not fork it -- a second independent copy of a gate is exactly how the _http.py JFrog credential leak survived a landing that called itself durable, a lesson this skill's own SKILL.md records. Atlas's half of the same routing (pyforge/atlas/mcp/tools.py) is ATLAS's to mint; mason's story touches no atlas file.
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. CAP-1..CAP-4, surface, surface-drift sentinel and both companions preserved; status stays shipped; the SKILL.md companion parenthetical moved (v8.79.0) -> (v8.90.1); three dated sections added — Currency re-read (8.90.1 / G1-G117 / 46 MCP tools, no contract change, D6 derived-pin hazard), the ungated CLI-tool-parity gap with vessel Story 16.3 and the extend-marshal's-primitive constraint, and the steward-Epic-44 cross-station trigger with the close-campaign-before-44.6 sequencing.
- (note by claude) Mechanical verified: sweep 2026-09-11, drift-presumed reconciliation: the spec-surface detector reported 7 governed paths as changed/added while this memlog moved without naming them. Verified each against git log -1 -- all landed on main by legitimate story commits within this spec's declared surface; none out-of-band. Reconciled -- c1cb0db042 (2026-09-11, Story 16.3 FR-155 parity gate): .claude/skills/conda-forge-expert/scripts/mcp_parity.py [added], .claude/skills/conda-forge-expert/scripts/mcp_tools.py [added], .claude/skills/conda-forge-expert/tests/meta/test_cli_tool_parity.py [added] ; 2665609371 (2026-09-09, v8.88.1 pr-artifacts noarch fix): .claude/skills/conda-forge-expert/scripts/pr_artifacts.py [changed], .claude/skills/conda-forge-expert/tests/unit/test_pr_artifacts_resolver.py [changed] -- clears the two pr_artifacts.py warns the 2026-09-09 entry deliberately deferred ; 2bb7bc58f5 (2026-09-10, Story 21.11): .claude/skills/conda-forge-expert/tests/meta/test_fleet_picture_capability_effect.py [added]. Also fixed two real drift bugs the CAP-1 meta-suite re-run surfaced: test_skill_md_consistency.py's existing-scripts check only recognised station tests/, not station src/ modules, so SKILL.md's v8.90.3 entry citing pyforge-mason's own cfe.py false-failed (added the analogous src/**/*.py glob); test_all_scripts_runnable.py's SCRIPTS/NO_HELP lists never gained mcp_tools.py/mcp_parity.py from Story 16.3 (added, both are pure library modules with no __main__). Closed a genuine CAP-3 doc-drift gap: reference/mcp-tools.md documented only 36 of the 46 live @mcp.tool registrations (channel_split, enrich_from_feedstock, env_inspect, get_feedstock_context, lookup_feedstock, platform_breakdown, prepare_submission_branch, pypi_intelligence, pypi_only_candidates, pyver_breakdown were undocumented) -- added all 10 with docstring-sourced descriptions and corrected the stale "30+ tools" intro line to "46+ tools". CAP-2 note: SKILL.md's own "Atlas Intelligence Layer (v8.1.0)" header text ("15 pipeline phases (B -> N) and exposed through 17 CLIs + 12 MCP tools") is stale against live bmad-groundtruth (atlas_phases=22, mcp_tools=46) -- NOT fixed here (a full phase-list/CLI-count re-audit is Rule-2 retro-sized work, not a mechanical verified: sweep); flagged for a future CFE retro. RECONCILES: .claude/skills/conda-forge-expert/reference/mcp-tools.md, .claude/skills/conda-forge-expert/tests/meta/test_all_scripts_runnable.py, .claude/skills/conda-forge-expert/tests/meta/test_skill_md_consistency.py. Scoped-stamped: --write-baseline --spec pyforge-mason/spec-packaging-factory.
- (event by claude) Surface reconcile 2026-09-14: commit f180624fd8 (Story 44.7, 'Wire mason to the foundry factory island') changed .claude/scripts/conda-forge-expert/native-build.sh to read MASON_FACTORY_ROOT from its environment and resolve ISLAND_ROOT from it when set, falling back to its prior cwd-walk behavior otherwise -- an additive, backward-compatible env-var opt-in, not a rewrite of the script's packaging-factory contract this Spec governs. Already landed and covered by mason's own tests/unit/test_factory_island.py. Scoped-stamped: --write-baseline --spec pyforge-mason/spec-packaging-factory.
- (event) 2026-09-17 absorbed into spec-pyforge-mason (one-chain mason fold). Pointer + memlog + companions retained.
- (event) 2026-09-17 pointer keep: restored `surface-drift: sentinel:CHANGELOG.md` and `surface-drift-exclude` (conftest).
- (event by claude) Surface reconcile 2026-09-18 (steward Story 59.2's own verify red on this repo-wide gate, unrelated to that story's own diff; co-governor of the same path already reconciled on spec-conda-forge-expert-rebuild's own memlog, same date). Commit `3aadb22e32` ("Prefer marshal factory dispatch and point fleet-picture at marshal watch") changed `.claude/skills/conda-forge-expert/tests/meta/test_fleet_picture_awaiting_operator.py`: stopped/idle ATTENTION remedies and the state column now steer operators to `bmad-build-auto` dispatch and `marshal watch --fleet` instead of `factory spin`, and the fixture assertions were updated to match. Already landed on `main`, unrelated to this story's own diff. Scoped-stamped: --write-baseline --spec pyforge-mason/spec-packaging-factory.
- (event) Surface reconcile 2026-09-25: .claude/skills/conda-forge-expert/scripts/github_version_checker.py, .claude/skills/conda-forge-expert/tests/unit/test_github_version_checker.py (plus SKILL.md / CHANGELOG.md / MANIFEST.yaml / config/skill-config.yaml version carriers) — CFE v8.90.6 Rule-2 retro of the bmad-suite advance: extract_github_repo reads the v1 about.repository / about.homepage (+ dev_url) beside v0 about.home; four unit cases. Co-governed with spec-conda-forge-expert-rebuild. No CAP change.
