---
topic: reusable-cicd-workflows — parked with trigger
updated: 2026-09-09T16:00
---

- (event) Parked spec authored 2026-08-22 (operator: get all seeds ready-to-implement): the Dream's own constraints forbid building until the trigger fires; zero stories minted — parking IS the decomposition decision
- (note) Trigger sharpened 2026-08-22: the candidate second consumer has a name — the sibling repo conda-forge-tracker (active, real commits) currently runs ZERO CI. The trigger fires the day tracker wants CI; until then the premise stays unfired
- (decision) REFRAMED by operator 2026-08-22: parked -> EXTENSION-POINT. Instead of waiting for a second consumer, this repo's workflows become consumable via workflow_call (a thin reusable wrapper + documented inputs/secrets contract) so any repo — conda-forge-tracker, an enterprise/air-gapped mirror vendoring them — plugs in self-service. One stub story minted (mason 9.1); the family premise stays out of scope
- (note by claude) Trigger watch, 2026-09-09 (fleet-readiness-decision-batch-2026-09-09 D11 / mason-E3). This Spec stays status: extension-point and the socket is real (five workflow_call workflows exist). Its declared trigger is a SECOND consuming repo needs this repo's workflows -- and steward S-44.7 (the python-foundry cutover) creates factory/ with its own pixi.toml and lock plus a recipes-only island CI that triggers on paths: factory/** only (steward epics.md:2650,2657). That is a second consuming CI surface inside the cutover, so 44.7 may FIRE this extension-point's trigger without anyone noticing. CONSULT THIS SPEC AT 44.7, NOT AFTER: if the island CI wants these workflows, the extension-point activates and the capability is briefed then; if it deliberately does not, record that as the trigger evaluated and declined. Requesting a reciprocal trigger cross-reference in steward's 44.7 story text (cross-station -- mason cannot write it).
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. status stays extension-point, trigger key unchanged, extension contract preserved; a dated Trigger watch section records that steward S-44.7's factory/ island CI is a second consuming CI surface that may fire this trigger, with the consult-at-44.7-not-after instruction and the requested reciprocal cross-reference (cross-station, mason cannot write it).
