---
topic: Shared pixi base-layer + secret-mount discipline across the repo's Containerfiles
updated: 2026-09-09T15:59
---

- (note) The Dream's premise is STALE: 'no Dockerfile exists' predates Stories 7.1/10.3/10.5 — the repo now ships THREE Containerfiles (root, src/platform, compose/dbgpt), all on ghcr.io/prefix-dev/pixi base tags (registry sites 14-16). The trigger ('a real containerized artifact exists') has FIRED; the official pixi image already provides the shared base layer, so the residual is standardization + the credential discipline the Dream calls out as worth retaining regardless
- (event) Spec authored + decomposed 2026-08-22 as mason Epic 8 (1 story); Dream fact-correction rides the landing
- (event by claude) status: ready -> shipped (2026-09-09). Operator ruling, fleet-readiness-decision-batch-2026-09-09 section 2.2 (Table A row pixi-container-image). CAP-1 is live: the base-layer convention is documented and guarded by tests/packaging/test_containerfile_base_layer_convention.py, and every shipped Containerfile follows it (registry-pinned base tag, multi-stage pixi materialization, no ENV-declared credential).
- (constraint by claude) The Containerfile set is enumerated BY GLOB, never by a hard-coded list (2026-09-09, same ruling). A FOURTH Containerfile now ships -- src/platform/compose/mcp-host/Containerfile (spec-mcp-era-isolation slice 1; ghcr.io/prefix-dev/pixi:0.80.0 builder + registry.access.redhat.com/ubi9/ubi-minimal:9.6 runtime, no ENV credential) -- and it follows the convention. But the two enumerations in the repo DISAGREE: scripts/pixi_version_registry.py:79-87 already covers all four, while tests/packaging/test_containerfile_base_layer_convention.py:50-52 hard-codes only three, so the fourth is UNGOVERNED by the convention guard. The omitted file is compliant today; the finding is that nothing would notice if it stopped being. On the re-derive, restate CAP-1 as across every tracked Containerfile (four today), enumerated by glob -- never a hard-coded list, and update Constraints (which reads 'until >=2 Containerfiles diverge') to note four now exist and none diverges. The guard fix itself is a CODE change (derive the list, do not append a fourth literal) and needs a story -- not applied in this pass.
- (event by claude) Vessel minted for the CAP-1 derivation constraint (2026-09-09): mason Story 16.4, The Containerfile convention guard derives its file list. It carries the constraint logged in this memlog the same day -- replace the hard-coded CONTAINERFILES tuple (tests/packaging/test_containerfile_base_layer_convention.py:50-52, three literals) with a derivation over the tracked tree, so the fourth file (src/platform/compose/mcp-host/Containerfile) stops being ungoverned; append a fourth literal and the same defect returns one file later. Its acceptance criteria require proof the derivation actually FINDS the files (a count or explicit membership assertion) so an empty glob cannot pass vacuously, plus a planted-violation check against the previously-omitted file. HOMED ON EPIC 16, not Epic 8: this Spec's subject epic is Epic 8 (Story 8.1, the convention is written and guarded), but epic-8 reads done in the tracked ledger and hanging a backlog story off it would flip that rollup and misreport a completed epic fleet-wide. Recorded so the subject-vs-home split is not later read as a mis-file.
- (event) Re-derived SPEC.md from this memlog 2026-09-09 (bmad-spec headless). Self-validate pass 1 coherence PASS, pass 2 preservation PASS. CAP-1 preserved and restated as 'every tracked Containerfile - four today, enumerated by glob, never a hard-coded list'; status ready -> shipped; Constraints note four exist and none diverges, and name the registry-vs-guard enumeration disagreement leaving src/platform/compose/mcp-host/Containerfile ungoverned; Success signal records the shipped state plus the Story 16.4 residual with its non-vacuous-derivation ACs and the Epic-16-not-Epic-8 homing rationale. surface: [] left as-is (the memlog declares none).
