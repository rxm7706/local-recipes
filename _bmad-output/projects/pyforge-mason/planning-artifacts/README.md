# pyforge-mason — planning artifacts

**Mason** — packaging CLI productizing the `conda-forge-expert` capability
(`mason recipe/package/environment`); wraps the CFE skill + MCP surface for
recipe authoring, builds natively for `package` (PyPI/conda-forge ship) and
`environment` (lockfile binding). Never forks the craft. dist `pyforge-mason`
/ module `pyforge.mason` / CLI `mason`.

BMAD Tier-2 output for this project (`research/`, `briefs/`, `prds/`,
`architecture/`, `epics.md`, `specs/`). Includes the absorbed Presenton
satellite chain (`epics-presenton-pixi-image.md`).
