---
scope: pyforge-mason — the mason packaging CLI (46 FRs / 16 NFRs / 9 D-records)
purpose: build-substrate
altitude: feature
updated: 2026-07-25T06:24
---

- (direction) Headless create from PRD (46 FRs/16 NFRs/9 D-records). Feature altitude, build-substrate purpose.
- (decision) AD-1 paradigm: ports-and-adapters with a knowledge-free core. CFE is framed as a THIRD-PARTY SYSTEM outside the hexagon, same footing as twine/conda-lock — this makes forking recipe knowledge a layering violation rather than a judgement call.
- (decision) AD-2 dependency direction inward-only; no use-case may import subprocess. Closes the leak path by which recipe logic migrates into Mason one helper at a time (PRD R-1).
- (decision) AD-3/AD-4 CFE port is the sole caller; subprocess-only with mandatory timeout and typed CfeResult. Import banned outright (5 hyphenated modules unimportable; import also inherits CFE Path(__file__) data resolution + hang history with no timeout).
- (decision) AD-5 resolution chains are PURE functions of (arg, env, start-dir) doing filesystem reads only — makes FR-2/FR-3 testable without a real CFE install, and records which step matched so doctor need not re-resolve.
- (decision) AD-6 capability tiers are STRUCTURAL: package.py resolves the CFE port PER TARGET, not per command, so a pypi ship succeeds with CFE absent. This is what keeps 2 of 3 verb families genuinely standalone under D-1's accepted tradeoff.
- (decision) AD-9 ShipReceipt is the single shared shape; state enum {not_attempted,failed,pending,terminal}; pending is NEVER collapsed into success in any rendering. Closes the highest-risk two-unit divergence (pypi vs conda-forge disagreeing on what 'done' means).
- (decision) AD-10 idempotence by target INTERROGATION, not local state. Mason persists no state dir/cache — prevents two owners of one truth (stale cache vs actual index/PR state). Revisit requires reopening AD-10 explicitly.
- (decision) AD-11 one owner per operation: staged-recipes submission implemented ONCE in recipe.py; package.py's conda-forge target CALLS it and wraps the result. Closes the mason-recipe-submit vs mason-package-ship-conda-forge duplication risk.
- (decision) AD-12 engines behind one protocol (name/probe/operation), PATH-discovered, never runtime-fetched, pixi.toml ranges mirrored by in-code constants with a sync meta-test (ported from warden).
- (decision) AD-13 no config file in v1 — flags+env only, uniform flag->env->default precedence. Prevents two config systems with undefined precedence.
- (decision) AD-14 credential blindness: Mason never reads JFROG_*, never logs env values, validates credentials before building. Confines CFE's known unconditional JFrog header injection to CFE's own process.
- (decision) AD-15 CFE surface is read-only forever; missing CFE behaviour is an open question routed to a CFE Rule-2 retrospective, never a local patch or vendored copy.
- (decision) AD-16 every test runs against a fixture fake CFE root; no test needs a real CFE install/network/recipes dir. Only the FR-46 fidelity test is excepted and is marked slow (warden marker convention).
- (constraint) CLAUDE.md Rule 1 (CFE authoritative on recipe semantics) and Rule 2 (every conda-forge effort closes with a CFE retro) bind this spine; spec-packaging-factory + spec_surface_check govern the CFE surface.
- (constraint) Inherited from PRD as settled and not re-decided: D-6 python >=3.12, D-9 argparse, D-8 no MCP in v1, D-3 three ship targets, D-2 co-located CFE root.
- (assumption) Warden/atlas conventions are normative for new members (inferred from 2 instances, no written standard); CFE stdout is a de-facto not formal contract; PyPI/GitHub interrogation is cheap enough for AD-10.
- (question) OQ-A1 CFE script mapping per FR-7..FR-14 (mechanical, first story); OQ-A2 pixi build vs publish one adapter or two; OQ-A3 concrete FR-42 deny-list content; OQ-A4 doctor as noun or top-level verb.
- (event) Operational envelope explicitly swept: locally-invoked CLI, no service/daemon/deployment topology/persistent state — recorded under Deferred so the dimension is not silently skipped.
- (event) spine finalized
