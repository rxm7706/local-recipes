---
topic: Sprint-status promotion runs on landing, not on memory — the tracked twin stops depending on someone remembering
updated: 2026-09-05T12:45
---

- (provenance) THIS RECORD STARTS HERE. Created 2026-08-09 by marshal S-13.5 (spec-surface-drift-reconciliation CAP-5 / FR-168): this Spec shipped declaring a `surface:` with no .memlog.md, so spec_surface_check's contract_hash() returned "" and the contract could never move — every governed change would have reported a hard [drift] whose printed remedy ("reconcile the spec") was unreachable, there being no contract to move. Nothing that happened before this entry was captured; do not read the absence of earlier entries as an absence of decisions. The baseline was scoped-stamped in the SAME change that created this file, because a new memlog moves the contract hash off "" and would otherwise downgrade this surface's next real drift from gating [drift] to informational [drift-presumed].
- (note) Governs 2 tracked files. The twin exists because implementation-artifacts/ is gitignored wholesale, so sprint-status.yaml — the only place that knows a story finished — is invisible to CI and absent from every clone.
- (capability) CAP-1 promotion runs on landing, not on memory — a story's completion reaches the tracked twin without an operator remembering to run the sync.
- (capability) CAP-2 staleness is detectable on its own — a detector reports a twin that has fallen behind its feed.
- (capability) CAP-3 the check is real, never approximated — no reconstruction of DONE from commit subjects, the archaeology that broke when squash-merging PR #132 made Epic 10's merge subjects unreachable from main.
- (capability) CAP-4 never races the orchestrator — promotion for a story cannot collide with bmad-loop writing the same feed.
- (change) RECONCILED BY NAME, 2026-08-09: pyforge.doctor.sources.fleet_scan changed under this surface — S-5.7 (FR-179) added `build_fleet_progress`, a read-only roll-up counted from the tracked sprint ledgers via the SAME `parse_sprint_status` the deploy already uses. It adds a key to the data contract and changes no existing behaviour, so nothing this Spec contracts is affected. Acknowledged by name rather than silently stamped: generate.py is governed by five surfaces at once, and stamping four of them while recording nothing is the laundering S-13.2 exists to end.
- (change) S-5.7 AMENDED, 2026-08-09 — the LOCAL board now matches the CLI. Reconciles by name: pyforge.doctor.sources.fleet_scan (`enrich_fleet_progress_live`, called only on the non-git source path) and docs/dashboard/index.html (the section reads "Fleet picture · live" locally, "· tracked" on Pages, and shows `→ projected` plus a RUNNING state only when the live block exists).
- (correction) The operator was right and the first cut was under-built: the LOCAL board is generated on the machine that has tmux and ~/.bmad-loops, so it CAN match `fleet-picture` completely — only the PAGES deploy is limited. The live fields were therefore added to the local branch, exactly where `apply_loop_inflight` already lives, and their ABSENCE is what makes a Pages render honest rather than stale.
- (correction) The first live-detection used `_live_loop_sessions()` (tmux) and was WRONG BOTH WAYS: it reported marshal running because a tmux session lingered hours after its engine died, and missed steward and mason entirely. A multiplexer session is not an engine. Replaced with `marshal status --format json` — the SAME authority scripts/fleet_picture.py reads — so the board and the CLI cannot disagree about who is running. Caught by checking `ps` against the rendered output rather than trusting the first green.
- (change) RECONCILES `pyforge.doctor.sources.fleet_scan` (2026-08-09). Doctor Story 6-8 gave the Guild vocabulary ONE home: `DREAM_STATUSES` and `DREAM_TYPES` were RE-DECLARED here, identical to `scripts/bmad_drift_check.py` by luck rather than construction; `GUILD_DREAMS` and `STATIONS` were imported FROM that detector, which was the right fix for the 2026-07-28 drift but made the board depend on a detector for constitutive data — and Story 6-9 retires that file into Doctor's package, where this environment cannot import it. All four now read `docs/governance/guild-roster.json`. Values verified byte-identical to the pre-extraction tuples, and the AST sweep the 6-8 spec demands reports an EMPTY intersection of module-level constants between the two files. No behavioural change to this Spec's own surface: `dashboard-gen` runs clean and `dashboard-check` renders (9 lines, 58 dreams, 60 chains).
- (note) Co-governed path touched (2026-08-11): pyforge.doctor.sources.fleet_scan — scan_backlog's 'is it building?' test now derives satellite build lines by mirroring chain-completeness INV-A (spec-<slug> OR bare slug across the station's PRD+epics) instead of PROGRAM_DREAM, a station-only slug map a satellite Dream could never appear in. Live effect: BACKLOG went 5 open -> 0; all five rows were satellites with real epics and running stations (mason E6, doctor E7, herald E13, steward E9, and steward E8 for jira-github-projects-sync — the last caught by the operator after a first, too-narrow spec-<slug>-only match missed it). This spec's own concern — landing-triggered promotion — is untouched; that is marshal S-15.2

## Surface reconcile 2026-08-12 — story 5-9 landing (repair_feed extracted for a second caller)

- (note) One of this spec's two governed paths, `scripts/promote_sprint_status.py`, moved when story `5-9-a-story-finished-by-hand-isnt-invisible-to-the-ledger` landed -- `pyforge.doctor.sources.fleet_scan` is untouched by this story. The change extracts the merge-then-write half of `main()`'s own `--repair-feed` path into a new, importable `repair_feed(feed_path, incoming, twin_values)` function, so `pyforge.marshal.cli.deploy::run_reconcile_completions` (owned by sibling spec `pyforge-marshal/spec-quick-dev-reconciliation`) can reuse the SAME logic to close the Tier-3 divergence its own ledger-advancing write creates, scoped to only the keys it just advanced. `main()`'s own CLI behavior is unchanged byte-for-byte (same printed output, same exit codes, same regression/monotonic guard) -- confirmed by re-running `python scripts/promote_sprint_status.py --project marshal` against this checkout's own real data: identical `wrote 1, unchanged 0, skipped 0, refused 0` shape (the one real diff was a pre-existing, unrelated ledger update from prior sessions, reverted after inspection). This spec's own CAP-1..CAP-4 (landing-triggered promotion, staleness detection) are untouched.
- (note) Re-verified after landing: `pyforge-marshal-test` 3538 passed (9 slow deselected).

## Surface reconcile 2026-08-12 -- review-pass patch on scripts/promote_sprint_status.py

- (note) `scripts/promote_sprint_status.py` moved again after the entry above: `main()` gained a defensive `assert merged is not None` immediately after its `repair_feed()` call, closing a review-pass patch finding (a future edit decoupling the outer `(lost or missing)` guard from `repair_feed`'s own internal recomputation could otherwise crash `render()` on `None.items()`; unreachable today, now asserted rather than silently relied on). `main()`'s own CLI behavior is unchanged. `pyforge-marshal-test` still 3538 passed (9 slow deselected).
- (note) Co-governed path touched (2026-08-13): pyforge.doctor.sources.fleet_scan -- same live-station-detection fix recorded in spec-pyforge-testing-charter's memlog (JSON-path bug + build-campaign panel's running/in-progress distinction). NOTHING in this spec's own concern (Tier-3 promotion / scan_backlog derivation) was altered. Named here because this surface co-governs the file and a stamp without a record is the laundering S-13.2 exists to end.
- (note) Co-governed path touched (2026-08-13): pyforge.doctor.sources.fleet_scan -- same apply_loop_inflight status-flip fix recorded in spec-pyforge-testing-charter's memlog (Tier-3 feed's done-at-dev-completion signal racing ahead of state.json's own phase authority during review). NOTHING in this spec's own concern (Tier-3 promotion / scan_backlog derivation) was altered.
- (note) Co-governed path touched (2026-08-15, marshal S-16.1, FR-140..143): pyforge.doctor.sources.fleet_scan -- the slug->project-directory resolver consolidation (full rationale in spec-dashboard-project-path-derivation's memlog, the owning surface). Collapsed five independent hand-authored slug/path mechanisms into one `resolve_project()` + one `_PROJECT_OVERRIDES` table, and made an unresolvable roster/campaign slug exit generate.py non-zero. NOTHING in this spec's own concern (Tier-3 promotion / scan_backlog derivation / repair_feed) was altered -- verified: scan_backlog and the promote_sprint_status.py-facing surface are untouched. Named here because this surface co-governs the file and a stamp without a record is the laundering S-13.2 exists to end.
- (note) Co-governed path touched again (2026-08-15, marshal S-16.1 review pass): pyforge.doctor.sources.fleet_scan -- adversarial-review patches to the resolver landed above (sprint_status_path explicit-None on 3 absorbed-satellite overrides, `_validate_project_overrides()`, a board-key collision guard in `_discovered_board_keys()`, a dead-variable cleanup in `scan_readiness()`). Full rationale in spec-dashboard-project-path-derivation's memlog. NOTHING in this spec's own concern (Tier-3 promotion / scan_backlog derivation / repair_feed) was altered.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log

- (note) Steward 30.2 (2026-08-25): RECONCILES docs/dashboard/generate.py (deleted; parsers now scripts/fleet_scan.py). Co-governed Guildhall path retired; this spec's own concern is unchanged except the generator file is gone.
- (direction by operator, 2026-09-02) Promotion must not run on the operator `main` checkout — use a worktree on the remote tip. Live leftover: every `marshal land` / hand-merge + promote left local `main` ahead-by-promote and behind-by-`gh pr merge` (steward 41.3, 41.4, 42.1).
- (decision, 2026-09-02) Resolved the landing-surface open question: publish the twin by fetch + throwaway detached worktree on `origin/<base>` + commit + fast-forward push. Never commit on `repo_root()` `main`. Never flip `done` on the story dispatch branch before merge.
- (capability) CAP-5 promotion never writes the operator checkout — same isolation lesson as Story 4.3 `merge_branch` (git refuses two worktrees on one branch; detached sha checkout + FF-push).
- (constraint) A non-fast-forward remote is `MRS-LAND-011` WARN, never `--force`. Operator `main` is left for `pull --ff-only`; land does not `update-ref` the local `main` while it is checked out.
- (note by claude) 2026-09-05 drift-presumed reconciliation (branch maint/marshal-docs-reground-2026-09-05): the spec-surface detector reported these governed paths as changed/added while this memlog moved without naming them. Verified each against `git log -1` — every path was landed by a story commit on main within this spec's declared surface; none is an out-of-band edit. Named here so the presumption is discharged; scoped --write-baseline follows. Paths by landing commit — 21c29320a9 (2026-09-02, marshal: publish land ledger promote from an isolated worktree): src/shared/packages/pyforge-marshal/src/pyforge/marshal/adapters/vcs_git.py [added], src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/land.py [added], src/shared/packages/pyforge-marshal/src/pyforge/marshal/ports/vcs.py [added]
- (change) RECONCILES `src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/land.py` (2026-09-10, marshal Story 28.26, PR #1123): `_promote_sprint_ledger`'s feed-sync step (this spec's own CAP-1 promotion path, the SAME function CAP-5's isolation guarantees already govern) gained a new `_land_feed_sync_refusal` helper and now refuses a promotion that would drop ANY twin-only key from the incoming Tier-3 feed, not just one moving out of `done`. Root cause: `promote_mod.regressions()` alone only catches a key moving OUT of a protected (`done`) state — it never caught a key the feed simply never had, which is exactly what happens the first time a freshly-authored epic's still-`backlog` stories meet a Tier-3 feed that predates them. Live incident: this exact gap silently dropped 5 of pyforge-warden's Epic 12 stories from the tracked ledger 6 seconds after this function auto-landed Story 12.1 (restored by hand, PR #1117). This spec's own CAP-1..CAP-5 (landing-triggered promotion, staleness detection, never-races-the-orchestrator, worktree isolation) are unchanged in shape — only the refusal condition widened, additively, alongside the pre-existing `regressions()`-based one. `pyforge-marshal-test` 7694 passed at landing.
