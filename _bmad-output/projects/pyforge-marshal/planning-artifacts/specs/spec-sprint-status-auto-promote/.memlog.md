---
topic: Sprint-status promotion runs on landing, not on memory — the tracked twin stops depending on someone remembering
updated: 2026-08-09T10:15
---

- (provenance) THIS RECORD STARTS HERE. Created 2026-08-09 by marshal S-13.5 (spec-surface-drift-reconciliation CAP-5 / FR-168): this Spec shipped declaring a `surface:` with no .memlog.md, so spec_surface_check's contract_hash() returned "" and the contract could never move — every governed change would have reported a hard [drift] whose printed remedy ("reconcile the spec") was unreachable, there being no contract to move. Nothing that happened before this entry was captured; do not read the absence of earlier entries as an absence of decisions. The baseline was scoped-stamped in the SAME change that created this file, because a new memlog moves the contract hash off "" and would otherwise downgrade this surface's next real drift from gating [drift] to informational [drift-presumed].
- (note) Governs 2 tracked files. The twin exists because implementation-artifacts/ is gitignored wholesale, so sprint-status.yaml — the only place that knows a story finished — is invisible to CI and absent from every clone.
- (capability) CAP-1 promotion runs on landing, not on memory — a story's completion reaches the tracked twin without an operator remembering to run the sync.
- (capability) CAP-2 staleness is detectable on its own — a detector reports a twin that has fallen behind its feed.
- (capability) CAP-3 the check is real, never approximated — no reconstruction of DONE from commit subjects, the archaeology that broke when squash-merging PR #132 made Epic 10's merge subjects unreachable from main.
- (capability) CAP-4 never races the orchestrator — promotion for a story cannot collide with bmad-loop writing the same feed.
