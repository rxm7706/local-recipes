# memlog — spec-marshal-drain-self-resolution

- (event) 2026-09-01: Kernel `ready`. Owner Dream is `marshal-dependency-aware-dispatch` addendum F. Standalone Dream archived as pointer. OQs locked (no auto-stub; cheap re-preflight; local-clean land; blast radius in verify). Decomposed Stories 28.18–28.23.
- (note) CAP-6 overlay half already on `main` (`20e88e8b0f`); 28.23 owns the stranded-work ATTENTION signal.
