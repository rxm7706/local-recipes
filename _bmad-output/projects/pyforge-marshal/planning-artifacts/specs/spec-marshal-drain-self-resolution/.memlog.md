---
topic: Marshal drains its own queue and resolves stranded work itself — no auto-stub, cheap re-preflight, local-clean land, blast radius in verify (owner Dream `marshal-dependency-aware-dispatch` addendum F)
updated: 2026-09-09T18:04
---

# memlog — spec-marshal-drain-self-resolution

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-drain-self-resolution`.

- (event) 2026-09-01: Kernel `ready`. Owner Dream is `marshal-dependency-aware-dispatch` addendum F. Standalone Dream archived as pointer. OQs locked (no auto-stub; cheap re-preflight; local-clean land; blast radius in verify). Decomposed Stories 28.18–28.23.
- (note) CAP-6 overlay half already on `main` (`20e88e8b0f`); 28.23 owns the stranded-work ATTENTION signal.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 6 governed path(s) drifted against this spec's recorded baseline (6 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/dispatch.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch_fleet.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch_landing.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch_retry.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/status.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_supervisor/__main__.py` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-drain-self-resolution` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.

- (note) Cross-story reconcile 2026-09-07 from Story 32.4 (`spec-fleet-consistency-standard` CAP-3): `core/dispatch_fleet.py`'s epics glob dropped its `epics-with-stories.md` exclusion, which retired fleet-wide with the artifact itself. The `epics*.md` glob stays — it also matches chain-scoped epics (`epics-regenerable-factory.md`, `epics-presenton-pixi-image.md`). No drain/self-resolution behaviour changes; marshal's 7530 tests pass. Appended by hand: this memlog predates the frontmatter convention and `memlog.py` refuses it. Scoped-stamped.
- (change by claude) Surface-touch note 2026-09-08: marshal status gained a `run_state_retired` fact and an MRS-STATUS-012 WARN so a retired or cleaned run reports the home `idle` rather than degrading the whole row to `unknown` (DW-STATUS-2026-09-08-1). `is_run_live` keeps such a run conservatively live, so no destructive guard is weakened. Reconciled in full on pyforge-marshal/spec-fleet-status-supervisor-fallback. Appended BY HAND: this memlog carries no frontmatter, so `memlog.py append` refuses it -- and a scoped stamp still succeeds, which would have left the spec stamped with no reconcile note at all. RECONCILES: src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/status.py. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-marshal-drain-self-resolution.
- (note by claude) 2026-09-09 hygiene (post-merge follow-up pass): this memlog gained the frontmatter it never had. Two earlier entries here record having to append BY HAND because `memlog.py` refuses a frontmatter-less file (`split()` raises '.memlog.md has no frontmatter' at line 98) — and one of them notes the real hazard: a scoped spec-surface stamp still SUCCEEDS in that state, so the spec could be stamped with no reconcile note at all. That failure mode is now closed; this entry was written by the tool. No product change, no SPEC.md edit.
- (event by claude) status: ready -> shipped -- CAP-1..7 decompose to Stories 28.18-28.24, all `done` (fleet-readiness-decision-batch-2026-09-09 Class B, row mars-B).
- (event) (self-validate pass 1 -- coherence) 2026-09-09 re-derive: PASS. Sole 2026-09-09 entry is a status event; status ready -> shipped, rationale recorded as an Assumption. No capability, constraint or non-goal touched; open_questions already [].
- (event) (self-validate pass 2 -- preservation) 2026-09-09 re-derive: PASS. The 28.18-28.24 all-done evidence is preserved in the Assumptions bullet; nothing else in the memlog is new since the last derive.
