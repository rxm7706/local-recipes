---
topic: A GitHub PR-merge branch from one project never masquerades as a same-numbered story in another (marshal land false-positive already-landed bug)
updated: 2026-08-15T14:05
---

- (direction) Source: docs/dreams/marshal-land-cross-project-story-key-collision.md, a complete, evidence-rich Dream authored this session after live root-causing a real marshal bug. Distilling express-mode, no elicitation needed.
- (note) Bug confirmed live 2026-08-15: marshal land pyforge-mason reported story 4.2 as already_landed/merged=true when origin/main provably does not contain that commit (git merge-base --is-ancestor a723618553 main -> not an ancestor).
- (note) Root cause traced to core/promotion.py:146 extract_story_key_from_github_merge_subject -- extracts a StoryKey from ANY GitHub PR-merge branch's final path segment with no project_slug check, unlike its sibling extract_story_key_from_bmadloop_merge_subject which IS scoped. PR #274 (branch marshal/4-2-teardown-reachability-spec-recovery, a MARSHAL story) was misattributed as mason's own story 4.2 purely by numeric coincidence.
- (note) Second confirmed false positive: PR #441 (branch 2026-08-11-Pixi-v0.76.2, a routine dependency bump, no story at all) parsed as bogus key 2026.8 -- shows normalize() has no plausibility bound, though that is explicitly out of scope (non-goal) for this fix.
- (capability) CAP-1: extract_story_key_from_github_merge_subject's branch match is scoped to the project_slug being queried, so a GitHub PR-merge branch belonging to a DIFFERENT project can never be mistaken for a same-numbered story in the project being checked. Success: re-running merged_story_keys(subjects_from_main, template, 'mason') no longer returns 4.2 (PR #274/marshal) or any other cross-project false positive; a new regression test pins PR #274's exact real subject against project_slug='mason' and asserts no key is returned.
- (capability) CAP-2: the existing, correct single-project extraction keeps working for BOTH real branch-naming shapes this repo's own history carries: <slug>/<epic>-<seq>-<desc> (e.g. marshal/4-2-teardown-...) and land/<slug>-<epic>-<seq>-<desc> (e.g. land/mason-4-3-mason-environment-lock). Success: every existing test_promotion.py case for extract_story_key_from_github_merge_subject still passes unmodified, plus new tests covering both shapes scoped correctly.
- (constraint) Pure-function discipline (AD-4) holds: no I/O, no subprocess, no pathlib methods added to core/promotion.py -- the fix is string/regex logic only, operating on the subject text and project_slug already passed in.
- (constraint) Must not weaken or bypass extract_story_key_from_bmadloop_merge_subject's existing project_slug scoping -- this fix brings the GitHub pattern up to the same standard, never loosens the bmad-loop pattern.
- (non_goal) Not adding a plausibility bound on raw epic/story numbers in core.identity.normalize (the 2026.8 symptom) -- different function, different contract; scoping the GitHub pattern to project_slug already prevents this bug's cross-project false positive without touching normalize.
- (non_goal) Not auditing every other caller of merged_story_keys (e.g. run_reconcile_completions/_reconcile_open_intents) for downstream consequences of past false positives -- scoped to the extraction bug itself, not a forensic audit.
- (non_goal) Not landing mason's actual 4-2 wave as part of this fix -- that is normal fleet operation once marshal land can be trusted again.
- (note) Success signal (whole spec): pixi run --frozen -e pyforge-marshal pyforge-marshal-test all green including new regression tests; then a live re-run of marshal land pyforge-mason reports a fresh landing for wave 4.2, not already_landed -- the fix's own acceptance evidence against the real bug that motivated it.
- (event) Self-validate pass 1 (coherence, rules 1-6+8): PASS. Both CAPs have intent+success, WHAT-level intents, 3 real bending constraints, 3 non-goals, testable success signal, stable unique CAP IDs, lean prose matching house style precedent (spec-marshal-status-harness-run-id-poisoning).
- (event) Self-validate pass 2 (preservation): PASS. Every load-bearing Dream claim (root cause + both concrete false-positive examples, all 3 constraints, all 3 non-goals, shared-function blast-radius note) landed in SPEC.md. No wrapper-only content dropped.
- (event) SPEC.md finalized: 2 capabilities, 0 companions, 1 source (the Dream, fully absorbed). No assumptions or open questions -- Dream input was complete and unambiguous.
- (note) IMPORTANT provenance correction (found mid-implementation): this is a KNOWN, previously-deferred gap, not a fresh discovery. implementation-artifacts/deferred-work.md (Tier-3) already carries entries against extract_story_key_from_github_merge_subject (2026-08-10, review passes 2+4, Blind Hunter + Edge Case Hunter independently), measuring the same contamination live against main's then-2,353 subjects (mason/doctor/scribe ~30 bogus keys each) and explicitly deferring a full fix. cli/status.py's _merged_keys_for_slug already hedges a present key as 'stronger evidence, not proof' because of this known gap -- but cli/land.py's already-landed short-circuit does not hedge at all. This Dream/Spec is the first concrete consequential failure, and scopes NARROWER than the deferred entries' own 'read both shapes' framing: only closes the cross-project false-positive hole in the EXISTING recognized <slug>/<epic>-<seq> shape via project_slug scoping, matching the sibling bmad-loop pattern's own precedent -- does not add new subject-shape recognition (that larger work stays exactly as deferred).
- (note) Design detail confirmed by direct execution: extract_story_key_from_github_merge_subject currently has signature (subject: str) only, no project_slug param -- must be extended to match the bmad-loop sibling's (subject, project_slug) signature, with _classify_merge_subject's call site updated to pass project_slug through. Also confirmed live: the land/<slug>-<epic>-<seq> branch shape (e.g. land/mason-4-4) is ALREADY harmless today -- its extracted segment starts with the slug name not a digit, so normalize() already raises MalformedStoryKeyError and returns None regardless of scoping -- so CAP-2's 'both shapes' constraint is satisfied automatically by the fix without special-case code, confirmed against real PR subjects (#274, #516, #441).
- (event) CAP-1/CAP-2 landed (2026-08-15, commit d7fd62707b): extract_story_key_from_github_merge_subject widened to non-greedy owner capture + project_slug prefix check in core/promotion.py, plus tests/unit/test_deploy.py coverage.
- (event) Review pass 1 landed (2026-08-15, commit ec5a4e3da2, Blind Hunter + Edge Case Hunter, parallel, no shared context): rejected an empty-leading-branch-segment subject instead of degrading to `branch.startswith("/")`, closing a narrow reopening of the same collision; corrected stale docstrings in core/status.py and cli/status.py that still narrated the cross-project contamination as an open deferral against core/promotion.py now that the fix threads through both call sites automatically; core/promotion.py and tests/unit/test_promotion.py also touched. Two low-severity, no-live-occurrence findings deferred (case-sensitive station matching; a PR whose fork-owner segment coincidentally equals the station name), out of this fix's scope per the Dream's own non-goals.
- (note) This entry establishes the spec's first spec-surface-baseline stamp -- no `.spec-surface-baseline.json` entry existed for this key before now (this spec declares no `surface:` glob, so the stamp records `files: {}`). Re-verified: `pixi run --frozen -e pyforge-marshal pyforge-marshal-test` 4679 passed, 9 deselected.
- (event) 2026-08-15 (unrelated station, mason Story 5.3 delegation-fidelity-test): this spec's `scripts/.spec-surface-baseline.json` entry was first-stamped by mason's verification-repair pass, purely to clear a repo-level `spec_surface_reconcile.py` [no-baseline] finding blocking that unrelated story's own gate -- this SPEC.md carries no `surface:` frontmatter block, so the stamp governs zero files (`"files": {}`) and reconciles nothing about CAP-1/CAP-2's own implementation. Recorded here only for audit-trail continuity (review finding, Blind Hunter, 2026-08-15).
