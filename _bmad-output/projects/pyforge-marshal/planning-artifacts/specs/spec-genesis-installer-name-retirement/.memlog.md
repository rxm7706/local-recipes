---
topic: Retire genesis-installer completely: full pyforge-marshal PRD/architecture/epics rewrite that unifies the FR/NFR/SC/OQ/K numbering, resolves the CLI-framework and init/check verb collisions the mechanical fold-in left open, and merges epics-genesis-installer.md into one epics.md
updated: 2026-08-02T19:30
---

- (direction) User: get rid of the genesis-installer name and reference completely (not just labeled on the dashboard, per PR #233's stopgap).
- (direction) User: 'its all marshall' -- no separate satellite identity should remain anywhere, dashboard or planning docs.
- (direction) User: do the entire marshal dream-to-spec/PRD/architecture/epics/story chain rewrite, so genesis goes away and the marshal CLI is built correctly -- widens scope from manual FR renumbering to running the standard planning chain.
- (decision) Depth chosen (of 3 offered): full ID unification, not just the epics-merge+dashboard fix or a prose-title cleanup -- confirmed via AskUserQuestion.
- (decision) Mechanism chosen: run bmad-prd -> bmad-architecture -> bmad-create-epics-and-stories against the already-unified Spec, rather than hand-renumbering FR1..FR62 -> FR-66..FR-127 -- a rewrite actually decides the two open contradictions the mechanical fold-in left standing; manual renumbering would not.
- (decision) Sequencing (of 2 offered, confirmed via AskUserQuestion): hold the full chain regeneration until the active Story 2.1 bmad-loop run (20260802-183704-36df) is fully done and merged into main. This Spec is drafted now, planning-only, zero live edits to PRD/architecture/epics.
- (capability) CAP-1 -- One combined epics.md (Epic 1-12), no separate epics-genesis-installer.md. Intent: a reader/dashboard never has to know two documents used to exist. Success: single file, single dashboard row, epics-genesis-installer.md archived not deleted.
- (capability) CAP-2 -- One contiguous FR space produced by the chain rewrite (not hand-renumbered). Intent: no bare-digit FR62-style island. Success: every functional requirement in the regenerated PRD carries the FR-N (dashed) form, sequential, no gaps/dupes.
- (capability) CAP-3 -- Installer-only namespaces (NFR-O1, SC-01..10, OQ-1..9, K-01..03) folded into or reconciled against Marshal's own equivalent sequences (NFR-1..14, however Marshal's PRD labels success criteria, Q-1..17). Intent: no second unlabeled numbering island survives the rewrite. Success: grep for the old bare forms returns zero hits outside archive/ and .memlog.md files.
- (capability) CAP-4 -- The CLI framework contradiction is decided, not carried forward. Intent: Marshal's own CLI is shipped argparse (Story 1.1); the installer satellite's own architecture specifies typer+rich for its unbuilt CLI -- one binary needs one framework. Success: the regenerated architecture states which framework the unified CLI uses and why, with no 'flagged, not resolved' language remaining on this point.
- (capability) CAP-5 -- The init/check verb collisions are resolved. Intent: genesis init collides with the shipped marshal init <slug>; a second collision exists between marshal check (FR-65/AD-50, routes to scripts/detectors.py's registry) and the installer's own check (answers a different question: does an externally-installed repo still conform). Success: the regenerated PRD names one verb surface -- either one check that does both jobs, or two distinctly-named verbs -- with the installer's distinct question given a real verb name if it isn't check.
- (capability) CAP-6 -- Every 'Satellite: Genesis Installer PRD/Architecture' section header and prose mention of genesis-installer as a separate thing is gone from the regenerated PRD, architecture, Spec, and brief. Intent: the content survives as ordinary Marshal capability; only the separate-sounding name retires. Success: grep -i 'genesis.installer' across the regenerated planning-artifacts/{prds,architecture,specs,briefs} returns zero hits outside archive/ and .memlog.md.
- (capability) CAP-7 -- Dashboard build-campaign section shows exactly one pyforge-marshal row (total stories = Epics 1-12 combined), no second genesis-installer row, and generate.py/index.html/IMPL_CAMPAIGN_LEDGER no longer reference genesis-installer. Intent: the dashboard reflects the unified project structure, superseding PR #233's epic-range-label stopgap. Success: docs/dashboard/generate.py's IMPL_CAMPAIGN has one marshal entry; dashboard_drift_check.py stays clean.
- (capability) CAP-8 -- Already-landed story identity survives the rewrite. Intent: Epic 1's 10 done stories (and whatever Story 2.1 lands as by the time this runs) keep their exact story keys in sprint-status-ledger.yaml -- the rewrite must not renumber or rekey shipped work. Success: every story key with status=done before the rewrite has the identical key after it; only backlog-only epics (currently all of 2-12) are free to be restructured.
- (constraint) Never rewrite .memlog.md history or archive/ content -- both are the permanent, append-only record of how the consolidation actually happened; the rewrite touches only live, current documents.
- (constraint) Hold ALL execution (bmad-prd/bmad-architecture/bmad-create-epics-and-stories, and the epics-genesis-installer.md merge) until Story 2.1's bmad-loop run (20260802-183704-36df) is fully done and merged into main -- epics.md is what the active run's story identity keys off of; this Spec itself is planning-only, drafted while the loop is still running.
- (constraint) Already-done story identity is frozen: any story key with status=done in sprint-status-ledger.yaml at rewrite time (Epic 1's 10, plus whatever lands before the rewrite starts) must keep its exact key. Only backlog-only epics may be restructured.
- (constraint) The CLI-framework and verb-collision contradictions must be actually DECIDED by the rewrite, not re-flagged as open questions a third time -- a constraint that doesn't bend this design decision defeats the point of doing a full rewrite instead of a mechanical renumber.
- (note) Non-goal: not a new BMAD project -- this stays inside pyforge-marshal's own planning tree, same shape as spec-bmad-output-hygiene and spec-dashboard-project-path-derivation.
- (note) Non-goal: not touching Story 2.1's own Epic 2 content or story key before it lands -- the rewrite targets Epics 7-12 (genesis-installer) and the FR/NFR/SC/OQ/K numbering it introduced; Epics 2-6 stay as currently speced except for whatever numbering shift the unified FR space requires in cross-references.
- (assumption) Assumption: bmad-prd/bmad-architecture/bmad-create-epics-and-stories, run in update mode against the existing pyforge-marshal Spec/PRD/architecture, will preserve done-story identity the way CAP-8 requires -- not independently verified against those skills' actual update-mode behavior; verify before the rewrite starts, not after.
- (question) Open question: does bmad-create-epics-and-stories in update mode guarantee existing done/in-progress story keys survive re-generation, or does it require manual reconciliation after the fact? Needs checking against the skill's own docs/behavior before the rewrite starts.
- (question) Open question (inherited, not resolved by this Spec): which way do the two contradictions resolve -- argparse vs typer+rich, and whether check stays one verb or splits into two? This Spec requires the rewrite to decide them; it does not itself decide them.
- (event) Self-validate Pass 1 (coherence): PASS. All 8 CAPs carry intent+success; constraints (history/archive immutability, hold-until-loop-clear, frozen done-story identity, must-decide-not-reflag) each bend a real decision; 2 non-goals present; success signal concrete and demonstrable; CAP IDs fresh and unique; prose lean, matches sibling-spec density.
- (event) Self-validate Pass 2 (preservation): PASS. Every load-bearing Dream claim landed -- the widened full-chain-rewrite mechanism (CAP-2/CAP-4/CAP-5), the two inherited contradictions (CAP-4/CAP-5), the sequencing/timing decision (constraint), the frozen-story-identity requirement (CAP-8/constraint). The Dream's line-by-line grep evidence stayed in the Dream (narrative/investigative, not contract) per Spec Law -- cited via sources:, not duplicated.
- (event) Spec finalized, status: draft. Planning-only -- zero live edits made to pyforge-marshal's PRD/architecture/epics/dashboard code in this pass. Execution (bmad-prd -> bmad-architecture -> bmad-create-epics-and-stories, then the epics-genesis-installer.md merge and dashboard cleanup) is deferred until Story 2.1's bmad-loop run (20260802-183704-36df) is fully done and merged into main.
- (note) Citation-map research (background agent, 37 tool uses) confirmed FR1..FR62 sequence is gap-free/dup-free in strict order -- the FR1->FR-66..FR62->FR-127 mapping (CAP-2) is valid as-is. Full table + every citation site landed in companion citation-map.md.
- (constraint) Caution for the rewrite: FR-66..FR-69 were briefly claimed and reverted by an unrelated spec-pyforge-testing-charter fold attempt the same day -- free today, but already used once; whoever runs the rewrite should know this before assigning genesis-installer's FR1-4 there.
- (correction) Correction to the Dream's premise (docs/dreams/genesis-installer-name-retirement.md): Marshal's own Open-Questions sequence is Q-1..Q-16, NOT Q-17 -- Q-17 does not exist anywhere live. No OQ-* item is cross-referenced anywhere as feeding a Marshal Q-number. SPEC.md's own bare-numbered item 17 ('Installer verb mapping') is self-referential, not an OQ-* import. CAP-3's OQ-1..9 reconciliation target is therefore open, not a known Q-17 merge point.
- (note) New facts narrowing CAP-3: Marshal's PRD has NO SC-N namespace at all (uses SM-1..7/SM-C1..3 instead) and NO kill-criteria/K- namespace at all -- SC-01..10 and K-01..03 have nothing existing to fold into; the rewrite must decide to adopt them as new to Marshal (SC- alongside or merged into SM-, K- adopted wholesale) rather than 'reconcile against an existing equivalent.' NFR-O1 is likely redundant with Marshal's own NFR-12 (near-identical substance).
- (event) Dream (docs/dreams/genesis-installer-name-retirement.md) corrected in place to match citation-map.md findings: Q-17 claim fixed to Q-16 (no cross-reference exists), SC-/K- namespaces corrected from 'unclear' to 'confirmed absent,' FR mapping confirmed valid with FR-66-69 caution noted. Realization log entry added recording the correction.
