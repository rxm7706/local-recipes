---
topic: bmad-loop-intent-gap-work-preservation (legacy memlog; frontmatter added by the 2026-08-22 format migration, marshal Story 25.3 — body below is the untouched original)
updated: 2026-09-09T18:09
---

# .memlog.md — spec-bmad-loop-intent-gap-work-preservation

## 2026-08-14 — Genesis baseline

Spec drafted 2026-08-14 (`status: draft`); no story has touched or reconciled this
surface yet. This baseline is a structural bootstrap only — it establishes the
S-13.7 spec-surface-reconciliation contract (`scripts/spec_surface_check.py
--write-baseline --spec <project>/spec-<slug>`) against the surface's current,
unmodified file state. No governed-file content is claimed reconciled by this entry.

## 2026-08-21 — Surface reconcile: bmad-loop 0.11.0 version range

`harness_bmadloop.py`'s `HARNESS_VERSION_RANGE_TEXT` + min/max tuples moved
`>=0.9.0,<0.10` → `>=0.11.0,<0.12` (bmad-loop 0.11.0 adoption on the BMAD
6.11 upgrade branch; 0.9.0 hardcodes `/bmad-dev-auto` and stalls unattended
sessions on BMAD ≥ 6.11). Full 5152-test marshal suite green against the
REAL installed 0.11.0.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Cross-surface edit 2026-08-22 (marshal Story 25.4, spec-bmad-611-era-alignment CAP-4): src/shared/packages/pyforge-marshal/src/pyforge/marshal/adapters/harness_bmadloop.py changed under this shared surface — five bmad-loop 0.10/0.11 knobs added to _POLICY_TEMPLATE + render_policy_toml ([review].on_timeout/.on_status_contradiction, [limits].dev_contract_nudge, [verify].stream_capture_kb, new [operator] section). No preservation/escalation seams touched. Narrative: spec-pyforge-marshal/.memlog.md, same date.
- (event by claude) Cross-surface note 2026-08-22 (marshal Story 25.5, spec-bmad-611-era-alignment CAP-5): adapters/harness_bmadloop.py::run_status_snapshot — a file this spec co-governs — now also reads bmad-loop 0.11's per-task preserve_ref (incl. the escalated task's, as RunStatusSnapshot.escalated_preserve_ref) and RunState.sweeps_refused into the snapshot, verbatim, inside the existing widened guard. Nothing this spec's own contract covers changed; the drift is Story 25.5's, reconciled in spec-pyforge-marshal's memlog with the full detail.
- (event by claude) Surface reconcile 2026-08-29: 1 drift finding, adapters/harness_bmadloop.py, verified via git log. Recent touches are Story 22-8 (session-harness profile-driven), the pyforge-core shared-hooks-plugin extraction of the loop runner, Story 24-1 (engine_liveness primitive), Story 20-4 (intent-gap attempts preserved at supervisor seam), and Story 25-5 (0.11 status vocabulary). All confirmed done in sprint-status-ledger.yaml. Story 20-4 is exactly this spec own subject; the rest are unrelated refinements to the same shared adapter file, none introducing behavior outside an existing CAP. Scoped baseline stamp only.
- (change by claude) Surface fix 2026-08-29: scripts/missing_preserve_check.py was ungoverned (no spec surface, no allowlist entry) despite its own docstring citing Story 20.5/FR-189/CAP-3 by exact name, which this spec is. Added to surface. No behavior change, governance-only.
- (event by claude) Surface reconcile 2026-08-31: `adapters/harness_bmadloop.py` changed once since the last stamp, by `e58ef1fa4b` (Story 28.1, spec-marshal-token-economy CAP-1 — adds a conditionally-rendered `[context]` block, inert when undeclared). Verified via `git log --oneline 5dfe5826c3..HEAD -- adapters/harness_bmadloop.py`. No preservation/escalation seam touched. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-bmad-loop-intent-gap-work-preservation`.
- (event by claude) Surface reconcile 2026-09-01: `adapters/harness_bmadloop.py` changed once since the last stamp by `80b773dc53` (Story 28.4, done) — savings telemetry snapshot wiring. No preservation/escalation seam touch. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-bmad-loop-intent-gap-work-preservation`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 1 governed path(s) drifted against this spec's recorded baseline (1 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/adapters/harness_bmadloop.py` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-bmad-loop-intent-gap-work-preservation` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (note by claude) Cross-spec surface touch (Story 31.3, CAP-4 spec-bmad-suite-lifecycle): adapters/harness_bmadloop.py changed to add a new rendered [review].min_score key (the tea-test-review marshal review lens's threshold, default 80) -- unrelated to this spec's own intent-gap-preservation concerns; no preserve-branch, attempt-preserve, or intent-gap-halt logic touched. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (event by claude) status: ready -> shipped (fleet-readiness-decision-batch-2026-09-09 Class B, row mars-A). Stories 20.4/20.5 are `done`: pyforge/marshal/supervisor/intent_gap_preserve.py parks the attempt proactively, supervisor-side, BEFORE the halt reverts (:38-39), and scripts/missing_preserve_check.py (pixi task missing-preserve-check, pixi.toml:940-942) is the watchdog. Both open questions were answered by the implementation: proactive supervisor snapshot (not adapter interception of bmad_loop own revert), and selective -- a closed intent-gap vocabulary (intent_gap_preserve.py:28-39, looks_like_intent_gap at :68) distinguishes an intent-gap halt from an ordinary retry revert.
- (note by claude) 2026-09-09 RESIDUAL, observation plane (fleet-readiness-decision-batch-2026-09-09 row mars-A-D2): scripts/missing_preserve_check.py:61 reads ~/.bmad-loops only -- the same dormant plane as the baseline-drift watchdog, and it likewise exits 0 "OK: every intent-gap halt has a present preserve artifact" over an empty set rather than reporting could-not-observe. Re-pointing it at the dispatch plane is marshal Story 33.7.
- (event) (self-validate pass 1 -- coherence) 2026-09-09 re-derive: PASS. status ready -> shipped; both questions retired into a dated closed-questions section with the implementation answers; the empty-observation-plane finding renders as its own Residual section. 3 CAP ids, unchanged.
- (event) (self-validate pass 2 -- preservation) 2026-09-09 re-derive: PASS. Both 2026-09-09 entries landed: the 20.4/20.5 shipped evidence (intent_gap_preserve.py:38-39, :28-39, looks_like_intent_gap at :68, missing_preserve_check.py + pixi.toml:940-942) and the residual at missing_preserve_check.py:61 with the Story 33.7 remedy.
