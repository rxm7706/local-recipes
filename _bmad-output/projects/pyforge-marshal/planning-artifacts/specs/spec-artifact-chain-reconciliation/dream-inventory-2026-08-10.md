# Dream inventory — Phase 2b (2026-08-10)

CAP-8's gate report: all 61 Dreams dispositioned. Method: frontmatter parsed
programmatically (61/61 parse), status/owner/type vocabulary validated
(zero violations), README table cross-checked (synced), status truth judged
against the README's own state definitions with this audit's Phase 0-2
measurements as evidence.

## Distribution (post-truth-up)

| status | count | verdict |
|---|---|---|
| archived | 33 | consolidation-era set; spot-checks against the Dream map hold; not re-litigated (Dream-level-only consolidation stands, incl. the operator's 4-station override) |
| realized | 19 | 18 held + **kedro-org-tooling-adoption dreamt→realized** (atlas Epic 12 shipped its decomposition; Spec draft→shipped in Phase 2) |
| specified | 5 | 3 held (mason — its dreamed product, the recipe/package/environment verbs, does not run yet; jira-github-projects-sync — 1/5 stories; secure-live-dashboards) + artifact-chain-reconciliation (this audit) + **deferred-work-visibility dreamt→specified** (four questions closed at `377611d5e4`, Spec `ready`, 6-9 dependency landed) |
| dreamt | 2 | conda-forge-expert-rebuild (feasibility unresolved, DEFERRED — and now carrying the AD-15 collision finding from mason's Phase 1 report), herald-moments-2-4-live-backend (premise unsettled, DEFERRED) — both correctly held back |
| pitched | 2 | pyforge-charter (constitutive), agentic-sdlc-autonomy (deck exists, correctly short of specified) |

## Chain completeness

`dream-chain` detector green throughout (INV-0..3): every Spec names a
Dream, every Dream has a Spec in its owner's project, sharded trees hold.
No stranded BMAD project (PROJECTS.md's 8 = the 8 station projects; the
genesis project's dissolution is recorded). Satellite consolidation spot-
checks (covers-dreams / Satellite-heading matching) resolve — the 19
archived-and-omitted-from-the-README-map satellites remain correctly folded.

## Stranded-artifact sweep

- Story specs: promotion gaps found and fixed in Phases 1-2 (mason 6,
  marshal 5, atlas 8 = 19 recovered); all 8 stations now 100% promoted.
- Decks: `presentations/` 14 dirs, all mapped (HER-9 surface; herald CAP-3
  continuously flags unlinked Design projects).
- The `docs/specs/` legacy tier: unchanged this audit; its `status:`
  frontmatter remains readable via `bmad_drift_check --specs` (transition
  ongoing, out of this audit's correction scope by design).

## Verdict

The Dream estate is truthful to its own vocabulary: two statuses moved (both
upward, both evidence-backed, both recorded in their Realization logs), zero
vocabulary violations, zero table drift, zero unresolvable chains. The two
`dreamt` holds are deliberate deferrals with recorded reasons — the estate's
only genuinely open planning surface is exactly the set Phase 3 addresses.
