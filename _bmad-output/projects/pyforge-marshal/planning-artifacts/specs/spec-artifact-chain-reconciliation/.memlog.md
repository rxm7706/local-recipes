---
topic: Fleet-wide artifact-chain reconciliation audit (pause-and-audit): every chain column verified against code before decomposition or re-spin
updated: 2026-08-10T09:10
---

- (direction) Seeded from docs/dreams/artifact-chain-reconciliation.md (owner marshal, dreamt) — pause building, audit the chain independently against code, fix artifacts through owning skills, only then decompose; drafted at the 267/335 fleet pause on main dfeb565231
- (decision) Operator 2026-08-10: marshal's 26 generated-artifact [drift-presumed] warns → regenerate, verify outputs match (proves no hand-edits), then judge + scoped stamp — chosen over judge-only stamp and over building a per-path generated exemption first
- (decision) Operator 2026-08-10: audits execute SERIALLY in the main session — judgment stays in one context; no agent fan-out (hybrid and full-parallel declined)
- (decision) Operator 2026-08-10: unfinished-station order steward(4) → mason(28) → marshal(35+blocked 8-5) — smallest first calibrates the method
- (direction) Operator 2026-08-10 (mid-spec): completed stations are FIRST-CLASS audit scope, not a lighter close-out — a critical part and point of the audit; structural reason: three of the four queued decomposition chains (atlas, herald, doctor) extend completed stations, so their audit gates decomposition exactly as the backlog audit gates re-spin
- (capability) CAP-1 mechanical-debt-zero: full detector sweep baseline; 51 [drift-presumed] closed (atlas 24 name-then-stamp, mason 1, marshal 26 regenerate+verify+stamp); dangling commits dispositioned — success: six detectors + meta-suite green, zero drift-presumed fleet-wide
- (capability) CAP-2 backlog-truth audit: every one of the 68 remaining stories gets a cited verdict (STILL-VALID/ALREADY-DONE/CONTRADICTED/NEEDS-RESPEC/DROP) in a traceability-matrix gate report — success: 68/68 verdict rows with file:line or command evidence, one gate report per unfinished station
- (capability) CAP-3 completed-station audit at equal rigor: all five (atlas, doctor, herald, scribe, warden) — done-claim sampling per epic, chain-column reconciliation (Spec status, retro promises, story-spec sets, board), Spec statuses corrected to earned values — success: per-station gate report, every chain column verified or dispositioned
- (capability) CAP-4 TEA/coverage refresh: per-epic AC→covering-test map across all 8 stations; an AC with no test is a coverage-debt row (visible, non-gating, operator-dispositioned) — success: coverage table per epic in the gate reports
- (capability) CAP-5 repair through owning skills: verdicts become artifact fixes only via bmad-correct-course / bmad-sprint-planning+sprint-ledger-sync / bmad-document-project / bmad-generate-project-context / dashboard-gen, each with memlog naming paths + scoped stamp — success: all fixes landed via owning skills, detectors green after each landing PR
- (capability) CAP-6 decomposition gate: each of the four queued chains (atlas → herald → doctor → steward) starts only after its owning station's audit gate report lands — success: every decomposition PR cites the landed gate report
- (capability) CAP-7 re-baseline + resume package: final --write-baseline, dashboard-gen, board render, fleet-picture; operator receives a per-station go/no-go summary — success: baseline stamped, board matches ledger, resume decision made on measured artifacts
- (constraint) Citation-or-nothing: a verdict without file:line or command output is rejected in review; prose is not an audit output — matrix rows only (emit the full contract or nothing)
- (constraint) Detectors own mechanical facts (counts, pins, hashes, ledger totals); the audit spends judgment only — premises, AC truth, coverage meaning
- (constraint) Anchoring guard: verdicts ground in code; done-claims sampled cold; blind lens-diverse parallel review (one structural, one coverage/edge-case, neither shown self-assessments) before every landing PR
- (constraint) S-13.2: artifact rewrites only through owning skills + memlog + scoped stamp; ledger only via sprint-ledger-sync, board only via dashboard-gen; no satellite docs — gate reports land in each station's own planning-artifacts
- (constraint) Build stays paused per station until its audit lands; a station's audit landing gates that station only, not the fleet
- (constraint) Verdicts are recommendations with citations: DROP and NEEDS-RESPEC are operator dispositions, never silent deletions
- (constraint) Tier-C (Dream/Deck/UX) untouched unless the audit proves one contradicted
- (assumption) Done-claim sampling depth: >=3 stories per epic or 20% of the epic (whichever greater), all ACs checked on each sampled story; deepened per station if findings warrant
- (assumption) Execution vehicle is the operator's main session driving phases directly (quick-dev shape), not bmad-loop — consistent with the serial decision and the prefer-quick-dev-for-hand-picked-work convention
- (assumption) The bmad-check-implementation-readiness gate-report class accepts the traceability-matrix format; if its template resists, the matrix is the report body and the template's summary wraps it
- (question) Complete-station demotion precedent: when sampling fails an AC on a shipped station, does the story reopen or does it become a coverage-debt row? Decide at first occurrence — sets what done means here (non-blocking)
- (question) One-shot campaign or standing practice (type: practice, repeated at N-story boundaries)? Priced after the first pass (non-blocking)
- (event) Self-validate pass 1 (coherence): PASS — Spec Law 1-6,8 hold; CAP-5's skill names are load-bearing mechanism (S-13.2), not HOW leakage
- (event) Self-validate pass 2 (preservation): PASS — Dream walked claim-by-claim; all load-bearing content in SPEC.md or audit-method.md; Dream's measurement narrative intentionally left as rationale in the owner-dream
- (event) Spec finalized at status: ready — three operator decisions baked in (marshal-26 regenerate+verify+stamp; serial; steward→mason→marshal), completed-stations elevated to first-class scope per operator mid-spec direction; two non-blocking questions remain open
- (direction) Operator 2026-08-10 (mid-spec, second): audit spans ALL of PyForge — every Dream in docs/dreams/ (61 files: stations, capabilities, practices, applications), every planning artifact, every chain stage, all code — not only the 8 station chains
- (decision) Operator 2026-08-10: completed stations audited AFTER steward → mason → marshal, in the same continuous stream at the same rigor — full order: steward → mason → marshal → atlas/doctor/herald/scribe/warden → full non-station inventory
- (decision) Operator 2026-08-10: rebuilding and correcting specs, CODE, decisions, and artifacts is PRE-AUTHORIZED — corrections land through the chain in reviewable PRs with verdict rows; nothing silent; supersedes the verdicts-are-recommendations constraint
- (capability) CAP-8 full-inventory audit: the non-station estate — every Dream's status truth, chain completeness, satellite-consolidation correctness, stranded artifacts; every claim gets a verdict row like any story — success: 61/61 Dreams dispositioned in an inventory gate report
- (note) CAP-5 widened to two-sided repair: artifact diverged from code → fix artifact via owning skills; code diverged from a still-valid contract → fix code via quick-dev with tests + blind review; decisions corrected via bmad-correct-course, recorded
- (event) SPEC re-derived: scope=all-PyForge, CAP-8 added, CAP-5 two-sided, correction pre-authorization replaces recommendation-only constraint; status stays ready
- (event) Self-validate re-run after scope widening: PASS both passes — CAP-8 carries intent+success; correction pre-authorization is a constraint that bends (rules out ask-per-item pauses AND silent changes); all three mid-spec operator directions preserved
- (note) Spec folder registered in the spec-surface baseline (scoped stamp) — files created this session: SPEC.md, audit-method.md, .memlog.md; zero-file surface (governs nothing), stamp is registration not drift acceptance
- (note) Pre-seeded audit finding #1 (found landing this spec, 2026-08-10): story-status red on doctor/6-9 — feed reads done and the landing IS on main (PRs #394/#395), but the story's recorded verify command is 'python scripts/spec_surface_check.py', a shim 6-9 itself retired to an rc=2 stub; the done-claim's verification is broken by its own delivery. Doctor-lane CAP-2/3 verdict row; evidence: stub rc=2, merge bf3d461030
- (note) Pre-seeded audit finding #2: unpushed-work detector lists ~30 dangling commits + branch attempt-preserve/20260809-114839 — the known CAP-1 disposition set, unchanged this session
