---
topic: BMAD skills reachable from Cursor's own interactive chat, not only headless dispatch — CAP-1's live subagent probe gates whether CAP-2's .mdc routing layer is buildable at all
updated: 2026-09-16T02:34
---

- (event by claude) 2026-09-07 bmad-spec create, headless/express mode: input docs/dreams/bmad-cursor-interactive-routing.md (seeded 2026-09-06 from a conversation auditing what "portable to Cursor" would require); project pyforge-marshal (the Dream's owner). Trigger: dream-chain INV-1 dream-without-spec, the one detectors-ci red found while sweeping main for CI/hygiene issues after PR #1079/#1080 landed. Precedent followed: spec-intelligence-hub and spec-golden-path-conda-blind-spot (draft seeded so the chain link is durable, registered in doctor DEFERRED_SPECS).
- (direction by claude) Operator (2026-09-07): resolve all CI/hygiene issues on main, including this pre-existing dream-without-spec finding.
- (decision by claude) Why = headless dispatch (`cursor-agent -p --trust`, Story 22.8/FR-193/CAP-8) is already solved and live for both pyforge-marshal and pyforge-atlas; the unsolved shape is a human inside Cursor's own IDE chat panel invoking a BMAD skill and getting the same review discipline back. Owner marshal (owns Cursor portability generally, per CAP-6/Epic 6).
- (capability by claude) CAP-1 the live subagent probe: settle, empirically, whether Cursor's interactive IDE chat surface (not the headless CLI) can invoke a genuine context-free subagent and get a synchronous result back in the same turn; success = a dated, reproducible probe result recorded in this memlog, pass or fail.
- (capability by claude) CAP-2 `.mdc` routing layer (contingent on CAP-1 passing): a `.cursor/rules/*.mdc` file per BMAD skill, mechanically derived from `SKILL.md` frontmatter + trigger body; success = a request typed into Cursor's chat runs the identical `workflow.md` a Claude Code session would, for the `bmad-build`/`bmad-build-auto` pilot pair.
- (capability by claude) CAP-3 the honest fallback (contingent on CAP-1 failing): `bmad-build-auto` HALTs `blocked`/`no subagents` as designed; operator picks CAP-3a (route review through the verified headless `cursor-agent -p` dispatch) or CAP-3b (restrict Cursor-chat routing to skills without a subagent mandate).
- (capability by claude) CAP-4 team-memory reachability: `.claude/memory/` content reachable from Cursor chat via whatever reference mechanism Cursor actually resolves at load time — verified, not assumed.
- (decision by claude) CAP-1 is the gate: CAP-2/CAP-3 are candidate shapes contingent on its result, not committed work — this Spec chooses nothing until the probe runs.
- (constraint by claude) Dream-first: no code from the seed; CAP-1 runs before CAP-2/CAP-3 are committed to (Dream).
- (constraint by claude) Do not re-litigate or duplicate CAP-6/Epic 6 (pyforge-marshal.md) — that Dream already owns "portability proven, not claimed" for the headless/dispatch shape; this Spec is scoped to the interactive-chat shape only (Dream).
- (constraint by claude) The probe must be live and empirical — no assuming Cursor's interactive chat has the headless CLI's subagent support just because the CLI does (Dream).
- (constraint by claude) `.mdc` generation, if built, stays a mechanical derivation from the existing `SKILL.md` files, never a hand-maintained fork that can drift (Dream).
- (decision by claude) Registered in doctor's DEFERRED_SPECS (board.py): CAP-1 has not run, so decomposing into marshal epics now would manufacture stories ahead of an unanswered empirical question. De-register once CAP-1's result is recorded and the operator's CAP-2/CAP-3 choice is settled.
- (decision by claude) 2026-09-09 OQ-1 ANSWERED by operator (fleet-readiness-decision-batch-2026-09-09 row mars-A-B6 / C10): RUN THE PROBE, AND PLAN FOR A NO. No in-repo evidence points either way and no probe artifact exists, but the two surfaces differ in the way that matters: the headless profile that works is `cursor-agent -p --trust --force` (_bmad-output/harness-profiles/cursor.toml), a non-interactive one-shot where the agent owns the whole turn, while Cursor interactive chat is a human-in-the-loop editor session whose parallel-agent features are session-level fan-out of the SAME prompt, not a model-invoked tool returning a synchronous result in-turn. Budget the probe at one session and treat CAP-3 as the likely branch. Rejected: skip the probe and adopt CAP-3b unconditionally -- it forfeits this Dream own stated discipline and would be the second time this estate declared a capability without exercising it.
- (decision by claude) 2026-09-09 OQ-2 ANSWERED by operator (fleet-readiness-decision-batch-2026-09-09 row mars-A-B7 / C10): if the probe fails, take CAP-3b NOW (restrict Cursor-interactive-chat skill access to skills that do not mandate a subagent, e.g. plain bmad-build) and name CAP-3a as a FOLLOW-ON -- do not ship 3a as the first move. CAP-3a re-implements the review fan-out at a second seam and this estate has been burned there twice: auto-memory feedback_bmad_dev_auto_needs_non_fork_agent.md (bmad-build-auto mandatory subagents break inside a fork) and feedback_fork_ignores_scope_inside_bmad_dev_auto.md (a fork ignoring declared scope). A shelled-out `cursor-agent -p` subagent is the same class of substitute reviewer with a process boundary instead of a fork boundary and no proof it preserves context-freeness. CAP-3b costs one .mdc allowlist and keeps the HALT honest. If 3a is ever chosen it needs its own equivalence evidence before any story dispatches under it.
- (decision by claude) 2026-09-09 OQ-3 ANSWERED by operator (fleet-readiness-decision-batch-2026-09-09 row mars-A-B8 / C10): .mdc generation is a MAINTAINED PIXI TASK PLUS A DRIFT DETECTOR -- never a one-time operator script. This Spec own Constraint already forbids what a one-time run produces ("never a hand-maintained fork that can drift"), and a one-time script BECOMES that fork the first time a SKILL.md changes. The repo has both the pattern and the enforcement shape (llms-full-check, bmad-drift-check) and a documented three-place rule for a new CI script. Auto-memory feedback_derive_dont_declare.md: hardcoded lists omit the newest thing. Accepted narrowing: a one-time script for a two-skill pilot is acceptable ONLY if the generated .mdc files stay untracked so they cannot silently rot in git.
- (decision by claude) 2026-09-09 OQ-4 ANSWERED by operator (fleet-readiness-decision-batch-2026-09-09 row mars-A-B9 / C10): ASSUME NO INLINE-IMPORT ANALOG to Claude Code `@path`; carry team memory by `alwaysApply` rule content plus explicit @file attachment, and VERIFY IT IN THE SAME PROBE SESSION AS CAP-1 (same ten minutes, same chat panel; running it separately doubles the setup). The repo already proves the loaders differ and already worked around it once: .cursor/rules/specs.mdc uses alwaysApply: true plus a plain-prose instruction ("Read AGENTS.md at the repo root first") rather than an import, while CLAUDE.md uses a bare @.claude/memory/MEMORY.md line whose own comment warns it must stay un-backticked to remain an import. Cost recorded honestly: Claude Code inlines ~40 index lines at load; a Cursor rule that says "read this file" spends a tool call and can be skipped.
- (event by claude) status: draft -> ready -- all four open questions are answered by the operator batch above (fleet-readiness-decision-batch-2026-09-09 rows mars-A-B6..B9 / C10), which is the condition the draft status recorded ("nothing chosen yet: CAP-1 gates every other CAP"). The shape is now chosen: probe first, plan for NO, CAP-3b as the fallback, .mdc generation as a maintained task plus detector, no inline-import analog assumed. The doctor DEFERRED_SPECS entry stays live (a `ready` Spec IS in OPEN_SPEC_STATUSES) until the probe runs and decomposition begins. docs/dreams/bmad-cursor-interactive-routing.md flips dreamt -> specified in the same pass.
- (event) (self-validate pass 1 -- coherence) 2026-09-09 re-derive: PASS. status draft -> ready; all four questions retired into a dated closed-questions section carrying each answer, its reasoning and its rejected alternative; the DEFERRED_SPECS-stays and Dream dreamt->specified facts render as Assumptions. 4 CAP ids, unchanged; nothing decomposed yet, so no Decomposition section is invented.
- (event) (self-validate pass 2 -- coherence) 2026-09-09 re-derive pass 2 (preservation): PASS. All five 2026-09-09 entries landed, including the OQ-3 accepted narrowing (a one-time two-skill pilot script is acceptable only if the generated .mdc files stay untracked) and the OQ-4 honest cost note. Only superseded frontmatter removed.
- (event) 2026-09-15 CAP-1 probe RAN in Cursor Agent interactive chat (not cursor-agent -p headless; not a tool-less Ask panel). Surface: model-invoked Task tool (subagent_type generalPurpose). First call agent 56105fa3-c052-429d-9061-d852e0640e2b: fixed add/return-a-b CONTENT, token HUNT7K2Q returned in the same turn with defect named (subtraction). Second call agent e74b7cd1-149c-4d18-b21f-06dcfd7adf6b: PARENT_USER_QUERY=UNKNOWN (clean leak check; first CONTEXT_LEAK=YES was tainted because the parent prompt named marshal/steward/BMAD). Verdict: PASS — genuine context-free subagent, synchronous in-turn result. Caveat: this is Cursor Agent+Task, not BMAD workflow.md Blind Hunter by name; Ask-without-Task was not probed.
- (decision) 2026-09-15 CAP-1 PASS commits CAP-2 (OQ-1 planned for NO; empirical result is YES on Agent+Task). CAP-2 is now committed work: maintained pixi task + drift detector generate .mdc from SKILL.md for the bmad-build / bmad-build-auto pilot pair so Cursor chat runs the same render_skill.py -> workflow.md path.
- (decision) 2026-09-15 CAP-3 is residual, not the primary branch. CAP-3b still applies on any interactive surface that lacks a model-invoked Task tool (Ask panel, no-tools): restrict routing / keep the workflow HALT blocked/no subagents — do not silently skip review. CAP-3a (headless cursor-agent -p as substitute reviewer) stays a later empty follow-on; no equivalence evidence, do not ship first.
- (event) 2026-09-15 CAP-4 verified in the same session. Read of .claude/memory/MEMORY.md succeeded (team index present). Claude Code @path inline import has no analog: .cursor/rules/specs.mdc is alwaysApply true and points at AGENTS.md, it does not inline MEMORY.md. Carry team memory by alwaysApply pointer plus explicit @file / Read — a tool call, skippable. OQ-4 assumption holds.
- (event) 2026-09-15 leave DEFERRED_SPECS: probe recorded, CAP-2/CAP-3 shape settled (CAP-2 committed; CAP-3 residual). Decompose marshal Epic 45. Dream stays specified until the epic ships.
- (event) Stories 45.1-45.3 implement: scripts/bmad_cursor_mdc_check.py generates and drift-checks .cursor/rules/bmad-build.mdc and bmad-build-auto.mdc; pixi.toml tasks bmad-cursor-mdc-check and bmad-cursor-mdc-generate; tests/scripts/test_bmad_cursor_mdc_check.py. HALT blocked/no subagents on no-Task surfaces; no cursor-agent -p substitute; team memory via Read or @file.
