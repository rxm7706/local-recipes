---
topic: Planning-tree hygiene cleanup across all BMAD stations
goal: every station's _bmad-output/projects/<station>/ tree tells the truth about itself
updated: 2026-08-02T17:11
---

- (note) Source: docs/dreams/bmad-output-hygiene.md (owner: marshal, status: dreamt), backed by a 5-agent research-only audit run 2026-08-02 across all 9 BMAD projects (679 files), plus a same-session finding about stale PROJECTS.md Dream pointers.
- (decision) This is bounded repo-hygiene cleanup, not a product. Skip PRD/architecture/epics chain per the Dream's own suggested 'direct execution, if mechanical enough' branch; go straight from Spec to execution.
- (decision) Spot-verified before committing to capabilities (session date 2026-08-02): dead tests/pytest.ini/playwright.config.ts confirmed present at project root in exactly atlas, doctor, marshal, mason, scribe, steward, warden (7); absent in herald, genesis. planning-artifacts/sprint-status.yaml confirmed hollow (0%, empty arrays) and structurally identical in all 9 pyforge-* projects. Neither file is read by any tracked script (verified via grep) — only implementation-artifacts/sprint-status.yaml (Tier-3, different file, same name) and sprint-status-ledger.yaml are live-read by dashboard_drift_check.py/story_status_check.py/promote_sprint_status.py.
- (capability) CAP-1 dead-scaffolding removal: delete tests/, pytest.ini, playwright.config.ts from the 7 confirmed station roots (no real test_*.py inside; real suites live at src/shared/packages/pyforge-<station>/tests/). Success: none of the 7 dirs remain; pixi test discovery for local-recipes is unaffected (nothing pointed at these paths).
- (capability) CAP-2 hollow sprint-status.yaml removal: delete planning-artifacts/sprint-status.yaml in all 9 pyforge-* projects (unused dead stub; sprint-status-ledger.yaml is the live tracked twin already). Success: file absent in all 9; dashboard_drift_check.py and bmad_drift_check.py still exit 0.
- (capability) CAP-3 Genesis test-architecture.md fix: regenerate the one fabricated test-architecture.md the 2026-08-02 follow-up commit (2957718d4c) missed, to describe Genesis's real, current, constitutive scope ('ships no product') instead of the stale pre-split src/pyforge_genesis installer narrative. Success: file makes no claim contradicted by Genesis's own PRD/architecture.
- (capability) CAP-4 README placeholder fill: replace unfilled [role]/[responsibilities] template text in marshal, herald, genesis (confirmed) and doctor (equivalent placeholder) planning-artifacts READMEs with real per-station content; verify the remaining 5 stations' READMEs during execution and fix any found. Success: no station README contains a literal [role] or [responsibilities] token.
- (capability) CAP-5 orphaned single-file archival: git mv Atlas's RESUME-EPIC-10.md and Herald's intake-video-scripts-manticore-2026-07-31.md into the mirrored archive/_bmad-output/... path per the existing satellite-consolidation convention (never delete outright). Success: both files present under archive/, absent from their original path, zero remaining repo references to the original path.
- (capability) CAP-6 project-context.md drift fix: regenerate Mason's and Herald's project-context.md in place (sole copy at each path, not archived) so claimed story counts (Mason 38/38 -> Dream's real 4/38; Herald 17/17 -> Dream's real 4/17) match their Dream/sprint-status-ledger reality. Success: both files' claimed counts match sprint-status-ledger.yaml.
- (capability) CAP-7 PROJECTS.md Dream-pointer fix: in _bmad-output/PROJECTS.md's Projects table, repoint mason's Dream: from docs/dreams/packaging-factory.md (a type:practice satellite that scope-noted itself out of the station chain 2026-07-25) to docs/dreams/pyforge-mason.md; repoint herald's Dream: from docs/dreams/design-code-bridge.md (no longer exists) to docs/dreams/pyforge-herald.md. Success: both pointers resolve to an existing type:dream file that is each station's actual charter.
- (constraint) Every change lands only under _bmad-output/, docs/dreams/, and this branch's docs — nothing in recipes/ or pixi.toml is touched, so the maintenance-label PR gate applies but the pixi/environment.yaml sync gate does not.
- (constraint) Deletions (CAP-1, CAP-2) require a live grep-verified 'nothing reads this path' check immediately before removal, not just trust in the Dream's original audit — the audit is same-day but this spec must re-confirm at execution time since tooling can change underfoot.
- (constraint) Archival (CAP-5) always uses git mv into the mirrored archive/ path, matching the convention already established by today's satellite-Dream consolidations; nothing in this spec is ever hard-deleted except the two confirmed-dead, confirmed-unread stub file classes in CAP-1/CAP-2.
- (non-goal) Not in scope: pyforge-marshal's separately-flagged legacy loose-file PRD/architecture duplicates (PRD.md, architecture.md, architecture-*.md, product-brief-pyforge-marshal.md at the marshal planning-artifacts root, superseded by the dated prds/ and architecture/ subdirs). Flagged in the same conversation but the user has not yet decided whether to fold it into this cleanup or handle separately.
- (non-goal) Not in scope: no new PRD, architecture, or epics chain for this cleanup itself; no changes to recipes/ or the conda-forge-expert skill; no changes to any station's actual product code.
- (question) Open: should the marshal loose-file PRD/architecture duplicate debt (flagged above as a non-goal) be folded into this same spec as CAP-8, or tracked separately? Left for the user.
- (event) Self-validate Pass 1 (coherence, rules 1-6+8): PASS. All 7 capabilities carry intent+success; constraints each rule something out; 3 non-goals present; success signal is concrete (re-audit + drift-checker exit codes); CAP IDs unique.
- (event) Self-validate Pass 2 (preservation): PASS. All 3 Dream clusters map to capabilities (Cluster 1 -> CAP-1..4, Cluster 2 -> CAP-5, Cluster 3 -> CAP-6/7). Dream's 'Confirmed clean, no action needed' section intentionally has no capability (negative finding, nothing to build/verify against) -- not a preservation drop.
- (event) Spec finalized: status ready, 7 capabilities, 0 companions, 1 open question (marshal loose-file duplicate debt fold-in), 1 assumption (delete-safety grounded in live grep verification).
- (event) CAP-8 investigation before execution overturned the approved framing: the loose top-level files are NOT stale marshal duplicates. 12 of them (architecture.md + its 4 parts + integration-architecture.md, PRD.md, validation-report-PRD.md, implementation-readiness-report.md, index.md, project-overview.md, source-tree-analysis.md, deployment-guide.md, development-guide.md, project-parts.json) plus SYNC-RUNBOOK.md at the project root declare project_name/project: local-recipes in their own frontmatter -- they are local-recipes' entire PRD/architecture doc set, misfiled under pyforge-marshal's tree (no .bmad-config.toml or planning-artifacts content exists yet under projects/local-recipes/ besides a specs/ dir). Matches the exact marker/symlink-desync hazard CLAUDE.md already documents; independently corroborated because CLAUDE.md's own text cites SYNC-RUNBOOK.md at the local-recipes path, but it physically resides under pyforge-marshal. product-brief-pyforge-marshal.md, by contrast, IS marshal's genuine sole brief (cited as a fully-absorbed input by both the canonical prd.md and architecture.md) -- just unsharded (no briefs/brief-pyforge-marshal-<date>/ subdir), a layout debt not a duplicate. Paused execution to report this to the user before proceeding -- the correct fix (relocate local-recipes' set to its own project tree) is materially different from what CAP-8 originally scoped (archive stale duplicates).
- (decision) User decision: CAP-8 relocates (git mv, preserving history) all 13 misfiled local-recipes files to _bmad-output/projects/local-recipes/ -- 12 planning-artifacts files land in local-recipes/planning-artifacts/, SYNC-RUNBOOK.md lands at local-recipes/ project root (mirroring pyforge-marshal's current root placement and CLAUDE.md's documented path).
- (capability) CAP-9 marshal-brief layout fix: git mv product-brief-pyforge-marshal.md to briefs/brief-pyforge-marshal-2026-07-25/brief.md (matches created: 2026-07-25 in its own frontmatter and every other station's briefs/brief-<slug>-<date>/brief.md shape). Success: file lives at the sharded path; no remaining reference to the old loose path.
- (event) SPEC.md re-derived: 9 capabilities, status ready, 0 open questions. CAP-8 corrected to a relocation (not archival); CAP-9 added for the marshal-brief layout fix. Both approved by the user before execution.
- (event) CAP-8 self-correction: executed the relocation (git mv 13 files to local-recipes/), then found scripts/bmad_drift_check.py line 50-53 states explicitly 'The factory document set moved local-recipes -> pyforge-marshal on 2026-07-28: the placeholder project dissolved under Charter S5, and this doc set IS the factory rebuild spec, owned by Marshal via the regenerable-factory practice.' The files were NOT misfiled -- they were correctly relocated to pyforge-marshal 2026-07-28, and I had it backwards. Reverted the git mv immediately (files restored to pyforge-marshal/planning-artifacts/ and project root) before this branch was pushed or merged anywhere; pyforge-marshal's pre-existing local-recipes/planning-artifacts/specs/ dir was untouched by the revert. Ran pixi run -e local-recipes bmad-drift-check -- --integrity-only post-revert: passes at the same baseline (21 pre-existing HARD findings, all in Cluster 1/CAP-1/CAP-2's scope, none related to this file set).
- (capability) CAP-8 REVISED (was: relocate 13 files; now: fix CLAUDE.md's stale prose). CLAUDE.md lines 102 and 107 said '_bmad-output/projects/local-recipes/' for the factory-doc set and its SYNC-RUNBOOK, but that set moved to pyforge-marshal on 2026-07-28 (per bmad_drift_check.py's own comment) and CLAUDE.md was never updated -- this is what caused my initial misdiagnosis. Fixed both mentions to pyforge-marshal. Success: CLAUDE.md's own text matches where scripts/bmad_drift_check.py, pixi.toml, and docs/dashboard/generate.py actually read from.
- (constraint) User mid-execution directive: every removal in this cleanup (CAP-1 dead scaffolding, CAP-2 hollow sprint-status.yaml) uses git mv into a mirrored archive/ path, never a hard delete -- overrides the earlier CAP-1/CAP-2 'delete outright' framing even though those two file classes were confirmed dead and confirmed unread.
- (event) SPEC.md re-derived: CAP-1/CAP-2 switched from delete to git-mv-into-archive/ (mirrored archive/_bmad-output/projects/<station>/... path, matching the existing consolidation convention found on disk at archive/_bmad-output/projects/pyforge-{atlas,mason,herald,marshal}/); CAP-8 rewritten to the CLAUDE.md pointer fix; constraints/non-goals/assumptions updated to match.
- (event) CAP-4 corrected mid-execution: the literal [role]/[responsibilities] placeholders are in each station's PROJECT-ROOT README.md (identical template, 8 of 9 stations, all but atlas), not planning-artifacts/README.md as the Dream implied. Also found: those root READMEs describe the CAP-1-archived tests/pytest.ini/playwright.config.ts as live. Fixed all 8 (7 mechanical + custom genesis rewrite for its ships-no-product status) plus rewrote the 6 generic (non-placeholder-token but still boilerplate) planning-artifacts/README.md stubs. Also found and fixed a third stale PROJECTS.md row (genesis's own entry still described the pre-split installer narrative CAP-3 already corrected) -- folded in as it's the same root cause as CAP-7.
- (event) CAP-6 executed: live-recount via sprint-status-ledger.yaml showed the Dream's cited counts already stale (Mason 4/38->4/48, Herald 4/17->4/27; total story inventories grew same-day). Both project-context.md files were also fabricated beyond the count -- generic role labels and dead SPEC.md/architecture.md reference paths. Fully regenerated both with live counts, accurate roles, and correct paths to the dated prd/architecture subdirs.
- (event) All 9 capabilities executed. Verified: pixi run -e local-recipes bmad-drift-check --integrity-only (3 HARD, pre-existing unrelated detector-coverage gap) and dashboard_drift_check.py (1 pre-existing unrelated genesis twin-missing finding) both clean of anything this spec touched. Ready for retro/PR.
- (event) Dream marked realized; SPEC.md status ready -> shipped. All 9 capabilities closed.
- (note) Reopened: user reported dashboard out of date, missing artifacts, gap -- for the 8 primary stations (atlas, doctor, herald, marshal, mason, scribe, steward, warden).
- (decision) Verified against docs/dashboard/generate.py's fleet scan, comparing main's committed data.js (pre-branch baseline) vs a fresh local regeneration. Confirmed herald gaps:['brief','arch'] is pre-existing on main, not branch-introduced. The other 7 stations show gaps:[] -- their partial:['research'] (inherited-research, coded as intentional) and staleBy currency findings are expected noise (some pre-existing, some a natural correct consequence of this branch's own commits touching files today), not defects to fix.
- (capability) CAP-10 herald brief/architecture directory rename: git mv briefs/brief-herald-pitch-2026-08-01/ -> briefs/brief-pyforge-herald-2026-08-01/ and architecture/architecture-herald-pitch-2026-08-01/ -> architecture/architecture-pyforge-herald-2026-08-01/, matching prds/prd-pyforge-herald-2026-08-01/'s already-correct convention and _stage_globs()'s brief-pyforge-<slug>-*/architecture-pyforge-<slug>-* pattern. Update live citations (project-context.md, prd.md inputs, spec sources) to the new paths; leave dated historical/narrative references (dream files, archived snapshot) untouched. Success: docs/dashboard/generate.py --source sprint-status reports pyforge-herald gaps: [] .
- (constraint) docs/dashboard/data.js is GENERATED (regenerate with generate.py, do not hand-edit) -- regenerate and commit it as the final step of CAP-10, not edited directly.
- (event) SPEC.md re-derived: status shipped -> in-progress, CAP-10 added, data.js-is-generated constraint added.
- (event) CAP-10 shipped and verified (herald gaps: [] confirmed, dashboard regenerated). Dream re-closed realized; Spec status -> shipped. 10/10 capabilities done.
- (note) Reopened again: user reported every station reading outdated after CAP-10's regeneration.
- (decision) Root-caused to _currency()'s _FEEDS check (docs/dashboard/generate.py) having zero grace period. Every spec/prd finding across all 8 stations is 0-1 days -- a spec's .memlog.md gets a fresh updated: stamp on every append, far more often than its PRD, making spec-newer-than-prd true by construction. 5 pairs are genuinely stale (multi-day, predate this branch): warden prd/arch 16d + prd/gates 18d, doctor prd/arch 8d, scribe prd/arch 7d, steward prd/arch 7d, mason arch/epics 8d. User decision: fix both the detector and the artifacts.
- (capability) CAP-11 currency grace period: add a small grace period (2 days) to _currency()'s _FEEDS loop in docs/dashboard/generate.py -- only flag a feed pair when the upstream stage is MORE than the grace period newer than downstream, not any positive difference. Preserves the existing behind-code suppression logic and shelf-life checks untouched. Success: re-running generate.py, the 0-1 day spec/prd findings disappear from all 8 stations; the 5 genuine multi-day pairs still appear.
- (capability) CAP-12 catch up genuinely stale pairs: for warden (prd vs arch, prd vs gates), doctor (prd vs arch), scribe (prd vs arch), steward (prd vs arch), mason (arch vs epics) -- read both sides of each pair, confirm the downstream artifact's content still accurately reflects the upstream artifact (no real requirements drift), then re-stamp the downstream artifact's dated re-validation the same way this repo's own PRD.md convention already does (a dated re_validated:/pin-forward note), rather than silently editing dates. If content genuinely has drifted, note it rather than papering over it. Success: after CAP-11's grace period, these 5 pairs show zero staleBy findings because the dates now genuinely reflect a just-verified-current state, not because the check was loosened past them.
- (event) SPEC.md re-derived: status shipped -> in-progress, CAP-11 and CAP-12 added.
- (event) CAP-12 executed with a mid-flight scope refinement, per each station's own PRD currency_review note: scribe/steward/warden's PRD bumps were confirmed PURELY STRUCTURAL by their own existing currency_review field (project relocation, not content change) -- safe to re-verify architecture content against the unchanged PRD and re-stamp. Doctor's PRD bump was NOT structural -- its own currency_review says a real dream-consolidation pass added FR-10..13, so doctor's architecture genuinely has fallen behind; left unfixed, real drift honestly surfaced, not papered over (out of scope for a hygiene spec -- needs actual architecture authoring). Mason's architecture (FR-1..46) vs PRD (frCount 50) is a genuine 4-FR gap, same reasoning, left unfixed.
- (event) Stamping architecture's updated: field for scribe/steward/warden pushed staleness downstream to arch/epics (a new finding that didn't exist before, since epics.md was now older than the freshly-stamped architecture). Fixed properly: verified epics.md content is also unaffected (each already decomposes the architecture's full FR set), then added a matching currency_review stamp to epics.md too, closing the chain honestly rather than leaving a moved goalpost.
- (event) warden prd/gates (implementation-readiness-report, 16d) deliberately left unstamped: readiness reports are point-in-time snapshots, not living documents -- bumping their date would misrepresent that a new gate check happened when none did. This is a structural property of the artifact type, not a defect; noted for a possible future generate.py refinement (exempt 'gates' from the feeds check, or treat it as shelf-life only) but out of scope here.
- (event) Final verification: fleet currency findings 16 -> 4 (atlas/herald/marshal/scribe/steward fully clean; doctor/mason/warden-gates left as 3 genuine, honestly-surfaced findings). No gaps anywhere. docs/dashboard/data.js regenerated.
- (event) CAP-11/CAP-12 shipped and verified. Dream re-closed realized; Spec status -> shipped. 12/12 capabilities done.
- (event) User caught a CAP-12 scoping oversight: genesis was excluded from the currency sweep (only checked the 8 non-constitutive stations), but it has its own prd/arch currency pair and was flagged stale (4d) too. Checked genesis's PRD currency_review -- same structural-only pattern as scribe/steward/warden. Re-stamped architecture.md and epics.md (both already say 'nothing here' -- constitutive, ships no product -- confirmed still accurate). Currency findings 4 -> 3, genesis now clean. Doctor/mason/warden's 3 findings remain, unchanged and still intentional.
