---
topic: Factory console (docs/dashboard): brownfield backfill kernel under regenerable-factory Wave 2
updated: 2026-07-29T04:40
---

- (direction) Backfill per spec-regenerable-factory CAP-2: spec describes what IS (console shipped pre-model, dream realized); kernel + exact behavioral contract of generate.py in companion console-contract.md so CAP-4 drill can rebuild it blind
- (capability) CAP-1 local status sync; CAP-2 CI git-derived refresh (upgrade-only); CAP-3 dreams scan (title/status/owner + warnings); CAP-4 stable data.js contract (only story statuses, dreams, snapshot timestamp mutated - narrative preserved)
- (constraint) stdlib-only; git mode never downgrades; Pages workflow uploads ONLY docs/dashboard (security boundary - repo private, page public); consumers parse data.js by stripping the window.DASHBOARD_DATA prefix
- (event) Self-validate pass 1+2: PASS - caps intent+success; contract companion normative and self-contained; preservation - every generate.py behavior (parsers, regexes, mutation scope, prints) landed in console-contract.md
- (event) rf(2.2) regeneration drill PASS: clean-room rebuild from console-contract.md alone -> data.js byte-equivalent (timestamp-normalized); evidence in drill-evidence.md; original restored to production
- (decision) rf(5.3) drill-through: dreams gain chain dict (deck/spec/project exact-slug links) rendered as glyph indicators + tooltip paths; data.js declared surface-drift-exclude (generated artifact, coverage still binds); contract companion updated
- (decision) BUG FIX (user-caught): git-mode done-detection pooled ids across projects - regen rf(5.1)/rf(5.2) commits falsely flipped WARDEN 5.1/5.2 done on the live site. done_ids_from_git now returns per-project sets (warden=Merge-bmad-loop only, atlas=story()/GN: only, regen=rf() only); contract companion updated
- (decision) User-driven board fixes: (1) dream chain gains DECK ALIASES (mason/agentic-sdlc/crew decks now marked) + program key -> live done/total badge on chips (answers 'built out but board does not show it'); (2) doctor/scribe/steward dreams advanced seeded->in-deck (statuses had lagged their decks); (3) warden velocity backfilled from run journals (6.3=265min, 6.4 bar added; 6.10/6.2 journals unavailable - noted, not invented)
- (decision) UX fix (user read 'in spec' as 'has a spec' 3x): stage display label in-spec -> 'in build' (the stage means specced-and-executing); dflow legend now states furthest-stage semantics + glyph chain; frontmatter values unchanged
- (decision) Console IA v2 (user, 2026-07-24): six sections — The Dreams (all 25, stage-chipped, dual-appearance OK) / The Specs (all _bmad-output/*/planning-artifacts/specs kernels; docs/specs legacy excluded) / The Pitch (deck-family cards, n/6 artifact checklist) / In Build (live program consoles) / Realized (shipped consoles, collapsed) / Archived (absorbed+retired+terminal+blocked; seeded cases + status:archived frontmatter forward)
- (decision) Pitch cards link to LOCAL repo paths (presentations/<slug>), not claude.ai/design — user override 2026-07-24
- (event) Console v2 shipped 2026-07-24: six-section IA live on Pages; scanners specs/pitch/archived added to generate.py; pitch links repo-local per user override
- (note) Governed surface moved 2026-07-25: docs/dashboard gained the Fleet view (scan_fleet + section + renderer) and the Spec-vs-kernel terminology sweep; console masthead renamed to PyForge · Guildhall.
- (note) Governed surface moved 2026-07-25: docs/dashboard gains a **Sync & Health** section — the three standing detectors (bmad-drift-check, spec-surface-check, llms-full-check, ~1s each) with verdict + finding count, plus the baseline fingerprint delta vs live. Partially realizes this Dream's own frontier item ('fleet health strip from pyforge-doctor'). A detector that cannot run (CI without pixi/atlas) reports 'unknown' rather than claiming green; the baseline comparison uses the factory FINGERPRINT, not commit counts (the baseline commit is often off main's first-parent chain in this staged-recipes fork, making counts meaningless).
- (note) UI consistency 2026-07-25: In Build / Realized / Archived now use the same panelled-collapsible pattern (dreams zsec) as the six sections above them — previously they were collapsible but unpanelled (bare headers), the only inconsistency left on the page. Archived's nested inner .dreams panel dropped to avoid double-framing; nested consoles step back to --panel-2.
- (note) UI fix 2026-07-25: the In Build console's in-flight chip ('1.3 in flight') overlapped the progress bar — the summary's first grid column was fixed at 100px while .pname held dot+label+chip, and .runchip's margin-left:auto (correct for story rows) shoved it to the column edge. Name column is now minmax(100px,max-content); the chip sits beside the label.
- (note) Console honesty + UI fix 2026-07-25: every In Build/Realized row now carries a state chip (in flight | paused · <resume story> | ready | complete) so the summary grid is aligned whether or not a line runs. Root fix behind it: 'in flight' was derived from a run directory being <12h old, so an 8.5h-DEAD run still read as running — the console claimed motion it had not measured. Now gated on a LIVE tmux loop session (fallback: run dir <30min); paused rows name their resume story from the sprint feed.
- (decision) Station ownership is propagated, not re-declared: one apply_owner() pass resolves every downstream row (Fleet, In Build/Realized, Pitch, Archived) from the Dream's owner: frontmatter, so the mapping lives in exactly one place and cannot drift per-section. owner: was already authored on all 26 Dreams and then DROPPED — no downstream section carried it, so the board showed a Dream with an accountable station and the same effort in build with none.
- (decision) Campaigns are deliberately left UNOWNED. A campaign spans many stations (spec-completion touched all eight), so stamping a single owner would be a lie. Recorded in code as a comment so a future pass does not 'fix' the omission.
- (constraint) smithChip() renders an unowned row as an amber 'unowned' chip rather than blank — a missing station must be VISIBLE, not silently absent, since that is the exact failure the station model exists to prevent. guild renders dashed (constitutive, legitimately station-less).
- (decision) Dream lifecycle re-stated so every state names the ACT THAT COMPLETED, not the artifact proving it: seeded->dreamt, in-deck->pitched, in-spec->specified (realized/archived unchanged). in-deck and in-spec were the only two naming a file/place rather than a state, and 'in the deck' reads colloquially as 'shuffled in, queued' — near the opposite of 'the case has been made'. 'specified' over 'specced' because spell-checkers flag the latter and this vocabulary renders into decks and enterprise docs.
- (decision) NO 'building' status, deliberately. Status declares what EXISTS (stable, honestly hand-declarable); the board DERIVES what is happening from live build lines. Evidence: hand-maintained activity-tracking statuses rot — pyforge-warden read 'in-spec' while shipped 31/31 (PR #110), deckcraft read 'dreamt' while holding both a deck and a Spec. Both found and corrected in this pass.
- (decision) Backlog is DERIVED, not declared: dreamt+pitched+specified, minus any Dream with a live/paused build line, minus type:practice. The naive 'not in Realized and not In Build' does not hold — those sections are driven by the 6 wired build lines, not the 26 Dreams, so it swept in 7 realized-but-never-a-line Dreams (factory-console, modernist-identity, design-code-bridge...), every future archived one, and all 5 practices. Bucketing off the Dream's own lifecycle is total and non-overlapping.
- (constraint) Two qualifiers sit OUTSIDE the lifecycle rather than becoming statuses: type:practice (perpetual concerns tended-never-finished — excluded from backlog because nobody can close them, and from realized because they are never done) and blocked-on: (splits backlog into available vs waiting, since backlog implies pickup-ready). blocked-on applies only to non-realized Dreams; marshal's BLOCKED-ON review is on the package, not the Dream, which is genuinely realized.
- (decision) New Guild section: one row per Smith, all eight always rendered (including empty), expandable to the Dreams each owns. This is the accountability view the owner: through-line exists for — grouping by station answers 'what is Atlas answerable for and where does each piece stand', which no existing section could. Rendering empty stations makes a thin station VISIBLE rather than absent: warden and doctor own one Dream each while marshal owns six.
- (constraint) 'guild' is NOT a ninth station and must never render as a row. It marks the one Dream that CONSTITUTES the stations (the Charter), so it sits above the roster in a dashed constitutive strip, not beside it. pyforge-genesis was briefly owner:guild and that was wrong — its origin doc is Marshal's own bmad-setup-plan.md and 'the bootstrapper that installs the operating model anywhere' is Marshal's craft; reassigned to marshal. Constitutive Dreams are also excluded from Backlog: they are no station's pending work.
- (decision) In Build / Realized show PRODUCT lines only; a line whose Dream is type:practice is skipped. regenerable-factory was rendering as a shipped program in Realized, but 'every line of code under a spec it can be rebuilt from' is a standing property — spec_surface_check must claim every new surface forever, so it is tended, never shipped. Its 4 CAPs / 14 stories stay in the data (proj.practice flag, not deletion); only the zone display changes.
- (decision) Archived is now driven ENTIRELY by Dream frontmatter; the hardcoded ARCHIVED_SEED list is retired. Its 5 entries became real archived Dreams (sentinel absorbed; microsoft-org-sweep absorbed into trendshift Track B; artifact-console retired by the Guildhall; db-gpt-packaging terminal via external PR #33883; copilot-cli-packaging blocked on LICENSE §2). This makes 'archived' a REACHABLE state — it had been vestigial, with zero Dreams ever carrying it — and fixes Sentinel rendering simultaneously as a live backlog Dream AND an archived entry.
- (constraint) blocked-on: is a LABEL, not a paragraph — the long-form reason belongs in the Dream body. presenton's 130-char value was being dumped into an auto-width grid cell, blowing out the row. Frontmatter now carries 'Phase-0 decision gate (Epic 1)'; the badge shows 'blocked', the gate name renders as a muted sub-line, and the full sentence lives in the title tooltip.
- (decision) Badge the EXCEPTION, not the norm: 'available' (9 of 10 backlog rows) no longer renders any text — absence means available. Only blocked gets a badge, styled in the same pill language as .smith/.rbadge rather than raw amber text. Row dimming (opacity .72) removed: it made the blocked row HARDER to read, which is backwards for the one row that needs attention.
- (constraint) Guild state columns render FULL WORDS, never abbreviated. The abbreviated header (drmt/ptch/spec/real/arch/prac) collided with the Fleet matrix, which already uses spec and arch for CHAIN STAGES (Spec authored / architecture authored) — so one board carried two meanings for 'arch' (archived vs architecture), and the Guild header read like a 9-stage chain that had dropped research/brief/prd/epics/code. The two matrices are DIFFERENT AXES: Fleet = 9-stage Dream-to-Code chain per project; Guild = counts of Dreams per state. Caption now says so explicitly.
- (decision) Fleet gains two row markers, both derived not declared. backfilled = the row's stage dates do not ascend, meaning the chain was retro-fitted after the fact — styled NEUTRAL (dashed, faint) because docs/dreams/README.md mandates it ('realized is not exempt from the chain'); warden shipped from a 07-14 PRD then got its Dream 07-23 and Spec 07-25. That is true history, not an error, and it was previously invisible. overtaken = the Spec still carries unresolved open_questions while its PRD AND architecture already exist — styled as a WARNING, because that is downstream passing the contract by. 5 backfilled, 1 overtaken (pyforge-atlas, 4 questions).
- (decision) Guild gains 'building' and 'backlog' columns: it counted Dreams but said nothing about activity, so a station could own four Dreams with nothing running and read as busy. Both derived — building from live build-line state (never declared), backlog from the Dream lifecycle. This is what makes a thin station meaningful: warden owns ONE Dream and has ZERO pending, which is a different fact from merely being small. Column order also fixed: practice sits with the ACTIVE states, before archived — archived is terminal and belongs last; a practice is tended, not ended.
- (decision) Build lines are DERIVED from epics.md, not hand-maintained. data.projects previously carried a hand-copied epic/story skeleton, so it could drift from epics.md silently AND a project with no entry simply did not exist on the board — which is exactly what happened: marshal, mason, steward and genesis all had epics (two had shipped code) while In Build showed nothing, because nobody had hand-added them. scan_projects() now derives the structure per project and carries forward only EDITORIAL fields (label, contract blurb, accent, seglabels, lineState). A new project appears the moment it has an epics.md.
- (constraint) Two guards make the derivation safe, both added after it broke something. (1) Guard on STORIES, not epics: pyforge-atlas parses to 9 epics with ZERO stories because its story headings are A1/B1/0.1 rather than 'Story 1.1', and a naive 'if not epics' guard let that through and OVERWROTE 32 real stories. A convention mismatch now keeps the existing line and WARNS loudly. (2) check_project_coverage() warns when any project has an epics.md but no line — turning the silent omission that caused this into a visible failure.
- (decision) In Build shows STARTED lines only (done>0, or lineState in flight/paused). Deriving a line for every specced project meant 0/N 'ready' rows would appear in In Build while the same Dream sat in Backlog — the same work counted twice in two zones that mean opposite things. A line that has never started is Backlog; In Build means building. Zero double-counting after the fix (deckcraft, genesis, marshal, presenton correctly Backlog-only).
- (constraint) Loop homes are DISCOVERED, not hardcoded. LOOP_HOMES was a fixed dict of four sibling directory names and missed a live run two ways at once: a project absent from the list was invisible, and loop homes had moved to a SHORT root (~/.bmad-loops/<slug>, adopted to keep the pixi-build-python path-length panic unreachable) so the sibling paths no longer resolved at all. Marshal's first run was live and unrendered for exactly this reason. Both roots are now searched (BMAD_LOOP_HOME_ROOT or ~/.bmad-loops, plus legacy siblings), short root wins on collision. Same class of defect as the hand-maintained data.projects fixed earlier the same day: hardcoded where it should be discovered.
- (decision) Delivery timing and velocity are DERIVED from bmad-loop run journals, not hand-pasted. In Build rendered no clock and no velocity while Realized did, because warden's and atlas's numbers were computed once from these same journals and then written into data.js by hand; any line without that manual step showed bare. scan_timing() now computes active agent-compute per story as the sum of session-end minus session-start (dev + review). Gate-pause wait falls out for free because a pause happens BETWEEN sessions and is never inside a summed span — which is exactly warden's stated metric, so derived and curated numbers mean the same thing.
- (constraint) Hand-authored timing/velocity are PRESERVED, never overwritten by derivation. Warden's carry human judgement no scan can reproduce: 6.4's bar is its delivered dev-2 pass and deliberately excludes a rolled-back dev-1 that cost ~49 min more; 6.9's is recovered from a stalled session plus a manual adversarial review. Derivation fills gaps; it does not second-guess a curated number. Atlas keeps a wall-clock metric with no bars because it never ran through bmad-loop — there are no journals to derive from, which is correct rather than missing.
- (note) Pairing pitfall worth keeping: session-end carries ONLY task_id — no story_key (verified against live journals). The first implementation keyed spans on story_key and therefore closed nothing, deriving zero for every line while reporting no error. The story is carried on session-START and must be looked up by task_id at session-end. Same failure shape as the day's other silent-matcher bugs: the code ran clean and produced nothing.
- (constraint) A derived object must satisfy the renderer contract IN FULL or not be emitted at all. scan_timing emitted timing without perStory and velocity without foot; the renderer does p.timing && p.timing.perStory[key] and v.foot.map(...), so a PARTIAL object passes the truthiness guard and then throws a TypeError that aborts the ENTIRE script — In Build, Realized and Archived rendered empty. Enumerate every field the render touches (grep the render for p.timing. and v.) before emitting; do not fix them one at a time, which is what I did.
- (constraint) Derived output must stay REPAIRABLE. The first fix silently did not apply: scan_timing skips projects that already have timing, and the broken timing was by then in data.js. A derived:true marker did not rescue it either, because data written before the marker existed carried no marker and read as curated. The rule that works keys off the CONTRACT, not provenance: a timing dict without perStory is broken by definition and is regenerated regardless of markers.
- (note) VERIFICATION GAP now closed: all three detectors were green and the 1013-test meta suite passed throughout, because none of them execute the dashboard's JavaScript. The operator found the broken board, not my checks. A DOM-stub harness now evals the full inline script against live data.js and catches this class of error; timers are stubbed and it exits hard so a clean run is distinguishable from a hang.
- (decision) docs/dashboard/check_render.js added 2026-07-26 as a fourth standing detector (pixi task dashboard-check + a meta test, suite 1013 -> 1014). It stubs a minimal DOM, evals the real inline script against the live data.js, and exits non-zero on any throw. It asserts only that the code COMPLETES — not what the page looks like — because that is the failure mode that actually bit: a partial derived object threw a TypeError and blanked every section below it while all three doc detectors stayed green. Negative-tested by re-injecting the exact bug. Timers are stubbed so a clean run is distinguishable from a hang.
- (constraint) The PUBLISHED board and the LOCAL board derive DONE from different sources, and only the local one sees the truth. Local uses --source sprint-status (the feed knows every story); CI uses --source git and reconstructs DONE from commit-subject archaeology, because the sprint-status feed is a gitignored Tier-3 artifact CI cannot read. Every divergence between rxm7706.github.io and a local render traces to that asymmetry, so diagnose it there first rather than suspecting the deploy.
- (constraint) Attribution in --source git must be DERIVED from the merge target, never a hardcoded project list. done_ids_from_git hardcoded six buckets and a five-entry target tuple with a blanket `warden` fallback. The moment marshal existed, its `Merge bmad-loop/…/1-1-… into loop/pyforge-marshal` matched no entry, fell through the fallback and was CREDITED TO WARDEN — the published board showed marshal 0/41 against 1/41 locally, and warden read 28 when only 26 were real (a stray claude/pdos-1-3 branch was inflating it the same way). Buckets now seed from the live project set and the slug is parsed from the target; an unrecognised target WARNS and is skipped instead of being charged to warden. Third instance of hardcoded-list-omits-the-newest-thing on this console.
- (note) The target regex must be `pyforge-([a-z0-9]+)` with NO hyphen in the class. Slugs are single words but targets carry suffixes: `loop/pyforge-marshal` and `pyforge-warden-loop-policy-tiering` must both resolve. An `[a-z0-9-]+` class swallowed the whole tail, matched no project and silently dropped 10 real warden stories — caught only by diffing counts against the previous published board, not by any detector.
- (gap) mason and steward still read 0 done in CI. Their Story 1.1 landed as plain subjects ("mason + steward: Story 1.1 — two stations reach code"), matching no bmad-loop merge, no story() and no rf() pattern. Adding a `<slug>: Story X.Y` regex was considered and REJECTED: it would also fire on "marshal: Story 2.3 blocked". The durable fix is a tracked done-manifest rather than more commit-message archaeology; until then the published board undercounts hand-implemented stories and that is a known, bounded inaccuracy.
- (decision) --source git now recognises FOUR completion conventions, not one, closing the published-vs-local gap to zero on every project. A story lands on main in whichever shape its era used: (1) `Merge bmad-loop/<run>/<X-Y>-… into loop/pyforge-<slug>`; (2) a plain PR merge `from <owner>/build/pyforge-<slug>-<X>-<Y>` or `from <owner>/claude/pdos-<X>-<Y>` (warden's pre-rename Epic-1 family); (3) a bare `story N.N:` subject (RETIRED, warden-only, all six on main are its Epic 1); (4) a PROJECT-QUALIFIED `<slug>[ + <slug>]: story N.N`. Before this, only shape (1) was matched, so the board published warden 26/31, mason 0/38 and steward 0/18 against 31, 1 and 1 locally.
- (constraint) The qualified form must SPLIT on `+` and validate each token against the live project set. mason and steward's only completion signal is `mason + steward: Story 1.1 — two stations reach code`, which names TWO projects in one subject; every single-project regex missed it, which is exactly why two smiths sat in Backlog on the published board while showing In Build locally. Validating each token means an unknown prefix is ignored rather than misattributed — the same derive-and-verify rule that replaced the `warden` fallback.
- (note) Fixing attribution can SUBTRACT truth as easily as add it. The first pass correctly stopped crediting marshal's story to warden but also stopped crediting `claude/pdos-1-3-deptry-first-engine`, which IS warden's Story 1.3 (PR #54, tracked spec spec-1-3-deptry-as-the-first-engine.md). Warden went 28 -> 26 and the regression was invisible to every detector: the board still rendered, still looked plausible, and only a count-by-count diff against the previously published board caught it. Diff the OLD published numbers against the new ones before trusting an attribution change.
- (gap) Health detectors remain the one honest local/published divergence: CI has no pixi, so bmad-drift-check, spec-surface-check and llms-full-check report `detector could not run here` / state unknown on rxm7706.github.io while reading green locally. That is by design (a detector that cannot run must not claim green), but it means the published health panel is informational only — never read it as an all-clear.
- (decision) Health detectors now run DIRECTLY (interpreter + the task's own cmd read from pixi.toml), with `pixi run` kept only as a fallback for any task that is not a plain `python script.py`. All three are stdlib-only and shell out to `git` alone — bmad_drift_check even counts pixi envs by PARSING pixi.toml, never by invoking pixi — so requiring the launcher bought nothing and cost the whole panel: the published board reported "detector could not run here" for all three while they read green locally. Verified by running generate.py with PATH stripped to /usr/bin:/bin and no pixi at all: 0/3 -> 3/3.
- (constraint) The direct runner must DERIVE each command from pixi.toml, never restate it. A task's cmd is written down once, so the fallback cannot drift from what `pixi run` does; adding a detector needs no second declaration. Same rule that fixed the git-attribution buckets an hour earlier.
- (decision) CI installs pixi via prefix-dev/setup-pixi with a new LEAN `pyforge-ci` env = { features = ["python", "pyforge-ci"], no-default-feature = true } — 117 linux-64 packages / 682 MB against local-recipes' 1102 / ~9.8 GB. feature.python supplies pixi, bmad-method, nodejs, gh, gitpython, spec-kit, truststore; the new feature.pyforge-ci adds the BUILD+PUBLISH surface: hatchling, python-build, pytest, pixi-build-python, pixi-build-rattler-build, twine. Both referenced BY FEATURE with the repo's existing pins, so no second source of truth. Kept as its OWN feature rather than additions to feature.python, which nine envs share — adding there fattens all nine. Named pyforge-ci to sort with the eight sibling pyforge-* envs.
- (note) VERIFIED end-to-end that the lean env really can build every publishable artifact, not just resolve: using ONLY pyforge-ci, pyforge-mason produced a wheel, an sdist and a .conda (python -m build --no-isolation for wheel+sdist; pixi build for the .conda). Publishing coverage: PyPI = twine, GitHub release = gh, conda-forge = nothing needed (it is a PR to staged-recipes/the feedstock and THEIR CI builds and uploads — you never push an artifact). REMAINING GAP: JFrog conda upload has no tool in ANY feature (anaconda-client / jfrog CLI absent repo-wide), so that target is unmet everywhere, not just in CI.
- (note) Renaming an env rewrites its whole lock block: ci -> pyforge-ci showed 539 insertions / 390 deletions, which LOOKS like a broad re-resolve. It was not — a per-env package-count diff of HEAD vs the working tree confirmed all 17 pre-existing envs UNCHANGED, and `pixi project export conda-environment -e build` stayed byte-identical so the ungated environment.yaml gate needed no resync. Diff the counts before trusting or fearing a lock diff.
- (change) Charter §7 amendment (the Guildhall is the unit of accountability made real, not merely visibility): generate.py gains gate_ownership(), wired BEFORE the data.js write so a violation blocks publication rather than flagging a published page. Gates on unowned Dreams, owners outside the eight (+guild), blank fleet stations and unowned build lines. Corrects an inversion in which check_render.js exited non-zero on a JS TypeError while an ownership hole printed and exited clean.
- (change) Board unbroken after the 2026-07-28 project migration + an atlas sprint-key rename. THREE defects, all self-inflicted the same day. (1) The baseline card read `_bmad-output/projects/local-recipes/.sync-baseline.json`, a project retired hours earlier; the file was simply absent so `base` stayed empty, NO comparison ever ran, and the board still printed "fingerprint matches baseline" — a vacuous match reported as a real one. Now prints "NO BASELINE at <path> — nothing compared" when it read nothing, and repointed at pyforge-marshal. (2) drift-check's row cited the same dead project's SYNC-RUNBOOK.md as its remedy link. (3) Re-keying atlas's Epic-10 sprint entries (`i3-…` -> `10-4-…`) so bmad-loop's `^(\d+)-(\d+)([a-z]?)-(.+)$` could parse them broke the board's OWN status lookup, which maps a dashboard story id to a sprint key by prefix: `I3` -> `i3-`, matching nothing. Epic 10's statuses silently froze at hand-authored values — I3 read "pending" while it was merged and green. Board ids are now 10.1-10.6 with the spec IDs moved into the titles (D-2 makes them the name every planning artifact uses).
- (change) The in-flight card — elapsed clock, progress-vs-median bar, ETA — has ALWAYS existed in the renderer and NOTHING ever fed it. `apply_loop_inflight`, despite the name, only flipped a story pending->active; it never set the `inflight` object, so the card rendered solely from a hand-pasted value and vanished the moment nobody pasted one. Now derived from the live run journal: `story-start` gives the clock its origin, the last `session-start`'s task_id tail (`-<phase>-<attempt>`) gives phase + attempt. Emitted ALL-OR-NOTHING deliberately — the renderer guards `if (p.inflight)` then reads `f.median / f.hi` unconditionally, so a partial object passes the guard and throws, taking the whole board down (the same failure class already fixed twice here, for `timing.perStory` and `velocity.foot`).
- (change) Velocity was curated PER PROJECT, not per FIELD. Atlas carries a curated `timing` (wall-clock from PR timestamps — waves 0-H ran in a web session with no journals) and NO `velocity`, so either curated field made the deriver skip the whole line and atlas could never gain a velocity graph even once it had real journals. Guard is now per field: atlas gets derived velocity, its curated wall-clock timing is preserved, and warden — both fields curated — stays untouched, honoring the standing "never flip warden to timing.derived" rule.
- (change) Fixing the above then HID atlas's Wave roadmap: the bottom panel was `if (p.velocity) … else if (p.roadmap)`. They are COMPLEMENTARY (per-story compute vs where the programme is), and the else-if only ever worked because no line carried both. Caught before it shipped. Also added Wave I to atlas's rail as a `wip` stop and amended the caption, which still read "the migration was closed out … 32/32" — true when written, false once Wave I opened.
- (constraint) COVERAGE IS STATED, NEVER IMPLIED. A velocity graph showing 2 bars on a 38-story line reads as data loss. It is not: only loop-driven stories have journals, and atlas's waves 0-H were never instrumented for active compute. Their wall-clock numbers are a DIFFERENT metric (gate waits + idle included) and must not share the axis — B1's 227 wall-clock minutes beside 10.4's 68 compute minutes would look comparable and would not be, the same false equivalence atlas story I4 exists to remove. The sub now says "N of M stories measured" and where the rest live, derived from live counts so it self-corrects.
- (event) Detector fallout verified: dashboard-gen clean, `check_render.js` OK (12 lines, 32 dreams, 5 archived), atlas velocity 2 bars + in-flight card live on 10.5, warden untouched.
