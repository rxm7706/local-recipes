---
topic: Close the library-llms-full.md / llms_full_check.py blind spot to each pyforge-* station's own nested pixi.toml [package.run-dependencies] manifest
updated: 2026-09-13T08:33
---

- (direction) Operator (via Claude Code) directed closure of a manifest-sync gap found auditing library-llms-full.md's scope; owner-dream docs/dreams/library-catalog-manifest-sync.md (marshal), satellite of docs/dreams/pyforge-unifying-strategy.md.
- (decision) Binds pyforge-unifying-strategy (finding recorded there, 2026-09-12) and regenerable-factory (the two-layer detector/reconciler loop pattern this closes a blind spot in) by kinship; mints its own CAP-1/CAP-2 rather than adding an unrelated tooling CAP to either mega-spec.
- (capability) CAP-1: root pixi.toml and docs/reference/library-llms-full.md document the seven libraries every pyforge-* station's own nested pixi.toml [package.run-dependencies] already declares and each station's source already imports directly, but that never appear in root pixi.toml today: packaging (marshal, mason, warden; conda-resolved 26.3), jsonschema (doctor, marshal, warden; 4.26.0), psutil (marshal; 7.2.2), attrs (atlas; 26.1.0), packageurl-python (warden, imports as packageurl; 0.17.6), license-expression (warden, imports as license_expression; 30.4.4). Success: pixi run -e local-recipes llms-full-check exits 0 and each of the seven appears as a versioned floor >= its conda-resolved version above in both root pixi.toml (local-recipes plus each owning station's own [feature.pyforge-<station>.dependencies] block) and the catalog; environment.yaml regenerated if the build env's closure changed.
- (capability) CAP-2: scripts/llms_full_check.py's manifest_deps() also parses every src/shared/packages/pyforge-*/pixi.toml's [package.run-dependencies] table (tomllib, merged into the same active-dependency set the root-pixi.toml walk already builds), so a station declaring a real run-dep in its own manifest without mirroring or documenting it fails the check the same way an undocumented root pixi.toml dep does today. Success: a synthetic station manifest entry not mirrored in root pixi.toml or the catalog is caught as undocumented-dep by a run of llms-full-check exercising the new code path (a unit/meta test), and the existing exit-code contract (0 clean / 1 drift / 2 missing input) and the commented-out-deps-are-out-of-scope rule are unchanged and verified still passing for the pre-existing root-pixi.toml-only checks.
- (constraint) Floors are set to the version pixi.lock already resolves on the reference machine today (packaging 26.3, jsonschema 4.26.0, psutil 7.2.2, attrs 26.1.0, packageurl-python 0.17.6, license-expression 30.4.4) -- never invented, never re-solved to a newer upstream release as part of this fix.
- (constraint) Detector CLI contract stays stable: pixi run -e local-recipes llms-full-check invocation, --json flag, and exit codes (0/1/2) are unchanged; only the set of manifests manifest_deps() reads grows.
- (constraint) pydantic is a related but out-of-scope case: already a direct run-dep of pyforge-atlas and pyforge-scribe's own nested manifests, and already partially documented in the catalog (credited only as transitive via pydantic-ai/fastmcp/agno). This Spec sharpens that existing note to also name the two stations' direct declarations; it does not add pydantic as a new root pixi.toml pin unless a downstream reviewer decides otherwise, since it already resolves and is already mentioned.
- (non-goal) Not a station library-adoption decision. The aspirational 'should station X bind library Y' items in pyforge-unifying-strategy's Estate leverage table (kedro/vizro/openlineage/etc., and filelock's marshal/scribe extension) are a separate, already-tracked, forward-looking question and stay untouched.
- (note) Success signal: a future station manifest drift (a new run-dep declared in a station's own nested pixi.toml but never mirrored/documented) is caught by the very next llms-full-check run, without requiring another manual cross-check audit like the one that found this gap on 2026-09-12.
- (event) Self-validate pass 1 (coherence): CAP-1/CAP-2 each carry intent+success (rule 1); intents are WHAT not HOW (rule 2, the HOW -- tomllib walk shape, floor values -- lives in the capability success/constraint text as supporting detail, not prescribed as the only implementation); both constraints bend real design decisions (rule 3: floors must match resolved reality, CLI contract frozen); one non-goal present (rule 4); success signal is a concrete, demonstrable check (rule 5); CAP-1/CAP-2 ids stable, no prior ids to collide with, first mint (rule 6); prose lean, no decoration (rule 8). No open questions -- every fact was confirmed live before this spec was authored (conda-resolved versions queried directly, direct imports grepped in each station's own src/, live import success verified in each station's own pixi env). status: ready.
- (event) Self-validate pass 2 (preservation): walked the seeding Dream (docs/dreams/library-catalog-manifest-sync.md) and the pyforge-unifying-strategy Realization log entry claim by claim -- the seven-library table, the pydantic sharpening note, the Story 12.1 precedent, the detector-scope description, and the two-part fix direction all land in CAP-1/CAP-2/constraints above. No wrapper-only content dropped.
- (event) Process correction 2026-09-12: CAP-1/CAP-2 were hand-implemented directly from this Spec with no Story decomposition -- caught mid-turn by the operator. Reconciled same-turn: Epic 36 (Stories 36.1, 36.2) minted in pyforge-marshal/epics.md, sprint_plan.py generate + sprint-ledger-sync + story-status-check run to land both as done in the tracked ledger. AGENTS.md Dream-first workflow item 5 and CLAUDE.md's Dream-first paragraph both updated to close this loophole for future work, citing this spec by slug.
- (note) Scoping correction: filelock (pyforge-atlas, duckdb_writer.py + admission.py) was in the original manual cross-check's missing list but wrongly excluded as 'already tracked' -- conflating pyforge-unifying-strategy's aspirational marshal/scribe filelock ADOPTION question with the fact that atlas's OWN existing filelock use was still undocumented in root pixi.toml/the catalog, the identical class of gap as the other six. Found again, correctly this time, the moment CAP-2's own station-manifest scan went live against the real repo. Folded into Story 36.1 rather than opening a third story.
- (event) CAP-1 verified: pixi run -e local-recipes llms-full-check clean (352 active deps / 320 catalog entries, zero findings); pixi lock --check reports already up to date for all seven floors. CAP-2 verified: tests/scripts/test_llms_full_check.py (5 new tests) passes; story-status-check and ledger-regression-check report zero marshal findings post-promotion. Both capabilities status: done.
- (event) Pre-merge spec-surface reconciliation: pixi.toml/pixi.lock/environment.yaml/docs/reference/library-llms-full.md/scripts/llms_full_check.py are also governed by eight OTHER specs' own surface globs (pyforge-atlas/spec-atlas-kedro-catalog-expansion, pyforge-doctor/spec-pixi-candidate-currency, pyforge-marshal/spec-pyforge-core, pyforge-steward/spec-mcp-era-isolation, pyforge-steward/spec-mcp-host-real-station-tools, pyforge-steward/spec-pyforge-steward, pyforge-steward/spec-pyforge-unifying-strategy, pyforge-steward/spec-unified-container). Verified via clean-main baseline diff (git stash + detectors-ci) that spec-surface is 100% clean on unmodified main and every one of these 8 findings is newly introduced by this PR's shared-infra-file edits, not pre-existing. Three-check procedure applied to each: (1) none of their own SPEC.md capabilities/intent describe anything this PR's diff touches -- confirmed by reading each spec's Why/surface directly; (2) all 8 are re-stamps of existing baselines (routine shared-file currency, not completing someone else's unfinished landing); (3) no other pending uncommitted drift exists on any of their governed paths beyond this PR's own diff. Scoped-stamped after commit, from the committed tree: --write-baseline --spec pyforge-marshal/spec-library-catalog-manifest-sync --spec pyforge-atlas/spec-atlas-kedro-catalog-expansion --spec pyforge-doctor/spec-pixi-candidate-currency --spec pyforge-marshal/spec-pyforge-core --spec pyforge-steward/spec-mcp-era-isolation --spec pyforge-steward/spec-mcp-host-real-station-tools --spec pyforge-steward/spec-pyforge-steward --spec pyforge-steward/spec-pyforge-unifying-strategy --spec pyforge-steward/spec-unified-container.
- (event) 2026-09-13 pixi.toml: graphifyy added on feature.pyforge-scribe (Story 8.2). Catalog surface overlap only; no catalog product change.
- (event by claude) Surface reconcile 2026-09-14: `pixi.toml` changed for two reasons, neither of which touches this Spec's own contract. (1) The `frame-preflight` task description was restated for Frame Spec v0.3 (steward Story 53.6 moved Frame identity from `name` to the qualified-ref `identifier`). (2) `pyforge-station-tests` was REPAIRED: it landed on 2026-09-14 as `set -euo pipefail` plus a bash `for` loop, which pixi's task shell (deno_task_shell) cannot parse, so the task errored on its first token and had never run a single station despite CLAUDE.md documenting it as PR CI gate #3. Replaced with a declarative cross-environment `depends-on` over the eight `pyforge-<station>-test` tasks. Verified by running it: all 8 stations green, exit 0. `environment.yaml` re-exported and byte-identical (description/task-wiring edits only, no dependency change). Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (PR #1355 CI remediation): `pixi.toml` gained nine tasks and `board.py` gained coverage pragmas; neither touches this Spec's contract. (1) Eight `pyforge-<station>-coverage-gate` tasks plus a `pyforge-station-coverage-gates` aggregate now mirror `.github/workflows/coverage-gates.yml`'s `named-module-gates` matrix locally -- that gate measures only the modules a branch TOUCHES, so `pyforge.doctor.sources.board` had sat at 70.1% against an 80% floor since the Guildhall console was retired and only surfaced when an unrelated edit touched the file. (2) A `pr-preflight` aggregate runs the four lanes that actually red a PR (`detectors-ci`, `test-ci`, `pyforge-station-tests`, `pyforge-station-coverage-gates`), because two CI failures on #1355 had no local equivalent at all. (3) `board.py`'s retired-Guildhall machinery is marked `# pragma: no cover` with a block comment stating why -- it cannot execute while `retired-console-check` fails CI on reintroduction; module coverage went 72% -> 94% with no test or behaviour change, tracked for deletion as `DW-DASHBOARD-DEAD-CODE-1`. `environment.yaml` re-exported and byte-identical (task-only edits). Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (herald Story 21.1, commit b3b220b7f77b0d25cc2ac96e55897d7c95f30244): `pixi.toml` gained one new task, `[feature.local-recipes.tasks.deck-trio]`, for deriving a deck's Infographic head from its standalone poster -- unrelated to this Spec's own library-catalog-manifest-sync contract. No `environment.yaml`, `docs/reference/library-llms-full.md`, or `scripts/llms_full_check.py` change. Verified via `git show --stat`: this commit's only tracked-file touches are `docs/specs/presentation-deck.md`, `pixi.toml`, `scripts/deck_trio.py`, `tests/scripts/test_deck_trio.py` -- none in this spec's surface but `pixi.toml`. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-library-catalog-manifest-sync.
