---
topic: Dashboard velocity/timing covers hand-driven (non-bmad-loop) story completions via honest wall-clock fallback derivation
updated: 2026-08-22T05:39
---

- (direction) Unattended headless run from Dream docs/dreams/dashboard-velocity-captures-hand-driven-work.md; express distill chosen (headless default); slug inherited from Dream slug
- (note) Grounded 2026-08-21 against pyforge.doctor.sources.fleet_scan::scan_timing (~L2730): velocity = active agent-compute summed from session-start/session-end pairs (matched by task_id) in ~/.bmad-loops/<slug>/.bmad-loop/runs/*/journal.jsonl; per-field curated preservation via derived:true marker; renderer requires FULL object shapes (velocity.{bars,sub,foot}, timing.{perStory,epicMin,metric,note,totalLabel,total})
- (note) Grounded: bmad-dev-auto step-03 writes baseline_revision (HEAD before changes), step-04 writes final_revision (HEAD after committing) into story-spec frontmatter; promotion (_execute_promotion_plan in pyforge.marshal.cli.deploy) copies Tier-3 bytes verbatim so tracked specs keep both (verified: marshal spec-11-3 carries both; final_revision reachable on main via merge commit)
- (note) Grounded: run_reconcile_completions (Story 5.9) detects not-loop-native completions from (corroborated AND full-merged) minus marshal-native, advances ledger backlog->done and promotes specs; it records NO timing; label is not-loop-native because the route cannot be proven
- (decision) Signal inventory for hand-driven stories is load-bearing multi-item content -> spec-authored companion signal-inventory.md; kernel cites it
- (decision) CAP-1: back-fill wall-clock timing for done stories with resolvable baseline_revision+final_revision in a tracked/promoted spec and zero closed journal sessions; derivation is offline git-only
- (decision) CAP-2: wall-clock class is visually/textually distinguished from journal-derived active-compute; never blended on the same axis undistinguished (atlas precedent already in scan_timing comments)
- (decision) CAP-3: coverage caption partitions the unmeasured bucket by true reason (journal-measured / wall-clock-derived / spec-without-revisions / no-spec-at-all), replacing the now-partial predates-instrumentation framing
- (decision) Per-story precedence: journal-derived active-compute wins the velocity axis for any story with at least one closed session; wall-clock fallback applies only at zero journal coverage
- (constraint) Never fabricate: no resolvable signal (NO_VCS, missing fields, unresolvable revisions, no spec) -> story stays absent; existing deliberately-absent discipline survives
- (constraint) Curated per-field values (warden timing+velocity, atlas timing) are never touched; new derivation obeys derived:true refreshability and emit-full-contract-or-nothing
- (decision) At-source capture (Dream option a: dev-auto HALT timing stamp) NOT specced as a capability: only helps future stories, lives in the regenerated BMAD install layer, and is naturally superseded by the marshal-single-story-dispatch verb journaling sessions at source -> recorded as open question + convergence assumption
- (assumption) Convergence noted, not absorbed: docs/dreams/marshal-single-story-dispatch.md (filed 2026-08-21, being specified in a parallel session) would be the natural at-source PRODUCER of per-story effort signal; this spec specifies display/derivation independently so already-landed stories are covered either way
- (assumption) Repo merge policy (--merge never --squash) keeps final_revision reachable from main; derivation runs on a local generate with full history and persists to Pages via committed data.js (same model as journal-derived velocity)
- (assumption) PR-timestamp-derived numbers stay a curated/manual path (atlas precedent); the automatic path stays offline. Claude Code session transcripts rejected as a signal: per-machine, not durable, no story-key structure
- (question) Wall-clock bound choice: ts(final)-ts(baseline) overstates (baseline commit predates dispatch); first-commit-in-range->final understates. Story-level design; caption must state the choice
- (question) Render surface for the wall-clock class: timing strip only, or also a distinct bar class on the velocity graph? CAP-2 distinction required either way
- (question) Partial journal coverage (escalated in-loop, finished by hand): journal floor wins per precedence; is the hand-finished remainder ever surfaced or accepted as under-measurement (proposed: accepted; existing floor-not-total caption covers it)?
- (question) Should bmad-dev-auto HALT additionally stamp a first-party duration despite install-layer override survivability cost (bmad-customize) and probable supersession by single-story dispatch journaling?
- (event) Self-validate pass 1 (coherence, Spec Law 1-6+8): PASS - 3 capabilities each with intent+success; 6 design-bending constraints; 4 non-goals; success signal testable against doctor 8.1-8.4 + byte-identical curated numbers
- (event) Self-validate pass 2 (preservation): PASS - every load-bearing Dream claim landed in SPEC.md or signal-inventory.md incl. both capture points ((a)->open question 4, (b)->CAP-1). Wrapper-only content dropped: Kinships narrative and Realization log (process metadata; Dream stays linked via owner-dream)
- (event) Spec finalized as draft; Dream frontmatter advanced dreamt->specified in the same branch
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log

- (note) Steward 30.2 (2026-08-25): RECONCILES docs/dashboard/generate.py, docs/dashboard/data.js (deleted) and docs/dashboard/index.html (retirement stub). Velocity capture no longer writes the Guildhall blob.
