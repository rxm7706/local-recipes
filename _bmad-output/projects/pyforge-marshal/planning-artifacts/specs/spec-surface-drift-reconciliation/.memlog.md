---
topic: Make the spec-surface drift verdict clearable per-spec and trustworthy per-file, then work the 61 live findings to zero
updated: 2026-09-09T18:11
---

- (direction) Operator direction 2026-08-08: the 61 spec-surface findings get the SAME discipline as everything else — seed a Dream, fold it into the station chain (Dream -> Spec -> PRD/arch -> epics/stories -> code). Explicitly NOT a hand-patch of the detector. A working prototype of both fixes was written earlier that session and REVERTED to keep Dream-first ordering honest.
- (note) Owner dream: docs/dreams/surface-drift-reconciliation.md (owner=marshal). Enforcement half of the regenerable-factory practice dream, which already built the surface map + drift checker and governs scripts/spec_surface_check.py.
- (note) Measured on main at cf885388fe: 61 findings = 34 [drift] + 24 [no-baseline] + 2 [ungoverned] + 1 [stale-allowlist]. Drift concentrates in five specs: steward/spec-pyforge-steward 23, scribe/spec-team-memory 5, marshal/{spec-fidelity-enforcement,spec-factory-console,spec-durable-runs} 2 each.
- (decision) Root cause is ONE disease at two granularities, not two unrelated bugs: the reconciliation claim is made at the wrong level. Baselines stamp all-or-nothing when they should be per-spec; drift clears per-spec when it should be per-file. Fixing granularity resolves both symptoms, which is why they are specced together rather than as separate efforts.
- (capability) CAP-1 baseline stamping is scopeable. intent: an operator settles one spec's baseline without accepting any other spec's pending drift. success: --write-baseline --spec NAME merges only that spec into the committed baseline, leaves every other entry byte-identical, and errors (exit 2) on an unknown spec name.
- (capability) CAP-2 a moved contract reconciles only the paths it names. intent: a memlog entry stops speaking for governed files it never mentions, so unrelated activity cannot launder pending drift. success: with the spec's memlog moved, a drifted file NAMED in the memlog clears while an unnamed one reports [drift-presumed]; appending an unrelated memlog entry no longer changes the verdict for an untouched governed file.
- (capability) CAP-3 the standing 61 findings are worked to zero by category. intent: the operator can act on the verdict because every finding is dispositioned rather than carried. success: each [no-baseline] scoped-stamped, each [drift] genuinely reconciled through its spec or scoped-stamped with the reasoning recorded in that spec's memlog, both [ungoverned] files given a surface or allowlist entry, the [stale-allowlist] pattern removed.
- (capability) CAP-4 the cleared gate is defended by tests that fail for the right reason. intent: the fixes cannot silently regress into the blanket behavior they replace. success: both are mutation-tested BOTH ways — removing --spec scoping re-reds the isolation test, and reverting to the per-spec short-circuit re-reds the laundering test — and spec-surface-check exits 0 on main.
- (constraint) Fix the granularity; do NOT weaken the gate. Widening a threshold, allowlisting the noisy specs, or making drift non-gating would clear the red without making the signal true — Charter section 6 forbids a station relaxing its own gate rather than meeting it.
- (constraint) .memlog.md history is append-only and is never rewritten. The per-file rule READS memlog text; it must not edit, reorder, or normalize any memlog to make matching easier.
- (constraint) A memlog that names no paths is NOT an error. Most existing entries predate any naming convention, so the per-file rule degrades to a visible [drift-presumed], never to a hard failure — otherwise the gate reds for every historical entry, reproducing the unclearable red in a new shape.
- (constraint) [drift-presumed] is INFORMATIONAL and never gates. Its purpose is making an unproven set visible; a gating variant would be the same unclearable red renamed.
- (constraint) A scoped stamp MERGES into the committed baseline. Rewriting from the in-memory current set would silently drop every spec not named on that invocation.
- (decision) Path matching is LITERAL substring on the repo-relative path, the form these memlog entries already cite files in. Deliberately not fuzzy or NLP-inferred: inferring reconciliation intent from prose would reintroduce exactly the presumed-reconciled blanket this replaces.
- (decision) Non-goal: re-homing this detector to Doctor. That is Charter section 6 work already scoped as Doctor Epic 6 (S-6.1..S-6.10); this Spec fixes the instrument where it lives. Marshal keeps the operational guard regardless — only the VERDICT moves, later.
- (decision) Non-goal: bulk-reconciling the 23-file steward cluster by stamping it. If that surface genuinely changed, its contract moves — that is the whole point of the factory. Stamping it would be the laundering this Spec exists to end.
- (note) Both defects were reproduced live, not read off the source. DW-SURFACE-2026-08-08-1: appending an unrelated allowlist note to spec-regenerable-factory's memlog dropped findings 63 -> 61, clearing scripts/bmad_drift_check.py and scripts/dream_chain_check.py which the author never touched; recorded verbatim in that memlog under a (NOT RECONCILED ...) entry so the fact survived the finding. DW-SURFACE-2026-08-08-2: --write-baseline stamps every spec in one write.
- (note) Reverted-prototype evidence (design input, not implementation): the change is small and local to main() — a scoped merge on the write path, a per-file check on the read path — and no other detector reads .spec-surface-baseline.json, so the format change is contained.
- (assumption) Assumed the two [ungoverned] files (docs/governance/spec-pyforge-charter/{SPEC.md,.memlog.md}) want a real surface rather than an allowlist exemption, since the Charter is the document defining the chain the detector polices. Not confirmed with the operator; an allowlist entry would also clear the finding.
- (question) Should the Charter's own spec folder be governed by a surface (which spec claims it?) or allowlisted as governance-tier content outside the station model?
- (question) For the 34 [drift] findings: which are genuine surface changes whose contract must move (steward's Epic-2/3 delivery looks like one) versus already-reconciled work that only lacks a stamp? The partition needs a per-spec pass and is the bulk of CAP-3's work.
- (decision) CORRECTION during self-validate: the Dream's 'joins the detectors CI actually enforces' was wrong and would have silently settled another Dream's open decision. spec_surface_check ALREADY runs on every PR and push via .github/workflows/detectors.yml (scope=repo registry subset), but is deliberately ADVISORY (|| true + annotations) per an operator decision of 2026-07-31 that docs/dreams/fidelity-enforcement.md records as still OPEN. Added as an explicit non-goal; Dream bullet rewritten. This Spec makes the signal true; whether a true signal should block a merge belongs to fidelity-enforcement.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6+8): PASS. 4 capabilities each carry intent+success; intents state WHAT (the observable operator outcome) with flags appearing only in testable success criteria; all 6 constraints rule something out; 5 non-goals; success signal is a runnable command plus two named mutation tests; CAP ids 1-4 unique and new.
- (event) Self-validate pass 2 (preservation): PASS with one correction. Every load-bearing Dream claim landed — the 61-finding partition, both defect mechanisms with their live reproductions, the one-disease-two-granularities root cause, all 6 constraints, all 4 original non-goals. One claim was found FALSE against the repo and corrected rather than carried (the CI-enforcement bullet, above). Wrapper-only content dropped: the Dream's narrative framing and its Realization log.
- (constraint) This Spec's surface claim on scripts/spec_surface_check.py is TRANSITIONAL. Doctor Epic 6 S-6.9 retires it together with spec-regenerable-factory's four globs when the detector moves into src/shared/packages/pyforge-doctor/, where spec-pyforge-doctor's own glob governs it. Epic 13 must therefore land BEFORE that move or be re-pointed with it — the fixes belong to the detector wherever it lives, and a stale glob here would rot silently since a surface matching nothing is not a finding.
- (note) Live input for Story 13.4, and a second instance of the laundering defect. Retiring the regen board line changed pyforge.doctor.sources.fleet_scan and docs/dashboard/index.html, both governed by spec-factory-console — which ALREADY carried unreconciled drift on those exact two files. Appending to that Spec's memlog would move its contract and, under today's per-SPEC short-circuit, clear BOTH findings including the pre-existing one nobody reconciled. Deliberately NOT appended: the two findings stay visible rather than being laundered by an unrelated-in-part change. Story 13.4 must reconcile spec-factory-console for BOTH the pre-existing change and the regen retirement, using S-13.2's per-file rule so each is claimed by name.
- (change) S-13.1 + S-13.2 SHIPPED in scripts/spec_surface_check.py. Scoped stamping merges into the committed baseline (unknown spec name exits 2); per-file reconciliation keys on memlog_text() naming the repo-relative path, with [drift-presumed] printed SUMMARIZED per spec rather than per file — the first live run surfaced 995 entries, 834 of them one station's whole surface, which would have buried the 62 gating findings underneath them. Full set stays in --json. Both mutation-tested BOTH ways: removing --spec scoping reds the isolation + unknown-name tests; restoring the per-spec short-circuit reds the laundering-replay + never-gates tests.
- (decision) RESOLVED (question) on the Charter's own spec folder, on a fact rather than a preference: docs/governance/spec-pyforge-charter/ is NOT discovered by SPEC_GLOB (_bmad-output/projects/*/planning-artifacts/specs/spec-*/SPEC.md), so it CANNOT declare a surface at all. An allowlist entry was the only mechanism that existed. The frontmatter assumption that it "wants a real surface" was wrong and is retired.
- (decision) RESOLVED (question) on the 34 [drift] partition, settled by reading each spec's CAPs rather than assuming: 9 were archived specs with deliberately empty `surface: []` whose files still exist (the baseline lagged the archival), 2 were genuine console change since 2026-08-01, and the 23-file steward cluster needed NO contract change — spec-pyforge-steward already names budget/deploy/provision across CAP-1..CAP-4 with 8 stories done, i.e. the code had caught up to a contract that was already correct.
- (note) REOPENED 2026-08-09 for CAP-5. Two days of using the green gate surfaced a third granularity error of the same family, found twice in two days: spec-bmad-loop-forward-dependency-blindness (2026-08-08) and spec-bmad-module-provisioning (2026-08-09) each shipped declaring a `surface:` with NO .memlog.md. contract_hash() returns "" for such a spec, the baseline stores "", and "" != "" is never true — so spec_moved is permanently False and every governed change reports a hard [drift] whose printed remedy ("reconcile the spec") is unreachable, there being no contract to move. Both memlogs were created by hand once noticed; nothing reported the condition.
- (capability) CAP-5 a governed surface with no contract behind it is reported. intent: a Spec declaring a `surface:` with no .memlog.md stops being silently drift-blind. success: a spec governing >=1 tracked file under the default `surface-drift: memlog` mode with no .memlog.md reports a GATING [drift-blind] finding naming the spec, its governed count and the expected memlog path; a zero-file spec, an `exempt` spec and a `sentinel:` spec each report nothing; the 7 live instances are dispositioned; mutation-tested both ways.
- (note) Measured on main at 7dde591811 (2026-08-09): the condition is not two instances, it is SEVEN specs governing 396 tracked files, all already baselined with "memlog": "" and therefore green today and unreconcilable tomorrow — pyforge-mason/spec-conda-forge-expert-rebuild 370, pyforge-herald/spec-herald-moments-2-4-live-backend 18, pyforge-marshal/{spec-dashboard-project-path-derivation 2, spec-dream-to-code-model-self-verification 2, spec-pyforge-core 1, spec-sprint-status-auto-promote 2}, pyforge-steward/spec-unified-container 1.
- (decision) [drift-blind] GATES, unlike [drift-presumed], and the distinction is not arbitrary. [drift-presumed] is an UNPROVEN reconciliation claim over historical entries nobody can retro-name, so gating it would recreate the unclearable red. [drift-blind] is a STRUCTURALLY IMPOSSIBLE one, and it is trivially clearable by creating one file — so it can gate without ever being a red an operator cannot honestly close.
- (constraint) Creating a memlog and scoped-stamping its baseline are ONE change. A new memlog moves the contract hash off "", so the NEXT comparison sees spec_moved=True and every subsequent drifted file downgrades from gating [drift] to informational [drift-presumed]. Fixing blindness by hand without stamping in the same move trades a false green for a quiet one. Verified by before/after diff that no [drift] became [drift-presumed].
- (constraint) The detector may NOT create the missing .memlog.md itself. Writing the file it checks for would make the finding self-clearing, and would author a decision record nobody decided.
- (decision) Non-goal: requiring a memlog of EVERY Spec. A spec governing zero files cannot drift (11 such exist and are correctly silent); `exempt` records no file hashes at all; `sentinel:<path>` supplies a second hash that CAN move. None of the three is blind. CAP-5 covers exactly the default mode over a non-empty governed set.
- (change) S-13.5 SHIPPED in scripts/spec_surface_check.py: a [drift-blind] finding for a governed, non-exempt, non-sentinel spec with no .memlog.md, emitted from the same pass that builds `current`. Reconciles scripts/spec_surface_check.py by name under S-13.2's per-file rule. Test added in .claude/skills/conda-forge-expert/tests/meta/test_spec_surface_check.py, mutation-tested both ways.
- (change) RECONCILES `scripts/spec_surface_check.py` (2026-08-09, Doctor Story 6-9) — the constraint two entries above ("This Spec's surface claim on scripts/spec_surface_check.py is TRANSITIONAL. Doctor Epic 6 S-6.9 retires it...") is now realized: the origin file is DELETED, its logic lives at `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py::gather_spec_surface`, governed by `spec-pyforge-doctor`'s own blanket glob. This Spec's surface glob on it is removed in the same change (see SPEC.md); the remaining `scripts/.spec-surface-baseline.json` glob is untouched — the data file's path did not move. `.claude/skills/conda-forge-expert/tests/meta/test_spec_surface_check.py` (S-13.5's own test, cited above) is re-targeted at `sources.chain.gather_spec_surface` in this same change.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) status: in-progress -> shipped (fleet-readiness-decision-batch-2026-09-09 Class B, row mars-B). Epic 13 (13.1-13.7) all `done`, and the instrument is verified in effect rather than by ledger: pyforge.doctor.sources.chain::gather_spec_surface over the live tree returns 6 entries -- five informational [drift-presumed] (pyforge-mason x3, pyforge-steward x2) plus the green verdict "every tracked file governed or allowlisted; no drift". Against this Dream own opening measurement (61 findings, then 994 presumed) the signal is trustworthy.
- (decision by claude) 2026-09-09 both frontmatter open questions retire to open_questions: [] (fleet-readiness-decision-batch-2026-09-09 Class B, row mars-B): their own text already begins RESOLVED -- S-13.3 (the Charter spec folder is allowlisted, the only mechanism available to it) and S-13.4 (the 34 [drift] split 9 archived-spec baseline lag / 2 genuine console change / 23 steward files whose contract was already correct). The resolutions are retained here in the memlog, which is where they belong.
- (event) (self-validate pass 1 -- coherence) 2026-09-09 re-derive: PASS. status in-progress -> shipped; both questions retired (their own text already began RESOLVED) into a dated closed-questions section that keeps the S-13.3 / S-13.4 resolutions. 7 CAP ids, unchanged; the surface-drift-exclude and retired-assumption frontmatter entries are untouched.
- (event) (self-validate pass 2 -- preservation) 2026-09-09 re-derive: PASS. Both 2026-09-09 entries landed, including the in-effect verification (gather_spec_surface returns 6 entries, five drift-presumed plus the green verdict) measured against the Dream's 61-then-994 opening.
