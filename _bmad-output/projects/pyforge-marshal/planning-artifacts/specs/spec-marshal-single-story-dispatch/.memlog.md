---
topic: marshal-single-story-dispatch: the validated one-story-per-fresh-agent dispatch pattern (N=22, 2026-08-21) becomes a governed marshal verb
updated: 2026-09-09T18:03
---

- (direction) Headless invocation (unattended agent session); express distill chosen per protocol default; slug marshal-single-story-dispatch inherited from the Dream filename; title kept from the Dream (already declarative, house style)
- (note) Input: docs/dreams/marshal-single-story-dispatch.md (unusually complete: N=22 evidence 2026-08-21, owned-inventory, five named decisions). Convergence surfaces read: cli/spin.py (factory spin/attach/resume; AD-22 detached default; AD-25 journal), cli/deploy.py reconcile-completions (Story 5.9/FR-186 label not-loop-native + FR-187 merge subject), PRD prd-pyforge-marshal-2026-07-25 section 5 (Q-1 wrap+supervise), spec-horizontal-run-concurrency (FR-184 clamp = in-loop plane only), epics.md Epic 15; FR space FR-1..FR-191 grepped: no dispatch-driver FR exists
- (decision) Dream decision 3 (wrap-vs-absorb) RESOLVED as BOUND, not PRD revision: PRD 5.3 Q-1 (wrap and supervise) stands; dispatch is a sibling launch mode beside factory spin, never a bmad-loop replacement; the dispatched story's engine is bmad-dev-auto (external skill) so marshal still contains no dev/verify/review/commit engine — supervisor-is-the-product extends to a second engine rather than absorbing the first; FR-52 single-seam discipline extended (one adapter for the session harness)
- (decision) Dream decision 1 (completion detection) RESOLVED: completion/failure judged only from git facts (AD-33: commits, merge refs) plus running-process facts; harness notifications and session self-reports are input, never verdicts; no busy-wait in any foreground path (the 600s-watchdog trap) — waiting belongs to a detached supervisor per Story 3.4 precedent; a killed/failed notification with live git progress must refuse redispatch (the near-duplicate trap)
- (decision) Dream decision 2 (verification is the product) RESOLVED: before any landing the driver itself runs the story's real verify commands via Epic 2's standalone gate objects and reads the diff surface against the story's frozen surface; never-false-green verdict lattice applies; the doctor-12.3 shape (self-marked shipped with two live-reproducible leaks) is the canonical refusal fixture
- (decision) Dream decision 4 (sequencing/parallelism) RESOLVED: one story in flight per station, HARD-enforced by the driver from the same git+process facts as completion detection; cross-station dispatches may run concurrently — the already-sanctioned cross-project plane, which spec-horizontal-run-concurrency explicitly excludes from FR-184's in-loop max_parallel clamp; cross-station surface disjointness is advisory-only (loud when declared frozen surfaces overlap), operator trusted otherwise — no reliable overlap oracle exists and the N=22 pattern trusted the operator
- (decision) Dream decision 5 (operator survival) RESOLVED: detached-by-default inherited from spin (AD-22); a dispatched run gets its own journal (AD-25/AD-30 discipline) and an attach/resume path; driver or terminal death orphans nothing invisibly — status/fleet-picture must report the run from journal+process facts alone, and a story completed while unsupervised is reconciled from git facts, not lost
- (capability) CAP-1 dispatch verb: launch exactly one story on a station as a fresh worktree-isolated bmad-dev-auto session under marshal governance (policy-composed budgets/model tier, BMAD_ACTIVE_PROJECT per-invocation, physical artifact paths, journaled launch). CAP-2 event-grounded completion judgment. CAP-3 independent verification before landing. CAP-4 landing/paper-trail via existing Epic 4 + FR-187 + Story 4.1 + Epic 15 machinery, classified marshal-native. CAP-5 per-station serialization, cross-station parallelism. CAP-6 detached survival: journal, attach/resume, reconcile-if-unsupervised. IDs minted fresh (create operation, no prior memlog)
- (constraint) HARD: PRD Q-1 not revised; dispatch never replaces/deprecates factory spin or bmad-loop. Always: composition not reimplementation (Epic 1/2/4/15 + FR-187 reused; no second landing/promotion/gate path). Always: no bmad-switch from the driver or its sessions; BMAD_ACTIVE_PROJECT + physical paths per standing HARD rule. Always: FR-184's in-loop clamp untouched; driver parallelism lives only on the cross-project plane. Always: bmad-dev-auto itself untouched (FR-186 precedent: the skill gains no marshal dependency)
- (note) Related-Dream convergence named, scope not absorbed: docs/dreams/dashboard-velocity-captures-hand-driven-work.md (being specified in parallel) wants per-story effort for hand-driven completions; a dispatch driver that journals its sessions is the natural producer of that timing signal (journal start/end + baseline/final revisions). This spec produces the signal; rendering/display stays with that Dream's spec (non-goal here)
- (assumption) Assumptions (headless calls): (a) verb naming left open (factory dispatch vs marshal dev family) — converges with PRD Q-15, not decided here; (b) session harness = the agent-CLI pattern validated at N=22 (plain background agent, never subagent_type fork — bmad-dev-auto's mandatory subagents break inside a fork); (c) whether dispatch completions get a distinct completion-path label beyond marshal-native classification is story-level; (d) escalation reuses Story 3.7 shapes unless a story finds a reduced ladder sufficient
- (question) Open questions: (1) exact verb naming (PRD Q-15 convergence); (2) concrete launch mechanism for a headless bmad-dev-auto session (binary/adapter profile; must preserve the plain-agent requirement); (3) what budget signal is actually enforceable on a dispatched session, given the loop's own token cap does not enforce; (4) whether the cross-station disjointness advisory compares declared frozen surfaces only or also live diff surfaces; (5) whether completion detection needs a dedicated sidecar (Story 3.4 shape) or the existing supervisor generalizes
- (event) Self-validate pass 1 (coherence, Spec Law 1-6+8): PASS — 6 capabilities each with intent+success, IDs fresh CAP-1..CAP-6, constraints all rule something out, 5 non-goals, success signal testable (replay + refusal fixture + kill-terminal), prose lean
- (event) Self-validate pass 2 (preservation): one gap found and fixed — the Dream's changes.patch safety-net weakness (failed-session work preservation) added to CAP-6 intent via Epic 1's teardown-refuses discipline. All five Dream decisions resolved first-class (1->CAP-2, 2->CAP-3, 3->Why+HARD constraint with PRD 5.3 quote, 4->CAP-5, 5->CAP-6). Wrapper-only drops: the Dream's rhetorical framing prose. Verdict: PASS. Spec finalized, status ready
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event) 2026-08-23: CAP-7 fleet drain + merge-in-agent policy added; companion `fleet-drain-playbook.md` (eight-station campaign oracle); `.cursor/pyforge-fleet-drain/` declared interim runner until Story 22.1 ships `marshal factory dispatch` / `marshal drain`
- (direction) Operator direction 2026-08-31: docs/dreams/marshal-single-story-dispatch.md gained a same-day addendum (station-scoped + sequence-scoped dispatch) after a live incident -- wanting to drain just pyforge-scribe's 2 remaining backlog stories without touching atlas's/marshal's own in-flight backlogs found no CLI primitive for it. Decompose into CAP-10.
- (note) Gap confirmed against source: cli/dispatch.py's drain argparse carries six flags (--mode, --leave-remaining, --once, --max-cycles, --tick-seconds, --campaign), none a station filter -- drain always reads all eight stations' ordered backlogs and launches one story per station in parallel. dispatch <slug> <story> launches exactly one story with no chaining at all.
- (capability) CAP-10 station-scoped and sequence-scoped dispatch: (1) drain --mode <mode> --station <slug> restricts CAP-7's campaign (chaining, preflight, journal) to exactly one station's own backlog instead of all eight; (2) dispatch <slug> --stories <key1>,<key2>,... chains a caller-supplied ordered list instead of the ledger's own backlog order, for prioritizing specific stories without editing fleet-drain-queue.yaml's order_overrides for a one-off push. Every named key validated against the station's actual backlog before anything launches -- an unknown or already-done key refuses loudly, never silently skipped. Success: --station scopes a drain cycle to one station's next backlog story only, other stations' backlogs untouched; --stories launches its list in the given order via the same chaining as drain, refuses on an invalid/already-done key before any launch; both reuse CAP-2's zombie/in-flight preflight, CAP-4's landing machinery, and CAP-9's divergence guard unchanged -- no second preflight or landing path.
- (constraint) CAP-10 constraint: --station and --stories change ONLY which stories/order the campaign reads -- they must reuse CAP-2's preflight and CAP-4's landing exactly as fleet-wide drain does, never a parallel gate-light path. A --stories list naming a story already done or absent from the station's tracked backlog refuses before any worktree is provisioned, matching drain's own zombie-refusal discipline rather than silently skipping it.
- (note) Non-goals for CAP-10: not a new landing/verification mechanism (reuses execute_dispatch_land / CAP-2 / CAP-4 unchanged); not a replacement for fleet-wide drain, which stays the default for draining everything.
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6+8) on CAP-10: clean -- intent+success present, new Constraint actually bends design (rules out a lighter preflight/landing path), success signal concrete/testable, ID unused/stable (next after CAP-9), non-goals unaffected (CAP-10 still dispatches one story at a time -- the existing 'not backlog orchestration' non-goal already covers it), lean.
- (event) Self-validate Pass 2 (preservation vs the 2026-08-31 Dream addendum): clean -- the gap (no station filter, no chaining on dispatch), the motivating incident, both new entry points (--station on drain, --stories on dispatch), the key-validation requirement, and the CAP-2/CAP-4/CAP-9 reuse constraint all landed (CAP-10 + new Constraint). Wrapper-only content dropped: none.
- (event) Decomposition 2026-08-31: CAP-10 landed as Story 22.11 (Station-scoped drain and an explicit story sequence, new -- epics.md + spec-22-11 + ledger backlog entry synced via bmad-sprint-planning's generate). Found + fixed en route: scripts/promote_sprint_status.py's reader can't parse PyYAML's line-wrapped long-key scalars (key: \n done form), misreading 9 pre-existing done stories as absent -- reformatted the Tier-3 feed with a wide dump width (no value changes) rather than force-writing over the correct tracked ledger. Also fixed 28.1's stale backlog value in the same feed (already done on the tracked twin, landed earlier 2026-08-31).
- (event by claude) Surface reconcile 2026-08-31: 2 drift-presumed paths on governed `cli/main.py` and `cli/spin.py` since baseline at `26102ea12c6d`. Eight commits, all already-done story work including this spec's own Story 22.11 delivery:
  - `98a61adc54` — Story 15.1 (done): one-command fleet homes refresh; main.py wiring.
  - `8bc225f708` — Story 17.4 (done): chain regenerate CLI entry on main.py.
  - `d4686b0aa7` — Story 22.1 (done): factory dispatch verb on main.py + spin.py.
  - `1a083201ce` — Story 22.6 (done): dispatch survives operator on spin.py.
  - `5e05193695` — Story 21.2 (done): planning chain regen CLI on main.py.
  - `58f2378e25` — pyforge-core hooks extraction: main.py/spin.py import reparenting.
  - `c42090a804` — Story 22.7 WIP autosave on spin.py.
  - `6796ef1282` — Story 28.2 (done): compression wiring on spin.py.
  (`2504eac0a1` Story 22.11 station-scoped drain landed same day but touches dispatch.py not these two governed files.) No CAP text change. RECONCILES: `src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/main.py`, `src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/spin.py`. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-single-story-dispatch`.
- (event by claude) Surface reconcile 2026-08-31 (marshal Story 28.8, spec-marshal-token-economy CAP-5): `cli/main.py` gained one import and one `add_*_subparser` call for a NEW top-level verb, `context` — the derived-context layer's freshness surface. Nothing about single-story dispatch changed: no dispatch/drain code path, no `--stories` handling, no launch seam. The verb is registered on the same `subparsers` object every other noun group uses and is declared CLI-only in `mcp/parity.py::CLI_ONLY_VERBS`. No CAP text change. RECONCILES: `src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/main.py`. Scoped stamp owed after landing: `--write-baseline --spec pyforge-marshal/spec-marshal-single-story-dispatch`.
- (direction) Operator 2026-09-02: stop the done-spec review loop seen on steward 41.2 (#1017, 27 write-backs, DIRTY) and mason 13.2 (18 write-backs, no PR). Lands on this Dream/Spec as CAP-11, not a new Dream.
- (capability) CAP-11: done + followup_review_recommended false HALTs with no review and no spec commit; done + true allows at most one follow-up then forces false; 0-patch review must not commit; after harness exits done marshal may only CAP-4 land or escalate — never another bmad-build-auto because main ledger is still backlog. Local .claude/skills/bmad-build-auto/ is in scope; vendored bmad_loop is not. Success: replay 41.2/13.2 fixtures → zero new review commits, zero new harness launches.
- (constraint) CAP-11: harness halt done is terminal for that story session. Re-invoking bmad-build-auto solely because sprint-status-ledger on main is backlog is forbidden.
- (note) Non-goal amendment: the 2026-08-21 "do not change bmad-dev-auto" line now means vendored bmad_loop + bmad-quick-dev stay untouched; the in-repo bmad-build-auto skill copy is the harness contract for CAP-11. Not risk-tiered-review-depth (do not shrink max_followup_reviews).
- (event) Decomposition 2026-09-02: Epic 29 Stories 29.1 (harness) and 29.2 (marshal terminal), ledger backlog, queue 29.1 then 29.2.
- (note by claude) 2026-09-05 drift-presumed reconciliation (branch maint/marshal-docs-reground-2026-09-05): the spec-surface detector reported these governed paths as changed/added while this memlog moved without naming them. Verified each against `git log -1` — every path was landed by a story commit on main within this spec's declared surface; none is an out-of-band edit. Named here so the presumption is discharged; scoped --write-baseline follows. Paths by landing commit — 6a3f310f4f (2026-09-02, marshal: halt bmad-build-auto on a done spec unless follow-up is tru): .claude/skills/bmad-build-auto/step-01-clarify-and-route.md [added], .claude/skills/bmad-build-auto/step-04-review.md [added] ; edc9cd31bf (2026-09-02, feat(pyforge-marshal): supervisor finalizes when harness cannot run): src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_supervisor/__main__.py [added] ; 2c21bdd8d6 (2026-09-03, fix(marshal): do not halt drain_to_zero on harness-done 040): src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch_fleet.py [added]
- (event by steward) 2026-09-06 BMAD-METHOD 6.11.0->6.12.0 apply (steward upgrade bmad-core, branch steward/bmad-core-upgrade-6.12.0) regenerated the in-repo .claude/skills/bmad-build-auto/ harness and DROPPED the local CAP-11 contract (Story 29.1 done + followup_review_recommended HALT in step-01/step-04/spec-template) and Story 28.8's derived-context/planning-graph blocks (step-01 items 1a/2, compile-epic-context.md). Re-applied by 3-way merge onto 6.12 upstream: step-01 keeps the followup_review_recommended gate and, only on the single allowed follow-up, adopts 6.12's new followup_pass=true + review_loop_iteration=0 routing (upstream 6.12 otherwise re-reviews every done spec unconditionally); step-04 keeps the flag-false-at-HALT rule on top of 6.12's diff_file/claims_file staging. Contract hash of step-01/step-04 therefore moves; scoped re-stamp follows. Follow-up owed (marshal): these edits live in installer-regenerated files with no marker the steward CAP-3 reconcile can key on — every core upgrade must re-run this merge (failure-modes.md trap 16)
- (direction by steward) 2026-09-06 surface widening requested: add .claude/skills/bmad-build-auto/compile-epic-context.md and spec-template.md (Stories 28.8/29.1 edits) to this spec's surface so the CAP-11 contract is governed end to end; the sprint-planning trio (references/generate-tracking.md, scripts/sprint_plan.py, scripts/tests/test_sprint_plan.py — the N.M-key fix 1aeb911b3b) needs a marshal ledger-owning spec surface as well. The 6.12 core apply regenerated all seven; spec-surface caught only the two governed files. Until steward CAP-8 ships, every core upgrade must 3-way re-apply them from the cached-package diff (spec-bmad-method-core-upgrade customization-inventory.md row C5).
- (decision by steward) 2026-09-06 surface widened: + .claude/skills/bmad-build-auto/spec-template.md (Story 29.1's followup_review_recommended field lives there; the 6.12 core apply regenerated it away unnoticed). compile-epic-context.md goes to spec-marshal-token-economy (Story 28.8 owner). Scoped re-stamp follows.
- (note) Cross-station surface touch (marshal Story 30.5, 2026-09-06): `cli/spin.py`'s docstring renamed a present-tense reference from `bmad-dev-auto` to `bmad-build-auto` (which spec-writing skill actually writes spec files today). Docstring-only, no single-story-dispatch behavior changed. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (decision by claude) 2026-09-06 (spec-31-4-every-in-place-edited-installer-owned-file-is-governed-by-a-marshal-spec-surface): closes the steward direction above -- the sprint-planning trio (`.claude/skills/bmad-sprint-planning/scripts/sprint_plan.py`, `.claude/skills/bmad-sprint-planning/scripts/tests/test_sprint_plan.py`, `.claude/skills/bmad-sprint-planning/references/generate-tracking.md`) added to this spec's `surface:`. This spec stays the ledger-owning spec for the trio (CAP-4 triggers `sprint-ledger-sync` on every landing; this memlog already narrates the 2026-08-31 promote-parser incident the trio's own generator can also inflict). `spec-marshal-token-economy` needs no new entries (it already covers `compile-epic-context.md` for an unrelated reason, Story 28.8). Split across the two specs, all seven installer-owned files marshal edits in place now sit under a marshal spec surface: this spec's six (three `bmad-build-auto` + the three `bmad-sprint-planning` files added here) plus `spec-marshal-token-economy`'s `compile-epic-context.md`.
- (event by claude) 2026-09-06 (spec-31-4): fixed the root cause behind the 2026-08-31 Story 22.11 corruption (and the same-day `promote_sprint_status.py` reader note above) -- `sprint_plan.py::_make_yaml()` now sets `yaml.width = 4096` (ruamel's line-fold threshold) AND `yaml.Emitter.MAX_SIMPLE_KEY_LENGTH = 4096` (ruamel's separate explicit-key `? key` / `: value` threshold, a class default of 128 -- review found `width` alone does not close it: a >=128-char key still folded even with `width=4096`), so ruamel never folds or explicit-keys a long `key: value` line into a form invisible to `fleet_scan.parse_sprint_status`'s line-based `_ENTRY` regex. Applies to every dump path through that one factory (load, dump, verify) -- no second yaml-construction path. Regression test `test_long_key_line_is_not_wrapped_and_survives_fleet_scan_parse` added to `test_sprint_plan.py`: a literal 100-char story key (the story AC's own size, now safe under both raised ceilings) round-trips through the real `fleet_scan.parse_sprint_status` with its status intact. Upstream-PR candidacy recorded here, not opened -- this is an in-place edit to an installer-owned skill file. `uv run .claude/skills/bmad-sprint-planning/scripts/tests/test_sprint_plan.py` -- 41/41 green.
- (decision by claude) 2026-09-09 OQ-1 CLOSED (fleet-readiness-decision-batch-2026-09-09 row mars-B-OQ1, operator-approved): the verb is `marshal factory dispatch`; there is no `marshal dev` family. Decided by shipment (Stories 22.1/22.7/22.9/22.11 all done) and now load-bearing OUTSIDE marshal: the branch grammar dispatch/<project_slug>/<key> is a constant in the shared spine (pyforge-core landing_evidence.py:50 DISPATCH_BRANCH_PREFIX, recognized at :172 and :305) and consumed by doctor. Renaming would break a cross-package grammar. PRD Q-15 inherits this answer.
- (decision by claude) 2026-09-09 OQ-2 CLOSED (fleet-readiness-decision-batch-2026-09-09 row mars-B-OQ2, operator-approved): the launch mechanism is profile-driven and already shipped as Story 22.8 -- core/harness_profile.py plus data/harness_profiles/**. The residue is not a closure but a Constraint (next entry): nothing in the profile mechanism enforces the plain-agent rule.
- (constraint by claude) 2026-09-09 NEW CONSTRAINT (fleet-readiness-decision-batch-2026-09-09 row mars-B-OQ2, operator-approved): a dispatched session is NEVER launched from a fork subagent -- bmad-build-auto mandatory subagents break inside a fork, so use a plain agent, never subagent_type: fork. Story 22.8 profile mechanism does not enforce this; it is a contract line, not a code guarantee. Recorded here so it survives the auto-memory layer (feedback_bmad_dev_auto_needs_non_fork_agent.md) and the fleet-drain-playbook companion.
- (decision by claude) 2026-09-09 OQ-3 ANSWERED by operator (fleet-readiness-decision-batch-2026-09-09 row mars-B-B11): the enforceable budget signal on a dispatched session is WALL-CLOCK plus IDLE-STRAND, enforced by the dispatch supervisor; TOKEN CEILINGS STAY ADVISORY until Epic 33 benchmark artifact exists. Evidence: the dispatch supervisor terminalizes on process and git facts only (dispatch_supervisor/__main__.py, core/dispatch_supervisor_state.py; Stories 22.2 and 28.17) and has no provider-side meter; the loop path weighted-token ceilings are harness self-reports (core/supervise.py) with mid-session blindness recorded as DW-FU-3-6-6. Rejected alternative: block dispatch budgets entirely on Epic 33 -- it leaves today unattended drain with no ceiling at all.
- (decision by claude) 2026-09-09 OQ-4 CLOSED (fleet-readiness-decision-batch-2026-09-09 row mars-B-B12, operator-approved): the cross-station disjointness advisory compares DECLARED surfaces only -- that is what shipped (cli/dispatch.py:868 _effective_surface_for_spec computes policy_surface intersect spec_surface from the candidate spec text; core/dispatch_fleet.py:770-790 intersects pairwise; an unknown surface refuses with reason=unknown-surface at :772-774). No live-diff comparison exists anywhere. CONSEQUENCE recorded as part of the closure: WITHIN-STATION FAN-OUT IS IMPOSSIBLE UNTIL STORY SPECS DECLARE THEIR OWN `surface:` -- Story 28.14 / CAP-16 auto-derives policy_surface to the station whole package tree when no [epic_surfaces] entry exists (core/gate.py:348-423), so two same-station stories overlap by construction and the pairwise test refuses.
- (decision by claude) 2026-09-09 OQ-5 CLOSED, and the shipped answer is neither option the question offered (fleet-readiness-decision-batch-2026-09-09 row mars-B-OQ5, operator-approved): a SIBLING supervisor was built -- marshal/dispatch_supervisor/__main__.py beside marshal/supervisor/**, with its own state and retry modules (core/dispatch_supervisor_state.py, core/dispatch_retry.py). The existing supervisor was NOT generalized and Story 3.4 sidecar was NOT reused. CONSEQUENCE, recorded because it is load-bearing elsewhere: TWO SUPERVISORS NOW WRITE RUN STATE -- which is exactly why the CAP-17 publishing seam must be ONE publisher (spec-marshal-token-economy CAP-18, Epic 33).
- (event by claude) status: in-progress -> shipped -- CAP-1..11 decompose to Epic 22 (22.1-22.11) and Epic 29 (29.1-29.2), all `done`, and all five open questions are closed by this batch (fleet-readiness-decision-batch-2026-09-09 Class B, row mars-B). open_questions: [] on the re-derive.
- (decision by claude) 2026-09-09 CROSS-STATION, ONE WRITER (fleet-readiness-decision-batch-2026-09-09 row mars-A-E3 / C7 48.1): marshal owns src/shared/packages/pyforge-marshal/tests/unit/test_promote_sprint_status_regressions.py:55 (test_terminal_is_only_done asserts TERMINAL == frozenset({"done"})), which PINS the scripts/promote_sprint_status.py:91 bug steward Story 48.1 fixes. Decision: NO marshal story is minted for the amendment -- steward 48.1 already names the file in its own Surface and And-clause, and a second marshal story would make two writers of one test. Marshal obligation is consent plus review; this spec (the ledger-owning one, CAP-4 triggers sprint-ledger-sync on every landing) carries the record.
- (change by claude) 2026-09-09 SURFACE SCOPE GREW, recorded here because this Spec owns the ledger contract and the sprint-planning trio (fleet-readiness-decision-batch-2026-09-09 § 2.4 row D9): the in-place-edited installer-owned pool is now ELEVEN, not seven, with FOUR ungoverned; three of those four have never been named in ANY artifact -- .claude/skills/bmad-retrospective/scripts/sprint_status.py, its test .claude/skills/bmad-retrospective/scripts/tests/test_sprint_status.py, and .claude/skills/bmad-sprint-planning/sprint-status-template.yaml, the last of which carries the Epic-44 `blocked` restore, so an ungoverned in-place edit to it is a silent change to a gating artifact. Story 31.4 (`backlog`) had its Surface and its Given/When/Then AMENDED to the pool of eleven rather than re-minted, per the batch instruction; no new story, no `done` key touched. This Spec claims the sprint-planning trio and the two sprint-status files when 31.4 lands; spec-marshal-token-economy keeps the four bmad-build-auto files.
- (event by claude) 2026-09-09 LEDGER MINT via this Spec CAP-4 machinery: twelve keys added to _bmad-output/projects/pyforge-marshal/planning-artifacts/sprint-status-ledger.yaml (Stories 33.1-33.10, epic-33 backlog, epic-33-retrospective optional) with `sprint_plan.py generate ... --set 33-4-...=blocked`, dry-run first and semantically diffed: +12 keys, 0 changed values, 0 dropped orphans, 0 illegal. `sprint-ledger-sync` was NOT run in any form -- the Tier-3 feed now lags the tracked twin by these twelve keys, which is correct until steward Story 48.1 lands its STICKY_STATUSES guard. Verified afterwards: 227 story keys == 227 `### Story` headings in epics.md. The generator also canonicalized the file (epic-grouped ordering plus the standard generated/last_updated/project/project_key/tracking_system/story_location block, which this ledger had been missing) -- matching pyforge-steward own post-generate shape; no story value moved.
- (event) (self-validate pass 1 -- coherence) 2026-09-09 re-derive: PASS. status in-progress -> shipped per the memlog status event; open_questions emptied and the body section rewritten as a dated closed-questions record (question text struck through, answer appended) rather than deleted; the fork prohibition renders as a HARD Constraint, not an Assumption, because it binds the launch path; the two Assumptions it supersedes are struck through with dated amendments. 11 CAP ids, unchanged.
- (event) (self-validate pass 2 -- preservation) 2026-09-09 re-derive: PASS. All nine 2026-09-09 entries landed: OQ-1..OQ-5 closures with their evidence and both recorded consequences (within-station fan-out impossible; two supervisors -> why token-economy CAP-18 is one publisher), the fork-prohibition Constraint, the 48.1 one-writer decision and the D9 eleven-file pool record in a new Records section, and the Epic 22/29 ledger rows flipped to done. Only superseded frontmatter and duplicated question prose were removed.
- (decision by claude) 2026-09-09 (spec-31-4 follow-up review): closes the D9 eleven-file pool -- the remaining three installer-owned files (`.claude/skills/bmad-sprint-planning/sprint-status-template.yaml`, `.claude/skills/bmad-retrospective/scripts/sprint_status.py`, `.claude/skills/bmad-retrospective/scripts/tests/test_sprint_status.py`) added to this spec's `surface:`. The sprint-planning trio landed 2026-09-06; this pass completes governance for the template (Epic-44 `blocked` restore docs) and the retrospective width-fix pair already patched in code during the first pass. All eleven in-place-edited installer-owned files now sit under marshal spec surfaces: this spec's ten (six marshal/pyforge paths + four bmad-build-auto/bmad-sprint-planning/bmad-retrospective skill files) plus `spec-marshal-token-economy`'s `compile-epic-context.md`.
- (event by claude) 2026-09-10 (spec-28-30-output-caveman-compresses-dispatch-sessions-too-not-just-spin): edited `.claude/skills/bmad-build-auto/step-01-clarify-and-route.md`, one of this spec's own governed `bmad-build-auto` skill files — added a new "Output compression (Story 28.30, do this before the intent check)" section instructing the agent to invoke the deployed `caveman` skill via the Skill tool when `cli/dispatch.py::_seed_dispatch_output_layer` has deployed it into the worktree. Placed before the existing "Intent check" section (not folded into the numbered INSTRUCTIONS list further down, which is heavily cross-referenced by number elsewhere in the same file) so it governs every early-exit branch. No other governed file in this spec's surface touched by this story.
- (event by claude) 2026-09-10 (spec-28-33-the-structure-graph-reference-is-wired-for-spin-unblocking-the-already-built-loop-home-index): edited `.claude/skills/bmad-build-auto/step-01-clarify-and-route.md` again, one of this spec's own governed `bmad-build-auto` skill files — added a new bullet to item 1 "Load context" telling the agent to prefer `codegraph context`/`codegraph explore` over unbounded file reads when `.codegraph/codegraph.db` is present. Self-gated on the index's own presence, so a dispatch worktree (no index until Story 28.31 resolves its own cost question) reads nothing new. No other governed file in this spec's surface touched by this story.
- (event by claude) Surface reconcile 2026-09-12 (fleet hygiene, same pass as spec-pyforge-core above): 4 governed paths changed by already-landed Epic 22 dispatch-supervision work and the 2026-09-11 `2267f21bb3` cross-project-collision fix, memlog never moved -- `cli/dispatch.py` (added), `cli/spin.py`, `core/dispatch_fleet.py`, `dispatch_supervisor/__main__.py`. No CAP text change. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-single-story-dispatch`.

