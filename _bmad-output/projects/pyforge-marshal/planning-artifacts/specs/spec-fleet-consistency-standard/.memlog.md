---
topic: Fleet consistency standard — the operating model matches BMAD 6.12, one test-suite vocabulary, no artifacts the toolchain no longer produces, machine-classifiable naming, declared Python floor equals tested floor, and governance docs that cannot go stale silently
updated: 2026-09-07T22:36
---

- (direction) Operator brief 2026-09-07: 'simplicity and consistency decides' -- converge on BMAD 6.12 defaults, retire customizations no longer relevant, do not introduce new inconsistencies. All decisions pre-made in this planning session and recorded in docs/dreams/pyforge-marshal.md; no re-elicitation.
- (note) Measured baseline 2026-09-07: 25/25 detectors green, five-tier 40/40, 2 TODO markers in all station source. The drift is in connective tissue, not content.
- (capability) CAP-1 -- the operating-model standard is BMAD 6.12-accurate and derives what it can rather than restating it. EXEMPLAR-STANDARD.md's 16-stage table names bmad-document-project, bmad-create-story, bmad-check-implementation-readiness, bmad-dev-auto (all removed/renamed 6.11-6.12) plus three research skills consolidated into bmad-deep-recon.
- (capability) CAP-2 -- one test-suite vocabulary across the fleet: unit/ + integration/ + meta/. Today eight names cover two concepts and the coverage gate's suite map sees neither 'conformance', leaving 51 real test files measured by nothing.
- (capability) CAP-3 -- no artifact survives that the toolchain no longer produces. epics-with-stories.md appears nowhere in BMAD 6.12 and is frozen at 2026-08-08 in six of eight stations while epics.md moved to 2026-09-06.
- (capability) CAP-4 -- artifact naming is machine-classifiable. implementation-readiness-report- carries three date formats across 27 files; bmad_drift_check.py's classifier matches only the hyphenated ISO form, so the compact variant reads 'uncovered' the moment the detector is pointed at those five stations.
- (capability) CAP-5 -- the declared Python floor equals the tested floor. pixi.toml pins python >=3.14.7,3.14.* and every env-scoped pin is 3.14.*; eight of ten packages declare >=3.12, a compatibility claim nothing has ever exercised.
- (capability) CAP-6 -- governance docs cannot go stale silently. A detector resolves every bmad-* skill name, script path and file reference in EXEMPLAR-STANDARD.md / AGENTS.md / CLAUDE.md / docs/reference/test-charter.md and fails when one no longer exists. Same shape as the existing failure_catalog_check.
- (decision) EXEMPLAR-STANDARD.md is rewritten as this Spec's companion, NOT deleted. pixi.toml:947 names it as the dream-chain detector's 'Contract:' and 33 other files reference it, including PROJECTS.md, four marshal Specs, three doctor Specs. Operator confirmed 2026-09-07.
- (decision) The Dream tier is a new section in the consolidated docs/dreams/pyforge-marshal.md, not a standalone Dream file. Fourteen Dreams have been folded into that file across 2026-08-02 and 2026-08-08, two of them on this exact subject (bmad-output-hygiene, dream-to-code-model-self-verification), each keeping its Spec live.
- (decision) Coverage work amends spec-pyforge-testing-charter CAP-4 rather than being re-minted here. CAP-4 is written, marshal-owned, status ready, and docs/reference/test-charter.md Phase 4 carries the matching unchecked box.
- (decision) Suite standard is BMAD 6.12's Python default (tests/conftest.py + unit/ + integration/) plus meta/ as the single justified addition -- 8/8 station adoption and load-bearing (spec-surface, portal, five-tier tests live there). api/ is a 6.12 default the fleet does not use and does not adopt.
- (decision) 'conformance' folds by test level, not by name: steward's 32 files are CLI-contract tests its own test-architecture.md classifies as unit level, so they go to unit/; warden's 19 are oracle/engine gates (dogfood, corpus determinism, perf, parallelism) and go to integration/. marshal's contract/ (1 file) and oracle/ (1 file) are empty placeholders declaring the same two concepts and fold the same way.
- (decision) marshal/support/ is renamed _support/ -- it holds __init__.py + testing_kit.py, a helper module, not a suite; the leading underscore keeps it out of suite globs. Imports updated with the move.
- (decision) atlas's 23 topic dirs move wholesale under unit/ preserving their structure (unit/<topic>/) rather than being re-sorted by level; they are a domain taxonomy, not a suite taxonomy. Operator confirmed 2026-09-07.
- (constraint) tests/conftest.py and tests/fixtures/ stay at each station's tests root during the convergence so shared fixtures remain visible to both unit/ and integration/.
- (constraint) AGENTS.md invariants are written only through bmad-project-context. Its managed block starts at line 9 and is replaced on refresh -- a hand-edit inside it is destroyed on the next run.
- (constraint) The spec-surface baseline re-stamp runs LAST, after every file move and after git add. --write-baseline reads the working tree and git ls-files, so stamping earlier would bake the pre-move state in and report every moved file as drift.
- (constraint) No station's own test suite may be red at any checkpoint. A green suite is the gate for each convergence commit, not a reading of the diff.
- (assumption) Raising requires-python to >=3.14 breaks no external consumer: no recipes/pyforge-* exists, so the stations are built via pixi-build-python for this repo's own 3.14 estate and are not published to conda-forge.
- (assumption) Folding steward's conformance suite into unit/ inherits the unit floor (80% default) rather than integration's 70%. Taken from steward's own test-architecture.md classification, not independently re-judged per file.
- (decision) Coverage floors are seeded from measurement, never from the charter's stated 80/70 on day one: add pytest-cov to the seven envs that lack it, measure all eight, write each station's measured value as its starting floor, and let floors only ratchet up. Nobody has ever measured the fleet's real coverage -- spec-pyforge-testing-charter's own assumptions say so.
- (decision) Path-level moves happen now rather than waiting for spec-python-foundry-cutover, which relocates every package to src/packages/ in a new repo. Operator judgment 2026-09-07: consistency now is better for everything including the rewrite, so foundry carries a converged estate instead of replicating today's drift.
- (note) Corrections this session made to an earlier by-hand audit, kept on the record per the provenance rules: the missing .claude/skills/pyforge-mason is NOT a gap (five_tier.py:120 resolves mason's skill tier to conda-forge-expert per Mason Epic 11.1); marshal's planning-artifacts root is not stale clutter (most files touched 2026-09-05/06, and the classifier reports no stray files); the django-* packages are not untested (30 test files across all nine packages import django_pyforge); and atlas/doctor's >=3.14 floor was the correct one, not the outlier.
- (question) Whether mechanizing INV-2/INV-3 into pyforge.doctor.sources lands as a later story here or in pyforge-doctor. Named as deferred in this Spec; EXEMPLAR-STANDARD's own verification section admits both were measured by hand and never mechanized.
- (question) Whether the coverage gate's --cov target should be extended over the django/portal tier. package_src() resolves to pyforge-<station>/src/pyforge/<station>, so django-pyforge's mcp_auth.py, access.py, rate_limit.py, middleware.py and circuits.py are outside every cov target by construction. Deferred to spec-pyforge-testing-charter CAP-4's decomposition.
- (note) resolve_config.py --project pyforge-marshal does not apply the project layer's output_folder override (returns {project-root}/_bmad-output regardless), so this spec folder was addressed by physical path -- which is also what every existing spec in the fleet uses and what bmad_drift_check.py's classifier regex expects.
- (event) Spec authored 2026-09-07 from a completed planning session; capabilities CAP-1..CAP-6 fixed, no open elicitation.
- (event) Self-validate pass 1 (coherence): all six capabilities carry intent+success; constraints each rule something out; five non-goals explicit; success signal is testable (bmad-drift returns zero uncovered against any station; CAP-6 detector reproduces the seven 2026-09-07 findings). PASS.
- (event) Self-validate pass 2 (preservation): every load-bearing claim from the Dream section landed -- stale-skill list in CAP-1, 51-unmeasured-files in CAP-2, epics-with-stories in CAP-3, three date formats in CAP-4, python floor in CAP-5, recurrence detector in CAP-6. The Dream's 'what is NOT drifting' passage (five-tier 40/40, mason->CFE) is preserved as a memlog correction note rather than kernel content: it is provenance, not contract. dream-chain verified green after authoring. PASS.
