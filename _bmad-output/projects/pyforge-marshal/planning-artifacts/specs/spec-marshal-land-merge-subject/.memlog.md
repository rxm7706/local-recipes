---
topic: marshal land renders a detectable, templated merge subject (currently defeats marshal_native_merged_keys detection for its own landings)
updated: 2026-08-12T17:32
---

- (capability) CAP-1: marshal land renders the same templated merge subject deploy land-story already does (identity.render_merge_subject(story_key, template), AD-24), instead of leaving GitHub to auto-generate one. Success: marshal_native_merged_keys classifies a real marshal-land-driven merge subject as native, same as it already does for land-story's.
- (constraint) gh pr merge supports -t/--subject for all three strategies (merge/squash/rebase) -- confirmed via gh pr merge --help. ForgePort.merge_pr's adapter (forge_gh.py) passes it through; the port method gains an optional subject: ForgeRef | None parameter (mirrors AD-34's egress-registry-completeness typing for other ForgeRef params), never a second pre-merge PR-title-edit API call.
- (constraint) Purely additive: no change to marshal land's landing_merge_strategy default, gates, branch-protection behavior, or any other existing step -- only the merge commit's subject line changes.
- (non_goal) Does not retroactively relabel already-'done' ledger rows misclassified as not-loop-native before this fix ships -- corrects classification for merges going forward only.
- (non_goal) Does not touch deploy land-story (already correct), bmad-quick-dev, or Story 5.9's reconcile-completions code -- 5.9 already reads marshal_native_merged_keys, so fixing the classifier's input here improves 5.9's downstream label precision with zero change to 5.9 itself.
- (direction) Not urgent, no current blocker depends on it -- discovered as a follow-up during pyforge-marshal Story 5.9's review pass 2 escalation (2026-08-12); tracked as backlog per operator decision, not sequenced ahead of anything in flight.
- (event) Self-validate pass 1 (coherence): 1 capability with intent+success, 2 constraints (both bend a real design decision: gh flag choice, additive-only scope), 2 non-goals, success signal testable via a real subject string. PASS. Pass 2 (preservation): both Dream frontier claims (marshal land's current behavior + the fix + the follow-on benefit to 5.9's classification) land in CAP-1/constraints/non-goals. No wrapper-only content dropped.
