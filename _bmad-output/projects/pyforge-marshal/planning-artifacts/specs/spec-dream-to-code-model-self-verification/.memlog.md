---
topic: The detectors get detected — fixture-based meta-tests for dream_chain_check and bmad_drift_check, plus a detector-incident log
updated: 2026-08-09T23:30
---

- (provenance) THIS RECORD STARTS HERE. Created 2026-08-09 by marshal S-13.5 (spec-surface-drift-reconciliation CAP-5 / FR-168): this Spec shipped declaring a `surface:` with no .memlog.md, so spec_surface_check's contract_hash() returned "" and the contract could never move — every governed change would have reported a hard [drift] whose printed remedy ("reconcile the spec") was unreachable, there being no contract to move. Nothing that happened before this entry was captured; do not read the absence of earlier entries as an absence of decisions. The baseline was scoped-stamped in the SAME change that created this file, because a new memlog moves the contract hash off "" and would otherwise downgrade this surface's next real drift from gating [drift] to informational [drift-presumed].
- (note) Governs scripts/dream_chain_check.py, scripts/bmad_drift_check.py and .claude/skills/conda-forge-expert/tests/meta/ (2 tracked files matched at the time of writing).
- (capability) 1. Fixture-based meta-test suite for dream_chain_check.py — known-good and known-bad Dream/Spec trees in a temp dir asserting EXACT findings, not merely "the live repo passes". Minimum cases each derived from a real incident: Spec with no owner-dream -> exactly one INV-0; Dream with no Spec -> INV-1; Dream covered via covers-dreams: or a `## Satellite:` heading -> NO INV-1; a Spec with unparseable frontmatter surfaced as a finding, never silently absent; owner: guild beyond the two constitutive Dreams -> INV-2 owner-unassigned; a Spec filed under the wrong station -> INV-2 spec-location-mismatch; flat prd.md / missing epics.md -> the three INV-3 kinds; a conformant tree -> zero findings.
- (capability) 2. Fixture-based meta-tests for bmad_drift_check.py's deterministic checks (pin-missing, archive-misplaced, stray-file, spec-status-stale at minimum). The existing live-repo integrity test stays: it gates the TREE, the new tests gate the DETECTOR.
- (capability) 3. A detector-incident log — a tracked companion recording every occasion a detector's output was found wrong (date, detector, wrong claim, true value, root cause, fixing commit, pinning fixture), seeded with the three real entries and mandatory in the same change that fixes a detector.
- (capability) 4. The --dreams hygiene mode promised by the 2026-07-23 restructure, boundary fixed here: hygiene = per-Dream-file frontmatter validity, as distinct from chain completeness (which stays INV-1/2/3). Fixture-covered from day one; it does not ship untested like its siblings did.
- (note) Not hypothetical: dream_chain_check's output was found materially wrong twice in its first 11 days — 2026-07-28 reported 21 Dreams without a Spec when the true number was 11 (ten Specs declared no owner-dream:, one had unparseable frontmatter that `except: return {}` silently converted to "no Spec"), and the wrong 21 was repeated into EXEMPLAR-STANDARD itself; 2026-08-08 the satellite-consolidation blind spot overcounted INV-1 16 vs 10 real, fixed in e3171bdcc6.
- (question) OPEN, carried from the Spec: does the meta-test obligation cover only these two detectors or every detector scripts/detectors.py discovers? Where the incident log lives (companion in this Spec's dir is the EXEMPLAR-STANDARD default and current lean). Whether --dreams is a new flag or a fourth --inv value.
- (change) RECONCILES `scripts/bmad_drift_check.py` (2026-08-09). Doctor Story 6-8 moved the four Guild-vocabulary constants this module DEFINED — `STATIONS`, `GUILD_DREAMS`, `DREAM_STATUSES`, `DREAM_TYPES` — into `docs/governance/guild-roster.json`, which this module now reads. It stops being the definition site for constitutive data it merely consumes: `stations` is Charter §§1-8 membership and `guild_dreams` is the Charter itself, so the data belongs beside the governance kernel, not inside a detector. That also un-breaks Story 6-9 in advance — retiring this file into Doctor's package would otherwise have stranded `docs/dashboard/generate.py`, which imported these names from here and runs in an environment that cannot import `pyforge.doctor`. Values verified byte-identical to the pre-extraction tuples; `bmad-drift-check` still reports OK.
- (change) RECONCILES `scripts/dream_chain_check.py` and `scripts/bmad_drift_check.py` (2026-08-09, Doctor Story 6-9). Both origin files are DELETED — the retirement this memlog's own prior entry already anticipated ("un-breaks Story 6-9 in advance"). Their logic lives now at `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py::gather_dream_chain` and `sources/factory.py::gather` respectively, governed by `spec-pyforge-doctor`'s own blanket glob. This Spec's surface glob on both files is removed in the same change (see SPEC.md); the third glob (`.claude/skills/conda-forge-expert/tests/meta/`) is unaffected and stays. Both `test_bmad_artifacts_in_sync.py` (re-targeted at `sources.factory.gather`/`ground_truth`) and `test_spec_surface_check.py` (re-targeted at `sources.chain.gather_spec_surface`) under that still-governed path are updated in this same change, per this Spec's own capability 2 (fixture-based meta-tests survive the port, not the file path) — reconciled here too.
