---
topic: marshal watch: promote the operator's re-pasted status/delta/pacing ritual (captured live as the Claude-only marshal-run-watch skill) into a real pyforge-marshal CLI capability
updated: 2026-09-15T22:53
---

- (capability) CAP-1: marshal watch CLI verb — pinned-run, station, or fleet scope; gathers ground truth (bmad-loop status/list for the bmad-loop pattern, marshal status dispatch_* fields for the bmad-build-auto pattern), diffs against the last observation, and reports only on real change or a caller-chosen cadence boundary. Ports the validated logic from .claude/skills/marshal-run-watch/SKILL.md, not a redesign.
- (capability) CAP-2: The stale-dispatch trap is closed — for a bmad-loop-pattern run, marshal watch never reads marshal status's dispatch_* fields (a possibly unrelated prior one-shot dispatch record) as that run's per-story ground truth; bmad-loop status/list is authoritative for that pattern.
- (capability) CAP-3 (follow-on, not this round): an MCP tool on POST /stations/marshal/mcp wrapping marshal watch, following django-marshal's existing bespoke @server.tool(...) registration pattern in mcp_asgi.py.
- (capability) CAP-4 (follow-on, not this round): a bmad-agent-marshal persona menu entry invoking pyforge marshal watch grammar — no persona skill-file change needed beyond the menu entry, since CAP-16 already allows any pyforge marshal ... grammar call.
- (capability) CAP-5 (follow-on, not this round): a django-marshal portal view (alongside chrome_home in views.py) rendering marshal watch's report for browser viewing.
- (constraint) Ports the skill's logic faithfully — the delta predicate (story phase/commit_sha, run status, escalation, loop-branch SHA, PR state) and the boundary/backoff pacing (min(300s, seconds-to-next-:00/:30-boundary) while active, boundary-only while paused/escalated) were validated live against a real multi-hour bmad-loop run (pyforge-herald 20260914-201759-bd47, Epic 21) and must not be redesigned in translation.
- (constraint) Read-only: marshal watch observes and reports; it never resolves escalations, resumes runs, dispatches new work, or writes any sprint ledger. Those stay bmad-loop resolve / marshal factory resume / marshal factory dispatch.
- (constraint) No second status engine: marshal watch is a caller-facing report shape over the SAME ground truth marshal status and bmad-loop status/list already read (journals and run state) — it must not compete with marshal status as a second verdict.
- (constraint) Persisted last-observation state is a local, regenerable cache (mirroring the skill's .claude/data/marshal-run-watch/*.json), never a new durable ledger; a missing or stale cache degrades to a first-observation report, never a wrong delta.
- (constraint) CLI first: this Spec's implementable round is CAP-1+CAP-2 (the marshal watch CLI verb, correct for both the bmad-loop and bmad-build-auto patterns). CAP-3/4/5 (MCP, persona menu, portal view) are named but explicitly deferrable to later stories in the same epic.
- (decision) CAP-1's output format for the pinned-run/station report mirrors the skill's five-section shape (Session Completions, Delta Since Last Update, Currently Running, Up Next & Full Queue, User Action Required); the fleet-scope report mirrors the skill's compact per-project snapshot + escalation-first sort.
- (decision) marshal watch does not itself call ScheduleWakeup or implement the /loop cadence — it is a single invocation that gathers, diffs, reports, and prints a recommended next-delay + reason; the caller (a human, a /loop session, or a future scheduler) decides whether and when to invoke it again. This matches marshal's own CLI layering: the CLI reports and recommends, it does not own a scheduler.
- (non_goal) Not in scope: MCP tool, persona menu entry, portal view (CAP-3/4/5, explicitly deferred), event-driven wake (no Monitor-equivalent — polling only), writing to any ledger, resolving escalations, dispatching work, or replacing marshal status as the fleet-summary command.
- (note) Success signal: an operator (or the bmad-agent-marshal persona) runs marshal watch --project <slug> [--run <run_id>] or marshal watch --fleet from a fresh terminal with no prior state and gets the same report shape and correctness the hand-driven skill produced against the live pyforge-herald run on 2026-09-15 — including correctly ignoring a stale unrelated dispatch_* record — without any Claude-skill file being consulted.
- (event) Self-validate Pass 1 (coherence): PASS — all 5 CAPs carry intent+success, intents are WHAT not HOW, 5 constraints each rule something out, 3 non-goals explicit, success signal is concrete/demonstrable, CAP IDs unique. No fixes needed.
- (event) Self-validate Pass 2 (preservation): PASS — every load-bearing Dream claim (the hand-driven ritual's cost, the stale-dispatch trap, the CLI-verb capability, the four deferred faces, the faithful-port constraint, read-only, no-second-engine, local-cache, CLI-first scoping, the success signal) lands in SPEC.md. Kinships intentionally stay in the Dream only, per convention. No wrapper-only drops.
- (event) Spec finalized: status ready. Zero open_questions — the seeding Dream left none open. Implementable round is CAP-1+CAP-2; CAP-3/4/5 are named follow-ons for later stories in the same epic.
- (event) CAP-3/4/5 landed: marshal_watch MCP tool, WATCH persona menu, portal /stations/marshal/watch/. Epic 44 done.
