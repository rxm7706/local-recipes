---
topic: promote_sprint_status.py's regression guard only catches done->backlog, silently allowing done->blocked (and other non-done statuses) to overwrite a correct tracked-ledger done with a stale Tier-3 value
updated: 2026-09-10T09:49
---

- (note) scripts/promote_sprint_status.py promotes each BMAD project's gitignored Tier-3 implementation-artifacts/sprint-status.yaml into its tracked planning-artifacts/sprint-status-ledger.yaml twin; run fleet-wide (8 pyforge stations + local-recipes dashboard render), idempotent, 'run it when a story lands, then commit' per its own pixi task description
- (note) Reproduced live 2026-09-10 against pyforge-doctor: story 20.4 was genuinely done (its spec frontmatter says status: done, landed commit 2deed798af9 says 'Flips 20-4 done in the ledger'), but the Tier-3 feed held a stale 'blocked' value predating that landing. Running promote_sprint_status.py --project doctor did not flag 20.4 in its refusal list (only 4 other keys with an explicit done->backlog mismatch were named) and silently overwrote the ledger's correct done with the stale blocked
- (constraint) the regression guard (refusal check) and --repair-feed (write-back) both currently test specifically for the transition done->backlog, not done->anything-other-than-done, so blocked/in-progress/ready-for-dev/review all pass through undetected
- (capability) CAP-1: an operator promoting Tier-3 status into the tracked ledger can trust that ANY drift away from a tracked done (not just done->backlog) is caught before it overwrites the ledger. success: given a tracked done key whose Tier-3 twin holds ANY other status, a plain promote refuses and names the key exactly as it does today for done->backlog, and --repair-feed restores it into Tier-3 exactly as it does today for done->backlog
- (constraint) no change to the script's other behavior: --allow-regression still overrides for a genuinely-wrong twin, --project scoping unchanged, the done->backlog case's existing message text/behavior stays byte-identical (only the detection condition widens)
- (note) fix implemented directly by the operator immediately after this spec (not dispatched); this is a proportional bug fix to an existing detection routine, not a new feature
- (event) Self-validate: Pass 1 coherence OK (CAP-1 has intent+success, constraints bend design, 2 non-goals, testable success signal). Pass 2 preservation OK (all 5 load-bearing memlog entries landed in SPEC.md; entry 6 is process metadata, correctly omitted). Spec finalized.
