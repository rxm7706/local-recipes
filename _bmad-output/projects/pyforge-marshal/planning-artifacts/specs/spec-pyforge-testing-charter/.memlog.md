---
topic: Fleet-wide systematic test architecture for the 8 PyForge Guild stations
updated: 2026-09-24T03:59
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-testing-charter`.


- (note) Input: docs/dreams/pyforge-testing-charter.md (pre-existing, real content — only frontmatter touched by the 2026-08-02 09:42 bulk commit dad47c408a). Verified against live repo state before distilling; several of the dream's own claims did not survive verification.
- (assumption) The dream's 'Fleet Completeness Tracking' TEST column (herald + warden both shown checked/complete) is not evidence — same-day audit found the per-station test-architecture.md files for atlas/doctor/mason/scribe/steward/warden are boilerplate: 78-81 lines, identical structure with only station nouns swapped, 'Target Stories: TBD' on every row, wrong filename vs what bmad_tea_playwright.py actually emits (test-architecture-tea.md, not test-architecture.md). Herald's (2077 lines) and Marshal's (438 lines) are real, story/epic/FR/AD-grounded content.
- (decision) The real gap is not 'no tests exist' — every station already has real pytest files at the canonical src/shared/packages/pyforge-<slug>/tests/ location (verified counts: atlas 78, warden 54, doctor 13, mason 8, steward 7, scribe 3, herald 20, roughly tracking each station's story-completion %). The real gaps are: (1) no fleet-wide traceable test-architecture DOCUMENT for 6 of 8 stations, (2) the automation script that should produce it was only ever run/adapted for 2 stations, (3) the dashboard's own 'tea' completeness signal (this session, generate.py _stage_globs) reads _bmad-output/projects/<slug>/tests/ — a planning-scaffold location holding only mocks/fixtures — not the canonical src/shared/packages/pyforge-<slug>/tests/ where the real tests live, so it under-reports every station's true coverage. (4) no shared fixtures package (pyforge-testing-kit) exists, confirmed absent repo-wide. (5) no CI coverage-threshold gate exists per station — pytest tasks run tests but nothing enforces unit>80%/integration>70%.
- (capability) CAP-1 — Correct fleet-wide TEA signal. intent: an operator or the dashboard can tell, per station, whether real test coverage exists, without being misled by a planning-scaffold directory that only ever holds mocks/fixtures. success: generate.py's tea glob points at src/shared/packages/pyforge-<slug>/tests/test_*.py (the canonical location per project-context.md's own workspace-package convention), not _bmad-output/projects/<slug>/tests/; re-running dashboard-gen after the fix reports atlas and warden as materially tested (78 and 54 real files) rather than pending.
- (capability) CAP-2 — One automation path, run for real per station. intent: bmad_tea_playwright.py is the single generator for a station's test-architecture document, and every station's document was actually produced by running it against that station's real epics-with-stories.md + architecture, not hand-typed. success: script's output filename convention is reconciled with the committed convention (currently a mismatch: script emits test-architecture-tea.md, committed files are test-architecture.md); running it against atlas/doctor/mason/scribe/steward/warden's real epics + existing code produces a document referencing actual story IDs and modules, replacing the 6 boilerplate stand-ins; a document that still contains the literal token 'TBD' after generation is a failed run, not a placeholder to fill in later.
- (capability) CAP-3 — Shared test-support package (pyforge-testing-kit). intent: a station author does not hand-roll CLI runner fixtures, DB factories, or mocks that another station already wrote. success: a pyforge-testing-kit package exists under src/shared/packages/ with CLI runner, web page-object, DB factory, and auth/HTTP/time mock primitives; Marshal's four already-real mocks (mock_github_api.py, mock_worktree.py, mock_supervisor.py, mock_runner.py, under _bmad-output/projects/pyforge-marshal/tests/mocks/) are evaluated as the seed rather than rewritten from scratch; at least one other station's tests import from the shared kit instead of a local duplicate.
- (capability) CAP-4 — Coverage gate enforced, not just measured. intent: a PR cannot merge with unit coverage under 80% or integration under 70% for the package it touches, mirroring the dream's stated targets. success: each station's existing pyforge-<slug>-test pixi task (or a new pyforge-<slug>-test-coverage task) runs with --cov and a --cov-fail-under threshold; CI invokes it; a station under threshold fails the build with the specific uncovered module named, not a bare percentage.
- (capability) CAP-5 — Retirement-grade test-architecture.md for the 2 stations that already have one. intent: Herald's and Marshal's existing real test-architecture documents stay current as more stories land, instead of freezing at today's snapshot. success: re-running bmad_tea_playwright.py (or its manual-equivalent process) against Marshal's epics after each epic completes regenerates the story-coverage table without hand-editing; a story that shipped without its test-architecture row updated is a detectable drift, not a silent gap.
- (constraint) No manual re-work per station — test-architecture.md is machine-generated by bmad_tea_playwright.py from epics + architecture, never hand-typed; a hand-typed one is exactly the failure mode this spec exists to close.
- (constraint) One framework, not many — Playwright for CLI/web/integration, pytest for unit, already proven by Herald (20 real tests) and Marshal (20 real tests); no station introduces a second test framework.
- (constraint) No fake passes — a generated document or scaffold that does not reflect real, runnable code is worse than an honestly-absent one, because it reads as done to both the dashboard and a future agent. A test-architecture.md containing the literal token 'TBD' fails validation.
- (non-goal) Retroactively writing missing unit/integration tests for every uncovered module across all 8 stations in one pass. This spec fixes the SIGNAL (CAP-1), the GENERATOR (CAP-2), the SHARED PRIMITIVES (CAP-3) and the GATE (CAP-4); closing individual per-story coverage gaps remains each station's own story-level work.
- (non-goal) Building a new test framework or methodology. Playwright + pytest + BMAD TEA is already chosen and proven twice over (Herald, Marshal); this spec scales the existing pattern, it does not replace it.
- (non-goal) Retroactively migrating Herald's or Marshal's already-real tests onto the shared pyforge-testing-kit. CAP-3 seeds the kit from Marshal's existing mocks for stations built from here forward; rewriting two stations' already-working test suites to consume it is not required.
- (note) Success signal: dashboard-gen (after CAP-1's glob fix) reports atlas and warden's tea stage as populated, not pending; all 8 stations' planning-artifacts/test-architecture.md was produced by bmad_tea_playwright.py and contains zero 'TBD' tokens; pyforge-testing-kit is importable from at least one station's tests other than the one it was seeded from; a PR that drops a touched package's coverage below its station's gate fails CI naming the uncovered module.
- (note) Spec-authored companion station-tea-status.md holds the per-station verified data table (real test counts vs test-architecture.md quality per station) — a multi-item catalog per Companions rule 1, cited by CAP-1/CAP-2/CAP-5.
- (event) Self-validate Pass 1 (coherence): PASS. 5 capabilities each carry intent+success (rule 1); intents are WHAT-shaped, CAP-2's naming of bmad_tea_playwright.py is specifying the canonical existing tool, not prescribing new HOW (rule 2); all 3 constraints genuinely rule something out (rule 3); 3 non-goals present, exceeds minimum (rule 4); success signal is 4 concrete pass/fail conditions (rule 5); CAP-1..CAP-5 unique, first assignment on create (rule 6); prose is lean, no throat-clearing found (rule 8).
- (event) Self-validate Pass 2 (preservation): PASS after one fix. Walked the Dream's 8-item Realization list + Mottos/roadmap section claim by claim: items 1/2/3 (Herald real, script exists, charter doc exists) verified true and cited; items 4/5/7/8 (testing-kit, fleet deployment, dashboard tracking, CI gates) map to CAP-3/CAP-2/CAP-1/CAP-4 respectively; item 6 (each story ships tests) folded into CAP-4 as an enforcement mechanism rather than a separate capability, not a silent drop. Found one preservation gap during the sweep: the Mottos table + per-Smith responsibility breakdown + Phase 1-4 roadmap in docs/reference/test-charter.md were initially listed under sources: (implying full absorption) but that content was not reproduced in the kernel — moved to companions: as adopted, per the Companions rule for pre-existing project-wide docs downstream still needs to read directly. No other wrapper-only content found to log.
- (event) Spec finalized: 5 capabilities, 1 spec-authored companion (station-tea-status.md), 1 adopted companion (docs/reference/test-charter.md), 2 assumptions, 0 open questions. status: ready.
- (event) CORRECTION, follow-up session: CAP-2's premise that bmad_tea_playwright.py 'mostly works, needs filename reconciliation' was wrong. Actually tested it (not just read): (1) it has a live Python regex SyntaxError on line 64 (unescaped ** inside epic_pattern -- 're.PatternError: multiple repeat') -- the script has never successfully executed once, on any project, ever. (2) Even hypothetically fixed, its parser requires a specific per-story '### Story N.M: Title' header format that does NOT match the real documents: Atlas's epics-with-stories.md uses Wave-lettered story ids (A1, B2...) with no '**Goal**:' epic marker; Warden's epics-with-stories.md is a dense prose epic-summary with inline 'Stories (N): 1.1 <title> · 1.2 <title>' lists, no per-story headers at all, and carries literal placeholder text ('title: "Station -- Complete Epics & Stories"') suggesting it is itself boilerplate, not real content. No re.DOTALL/re.MULTILINE flags either, so even a fixed regex could not span multi-line paragraphs. Herald's and Marshal's real test-architecture.md docs were therefore NOT produced by running this script -- they were hand-authored by an agent applying the TEA methodology directly. Robustly fixing the script to handle the real format diversity across all 8 stations is a real, separate engineering task, not a rename -- deferred, not attempted this pass. Atlas and Warden's test-architecture.md are being hand-authored this session instead, matching Herald/Marshal's demonstrated quality bar, to unblock the immediate ask without a risky rewrite under time pressure.
- (change) RECONCILED BY NAME, 2026-08-09: pyforge.doctor.sources.fleet_scan changed under this surface — S-5.7 (FR-179) added `build_fleet_progress`, a read-only roll-up counted from the tracked sprint ledgers via the SAME `parse_sprint_status` the deploy already uses. It adds a key to the data contract and changes no existing behaviour, so nothing this Spec contracts is affected. Acknowledged by name rather than silently stamped: generate.py is governed by five surfaces at once, and stamping four of them while recording nothing is the laundering S-13.2 exists to end.
- (change) S-5.7 AMENDED, 2026-08-09 — the LOCAL board now matches the CLI. Reconciles by name: pyforge.doctor.sources.fleet_scan (`enrich_fleet_progress_live`, called only on the non-git source path) and docs/dashboard/index.html (the section reads "Fleet picture · live" locally, "· tracked" on Pages, and shows `→ projected` plus a RUNNING state only when the live block exists).
- (correction) The operator was right and the first cut was under-built: the LOCAL board is generated on the machine that has tmux and ~/.bmad-loops, so it CAN match `fleet-picture` completely — only the PAGES deploy is limited. The live fields were therefore added to the local branch, exactly where `apply_loop_inflight` already lives, and their ABSENCE is what makes a Pages render honest rather than stale.
- (correction) The first live-detection used `_live_loop_sessions()` (tmux) and was WRONG BOTH WAYS: it reported marshal running because a tmux session lingered hours after its engine died, and missed steward and mason entirely. A multiplexer session is not an engine. Replaced with `marshal status --format json` — the SAME authority scripts/fleet_picture.py reads — so the board and the CLI cannot disagree about who is running. Caught by checking `ps` against the rendered output rather than trusting the first green.
- (change) RECONCILES `pyforge.doctor.sources.fleet_scan` (2026-08-09). Doctor Story 6-8 gave the Guild vocabulary ONE home: `DREAM_STATUSES` and `DREAM_TYPES` were RE-DECLARED here, identical to `scripts/bmad_drift_check.py` by luck rather than construction; `GUILD_DREAMS` and `STATIONS` were imported FROM that detector, which was the right fix for the 2026-07-28 drift but made the board depend on a detector for constitutive data — and Story 6-9 retires that file into Doctor's package, where this environment cannot import it. All four now read `docs/governance/guild-roster.json`. Values verified byte-identical to the pre-extraction tuples, and the AST sweep the 6-8 spec demands reports an EMPTY intersection of module-level constants between the two files. No behavioural change to this Spec's own surface: `dashboard-gen` runs clean and `dashboard-check` renders (9 lines, 58 dreams, 60 chains).
- (note) Co-governed path touched (2026-08-11): pyforge.doctor.sources.fleet_scan — scan_backlog's satellite-build-line derivation (full rationale in spec-sprint-status-auto-promote's memlog; console presentation rationale in spec-factory-console's). NOTHING in this spec's own concern was altered: _stage_globs and the TEA completeness signal are untouched — and per the Phase-2 audit FR-128 is already shipped (generate.py:1724 has read the canonical src/shared/packages/<slug>/tests/ tree since 2957718d4c). Named here because this surface co-governs the file and a stamp without a record is the laundering S-13.2 exists to end
- (note) Co-governed path touched (2026-08-13): pyforge.doctor.sources.fleet_scan -- fixed enrich_fleet_progress_live/scan_impl_campaign's live-station detection, which had always read marshal status --format json's homes list from the wrong top-level key (should be the data.homes envelope, per fleet_picture.py's own unwrap) and so never found a single running station; also gave scan_impl_campaign its own live/not-live distinction (in-progress vs running) instead of a done>0 heuristic that mislabeled stopped stations RUNNING. NOTHING in this spec's own concern (TEA generator / coverage signal) was altered. Named here because this surface co-governs the file and a stamp without a record is the laundering S-13.2 exists to end.
- (note) Co-governed path touched (2026-08-13): pyforge.doctor.sources.fleet_scan -- fixed apply_loop_inflight's status-flip condition, which only fired when a story's current status was exactly 'pending'. The Tier-3 feed marks a story done at DEV completion, before review, so a story mid-review already read 'done' by the time this ran -- the flip silently never fired even though state.json (the run's own authority, already gated above on done_phases) proved the story still active, so the elapsed-clock card populated (unconditional) while the line-state chip stayed PAUSED next to it (caught steward 9.2). Now flips whenever the key is in active_ids regardless of prior status, since active_ids is already the authority. NOTHING in this spec's own concern (TEA generator / coverage signal) was altered.
- (note) Co-governed path touched (2026-08-15, marshal S-16.1, FR-140..143): pyforge.doctor.sources.fleet_scan -- the slug->project-directory resolver consolidation (full rationale in spec-dashboard-project-path-derivation's memlog, the owning surface). Collapsed five independent hand-authored slug/path mechanisms (PROJECT_SOURCES, _KEY_SLUG_OVERRIDE's five call sites, CAMPAIGN_PROJECT_OVERRIDE, IMPL_CAMPAIGN's ad hoc epics_path, IMPL_CAMPAIGN_LEDGER) into one `resolve_project()` + one `_PROJECT_OVERRIDES` table, and made an unresolvable roster/campaign slug exit generate.py non-zero. NOTHING in this spec's own concern (the TEA generator, _stage_globs, the coverage signal) was altered -- verified: no _stage_globs/TEA-related line was touched. Named here because this surface co-governs the file and a stamp without a record is the laundering S-13.2 exists to end.
- (note) Co-governed path touched again (2026-08-15, marshal S-16.1 review pass): pyforge.doctor.sources.fleet_scan -- adversarial-review patches to the resolver landed above (sprint_status_path explicit-None on 3 absorbed-satellite overrides, `_validate_project_overrides()`, a board-key collision guard in `_discovered_board_keys()`, a dead-variable cleanup in `scan_readiness()`). Full rationale in spec-dashboard-project-path-derivation's memlog. NOTHING in this spec's own concern (TEA generator / _stage_globs / coverage signal) was altered.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log

- (note) Steward 30.2 (2026-08-25): RECONCILES docs/dashboard/generate.py (deleted; parsers now scripts/fleet_scan.py). Co-governed Guildhall path retired; this spec's own concern is unchanged except the generator file is gone.
- (change by claude) Surface fix 2026-08-29: scripts/coverage_gates_ci.py and scripts/run_station_coverage_gate.py were ungoverned (no spec surface, no allowlist entry) despite both citing Story 19.3/FR-131 named-module coverage gates in their own docstrings, which this spec decomposes. Added both to surface. No behavior change, governance-only.
- (event) Surface reconcile 2026-09-04 (fix/coverage-gates-push-base): `scripts/coverage_gates_ci.py` normalises its `--base` through `_normalize_base` — a bare commit sha (the push-event workflow's `git rev-parse HEAD~1`) is kept as a revision; only a bare branch name gets `origin/`. Every push-to-main Coverage-gates run had died with "unknown revision 'origin/<sha>'" since 2026-08-24 (seen again on merge commit 09a84392fe). Regression test in `pyforge-marshal/tests/meta/test_coverage_gates_ci_driver.py`; the gate itself is unchanged. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-testing-charter`.
- (change by claude) New capability 2026-09-05 (PR #1045): added `pyforge.testing_kit.branch_diff_guard` — shared mechanics (`changed_paths_since`, `diff_text_since`, `existed_at_ref`, `pyforge_import_offenders`, `commits_since`, `commit_subject`, `commit_files`, `unsanctioned_commits`) for the fleet's whole-branch diff-guard meta-tests, replacing 23 near-duplicated call sites across all 8 stations plus src/platform/tests (retro-pyforge-steward-2026-09-04.md action item 11: the fleet's whole-branch diff-guard test mechanics (git subprocess calls computing files/commits changed since origin/main) were consolidated into a shared `pyforge.testing_kit.branch_diff_guard` module (PR #1045).). Fixes a real bug most copies carried: a bare `git diff origin/main` raises `CalledProcessError` on a depth-1 CI checkout instead of skipping loudly (retro row 20's fix, now shared everywhere). `src/platform/tests/test_warden_portal_audit_start_get.py` deliberately keeps its own copy — importing `pyforge.testing_kit` there would trip the very pyforge-import-free boundary it defines. 12 new unit tests in tests/unit/test_branch_diff_guard.py. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-testing-charter`.
- (note) Cross-station reconcile from pyforge-marshal Story 32.2 (spec-fleet-consistency-standard CAP-5): this spec's pyforge-testing-kit pyproject.toml raised requires-python to ">=3.14". Note for CAP-4: Story 32.7 amends this spec's coverage gate -- pytest-cov is declared in only 2 of 10 pixi features today, so seven station envs cannot run it (verified live against scribe: exit 1, 'unrecognized arguments: --cov'), and floors will be seeded from measurement rather than the stated 80/70.
- (note) Story 32.4 (spec-fleet-consistency-standard CAP-3) retired epics-with-stories.md fleet-wide -- BMAD 6.12 produces it nowhere, six of eight copies were frozen at 2026-08-08 while epics.md moved to 2026-09-06, and a line-diff audit of all eight confirmed nothing normative lived only there (steward's test-tier mandate is at its own epics.md:74; mason's OQ-E2/OQ-E5 and steward's story-sizing convention are likewise in epics.md; herald's 30 stories are a SUBSET of the 64 in its epics.md). This spec's surface: _bmad/scripts/bmad_tea_playwright.py's _read_epics_corpus no longer falls back to epics-with-stories.md. It was only ever a fallback -- epics.md is canonical per AD-72 -- so the generator's inputs are unchanged in practice for every station. marshal's test_tea_architecture_generator.py and test_tea_architecture_drift.py pass unchanged.
- (note) Story 32.5 (spec-fleet-consistency-standard CAP-2): the fleet converged on one test-suite vocabulary -- tests/unit/ + tests/integration/ + tests/meta/ (BMAD 6.12's Python default plus the one addition with 8/8 adoption), with fixtures/ and _support/ never collected. Eight suite names collapsed to three; the coverage gate's suite map now needs no per-station special case. This spec's surface: run_station_coverage_gate.py and coverage_gates_ci.py's _suite_test_paths dropped 'contract' and now read (unit, meta) + (integration,) with no station special case. CAP-4 note: the 51 files these maps could not see (steward 32, warden 19) are now inside measured suites.
- (change by claude) 2026-09-09 pre-existing-failure fix (operator-directed): `branch_diff_guard.py` moved its `import pytest` from module scope into `_require_ref`, the single function that uses it (one call, `pytest.skip`). WHY: `tests/packaging/test_dependency_completeness.py` was FAILING on main -- pyforge-testing-kit declares `dependencies = []` and calls itself a stdlib leaf, but a module-level `import pytest` makes pytest a real undeclared runtime dependency, and that checker inspects module-level imports only. The module's own docstring had reasoned the import was harmless because 'every consumer is itself a pytest test file'; true in practice, but it does not make the declaration honest, and the gate is right. Chose the function-local import over declaring pytest in pyproject + pixi run-dependencies precisely BECAUSE it preserves the zero-dependency stdlib-leaf property the package documents -- declaring it would have made every install of the kit pull pytest. Docstring updated so the prose no longer justifies an import that has moved. Verified in the pyforge-mason env: no module-level `pytest` attribute, and `_require_ref` on a missing ref still raises Skipped, so skip-on-missing-base-ref is unchanged for every consumer. pyforge-mason 1579 passed / 3 deselected (identical to the recorded baseline); tests/packaging 173 passed, 2 skipped. RECONCILES: src/shared/packages/pyforge-testing-kit/src/pyforge/testing_kit/branch_diff_guard.py. Scoped-stamped.
- (change by claude) 2026-09-09 (second touch this pass): `unsanctioned_commits` now accepts a Conventional-Commits SCOPE on the sanctioned-retro subject -- `retro:` OR `retro(<scope>):` -- via a shared `_RETRO_SUBJECT` regex, and `scripts/mason_cfe_surface_check.py` gets the identical change so the two copies do not disagree. FOUND BY RUNNING THE TRIGGERED CI LANES LOCALLY, which is the only reason it surfaced: `pyforge-atlas`'s `test_conda_forge_expert_not_replaced` (plus the marshal/mason/steward copies) failed on this pass's own `retro(cfe): v8.90.0` commit. WHY WIDEN RATHER THAN RENAME THE COMMIT: the repo's own practice had already moved to the scoped form -- v8.87.2, v8.88.0, v8.88.1 and v8.89.0 are ALL on main as `retro(cfe):` -- and they landed only because this guard inspects `base..HEAD` alone, so a subject it would reject becomes invisible the moment it merges. The load-bearing half of the rule (the CHANGELOG must move in the SAME commit) is untouched, so nothing is weakened. The rejected alternative was rewriting the retro commit's subject, which would have changed its SHA -- and that SHA is recorded as `brief_mirrored_through` in spec-conda-forge-expert-rebuild's campaign-state and validated by `cfe_rebuild_guard_check`. Verified after: atlas 16/16 on that file, marshal 7534, mason 1579, steward 1197, mason-cfe-surface-check clean. RECONCILES: src/shared/packages/pyforge-testing-kit/src/pyforge/testing_kit/branch_diff_guard.py. Scoped-stamped.
- (decision) 2026-09-16 fold-exemption: cross-station-seam — declared seam (kernel / testing kit); survives the marshal one-chain fold; marshal stays accountable; not absorbed.
- (event by claude) Surface reconcile 2026-09-19 (pre-existing CI defect, fix-now; found on PR #1513): .github/workflows/coverage-gates.yml named-module-gates gains the pgvector/pgvector:pg16 service (port 5433, user postgres, db scribe_graph — verbatim from pyforge-station-tests.yml's scribe-test job) and a scribe-only 'Enable pgvector extension' step. Every scribe-selected run of this lane (any scribe edit, or the shared surface) had died with 18 Postgres connection errors before the gate evaluated anything — the lane's own log: 'pytest failed for pyforge-scribe unit (exit 1)' then 'no touched source modules ... skipping evaluate'. Services are job-level in a matrix, so all eight legs get the container; only scribe touches it. CAP-4 (the named-module gate) unchanged.
- (event) Surface reconcile 2026-09-19: updated Diátaxis docs and package READMEs
- (event by claude) Surface reconcile 2026-09-19 (doctor Story 30.1 / PR #1529): src/shared/packages/pyforge-testing-kit/README.md — one plain-text docs pointer line. No kit code.
- (event by claude) Surface reconcile 2026-09-20 (steward Story 66.1, spec-pyforge-steward:CAP-153, PR #1553): src/shared/packages/pyforge-testing-kit/** -- Surface reconcile 2026-09-20 (steward Story 66.1, spec-pyforge-steward spec-pyforge-steward:CAP-153 -- the ten-package lint / type gate landed in PR #1553): pyproject.toml gains [tool.ruff] (py314, line-length 120, E/F/W/I, E501 off) and [tool.mypy] (3.14, explicit_package_bases, import-untyped disabled; the 2026-09-20 baseline as [[tool.mypy.overrides]] per module, per error code); every .py under src/ and tests/ import-sorted (I001) and reformatted by `ruff format` -- semantics-preserving, verified by the station suite. One attr-defined finding baselined per module.
- (event by claude) Surface reconcile 2026-09-20 (steward Story 66.1, spec-pyforge-steward:CAP-153 -- found by the pre-push hook's first live run, PR #1553): the touched-module coverage floor measured the whole fleet after the ruff format (atlas 16 modules under 80% from formatting alone). src/shared/packages/pyforge-marshal/src/pyforge/marshal/coverage_gate.py gains ast_fingerprint (ast.dump with every import statement and docstring dropped -- invariant under ruff format, I001 sorting/merging, F401 removal, a type-only import; moves on any other statement change; None on a syntax error) and format_only_paths; scripts/coverage_gates_ci.py drops changed station .py files whose fingerprint equals the merge-base's before naming touched modules (added / deleted / unparseable stay touched); tests/unit/test_coverage_gate_ast_fingerprint.py (5). All eight station gates pass on the landed tree measuring only code that changed.
- (event by claude) Surface reconcile 2026-09-20 (doctor Story 24.1, spec-coverage-gate-independence CAP-1): scripts/coverage_gates_ci.py and scripts/run_station_coverage_gate.py both repoint their evaluator import from `pyforge.marshal.coverage_gate` (removed) to a new sibling, scripts/coverage_gate.py — the evaluator moved outside every pyforge.<station> package so no station governs its own CI gate. Both drivers' own paths and behavior are unchanged; only the import site and the PYTHONPATH entry a child `pytest --cov` subprocess inherits moved from marshal's src/ to this file's own directory. These two files stay named in this Spec's `surface:` for now; doctor Story 24.3 hands their governance to docs/governance/spec-coverage-gate-independence/ in a follow-up reconcile so no file ends up governed twice or not at all.
- (event by claude) Surface hand-over 2026-09-24 (doctor Story 24.3, spec-coverage-gate-independence CAP-1/CAP-2): scripts/coverage_gates_ci.py and scripts/run_station_coverage_gate.py drop out of this Spec's surface: -- their governance moves to docs/governance/spec-coverage-gate-independence/, which now declares both in its own surface: (documentation-only there, per that Spec's own note -- spec_surface_check.py's SPEC_GLOB does not discover docs/governance/, so both files are also added to scripts/spec_surface_allowlist.txt so the live detector still sees them governed). Neither file's own content changed in this pass -- governance-only. The two surfaces (this Spec's and spec-coverage-gate-independence's) are now disjoint: no file is governed twice or left ungoverned. Re-stamp scoped after this reconcile: --write-baseline --spec pyforge-marshal/spec-pyforge-testing-charter.
