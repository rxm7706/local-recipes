# spec-landing-evidence-grammar — memlog

## 2026-08-14 — baseline initialized

- Spec authored by the 2026-08-14 implementation-readiness batch (PR #499) without a
  spec-surface baseline stamp; initialized at the 10.1 landing pass. Surface paths are
  pre-implementation (Epic 20 stories 20.8-20.10 own them).

## 2026-08-15 — surface reconcile, incidental overlap, unrelated marshal-land-cross-project-story-key-collision review pass

- Two of this spec's still-pre-implementation governed paths,
  `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/promotion.py` and
  `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/status.py`, moved via commit
  `ec5a4e3da2` ("marshal: review pass 1 -- close the empty-station reopening, refresh stale
  docstrings"), landed by the unrelated `spec-marshal-land-cross-project-story-key-collision`
  effort. `core/status.py`'s change is docstring-only (the `failed_patches` field comment,
  correcting a stale cross-project-contamination note to record it CLOSED). `core/promotion.py`
  gained one real line: `extract_story_key_from_github_merge_subject` now also rejects an empty
  `station` (`project_slug` stripped of `pyforge-` is `""`) instead of degrading to
  `branch.startswith("/")`, plus the docstring documenting it. Both changes belong entirely to the
  story-key-collision fix; this spec's own CAP-1..CAP-3 (the shared landing-evidence grammar) are
  UNTOUCHED and still unimplemented. Named here only so this spec's own baseline can be
  scoped-stamped without accepting any OTHER pending drift as reconciled.
