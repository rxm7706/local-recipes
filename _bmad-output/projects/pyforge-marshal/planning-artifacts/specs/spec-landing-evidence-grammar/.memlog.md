# spec-landing-evidence-grammar — memlog

## 2026-08-14 — baseline initialized

- Spec authored by the 2026-08-14 implementation-readiness batch (PR #499) without a
  spec-surface baseline stamp; initialized at the 10.1 landing pass. Surface paths are
  pre-implementation (Epic 20 stories 20.8-20.10 own them).

## Surface reconcile 2026-08-15 — incidental overlap, unrelated story spec-marshal-land-cross-project-story-key-collision landing

- (note) Two of this spec's governed paths, `core/promotion.py` and `core/status.py`, moved when
  `spec-marshal-land-cross-project-story-key-collision` landed (commit d7fd62707b, the CAP-1 fix
  itself) and its review pass 1 (commit ec5a4e3da2, 2026-08-15) -- this spec's own grammar work is
  still pre-implementation (Epic 20 stories 20.8-20.10 own it, per the entry above), untouched by
  either commit. The change scopes `extract_story_key_from_github_merge_subject`'s branch match to
  `project_slug` (closing a cross-project already-landed false positive) and refreshes stale
  docstrings that narrated the fix as still-open -- unrelated to this spec's landing-evidence
  grammar. Named here only so this spec's own baseline can be scoped-stamped without accepting any
  OTHER pending drift as reconciled.
- (note) Re-verified after landing: `pixi run --frozen -e pyforge-marshal pyforge-marshal-test` 4679 passed, 9 deselected.
