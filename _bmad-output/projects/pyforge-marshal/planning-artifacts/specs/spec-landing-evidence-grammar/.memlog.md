# spec-landing-evidence-grammar — memlog

## 2026-08-14 — baseline initialized

- Spec authored by the 2026-08-14 implementation-readiness batch (PR #499) without a
  spec-surface baseline stamp; initialized at the 10.1 landing pass. Surface paths are
  pre-implementation (Epic 20 stories 20.8-20.10 own them).

## Surface reconcile 2026-08-15 — incidental overlap, unrelated spec-marshal-land-cross-project-story-key-collision landing

- (note) Two of this spec's four governed paths, `core/promotion.py` and `core/status.py`, moved
  under commits `d7fd62707b` ("marshal: scope the GitHub PR-merge story-key pattern to
  project_slug") and its review-pass follow-up `ec5a4e3da2` -- both belong to the unrelated,
  already-shipped spec `spec-marshal-land-cross-project-story-key-collision`
  (`extract_story_key_from_github_merge_subject` gained a required `project_slug` parameter and
  now rejects a cross-project branch match; `core/status.py`'s change is docstring-only, narrating
  that fix as closed). This spec's own Epic 20 stories 20.8-20.10 (the landing-evidence grammar
  itself) have not started -- still pre-implementation, per the baseline-initialized note above.
  `doctor/sources/marshal.py` and `pyforge-core/` are unaffected (hashes unchanged). Named here
  only so this spec's own baseline can be scoped-stamped without accepting any OTHER pending drift
  as reconciled.
