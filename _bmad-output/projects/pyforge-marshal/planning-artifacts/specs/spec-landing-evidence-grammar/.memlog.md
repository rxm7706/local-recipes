---
topic: landing-evidence-grammar (legacy memlog; frontmatter added by the 2026-08-22 format migration, marshal Story 25.3 — body below is the untouched original)
updated: 2026-09-09T18:05
---

# spec-landing-evidence-grammar — memlog

## 2026-08-14 — baseline initialized

- Spec authored by the 2026-08-14 implementation-readiness batch (PR #499) without a
  spec-surface baseline stamp; initialized at the 10.1 landing pass. Surface paths are
  pre-implementation (Epic 20 stories 20.8-20.10 own them).

## Surface reconcile 2026-08-15 — incidental overlap, unrelated story spec-marshal-land-cross-project-story-key-collision landing

- (note) Two of this spec's governed paths, `core/promotion.py` and `core/status.py`, moved when
  `spec-marshal-land-cross-project-story-key-collision` landed (commit d7fd62707b, the CAP-1 fix
  itself) and its review pass 1 (commit ec5a4e3da2, 2026-08-15) -- this spec's own grammar work is
  still pre-implementation (Epic 20 stories 20.8-20.10 own it, per the entry above), untouched by
  either commit. The change scopes `extract_story_key_from_github_merge_subject`'s branch match to
  `project_slug` (closing a cross-project already-landed false positive) and refreshes stale
  docstrings that narrated the fix as still-open -- unrelated to this spec's landing-evidence
  grammar. Named here only so this spec's own baseline can be scoped-stamped without accepting any
  OTHER pending drift as reconciled.
- (note) Re-verified after landing: `pixi run --frozen -e pyforge-marshal pyforge-marshal-test` 4679 passed, 9 deselected.
- (note) 2026-08-15, two further independent surface-reconcile passes (pyforge-mason Story 5.3
  and pyforge-atlas Story 15.2) recorded the identical fact above again, from slightly different
  observation points (mason's also naming review pass 1's empty-station rejection detail;
  atlas's also confirming `doctor/sources/marshal.py` and `pyforge-core/` hashes unaffected);
  deduplicated here to one entry rather than kept as separate near-identical blocks -- no
  information lost, all passes agree.

## 2026-08-21 — Surface reconcile (foreign-drift documentation)

`core/status.py`'s drift belongs to the marshal 5.8 `engine_alive` recovery
(PR #601, 2026-08-21 evening session), which landed without this spec's
memlog moving — that landing is the record; no code change rides this entry.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Surface reconcile 2026-08-31: 3 drift-presumed paths since baseline at `87abc37e5f09` (marshal 5.8). Nine commits, Epic 20 landing-evidence grammar delivery + downstream consumers (all done in ledger):
  - `ef5b32fb91` — Story 25.5 (done): status.py 0.11 vocabulary fields.
  - `e880dcd8c7` — Story 20.9 (done): doctor story-status wired to landing-evidence grammar (`marshal.py`).
  - `a1d48c113a` — Story 20.10 (done): promotion.py + status.py wired to grammar (this spec's core CAP).
  - `d4686b0aa7` — Story 22.1 (done): dispatch verb status integration on status.py.
  - `031cf6f550` — Story 22.2 (done): dispatch completion facts on status.py.
  - `331203258c` — Story 22.3 (done): dispatch verification on status.py.
  - `1a083201ce` — Story 22.6 (done): dispatch survives operator on status.py.
  - `e07834580d` — steward Story 30.2: Guildhall deletion doc cleanup on status.py.
  - `79ff7da6b7` — doctor Story 20.11 (done): close story-status's own landing-evidence adoption gap on `marshal.py`.
  No CAP text change. RECONCILES: `core/promotion.py`, `core/status.py`, `pyforge-doctor/.../marshal.py`. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-landing-evidence-grammar`.
- (event by claude) Surface reconcile 2026-09-01: `core/status.py` drift since the 2026-08-31 stamp — Story 28.4 savings telemetry fields on the status surface grammar consumers read (`80b773dc53`, `92aa9d4694`). No landing-evidence CAP change; bookkeeping only. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-landing-evidence-grammar`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 1 governed path(s) drifted against this spec's recorded baseline (1 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/status.py` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-landing-evidence-grammar` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (change) Surface-touch note 2026-09-08: marshal status gained a `run_state_retired` fact and an MRS-STATUS-012 WARN so a retired or cleaned run reports the home `idle` rather than degrading the whole row to `unknown` (DW-STATUS-2026-09-08-1). `is_run_live` keeps such a run conservatively live, so no destructive guard is weakened. Reconciled in full on pyforge-marshal/spec-fleet-status-supervisor-fallback. RECONCILES: core/status.py, cli/status.py, core/findings.py and their unit tests. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-landing-evidence-grammar.
- (decision by claude) 2026-09-09 (fleet-readiness-decision-batch-2026-09-09 row mars-B-B14, operator-approved): ACCEPT the green verdict and RECORD THE COVERAGE NUMBER in the success signal -- do not mint a coverage story. pyforge.doctor.sources.marshal::gather_story_status over the live tree today reports "no `done` story contradicts its landing evidence (898 audited, 708 with no run record (unchecked))". The hedge is correct by design (Dream Constraints: absence of evidence stays hedged where it is hedged today); the green is real over the 190 stories that carry a run record, and the success signal must say so rather than read as fleet-wide. A coverage story is worth it only if run records become recoverable for pre-dispatch-era stories, which the Dream non-goals forbid rewriting history to achieve.
- (event by claude) status: ready -> shipped -- CAP-1..4 decompose to Stories 20.8-20.11, all `done`; the grammar lives in the shared spine as pyforge-core landing_evidence.py (no station import, the Story 14.2 precedent), consumed by doctor/sources/marshal.py:53 and marshal/core/promotion.py:58, and the three standing false positives are fixture-pinned at landing_evidence.py:441/450/461 with no per-story whitelist. Both frontmatter open questions are RESOLVED by Story 20.8 (home = a pyforge-core module; pre-convention history = a SHA-prefix allowlist, parse_recovery_commit_sha at :315, never a rewrite) -> open_questions: [] on the re-derive (fleet-readiness-decision-batch-2026-09-09 Class B, row mars-B).
- (event) (self-validate pass 1 -- coherence) 2026-09-09 re-derive: PASS. status ready -> shipped; both frontmatter questions retired and the body section rewritten as a dated closed-questions record with the Story 20.8 answers; the 708/898 coverage number lands INSIDE the Success signal per the decision, with the no-coverage-story rationale attached. 4 CAP ids, unchanged.
- (event) (self-validate pass 2 -- preservation) 2026-09-09 re-derive: PASS. Both 2026-09-09 entries landed: the measured 898-audited/708-unchecked figure with its by-design hedge, and the shipped-grammar evidence (pyforge-core landing_evidence.py, consumers at doctor sources/marshal.py:53 and marshal core/promotion.py:58, fixtures at :441/:450/:461) plus a Decomposition line for Stories 20.8-20.11. Only the duplicated question prose and superseded frontmatter were removed.
