---
topic: bmad-switch-scope-enforcement (legacy memlog; frontmatter added by the 2026-08-22 format migration, marshal Story 25.3 — body below is the untouched original)
updated: 2026-09-09T18:07
---

# .memlog.md — spec-bmad-switch-scope-enforcement

## 2026-08-14 — Genesis baseline

Spec drafted 2026-08-14 (`status: draft`); no story has touched or reconciled this
surface yet. This baseline is a structural bootstrap only — it establishes the
S-13.7 spec-surface-reconciliation contract (`scripts/spec_surface_check.py
--write-baseline --spec <project>/spec-<slug>`) against the surface's current,
unmodified file state. No governed-file content is claimed reconciled by this entry.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Surface reconcile 2026-08-31: 2 drift-presumed paths since baseline at `736a2af4070f`. Four commits on governed `scripts/bmad-switch` and `cli/init.py`:
  - `297d09ae893` — Story 20.7 (done): hard-fail both scope guards on drift; bmad-switch and init.py are the guard surfaces.
  - `430a0f71f67` — Story 20.7 review pass: sort init imports and tighten test lint.
  - `58f2378e25` — pyforge-core hooks extraction: init.py import reparenting.
  - `f7fb0275994` — Story 28.3 (done): Genesis seed-kit incidental touch on init.py.
  No CAP text change. RECONCILES: `scripts/bmad-switch`, `src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/init.py`. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-bmad-switch-scope-enforcement`.
- (note by claude) 2026-09-05 hygiene-2 PR (branch maint/hygiene-2-2026-09-05): scripts/bmad-switch changed — _load_verify_scope falls back to the checkout the script sits in when the given root has no src/shared/packages/pyforge-marshal/src (the Story 20.7 tests root at tmp_path; the primitive is still the sole Story 20.6 one, never a reimplementation). Scoped --write-baseline follows.
- (decision by claude) 2026-09-09 OQ-1 ANSWERED by operator (fleet-readiness-decision-batch-2026-09-09 row mars-A-B5 / C10): NO further BMAD write-skill gets the preflight in local-recipes -- wire verify_scope at `marshal factory dispatch` instead, and DEFER the skill-injection question to the foundry cutover 44.5 layout. Story 20.7 shipped exactly two call sites and said so (spec-20-7 :19,:31). The remaining exposure is not skills-in-general, it is the PARALLEL-AGENT case CLAUDE.md documents as a HARD rule and auto-memory records as a live incident (feedback_parallel_bmad_physical_paths.md, the 2026-07-25 five-agent fan-out). Every parallel BMAD write today enters through `marshal factory dispatch`, which already stamps BMAD_ACTIVE_PROJECT per invocation (verified in a live journal payload: "bmad_active_project": "pyforge-steward") -- one verify_scope call there covers the whole class without inventing an injection mechanism, and the two gitignored compatibility symlinks this Dream guards may not survive the cutover at all. Rejected: wire a bmad-customize override into every write-skill now -- it lives in the regenerated BMAD install layer and must survive every `bmad-method update`. Decomposed as marshal Story 33.9.
- (event by claude) status: ready -> in-progress (fleet-readiness-decision-batch-2026-09-09 Class B, row mars-A; Table A). CAP-1/2 are in effect: Stories 20.6/20.7 shipped one shared stdlib-only primitive (pyforge/marshal/scope.py) consumed by exactly two hard-failing call sites, scripts/bmad-switch:207-210,232-233 and pyforge/marshal/cli/init.py:247; DW-1-4-2 closed. NOT `shipped`: the third call site the operator just chose (verify_scope at `marshal factory dispatch`) is unbuilt -- Story 33.9.
- (event) (self-validate pass 1 -- coherence) 2026-09-09 re-derive: PASS. status ready -> in-progress; OQ-1 retired and its body section rewritten as a dated closed-question record (text struck through, answer appended) naming Story 33.9 and the 44.5 deferral. The Decisions (Story 20.6) section is untouched. 3 CAP ids, unchanged.
- (event) (self-validate pass 2 -- preservation) 2026-09-09 re-derive: PASS. Both 2026-09-09 entries landed with their evidence (spec-20-7:19,:31; the live journal payload bmad_active_project=pyforge-steward; the 2026-07-25 five-agent incident) and the rejected bmad-customize alternative. Nothing dropped -- the one apparent deletion is a no-newline-at-EOF artifact.
