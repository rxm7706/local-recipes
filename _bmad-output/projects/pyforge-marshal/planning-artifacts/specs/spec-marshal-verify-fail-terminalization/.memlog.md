---
created: 2026-09-01
updated: 2026-09-09T18:04
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-verify-fail-terminalization`.


- (event by claude) First-ever baseline stamp, 2026-09-01: folder-format spec authored for Story 28.17 (verify-fail terminalization). `9541dc6f80` landed CAP-1 on `dispatch_supervisor/__main__.py` and `core/dispatch_supervisor_state.py`; CAP-2 compose hook uses existing `core/dispatch_retry.py` + `cli/dispatch.py` transient classification. Spec status `ready`; baseline establishes reference point at landing revision. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-verify-fail-terminalization`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 4 governed path(s) drifted against this spec's recorded baseline (4 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/dispatch.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch_retry.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch_supervisor_state.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_supervisor/__main__.py` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-verify-fail-terminalization` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (decision by claude) 2026-09-09 oq1 ANSWERED by operator (fleet-readiness-decision-batch-2026-09-09 row mars-B-B7): the transient-retry cap is 3 PER STORY PER CAMPAIGN, POLICY-DECLARED, default 3 -- not unlimited. Unlimited transient retry is how a red repo-global gate becomes an infinite drain: the MRS-GATE-001 pandas incident (drain-self-resolution addendum F) was a repo-global gate 28.17 would have re-hit every tick, and CAP-5 pre-existing-gate WARN narrows but does not bound it. Rejected: keep unlimited -- defensible only once pre-existing-gate classification is proven over a full campaign.
- (decision by claude) 2026-09-09 oq2 CLOSED (fleet-readiness-decision-batch-2026-09-09 row mars-B-B8, operator-approved): KEEP v1 -- the preserve patch is left at failed/<story>/changes.patch and is NEVER auto-applied on redispatch. Fleet-wide standing policy is non-destructive preserve-then-restore, operator-driven (auto-memory feedback_escalation_nondestructive_standing_policy.md); the 647-line uncommitted 28.2 diff is the motivating loss. Rejected: auto-apply -- it silently re-bases a diff the operator has not reviewed.
- (event by claude) status: ready -> shipped -- CAP-1..3 decompose to Story 28.17, `done`, and both open questions are answered by this batch (fleet-readiness-decision-batch-2026-09-09 Class B, row mars-B).
- (event) (self-validate pass 1 -- coherence) 2026-09-09 re-derive: PASS. status ready -> shipped; both open questions retired into a dated ANSWERED block; oq1's answer bends design, so it also renders as a real Constraint and strikes through the v1 non-goal it supersedes; oq2 re-affirms the existing non-goal rather than duplicating it. CAP ids unchanged.
- (event) (self-validate pass 2 -- preservation) 2026-09-09 re-derive: PASS. Both decisions landed with their evidence (MRS-GATE-001 pandas incident; the 647-line uncommitted 28.2 diff) and both rejected alternatives. Only superseded frontmatter was removed; the Explicit non-goals list keeps both entries, one struck through with its amendment.
