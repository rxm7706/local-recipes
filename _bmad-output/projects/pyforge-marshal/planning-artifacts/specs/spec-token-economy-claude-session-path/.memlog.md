---
topic: token-economy-claude-session-path — Claude uses the kit already built
updated: 2026-09-16T03:56
---

- (event by operator) 2026-09-16 seeded after a live session found Claude still paying the uncompressed tax while Epic 28/33 machinery sits on marshal dispatch and Cursor-first policy. Dream `docs/dreams/token-economy-claude-session-path.md` at `dreamt`. Spec `draft` so INV-1 holds. Three open questions gate `ready`. Do not mint an epic or flip harness_preference from this seed.
- (decision by operator) 2026-09-16 OQ2 ANSWERED: both engines gain the kit by default through the REPO-DEFAULTS layer, not the code default. _bmad-output/policy-defaults.toml declares [context.output], [context."derived-context"], [context."planning-graph"] enabled = true (harness-agnostic; derived/planning are inert on spin by construction — the step-01 compile is bmad-build-auto's), plus [context."structure-graph"] on the linux fleet. wire stays PER-STATION, paired with harness_preference = ["claude", ...] — cursor.toml has no [wrapper] (28.29), so a repo-default wire would journal a named degrade on every Cursor-first run. Verified live: cli/spin.py:615 and cli/dispatch.py:352 both fold read_repo_policy_defaults() (33.3 closed DW-FU-28-2-3); DEFAULT_POLICY context stays {} (CAP-1 absent-means-off contract unchanged).
- (decision by operator) 2026-09-16 NEW CAP CANDIDATE: marshal is the execution front door. Bare bmad-build-auto / bmad-build in chat is for inspection and dry-runs, not story execution — it gets no wire wrap (marshal owns the launch argv), no ceilings / idle ladder / tiering, no savings journals or ledger sync. Lands as a docs rule (AGENTS.md Policy + CLAUDE.md + .cursor/rules/) plus a routing note atop bmad-build-auto/SKILL.md; explicitly NOT a gate. Optional doctor detector (ledger moved with no marshal journal for that story) only if the rule is violated in practice — do not build preemptively. Re-derive should mint this as a capability alongside CAP-1..4.
