---
topic: token-economy-claude-session-path — Claude uses the kit already built
updated: 2026-09-16T04:03
---

- (event by operator) 2026-09-16 seeded after a live session found Claude still paying the uncompressed tax while Epic 28/33 machinery sits on marshal dispatch and Cursor-first policy. Dream `docs/dreams/token-economy-claude-session-path.md` at `dreamt`. Spec `draft` so INV-1 holds. Three open questions gate `ready`. Do not mint an epic or flip harness_preference from this seed.
- (decision by operator) 2026-09-16 OQ2 ANSWERED: both engines gain the kit by default through the REPO-DEFAULTS layer, not the code default. _bmad-output/policy-defaults.toml declares [context.output], [context."derived-context"], [context."planning-graph"] enabled = true (harness-agnostic; derived/planning are inert on spin by construction — the step-01 compile is bmad-build-auto's), plus [context."structure-graph"] on the linux fleet. wire stays PER-STATION, paired with harness_preference = ["claude", ...] — cursor.toml has no [wrapper] (28.29), so a repo-default wire would journal a named degrade on every Cursor-first run. Verified live: cli/spin.py:615 and cli/dispatch.py:352 both fold read_repo_policy_defaults() (33.3 closed DW-FU-28-2-3); DEFAULT_POLICY context stays {} (CAP-1 absent-means-off contract unchanged).
- (decision by operator) 2026-09-16 NEW CAP CANDIDATE: marshal is the execution front door. Bare bmad-build-auto / bmad-build in chat is for inspection and dry-runs, not story execution — it gets no wire wrap (marshal owns the launch argv), no ceilings / idle ladder / tiering, no savings journals or ledger sync. Lands as a docs rule (AGENTS.md Policy + CLAUDE.md + .cursor/rules/) plus a routing note atop bmad-build-auto/SKILL.md; explicitly NOT a gate. Optional doctor detector (ledger moved with no marshal journal for that story) only if the rule is violated in practice — do not build preemptively. Re-derive should mint this as a capability alongside CAP-1..4.
- (decision by operator) 2026-09-16 OQ1 ANSWERED: both Claude paths are sanctioned, as different tiers. Dispatch (factory dispatch / spin) is the MEASURED path — policy render, wire wrap, caveman seed, savings journals, and the benchmark artifact the parent Dream's realized-guard cites. Interactive Claude on the shared checkout is a documented CONVENIENCE path using the same instruments by hand: headroom wrap claude --code-memory none from repo root, caveman-install --only claude --with-hooks, scribe recall --mode planning and marshal context retrieve instead of wholesale epics.md / PRD loads. Neither path gets a second kit; interactive gets no separate benchmark.
- (decision by operator) 2026-09-16 OQ3 ANSWERED: thinning CLAUDE.md / AGENTS.md is a LATER DREAM, not v1. The always-on doc tax (~84 KB across CLAUDE.md + AGENTS.md + MEMORY import) is real but is a documentation-architecture problem with a different blast radius — AGENTS.md is the cross-tool entry point (Cursor, Copilot, Gemini, Devin, Codex, BMAD, frameworks) and carries the bmad:context managed block; CLAUDE.md carries the SKF index. Owner would be the docs surface, not marshal's harness wiring. v1 records the tax as a known sink and explicitly defers it; the five layers' savings are measured first.
- (decision by operator) 2026-09-16 OQ2 REVISED (supersedes the per-station wire answer above): repo default for ALL FIVE layers, with wire as a tri-state. _bmad-output/policy-defaults.toml declares [context.output], [context."derived-context"], [context."planning-graph"] enabled = true (harness-agnostic), [context."structure-graph"] on the linux fleet, and [context.wire] enabled = "auto" — a NEW value. At render time auto intersects the request with the resolved harness profile's declared capability: profile has [wrapper] -> wire ON; no [wrapper] -> wire OFF. The profile TOML is already the capability matrix (harness_profile.py parse_wrapper); no new registry. Per-station [context.wire] survives only as a force-override (true/false). Rationale: a naive repo-default true would journal MRS-DISP-033 and launch unwrapped on every Cursor-first run — named but operationally invisible, the belief/reality divergence that recreates the 2026-09-01 spend scare.
- (decision by operator) 2026-09-16 NEW CAP CANDIDATE (silent saves): capability-aware auto resolution + journal taxonomy split + savings rollup. (a) policy schema accepts enabled = true | false | "auto" for context layers; render resolves auto against harness capability (additive, bool behavior unchanged). (b) Split 'unsupported' from 'degraded': auto-skip on a wrapperless profile journals a NEW info line (harness declares no wrapper), while MRS-DISP-033 keeps its WARN meaning for genuine faults (binary missing, store uncreatable) and for enabled=true force-ons (harness_profile.py:90-93, dispatch.py:2112). (c) LOUD ABSENCE: a savings rollup (doctor source or fleet-picture line) aggregating the existing per-run savings journals (layer_savings_sources.py, token_economy_benchmark.py) into per station x harness wire-coverage % and realized compression over N days — none exists today; full-price stations must be named as a standing fleet fact. (d) Interactive persistence: doctor advisory when the Claude settings headroom wrap / caveman hooks are clobbered (update, settings rewrite) so the install-once convenience path cannot silently return to full price. (e) Payoff: when a cursor [wrapper] ships later (28.29 open thread), every Cursor-first station starts wrapping on its next run with zero policy edits. Agents inherit by construction — every dispatch/spin renders policy. Re-derive should mint this as a capability alongside CAP-1..4 and the front-door CAP.
- (decision by operator) 2026-09-16 MULTI-HARNESS MATRIX (extends the silent-saves CAP; GitHub Copilot ruled on). Copilot is three surfaces: (1) COPILOT CLI AS DISPATCH HARNESS — packaged copilot.toml exists, verified 2026-08-27 (-p {prompt} --allow-all-tools, binary in .pixi/envs/local-recipes/bin, rides GitHub credentials; account-quota caveat: same-day re-probe hit 'no quota', surfaces in session.log, nothing pre-detects it). The four harness-agnostic layers apply by repo default today; wire auto-skips (no [wrapper]) — but headroom ships 'wrap copilot' as a real argv-prefix wrapper (unlike 'wrap cursor', IDE-only, prints config instructions — cursor.toml can NEVER get a [wrapper]). NEW STORY CANDIDATE: mint copilot.toml [wrapper] mirroring claude.toml (wrap copilot --port {wire_port} --, same HEADROOM_WORKSPACE_DIR loop-home scoping), verify live; on landing every copilot dispatch wraps with zero policy edits — the capability-driven payoff demonstrated. (2) COPILOT CHAT / AGENT MODE IN VS CODE — wire via 'headroom wrap vscode' (preserves model picker), install-once then silent; the persistence advisory extends to the wrapped VS Code launcher. Caveman does NOT reach it (Claude Code hook seam; Copilot has none) — lever is convention via .github/copilot-instructions.md (already thin, 1.1 KB pointer). Retrieval layers (scribe recall, codegraph, marshal context retrieve) are tool-agnostic CLIs Copilot agent mode can run — pointing its instructions at them is a docs edit inside the front-door CAP. (3) COPILOT CLOUD CODING AGENTS — run on GitHub runners; repo-local proxy unreachable (exotic copilot-setup-steps.yml wiring unbuilt); only lever is the injected instruction surface = the deferred OQ3 doc-tax Dream. Outward dispatch (cloud agents opening PRs) stays operator-gated per policy. Resulting matrix: claude = full kit; copilot = full kit once the wrapper story lands; cursor = harness-agnostic layers only, forever; gemini/devin = same auto rule resolves them on first use.
