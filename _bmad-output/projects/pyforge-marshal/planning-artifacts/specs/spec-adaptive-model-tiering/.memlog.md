---
topic: Adaptive model tiering — FR-51's tier chain fed with real data, plus retry-triggered escalation
updated: 2026-08-22T06:34
---

- (note) Input: docs/dreams/adaptive-model-tiering.md, a rich, code-grounded Dream (file:line citations throughout) -- express mode, no elicitation needed.
- (decision) Slug is adaptive-model-tiering (Dream's own filename stem) -- describes the thing being specced, matches Dream-to-Spec 1:1 convention used by spec-fleet-status-supervisor-fallback.
- (capability) CAP-1: a project's model_tier_map, once populated with real {difficulty: {stage: model}} entries, and a story that declares a real difficulty, together change what render_policy_toml actually writes -- closing the gap where the full chain (Story 6.1) is built but zero project/story ever feeds it (grep-confirmed on all 8 loop-home projects).
- (capability) CAP-2: a story whose attempts/review cycles show it is genuinely struggling gets a floor-raised model without an operator hand-editing policy.toml and relaunching -- closes the retry-escalation gap confirmed absent in both core/policy.py (tier resolved once per launch, before bmad-loop run is spawned) and vendored bmad_loop 0.9.0 (escalation.py:68 couples task.attempt only to a budget check, never to model selection).
- (constraint) No new tiering mechanism: model_tier_map's shape, render_policy_toml's tier-batching (harness_bmadloop.py:450-459), and cli/spin.py's difficulty-resolution chain (Story 6.1) are already correct and shipped -- this Spec is about feeding real data and adding the retry path, not rebuilding FR-51.
- (constraint) Run-level batching granularity stands: one difficulty governs a whole launch, a mismatched story is reported not silently ignored (cli/spin.py::_resolve_governing_difficulty) -- matches upstream-register.json's own accepted per-story-model-tiering workaround; not relitigated.
- (constraint) A retry-escalation trigger must be derived from journal/attempt facts the fold already accumulates (AD-26) -- never a new hand-maintained flag.
- (constraint) Every model change, declared or escalated, is journaled with the same intent/outcome discipline (AD-28) every other supervisor action already uses.
- (constraint) An escalation is a floor-raise only, on top of whatever the declared tier already picked -- never a downgrade.
- (assumption) run_resume (Story 3.7, cli/spin.py ~line 177) already re-resolves and re-renders model tiering before detach-launching 'bmad-loop resume', the same as run_spin does before 'bmad-loop run' -- so a resume-time re-tiering path already exists structurally; CAP-2's downstream story may be able to lean on it (e.g. escalate on the NEXT resume after a deferral) rather than requiring a brand-new mid-run intervention. Not prescribed here -- a downstream design decision.
- (question) Which signal decides a story's difficulty when nobody hand-authors one: an authored convention (a human or bmad-create-story names it against a stated rubric) or a derived heuristic (spec size / epic / prior attempt history, the same signals FR-14's budget advisory already reasons about)? Left to the downstream story per the Dream's own explicit non-prescription.
- (question) Does CAP-2 need Marshal to intervene mid-run (kill+relaunch the harness session under a re-rendered policy) since bmad_loop 0.9.0 loads policy once at run start with no per-attempt model hook, or can it work entirely through the existing deferral-then-resume path (Story 3.7) where re-tiering already happens naturally? Determines whether CAP-2 is buildable as a live in-run mechanic or a resume-time one.
- (question) What attempt-count or review-cycle threshold should trigger a retry escalation, and is it a fixed default (like max_dev_attempts=2) or itself policy-configurable?
- (event) Pass 1 (coherence) PASS: both CAPs carry intent+success (rule 1); intents are WHAT not HOW, mechanism deferred to open questions/downstream (rule 2); every constraint rules something out -- no per-story mid-run granularity, no downgrade, journal-derived trigger only (rule 3); 4 non-goals stated (rule 4); success signal is diff-provable / journal-provable (rule 5); CAP-1/CAP-2 ids stable, no prior spec existed for this slug (rule 6); prose is lean, no decoration added beyond what the Dream's own citations support (rule 8).
- (event) Pass 2 (preservation) PASS: every load-bearing claim from the Dream (flat sonnet/opus assignment, the full Story 6.1 chain being built-but-unfed, the grep-confirmed zero-declarations finding, the retry-escalation absence on both sides of the seam, the run-level-batching constraint, the resume-time re-render assumption) lands in SPEC.md's Why/Capabilities/Constraints/Open Questions. No wrapper-only content dropped -- the Dream's own file:line citations are preserved via the surface: list and Why section rather than restated verbatim.
- (event) Spec finalized: 2 capabilities, 0 companions, 3 open questions carried from the Dream plus the resume-path assumption. status: draft.
- (change) RECONCILED BY NAME, 2026-08-13: `src/shared/packages/pyforge-marshal/src/pyforge/marshal/adapters/harness_bmadloop.py` changed under this surface as part of marshal Story 14.2 (atomic-write consolidation, `spec-pyforge-core` CAP-2) — its own hand-written tmp-in-dir + `os.replace` write helper was replaced by `pyforge.core.atomic_write`. Incidental: nothing in this edit touches `render_policy_toml`'s tier-batching logic (harness_bmadloop.py:450-459) or any other surface this Spec's still-unbuilt CAP-1/CAP-2 contract cares about — the file is simply governed by two surfaces at once (also `spec-pyforge-marshal`, reconciled independently in its own memlog). This Spec's own contract does not move.
- (event) Story 3.12 SHIPPED, landed by hand 2026-08-13 (dev commit `c0e7ca38a0`, run
  `20260811-190409-5c73`, deferred by the loop at its own verify step over spec-surface drift
  never a content defect — see `spec-pyforge-marshal`'s own memlog for the full deferral/hand-
  landing account). Ships this Spec's CAP-2 (retry-triggered escalation): `run_resume`
  (`cli/spin.py`) now floor-raises a resumed run's dev-stage model to its own review-stage model
  when a deferred story's attempt/review_cycle count has reached its configured ceiling, read off
  the on-disk `policy.toml` rather than the running `bmad_loop` engine's own cached `Policy`
  object (which never re-reads mid-run) — resolving Open Question 2 in favor of the resume-time
  path the Spec's own `assumption` entry above already anticipated as structurally available.
  Bounded: never re-fires once already escalated, journaled on the existing run-resume
  intent/outcome entries (AD-28). Its own review pass hardened `[limits]` ceiling validation
  (reject non-int/bool/non-positive), restored escalation visibility in the default CLI text
  output, derived `ADAPTER_REVIEW_MODEL_STOCK_DEFAULT` from `_POLICY_TEMPLATE` instead of
  hand-duplicating it, and added coverage for multi-story partial crossing plus
  review_cycle-triggered escalation. Four narrower findings logged to `deferred-work.md`
  (`DW-FU-3-12` through `-4`), left open per that story's own review record. Governed paths:
  `adapters/harness_bmadloop.py`, `cli/spin.py`, `core/findings.py` (new `MRS-SPIN-016`),
  `core/supervise.py`, `core/verdict.py` (new `MRS-SPIN-016` classification), `ports/harness.py`,
  and the five touched/added test modules. CAP-1 (feeding real `model_tier_map` data) remains
  unbuilt — this story shipped CAP-2 only; Open Questions 1 and 3 stay open for CAP-1's own
  downstream story.
- (event) Story 3.11 SHIPPED 2026-08-13 (dev commit `ea0da8801c`). Ships CAP-1 (feeding real
  `model_tier_map` data): `_bmad-output/projects/pyforge-marshal/planning-artifacts/marshal-policy.toml`
  changed — the stale pre-Story-6.1 "vocabulary does not exist yet" placeholder replaced with a
  real `[model_tier_map.heavy]` (`dev = "opus"`, `review = "opus"`) / `[model_tier_map.easy]`
  (`dev = "haiku"`) block, placed after `[[landing_rules]]` per the file's own TOML
  table-placement hazard (a dotted-table key placed earlier would silently swallow later bare
  assignments into `model_tier_map.easy`), plus the stale `[adapter.review] model = 'fable'`
  comment corrected to the real baseline `"opus"`. Open Question 1 (authored vs. derived
  difficulty) resolved in favor of authored, in a story's own Tier-3 spec frontmatter — recorded
  as this Spec's own design choice, not deferred further. This story's own spec (`spec-3-11-*`)
  declares `difficulty: heavy` as the first real end-to-end declaration the resolution chain has
  ever consumed. Open Question 3 (attempt/review-cycle escalation threshold) stays open — out of
  this story's scope. Governed path under THIS Spec's surface: `marshal-policy.toml` only — the
  story's other three touched paths (`bmad-dev-auto`/`bmad-quick-dev` spec-template.md, this
  Spec's own memlog) sit outside this Spec's declared surface and are reconciled, where
  applicable, in their own governing spec or the general `.claude/**` allowlist.
- (decision) SURFACE DRIFT RECONCILED, 2026-08-13 (dev-verify repair pass on Story 14.3, "one
  lattice, one envelope, one exception root", commit `1bd8c8a2de`, not this Spec's own story). Two
  governed paths changed as part of CAP-5's fleet-wide exception re-parenting (`pyforge-core/spec-
  pyforge-core`'s own contract): `adapters/harness_bmadloop.py`'s two family-root exception
  classes (`HarnessPolicyWriteError`, `HarnessError`, both currently `Exception` subclasses) and
  `core/spec_difficulty.py`'s one (`DifficultyParseError`, currently a `ValueError` subclass) each
  gained `PyforgeError` as an added base via multiple inheritance — original stdlib base preserved
  in the MRO, so every existing `except <ThatClass>` / `except (ThatClass, ...)` /
  `except ValueError` site still catches it unchanged; only `except PyforgeError` is new. This
  Spec claims no capability here — CAP-5 is `pyforge-marshal/spec-pyforge-core`'s own contract;
  this entry reconciles surface/drift bookkeeping only, for the two paths landing under this
  Spec's own narrower surface. Both paths are ALSO governed by the broader
  `pyforge-marshal/spec-pyforge-marshal`, reconciled independently in its own memlog; harness_bmadloop.py
  is additionally governed by the narrower sibling `spec-horizontal-run-concurrency`, likewise
  reconciled independently — each baseline-stamped via its own `--spec` scope. `pyforge-marshal-test`
  green post-change (3598 passed, 9 slow deselected).
- (decision) SURFACE DRIFT RECONCILED, 2026-08-13 (Story 14.4, "the subprocess seam is reconciled
  and sole ownership is gated," `spec-pyforge-core`'s own CAP-6/CAP-7 contract): two of this Spec's
  governed paths changed. `adapters/harness_bmadloop.py`'s `_run`/`spin`/`stop` internal launches
  moved from a direct `subprocess.run`/`Popen` call to `pyforge.core.process.PosixProcess`
  (`render_policy_toml`'s own tier-batching logic, lines 450-459, is untouched); `cli/spin.py`'s
  `ProcessPort`/`PosixProcess` imports re-pointed from `pyforge.marshal.ports.process`/
  `adapters.process_posix` to `pyforge.core.process` (mechanical, no behavior change). This Spec
  claims no capability here -- CAP-1/CAP-2 (real `model_tier_map` data, retry-triggered escalation)
  are untouched and this entry reconciles surface/drift bookkeeping only. Both paths are ALSO
  governed by the broader `pyforge-marshal/spec-pyforge-marshal`, reconciled independently in its
  own memlog, each baseline-stamped via its own `--spec` scope. `pyforge-marshal-test` green
  post-change (3552 passed, 9 slow deselected).
- (change) RECONCILED BY NAME, 2026-08-13 (repair pass, same landing revision): `adapters/
  harness_bmadloop.py` changed again under this Spec's declared surface -- a step-04 review of
  this story's own deterministic-verification repair found `spin()`'s empty-argv-guard cause
  chaining lacked the `cause or exc` fallback `vcs_git.py`/`forge_gh.py` already carry for the
  identical gap; fixed to match. This Spec claims no capability here; bookkeeping only, reconciled
  independently of the broader `spec-pyforge-marshal` and narrower sibling
  `spec-horizontal-run-concurrency`. `pyforge-marshal-test` green post-change (3552 passed, 9
  deselected).
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Cross-surface edit 2026-08-22 (marshal Story 25.4, spec-bmad-611-era-alignment CAP-4): src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/policy.py and src/shared/packages/pyforge-marshal/src/pyforge/marshal/adapters/harness_bmadloop.py changed under this shared surface — five bmad-loop 0.10/0.11 SEED knobs added to the policy vocabulary and render pipeline. No tier-batching behavior touched ([adapter.<stage>] logic unchanged). Narrative: spec-pyforge-marshal/.memlog.md, same date.
- (note by claude) The 2026-08-22 scoped stamp (Story 25.4) also trued up stale baseline hashes for 3 files under this surface landed by earlier stories (cli/spin.py, core/spec_difficulty.py, planning-artifacts/marshal-policy.toml) — stale-baseline catch-up verified against main (working tree identical to committed content), not content change by Story 25.4; 25.4 itself touched only core/policy.py and adapters/harness_bmadloop.py here.
