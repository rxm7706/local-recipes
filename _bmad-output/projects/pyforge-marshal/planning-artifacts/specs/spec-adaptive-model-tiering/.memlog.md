---
topic: Adaptive model tiering — FR-51's tier chain fed with real data, plus retry-triggered escalation
updated: 2026-08-11T06:46
---

- (note) Input: docs/dreams/adaptive-model-tiering.md, a rich, code-grounded Dream (file:line citations throughout) -- express mode, no elicitation needed.
- (decision) Slug is adaptive-model-tiering (Dream's own filename stem) -- describes the thing being specced, matches Dream-to-Spec 1:1 convention used by spec-fleet-status-supervisor-fallback.
- (capability) CAP-1: a project's model_tier_map, once populated with real {difficulty: {stage: model}} entries, and a story that declares a real difficulty, together change what render_policy_toml actually writes -- closing the gap where the full chain (Story 6.1) is built but zero project/story ever feeds it (grep-confirmed on all 8 loop-home projects).
- (capability) CAP-2: a story whose attempts/review cycles show it is genuinely struggling gets a floor-raised model without an operator hand-editing policy.toml and relaunching -- closes the retry-escalation gap confirmed absent in both core/policy.py (tier resolved once per launch, before bmad-loop run is spawned) and vendored bmad_loop 0.9.0 (escalation.py:68 couples task.attempt only to a budget check, never to model selection).
- (constraint) No new tiering mechanism: model_tier_map's shape, render_policy_toml's tier-batching (harness_bmadloop.py:450-459), and cli/spin.py's difficulty-resolution chain (Story 6.1) are already correct and shipped -- this Spec is about feeding real data and adding the retry path, not rebuilding FR-51.
- (constraint) Run-level batching granularity stands: one difficulty governs a whole launch, a mismatched story is reported not silently ignored (cli/spin.py::_resolve_governing_difficulty) -- matches upstream-register.json's own accepted per-story-model-tiering workaround; not relitigated.
- (constraint) A retry-escalation trigger must be derived from journal/attempt facts the fold already accumulates (AD-26) -- never a new hand-maintained flag.
- (constraint) Every model change, declared or escalated, is journaled with the same intent/outcome discipline (AD-28) every other supervisor action already uses.
- (constraint) An escalation is a floor-raise only, on top of whatever the declared tier already picked -- never a downgrade.
- (assumption) run_resume (Story 3.7, cli/spin.py ~line 177) already re-resolves and re-renders model tiering before detach-launching 'bmad-loop resume', the same as run_spin does before 'bmad-loop run' -- so a resume-time re-tiering path already exists structurally; CAP-2's downstream story may be able to lean on it (e.g. escalate on the NEXT resume after a deferral) rather than requiring a brand-new mid-run intervention. Not prescribed here -- a downstream design decision.
- (question) Which signal decides a story's difficulty when nobody hand-authors one: an authored convention (a human or bmad-create-story names it against a stated rubric) or a derived heuristic (spec size / epic / prior attempt history, the same signals FR-14's budget advisory already reasons about)? Left to the downstream story per the Dream's own explicit non-prescription.
- (question) Does CAP-2 need Marshal to intervene mid-run (kill+relaunch the harness session under a re-rendered policy) since bmad_loop 0.9.0 loads policy once at run start with no per-attempt model hook, or can it work entirely through the existing deferral-then-resume path (Story 3.7) where re-tiering already happens naturally? Determines whether CAP-2 is buildable as a live in-run mechanic or a resume-time one.
- (question) What attempt-count or review-cycle threshold should trigger a retry escalation, and is it a fixed default (like max_dev_attempts=2) or itself policy-configurable?
- (event) Pass 1 (coherence) PASS: both CAPs carry intent+success (rule 1); intents are WHAT not HOW, mechanism deferred to open questions/downstream (rule 2); every constraint rules something out -- no per-story mid-run granularity, no downgrade, journal-derived trigger only (rule 3); 4 non-goals stated (rule 4); success signal is diff-provable / journal-provable (rule 5); CAP-1/CAP-2 ids stable, no prior spec existed for this slug (rule 6); prose is lean, no decoration added beyond what the Dream's own citations support (rule 8).
- (event) Pass 2 (preservation) PASS: every load-bearing claim from the Dream (flat sonnet/opus assignment, the full Story 6.1 chain being built-but-unfed, the grep-confirmed zero-declarations finding, the retry-escalation absence on both sides of the seam, the run-level-batching constraint, the resume-time re-render assumption) lands in SPEC.md's Why/Capabilities/Constraints/Open Questions. No wrapper-only content dropped -- the Dream's own file:line citations are preserved via the surface: list and Why section rather than restated verbatim.
- (event) Spec finalized: 2 capabilities, 0 companions, 3 open questions carried from the Dream plus the resume-path assumption. status: draft.
