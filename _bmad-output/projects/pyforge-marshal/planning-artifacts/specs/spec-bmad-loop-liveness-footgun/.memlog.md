---
topic: Marshal-side liveness primitive + one-command operator answer for bmad-loop run liveness (engine.pid footgun containment)
updated: 2026-09-09T18:07
---

- (direction) Unattended headless run (agent-driven); express mode chosen for sparse-gap handling -- every unresolved call recorded as assumption or open question, never invented.
- (decision) Slug bmad-loop-liveness-footgun inherited from the Dream filename; project pinned via BMAD_ACTIVE_PROJECT=pyforge-marshal and physical-path writes per the repo's parallel-agent rule (no bmad-switch).
- (note) Convergence check performed against main @ d7b19b255b (2026-08-21), per the repo's standing check-convergence-before-new-FR rule.
- (note) Covered: the fleet-status dead-sidecar fallback is spec-fleet-status-supervisor-fallback (Story 5.8 / FR-181, epics.md:1439) -- not re-minted here.
- (note) Convergence discovery: Story 5.8's engine_alive implementation exists ONLY on unlanded branch bmad-loop/20260811-190409-5c73/5-8-...; git grep + git log -S confirm engine_alive never reached main despite ledger done + spec frontmatter shipped -- landing it is 5.8's own affair, recorded as an assumption, not scope.
- (note) Covered: marshal status --format json (Story 5.1) + scripts/fleet_picture.py::running_stations already answer supervised-run liveness without engine.pid; scripts/loop_stall_check.py answers progress via state.json + log/journal mtime + tmux pane, never engine.pid.
- (note) Covered: the probe itself already ships upstream as bmad-loop status <run_id> --json (clean {run_id,status}, confirmed live in the Dream against run 20260814-201915-b953) -- the residual is BINDING this repo to that surface, not building a probe.
- (note) git grep confirms no code in this repo parses engine.pid today -- the footgun is operator/protocol-level, exactly as the Dream's empirical section states.
- (note) Adjacent-not-absorbed: DW-5-8-1 (is_run_live never consults engine_alive) and DW-5-8-2 (ProcessPort.is_alive pid-reuse/pid-0 hardening) are separate deferred stories on Marshal's own pid probes; the CAP-1 primitive may later serve them but does not fix them.
- (capability) CAP-1: a Marshal-side liveness primitive consuming bmad-loop status <run_id> --json -- the missing HarnessPort counterpart spec-3-7's deferred [medium] entry names; tri-state alive/dead/unknown; unblocks (not fixes) the factory-resume double-drive gap.
- (capability) CAP-2: the operator answer is one command -- the fleet landing-pass protocol stops prescribing ps-grep + engine.pid hand-parsing as primary; any residual corroboration grep must match all three argv forms (bmad-loop run|resume|resolve), per the 2026-08-14 mason incident.
- (capability) CAP-3: an UNSUPERVISED/unknown row for a run Marshal did not spawn has a documented one-command double-check path via the primitive -- closing the Dream's 'no cheaper way than raw ps/tmux' bullet without touching Story 5.8's row derivation.
- (constraint) HARD: no in-place edits to the installed bmad_loop package (git-pinned pixi dep, wiped on pixi install, live-imported); no private-API imports (bmad_loop.runs.engine_liveness etc.) -- documented CLI/output contract only.
- (constraint) NFR-14: the primitive is on-demand (resume preflight, explicit double-check), never wired as a per-home subprocess probe into marshal status's fleet sweep -- 'no live query per home' stands.
- (decision) Convergence exclusions land in companion convergence.md (multi-item catalog per the companion rule); the kernel cites it.
- (question) unknown-verdict policy at resume preflight: refuse, warn-and-proceed, or operator-confirm (spec-3-7 flags exactly this policy decision).
- (question) Primitive home: HarnessPort method (ports/harness.py) vs scripts/*.py helper vs pyforge.doctor source -- story-level design decision.
- (question) Operator front door: document bmad-loop status --json directly (the Dream's own wording) vs a marshal wrapper subcommand (one-front-door tension).
- (assumption) bmad-loop status --json's {run_id,status} shape is treated as the stable contract at the 0.9.0 pin; a future upstream bump revalidates it (same posture as every bmad-loop-* sibling spec).
- (event) Self-validate pass 1 (coherence): PASS -- 3 capabilities each with intent+success; constraints all decision-bending; 5 non-goals; success signal replays two dated incidents; CAP-1's named consumption of bmad-loop status --json is Dream-mandated boundary, not HOW-prescription.
- (event) Self-validate pass 2 (preservation): PASS -- every load-bearing Dream claim traced into SPEC.md or convergence.md; wrapper-only drops: heading ceremony, kinship wiki-link syntax, Realization-log narrative beyond incident facts.
- (event) Spec finalized at status: ready; Dream frontmatter dreamt -> specified in the same branch (spec/bmad-loop-liveness-footgun).
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Surface reconcile 2026-08-31: 2 drift-presumed paths since baseline at `26102ea12c6d` (Story 14.4 subprocess-seam merge). Eight commits, all already-done story work on governed `ports/harness.py` and `cli/spin.py`:
  - `ef5b32fb91` — Story 25.5: HarnessPort gains preserve_ref/sweeps_ref for 0.11 status vocabulary.
  - `48bcdf9275` — Story 20.4: preserve_ref on HarnessPort for intent-gap supervisor seam.
  - `d4686b0aa7` — Story 22.1: spin.py gains factory dispatch verb wiring.
  - `1a083201ce` — Story 22.6: spin.py dispatch-survives-operator journal timing.
  - `b829ccfe4d` — Story 24.1 (this spec's CAP-1): HarnessPort.engine_liveness primitive landed.
  - `58f2378e25` — pyforge-core hooks extraction: spin.py import reparenting.
  - `c42090a804` — Story 22.7 WIP autosave: spin.py partial checkpoint.
  - `6796ef1282` — Story 28.2: spin.py compression wiring at harness seam.
  No CAP text change. RECONCILES: `src/shared/packages/pyforge-marshal/src/pyforge/marshal/ports/harness.py`, `src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/spin.py`. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-bmad-loop-liveness-footgun`.
- (event by claude) Surface reconcile 2026-09-01: `ports/harness.py` changed once since the 2026-08-31 stamp by `80b773dc53` (Story 28.4, done) — savings telemetry primitive extension at the HarnessPort seam. CAP-1 `engine_liveness` unchanged. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-bmad-loop-liveness-footgun`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 2 governed path(s) drifted against this spec's recorded baseline (2 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/spin.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/ports/harness.py` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-bmad-loop-liveness-footgun` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (note) Cross-station surface touch (marshal Story 30.5, 2026-09-06): `cli/spin.py`'s docstring renamed a present-tense reference from `bmad-dev-auto` to `bmad-build-auto` (which spec-writing skill actually writes spec files today, under the 6.12 shim-roster rename). Docstring-only, no liveness-probe or harness-port behavior changed. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (decision by claude) 2026-09-09 OQ-1 ANSWERED by operator (fleet-readiness-decision-batch-2026-09-09 row mars-A-B4 / C10): the resume-preflight `unknown` verdict policy is WARN-AND-PROCEED, with `unknown` a FIRST-CLASS PRINTED REASON -- not refuse, not operator-confirm. EngineLiveness is a deliberate tri-state and ports/harness.py:781-782 states "unknown" is never coerced; refusing would coerce it into alive semantics at the operator expense, and every `unknown` cause listed there (unreadable run dir, unimportable bmad_loop) is a PROBE failure, not a run failure. Live evidence it is the common case: auto-memory project_steward_unknown_landing_journal records steward reading `unknown` from a landing journal after every land-story, so a refuse policy would block routine resumes daily. Rejected: operator-confirm -- it converts an unattended resume into an attended one on the fleet most frequent condition. Open questions 2 and 3 were already answered by Epic 24 shipment; this closes the last one.
- (event by claude) status: ready -> in-progress (fleet-readiness-decision-batch-2026-09-09 Class B, row mars-A; Table A). Epic 24 is 3/3 `done` and CAP-1..3 are in effect -- HarnessPort.engine_liveness (ports/harness.py:771-782) implemented at adapters/harness_bmadloop.py:1657 by shelling to `bmad-loop status <run_id> --json` plus `list --json`, never importing bmad_loop; consumed live by cli/dispatch.py:2711-2726 liveness refusal. NOT `shipped`: OQ-1 above was explicitly out of Epic 24 scope (spec-24-2 :18, "resume preflight consumption (spec-3-7 deferred)") and the operator answer still needs wiring into the resume preflight, so the Spec sits in-progress until that lands.
- (change by claude) Surface touch 2026-09-09 (naming the governed path for spec-surface reconciliation): docs/dreams/bmad-loop-liveness-footgun.md changed -- frontmatter status specified -> realized plus a dated Realization-log entry recording Epic 24 in effect and the operator answer to OQ-1 (warn-and-proceed on unknown). Content-only, no CAP or surface semantics changed. RECONCILES: docs/dreams/bmad-loop-liveness-footgun.md. NOT stamped: the scoped --write-baseline --spec pyforge-marshal/spec-bmad-loop-liveness-footgun is deliberately left to the lead after the bmad-spec re-derives (fleet-readiness-decision-batch-2026-09-09 apply-order step 8), so spec-surface reports one drift-presumed warn on this path until then.
- (event) (self-validate pass 1 -- coherence) 2026-09-09 re-derive: PASS. status ready -> in-progress (NOT shipped -- the memlog is explicit that OQ-1's wiring is unbuilt); all three questions retired, OQ-2/OQ-3 as answered-by-Epic-24 and OQ-1 with its own dated policy section carrying the tri-state reasoning and the rejected alternative. 3 CAP ids, unchanged.
- (event) (self-validate pass 2 -- preservation) 2026-09-09 re-derive: PASS. All three 2026-09-09 entries landed: the warn-and-proceed answer with ports/harness.py:781-782 and the steward-unknown live evidence, the in-progress rationale with the Epic 24 shipped-surface citations, and the Dream's specified -> realized flip. Only superseded frontmatter removed.
