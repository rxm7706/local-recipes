---
topic: Marshal token economy -- the loop spends tokens it never had to: brakes exist (ceilings, idle ladder, cache TTL, tiering) but nothing shrinks what an iteration reads and writes
updated: 2026-09-09T18:39
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-token-economy`.


- (note) Source: docs/dreams/marshal-token-economy.md (captured 2026-08-30). Governs the marshal loop context pipeline: core/policy.py (EffectivePolicy + render), core/harness_profile.py + data/harness_profiles/*.toml, seed/ (Genesis), supervisor/ + core/supervise.py (telemetry, ladder), cli/check.py (detector front door).
- (note) Ground truth from repo exploration 2026-08-30: marshal token posture is brakes only -- E3.6 ceilings (50M/500M weighted), idle ladder, NFR-14 prompt-cache TTL discipline (poll <=60s, cache_read_weight=0.1), FR-51 model tiering (wired, rarely fed), E22 dispatch. Nothing compresses/indexes/graph-serves context anywhere in marshal, bmad-loop, or bmad-build-auto.
- (note) Per-iteration spend map: ~23k tokens identical always-on docs (CLAUDE.md ~10k + AGENTS.md ~3.6k + project-context.md ~9k) per fresh session; story spec + epic-context + continuity 5-16k; unbounded mid-story tool output (test logs 80-95 pct noise); full codebase re-exploration per session; 4 fresh reviewer lenses multiply it; agent output weighted heaviest.
- (note) Instruments already packaged, none wired: recipes/headroom-ai (0.32.1 Apache-2.0, lib+proxy+MCP+agent-wrap, reversible CCR, live-zone-only so provider cache hot zone untouched), recipes/caveman (2.4.0 MIT skill installer only, ~65 pct output cut; input proxy @caveman-ai/cli is BSL-1.1, NOT packaged), recipes/codegraph (1.6.0 MIT, local pre-indexed code KG, linux-64 only in pixi), recipes/cocoindex (1.0.20 Apache-2.0, on conda-forge, incremental derived indexes), recipes/graphifyy (0.9.44 MIT, folder-to-KG; Scribe plans GraphStore seam). headroom-ai + caveman commented-out in pixi.toml, root-caused blocked in docs/dreams/pixi-candidate-currency.md (conda-recipe-manager==8.2.1 pin chain).
- (capability) CAP-1 declared context pipeline: a [context] policy block in EffectivePolicy, rendered into every policy.toml/launch surface identically for BOTH engines (bmad-loop spin and bmad-build-auto dispatch) -- which layers are on, aggressiveness, store paths. Success: rendered output carries the block from one composition site; a run on either engine resolves the same declaration; absent block means every layer off (today behavior byte-identical).
- (capability) CAP-2 wire compression at the harness seam: when policy enables it, sessions launch through the compression layer (agent wrap/proxy) so tool outputs, logs, file reads compress before the provider call; compressed originals land in a loop-home-scoped CCR store the agent can retrieve from. Success: launched command demonstrably wrapped; a compressed artifact is retrievable byte-exact; prompt prefix (system prompt, tool defs, older turns) byte-identical wrapped vs unwrapped.
- (capability) CAP-3 output-compression seeding: Genesis deploys the caveman skill per loop home so dev-session speech compresses ~65 pct, while review verdicts, journals, escalation context stay fully articulated. Success: marshal seed check verifies deployment; a landed story verdict + journal read as normal prose.
- (capability) CAP-4 structure from the graph: loop-home provisioning builds/syncs the codegraph index and wires the agent integration so structure questions resolve from the graph instead of file re-reads. Success: seed check proves index present + fresh at admission; a session answers a structure query without re-reading the files it names.
- (capability) CAP-5 incremental derived context: epic-context / continuity distills become incrementally-maintained derived artifacts (cocoindex) recomputed only when their planning sources change. Success: unchanged sources yield zero recompute across two consecutive iterations; a source edit yields exactly one refresh.
- (capability) CAP-6 planning-graph retrieval: story routing retrieves the scoped planning context a story binds to (graph query via the Scribe-owned GraphStore seam over graphifyy) instead of loading epics.md/prd.md wholesale; degrades to epic-context files when the seam is absent. Success: an epic-path iteration completes within the epic-context token target with zero full-document loads; fallback proven by disabling the seam.
- (capability) CAP-7 savings telemetry: the supervisor journals per-story spend AND per-layer savings (wire-compression stats, output-compression delta, graph-hit vs file-read counts); marshal status renders both mid-run. Success: journal entries carry savings fields; status shows them while a run is live (chips at DW-FU-3-6-6 mid-session blindness).
- (capability) CAP-8 graduated compression ladder: as a story approaches its token ceiling the supervisor raises compression aggressiveness (and may lower the model floor via existing FR-51 tiering) BEFORE the stop-retry-defer ladder fires. Success: a test proves ladder ordering -- compression escalation strictly precedes kill -- and that no gate or reviewer is ever skipped by escalation.
- (capability) CAP-9 measured baseline: a pinned, re-runnable benchmark (same story, layers on vs off) reports before/after weighted tokens per layer. Success: benchmark artifact reproducibly emits the comparison; ceiling recalibration cites it.
- (capability) CAP-10 index freshness is an admission signal: marshal check gains advisory codegraph/cocoindex staleness findings. Success: a stale index yields a named finding; exit-code domain {0,1,2,3,4,130} unchanged; finding never blocks a run by itself.
- (constraint) Never compress the contract: story specs, acceptance criteria, gate verdicts, escalation context cross the wire intact -- compression applies to what the agent reads along the way and says, never what it is bound by.
- (constraint) Never break the provider prompt cache (NFR-14): any layer that rewrites the prompt prefix is inadmissible; live-zone-only compression is the admission requirement, proven by prefix byte-comparison.
- (constraint) Reversible or absent: lossy-with-retrieval (CCR) is acceptable; silently-lossy is not -- a compressed FATAL line must be recoverable byte-exact.
- (constraint) BSL boundary: the caveman input proxy (@caveman-ai/cli, BSL-1.1) stays unpackaged and unwired; headroom-ai (Apache-2.0) is the input side.
- (constraint) Telemetry stays advisory: savings numbers inform ceilings and tiering, never a pass/fail verdict -- no second PR gate (Warden doctrine).
- (constraint) Graceful degradation per layer: an unavailable instrument (platform gap, pixi blocker) disables its layer with a named finding, never blocks a run -- headroom-ai/caveman pixi activation is a prerequisite tracked in pixi-candidate-currency, not silently assumed.
- (note) Non-goals (from the Dream): not altering BMAD skill semantics (story contract, gates, review occurrence untouched); not replacing the spend brakes (ceilings/idle/tiering stay); not a second knowledge graph inside marshal (GraphStore is Scribe-owned; marshal consumes); not the pixi unblocking work itself (owned by pixi-candidate-currency); not review-depth scheduling (owned by risk-tiered-review-depth spec).
- (direction) Operator direction 2026-08-30: run bmad-spec on the Dream and prepare everything so marshal factory dispatch/drain (drain_to_zero) can run -- only implementation of the epic + stories remains for the next session. Decomposition goes to pyforge-marshal epics.md as Epic 28 with per-story ready-for-dev specs and ledger backlog entries.
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6+8): clean -- 10 capabilities each with intent+success, constraints all design-bending, 5 non-goals, concrete success signal, IDs unique, lean.
- (event) Self-validate Pass 2 (preservation vs docs/dreams/marshal-token-economy.md): clean -- spend map, 5-instrument matrix + availability gates (to integration-layers.md companion), 7 marshal features (to CAP-1..CAP-10), guardrails (to Constraints), benchmark-first gate (CAP-9), Scribe seam ownership (CAP-6 + non-goal) all landed. Dream open question "where does compression config live across engines" resolved INTO CAP-1 (one composition site, both engines identical). Wrapper-only content dropped: none.
- (event) SPEC.md derived at status: ready + companion integration-layers.md written, 2026-08-30. Decomposition: Epic 28 in epics.md with 9 stories, per-story ready-for-dev specs, ledger backlog entries -- for marshal factory dispatch/drain.
- (direction) Operator direction 2026-08-30 (second pass): analyze whether adaptive-model-tiering or the pyforge-unifying-strategy Canopy work must fold into this spec; combine implementation where it cuts cycles.
- (correction) CAP-8 amended 2026-08-30: the graduated ladder is COMPRESSION-ONLY -- the original "may lower the model floor via FR-51 tiering" clause conflicted with spec-adaptive-model-tiering's shipped constraint (escalation is a floor-raise only, never a downgrade; Stories 3.11-3.13 done, tiering realized). Model movement stays with declared difficulty (static) and Story 3.12's upward-only floor-raise (dynamic). Propagated to SPEC.md CAP-8, epics.md Story 28.6, spec-28-6, and the Dream's ladder bullet.
- (note) Tiering adoption side-effect: Epic 28's nine specs are the fleet's first stories declaring difficulty:. Found + fixed a dispatch-path quirk: resolve_dispatch_model falls back to next(iter(tier_map)) for unmapped difficulties, so difficulty: medium would have inherited the heavy tier (opus dev) on dispatch. marshal-policy.toml now pins [model_tier_map.medium] dev=sonnet review=opus explicitly (spin was unaffected: unmapped -> no override there).
- (decision) CAP-5/CAP-6 binding sharpened 2026-08-30 per unifying-strategy Grounding 2026-08-30 (Scribe three CAP-18 ports: graph_store persist / compile_surface ingest / recall_ranker): the cocoindex engine for CAP-5 is Scribe's compile_surface cocoindex extra (scribe Story 6.2), the graphify ingest for CAP-6 is scribe Story 6.1 -- marshal consumes by scribe grammar only, no marshal-private flow. One binding serves two consumers (foundry-cutover move-list + token-economy layers 3-4). Scribe Epic 6 (Stories 6.1, 6.2) minted this pass in pyforge-scribe's epics.md + ledger (backlog) because the extras had zero stories anywhere.
- (constraint) spec-28-2 gained a Never 2026-08-30: headroom's cross-agent SharedContext memory stays off -- a second memory store-of-record by the back door; Scribe capture/recall is the fleet's only memory face (no mem0.add in place of scribe capture).
- (note) Sequencing ruling from the fold-in analysis 2026-08-30: drain Epic 28 BEFORE the foundry package fold (`docs/dreams/pyforge-unifying-strategy.md` § One working tree / phases 0–6) -- the cutover relocates every path Epic 28's code maps and the spec surface globs cite (src/shared/packages/ -> src/packages/, .claude/skills/ -> skills/), and it would invalidate the 28.5 benchmark baseline. Cutover work itself stays out of this spec (different Dream, steward/scribe scope).
- (direction) Operator direction 2026-08-30 (third pass): supplied the live 2026 model/cost catalog across the four active subscriptions (Cursor Ultra $200/mo, Claude Max, Copilot, Gemini API) -- use it to refine the dream/spec/stories for multi-agent multi-model marshal.
- (capability) CAP-11 declared model-cost catalog: policy carries a price table (per provider/model: in/out/cache-read/cache-write per 1M + subscription-pool membership; seed snapshot = companion model-economics.md); telemetry (CAP-7) and benchmark (CAP-9) render dollar estimates alongside weighted tokens; per-provider token weights derive from declared ratios (global constant fallback). Success: dollars present when catalog declared, absent (never fabricated) otherwise; Cursor-pool cache reads weighted at declared 0.25-0.40 not the Anthropic 0.10 constant; no network price fetch.
- (capability) CAP-12 cross-provider tier routing: model_tier_map stage entries can name (harness profile, model) pairs -- easy->economy class, medium->standard, heavy->frontier (ladder is data in model-economics.md) -- preferring subscription-marked pools before metered API; serving pool journaled; exhausted pool falls through, never blocks. Success: rendered-launch diff proves the pair per declared difficulty on both engines; FR-51 seam remains the only selection mechanism; run-level batching unchanged; review floor holds.
- (constraint) Declared prices only: the catalog is hand-refreshed policy data (dated snapshot), never a live price/billing fetch. Dollar figures advisory (extends the telemetry constraint). Review floor holds under routing: cross-provider routing never places review below the policy-declared floor.
- (note) Non-goals added: not a billing integration (no live reconciliation against provider dashboards); not multimodal surfaces (audio/TTS/video/music/robotics/embeddings out of scope).
- (note) Catalog ground truth 2026-08-30: input-rate spread across the ladder ~50x (Flash-Lite/Luna 0.10-0.20 -> Fable 10.00 per 1M), output ~125x. Cache-read/input ratio is 0.10 for Anthropic+OpenAI+Gemini (NFR-14's cache_read_weight=0.1 exactly right there) but 0.25-0.40 for Cursor first-party -- the global constant silently understates that pool.
- (correction) Operator clarification 2026-08-30 (b): Claude Max is the 20x-Pro usage tier -- the largest flat pool in the estate; first preference for claude-harness runs and the reason the claude harness stays the drain default.
- (correction) Operator clarification 2026-08-30: the Copilot subscription is Pro ($100/yr) with a DOWNGRADE PENDING, plus GitHub Pro ($48/yr, repo features not a model pool). Copilot Pro's premium-request quota can drive the copilot harness while it lasts, but routing treats it as a transitional fall-through candidate only, never a preferred pool; post-downgrade it exits the routing table (inline/chat only).
- (event) Second decomposition pass 2026-08-30: Stories 28.10 (CAP-11) and 28.11 (CAP-12) added to epics.md (storyCount 179->181), ready-for-dev specs written, ledger backlog synced. SPEC surface extended with core/dispatch.py + cli/dispatch.py; companions now integration-layers.md + model-economics.md.
- (event) Availability gate RESOLVED 2026-08-30 (operator-directed hygiene pass before the drain): headroom-ai 0.37.0 ACTIVE in pixi (all platforms) and caveman 2.4.0 build 2 ACTIVE (linux-64 target) -- Layers 0-1 no longer degrade on the linux loop fleet. Two levers: (1) the conda-recipe-manager click==8.2.1 wall was REMOVED, not waited out -- crm+feedrattler (zero in-repo invocations, verified) moved to a grayskull-env-only [feature.crm.dependencies]; dbt trio unblocked by the same move; (2) caveman got a patched SelfExplainML rebuild (build 2, host nodejs held at 24.* so its run-export lands inside codegraph's nodejs >=24.19,<25 window -- the sharpened root cause: build 1 was mutually exclusive with codegraph, independent of marp-cli). Stories 28.2/28.3 will now wire live instruments instead of raising degradation findings; the degradation path remains specced for non-linux platforms and future regressions. Propagated: Dream availability gate, SPEC.md degradation constraint, integration-layers.md rows 0-1, pixi-candidate-currency.md addendum, library-llms-full.md.
- (correction) Placement correction 2026-08-30: headroom-ai moved from the cross-platform table to the linux-64 target table (operator preference -- grouped with graphifyy/codegraph/caveman; it solves cross-platform and can be promoted if win/osx ever need it). Both Layer 0-1 instruments are therefore linux-64-only, matching the linux loop fleet; the SPEC's degradation constraint covers non-linux platforms.
- (direction) Operator direction 2026-08-31: compared Mem0's OSS memory-layer consolidation model (retrieve-then-decide ADD/UPDATE/DELETE/NOOP via LLM tool-call) against Scribe's own supersession (Story 2.3, author-declared supersedes: only) to see if it sharpens CAP-6/Story 28.9's design.
- (note) Finding: Scribe's supersession is purely author-declared, no automatic staleness detection -- a graph node whose source moved with no supersedes: link back to it can be silently served as stale planning context by CAP-6 retrieval. Mem0's fix (LLM-judged consolidation, one call per write) is not worth porting: Scribe's memories are already structured, so the only missing signal is a timestamp comparison, not a judgment call. Consistent with the existing spec-28-2 Never (no mem0.add in place of scribe capture) -- this borrows the CONCEPT, not the dependency.
- (capability) CAP-13 graph-node staleness flag: compile_graph flags a node stale: true when its source file's latest git commit postdates the node's own valid_from and no supersedes: edge points at it -- zero LLM calls, one more field on data compile already touches (it already walks git history for other surfaces). Success: a node whose source changed since compile with no declared supersession is flagged stale; an unchanged source or a properly superseded node is never flagged; no new dependency, no network call.
- (decision) CAP-13 ownership split 2026-08-31: the staleness flag is Scribe's own compile-step capability (GraphStore/compile_surface, CAP-18) -- Story 28.9 cannot own it (its Boundaries already forbid touching Scribe's station code beyond the consumer side). Lands as a new Scribe Epic 6 story (sibling to 6.1) plus a fifth AC on Story 28.9: a graph answer whose backing node is flagged stale falls back to the epic-context file path (28.8's proven fallback), never served silently. CAP-6's existing contract is unchanged; CAP-13 layers on top of it.
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6+8) on CAP-13: clean -- intent+success present, constraint added actually bends the design (rules out LLM-judged consolidation), success signal concrete/testable, ID unused/stable, lean. 11 capabilities total, non-goals unaffected.
- (event) Self-validate Pass 2 (preservation vs the 2026-08-31 Dream addendum): clean -- Mem0 comparison finding, the staleness gap, the git-timestamp resolution mechanism, and the zero-LLM/no-new-dependency property all landed (CAP-13 + new Constraint). Ownership split (Scribe Epic 6 story + Story 28.9 fifth AC) deliberately left to the decomposition step, matching how CAP-11/CAP-12 -> Stories 28.10/28.11 was handled. Wrapper-only content dropped: none.
- (event) Decomposition 2026-08-31: CAP-13 landed as scribe Story 6.3 (The graph-node staleness flag, new -- pyforge-scribe epics.md + spec-6-3 + ledger backlog entry synced via bmad-sprint-planning's generate) and as Story 28.9's fifth AC (amendment -- spec-28-9 + epics.md updated in place, no new marshal story minted, Deps gained scribe S-6.3). No CAP-6 contract change.
- (event) Story 28.3 implemented 2026-08-31 (CAP-3 + CAP-4, the loop-home half). Genesis now provisions and verifies the per-loop-home token-economy kit: three items, each gated by its own Story 28.1 [context] layer -- caveman-skill (layer output) deployed to <home>/.claude/skills/caveman/SKILL.md, ccr-store (layer wire) at <home>/.marshal/wire, codegraph-index (layer structure-graph) at <home>/.codegraph/codegraph.db. Surface touched, named for the spec-surface detector: src/shared/packages/pyforge-marshal/src/pyforge/marshal/seed/model/kit.py (new, the closed KitItem vocabulary + ARTICULATE_SURFACES), src/shared/packages/pyforge-marshal/src/pyforge/marshal/seed/detect/kit.py (new, kit_checks/kit_findings -- three distinct read-only checks), src/shared/packages/pyforge-marshal/src/pyforge/marshal/seed/verbs/kit.py (new, run_kit -- provisioning, dry-run by default), src/shared/packages/pyforge-marshal/src/pyforge/marshal/seed/detect/findings.py (three new FindingType members + remedies), src/shared/packages/pyforge-marshal/src/pyforge/marshal/seed/verbs/check.py (the optional context_layers keyword + CheckReport.kit). Also outside this spec's own surface globs but inside Epic 28's: cli/seed.py (the `marshal seed kit` verb + resolve_context_layers), cli/init.py (preflight provisions the kit), core/findings.py + core/verdict.py (MRS-PREFLIGHT-015, WARN), docs/finding-remedy-reference.md, .gitignore.
- (constraint) Realized 2026-08-31: "never blocks a run" is now STRUCTURAL for the kit, not conventional -- no kit FindingType is HARD (kit-item-missing / kit-item-stale are DRIFT, kit-instrument-unavailable is INFO, which not even --strict fails on), MRS-PREFLIGHT-015 classifies WARN, and `marshal seed kit` returns 0 on every completed run. A HARD kit type would have made a missing optional instrument fail `marshal seed check`, which the degradation constraint forbids.
- (correction) Vendor-claim correction 2026-08-31: the codegraph FIRST index build is `codegraph init -y`, not `codegraph index`. `codegraph index --help` says "Rebuild the full index from scratch (same result as a fresh init)"; run live against a directory with no .codegraph/ it exits 1 with `Run "codegraph init" first`. Caught by an end-to-end smoke against the real instrument. Stale-index refresh is `codegraph sync -q`. Recorded here because integration-layers.md's Layer-2 row ("synced on change") does not distinguish the two verbs.
- (note) AC-2 mechanism 2026-08-31: "never compress the contract" is enforced at the deployment, not hoped for. The deployed skill is the packaged upstream SKILL.md byte-for-byte plus a Genesis-managed `marshal-seed:begin/end region=token-economy-articulate` region whose body is rendered from ARTICULATE_SURFACES (review verdicts, journal entries, escalation context, the story contract). A deployed skill WITHOUT that region is reported `missing`, never `ok`. Residual, recorded honestly: nothing in a test suite can prove a model honoured the carve-out -- what is proven is that the instruction is deployed, names every contract surface, and that marshal's own verdicts/journals are composed by marshal code that never passes through the skill.
- (note) Deployment mechanism 2026-08-31: Genesis writes the skill bytes itself rather than shelling out to caveman-install. Verified against the installed 2.4.0: the installer's Claude Code mechanism is `claude plugin install` (user-global, marketplace, networked) plus `npx skills add`, and --config-dir scopes hook files and settings.json only. None can be scoped to one loop home. The payload is resolved from caveman-install on PATH (<prefix>/lib/node_modules/caveman-installer/skills/caveman/SKILL.md), so the deployment tracks the installed package without a hardcoded prefix. Open: the deployment is a snapshot -- a caveman upgrade leaves seeded homes on the old body, reported OK (carried in spec-28-3's deferred list).
- (note) Deferred out of Story 28.3, carried in its own frontmatter: the codegraph AGENT INTEGRATION (`codegraph install --target claude --location local`) is unwired -- it writes the home's .mcp.json, which marshal already owns and renders from the mcp_servers policy key, so a second writer would fight the renderer. AC 1's check list names the index only, which is what shipped. The reachable path is teaching the mcp_servers render to append a codegraph entry when the structure-graph layer is enabled (Story 28.1's policy-render surface).
- (correction) Story 28.3 review pass 2026-08-31, 18 findings patched, no spec loopback (intent-contract unchanged). Four corrections worth recording at spec level because they generalise past this story: (1) TEST HYGIENE -- two kit tests resolved the real instruments from the ambient PATH, so they proved the developer's shell rather than the code (green locally, red on CI where only the pyforge-marshal env installs, and on macOS where caveman/codegraph cannot exist). Instrument availability is now DECIDED through an injected probe in every kit test, and `cli/init.py::run_preflight` grew `kit_probe=`/`kit_index_builder=` DI seams to make that possible. Any later CAP-3/CAP-4 story that reaches for a real instrument in a test inherits the same trap. (2) A write seam and its verification must be the SAME seam: `run_kit` wrote through an `FsPort` while its post-apply re-check read raw `pathlib`, so under any non-local port it reported its own successful applies as missing. `kit_checks` now takes a `KitReads` seam (`FsPort` satisfies it structurally). (3) `codegraph sync` succeeding with nothing to resync does NOT touch the db, so an index whose mtime predates HEAD stayed `stale` forever and every preflight re-synced and re-reported DRIFT -- a successful step now stamps the index mtime. Recorded here because the same shape would bite CAP-5's cocoindex freshness. (4) A failed provisioning step must always yield a finding even when the re-check reads OK: a build killed at the timeout leaves a partial artifact with a fresh mtime, which the detector correctly reads as present-and-fresh.
- (note) Freshness cadence, recorded against CAP-10/CAP-13 rather than only in spec-28-3's deferred list: the `index_mtime < head_commit_time` rule means EVERY commit marks the index stale. In a bmad-loop/dispatch worktree that commits many times per story, an enabled `structure-graph` layer therefore sits in near-continuous `kit-item-stale` DRIFT between preflights. Deliberate (deterministic, no working-tree walk, no possible false GREEN for the case that matters) but the cadence, not the two accuracy bounds, is the consequence an operator actually meets -- and CAP-10's advisory staleness findings in `marshal check` will inherit it.
- (event by claude) Surface reconcile 2026-08-31: 4 drift-presumed paths since baseline at `0770dd7a20` (PR #931 dispatch-landing fix). Three commits, Epic 28 token-economy + Story 22.11 drain (all done in ledger):
  - `e58ef1fa4b` — Story 28.1 (done): `[context]` policy block landed in `core/policy.py`.
  - `2504eac0a1` — Story 22.11 (done): station-scoped drain + explicit story sequence on `cli/dispatch.py`.
  - `6796ef1282` — Story 28.2 (done): compression wired at harness seam — `core/harness_profile.py`, `data/harness_profiles/claude.toml`.
  Memlog entries above (2026-08-31) narrate 28.1-28.3 delivery but did not name these governed paths for spec-surface per-file reconciliation. No CAP text change. RECONCILES: `cli/dispatch.py`, `core/harness_profile.py`, `core/policy.py`, `data/harness_profiles/claude.toml`. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-token-economy`.
- (event by claude) Surface reconcile 2026-09-01: 5 drift-presumed paths since the 2026-08-31 stamp. Overnight drain landings + Epic 28 close-out (all done in ledger):
  - `af315d4bd7` / `86973a5a8d` — Story 28.7 (done, PR #975): `cli/check.py` — CAP-10 index freshness advisory (`MRS-IDXF-*`) wired through `marshal check` and the detector registry; canonical kit paths live in `scripts/index_freshness_check.py`.
  - `951eddb6b7` / `92aa9d4694` / `80b773dc53` — Story 28.4 (done, PR #973): savings telemetry at the loop-runner seam — `supervisor/__main__.py`.
  - `f4dc90b716` — Story 28.8 (done): derived context recomputes on source change — `core/policy.py` composition/render path.
  - `c7a0929ff5` / `2dba3cdf33` — Story 28.15 (done): scope-violation enforcement mode — `core/policy.py`, `cli/dispatch.py`, `core/dispatch.py`.
  - `ebc4915f33` / `6a6f442cab` — Story 22.9 (done): dispatch branch names its station — `core/dispatch.py`.
  - `90c5a28bd6` / `b04a30c6b0` — dispatch autonomy hotfixes (verify sidecar read, Epic 28 artifact catch-up) — `cli/dispatch.py`.
  No CAP text change. RECONCILES: `cli/check.py`, `cli/dispatch.py`, `core/dispatch.py`, `core/policy.py`, `supervisor/__main__.py`. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-token-economy`.
- (change) Surface extension 2026-09-01 (Story 28.7): `scripts/index_freshness_check.py` added to this spec's `surface:` — the runtime detector emitting `MRS-IDXF-*` structured findings consumed by `marshal check` and `scripts/detectors.py`. Closes the ungoverned finding for that script; CAP-10 contract unchanged.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): this spec's memlog HAD moved for the same pass, but did not NAME the 6 governed path(s) below, so spec-surface reported them `drift-presumed` (warn, non-gating). Naming them here closes that. All already landed on `main`; this records the baseline lag, not a re-review. No `SPEC.md` edit. Paths:
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/dispatch.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/harness_profile.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/policy.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/supervise.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/supervisor/__main__.py` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-token-economy`.
- (decision by steward) 2026-09-06 surface widened: + .claude/skills/bmad-build-auto/compile-epic-context.md — Story 28.8's derived-context block lives in this installer-owned file; the 6.12 core apply regenerated it away and only spec-surface-governed files were caught. step-01 stays governed by spec-marshal-single-story-dispatch (no double governance). Scoped re-stamp follows.
- (note by claude) Cross-spec surface touch (Story 31.3, CAP-4 spec-bmad-suite-lifecycle): core/policy.py changed to add a new SEED key, review_min_score (the tea-test-review marshal review lens's --min-score threshold, default 80, the vocabulary's 33rd key) -- unrelated to this spec's own token-economy/context-pipeline concerns; [context] resolution, model-cost-catalog, and scope_violation_mode untouched. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (note by claude) 2026-09-06 (spec-31-4): the sprint-planning trio (`.claude/skills/bmad-sprint-planning/scripts/sprint_plan.py`, `scripts/tests/test_sprint_plan.py`, `references/generate-tracking.md`) was evaluated for this spec's surface during spec-31-4 and assigned to `spec-marshal-single-story-dispatch` instead -- that spec is the ledger-owning one (its CAP-4 triggers `sprint-ledger-sync` on every landing). No surface change here; this spec's `surface:` list and files are otherwise untouched.
- (capability by claude) CAP-18 (new, 2026-09-09, fleet-readiness-decision-batch-2026-09-09 row C5 / mars-B-B1, operator-approved) -- ONE PUBLISHER: marshal publishes run state AND savings telemetry to django_pyforge.supervisor. INTENT: a single supervisor-side publisher hop carries both the bmad-loop/dispatch run state that Unifying CAP-17 (QUALIFIED: run-state-as-a-service on spec-pyforge-unifying-strategy -- NOT this Spec own CAP-17, which is scope_violation_mode) and the Hub Track (hub:CAP-3 on spec-intelligence-hub) require, and this Spec own CAP-7 per-layer savings fields; marshal and doctor stop reading ~/.bmad-loops as the source of run truth. SUCCESS: marshal imports django_pyforge in exactly one publisher module (today it imports it zero times); cli/init.py:331 and pyforge-doctor sources/marshal.py:544 read the published plane instead of Path.home()/".bmad-loops"; the front door shows live run state and the per-story timing survives the workstation; and CAP-7 savings fields carry real numbers, not the four `None` stubs at adapters/harness_bmadloop.py:1880-1898. ONE writer, not one per engine -- the two supervisors (loop and dispatch) both feed this single publisher. Decomposed as marshal Story 33.4; steward Story 49.8 is ledger `blocked` on it.
- (decision by claude) 2026-09-09 (fleet-readiness-decision-batch-2026-09-09 row C5, operator-approved): marshal EPIC 33 "Token economy in effect" is minted by sprint-change-proposal-2026-09-09-token-economy-enablement.md -- ten stories that turn on what Epic 28 built. MEASUREMENT FIRST is preserved as the Dream own gate: Story 33.1 (the on/off benchmark artifact plus the four real savings getters) blocks 33.2 and everything after it. Enablement order: 33.1 measure -> 33.2 enable the layers on dispatch (resolve_wire_wrap folded on factory dispatch) -> 33.3 enable on spin (fold repo defaults) -> 33.4 CAP-18 one publisher -> 33.5 risk-tiered review wiring -> 33.6 adaptive tiering fed on all 8 stations plus floor-raise on dispatch -> 33.7 the two Epic-20 watchdogs re-pointed off ~/.bmad-loops -> 33.8 first live fan-out wave -> 33.9 verify_scope at factory dispatch -> 33.10 the derived CFE pin. The Dream stays `specified` until 33.1 benchmark artifact reports a measured saving on a real story -- nothing else counts.
- (note by claude) 2026-09-09 CAP-AXIS QUALIFICATION, recorded so Epic 33 stories bind to the right capability (fleet-readiness-decision-batch-2026-09-09 row mars-B-B2 / D-2): the Track is hub:CAP-3 (spec-intelligence-hub SPEC.md:61), NOT this Spec CAP-3, which is caveman output-compression seeding via Genesis; only CAP-7 is a correct token-economy citation for savings telemetry; and Unifying CAP-17 (run state as a service) must ALWAYS be written qualified because this Spec has its own unrelated CAP-17 (scope_violation_mode, core/policy.py:601-609). Bare CAP-n inside this Spec means this Spec.
- (event) (self-validate pass 1 -- coherence) 2026-09-09 re-derive: PASS. CAP-18 carries intent+success; the CAP-axis qualification and the Epic 33 enablement order render as Assumptions (notes, not contract clauses); measurement-first renders as a Constraint because it bends the enablement order. 18 CAP ids, none renumbered or retired; status stays ready (no status event in the memlog).
- (event) (self-validate pass 2 -- preservation) 2026-09-09 re-derive: PASS. All three 2026-09-09 entries landed -- CAP-18 in Capabilities, the Epic 33 ten-story order + Dream-stays-specified gate in Assumptions + the measurement-first Constraint, the CAP-axis qualification in Assumptions. No prior Capability, Constraint, Non-goal, companion or surface entry dropped (git diff shows zero removed lines).
- (decision by claude) 2026-09-09 (operator): Epic 33 dispatch sequence 33.1 -> 33.5 -> 33.4 -> 33.2/33.3 -> 33.6/33.7/33.9/33.10 -> 33.8 last, written into implementation-readiness-report-2026-09-09.md; 33.11 (unforgeable attribution, spec-pyforge-marshal F-4) is blocked on its trigger and sits outside the throughput queue. Hypothesis until 33.1 reports a measured baseline.
- (event by claude) 2026-09-10 (spec-28-28/28-29/28-30, the Epic 28 gap-closing pass): three of the four remaining layer stories landed together. Story 28.28 (CAP-5) closed the derived-context gap end-to-end — scribe's `index refresh` gained `--declare <manifest>`, plus a packaging fix (`cocoindex` added to `[feature.pyforge-scribe.dependencies]` in `pixi.toml`, since the lean scribe env never carried it) — live-verified `marshal context refresh` now reports `mode: incremental` with zero findings. Story 28.30 (CAP-3, dispatch half) deploys the caveman skill into dispatch worktrees via a new `cli/dispatch.py::_seed_dispatch_output_layer`, reusing `seed/verbs/kit.py`'s packaged-payload resolution through this spec's own `cli/dispatch.py` surface entry (new `MRS-DISP-042` finding registered in `core/findings.py`/`core/verdict.py`, outside this spec's own surface but load-bearing for the same change). Story 28.29 (CAP-2) named cursor's wire incompatibility explicitly in `core/harness_profile.py::resolve_wire_wrap`'s reason string and annotated `data/harness_profiles/cursor.toml`'s `[wrapper]`-absence with a comment citing the finding — closing this spec's own drift-presumed WARN for that file. `data/harness_profiles/claude.toml`'s own still-open drift-presumed WARN is unrelated to this pass (not touched here). Story 28.31 (structure-graph/codegraph for dispatch) stays `backlog` — a genuine, unresolved provisioning-cost question, not forced through this pass.
- (event by claude) 2026-09-10 (spec-28-32-derived-context-and-output-roll-out-fleet-wide, spec-28-33): rolled `derived-context` + `output` out to all 7 non-marshal stations' `marshal-policy.toml` (mirroring 28.27's own planning-graph rollout), live-verified per station (`mode: incremental`, zero findings; caveman deploys with no finding). Found and fixed a real bug on `seed/detect/kit.py` (this spec's own `seed/**` surface) before rolling out: `_carve_out_state` string-sliced a Python `str` with `RegionSpan.body_span`'s BYTE offsets instead of byte-slicing, desyncing whenever non-ASCII content preceded the caveman carve-out region -- reproduced live against `pyforge-marshal`'s real loop home (`marshal preflight` re-applied the skill and reported a false carve-out-mismatch WARN on EVERY run, never idempotent). Fixed by using the already-correct `detect/hashes.py::region_body_text`; 1 new regression test added (`test_seed_kit.py`), verified to fail pre-fix and pass post-fix. Also closed the mirror-image gap for `structure-graph`'s spin half (28.33): the loop-home `.codegraph/codegraph.db` index has existed since Story 28.3 but was never referenced anywhere -- wired one bullet into `step-01-clarify-and-route.md`'s item 1. Story 28.31 (the DISPATCH-side codegraph cost spike) stays backlog, narrowed to dispatch-only now that the spin half no longer waits on it.
- (event by claude) Surface reconcile 2026-09-10: 2 drift-presumed paths since the 28-28/28-29/28-30 stamp above (`8ec6b89f7a`) -- both landed after it, both live-bug fixes found while resuming a stalled `pyforge-marshal` spin run:
  - `8182841e90` — fix(marshal): `core/harness_profile.py` gains `substitute_wire_port`, called from the Story 33.3 spin-side wire overlay before it writes bmad-loop's adapter profile TOML. Without it, the literal `{wire_port}` template token from `claude.toml`'s `[wrapper].argv` reached headroom unsubstituted, which refused every launch ("Invalid value for '--port'") -- `render_dispatch_argv`, the dispatch-engine counterpart, already substituted this token; the spin path never did. No prior test exercised `attempt_spin_wire_layer` at all; this also closes that coverage gap.
  - `af15fa011a` — chore: `seed/templates/files/model-ignores.gitignore.j2` gains a `.bmad-loop/profiles/` ignore line (Story 33.3's wire-overlay derived artifact, `SPEC-marshal-token-economy` CAP-2) -- untracked and unignored, it made `bmad-loop run` refuse the loop home with "git worktree is not clean."
  No CAP text change. RECONCILES: src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/harness_profile.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/seed/templates/files/model-ignores.gitignore.j2. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-marshal-token-economy.
