---
topic: Marshal token economy -- the loop spends tokens it never had to: brakes exist (ceilings, idle ladder, cache TTL, tiering) but nothing shrinks what an iteration reads and writes
updated: 2026-08-30T11:45
---

- (note) Source: docs/dreams/marshal-token-economy.md (captured 2026-08-30). Governs the marshal loop context pipeline: core/policy.py (EffectivePolicy + render), core/harness_profile.py + data/harness_profiles/*.toml, seed/ (Genesis), supervisor/ + core/supervise.py (telemetry, ladder), cli/check.py (detector front door).
- (note) Ground truth from repo exploration 2026-08-30: marshal token posture is brakes only -- E3.6 ceilings (50M/500M weighted), idle ladder, NFR-14 prompt-cache TTL discipline (poll <=60s, cache_read_weight=0.1), FR-51 model tiering (wired, rarely fed), E22 dispatch. Nothing compresses/indexes/graph-serves context anywhere in marshal, bmad-loop, or bmad-build-auto.
- (note) Per-iteration spend map: ~23k tokens identical always-on docs (CLAUDE.md ~10k + AGENTS.md ~3.6k + project-context.md ~9k) per fresh session; story spec + epic-context + continuity 5-16k; unbounded mid-story tool output (test logs 80-95 pct noise); full codebase re-exploration per session; 4 fresh reviewer lenses multiply it; agent output weighted heaviest.
- (note) Instruments already packaged, none wired: recipes/headroom-ai (0.32.1 Apache-2.0, lib+proxy+MCP+agent-wrap, reversible CCR, live-zone-only so provider cache hot zone untouched), recipes/caveman (2.4.0 MIT skill installer only, ~65 pct output cut; input proxy @caveman-ai/cli is BSL-1.1, NOT packaged), recipes/codegraph (1.6.0 MIT, local pre-indexed code KG, linux-64 only in pixi), recipes/cocoindex (1.0.20 Apache-2.0, on conda-forge, incremental derived indexes), recipes/graphifyy (0.9.44 MIT, folder-to-KG; Scribe plans GraphStore seam). headroom-ai + caveman commented-out in pixi.toml, root-caused blocked in docs/dreams/pixi-candidate-currency.md (conda-recipe-manager==8.2.1 pin chain).
- (capability) CAP-1 declared context pipeline: a [context] policy block in EffectivePolicy, rendered into every policy.toml/launch surface identically for BOTH engines (bmad-loop spin and bmad-build-auto dispatch) -- which layers are on, aggressiveness, store paths. Success: rendered output carries the block from one composition site; a run on either engine resolves the same declaration; absent block means every layer off (today behavior byte-identical).
- (capability) CAP-2 wire compression at the harness seam: when policy enables it, sessions launch through the compression layer (agent wrap/proxy) so tool outputs, logs, file reads compress before the provider call; compressed originals land in a loop-home-scoped CCR store the agent can retrieve from. Success: launched command demonstrably wrapped; a compressed artifact is retrievable byte-exact; prompt prefix (system prompt, tool defs, older turns) byte-identical wrapped vs unwrapped.
- (capability) CAP-3 output-compression seeding: Genesis deploys the caveman skill per loop home so dev-session speech compresses ~65 pct, while review verdicts, journals, escalation context stay fully articulated. Success: marshal seed check verifies deployment; a landed story verdict + journal read as normal prose.
- (capability) CAP-4 structure from the graph: loop-home provisioning builds/syncs the codegraph index and wires the agent integration so structure questions resolve from the graph instead of file re-reads. Success: seed check proves index present + fresh at admission; a session answers a structure query without re-reading the files it names.
- (capability) CAP-5 incremental derived context: epic-context / continuity distills become incrementally-maintained derived artifacts (cocoindex) recomputed only when their planning sources change. Success: unchanged sources yield zero recompute across two consecutive iterations; a source edit yields exactly one refresh.
- (capability) CAP-6 planning-graph retrieval: story routing retrieves the scoped planning context a story binds to (graph query via the Scribe-owned GraphStore seam over graphifyy) instead of loading epics.md/prd.md wholesale; degrades to epic-context files when the seam is absent. Success: an epic-path iteration completes within the epic-context token target with zero full-document loads; fallback proven by disabling the seam.
- (capability) CAP-7 savings telemetry: the supervisor journals per-story spend AND per-layer savings (wire-compression stats, output-compression delta, graph-hit vs file-read counts); marshal status renders both mid-run. Success: journal entries carry savings fields; status shows them while a run is live (chips at DW-FU-3-6-6 mid-session blindness).
- (capability) CAP-8 graduated compression ladder: as a story approaches its token ceiling the supervisor raises compression aggressiveness (and may lower the model floor via existing FR-51 tiering) BEFORE the stop-retry-defer ladder fires. Success: a test proves ladder ordering -- compression escalation strictly precedes kill -- and that no gate or reviewer is ever skipped by escalation.
- (capability) CAP-9 measured baseline: a pinned, re-runnable benchmark (same story, layers on vs off) reports before/after weighted tokens per layer. Success: benchmark artifact reproducibly emits the comparison; ceiling recalibration cites it.
- (capability) CAP-10 index freshness is an admission signal: marshal check gains advisory codegraph/cocoindex staleness findings. Success: a stale index yields a named finding; exit-code domain {0,1,2,3,4,130} unchanged; finding never blocks a run by itself.
- (constraint) Never compress the contract: story specs, acceptance criteria, gate verdicts, escalation context cross the wire intact -- compression applies to what the agent reads along the way and says, never what it is bound by.
- (constraint) Never break the provider prompt cache (NFR-14): any layer that rewrites the prompt prefix is inadmissible; live-zone-only compression is the admission requirement, proven by prefix byte-comparison.
- (constraint) Reversible or absent: lossy-with-retrieval (CCR) is acceptable; silently-lossy is not -- a compressed FATAL line must be recoverable byte-exact.
- (constraint) BSL boundary: the caveman input proxy (@caveman-ai/cli, BSL-1.1) stays unpackaged and unwired; headroom-ai (Apache-2.0) is the input side.
- (constraint) Telemetry stays advisory: savings numbers inform ceilings and tiering, never a pass/fail verdict -- no second PR gate (Warden doctrine).
- (constraint) Graceful degradation per layer: an unavailable instrument (platform gap, pixi blocker) disables its layer with a named finding, never blocks a run -- headroom-ai/caveman pixi activation is a prerequisite tracked in pixi-candidate-currency, not silently assumed.
- (note) Non-goals (from the Dream): not altering BMAD skill semantics (story contract, gates, review occurrence untouched); not replacing the spend brakes (ceilings/idle/tiering stay); not a second knowledge graph inside marshal (GraphStore is Scribe-owned; marshal consumes); not the pixi unblocking work itself (owned by pixi-candidate-currency); not review-depth scheduling (owned by risk-tiered-review-depth spec).
- (direction) Operator direction 2026-08-30: run bmad-spec on the Dream and prepare everything so marshal factory dispatch/drain (drain_to_zero) can run -- only implementation of the epic + stories remains for the next session. Decomposition goes to pyforge-marshal epics.md as Epic 28 with per-story ready-for-dev specs and ledger backlog entries.
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6+8): clean -- 10 capabilities each with intent+success, constraints all design-bending, 5 non-goals, concrete success signal, IDs unique, lean.
- (event) Self-validate Pass 2 (preservation vs docs/dreams/marshal-token-economy.md): clean -- spend map, 5-instrument matrix + availability gates (to integration-layers.md companion), 7 marshal features (to CAP-1..CAP-10), guardrails (to Constraints), benchmark-first gate (CAP-9), Scribe seam ownership (CAP-6 + non-goal) all landed. Dream open question "where does compression config live across engines" resolved INTO CAP-1 (one composition site, both engines identical). Wrapper-only content dropped: none.
- (event) SPEC.md derived at status: ready + companion integration-layers.md written, 2026-08-30. Decomposition: Epic 28 in epics.md with 9 stories, per-story ready-for-dev specs, ledger backlog entries -- for marshal factory dispatch/drain.
- (direction) Operator direction 2026-08-30 (second pass): analyze whether adaptive-model-tiering or the pyforge-unifying-strategy Canopy work must fold into this spec; combine implementation where it cuts cycles.
- (correction) CAP-8 amended 2026-08-30: the graduated ladder is COMPRESSION-ONLY -- the original "may lower the model floor via FR-51 tiering" clause conflicted with spec-adaptive-model-tiering's shipped constraint (escalation is a floor-raise only, never a downgrade; Stories 3.11-3.13 done, tiering realized). Model movement stays with declared difficulty (static) and Story 3.12's upward-only floor-raise (dynamic). Propagated to SPEC.md CAP-8, epics.md Story 28.6, spec-28-6, and the Dream's ladder bullet.
- (note) Tiering adoption side-effect: Epic 28's nine specs are the fleet's first stories declaring difficulty:. Found + fixed a dispatch-path quirk: resolve_dispatch_model falls back to next(iter(tier_map)) for unmapped difficulties, so difficulty: medium would have inherited the heavy tier (opus dev) on dispatch. marshal-policy.toml now pins [model_tier_map.medium] dev=sonnet review=opus explicitly (spin was unaffected: unmapped -> no override there).
- (decision) CAP-5/CAP-6 binding sharpened 2026-08-30 per unifying-strategy Grounding 2026-08-30 (Scribe three CAP-18 ports: graph_store persist / compile_surface ingest / recall_ranker): the cocoindex engine for CAP-5 is Scribe's compile_surface cocoindex extra (scribe Story 6.2), the graphify ingest for CAP-6 is scribe Story 6.1 -- marshal consumes by scribe grammar only, no marshal-private flow. One binding serves two consumers (foundry-cutover move-list + token-economy layers 3-4). Scribe Epic 6 (Stories 6.1, 6.2) minted this pass in pyforge-scribe's epics.md + ledger (backlog) because the extras had zero stories anywhere.
- (constraint) spec-28-2 gained a Never 2026-08-30: headroom's cross-agent SharedContext memory stays off -- a second memory store-of-record by the back door; Scribe capture/recall is the fleet's only memory face (no mem0.add in place of scribe capture).
- (note) Sequencing ruling from the fold-in analysis 2026-08-30: drain Epic 28 BEFORE the foundry package fold (pyforge-target-monorepo phases) -- the cutover relocates every path Epic 28's code maps and the spec surface globs cite (src/shared/packages/ -> src/packages/, .claude/skills/ -> skills/), and it would invalidate the 28.5 benchmark baseline. Cutover work itself stays out of this spec (different Dream, steward/scribe scope).
- (direction) Operator direction 2026-08-30 (third pass): supplied the live 2026 model/cost catalog across the four active subscriptions (Cursor Ultra $200/mo, Claude Max, Copilot, Gemini API) -- use it to refine the dream/spec/stories for multi-agent multi-model marshal.
- (capability) CAP-11 declared model-cost catalog: policy carries a price table (per provider/model: in/out/cache-read/cache-write per 1M + subscription-pool membership; seed snapshot = companion model-economics.md); telemetry (CAP-7) and benchmark (CAP-9) render dollar estimates alongside weighted tokens; per-provider token weights derive from declared ratios (global constant fallback). Success: dollars present when catalog declared, absent (never fabricated) otherwise; Cursor-pool cache reads weighted at declared 0.25-0.40 not the Anthropic 0.10 constant; no network price fetch.
- (capability) CAP-12 cross-provider tier routing: model_tier_map stage entries can name (harness profile, model) pairs -- easy->economy class, medium->standard, heavy->frontier (ladder is data in model-economics.md) -- preferring subscription-marked pools before metered API; serving pool journaled; exhausted pool falls through, never blocks. Success: rendered-launch diff proves the pair per declared difficulty on both engines; FR-51 seam remains the only selection mechanism; run-level batching unchanged; review floor holds.
- (constraint) Declared prices only: the catalog is hand-refreshed policy data (dated snapshot), never a live price/billing fetch. Dollar figures advisory (extends the telemetry constraint). Review floor holds under routing: cross-provider routing never places review below the policy-declared floor.
- (note) Non-goals added: not a billing integration (no live reconciliation against provider dashboards); not multimodal surfaces (audio/TTS/video/music/robotics/embeddings out of scope).
- (note) Catalog ground truth 2026-08-30: input-rate spread across the ladder ~50x (Flash-Lite/Luna 0.10-0.20 -> Fable 10.00 per 1M), output ~125x. Cache-read/input ratio is 0.10 for Anthropic+OpenAI+Gemini (NFR-14's cache_read_weight=0.1 exactly right there) but 0.25-0.40 for Cursor first-party -- the global constant silently understates that pool.
- (correction) Operator clarification 2026-08-30 (b): Claude Max is the 20x-Pro usage tier -- the largest flat pool in the estate; first preference for claude-harness runs and the reason the claude harness stays the drain default.
- (correction) Operator clarification 2026-08-30: the Copilot subscription is Pro ($100/yr) with a DOWNGRADE PENDING, plus GitHub Pro ($48/yr, repo features not a model pool). Copilot Pro's premium-request quota can drive the copilot harness while it lasts, but routing treats it as a transitional fall-through candidate only, never a preferred pool; post-downgrade it exits the routing table (inline/chat only).
- (event) Second decomposition pass 2026-08-30: Stories 28.10 (CAP-11) and 28.11 (CAP-12) added to epics.md (storyCount 179->181), ready-for-dev specs written, ledger backlog synced. SPEC surface extended with core/dispatch.py + cli/dispatch.py; companions now integration-layers.md + model-economics.md.
