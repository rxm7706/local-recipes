---
topic: Marshal trusts the environment it launches into -- until it silently doesn't (docs/dreams/marshal-launch-environment-integrity.md)
updated: 2026-09-11T13:14
---

- (direction) Retroactive Spec authored 2026-09-11: docs/dreams/marshal-launch-environment-integrity.md's own Realization log claims 'decomposed into marshal Epic 34, fully specced and dispatch-ready' and status: specified, but no spec-marshal-launch-environment-integrity/ folder was ever authored -- dream-chain-check's INV-1 correctly flags this. Epic 34 (Stories 34.1-34.4) is already fully done in the tracked ledger; this Spec documents what shipped, matching sibling shipped specs' own retroactive-documentation convention.
- (capability) CAP-1: factory spin refuses a second launch against a live loop home. Intent: reuse cli/dispatch.py's station_in_flight_conflict check (narrowed call, never re-derived) so a second spin against the same slug refuses before launching, naming the live run's id/pid. Success: a fixture reproducing the exact 2026-09-10 race (two spin calls in rapid succession) asserts the second refuses. (Story 34.1.)
- (capability) CAP-2: a dispatch/spin session's worktree is checkpointed before it can be lost to a crash. Intent: the supervisor's poll loop commits a local-only wip: <story> (auto-checkpoint) commit when uncommitted changes sit past an idle threshold (or on explicit factory checkpoint), never pushed, never opens a PR. Success: a fixture simulating a mid-session crash after the checkpoint fires asserts the worktree's changes survive as a commit. (Story 34.2.)
- (capability) CAP-3: factory drain tells a crashed session apart from a genuinely failed one. Intent: a blocked story with zero git progress and zero review/verify-cycle evidence classifies as environment rather than story; a sanctioned retry path clears only environment-classified blocks, never weakening the existing never-auto-retried guarantee for a genuine failure. Success: a fixture reproducing one of each (crashed-before-progress, real verify failure) asserts only the crashed one is eligible for retry. (Story 34.3.)
- (capability) CAP-4: the ATTENTION-block's own refused-verdict check gets the same test coverage its station_state() sibling has. Intent: a dedicated fixture pins fleet_picture.py's main() ATTENTION-block branch (mocking subprocess.run for marshal status), matching the coverage station_state()'s own fix already has. Success: a station with dispatch_phase set + refused verdict produces the ATTENTION line; the same verdict with dispatch_phase=None (a different engine live) produces none -- the exact regression the 2026-09-10 fix guards against. Test-coverage-only, no behavior change. (Story 34.4.)
- (constraint) Story 34.1 reuses station_in_flight_conflict as a narrowed call, never a re-derived guard (mirrors Epic 22/28's own precedent).
- (constraint) Story 34.2's checkpoint commit is local-only -- never pushed, never opens a PR; landing a story remains a human/operator-triggered act.
- (constraint) Story 34.3 never weakens factory drain's existing never-auto-retried/never-forced-past guarantee for a genuine story failure -- only adds a way to tell an environment crash apart from one.
- (note) Non-goal: not a rewrite of the dispatch/spin split, the Tier-3/ledger two-file design, or promote_sprint_status.py's architecture -- every fix works within the existing shape (those four other findings are closed directly by the Dream's own 2026-09-10 work, not by this Epic).
- (note) Non-goal: not a general crash-recovery framework for every marshal subprocess -- scope is the dev/review session a story's worktree lives in.
- (event) Self-validate pass 1 (coherence): PASS. CAP-1..4 each carry intent+success, WHAT not HOW; 3 constraints each rule something out; 2 non-goals explicit; success signal testable (each CAP cites its own fixture); CAP IDs map 1:1 to Stories 34.1-34.4, already-shipped and ledger-done; prose lean.
- (event) Self-validate pass 2 (preservation): PASS. All four Epic 34 story bodies (34.1-34.4) map to CAP-1..4; the Epic's own HARD boundaries map to the 3 constraints; the Dream's own 2 non-goals map to the 2 non-goals. No wrapper-only drops.
- (event) Spec finalized, status: shipped (retroactive -- all 4 CAPs already verified-done in the tracked ledger). 4 capabilities, 3 constraints, 2 non-goals, 0 open questions.
