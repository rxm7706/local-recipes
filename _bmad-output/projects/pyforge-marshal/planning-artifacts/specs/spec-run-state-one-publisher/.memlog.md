---
topic: One publisher: bmad-loop and dispatch run state leaves the laptop for the host's supervisor store; the host learns to hold a run it does not own; the deployed proof is exercised; no run-state read from a home directory remains in the fleet
updated: 2026-09-12T02:34
---

- (note) owner-dream: docs/dreams/run-state-one-publisher.md (owner: marshal, status: dreamt, seeded 2026-09-12 by operator ruling: gap-closure enters through the Dream-to-Code chain). Mode: headless, express; caller pre-supplied slug run-state-one-publisher and project pyforge-marshal (marker + symlinks verified in sync).
- (decision) BIND, NEVER RE-MINT. The criterion stays on spec-pyforge-unifying-strategy CAP-17 (steward; in-effect story 49.8) and the publisher stays on spec-marshal-token-economy CAP-18 + CAP-7 (marshal; implementation story 33.4). This Spec names only what neither covers. Reason: feedback_check_convergence_before_new_fr — a second publisher capability would be two contracts for one module and a spec-surface collision.
- (decision) Owner marshal by the C8 precedent (fleet-readiness-decision-batch-2026-09-09 § 2.3): the surface that changes most — both supervisors and the publisher — is marshal's. The host-side path is steward's surface and doctor's read is doctor's; each lands only after an incoming surface claim in that station's memlog (doctor already recorded 49.8's on 2026-09-09).
- (decision) Evidence and inventories go to a brownfield.md companion (Spec Law: tables never in the kernel): the 2026-09-12 live-code evidence table and the residual ~/.bmad-loops reader inventory.
- (capability) CAP-1 — The host holds a run it does not own. intent: the supervisor store accepts, keeps alive and finishes a run whose process is external to the platform (a bmad-loop story, a dispatch wave) — publish, heartbeat, complete — without wrapping it in a Celery task. success: an externally published run stays running across its heartbeats and is marked lost only when its heartbeat ages past the bound; sweep_lost_runs never marks a heartbeating external run worker_lost for lacking a Celery task id; the same /runs/ query lists it beside MCP runs. Evidence today: supervisor.py:435 publish_start enqueues a Celery task, supervisor.py:681 sweep judges by live_task_ids.
- (capability) CAP-2 — A published external run is attributable to a verified subject. intent: the out-of-host publisher presents an assertion the supervisor verifies (the CAP-6 shape: signed, audience-bound, short-lived) so every published loop run carries a subject and the run bounds and revocation apply to it. success: a publish without a verifiable assertion is refused and leaves no row; a published loop run carries the operator's idp_subject and roles; revoke --sub selects it; no forwarded header and no shared laptop secret is accepted.
- (capability) CAP-3 — The deployed proof is exercised and recorded. intent: Unifying CAP-17's success criterion is exercised in a deployed, egress-blocked namespace — a live bmad-loop run visible on /runs/ with no operator home mounted, and a completed run's timing queryable after the workstation is gone — and the evidence is written where the realization gate reads it. success: CAP-17's verified: line names the lane, date and run id of that exercise in place of 'deployed egress-blocked proof unexercised'; test_front_door_queries_supervisor.py:235 still passes; Epic 49's last open row (49.8) and marshal 33.4 close on that evidence, and the Dream flips to realized on it.
- (capability) CAP-4 — No run-state read from a home directory remains in the fleet. intent: every ~/.bmad-loops read outside the two stories' Surface lines (steward cli.py and upgrade.py, scribe promote.py, doctor sources/__init__.py story-status) is either moved to the published plane or classified as loop-home FILE access (worktrees, journals) — and a guard fails when a run-state read of the home reappears. success: a fleet-wide grep for .bmad-loops outside test files returns only sites carrying a loop-home-file classification; a new run-state read of the home reds the guard; the classification lives beside each site, not in a document.
- (constraint) Single writer, one store: run_state and mcp_handles are written only through django_pyforge.supervisor (AD-12) — the publisher goes through that API, never raw SQL, never a second table, and no station keeps a private copy of the fact.
- (constraint) External runs stay external: a bmad-loop run is never wrapped in a Celery task to make it publishable. The host learns to hold a run it does not own (CAP-1); it does not take ownership.
- (constraint) The loop home stays: this Spec retires READS of run state from ~/.bmad-loops. bmad-loop's own worktrees, state.json and journal.jsonl keep landing there; BMAD_LOOP_HOME_ROOT remains the override for that purpose.
- (constraint) Identity is carried, never trusted: the publisher presents an assertion the host verifies, or a dated, recorded exception in this memlog. A forwarded header or a laptop shared secret is not an identity (RFC-3, CAP-6).
- (constraint) Cross-station by construction: django-pyforge/** is steward's surface, sources/** is doctor's, promote.py is scribe's. A story touching any of them records an incoming surface claim in that station's memlog BEFORE code lands, or spec-surface-check reds the merge. This Spec's own surface: claim covers marshal paths only.
- (constraint) Realized on effect, never on ledger: the Dream flips to realized only on CAP-3's recorded exercise. Two ledger rows reading done is not the signal.
- (constraint) Vessels stay where they are: marshal 33.4 and steward 49.8 remain the joint-landing stories for the publisher and the criterion; this Spec's stories carry only CAP-1..4's residue and dispatch beside them, never instead of them.
- (assumption) The /runs/ board needs no change beyond what CAP-18 delivers — it already reads RunState (front_door/views.py:53 runs_board) and already refuses laptop state (tests :235).
- (assumption) The heartbeat bound for an external run reuses station_time_limit unless a story finds the loop's cadence needs its own bound — the sweep already measures from heartbeat_at.
- (assumption) Steward accepts the CAP-1/CAP-2 host changes under 49.8's own Surface line, which already names django-pyforge/**; no new steward story is minted here.
- (question) OQ-1 (CAP-2): which credential path does an out-of-host publisher use — the operator's IdP bearer exchanged at the host for a delegated assertion (Story 40.1's verify-then-mint), or a service credential referenced from the environment? Neither the Dream nor 33.4 decides it.
- (question) OQ-2 (CAP-1): does the publisher reach the store in-process (import django_pyforge + DATABASE_URL, as 33.4's text reads) or over the host's HTTP/MCP face? In-process on a laptop needs DB credentials in the operator's environment, which strains the secrets-by-reference rule; a host face keeps the DB behind the identity boundary.
- (question) OQ-3 (CAP-3): which lane exercises the deployed proof — platform-ci-local, attended CRC, or the Enterprise Managed OCP namespace? 49.3 used a lane for its disconnect proof; reuse it or name another.
- (question) OQ-4 (CAP-4): are steward cli.py's and upgrade.py's reads run-state reads or loop-home file reads? Classification is the first story's job; the answer decides whether they move.
- (event) drafted 2026-09-12 from docs/dreams/run-state-one-publisher.md; SPEC.md and brownfield.md to be derived from this memlog (bmad-spec headless, create, express).
- (event) SPEC.md + brownfield.md derived from this memlog 2026-09-12 (bmad-spec headless, create, express). Self-validate pass 1 coherence PASS: CAP-1..4 each carry intent+success; intents say WHAT (the Celery clause is the bounding constraint, not a how); 7 constraints each rule something out; 5 non-goals; success signal is a demonstrable moment; IDs unique; lean prose.
- (event) Self-validate pass 2 preservation PASS: every Dream claim landed — The Dream section → Why + constraints; the Why-now table → brownfield.md evidence table; the seven 'when real' bullets → CAP-1 (heartbeat sweep), CAP-3 (board, timing, verified line), CAP-4 (readers moved) with the one-writer/savings bullets bound to CAP-18/CAP-7 via non-goals; Constraints → Constraints; Kinships → the binding blockquote. Wrapper-only content dropped: the Dream's Realization log (process record, not contract). Inventory rendered with a kind column after the first render counted comments and help strings as reads.
- (direction) Operator 2026-09-12: accepted all four OQ recommendations as researched (four parallel read-only passes over pyforge code, PRD, architecture and Specs, spot-checked); append them here and re-derive the Spec to ready.
- (decision) OQ-1 RESOLVED — credential path is the operator's IdP bearer exchanged ONCE at the host for a delegated assertion (verify-then-mint: POST /assertion/mint/ in django_pyforge assertion/views.py:33-76 verifies JWKS/iss/aud/exp then mint_assertion signs RS256 with the host-held PYFORGE_ASSERTION_PRIVATE_KEY; client half is pyforge.core.assertion.HostMintClient, zero callers today). The assertion is consumed at publish; the lifecycle (heartbeat, complete) is by the subject-bound McpHandle with a sliding expiry, so the hours-long sidecar holds no identity. Rejected: private key by reference to the publisher (fails RFC-3 — the laptop becomes the minter and delegated_by=pyforge-host becomes false); Keycloak service-account client (sub is the machine, fails CAP-2, revoke --sub goes fleet-wide).
- (decision) OQ-1 mechanics: bearer reaches the publisher BY REFERENCE via PYFORGE_IDP_BEARER_FILE (0600, never the value in env or journal; add the shape to core/egress.py scrubbing). Local/no-IdP profile: config.local_dev.mint writes it and the JWKS is the dev file — byte-identical path. Revocation: in-flight via steward revoke --sub cancelling the row (next heartbeat gets a terminal answer, the sidecar journals revoked per AD-9, never crashes); future runs via IdP role removal (next mint 403). Prerequisites found missing: no pyforge:station:marshal group exists in the realm or dev personas (roles.py:41 prefix only, so a marshal mint is 403 today), and no CLI login story exists anywhere in the fleet.
- (capability) CAP-5 — The operator's bearer reaches the publisher by reference, and marshal is a station the host will mint for. intent: an operator obtains an IdP bearer for the CLI without pasting a secret (pyforge login — device-code or PKCE-loopback), it lands in the referenced bearer file, and the pyforge:station:marshal role exists in the realm and the dev personas. success: pyforge login writes the bearer file 0600 and nothing else; POST /assertion/mint/ for station marshal returns 200 for a role-holding operator in both the local and the deployed profile; no token value appears in any env dump or journal line; removing the role at the IdP makes the next mint 403.
- (decision) Unattended re-spins (dispatch_fleet_supervisor re-spinning hours later) cannot mint without CAP-5; until CAP-5 lands they are the dated exception this Spec permits (recorded here 2026-09-12), scrubbed and journaled, never a shared secret.
- (decision) OQ-2 RESOLVED — the publisher reaches the store over the HOST FACE, never in-process. Evidence: marshal's pixi feature carries no django/django-pyforge/psycopg/celery (pixi.toml pyforge-marshal deps); the platform image cannot run marshal's git orchestration (Containerfile:18-21, 210-217) so both supervisors always run on the operator workstation; Postgres ingress NetworkPolicy admits only pods carrying DATABASE_URL; an in-process importer would put the app-role DSN, Redis URLs, Django settings and the assertion key on every laptop (breaks CAP-12 secrets-by-reference and extends the DML role's reach). Precedent for the shape: pyforge.core.client.PyForgeStationClient carries the assertion as Bearer (already a marshal run-dep); Story 43.3's STATION_REMOTE switch picks in-process vs HTTP behind a port.
- (decision) OQ-2 shape (marshal side, 33.4 surface): ports/publisher.py RunPublisherPort (publish, heartbeat, complete); adapters/publisher_host.py — THE one publisher module, PyForgeStationClient(station=marshal) + HostMintClient re-minting inside the 300 s TTL, failures become journal findings never loop stops (mirrors _publish_run_started_event's best-effort at supervisor.py:377-434); core/publish.py pure record shaping incl. CAP-7 savings fields (AD-4); both supervisor/__main__.py and dispatch_supervisor/__main__.py call the adapter (AD-9 forbids only cli). Host side (49.8 surface, django_pyforge.supervisor): publish_held_run(station, assertion, payload) — same bounds and handle path as publish_start, celery_task_id empty, no enqueue; heartbeat_run(handle); complete_held_run(handle, status, result); sweep_lost_runs judges empty-celery_task_id rows by heartbeat age only, reason heartbeat_lost (not worker_lost). Exposed as MCP tools publish_loop_run / heartbeat_loop_run / complete_loop_run on /stations/marshal/mcp so verification, rate limiting and the mcp_http sidecar proxy apply unchanged.
- (decision) OQ-2 consequence — sibling rewording is OWED and named here verbatim. Marshal Story 33.4 Surface: 'the single publisher module (adapters/publisher_host.py), reaching django_pyforge.supervisor through the host's /stations/marshal/mcp face'; 33.4 Then and token-economy CAP-18 success: 'grep -r django_pyforge src/shared/packages/pyforge-marshal/src/ returns NOTHING, and a meta-test proves exactly one module imports pyforge.core.client for publishing'. Those edits land through spec-marshal-token-economy's own memlog and marshal epics.md before 33.4 dispatches; steward 49.8 needs no wording change. Until they land, this Spec's CAP-1/CAP-2 text is the tie-breaker for the joint landing.
- (decision) OQ-2 risks bound as contract: (1) DEFAULT_MAX_RUNNING_PER_SUB = 5 (supervisor.py:101) would refuse a fan-out wave (33.8) under one operator subject — the held-run path takes a per-tool override; (2) station_time_limit for marshal is 300 s (queues.py:174) so a lost host marks a live loop heartbeat_lost, and terminal rows are never rewritten (complete_run is terminal-wins) — the publisher opens a NEW attempt row linked by harness_run_id, never rewrites; (3) the IdP bearer lives on the laptop for the run's life — 33.11 (unforgeable attribution) stays behind 33.4 on its own trigger.
- (decision) OQ-3 RESOLVED — two tiers, the estate's own precedent (CAP-9's 'documentary only, not an automated CI gate'; Story 12.9 vs 12.7; 48.3's pytest + on-CRC pair). No Epic 49 story used a cluster: 49.3/49.5/49.14 proved their clause with src/platform tests against real services, 49.4/49.7 by honest criterion rewrite. The Enterprise Managed OCP namespace has no access record anywhere in the repo (no kubeconfig, namespace or oc login), so binding to it would leave 49.8/33.4 blocked indefinitely — recorded as NOT exercised. ocp-portability-smoke stays out of the claim until CRC_PULL_SECRET and Actions minutes exist.
- (decision) OQ-3 mechanism tier (gating, repeatable, platform-ci-local --test): three additions to src/platform/tests — (a) publisher writes a run, the process exits, /runs/ shows it and completed_at/duration_ms persist across a fresh connection; (b) a socket guard on the render path (the CAP-13 pattern at test_openfeature_file_flags.py:242-254: monkeypatch socket.create_connection to pytest.fail); (c) a chart invariant that no template declares a hostPath volume (nothing asserts this today — test_chart_invariants.py has no hostPath check). The existing no-laptop-state scrapes (test_front_door_queries_supervisor.py:235, test_supervisor_tables.py:205-238) stay.
- (decision) OQ-3 criterion tier (documentary, one attended CRC exercise): helm upgrade with Story 48.3's default-deny + egress NetworkPolicies active (this doubles as the envelope's first live proof — its DNS selector still targets kube-system/kube-dns while OCP uses openshift-dns, a 48.3 deferral that this exercise resolves or breaks); drive a real bmad-loop story from the workstation through the CAP-2/CAP-5 path; observe it live on /runs/ through the Route; complete it; tear down the laptop session; re-curl /runs/; oc exec web -- curl https://pypi.org must fail; oc get pods -o yaml hostPath count = 0. Recorded as spec-run-state-one-publisher/verification-<date>.md in Story 12.7's shape (cluster, run id, commands without secrets, pass/fail table, 'what is not claimed').
- (decision) OQ-3 verified: wording for Unifying CAP-17 (CAP-9's grammar): 'front-door /runs/ from RunState + no home-dir literals live in test_front_door_queries_supervisor.py:<lines>; publish→exit→timing-survives + egress guard + no-hostPath invariant live in <tests>:<lines> (platform-ci-local --test); deployed egress-blocked exercise: attended CRC <date>, run <run_id> — live on /runs/ via Route, timing present after workstation teardown, default-deny NetworkPolicy enforced, zero hostPath (record <file>) — documentary only, not an automated CI gate. Enterprise Managed OCP namespace: not exercised (Epic 51 target).' Steward 49.8 OWNS this rewrite as an explicit acceptance criterion — 49.5 and 49.6 never rewrote CAP-11/CAP-12's lines (SPEC.md:371-373 and :386-389 still read unexercised / test hooks only), the exact stale shape 49.2's detector exists to catch.
- (decision) OQ-4 RESOLVED — steward's two reads are LOOP-HOME FILE reads and STAY, tagged: upgrade.py:3222 _enumerate_loop_home_relays byte-compares each home's .bmad-loop/bmad_loop_hook.py against the packaged relay (core-upgrade CAP-4, trap 5); upgrade.py:4025 build_prove_landed_report lists homes and shells bmad-loop init + validate over install files, policy.toml, adapter profiles and worktree git state, never state.json or journal.jsonl (core-upgrade CAP-5, trap 7). No RunState field could replace either. cli.py:772/798/830 are the --loops-home help strings for those two sites (not reads); scribe promote.py:124-125 is a docstring about Claude Code's path encoding (not a read). Optional tidy: default --loops-home through BMAD_LOOP_HOME_ROOT like init.py:336 and django_pyforge assertion/client.py:155.
- (decision) OQ-4 doctor: sources/marshal.py:544 gather_story_status IS a RUN-STATE read — it globs runs/*/state.json for tasks[key].phase in NOT_LANDED = {deferred, escalated, abandoned} (marshal.py:84) and tasks[key].commit_sha, most-advanced across runs. RunState.status cannot express those phases and carries no commit, so 49.8's 'doctor no longer reads ~/.bmad-loops' is achievable only when the publisher carries per-story phase + commit_sha + run_id in the run's result payload (keyed station slug + story key) and doctor gets a read seam that is not import django_pyforge (doctor's env lacks it; doctor speaks grammar/MCP) with the same CI degrade (no DB in CI must WARN/OK exactly as the gitignored Tier-3 feeds do today). Until that payload exists the read stays, tagged as CAP-17 debt, and the sources/__init__.py:223 comment is reworded per 49.8's And-clause (it still says preserve, don't redesign).
- (decision) OQ-4 widened inventory — the literal grep missed run-state reads reached through _loop_home_root(): marshal cli/spin.py:395 globs .bmad-loop/runs/*/state.json for tasks[*].attempt/phase (RUN-STATE); cli/status.py _gather_home_facts / _latest_run_dir read journals (RUN-STATE; the unknown-from-landing-journal defect, FR-196); supervisor/__main__.py:2729 appends and reads the run journal (RUN-STATE, writer side). These belong to 33.4's inventory. CAP-4's guard matches .bmad-loop/runs, state.json and journal.jsonl as well as the .bmad-loops literal, in non-test code, kind-aware (comments, docstrings, argparse help never trip it), allow-listing only the publisher module and the tagged LOOP-HOME sites. The pattern to copy is django_pyforge assertion/client.py:120-166 list_loop_homes: BMAD_LOOP_HOME_ROOT only, no Path.home() fallback, test-guarded.
- (constraint) The publisher never imports django_pyforge (supersedes the in-process reading of 33.4): marshal's one publisher module imports pyforge.core.client and pyforge.core.assertion, reaches the store over /stations/marshal/mcp, and a meta-test proves it is the only module that does. In-process publishing is permitted only if marshal ever runs inside the platform image, as a second adapter behind the same port.
- (constraint) Held rows are a distinct shape in the store: empty celery_task_id, judged by heartbeat age with reason heartbeat_lost, never by Celery inspect; a terminal row is never rewritten; a per-tool run-bound override exists for loop runs so fan-out waves are not refused by the per-subject default.
- (constraint) The deployed proof is attended CRC, recorded once, documentary: the automated tier gates; the CRC record is evidence, not a CI gate; the Enterprise Managed OCP namespace is written as not exercised until an access record exists.
- (assumption) CRC stays reachable to the operator as it was for Story 12.7 (CRC 2.63.0 / OCP 4.22.7 on an Ubuntu host, unsupported but working per spec-12-1 verification 2026-08-25).
- (assumption) The realm and dev-persona change adding pyforge:station:marshal is small enough to ride CAP-5's story rather than a steward story of its own; steward records the incoming surface claim.
- (event) All four open questions resolved 2026-09-12; open_questions[] is empty. SPEC.md and brownfield.md to be re-derived from this memlog at status ready.
- (event) SPEC.md re-derived to status ready 2026-09-12 (bmad-spec headless, update); stack.md added as a spec-authored companion (mechanism: modules, host API, credential flow, proof tiers, verified wording, sibling rewording); brownfield.md re-rendered with the OQ-4 classifications and the three run-state reads the literal grep misses. Self-validate pass 1 coherence PASS: CAP-1..5 each carry intent+success (CAP-5 is the next unused id; none reused); intents stay WHAT — module names, endpoints, tool names and test lists moved to stack.md; 11 constraints each rule something out; 6 non-goals; success signal unchanged and demonstrable; open_questions empty.
- (event) Self-validate pass 2 preservation PASS over the 22 entries appended today: OQ-1 → CAP-2, CAP-5, the identity and secrets-by-reference constraints, the unattended-re-spin exception, stack.md § Credential flow; OQ-2 → the never-imports and held-rows constraints, CAP-1's heartbeat_lost / new-attempt / fan-out clauses, stack.md § Marshal side, § Host side, § Sibling rewording (verbatim); OQ-3 → CAP-3's two tiers, the attended-CRC constraint, the ocp-smoke non-goal, stack.md § Proof tiers incl. the verified: wording; OQ-4 → CAP-4's guard clauses, brownfield.md inventory + missed reads, stack.md § The guard; the two new assumptions → Assumptions. Wrapper-only content dropped: the operator-acceptance direction entry (process record).
