---
topic: The dashboard derives project paths — slug != directory stops being a bug factory
updated: 2026-08-22T05:39
---

- (provenance) THIS RECORD STARTS HERE. Created 2026-08-09 by marshal S-13.5 (spec-surface-drift-reconciliation CAP-5 / FR-168): this Spec shipped declaring a `surface:` with no .memlog.md, so spec_surface_check's contract_hash() returned "" and the contract could never move — every governed change would have reported a hard [drift] whose printed remedy ("reconcile the spec") was unreachable, there being no contract to move. Nothing that happened before this entry was captured; do not read the absence of earlier entries as an absence of decisions. The baseline was scoped-stamped in the SAME change that created this file, because a new memlog moves the contract hash off "" and would otherwise downgrade this surface's next real drift from gating [drift] to informational [drift-presumed].
- (note) Governs docs/dashboard/generate.py and docs/dashboard/index.html (2 tracked files).
- (capability) CAP-1 one resolver, one override table — a single Python-side function maps dashboard key -> project directory, with every divergence declared in one table instead of re-derived at each call site.
- (capability) CAP-2 PROJECT_SOURCES is derived, not declared — the project -> sprint-status path map comes from discovery rather than a hardcoded dict carrying its own TODO.
- (capability) CAP-3 resolution ships in data.js; the JS never re-derives — index.html stops re-deriving paths (including the literal `r.slug === "pyforge-genesis"` special case).
- (capability) CAP-4 an unresolvable slug fails loud — the existing detectors catch it rather than a silent wrong path.
- (note) Both known live bugs (found 2026-08-02: real merged work reading "nothing landed", and dead links) came from the same "slug == project dir" assumption, patched per-site each time. The Spec's code facts were verified against generate.py on 2026-08-08.
- (question) OPEN, carried from the Spec: discovery basis under a CI checkout (sprint-status.yaml is Tier-3 gitignored, so scanning implementation-artifacts finds nothing); whether the regen line's _DERIVE_EXCLUDE special becomes a field on the same override table; whether _discover_loop_homes()'s independent slug normalization unifies onto the same resolver.
- (change) S-5.7 SHIPPED, 2026-08-09 (FR-179). Reconciles by name: docs/dashboard/generate.py (build_fleet_progress, wired after apply_line_state) and docs/dashboard/index.html (the 'Fleet picture · tracked' section). Per-station done/stories/blocked/epicsDone/epics plus a PyForge roll-up, counted through the SAME parse_sprint_status the deploy already uses — never a second parser, and never the gitignored Tier-3 feed CI cannot read.
- (decision) BLOCKED is counted separately because the board's own story states are done/active/pending and structurally cannot distinguish a blocked story from an unstarted one. Measured 2026-08-09: 6 blocked rendered identically to 119 pending. Counting from the tracked ledger is what makes the distinction real.
- (decision) NO LIVE FIELD IS PUBLISHED. Run state, projection and the ATTENTION block derive from marshal status, which reads tmux and ~/.bmad-loops; CI has neither, so publishing them would publish a number the deploy cannot measure. Hence the section is named 'Fleet picture · tracked' — kin to `pixi run -e local-recipes fleet-picture`, with the limitation in the name rather than in a footnote nobody reads. Identical names would have promised a parity that does not exist.
- (change) S-5.7 AMENDED, 2026-08-09 — the LOCAL board now matches the CLI. Reconciles by name: docs/dashboard/generate.py (`enrich_fleet_progress_live`, called only on the non-git source path) and docs/dashboard/index.html (the section reads "Fleet picture · live" locally, "· tracked" on Pages, and shows `→ projected` plus a RUNNING state only when the live block exists).
- (correction) The operator was right and the first cut was under-built: the LOCAL board is generated on the machine that has tmux and ~/.bmad-loops, so it CAN match `fleet-picture` completely — only the PAGES deploy is limited. The live fields were therefore added to the local branch, exactly where `apply_loop_inflight` already lives, and their ABSENCE is what makes a Pages render honest rather than stale.
- (correction) The first live-detection used `_live_loop_sessions()` (tmux) and was WRONG BOTH WAYS: it reported marshal running because a tmux session lingered hours after its engine died, and missed steward and mason entirely. A multiplexer session is not an engine. Replaced with `marshal status --format json` — the SAME authority scripts/fleet_picture.py reads — so the board and the CLI cannot disagree about who is running. Caught by checking `ps` against the rendered output rather than trusting the first green.
- (change) RECONCILES `docs/dashboard/generate.py` (2026-08-09). Doctor Story 6-8 gave the Guild vocabulary ONE home: `DREAM_STATUSES` and `DREAM_TYPES` were RE-DECLARED here, identical to `scripts/bmad_drift_check.py` by luck rather than construction; `GUILD_DREAMS` and `STATIONS` were imported FROM that detector, which was the right fix for the 2026-07-28 drift but made the board depend on a detector for constitutive data — and Story 6-9 retires that file into Doctor's package, where this environment cannot import it. All four now read `docs/governance/guild-roster.json`. Values verified byte-identical to the pre-extraction tuples, and the AST sweep the 6-8 spec demands reports an EMPTY intersection of module-level constants between the two files. No behavioural change to this Spec's own surface: `dashboard-gen` runs clean and `dashboard-check` renders (9 lines, 58 dreams, 60 chains).
- (note) Co-governed path touched (2026-08-11): docs/dashboard/index.html — CSS-only change adding default .pdot/.pbar-fill backgrounds so stations outside the hardcoded {warden,atlas} class map render their dot and progress fill (full rationale in spec-factory-console's memlog, the owning surface for console presentation). NOTHING in this spec's own concern — slug->path derivation, PROJECT_SOURCES, the retired-slug special case — was altered; that work is marshal S-16.1 (FR-140..143), still backlog. Named here because this surface governs the same file and a stamp without a record is the laundering S-13.2 exists to end
- (note) Co-governed path touched (2026-08-11): docs/dashboard/generate.py — scan_backlog's satellite-build-line derivation only (mirrors chain-completeness INV-A; BACKLOG 5 open -> 0, every row had a real epic and a running station). Full rationale in spec-factory-console's memlog. NOTHING in this spec's own concern was altered. Named before the scoped stamp per S-13.2
- (change) S-16.1 SHIPPED, 2026-08-15 (FR-140..143). Reconciles by name: docs/dashboard/generate.py and docs/dashboard/index.html — this time IS this spec's own concern, not a co-governed pass-through. Delivers all four capabilities: CAP-1 a single `resolve_project(slug) -> ProjectResolution` plus one `_PROJECT_OVERRIDES` table (dissolved/absorbed entries only: pyforge-genesis/presenton-pixi-image/wasm-analytics-stack/unity-data-stack) replaces `_KEY_SLUG_OVERRIDE`'s five call sites, `CAMPAIGN_PROJECT_OVERRIDE`, `IMPL_CAMPAIGN`'s ad hoc `epics_path` entries, and `IMPL_CAMPAIGN_LEDGER`. CAP-2 `PROJECT_SOURCES` is now a dict comprehension over `resolve_project()` keyed by `_discovered_board_keys()` (the same tracked-`epics.md` glob `scan_projects()` uses) — the `TODO` at the old generate.py:44 is gone. CAP-3 every planning/build-campaign row now carries resolver-computed `epics_path`/`redirect` fields (previously only 3 of 9 IMPL_CAMPAIGN rows had `epics_path`); `index.html` lost both slug->path special cases (`r.slug === "pyforge-genesis"` and `"epics_path" in r`) and reads the shipped fields directly. CAP-4 `resolve_project()` raises `UnresolvableProjectError` for a slug with no override and no matching `_bmad-output/projects/<dir>`; `main()` resolves every PROJECT_SOURCES/CAMPAIGN_ROSTER/IMPL_CAMPAIGN slug up front via `_resolve_roster_slugs()`, prints a `[resolve] N slug(s) resolved (M via override, K default)` summary, and a failure there now exits generate.py non-zero naming the slug instead of shipping a silently wrong path. All three of the Spec's open_questions are answered by the shipped code: discovery is the tracked-epics.md glob (never the gitignored Tier-3 sprint-status.yaml); the retired `regen`/`_DERIVE_EXCLUDE` special stays a separate, still-empty mechanism (kept only for pyforge-doctor's attribute-shape pin, per the story's own Design Notes judgment call); `_discover_loop_homes()` stays independent (different resolution domain, outside the repo). `pyforge-doctor`'s `test_real_generate_py_loads_and_exposes_the_expected_attribute_surface` pin (isinstance-only, no exact-value assertion) verified still green against the derived attributes. `dashboard-gen`/`dashboard-check` both verified clean; `data.js` diffed clean of everything except the new resolved fields (expected per CAP-3 — every row, not just genesis's) plus ordinary live-state noise from concurrently-running stations. `status:` deliberately LEFT at `draft` in this pass rather than flipped to `shipped`: flipping it drops this spec out of `scan_readiness()`'s open-specs panel, a rendered-content change the story's own Boundaries reserve for a human/closeout decision, not a spec-surface-reconciliation side effect.
- (change) S-16.1 review-pass patches, 2026-08-15. Blind Hunter + Edge Case Hunter adversarial review (no shared context) surfaced 5 real, change-caused gaps in the shipped resolver, all auto-fixed same pass: (1) the three absorbed-satellite `_PROJECT_OVERRIDES` entries now set `"sprint_status_path": None` explicitly — previously it silently defaulted to the OWNING project's own real sprint-status.yaml path (e.g. presenton-pixi-image resolved to mason's own file), a plausible-but-wrong value untested by the original suite; (2) a new `_validate_project_overrides()` (run once at import, also directly unit-tested) rejects a future override entry missing `project_dir`, carrying an unknown key, or setting `project_dir: None` without a `redirect` — closes a class of silent-misauthoring bugs the one-table consolidation was supposed to prevent; (3) `_discovered_board_keys()` now raises on two directories stripping to the same bare board key instead of letting the `PROJECT_SOURCES` dict comprehension silently keep only the later one and drop a station's board line with no warning; (4) documented (not code-changed, since fixing it would require speculative round-trip repair for a state the Charter's Identity & Vocabulary section already forbids) that `PROJECT_SOURCES`'s import-time construction can raise `UnresolvableProjectError` before `main()`'s own try/except runs if a discovered directory ever violated the constitutive `pyforge-<key>` naming rule — still a loud, non-zero-exit, slug-naming failure per FR-143, just via an uncaught traceback rather than the polished `[resolve] FAIL` message; (5) dropped the dead `rel` loop variable in `scan_readiness()`. Two findings triaged `defer` (not caused by this story): `pyforge-doctor`'s `board.py` independently re-derives a project directory via its own `_KEY_SLUG_OVERRIDE` fallback rather than calling `resolve_project()` — agrees today only because that dict is empty on both sides, a pre-existing cross-package gap out of this story's surface; and this Spec's own Constraints section still names the retired `epics-genesis-installer.md` example (the slug was retired 2026-08-08, before this story), stale prose this story didn't touch. Three findings rejected as noise (a defensive-but-currently-unreachable `try/except` left as valid style; a literal reading of "byte-identical" against a JSON data artifact when the actual constraint — rendered/visible board content — was verified unchanged; and `data.js` naturally carrying concurrent live-fleet-state noise alongside the schema change, which is how a live-regenerating dashboard tool is designed to behave). Re-verified after patches: `dashboard-gen`/`dashboard-check` clean, 18/18 dashboard tests + 26/26 doctor board-drift tests green.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
