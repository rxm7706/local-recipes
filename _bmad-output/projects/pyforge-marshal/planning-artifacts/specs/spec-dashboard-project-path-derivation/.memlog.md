---
topic: The dashboard derives project paths — slug != directory stops being a bug factory
updated: 2026-08-09T10:15
---

- (provenance) THIS RECORD STARTS HERE. Created 2026-08-09 by marshal S-13.5 (spec-surface-drift-reconciliation CAP-5 / FR-168): this Spec shipped declaring a `surface:` with no .memlog.md, so spec_surface_check's contract_hash() returned "" and the contract could never move — every governed change would have reported a hard [drift] whose printed remedy ("reconcile the spec") was unreachable, there being no contract to move. Nothing that happened before this entry was captured; do not read the absence of earlier entries as an absence of decisions. The baseline was scoped-stamped in the SAME change that created this file, because a new memlog moves the contract hash off "" and would otherwise downgrade this surface's next real drift from gating [drift] to informational [drift-presumed].
- (note) Governs docs/dashboard/generate.py and docs/dashboard/index.html (2 tracked files).
- (capability) CAP-1 one resolver, one override table — a single Python-side function maps dashboard key -> project directory, with every divergence declared in one table instead of re-derived at each call site.
- (capability) CAP-2 PROJECT_SOURCES is derived, not declared — the project -> sprint-status path map comes from discovery rather than a hardcoded dict carrying its own TODO.
- (capability) CAP-3 resolution ships in data.js; the JS never re-derives — index.html stops re-deriving paths (including the literal `r.slug === "pyforge-genesis"` special case).
- (capability) CAP-4 an unresolvable slug fails loud — the existing detectors catch it rather than a silent wrong path.
- (note) Both known live bugs (found 2026-08-02: real merged work reading "nothing landed", and dead links) came from the same "slug == project dir" assumption, patched per-site each time. The Spec's code facts were verified against generate.py on 2026-08-08.
- (question) OPEN, carried from the Spec: discovery basis under a CI checkout (sprint-status.yaml is Tier-3 gitignored, so scanning implementation-artifacts finds nothing); whether the regen line's _DERIVE_EXCLUDE special becomes a field on the same override table; whether _discover_loop_homes()'s independent slug normalization unifies onto the same resolver.
