---
topic: marshal-recall-in-the-loop
updated: 2026-09-20T20:35
---

- (capability by claude) CAP-1 -- a bmad-loop dev pass automatically receives relevant scribe feedback before it starts. Intent: before a bmad-loop dev pass (bmad-dev-auto) starts a story, it queries scribe recall scoped to the story's touched surface/station, and any relevant feedback entries fold into that session's starting context -- no human has to remember to run recall first. Success: given a scribe feedback entry scoped to a station/surface a new story touches, that story's dev-pass session context includes it without a manual recall invocation; given no relevant entry exists, nothing is injected.
- (capability by claude) CAP-2 -- a bmad-loop review pass sees the same scoped feedback the dev pass saw. Intent: the review pass (opus, trigger=recommended) receives the same scoped scribe-feedback context as CAP-1's dev pass, so a correction from a prior session is visible to whoever grades the new work, not just the agent that wrote it. Success: given CAP-1 injected a feedback entry into the dev pass, the review pass for the same story sees the same entry; a review that contradicts a known, injected correction is a visible discrepancy, not a silent miss.
- (capability by claude) CAP-3 -- recall-injection composes with the existing [context] pipeline as its own layer. Intent: CAP-1/CAP-2's injection is a named layer alongside marshal's existing wire/output/structure-graph/derived-context/planning-graph layers in policy.toml's [context] block, not a bolted-on side mechanism outside that discipline. Success: the layer has its own [context.<name>] enabled/aggressiveness toggle, matching the existing five; disabling it removes the injection with no effect on the other five layers.
- (capability by claude) CAP-4 -- a genuine recall miss injects nothing, never a fabricated confidence claim. Intent: scribe recall's own honesty (a grounded miss is explicit, never an invented uncited answer) is preserved end to end through the injection path -- a query with no relevant hits adds nothing to context, never a synthesized "no relevant corrections found" line presented as if it were a checked fact. Success: given a scoped recall query with zero hits, the injected context block is empty/absent, not a generated null-result sentence.
- (constraint by claude) Scribe stays the sole owner of capture and compile -- this Spec adds a read-only consumer of scribe recall, never a second capture path, never a change to recall's own grounding rules (recall.py stays authoritative on what counts as a citable hit).
- (constraint by claude) Stays inside marshal's existing token-economy discipline -- CAP-1/CAP-2's injected context is scoped (not a full-repo pull) and counted the same way the other five [context] layers already are (budget ceilings, per-story/per-run weighted-token accounting), never an unbounded reread.
- (non-goal by claude) Not a general RAG-everything layer -- scoped strictly to the feedback/correction material scribe already curates (capture types feedback/project/reference), never a blanket full-repo retrieval pass added on top.
- (non-goal by claude) Does not replace or lower the bar for human review -- the review pass gets more of what it should already have known, not less scrutiny or an automated approval path.
- (non-goal by claude) Does not change what scribe captures or how -- capture-side behavior (feedback/project/reference types, the promotion workflow) is out of scope; this Spec is a consumer only.
- (question by claude) Exact scope mechanism for CAP-1/CAP-2's recall query is unresolved -- the Dream says "scoped to the story's touched surface/station" but does not pin down whether this reuses --scope <slug> the way marshal's planning-graph retrieve already does (per scribe-marshal-fact-visibility CAP-1), or needs a narrower per-touched-file-glob scope. Left for architecture.
- (question by claude) Whether this applies only to unattended bmad-loop runs or also to bmad-build-auto single-story dispatch sessions is unaddressed -- the Dream names only "bmad-loop dev pass and review pass." Left open.
- (question by claude) Default aggressiveness/token-budget for the new [context] layer, and how it interacts with session_budget_mode (off/warn/enforce), is not specified in the source Dream. Left for architecture to size against the existing five layers' own defaults.
- (question by claude) Whether a NEW correction a review pass itself surfaces (not previously in scribe) should auto-capture back into scribe, closing the loop end to end, or stays a manual scribe capture step afterward -- the Dream describes propagation forward (memory to agent) but not this reverse direction. Left open; likely a separate capability if pursued.
- (decision by claude) Why: scribe's capture/recall primitive already exists and is real, shipped, a complete BMAD project -- the gap is propagation, not capture. Named directly from the article 'Towards Self-Driving Codebases' (blog.detail.dev): 'the code review bot needs to be aware of the correction you issued to the agent... two weeks ago.' Today neither bmad-loop's dev pass nor its review pass automatically queries scribe recall before starting -- a human (or this session, by hand) has to remember to pull it in.
- (assumption by claude) Assumes marshal is the correct primary owner (this Spec lives in pyforge-marshal's planning tree, fold-exemption: cross-station-seam) because the injection point is marshal's own dev/review pass orchestration, even though the data being injected is scribe's. Mirrors the existing spec-pyforge-core precedent (kernel/testing-kit, marshal-owned, cross-station-seam) named in the source Dream's Kinships.
- (decision by claude) Success signal: an operator stops needing to manually run scribe recall and paste results into a dev/review session before it starts on touched-before territory; a review pass catches (or does not silently repeat) a mistake scribe already has a captured correction for, without the operator re-supplying it by hand; recall's grounded-miss honesty is unbroken by the injection path -- verified by an empty-hit case producing no injected text.
- (event by claude) Self-validate pass 1 (coherence): PASS. All four capabilities carry both intent and success; intents describe observable WHAT (matches this repo's own convention of naming a CLI/behavior surface, not internals); both constraints actually rule something out (no second capture path, no unbounded reread); three non-goals present; success signal pairs one qualitative operator-observed claim with one concretely testable one (CAP-4's empty-hit case); four fresh, unique CAP ids; prose density matches sibling specs (spec-pyforge-core, spec-pyforge-doctor).
- (event by claude) Self-validate pass 2 (preservation): PASS. Source docs/dreams/marshal-recall-in-the-loop.md's load-bearing claims all landed: the core problem (Why + CAP-1/2), all four 'what it looks like when real' bullets (CAP-1 auto-query before dev start, CAP-2 review-pass parity, CAP-3 composes with [context] pipeline, CAP-4 recall-honesty preservation), all three constraints/non-goals (scribe stays sole capture owner, doesn't replace review, not blanket RAG), and the Kinships (spec-pyforge-core cited as the owning precedent in Assumptions; scribe-marshal-fact-visibility cited by name in Open Questions; marshal-token-economy referenced via the [context]/token-economy-discipline language). No wrapper-only content dropped. Four genuine open_questions recorded rather than invented answers.
- (event by claude) NOTE: this spec folder's active-project switch drifted to pyforge-herald mid-operation (root cause: a background poll job's 'marshal watch --project pyforge-herald' calls appear to mutate the shared switch as a side effect), causing SPEC.md to briefly land under pyforge-herald's physical tree instead of this one. Caught by verifying placement after writing (per this repo's own parallel-agents discipline), moved to the correct physical path before this entry. Remaining writes for this spec went to the physical path directly rather than through the symlink.
- (event by claude) chain-sprawl-check initially failed against this spec folder -- fold-exemption: cross-station-seam on the Dream's own frontmatter does not propagate to the Spec folder; the Spec's own SPEC.md frontmatter needs the same key independently (confirmed by inspecting spec-pyforge-core's own frontmatter as precedent). Added; re-verified clean.
- (event by claude) Surface reconcile 2026-09-20 (steward Story 66.1, spec-pyforge-steward:CAP-153, PR #1553): src/shared/packages/pyforge-scribe/src/pyforge/scribe/recall.py reformatted by ruff format (import sort + formatting only; no behaviour change, scribe suite green).
- (event) Story 47.1 (CAP-1) implemented: harness_bmadloop.py now shells scribe recall --scope <station-slug> before a bmad-loop dispatch, folding a grounded hit into loop_home's implementation-artifacts recall-feedback.md; a miss or scribe-unavailable path degrades to a WARN finding (MRS-SPIN-018), never blocking. Governed paths changed: src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/recall_feedback.py (new), src/shared/packages/pyforge-marshal/src/pyforge/marshal/adapters/harness_bmadloop.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/spin.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/findings.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/verdict.py, plus new/updated tests under src/shared/packages/pyforge-marshal/tests/unit/{test_recall_feedback.py,test_harness_bmadloop_recall.py,test_spin.py,test_findings.py} and tests/meta/test_ad11_write_boundary.py. spec_surface_reconcile.py reported no drift and no co-governor for this change set. Also fixed a pre-existing flaky race in the unrelated bmad-build-auto test helper _await_file (src/shared/packages/pyforge-marshal/tests/unit/test_harness_bmadbuild.py) -- it returned on file-existence rather than file-content, racing the dispatched child's write; found while independently verifying this story (subagent self-reported 0 failures, my own run showed 1).
