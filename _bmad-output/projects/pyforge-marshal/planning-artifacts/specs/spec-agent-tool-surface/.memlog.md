---
topic: agent-tool-surface — the factory, callable
updated: 2026-09-09T18:07
---

- (decision) Spec authored 2026-07-28 to close INV-1's only realized-without-a-contract case. The surface reached status: realized without ever passing through specified, which is why four artifacts disagree on .mcp.json and why 2-of-6 station coverage went unmeasured. Q1 (one surface or two) left open and owned here; the .mcp.json contradiction is stated as requiring resolution before CAP-2 can be built.
- (event) Round-4 code-audit remediation (2026-07-29) hardened both governed MCP servers. `conda_forge_server.py`: `trigger_build` now confines its caller-supplied recipe path to `recipes/` before probing for recipe.yaml/meta.yaml (AUD-CFE-006), and `query_atlas` gained allowlists on `select`/`order_by` (the latter was interpolated into the SQL with no validation at all), a read-only connection, a closed connection, and a `limit` clamped at both ends — `min(limit, 1000)` alone admitted `limit=-1`, which SQLite reads as *no limit*. `gemini_server.py`: the API key moved from the `?key=` query string to the `x-goog-api-key` header on all four transport paths (AUD-CFE-010), behind one helper so a new transport cannot reintroduce the leak. `mcp_call.py` (the trusted-operator CLI wrapper) gained argument validation. No capability was added or removed; this is CAP-surface hardening, not a contract change — Q1 (one surface or two) and the `.mcp.json` contradiction remain open and unaffected.
- (assumption) The path-confinement helper the recipe-facing tools share (`.claude/skills/conda-forge-expert/scripts/_path_guard.py`) is now a load-bearing dependency of `conda_forge_server.py`. It was previously written but NEVER COMMITTED — `/.claude/skills` sat in `.git/info/exclude`, which hid new files from `git status` — so the abandoned PR #131 imports a module it does not ship. Any future tool on this surface that accepts a path must confine through that helper rather than hand-rolling the check.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Surface reconcile 2026-08-31: 2 drift-presumed paths since baseline stamped at `864558e02d3` (Round-4 audit remediation, 2026-07-29). One commit since then: `06fe64f6037` (mcp >2.0) — upgrades both governed MCP servers to MCP SDK >2.0 (FastMCP migration via kedro-mcp patch); `.claude/tools/conda_forge_server.py` and `.claude/tools/gemini_server.py` updated for SDK API compatibility. Infra/chore landing, no CAP text change. RECONCILES: `.claude/tools/conda_forge_server.py`, `.claude/tools/gemini_server.py`. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-agent-tool-surface`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 1 governed path(s) drifted against this spec's recorded baseline (1 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `.claude/tools/conda_forge_server.py` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-agent-tool-surface` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (decision by claude) 2026-09-09 Q1 ANSWERED by operator (fleet-readiness-decision-batch-2026-09-09 row mars-A-B1 / C10): the surface FEDERATES -- the HTTP station face POST /stations/<name>/mcp (django-pyforge mcp_http.py:137, with auth mcp_auth.py:1, rate-limiting rate_limit.py:3 and dual-era routing mcp_dual_era.py:29,40) is the ONE GOVERNED FRONT DOOR, and the stdio servers are LOCAL ADAPTERS. The question was stale as posed: it is no longer two surfaces but five (.claude/tools/conda_forge_server.py with 46 @mcp.tool decorators, .claude/tools/gemini_server.py, pyforge/atlas/mcp/server.py, pyforge/marshal/mcp/server.py, and the HTTP transport). Merging five into one binary re-couples the crafts the Charter deliberately split; leaving them split leaves the surface ownerless, the failure this Dream exists to name. Federation is the only option the existing code already implements -- the HTTP face is a mount point, not a server, so each craft keeps its own tools.
- (note by claude) 2026-09-09 measured facts the Spec body and the Dream both understate (fleet-readiness-decision-batch-2026-09-09 rows mars-A-D9/D10/E6): coverage is 3/6 today, not 2/6 -- running the gate this Dream asked for (pyforge/marshal/mcp/coverage.py::tool_surface_coverage_report) returns {"ratio": "3/6", "coverage": 0.5} (mason via conda_forge_server.py, atlas, and marshal itself, no longer at zero). CLI-tool parity is GATED FOR MARSHAL ONLY (mcp/parity.py, tests/meta/test_cli_tool_parity.py, Story 18.2 / FR-155); the 46 CFE tools and pyforge/atlas/mcp/tools.py have no equivalent gate -- CROSS-STATION, routed to mason and atlas per the Spec own ownership split (a change to what a tool does is the craft owner). CAP-4 number is computed but not published (mcp/coverage.py has a main(); no pixi task exposes it and the console that would have carried it is retired). .mcp.json still exists NOWHERE -- not at the repo root and not in any of the eight loop homes, despite cli/init.py:447 _render_mcp_json.
- (event) (self-validate pass 1 -- coherence) 2026-09-09 re-derive: PASS. Q1 retired with a dated ANSWERED block carrying the federation answer and the five-surface restatement; the measured facts render as their own dated section because four of them are file:line claims downstream will re-verify; the stale 2-of-6 sentence is struck through with its 3-of-6 amendment rather than silently rewritten. No status event in the memlog, so status stays in-progress. 4 CAP ids, unchanged.
- (event) (self-validate pass 2 -- preservation) 2026-09-09 re-derive: PASS. Both 2026-09-09 entries landed: the federation decision with its rejected alternatives, and all four measured facts (3/6 coverage, marshal-only parity gate with the mason/atlas cross-station routing, CAP-4 computed-not-published, .mcp.json absent everywhere). The .mcp.json four-artifact contradiction section is untouched.
