---
topic: pyforge-core — the shared leaf every station's duplicated primitive collapses into
updated: 2026-09-20T08:39
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (event) Surface reconcile 2026-09-07 (fleet hygiene, spec-45-2 CAP-2 pilot landing): `pixi.toml` changed to add three `local-recipes` pixi tasks (`eval-quality-smoke`, `eval-quality-review-twin-run`, `eval-quality-review-replay`) for pyforge-steward's `spec-bmad-eval-quality` CAP-2 pilot — unrelated to this spec's own surface, but this spec's blanket `pixi.toml` glob makes it a co-governor of that one file. No product work here. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.


- (provenance) THIS RECORD STARTS HERE. Created 2026-08-09 by marshal S-13.5 (spec-surface-drift-reconciliation CAP-5 / FR-168): this Spec shipped declaring a `surface:` with no .memlog.md, so spec_surface_check's contract_hash() returned "" and the contract could never move — every governed change would have reported a hard [drift] whose printed remedy ("reconcile the spec") was unreachable, there being no contract to move. Nothing that happened before this entry was captured; do not read the absence of earlier entries as an absence of decisions. The baseline was scoped-stamped in the SAME change that created this file, because a new memlog moves the contract hash off "" and would otherwise downgrade this surface's next real drift from gating [drift] to informational [drift-presumed].
- (note) Governs 1 tracked file at the time of writing; the package itself is unbuilt. Decomposed into PRD FR-157/FR-158 et seq — marshal S-10.1 is BLOCKED on those landing.
- (capability) CAP-1 the leaf exists and is provably a leaf — a dependency direction enforced, not asserted.
- (capability) CAP-2 atomic write has one implementation and twenty fewer copies.
- (capability) CAP-3 the verdict lattice is declared once, and narrowing is EXPRESSED, not asserted.
- (capability) CAP-4 one report envelope, extended rather than rivalled.
- (capability) CAP-5 one exception root — herald's and mason's independent roots re-parent, with no existing `except` clause changing behaviour (asserted by test, since re-parenting can silently widen a catch).
- (capability) CAP-6 the subprocess seam is reconciled, and the reconciliation is honest about Marshal — Marshal's own 7 importing modules are the widest ungated surface in the fleet.
- (capability) CAP-7 a second implementation cannot appear unnoticed — one sole-ownership meta-test per extracted primitive, failing the build when a second implementation appears anywhere under src/shared/packages/.
- (change) RECONCILED BY NAME, 2026-08-09: pixi.toml changed under this surface — doctor's Epic 6 S-6.2 added the `pyforge-doctor-scripts-test` task (feature.pyforge-ci) for scripts/detectors.py's Doctor-registry integration. No dependency change and no effect on this Spec's own contract; pixi.toml is simply governed by three surfaces at once (also spec-pyforge-doctor and spec-unified-container), so each acknowledges the same edit independently.
- (note) This is the FIRST movement of this contract since it was created on 2026-08-09 by marshal S-13.5, which found this Spec drift-blind — it declared a surface with no .memlog.md, so contract_hash() returned "" and no governed change was reconcilable except by stamping. The mechanism working here, on a real edit two hours later, is the evidence that fix was worth making.
- (correction) The prior entry's "governed by three surfaces at once (also spec-pyforge-doctor and spec-unified-container)" is wrong and was wrong when written: `spec-pyforge-doctor/SPEC.md`'s `surface:` block has never listed `pixi.toml` (checked its full git history). `pixi.toml` is governed by exactly two surfaces — this Spec and `spec-unified-container`.
- (change) RECONCILED BY NAME, 2026-08-09: pixi.toml changed under this surface — mason's Story 1.9 (fake-cfe-root-fixture-and-test-harness) split the `pyforge-mason-test` task into a fast default (`-m "not slow"`) plus a new `pyforge-mason-test-slow` task, mirroring pyforge-marshal-test-slow's split (pyforge-warden's equivalent task predates the `slow`-marker convention and is named `pyforge-warden-test-corpus-oracle`, not `*-test-slow`). A task-definition edit only: no dependency change and no effect on this Spec's own contract; pixi.toml is governed by two surfaces at once (also spec-unified-container), so each acknowledges the same edit independently.
- (change) RECONCILED BY NAME, 2026-08-09: pixi.toml changed under this surface — doctor's Epic 6 S-6.6 (the chain verdicts come home) adjusted the root manifest as part of moving dream-chain/spec-surface/deferred-work verdicts into pyforge-doctor. No dependency change and nothing that touches this Spec's own contract; pixi.toml is governed by three surfaces at once, so each acknowledges the same edit independently. Recorded because the alternative — stamping the baseline and moving on — is precisely the laundering S-13.2 exists to end, and it would have been invisible in the resulting green.
- (change) RECONCILED BY NAME, 2026-08-09: pixi.toml changed under this surface — S-5.7 added the `fleet-picture` task (a progress REPORT, never a detector: read-only, always exits 0). Task-only edit, no dependency change; environment.yaml verified unchanged by re-export.
- (change) RECONCILED BY NAME, 2026-08-10: pixi.toml changed under this surface — Story 4.15 (FR-177, "one pusher, not two") removed the `loop-push-watch` task (the standalone interval-push watcher retired as fully subsumed by the supervisor's own FR-61 stage-boundary + interval-fallback push) and added one confirming sentence to `unpushed-work-check`'s description naming it the standing signal for between-runs staleness. Task-only edit, no dependency change, and no effect on this Spec's own (still-unbuilt) contract; pixi.toml is governed by two surfaces at once (also spec-unified-container), so each acknowledges the same edit independently.
- (change) RECONCILED BY NAME, 2026-08-12 (landed 2026-08-13): Story 14.1 (the leaf exists and is provably a leaf, CAP-1) shipped the package scaffold under this surface -- src/shared/packages/pyforge-core/.gitignore, README.md, pixi.toml, pyproject.toml, src/pyforge/core/__init__.py, src/pyforge/core/py.typed, tests/meta/test_leaf_constraint.py -- plus the new pyforge-core feature/env/tasks in the root pixi.toml (also governed by spec-unified-container). The dependency-direction enforcement CAP-1 promises is the new test_leaf_constraint.py meta test; the package itself carries no third-party run-dep yet (stdlib-only leaf), matching this surface's own note that it was 'unbuilt' until now.
- (change) RECONCILED BY NAME, 2026-08-12 (landed by hand 2026-08-13 after a deferral -- see spec-pyforge-marshal's own memlog for the full deferral/hand-landing account): Story 14.2 (atomic write has one implementation, CAP-2) ships the capability this Spec already declared: root `pixi.toml` (the pyforge-core-consuming feature entries for atlas/herald/marshal/scribe/steward/warden, plus this package's own `[environments]`; governed by three surfaces at once per this memlog's own established convention -- also spec-pyforge-marshal and spec-unified-container), `src/shared/packages/pyforge-core/src/pyforge/core/atomic_write.py` (added -- mkstemp-based, umask-respecting default, lock-guarded probe), `src/shared/packages/pyforge-core/tests/meta/test_atomic_write_sole_ownership.py` (added -- CAP-7's per-primitive sole-ownership meta-test, failing the build if a second implementation appears anywhere under `src/shared/packages/`) and `src/shared/packages/pyforge-core/tests/unit/test_atomic_write.py` (added). The twenty retired call sites across atlas/herald/marshal/scribe/steward/warden are each named in their OWNING station's own spec memlog, not here -- this entry covers only the leaf package itself.
- (change) RECONCILED BY NAME, 2026-08-13: Story 14.3 ("one lattice, one envelope, one exception
  root", commit `1bd8c8a2de`) ships CAP-3, CAP-4 and CAP-5 -- the verdict lattice, the report
  envelope base, and the exception root -- plus CAP-7's two new fleet-wide sole-ownership guards.
  Ten new/changed paths under this surface: `src/pyforge/core/verdict.py` (new -- `Lattice(order,
  exit_by_member)` with `.rank`/`.exit_codes`/`.narrows`, generic over any hashable member type, no
  `enum` import; plus `dispatch_exit_code(exc, table, default)`, herald's most-specific-first
  isinstance algorithm generalized; zero non-stdlib imports), `src/pyforge/core/report.py` (new --
  `BASE_ENVELOPE_SCHEMA: dict` + `compose(base, station_schema) -> dict`, a pure
  `{"allOf": [base, station]}` dict merge, no `jsonschema` import -- CAP-1's leaf constraint holds),
  `src/pyforge/core/errors.py` (new -- `PyforgeError(Exception)`, a bare marker with no `__init__`
  override), `tests/unit/test_verdict.py`, `tests/unit/test_report.py`, `tests/unit/test_errors.py`
  (new -- primitive-level unit coverage), `tests/meta/test_verdict_lattice_sole_ownership.py` (new
  -- CAP-7 guard for CAP-3, scanning for the retired `{k: i for i, k in enumerate(seq)}`
  rank-comprehension shape outside pyforge-core), `tests/meta/test_exception_root_sole_ownership.py`
  (new -- CAP-7 guard for CAP-5, AST-scanning every sibling station for a stdlib-exception-rooted
  class missing `PyforgeError`), `tests/meta/conftest.py` (new -- `sibling_station_dirs`/
  `station_source_files`/`parse_module` extracted from the pre-existing
  `test_atomic_write_sole_ownership.py`, a review-pass dedup so all three meta-test files share one
  station-enumeration/parse implementation instead of three copies), `tests/meta/
  test_atomic_write_sole_ownership.py` (changed -- updated to import the extracted helpers from
  `conftest.py` rather than defining its own), and root `pixi.toml` (changed -- adds
  `pyforge-core` as a real run-dependency for `pyforge-doctor` and `pyforge-mason` for the first
  time, both untouched by Story 14.2; also governed by `spec-pyforge-doctor`, `spec-pyforge-mason`
  and `spec-unified-container` per this memlog's own established multi-surface convention, each
  reconciling independently). Every consuming station's own re-parented exception classes,
  `Lattice`-wired verdict modules, and `compose(...)`-wrapped envelope validation calls are named
  in THEIR OWNING station's own spec memlog, not here -- this entry covers only the leaf package
  itself plus root `pixi.toml`. `pyforge-core-test`: 848 passed (847 + 1 new, after the conftest.py
  extraction landed inside this same story). `ruff check src/shared/packages/pyforge-core`: zero
  findings.
- (change) RECONCILED BY NAME, 2026-08-13 (landed 2026-08-13, commit `901f211f5a`): Story 14.4
  ("the subprocess seam is reconciled and sole ownership is gated") ships CAP-6 and a second CAP-7
  guard -- three new paths under this surface: `src/pyforge/core/process.py` (new --
  `ProcessResult` (frozen dataclass: returncode/stdout/stderr), `ProcessPort` (Protocol:
  run/is_alive/spawn_detached), `PosixProcess` (concrete adapter), `ProcessError(PyforgeError,
  Exception)` -- verbatim move of Marshal's own `ports/process.py` + `adapters/process_posix.py`,
  chosen over Doctor's simpler `cli_bridge.run_cli_json`/`run_git` pair for its
  non-raising-on-nonzero-exit contract and `is_alive`/`spawn_detached` capability, preserving the
  exact exception-translation discipline -- each `raise ... from exc`, so downstream
  `isinstance(exc.__cause__, subprocess.TimeoutExpired)`-style checks keep working),
  `tests/unit/test_process.py` (new -- unit coverage adapted from Marshal's own retired
  `tests/unit/test_process_posix.py`), `tests/meta/test_process_sole_ownership.py` (new -- CAP-7
  guard for CAP-6, generalizing Doctor's own `_subprocess_violations` AST detector to scan every
  sibling station; excludes Doctor/Warden/Steward station-level, already-conforming/sanctioned per
  this Spec's own CAP-6 success text, and Marshal's own `adapters/harness_bmadloop.py` file-level,
  mirroring Doctor's own `_EXEMPT_RELATIVE_PATHS` pattern, covering its 4 sanctioned
  interactive-stdio/append-mode-log call sites; Herald/Mason/Scribe's own real, un-migrated
  subprocess call sites, discovered live by this guard's fleet-wide scan, are also excluded and
  recorded as DW-FU-14-4 for a future story rather than fixed here). Marshal's own
  re-pointed/migrated call sites and updated test-file import paths belong to
  `spec-pyforge-marshal`'s own surface and are named in ITS OWNING memlog, not here -- this entry
  covers only the leaf package itself. `pyforge-core-test`: 1051 passed. `ruff check
  src/shared/packages/pyforge-core`: zero findings.
- (correction) This entry was owed at landing time -- `spec-pyforge-marshal`'s own memlog entry for
  this story already stated "reconciled independently in its own memlog for the leaf package
  itself," but the entry was never written, leaving this surface's 3 new files ungoverned by a
  moved contract. Added now, on the same landing revision, to close that gap; no code changed.
- (change) RECONCILED BY NAME, 2026-08-13 (same repair pass): a step-04 review of the above entry
  found `ruff check src/shared/packages/pyforge-core` did NOT actually report zero findings as
  claimed -- one `I001` import-order violation in `tests/unit/test_process.py` (its own new
  `from pyforge.core.process import ...` line landed unsorted relative to stdlib imports). Fixed
  with `ruff check --fix`; re-verified clean. `tests/unit/test_process.py` is the one file this
  surface owns that changed as a result.
- (change) RECONCILED BY NAME, 2026-08-13: pixi.toml changed under this surface -- Herald's Story 13.6 (`a ship records itself, end to end`) added `daphne = ">=4.2.2,<5.0"` under `[feature.pyforge-herald.dependencies]` (line ~1657), backing the new `webhook-host` optional-dependencies extra `webhook_host.py`'s daphne-hosted ASGI wrapper needs at runtime -- byte-identical floor to `pyproject.toml`'s own `webhook-host` extra pin, to `[feature.pyforge-steward.dependencies]`'s pre-existing daphne pin (Story 9.5), and to this file's pre-existing wagtail/coderedcms pin, per the pin-byte-identity convention this repo already established. No effect on this Spec's own contract -- CAP-1 through CAP-7 govern the pyforge-core leaf package and its dependency-direction/atomic-write/verdict-lattice/report-envelope/exception-root/subprocess-seam/sole-ownership capabilities, none of which this edit touches; the new dependency lives entirely under `[feature.pyforge-herald.dependencies]`, a station-owned feature edge this Spec's own `surface:` comment ("the new workspace member + its per-station dep edges") already anticipates. `pixi.toml` remains governed by exactly two surfaces at once (also spec-unified-container), each acknowledging the same edit independently. `pixi project export conda-environment -e build` re-verified byte-identical (unaffected -- `pyforge-herald` is not a member of the `build` environment's feature set), so `environment.yaml` needed no re-export for this change.
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Surface reconcile 2026-08-31: 24 drift-presumed paths since baseline at `2cd08f7e76ac` (Story 14.3 merge). Eight commits, pyforge-core CAP delivery + cross-station integration (all done in ledger):
  - `5141daad88` / `26102ea12c` — Stories 14.3/14.4 (done): one lattice, one envelope, one exception root; subprocess seam reconciled.
  - `b6addf95e4` — Story 20.8 (done): shared landing-evidence grammar in pyforge-core.
  - `53298661db` — steward: shared hook-spec and plugin registration landed in pyforge-core.
  - `2f4f4e8e5d` — steward: RS256 assertion shared across portal and CLI clients.
  - `ef0430f36f` — steward: dispatch pyforge station verbs without reimplementing.
  - `ebc4915f33` — Story 22.9 (done): marshal review patches on core surfaces.
  - `cfadb82d93` — sole-ownership meta-test failures resolved (13 sites reparented to domain errors).
  No CAP text change. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (event by claude) Surface reconcile 2026-09-01: `pixi.toml` changed once since the 2026-08-31 stamp by `af315d4bd7` (Story 28.7, done, PR #975) — adds `[feature.local-recipes.tasks.index-freshness-check]` only; no pyforge-core package or CAP touch. Cross-package bookkeeping under this spec's pixi.toml wildcard. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (event by claude) Surface reconcile 2026-09-01, second pass: `[feature.pyforge-ci.dependencies]` gained `"ruamel.yaml"` (failure_catalog_check test import fix); unrelated to pyforge-core CAPs. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 1 governed path(s) drifted against this spec's recorded baseline (1 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `pixi.toml` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (event) Surface reconcile 2026-09-04 (fleet hygiene, PR #1043): PR #1040's `pixi lock` (64f3f8e584 — `pnpm >=12.3.1` → `>=12.3.2`, lock re-solved) moved this Spec's `pixi.toml` / `pixi.lock` rows without this memlog moving. Accept the current tree as the post-land surface; no contract change, no new product work. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core` (scoped, never a bare `--write-baseline`), from a clean tree so the recorded hashes are committed content.
- (event) Surface reconcile 2026-09-04 (PR #1043, second pass — the Detectors env): `pixi.toml` gains `[feature.detectors.dependencies]` (pyforge-doctor path dep, `ruamel.yaml`, `playwright-python`) and the `detectors` environment (no-default-feature; 86 packages) that `.github/workflows/detectors.yml` now installs pixi-first with `locked: true`, pip as the visible fallback; `pixi.lock` gains its block; `environment.yaml` (derived, `build` env) re-exported and caught up (python 3.14.6→3.14.7, nodejs 24.16→24.19 floors already in `pixi.toml`). Nothing this Spec governs changed in meaning; no contract change. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core` from a clean tree.
- (event) Surface reconcile 2026-09-04 (PR #1043, third pass): `pixi.toml` / `pixi.lock` rows moved for OTHER features (platform-ci-test gains the pyforge-steward path dep; dbgpt-sidecar gains pytz — Platform CI was red on both); nothing this Spec governs changed in meaning. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (event) Surface reconcile 2026-09-04 (PR #1043, fourth pass — the fleet CI health pass): `pixi.toml` rows moved for the python-agent-platform feature only: the `rjsmin` / `rcssmin` `build = "py312*"` pins were dropped (they locked cp312 builds into the py3.14 image env, so the Containerfile's `compress` step was red) and the `cachebox <6` / `openfeature-provider-flagd <0.5.1` ceilings the platform policy suite guards were restored after commit 098f0f0672 had dropped them. Nothing this Spec governs changed in meaning. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core` (scoped, never a bare `--write-baseline`).
- (event) Surface reconcile 2026-09-04 (PR #1043, seventh pass — the local Platform CI replay): `pixi.toml` gains the `platform-ci-local` task under `[feature.local-recipes.tasks]` (`bash scripts/platform-ci-local.sh`): a local replay of `.github/workflows/platform-ci.yml` — the test job step for step, the three image builds, the container job's runtime smokes and golden-path-promotion + the deploy verifier — against an ephemeral PostgreSQL 17/Redis 7 from platform-dev, so a platform change is proven before it is pushed (operator ask, after four sequential Platform CI cycles for four one-line CI-only fixes). No dependency, environment or lock change; `pixi lock --check` clean, `environment.yaml` untouched. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (change by claude) Surface touch 2026-09-05 (PR #1045): pixi.toml's content hash moved because unrelated stations' feature blocks gained a `pyforge-testing-kit` test-time dependency (retro-pyforge-steward-2026-09-04.md action item 11: the fleet's whole-branch diff-guard test mechanics (git subprocess calls computing files/commits changed since origin/main) were consolidated into a shared `pyforge.testing_kit.branch_diff_guard` module (PR #1045).); pyforge-core's own dependency block and run-dep contract are untouched. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (change by claude) Surface touch 2026-09-05: pixi.toml's content hash moved for the new `platform-policy-suite-check` task (retro action item 3 (retro-pyforge-steward-2026-09-04.md, 2026-09-05): a new pyforge-doctor repo-scope source, `platform-policy-suite`, judges src/platform's manifest-only tests/policy subset by shelling out to the platform-ci-test pixi env's own interpreter (cli_bridge.run_pytest, AD-5), so a pixi.toml regression like the openfeature-provider-flagd/cachebox ceiling drop is caught even when Platform CI itself is disabled. WARN when that env is not installed, FAIL only on an actual policy-suite failure.); pyforge-core's own dependency block and run-dep contract are untouched. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (note by claude) 2026-09-05 (Story 45.1 pin PR, follows #1059): pixi.toml changed — bmad-eval-quality >=0.2.0.dev0 added to feature.local-recipes' linux-64 and osx-arm64 target tables (unix-only variant on SelfExplainML), bmad-method-wds-expansion pin dropped (upstream deprecated), bmad-labs-skills comment 22 skills; lock re-solved for the pin swap only (no other package moved). Routine suite-pin upkeep this spec's surface already anticipates; no capability change against this spec.
- (note by claude) 2026-09-05 pixi upgrade sweep PR (branch maint/pixi-upgrade-sweep-2026-09-05): pixi.toml changed — operator `pixi upgrade --pinning-strategy latest-up` pass landed as one maintenance PR: pixi 0.78.0->0.79.0 (requires-pixi + $schema + all 15 registered external sites via bump-pixi-version, environment.yaml regenerated), bmad-method >=6.12.0, bmad-suite >=2026.9.5, ~20 other floors bumped, lock re-solved; the cachebox <6 and openfeature-provider-flagd <0.5.1 ceilings the platform policy suite guards were restored after the sweep dropped them. No spec-content change; scoped --write-baseline follows this entry.
- (note by claude) 2026-09-05 hygiene-2 PR (branch maint/hygiene-2-2026-09-05): pixi.toml changed — `lint` task -> CI-parity conda-smithy via pixi exec over real recipe dirs; no dependency, env, or lock change. Scoped --write-baseline follows.
- (note by claude) 2026-09-05 hygiene-2 PR, second pixi.toml touch (same branch): the conda-smithy `lint` task now exports GH_TOKEN from `gh auth token` when unset (the CalVer linter's --conda-forge mode calls GitHub per recipe; anonymous is 60 req/h) and its description records the corpus runtime. No dependency, env, or lock change. Scoped --write-baseline follows.
- (event) Surface reconcile 2026-09-07 (fleet hygiene, pyforge-steward Story 46.3, spec-bmad-suite-lifecycle CAP-4 half): `pixi.toml` changed to add one `local-recipes` pixi task (`tea-test-review`, wrapping the real `tea-test-review` CLI with `--base origin/main --min-score 80` defaults) for TEA provisioning -- unrelated to this spec's own surface, but this spec's blanket `pixi.toml` glob makes it a co-governor of that one file. No product work here. Re-derived the governing set the same way Story 45.2/46.1 did (grep `pixi.toml` in every spec's `surface:` frontmatter) rather than assuming their own six-spec list still holds: it now numbers nine (this spec included), not six -- the surface set has grown since 45.2/46.1 landed. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (note by claude) Cross-station surface touch (pyforge-doctor Story 20.2, 2026-09-06): pixi.toml gained one new task, [feature.local-recipes.tasks.bmad-render-config-ambiguity-check] (a new doctor detector, unrelated to the pyforge-core extraction plan). This spec's own pyforge-{atlas,doctor,herald,marshal,mason,scribe,steward,warden}/src/** surface line does not literally match any real path (chain.py's _glob_to_re treats brace characters as literal text, so that entry never matches anything -- verified directly against the glob-compiling code); Story 20.2's other, real edits under src/shared/packages/pyforge-doctor/src/** are therefore genuinely outside this spec's functioning surface, confirmed by the detector itself only flagging pixi.toml. Verified via git stash: pixi.toml carried zero drift against this spec's baseline immediately before this edit, so this is the sole cause. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (note by claude) Cross-station surface touch (pyforge-doctor Story 20.3, 2026-09-07): pixi.toml gained one new task, [feature.local-recipes.tasks.frozen-path-changed-check] (a new doctor detector, unrelated to the pyforge-core extraction plan). This spec's own pyforge-{atlas,doctor,herald,marshal,mason,scribe,steward,warden}/src/** surface line still does not literally match any real path (unchanged from the prior touch's own finding). Verified via git stash: pixi.toml carried zero drift against this spec's baseline immediately before this edit, so this is the sole cause. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (note by claude) 2026-09-06 mason Story 14.1: pixi.toml changed -- reworded both bmad-eval-quality pin comments (feature.local-recipes' linux-64/osx-arm64 target tables) to state the recipe is re-verified win-ready and only the __win artifact's Windows build/upload is outstanding, matching install-matrix.md's hazard-cell wording; no dependency, env, or lock change. Scoped --write-baseline follows.
- (change by claude) DW-FU-20-2 fix (2026-09-07, pyforge-doctor CI/hygiene sweep): this spec's own surface glob 'src/shared/packages/pyforge-{atlas,doctor,herald,marshal,mason,scribe,steward,warden}/src/**' never matched any real path -- chain.py's _glob_to_re has no {a,b,c} brace-expansion support (confirmed directly against the glob-compiling code, and already independently noted twice in this memlog's own two immediately-preceding entries as an aside while reconciling unrelated pixi.toml touches). Replaced with 8 real per-station globs (src/shared/packages/pyforge-<station>/src/** for each of the 8 stations), verified against the actual on-disk tree. This makes ~475 pre-existing files across all 8 stations' src/** trees enter this spec's tracked surface for the FIRST TIME -- not new work: this spec is still status: draft (the pyforge-core extraction has not started), so every one of these files already existed, untouched by this fix, simply never governed before because the glob could not match them. Treated as a first-time baseline capture (mirrors this memlog's own 2026-08-09 'THIS RECORD STARTS HERE' provenance entry, not a per-file drift review) -- the current tree becomes the starting point future real drift is measured against. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-pyforge-core.
- (note by claude) Cross-station surface touch (CI/hygiene sweep 2026-09-07, parallel per-station deferred-work fixes landing after DW-FU-20-2's glob fix above): the newly-corrected 8-station src/** surface immediately caught 8 files edited by 4 unrelated parallel worktree agents merged in the same sweep -- atlas's vulnerability/nodes.py + pipeline.py (DW-B2-3), doctor's sources/chain.py (the same DW-FU-20-2 truncation-marker fix, already covered under spec-pyforge-doctor's own memlog), marshal's core/egress.py + core/journal.py (DW-FU-3-2-12), steward's dashboard/export.py (DW-9-4-3/DW-9-4-4), and warden's cli.py + tea_advisory.py (DW-FU-11-2). None of these touch the pyforge-core extraction plan itself; each is independently reconciled under its own station's spec memlog (spec-pyforge-atlas, spec-pyforge-doctor, spec-pyforge-marshal, spec-pyforge-steward, spec-pyforge-warden) with its own RECONCILES list. This note exists only because this spec's broadened glob now co-governs the same paths. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-pyforge-core.
- (note) Cross-station reconcile from pyforge-marshal Story 32.2 (spec-fleet-consistency-standard CAP-5): this spec's pyforge-core pyproject.toml raised requires-python to ">=3.14" (same reason as the fleet-wide pass), and pixi.toml gained [feature.pyforge-atlas.tasks.pyforge-atlas-test] with kedro-test retained as a delegating alias -- atlas was the one station where the documented fleet grammar 'pyforge-<station>-test' did not resolve. No source change.
- (note) Cross-station reconcile from Story 32.3 (spec-fleet-consistency-standard CAP-4): pyforge.doctor.sources.factory's artifact classifier gained a rule for 'planning-artifacts/retros/' -> archive:retros. Marshal's four epic retros (epic-2/-3/-4/-5) lived in gitignored implementation-artifacts/, so they were absent from every clone and one worktree teardown from the loss that cost pyforge-warden 13 story specs; promoting them to the tracked retros/ directory every other station already uses left them UNKNOWN to the classifier, because marshal is the only project this detector scans and it was the only station with no planning-artifacts/retros/ to classify. The residual scripts/bmad_drift_check.py carries the identical rule so the mutation-only --fix path does not diverge from the ported verdict.
- (note) Story 32.4 (spec-fleet-consistency-standard CAP-3) retired epics-with-stories.md fleet-wide -- BMAD 6.12 produces it nowhere, six of eight copies were frozen at 2026-08-08 while epics.md moved to 2026-09-06, and a line-diff audit of all eight confirmed nothing normative lived only there (steward's test-tier mandate is at its own epics.md:74; mason's OQ-E2/OQ-E5 and steward's story-sizing convention are likewise in epics.md; herald's 30 stories are a SUBSET of the 64 in its epics.md). This spec's surface: doctor's deps.py and marshal's dispatch_fleet.py both dropped their 'epics*.md except epics-with-stories.md' exclusion -- the glob itself stays, because it also matches chain-scoped epics (epics-regenerable-factory.md, epics-presenton-pixi-image.md). doctor's hygiene_definitions.py dropped the same name from _CONVENTIONAL_FILENAMES, so a file reappearing under it is now correctly an orphan. doctor 1450 and marshal 7530 tests pass.
- (note) Story 32.6/32.7 (spec-fleet-consistency-standard CAP-6, and CAP-4 of spec-pyforge-testing-charter): pixi.toml gained the governance-currency detector task, pytest-cov in the seven station features that lacked it, and seven pyforge-<station>-test-coverage tasks. No pyforge-core source change.
- (note) Story 32.8 (CI follow-up to 32.5, spec-fleet-consistency-standard CAP-2): atlas's dashboard/, publish/ and wasm/ moved from tests/unit/ to tests/integration/. All three are fail-loud gates needing provisioned prerequisites (Playwright's managed Chromium, the DuckDB httpfs extension, a built WASM artifact) that pyforge-station-tests.yml provisions for its whole-tree run but a coverage lane has no business installing. Integration weight by the same reasoning that placed warden's oracle gates there. Ten pixi tasks that still named pre-move atlas paths were repointed, and the eight test-architecture.md files regenerated from the live inventory. This spec's surface: pixi.toml's ten atlas test-path task rewrites, plus python-version 3.12 -> 3.14 in pyforge-core.yml and the coverage-gates setup-uv step.
- (note) Deferred-work sweep 2026-09-08: same cli/config.py comment-only change recorded on spec-pyforge-marshal (DW-FU-31-3-3 -- stale '9 keys' numeral removed, _UNSETTABLE_KEYS made the authority). This spec's blanket glob also governs the file; no pyforge-core source change.
- (change) Sweep follow-ups 2026-09-08 -- surface-touch note, no change to this spec own subject. pixi.toml gained one read-only detector task (`ledger-direction-check` -> `python -m pyforge.doctor.sources ledger-direction`); no dependency, channel, platform, environment or pin changed, and `pixi project export conda-environment -e build` is byte-identical to the committed environment.yaml. chain.py and ledger.py changed for the deferred-work sweep follow-ups (path-token precision + ledger-direction base_ref evidence), reconciled in full on pyforge-doctor/spec-pyforge-doctor and pyforge-doctor/spec-deferred-work-visibility. RECONCILES: pixi.toml, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/chain.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/ledger.py. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-pyforge-core.
- (change) Fleet consistency pass 2026-09-08 (round 2) -- surface-touch note, no change to this spec own subject. pixi.toml gained one read-only detector task (`ad-citation-check` -> `python scripts/ad_citation_check.py`, architecture-decision citation integrity); no dependency, pin, platform or environment changed, and `pixi project export conda-environment -e build` remains byte-identical to the committed environment.yaml. Reconciled in full on pyforge-marshal/spec-fleet-consistency-standard, which governs the detector. RECONCILES: pixi.toml. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-pyforge-core.
- (change) Surface-touch note 2026-09-08: pixi.toml and pixi.lock gained the `scribe-pg` feature (postgresql + pgvector, linux-64/osx-arm64 only) and the composed `pyforge-scribe-pg` environment, giving pyforge-scribe durable GraphStore tests a pixi-provisioned database instead of a hand-rolled container. No existing environment, dependency or pin changed, and `pixi project export conda-environment -e build` remains byte-identical to the committed environment.yaml. Reconciled in full on pyforge-scribe/spec-pyforge-scribe. RECONCILES: pixi.toml, pixi.lock. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-pyforge-core.
- (change) Surface-touch note 2026-09-08 (follow-up): one comment line in pixi.toml qualified an unprefixed citation to pap:AD-16, the local-first / zero-containers decision, after the ad-citation detector flagged the same unprefixed form in scribe memlog. Text only -- no dependency, pin, platform or environment changed. RECONCILES: pixi.toml.
- (change) Surface-touch note 2026-09-08: marshal status gained a `run_state_retired` fact and an MRS-STATUS-012 WARN so a retired or cleaned run reports the home `idle` rather than degrading the whole row to `unknown` (DW-STATUS-2026-09-08-1). `is_run_live` keeps such a run conservatively live, so no destructive guard is weakened. Reconciled in full on pyforge-marshal/spec-fleet-status-supervisor-fallback. RECONCILES: core/status.py, cli/status.py, core/findings.py and their unit tests. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-pyforge-core.
- (note by claude) Cross-station surface touch 2026-09-09 (foreign-spec reconcile): `pixi.toml` COMMENT-ONLY edits -- no dependency, version, floor, task command, or env membership changed. `pixi install --frozen -e local-recipes` still agrees with `pixi.lock`, and `pixi project export conda-environment -e build` is byte-identical, so `environment.yaml` needed no regeneration. Two comments were factually stale: (1) the `pr-artifacts` task description advertised `conda_pkgs_(linux|osx|win)`, omitting `conda_pkgs_noarch` -- the ONLY artifact carrying a `noarch:` recipe's package -- a real defect fixed in CFE v8.88.1 that had made the task return 0 packages for every noarch recipe since v8.14.0; (2) both `bmad-eval-quality` pin sites (`[feature.local-recipes.target.{linux-64,osx-arm64}.dependencies]`) claimed the `__win` artifact's Windows build/upload was outstanding, which stopped being true on 2026-09-09 when that variant was built on a GH Actions windows-2022 runner and uploaded to SelfExplainML (a staged-recipes PR builds it, tests it green, then discards it -- CFE G115). The version FLOOR deliberately stays at `>=0.2.0.dev0` even though the recipe is now 1.3.0: the floor already admits 1.3.0, while raising it would make `pixi.lock` stale and red every `pixi run --frozen` lane until a full re-lock -- a separate change with its own blast radius, now recorded inline so it is not re-litigated. RECONCILES: pixi.toml. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (change by claude) 2026-09-09 pixi.toml/pixi.lock reconcile — operator-directed dependency refresh, three changes in one pass. (1) `pixi upgrade --pinning-strategy latest-up` with 19 excludes: 14 floors moved (gh, gitpython, spec-kit, crispy-bootstrap5 x2, django-anymail x2, djlint, sse-starlette, copier, pnpm) and the lock followed. (2) pixi 0.79.0 -> 0.80.0 across all 17 registry sites via `bump-pixi-version -- 0.80.0` (the operator self-updated the machine); pixi-version-check is clean and no 0.79.0 remains anywhere. (3) bmad-suite currency: TEA floor >=1.24.0 -> >=1.25.0 (published), and bmad-eval-quality left the linux-64/osx-arm64 target tables for the shared table at >=1.3.0 (mason Story 14.1 — both noarch variants are now published, so the win-64 solve resolves h2fd06db_0; the lock diff shows exactly that new win-64 entry). CORRECTION APPLIED IN THE SAME PASS: latest-up STRIPPED five deliberate upper bounds, which was a real regression, not a refresh — openfeature-provider-flagd `,<0.5.1` (its own comment says 0.5.2 needs protobuf 7 and cannot solve with langflow's a2a-sdk), cachebox `,<6` x2, and asgiref `,<4.0` + channels_redis `,<5.0` (both under a byte-identity contract with pyforge-steward's pyproject `[dashboard]` extra, enforced by tests/meta/test_invariants.py, which failed until restored). All five caps restored; the lock still satisfies them (pixi lock --check clean) and the invariant suite is 17/17. RECONCILES: pixi.toml, pixi.lock. Scoped-stamped.
- (change by claude) 2026-09-09 (second pixi movement this pass): bmad-eval-quality floor >=1.3.0 -> >=1.4.1 and pixi.lock re-solved, now that BOTH noarch variants of 1.4.1 are published and indexed (`__unix` h07402fc_0 via macos-14, `__win` h2fd06db_0 via windows-2022). This is the floor rule stated in CFE G117 being exercised: the pin tracks the newest PUBLISHED build, never the newest the recipe builds -- it moved only once the artifacts existed. Lock resolves both variants; the one remaining 1.3.0 entry belongs to the bmad-suite-full env, which resolves through the PUBLISHED metapackage 2026.9.5 and so is expected. environment.yaml re-exported and unchanged. tests/packaging 173 passed / 2 skipped. RECONCILES: pixi.toml, pixi.lock. Scoped-stamped.
- (change by claude) 2026-09-09 (third pixi movement this pass): `networkx >=3.0` RESTORED to [feature.build.dependencies], so environment.yaml carries it again. Not a currency bump -- a missing runtime dep. `.ci_support/build_all.py` does `import networkx as nx` at line 8, and `.scripts/build_steps.sh` runs it from THIS env (it installs environment.yaml, exported from [feature.build]); the pin had been commented out, so every Linux Docker run died on `ModuleNotFoundError: No module named 'networkx'` before reaching a single recipe. That is a THIRD independent reason that lane could never build here, alongside the private-repo `git fetch` and the prune-everything-on-main selection -- all three pre-existing and all three found only by actually dispatching the lane. Restored WITHOUT the old `2.4.*` ceiling: it dates to 2019, its stated reason ("conda-build compat") no longer holds, and build_all.py uses only DiGraph/topological_sort. Strictly additive -- the pin was commented out, so nothing depended on it. Re-locked, environment.yaml re-exported (now carries `networkx >=3.0`); pixi lock --check, pixi-version-check, llms-full-check all clean and tests/packaging 173 passed / 2 skipped. RECONCILES: pixi.toml, pixi.lock. Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-10: 59 drift-presumed paths since baseline at `52bcab10e4` (the networkx-restore stamp). 140 commits landed fleet-wide in the interim (Epic 33 "token economy in effect", Epic 28 dispatch-context-layers, Epic 12 warden deploy-gate, Epic 48 steward platform, Epic 25 atlas, assorted fixes) — every one already reviewed and landed via its own PR, done in the tracked ledger. This entry is fleet hygiene, not a re-review. By commit:
  - `9bf5dc3470` — Story 33.1 (done): wired the real CAP-7 per-layer savings getters — `core/layer_savings_sources.py` (new), `core/token_economy_benchmark.py`, `supervisor/__main__.py`, `ports/harness.py`, `cli/status.py`.
  - `8ec6b89f7a` — Story 33.2 (done): wired 3 of 5 token-economy context layers into dispatch — `cli/dispatch.py`, `core/findings.py`, `core/verdict.py`, `data/harness_profiles/cursor.toml`, `scribe/cli.py`.
  - `afbafc8a03` — Story 33.3 (done): wire layer on factory spin via bmad-loop profile overlay — `cli/spin.py`, `core/harness_profile.py`.
  - `8182841e90` — fix (done, found resuming a stalled spin): substitute `{wire_port}` in the bmad-loop spin wire overlay — `core/harness_profile.py`.
  - `af15fa011a` — chore (done, same spin resume): gitignore `.bmad-loop/profiles/` — `seed/templates/files/model-ignores.gitignore.j2`.
  - `63e9987529` — Story 33.6 (done): feed adaptive tiering on all stations, dispatch floor-raise — `core/dispatch.py`, `core/dispatch_retry.py`, `core/findings.py`, `core/verdict.py`.
  - `d9f24c940a` — Story 33.8 (done): mint `dispatch.max_parallel`, prove first fan-out wave — `adapters/harness_bmadbuild.py`, `cli/config.py`, `core/harness_profile.py`, `core/policy.py`, `data/harness_profiles/claude.toml`, `schemas/policy.json`.
  - `40c75b8ab6` — Story 33.9 (done): guard factory dispatch with `verify_scope` — `cli/dispatch.py`, `core/verdict.py`, `scope.py`.
  - `5b33d94428` — Story 33.10 (done): derive the CFE pin from SKILL.md — `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py`.
  - `5363822028` — Story 28.32/28.33 (done): rolled derived-context+output fleet-wide, wired codegraph for spin; fixed a real byte/str-offset bug in `seed/detect/kit.py::_carve_out_state`.
  - `b34fc23319` — Story 28.27 (done): planning-graph fleet-wide + scribe recall project-scoping fix — `adapters/scribe_cli.py`, `cli/context.py`, `core/planning_graph.py`, `scribe/cli.py`, `scribe/recall.py`.
  - `ed164c29a0` — Story 28.25 (done): finalize escalations self-clear past a later success — `cli/dispatch.py`.
  - `c9fe812c98` — feat (done): sync `sprint-status.yaml` in refresh, detect drift in preflight — `cli/init.py`, `cli/refresh.py`, `core/findings.py`.
  - `0d600aad04` — Story 28.26 (done): CAP-4's own feed-sync guards missing keys too — `cli/land.py`.
  - `d83d34169c` — fix (done): add tmux as a pyforge-marshal env dependency — `pixi.toml`.
  - `3e5cb23926` — Story 48.5 follow-up (done): lint/format/mypy + read-only guard exemption — `src/shared/packages/pyforge-doctor/src/pyforge/doctor/__main__.py`, `actuators/__init__.py`.
  - `83c9bfc489` — Story 21.1 checkpoint (done): deferred_specs live-status reconciliation — `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py`.
  - `65c883c425` — marshal supervisor finalize (Story 28.24 automation, bundling a done station landing): `src/shared/packages/pyforge-steward/src/pyforge/steward/upgrade.py`.
  - `50a83feccd` — marshal supervisor finalize (Story 28.24 automation, bundling a done pyforge-warden Epic 12 landing): `src/shared/packages/pyforge-warden/src/pyforge/warden/cli.py`, `extract/__init__.py`, `extract/lockfiles.py`.
  - `d6eb4015ca` — marshal supervisor finalize (Story 28.24 automation, bundling a done Story 33.6 landing): `cli/gate.py`, `core/spec_low_risk.py`, `core/verdict.py`.
  - `b284c1034d` — marshal supervisor finalize (Story 28.24 automation, bundling a done Story 48.5 landing): `pixi.toml`, `src/shared/packages/pyforge-doctor/src/pyforge/doctor/__main__.py`, `actuators/__init__.py`, `actuators/flag_kill_switch.py` (new).
  - `b7fab216b2` — marshal supervisor finalize (Story 28.24 automation, bundling a done Story 44.13/memlog-fidelity landing): `pixi.toml`.
  - `caeae255d0` — marshal supervisor finalize (Story 28.24 automation, bundling a done pyforge-atlas dashboard-rework landing): `pixi.toml`, `src/shared/packages/pyforge-atlas/src/pyforge/atlas/dashboard/__init__.py`, `dashboard/app.py`, `views/__init__.py`, `views/asgi.py`, `views/cli_bridge.py`, `views/live.py`, `views/registry.py`, `views/render.py`, `views/resources.py`, `views/widgets.py` (`views/` package retired wholesale as the atlas MCP/dashboard rework's own reconciled surface — named here only because this spec's blanket glob co-governs `pyforge-atlas/src/**`; the rework itself is reconciled in full on pyforge-atlas's own spec).
  - `5bdfea9083` — marshal supervisor finalize (Story 28.24 automation, bundling a done pyforge-atlas MCP-parity landing): `src/shared/packages/pyforge-atlas/src/pyforge/atlas/__main__.py`, `mcp/parity.py` (new), `mcp/tools.py`, `src/shared/packages/pyforge-core/src/pyforge/core/dispatch.py`, `src/shared/packages/pyforge-core/tests/meta/test_cli_parity_matrix.py`.
  - `5eff1f7424` — THIS story (Story 8.1, pyforge-scribe nightly-compile trigger): `pixi.toml` gained `[feature.pyforge-scribe.tasks.scribe-nightly-compile]` and `[feature.pyforge-ci.tasks.scribe-graph-freshness-check]` — unrelated to this spec's own CAP text; pixi.toml is co-governed per this memlog's established multi-surface convention. Reconciled in full on pyforge-scribe/spec-pyforge-scribe.
  No CAP text change. RECONCILES: all 59 paths above plus `pixi.toml`. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (event by claude) Surface reconcile 2026-09-12 (fleet hygiene, discovered via mason 15.1's `20260910-100021-1dbc` failed verify -- this repo-level gate reds every dispatch fleet-wide until cleared, per [[feedback_foreign_spec_surface_finding_reconcile]]): 37 governed paths changed by already-landed, already-merged work since the last stamp, none of it new product work here -- accepting the current tree as the post-land surface, grouped by area:
  - `pixi.toml` -- routine cross-fleet dependency/task churn (broad surface, expected to move often).
  - Doctor `sources/` (Epics 21-22): `models.py`, `sources/__init__.py`, `sources/__main__.py`, `sources/board.py`, `sources/chain.py`, `sources/sibling_dreams.py`, plus three new source modules `sources/capability_effect.py` (Story 21.1, the realization-gate check), `sources/pixi_currency.py` (pixi-candidate-currency), `sources/status_body_consistency.py` and `sources/general_docs_consistency.py` (spec-status-body-consistency / spec-general-docs-consistency, both this pass's siblings), and `data/report-schema.json` (schema fields for the new sources).
  - Herald: `station_api.py` (new, the `/stations/herald/mcp` face) and `webhook.py` (Epic 13/19 live-backend work).
  - Marshal `cli/`+`core/`: `cli/benchmark.py`, `cli/checkpoint.py` (Story 34.2), `cli/dispatch.py`, `cli/spin.py`, `core/dispatch_fleet.py`, `core/dispatch_retry.py`, `core/findings.py`, `core/gate.py`, `core/promotion.py` (incl. the 2026-09-11 `2267f21bb3` cross-project-collision fix), `core/structure_graph_dispatch_benchmark.py` (new), `core/verdict.py`, `core/worktree_checkpoint.py` (new), `dispatch_supervisor/__main__.py`, `dispatch_verify.py`, `supervisor/__main__.py` -- Epic 22 (dispatch supervision) and Epic 28/33 (token economy) surface, both already governed here.
  - Mason: `engines/__init__.py`.
  - Scribe: `cli.py`, `embeddings.py` (semantic recall work, `spec-pyforge-scribe`'s own memlog reconciled separately below).
  - Steward: `dashboard/__init__.py`, `dashboard/asgi.py`, `dashboard/consumers.py`, `dashboard/routing.py` (new -- the live browser-streaming Manticore Studio work, Epic 48), `suite.py`.
  No CAP text change; no file outside this spec's existing declared surface. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (note by claude) BUG found in this spec's own governed `sources/chain.py` during the reconcile above (not fixed here — out of scope for a hygiene sweep, flagged for a future doctor story): `run_git(target, ["ls-files"])` (`chain.py:1545`) never passes `-c core.quotepath=false`, so git quote-escapes any tracked filename with a non-ASCII byte (e.g. `di\303\241taxis`) into a literal `"...\NNN..."` string that can never match a surface glob or an allowlist pattern written in real UTF-8 — the file reports `[ungoverned]` no matter what. Reproduced live via `_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/spec-22-4-a-diátaxis-...md`; worked around this pass by renaming the file to ASCII (`diataxis`), not by patching the git invocation.
- (event by claude) Surface reconcile 2026-09-12 (mason 15.1 recovery landing, same day as the sweep above): `pixi.toml` changed again — a two-line comment/description reword on the `test-ci` task (Story 12.2's comment + the task's own `description:`), replacing a stale reference to the now-deleted slice-equivalence harness with the accurate "slow tests (e.g. test_cve_manager.py)" wording. Purely descriptive; no task behavior change. No CAP text change. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (change by claude) 2026-09-12, same pass: fixed the exact bug this spec's own memlog flagged one entry above (the BUG note), applied to `sources/factory.py` instead of `sources/chain.py` — a real, separate coverage gap the mason 15.1 recovery's own diff exposed: `pixi run -e local-recipes pytest .claude/skills/conda-forge-expert/tests` reds on `test_bmad_artifacts_in_sync.py::test_bmad_artifacts_integrity` (a HARD `uncovered` finding from `bmad_drift_factory.gather()`) for 5 tracked-but-unclassified files that predate this session and are unrelated to it: `planning-artifacts/benchmarks/structure-graph-dispatch-28-31.json` (Story 28.31, new `benchmarks/` directory convention) and 4 flat spec files under `planning-artifacts/specs/` whose slugs or filenames the existing `classify()` regexes rejected — two for an underscore in the slug (`...max_parallel-key.md`, `...station_state-sibling-has.md`, since the character class was hyphen-only) and one for being a flat spec's OWN `.memlog.md` sibling filed beside it rather than inside a `spec-<name>/` folder (`spec-33-3-....md` + `spec-33-3-....memlog.md`). Fixed per user direction ("fix pre-existing failures... no broken windows... clean as you go") rather than left as someone else's debt: widened the flat-spec regex to `[a-z0-9_-]+`, added a new flat-memlog-sibling rule, and added a new `planning-artifacts/benchmarks/[a-z0-9-]+\.json` rule classified `archive:benchmark` (same one-off-frozen-record treatment as the existing spike-report/readiness-assessment rules it sits beside). 3 new regression tests added to `test_sources_factory.py` mirroring the existing spike-report test shape. Separately, `test_sources_chain_dreams_hygiene.py::test_live_tree_dream_readme_missing_count` (a live-measurement snapshot, not a code gap) had drifted 65→66 since its 2026-09-11 stamp — re-stamped to 66 per that test's own re-measurement convention; the extra missing Dream is unrelated to this session and not chased down. Full doctor suite green (1,626 passed) after both fixes; `pixi run -e local-recipes pytest .claude/skills/conda-forge-expert/tests` green. RECONCILES: `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/factory.py`, `src/shared/packages/pyforge-doctor/tests/unit/test_sources_factory.py`, `src/shared/packages/pyforge-doctor/tests/unit/test_sources_chain_dreams_hygiene.py`. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (capability by claude) Story 33.4 landed 2026-09-12 (`feat/marshal-33-4-one-publisher-run-state`), spec-marshal-token-economy CAP-18 + CAP-7's mechanical core (PARTIAL scope against spec-run-state-one-publisher's own CAP-1..5 — full detail + what's deferred recorded in that Spec's own memlog, not duplicated here). New: `adapters/publisher_host.py`, `ports/publisher.py`, `core/publish.py` — the single publisher, reaching `django_pyforge.supervisor` over `/stations/marshal/mcp` via `pyforge.core.client`, never a direct `django_pyforge` import (`grep -r django_pyforge src/shared/packages/pyforge-marshal/src/` returns nothing; `tests/meta/test_publisher_single_importer.py` proves exactly one importer). Both supervisors (`supervisor/__main__.py`, `dispatch_supervisor/__main__.py`) wired to publish/heartbeat/complete, best-effort — publish failures become a new `MRS-SUPV-010` journal finding (registered in `core/findings.py` + `core/verdict.py`, `Verdict.WARN`) and never stop the loop; `core/egress.py`'s `EGRESS_PORTS` gains `RunPublisherPort: False` (network lives in the adapter, the port only carries shaped facts). No CAP text change on this spec. RECONCILES: `adapters/publisher_host.py`, `ports/publisher.py`, `core/publish.py`, `core/egress.py`, `core/findings.py`, `core/verdict.py`, `supervisor/__main__.py`, `dispatch_supervisor/__main__.py`. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (capability by claude) Story 33.12 landed 2026-09-12 (spec-run-state-one-publisher CAP-1/2/4/5, continuing Story 33.4's mechanical core). New: `pyforge-core/src/pyforge/core/published_loop.py` (`fetch_story_tasks(slug)`) -- the read side of the publisher, calling the host's `list_loop_story_tasks` MCP tool over `/stations/marshal/mcp` via `pyforge.core.client.PyForgeStationClient` + `pyforge.core.assertion.HostMintClient`, returning `None` (never raising) when the plane is unreachable so every caller degrades to its existing loop-home file fallback. Marshal re-exports it locally at `adapters/published_plane.py` (a thin alias, no new logic) for import-boundary consistency with the rest of the adapters package. Consumers re-pointed at the published plane first, loop-home file second (CAP-4): `cli/init.py::published_story_tasks`, `cli/spin.py::_prior_attempt_keys`, and doctor's `sources/marshal.py::_harness_tasks`/`gather_story_status` (this spec's own surface glob already co-governs that one doctor file, per its `[drift]` attribution here rather than to a doctor-owned spec; both `_harness_tasks` and `gather_story_status`'s loop-home-root fallback gained `# CAP-4: loop-home FILE read` tags at the sites that remain filesystem-based). New `cli/login.py` (CAP-5): `pyforge marshal login <persona>` wraps the local-dev mint path (`python -m config.local_dev.mint`) and writes `PYFORGE_IDP_BEARER_FILE` at 0600; registered in `cli/main.py` and added to `mcp/parity.py`'s `CLI_ONLY_VERBS` (operator-only, no MCP equivalent). `adapters/publisher_host.py` gained no new import surface, only a small internal adjustment to support the read path's shared transport helpers. On the host side (django-pyforge/django-marshal, steward's surface -- incoming claim already recorded on `spec-pyforge-unifying-strategy`'s memlog ahead of this landing): `django_pyforge.supervisor` gained the held-run publish/heartbeat/complete path (`celery_task_id=""`, judged by heartbeat age only) and the per-tool `MAX_RUNNING_PER_SUB` override for the loop-publish tool; `django-marshal`'s `mcp_asgi.py` registers the three tools this reader and Story 33.4's writer both call (`publish_loop_run`, `heartbeat_loop_run`, `complete_loop_run`) plus the new `list_loop_story_tasks` read tool. No CAP text change on this spec (spec-run-state-one-publisher's own memlog carries the full capability narrative, PARTIAL-vs-full-scope accounting, and what remains open). RECONCILES: `pyforge-core/src/pyforge/core/published_loop.py`, `pyforge-marshal/src/pyforge/marshal/adapters/published_plane.py`, `pyforge-marshal/src/pyforge/marshal/adapters/publisher_host.py`, `pyforge-marshal/src/pyforge/marshal/cli/init.py`, `pyforge-marshal/src/pyforge/marshal/cli/login.py`, `pyforge-marshal/src/pyforge/marshal/cli/main.py`, `pyforge-marshal/src/pyforge/marshal/cli/spin.py`, `pyforge-marshal/src/pyforge/marshal/cli/status.py`, `pyforge-marshal/src/pyforge/marshal/mcp/parity.py`, `pyforge-doctor/src/pyforge/doctor/sources/marshal.py`. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-pyforge-core`.
- (note by claude) Cross-station surface touch (steward Story 49.8, 2026-09-12): comment-only reword at `pyforge-doctor/src/pyforge/doctor/sources/__init__.py:223` noting gather_story_status now reads the published plane first (marshal Story 33.12) with ~/.bmad-loops fallback only when unreachable -- Unifying CAP-17 retirement is landed. No behavior change; full narrative reconciled on spec-pyforge-doctor's own memlog. RECONCILES: `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/__init__.py`.
- (event by claude) Surface reconcile 2026-09-12: 6 drift-presumed paths from the dispatch-tier-routing-fails-safe fix (6113ac34f2, 97ca64d1e3) -- resolve_stage_candidate's fail-safe fix (tier_routing.py), the new provider_declaring_model reverse-lookup (model_cost.py), render_policy_toml's + cli/dispatch.py's provider-mismatch guards (harness_bmadloop.py, dispatch.py), and the new MRS-DISP-043 finding (findings.py, verdict.py). Already governed and freshly reconciled by the new spec-dispatch-tier-routing-fails-safe. No CAP text change -- routine kernel double-coverage, this station's own established pattern (spec-surface-overlap-tolerance, dreamt the same day). RECONCILES (via surface-drift-exclude): adapters/harness_bmadloop.py, cli/dispatch.py, core/findings.py, core/model_cost.py, core/tier_routing.py, core/verdict.py. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-pyforge-core.
- (event) 2026-09-13 overlapping surface: scribe Epic 8–12 changed src/shared/packages/pyforge-scribe/src/pyforge/scribe/{cli,compile,graph_store,recall}.py extras/graphify.py; Story 12.1 touched planning_graph.py; pixi.toml gained graphifyy on feature.pyforge-scribe. Accept; no marshal product change.
- (event) 2026-09-13 spec-surface: src/shared/packages/pyforge-scribe/src/pyforge/scribe/compile.py — scribe Story 13.1 planning pointers. No pyforge-core CAP change.
- (event) 2026-09-13 spec-surface: src/shared/packages/pyforge-scribe/src/pyforge/scribe/compile.py, src/shared/packages/pyforge-scribe/tests/unit/test_compile.py — Story 14.1 named docs extras (how-tos + library-catalog extract). This spec CAP unchanged.
- (event) 2026-09-13 spec-surface: src/shared/packages/pyforge-scribe/src/pyforge/scribe/extras/graphify.py, src/shared/packages/pyforge-scribe/src/pyforge/scribe/cli.py, src/shared/packages/pyforge-scribe/src/pyforge/scribe/compile.py, src/shared/packages/pyforge-scribe/tests/unit/test_extras_graphify.py, .claude/skills/pyforge-scribe/active/pyforge-scribe/SKILL.md — Story 15.1 named graphify target list. This spec CAP unchanged.
- (event) 2026-09-13 spec-surface: src/shared/packages/pyforge-scribe/src/pyforge/scribe/recall.py, src/shared/packages/pyforge-scribe/src/pyforge/scribe/cli.py, src/shared/packages/pyforge-scribe/tests/unit/test_recall.py, src/shared/packages/pyforge-scribe/tests/unit/test_cli.py, AGENTS.md, .claude/skills/pyforge-scribe/0.1.0/pyforge-scribe/SKILL.md — Story 16.1 --mode bags. This spec CAP unchanged.
- (event by claude) Surface reconcile 2026-09-14 (bulk, post-outage): this Spec's surface overlaps every station's own source tree, so it picks up drift from several independent, already-landed, already-reviewed stories whose OWNING specs reconcile the substance -- this entry names them rather than re-narrating: d021af5df0 (cutover flag reader + replay harness: pyforge-core/cutover_root.py + its test, steward/cutover.py + cli.py), c8f4eb44f8 (steward/track.py, per-run track.json assembly), 39ff0ad964 (steward/guards.py, Hub Guard library exposure), 18f8823cbb (steward/frames.py + data/track.schema.json, Frames v0.3/Epic 55), f30fa3d846 (steward/provision.py, suite-skip authoring tool), 797ceb0d01 (marshal/core/planning_graph.py, retrieve wired to scribe --mode planning), 932d03da84 (scribe/extras/graphify.py, Scribe Epic 17 / Hub CAP-1..4), c48a65e8ad + 9d7528297866 (doctor's capability-ledger detector, reconciled in spec-pyforge-doctor's own memlog), f180624fd8 + local-recipes PR #1354's own AD-14 fix (mason/cfe.py + recipe.py, reconciled in spec-pyforge-mason's own memlog). Plus pixi.toml (routine dependency/task additions, PR #1354). No pyforge-core sole-ownership or optional-dependency mandate violated by any of these -- none adds a new hard cross-package import. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-pyforge-core.
- (event by claude) Surface reconcile 2026-09-14: board.py's INV-A gained a delivered-Spec branch. It previously skipped EVERY non-open Spec status (`if status not in OPEN_SPEC_STATUSES: continue`), so a `shipped` Spec delivered with no epic and no ledger row was structurally invisible -- ten existed fleet-wide. New constants DELIVERED_SPEC_STATUSES/DECOMPOSITION_OWED_STATUSES add `shipped` only; `absorbed`/`archived`/`superseded`/`extension-point` stay exempt for stated reasons. The delivered branch is held to the WHOLE-SPEC standard (does any FR or epic reference it at all), never INV-A's per-CAP citation test -- per-CAP flagged 34 Specs against 12 real ones because epics predating the cite-every-CAP-id convention name the Spec or its Dream without enumerating CAP-n. Matching also accepts an owner-dream path, since Epic 51 claims its Spec that way. Open-Spec behaviour is byte-unchanged. Also registered spec-vocabulary-one-name-one-job in DEFERRED_SPECS (operator-owned open questions). Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14: three foreign paths in this Spec's surface moved, none changing its own contract. (1) `board.py` -- INV-B epic arm (doctor DW-CHAIN-COMPLETENESS-7), comparing `## Epic N` headings against `epic-N` ledger keys both directions; fixed marshal's own Epic 29, which had no heading at any level and whose absence marshal's `epics.md` frontmatter had already admitted in prose ("183 (181 + Epic 29's two, never counted)"). (2) `frames.py` -- steward Story 53.6 re-keyed the Frame preflight from `name` onto the v0.3 qualified-ref `identifier`. (3) `pixi.toml` -- the `frame-preflight` description plus the `pyforge-station-tests` repair (it had shipped as a bash `for` loop pixi's shell cannot parse and had never run; now a declarative cross-environment `depends-on`, verified green across all 8 stations). `environment.yaml` re-exported byte-identical. Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (PR #1355 CI remediation): `pixi.toml` gained nine tasks and `board.py` gained coverage pragmas; neither touches this Spec's contract. (1) Eight `pyforge-<station>-coverage-gate` tasks plus a `pyforge-station-coverage-gates` aggregate now mirror `.github/workflows/coverage-gates.yml`'s `named-module-gates` matrix locally -- that gate measures only the modules a branch TOUCHES, so `pyforge.doctor.sources.board` had sat at 70.1% against an 80% floor since the Guildhall console was retired and only surfaced when an unrelated edit touched the file. (2) A `pr-preflight` aggregate runs the four lanes that actually red a PR (`detectors-ci`, `test-ci`, `pyforge-station-tests`, `pyforge-station-coverage-gates`), because two CI failures on #1355 had no local equivalent at all. (3) `board.py`'s retired-Guildhall machinery is marked `# pragma: no cover` with a block comment stating why -- it cannot execute while `retired-console-check` fails CI on reintroduction; module coverage went 72% -> 94% with no test or behaviour change, tracked for deletion as `DW-DASHBOARD-DEAD-CODE-1`. `environment.yaml` re-exported and byte-identical (task-only edits). Scoped-stamped.
- (event by claude) Surface reconcile 2026-09-14 (post-merge of PR #1355): `board.py` registered `spec-coverage-gate-independence` in `DEFERRED_SPECS` — a one-key dict addition, no behaviour change to this Spec's contract. Seeding that Spec in #1355 reddened `chain-completeness` INV-A on main (doctor owned an open `draft` Spec no epic referenced); it is not decomposable because its five open questions are design decisions about where a fleet-wide blocking gate may live, and every obvious home is already ruled out. Full entry in spec-pyforge-doctor's memlog. Scoped-stamped.
- (change by claude) Surface reconcile 2026-09-14: `board.py` lost **494 lines** of retired-Guildhall dead code (ten functions, ten `_LAYOUT_*` constants) with **zero behaviour change**, closing `DW-DASHBOARD-DEAD-CODE-1`. An AST call-graph pass proved they formed two closed PRIVATE ORPHAN islands -- the layout chain (`_run_check_layout`, called by nothing) and the drift chain (`_check_dashboard_drift`, called by nothing) plus two standalone orphans -- so ten of eleven suspects had no live caller at all. Both gathers, their DISPATCH entries, their `Source` taxonomy members and `report-schema.json` are untouched; the ledger entry had over-stated the scope as "a behaviour change to two registered sources plus taxonomy and schema", inferred from the code's location rather than measured from its call graph, which is why it had been deferred as needing a Dream when it was hygiene. Verified by byte-comparing `--json` findings for `dashboard-drift`, `check-layout`, `chain-completeness` and the `--layers` audit before and after: all four IDENTICAL. Full doctor suite green (1378 passed). Coverage 72% (pre-pragma) -> 94% (pragma) -> **95% with the code actually gone**. Only `_load_dashboard_generate` survives, genuinely reachable from `_gather_chain_layers_audit`; its pragma stays because its reachable path is closed by policy (`retired-console-check` fails CI on reintroduction), not by structure. Stale prose rewritten rather than left pointing at deleted helpers. Scoped-stamped.
- (event) Surface reconcile 2026-09-14: doctor board.py DEFERRED_SPECS gained two steward draft Specs. Co-governor of that path; no CAP change. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-pyforge-core.
- (change by claude) Surface reconcile 2026-09-14 (later the same day, operator rulings pass): de-registered `spec-coverage-gate-independence` from `board.py`'s `DEFERRED_SPECS` — the entry registered this morning is now stale by its own invariant ("remove stale entries rather than leaving them inert"). The operator ruled the Spec's home the Guild (Charter §5 amendment), so it moved by `git mv` to `docs/governance/spec-coverage-gate-independence/` — outside `pa.glob("specs/spec-*")`, exactly as `spec-pyforge-charter` is — and its mechanism stories are doctor Epic 23 (23.1-23.3, `backlog`), which INV-A can see. `src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py` is the one governed file that changed: the dict entry became a dated comment explaining the de-registration; no logic changed. Scoped stamp for this key follows (`--write-baseline --spec <key>`), never bare.
- (change) 2026-09-15: board.py DEFERRED_SPECS dropped spec-vocabulary-one-name-one-job (steward Epic 59). Co-governor; no CAP change. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py.
- (change) 2026-09-15: board.py DEFERRED_SPECS dropped spec-self-hosted-bmad-marketplace (steward Epic 60). Co-governor; no CAP change. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py.
- (change) 2026-09-15: board.py DEFERRED_SPECS dropped spec-work-passports-dated-extracts (steward Epic 61). Co-governor; no CAP change. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py.
- (change) 2026-09-15: board.py DEFERRED_SPECS dropped spec-build-league-scorecard (steward Epic 62). Co-governor; no CAP change. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py.
- (event) 2026-09-15 incoming surface (marshal Epic 45 / spec-bmad-cursor-interactive-routing): drop DEFERRED_SPECS entry for spec-bmad-cursor-interactive-routing after CAP-1 PASS. Paths: src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/board.py, src/shared/packages/pyforge-doctor/tests/unit/test_sources_board.py. No product change on this spec.
- (event) 2026-09-15 incoming surface (herald Epic 21 recover): pixi.toml gained a deck-trio task only. No product change on this spec. Paths: pixi.toml.
- (event) Surface reconcile 2026-09-16 Marshal leftover 42/45: named in this finding — chain.py / test_sources_chain_spec_surface.py / pixi.toml / scripts/bmad_cursor_mdc_check.py / tests/scripts/test_bmad_cursor_mdc_check.py / .cursor/rules/bmad-build.mdc / .cursor/rules/bmad-build-auto.mdc / sprint-status-ledger.yaml / epics.md as applicable.
- (decision by operator) 2026-09-16 board.py surface move: DEFERRED_SPECS gained spec-token-economy-claude-session-path (draft seed; OQs answered 2026-09-16 in its own memlog; decomposition mints with the re-derive, not by hand). Same entry recorded on spec-pixi-candidate-currency and spec-pyforge-doctor, the other two surfaces governing board.py. Part of the operator-ruled token-savings Dream consolidation.
- (decision by operator) 2026-09-16 board.py surface move (second today): DE-registered spec-token-economy-claude-session-path from DEFERRED_SPECS — the operator ruled no satellite Specs, so it folded into spec-marshal-token-economy as CAP-19..CAP-24 (status superseded, terminal — not owed) with marshal Epic 46 (46.1-46.10) as the dispatch home. The morning registration comment stays as the record.
- (change) 2026-09-16 overlap-surface note (doctor Epic 25, spec-one-chain-per-station): doctor's models.py Source enum gains CHAIN_SPRAWL / FR_WITHOUT_CAP, report-schema.json enum extended additively, new pyforge/doctor/rekey.py + sources/one_chain.py, ledger.py/marshal.py read the fold PR's re-key map. No pyforge-core contract touched; recorded because spec-pyforge-core's surface overlaps doctor's package (Epic 42 OR semantics).
- (decision) 2026-09-16 fold-exemption: cross-station-seam — declared seam (kernel / testing kit); survives the marshal one-chain fold; marshal stays accountable; not absorbed.
- (event) 2026-09-18 co-governor note: herald Story 23.3/23.4 recovery (spec-pyforge-herald's own memlog carries the full entry) touched seven pyforge-herald package files this spec also governs. No pyforge-core contract touched; recorded because spec-pyforge-core's surface overlaps herald's package. RECONCILES: src/shared/packages/pyforge-herald/src/pyforge/herald/cli.py, src/shared/packages/pyforge-herald/src/pyforge/herald/deck_pipeline.py, src/shared/packages/pyforge-herald/src/pyforge/herald/errors.py, src/shared/packages/pyforge-herald/src/pyforge/herald/registry.py, src/shared/packages/pyforge-herald/src/pyforge/herald/stamps.py (new), src/shared/packages/pyforge-herald/src/pyforge/herald/transport/base.py, src/shared/packages/pyforge-herald/src/pyforge/herald/transport/mcp_transport.py. Scoped stamp follows: --write-baseline --spec pyforge-marshal/spec-pyforge-core.
- (event by claude) Surface reconcile 2026-09-18 (steward Story 59.2's own verify red on this repo-wide gate, unrelated to that story's own diff -- reconciled here by name, same pattern as this memlog's other co-governor entries). Commit `1b5be7990e` ("fix(core): point atlas prep-spec at story 24.3 for parity matrix") changed `src/pyforge/core/dispatch.py` and `tests/meta/test_cli_parity_matrix.py`: `PREPARATORY_UNINTROSPECTABLE` named a non-existent `spec-25-3` path; corrected to the live story `24.3`, unblocking `pip-install` on PRs touching shared surface. Already landed on `main`, unrelated to this story's own diff. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-pyforge-core.
- (event by claude) 2026-09-18 co-governor note: bmad_loop_baseline_drift_check recovery of atlas Stories 13.1/13.2/13.3 (spec-pyforge-atlas's own memlog carries the full entry) added seven pyforge-atlas package files this spec also governs (views/ depends on pyforge-core's atomic_write/atomic_write_text). No pyforge-core contract touched; recorded because spec-pyforge-core's surface overlaps atlas's package. RECONCILES: src/shared/packages/pyforge-atlas/src/pyforge/atlas/views/__init__.py, src/shared/packages/pyforge-atlas/src/pyforge/atlas/views/asgi.py, src/shared/packages/pyforge-atlas/src/pyforge/atlas/views/cli_bridge.py, src/shared/packages/pyforge-atlas/src/pyforge/atlas/views/live.py, src/shared/packages/pyforge-atlas/src/pyforge/atlas/views/registry.py, src/shared/packages/pyforge-atlas/src/pyforge/atlas/views/render.py, src/shared/packages/pyforge-atlas/src/pyforge/atlas/views/widgets.py. Scoped stamp follows: --write-baseline --spec pyforge-marshal/spec-pyforge-core. (The 8th finding in this same reconcile run, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/layer_savings_sources.py, pre-existed before this recovery and is left unreconciled here -- unrelated, not this change's own drift.)
- (event by claude) 2026-09-18 co-governor note: herald Story 23.1 (`spec-design-sync-loop` CAP-1, "the account is enumerated and reconciled against the registry"; spec-pyforge-herald's own memlog carries the full entry) touched seven pyforge-herald package files this spec also governs. No pyforge-core contract touched; recorded because spec-pyforge-core's surface overlaps herald's package. RECONCILES: src/shared/packages/pyforge-herald/src/pyforge/herald/cli.py, src/shared/packages/pyforge-herald/src/pyforge/herald/deck_pipeline.py, src/shared/packages/pyforge-herald/src/pyforge/herald/registry.py, src/shared/packages/pyforge-herald/src/pyforge/herald/transport/__init__.py, src/shared/packages/pyforge-herald/src/pyforge/herald/transport/agent_sdk_transport.py, src/shared/packages/pyforge-herald/src/pyforge/herald/transport/base.py, src/shared/packages/pyforge-herald/src/pyforge/herald/transport/mcp_transport.py. Scoped stamp follows: --write-baseline --spec pyforge-marshal/spec-pyforge-core.
- (event by claude) Surface re-stamp 2026-09-18 for herald Story 23.1's landing (PR #1459, `1dd017bc21 Merge pyforge-herald/23-1 into main`): the co-governed herald paths `herald/cli.py`, `herald/deck_pipeline.py` and `herald/registry.py` were patched again by bmad-build-auto's review pass after the story's own reconcile entry above was stamped (checkpoints `86fe4d482c`..`506fc1b9dd`), so main landed with `spec-surface: drift` on this spec too. Nothing pyforge-core-facing changed beyond that entry — the review patches tightened herald-local status classification and README exclusion parsing; no `pyforge.core` import, port or seam moved. Re-stamped scoped to this spec only.
- (event by claude) Co-governor note 2026-09-18 for herald Story 23.2's landing (PR #1461, `489a2d2d19 Merge pyforge-herald/23-2 into main`): `herald/deck_pipeline.py` grew the adopt path (+294 — windowed `read_file` pagination and byte-exact twin/library pulls; see spec-pyforge-herald's own memlog entry). Herald-local; no `pyforge.core` import, port or seam moved. Re-stamped scoped to this spec only.
- (event by claude) Co-governor note 2026-09-18 for herald Story 23.6's landing (PR #1465, `fbe1bffc92 Merge pyforge-herald/23-6 into main`): new `herald/sync_all.py` (+686) and `herald/cli.py`'s `deck sync-all` verb (+73) — the one-command Design sync loop composing the kernel's bridge verbs (see spec-pyforge-herald's own memlog entry). Herald-local; no `pyforge.core` import, port or seam moved. Re-stamped scoped to this spec only.
- (event by claude) Co-governor note 2026-09-18 for herald Story 24.1's landing (`fb2e983f96 Merge pyforge-herald/24-1 into main`; spec-pyforge-herald CAP-48): `herald/deck_pipeline.py`'s `_windowed_read` gains the stalled-window guard (a typed pagination error when `last_line` does not advance). Herald-local; no `pyforge.core` import, port or seam moved. Re-stamped scoped to this spec only.
- (event by claude) Co-governor note 2026-09-18 for marshal Story 50.2's landing (PR #1474, `b0e23e2f2b Merge pyforge-marshal/50-2 into main`; spec-pyforge-marshal CAP-245): `marshal/core/harness_session.py`'s quota markers became a per-harness table (`_QUOTA_MARKERS_BY_HARNESS`) covering Cursor's live 'out of usage' wording beside Claude's. Marshal-local; no `pyforge.core` import, port or seam moved. Re-stamped scoped to this spec only.
- (event by claude) Co-governor note 2026-09-18 for herald Story 24.3's landing (`spec-pyforge-herald` CAP-50, "the second `sync-all` run is proven unchanged on a real deck"; spec-pyforge-herald's own memlog carries the full entry): `herald/sync_all.py` gains an opt-in `proof_dir` seam (`_report_to_dict`/`_write_proof`) that serializes each deck's report plus a `stamps.write_stamp` sidecar under a proof dir, and `herald/cli.py`'s `sync-all` verb gains `--proof-dir` gated on `HERALD_LIVE_SYNC_PROOF=1`. Herald-local; no `pyforge.core` import, port or seam moved. Re-stamped scoped to this spec only.
- (event by claude) Surface re-stamp 2026-09-18 for herald Story 24.3's landing: the story's own reconcile above was stamped, then bmad-build-auto's review pass patched `herald/sync_all.py` again (an unwrapped `OSError`, missing per-deck proof-write isolation, a bypassed clock seam, and a filename-collision guard, all fixed) and `herald/cli.py` (a proof-dir output-path print line added) — a stamp is only valid until the next edit of any file in the surface, so this landed with `spec-surface: drift` on this spec again. Nothing `pyforge.core`-facing changed beyond the entry above; the review patches tightened herald-local error handling and output only. Re-stamped scoped to this spec only.
- (event by claude) Co-governor note 2026-09-18 for doctor Story 27.5's landing (PR #1484, `f33e9f0966 Merge pyforge-doctor/27-5 into main`; spec-pyforge-doctor CAP-80, spec-pyforge-doctor's own memlog carries the full entry): new `doctor/bare_merge.py` (bare-form merge attribution by first-parent diff paths) plus `doctor/sources/marshal.py` and `doctor/sources/ledger.py` wired through it. Doctor-local; no `pyforge.core` import, port or seam moved. RECONCILES: src/shared/packages/pyforge-doctor/src/pyforge/doctor/bare_merge.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/marshal.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/ledger.py. Re-stamped scoped to this spec only.
- (event by claude) Co-governor note 2026-09-18 for marshal Story 50.4's landing (PR #1487, `3e2859425c Merge pyforge-marshal/50-4 into main`; spec-pyforge-marshal CAP-247, spec-pyforge-marshal's own memlog carries the full entry). This one DOES move a pyforge-core contract: `core/landing_evidence.py` — `parse_templated_merge_subject(subject, template, project_slug)` and `classify_commit(..., project_slug=)` fill a `{slug}` token in the template (`_instantiate_slug`; a template without it passes through unchanged), and the bare `Story N.M:` direct-commit shape needs branch corroboration (`_branch_belongs_to_project`; DW-FU-50-4: no production caller supplies branch data yet). Every in-repo caller updated (marshal promotion/identity/dispatch_land, doctor sources/bare_merge). Co-governed marshal package paths: core/identity.py, core/policy.py, core/promotion.py, cli/deploy.py, cli/land.py, dispatch_land.py. RECONCILES: src/shared/packages/pyforge-core/src/pyforge/core/landing_evidence.py, src/shared/packages/pyforge-core/tests/unit/test_landing_evidence.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/identity.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/promotion.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/policy.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/deploy.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/land.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_land.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/bare_merge.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/ledger.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/marshal.py. Re-stamped scoped to this spec only.
- (event by claude) Co-governor note 2026-09-19 for marshal Story 50.5's landing (PR #1490, `62047f2834 Merge pyforge-marshal/50-5 into main`; spec-pyforge-marshal CAP-248, spec-pyforge-marshal's own memlog carries the full entry): `marshal/core/promotion.py` and `marshal/core/spec_surface.py` gain a leading-banner skip before their frontmatter fence check. Marshal-local; no `pyforge.core` import, port or seam moved. RECONCILES: src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/promotion.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/spec_surface.py. Re-stamped scoped to this spec only.
- (decision by claude) Status 2026-09-19: `draft` was stale — marshal Epic 14 (Stories 14.1–14.4, all `done`) realized CAP-1..CAP-7 in August 2026 and this memlog has carried co-governor landing notes since; with CAP-8/CAP-9 minted below the contract is `ready` (open capabilities exist), not `draft` and not yet `shipped`.
- (capability by claude) CAP-8 — the floor is enforced where the build happens. Intent: the conformance suite — the whole `pyforge-core` `tests/` tree, the four sole-ownership meta-tests included — runs on every PR that touches `src/shared/packages/**` and inside `pr-preflight`, so a CAP-5/CAP-7 violation reds the PR that introduces it instead of accumulating on main. Source: docs/dreams/pyforge-marshal.md 2026-09-18 (later) item (8), corrected on the evidence: #1086's retirement of `pyforge-core.yml` was coverage-neutral — that lane and its successor `pyforge-pip-install.yml` both run the same enumerated subset (`tests/unit` + three meta files), and `tests/meta/*_sole_ownership.py` (added 2026-08-12/13) were never wired into any workflow; no workflow invokes the `pyforge-core-test` pixi task at all (`pyforge-station-tests.yml` lists `pyforge-core/**` only as a fan-out trigger for the eight station jobs). CAP-7's own success reads 'fails the build' — this is the realization gap.
- (capability by claude) CAP-9 — the six accumulated violations are cleared. Intent: the six CAP-5/CAP-7 conformance failures live on main today (`pixi run --frozen -e pyforge-core pyforge-core-test` → 6 failed / 1855 passed, 2026-09-19: exception root — marshal `adapters/oidc_pkce.py`, `cli/watch.py`, warden `tea_advisory.py`; second subprocess implementation — marshal `cli/login.py`, `cli/refresh.py`, testing-kit `branch_diff_guard.py`) are cleared the way CAP-5 and CAP-6 prescribe — re-parent to the core exception root without widening any `except` clause, route through the core subprocess guard — so the suite is green on main before CAP-8's lane is born red. Source: the same Dream item (8); the failures surfaced only because 50.4's hand-merge was verified on that suite.
- (constraint by claude) CAP-8's lane runs the pixi task (`pixi run --frozen -e pyforge-core pyforge-core-test`, the lean stdlib-only env), not another hand-enumerated file list — enumeration is how the meta-tests were lost the first time; and CAP-9 lands before CAP-8 (a gate born red is a gate nobody trusts).
- (event by claude) Self-validate pass 1 (coherence) on CAP-8..9: PASS — both carry intent (WHAT: enforced in CI; cleared on main) and success (the lane exists and reds a fixture violation; 0 failed today's 6); the constraint rules out enumeration and a red-born lane; IDs 8 and 9 are fresh; status decision recorded.
- (event by claude) Self-validate pass 2 (preservation) on CAP-8..9: PASS — Dream item (8)'s claim (no CI lane since #1086; six violations named file-by-file) lands in CAP-8/CAP-9 with the causal correction on the record (coverage-neutral retirement; meta-tests never wired) rather than repeated as stated.
- (event by claude) Ready for decomposition 2026-09-19: CAP-9 → marshal Story 52.1 (hand-driven: half the files are warden/testing-kit, outside marshal's dispatch surface), CAP-8 → Story 52.2 (Deps: 52.1); Epic 52 on pyforge-marshal, the project that hosts this spec, as Epic 14 was.
- (event by claude) Co-governor note 2026-09-19 for marshal Story 51.8's landing (PR #1495, `62c09c2e27 Merge pyforge-marshal/51-8 into main`; spec-pyforge-marshal:CAP-256, spec-pyforge-marshal's own memlog carries the full entry): `marshal/core/promotion.py`, `core/spec_surface.py`, `core/spec_low_risk.py`, `core/spec_difficulty.py`, `core/dispatch_harness_done.py` each gained/extended a banner-skip helper (BOM/whitespace tolerance). Marshal-internal parsers; no pyforge-core primitive touched, no contract moved here.
- (event by claude) Co-governor note 2026-09-19 for marshal Story 51.2's landing (PR #1498 `d13b244832`; spec-pyforge-marshal:CAP-250): `marshal/cli/deploy.py`, `marshal/dispatch_land.py`, `marshal/dispatch_land_finalize/__main__.py` — marshal-internal landing/promotion code; no pyforge-core primitive touched.
- (correction by claude) CAP-9 success corrected 2026-09-19 at Story 52.1's review: the sentence 'each subprocess call routes through the core guard' over-reached for `pyforge-testing-kit/.../branch_diff_guard.py` — this Spec's own Non-goals exclude the kit ('Not pyforge-testing-kit … until Q2 decides'), Q-26 (2026-08-23) ships it as a stdlib leaf (`dependencies = []`, run-deps python only), and `test_dependency_completeness.py` skips the `pyforge` namespace, so routing it would give the built kit an undeclared runtime dependency. The kit's file is cleared as CAP-6's 'recorded as a sanctioned, tested opt-out': a file-level entry in the guard's `_EXEMPT_RELATIVE_PATHS` with the Q-26/Non-goals rationale and a premise pin (`test_branch_diff_guard_exemption_rests_on_the_kits_leaf_declaration` asserts the kit's `dependencies == []`, so the exemption self-retires when Q2 changes the declaration). Marshal's `cli/login.py` and `cli/refresh.py` route through `PosixProcess.run` as written. Story 52.1's first review pass had migrated the kit via a function-local import on the false premise that the completeness gate inspects module-level imports; the follow-up pass reverted it on the evidence.
- (event by claude) Story 52.1 landed 2026-09-19 (hand-driven bmad-build-auto in worktree `local-recipes-wt-52-1`; CAP-9 realized — `pyforge-core-test` 6 failed → 0 failed / 1863 passed): CAP-5 re-parents in marshal `adapters/oidc_pkce.py` (`PkceLoginError`), `cli/watch.py` (`LoopCliError`, `ProbeError`), warden `tea_advisory.py` (`TeaRosterMissingError`); CAP-6 routing in marshal `cli/login.py::_mint_local_token` and `cli/refresh.py::_sync_status_step` through `PosixProcess.run`; `tests/meta/test_process_sole_ownership.py` gains the testing-kit exemption (Q-26 leaf; Non-goals/Q2) with three pins incl. the premise pin on the kit's `dependencies == []`; `src/pyforge/core/errors.py` docstring count 37 → 41. Tests: marshal `tests/unit/test_login.py` (two real-child runs), `test_refresh.py`, `test_oidc_pkce.py`, `test_watch.py`; warden `tests/unit/test_tea_advisory.py`. CAP-8 (the CI lane) is Story 52.2, next.
- (event by claude) Story 52.2 landed 2026-09-19 (hand-driven bmad-build-auto in worktree `local-recipes-wt-52-2`; CAP-8 realized): `.github/workflows/pyforge-station-tests.yml` gains the `core` filter output and the `core-test` job (peer of the eight station jobs, gate = shared surface OR any station, runs the `pyforge-core-test` pixi task); `pixi.toml`'s `pyforge-station-tests` depends-on gains the core leg first (pr-preflight inherits it; `environment.yaml` byte-identical); `tests/meta/test_conformance_lane_wired.py` (new, stdlib-only) pins the job, gate, run step, wiring, pixi leg and the filesystem-derived roster vs triggers with six mutation proofs; docs corrected (pip-install header, CLAUDE.md, the GitHub-Actions how-to). Fixture proof: a scratch second `subprocess.run` under marshal reds the task (exit 1, `test_no_second_subprocess_implementation[...]`), removed → exit 0. CAP-7's 'fails the build' is now true in CI. Epic 52 done; CAP-1..9 all realized.
- (correction by claude) CAP-8 wording narrowed 2026-09-19 at Story 52.2's review: 'every PR that touches `src/shared/packages/**`' → the gate fires on the shared surface (`pyforge-core`, `pyforge-testing-kit`, `pixi.toml`, `pixi.lock`, the workflow file) OR any `pyforge-<station>` change — exactly what the sole-ownership scan covers (`tests/meta/conftest.py::sibling_station_dirs` iterates `pyforge-*` directories only), so the `django-*` packages are neither triggers nor inputs by design; the structural test asserts every `pyforge-*` directory is gated, so a ninth station cannot appear un-gated.
- (event by claude) Co-governor note 2026-09-19 for marshal Story 51.6's landing (PR #1505 `2bbcdee7bc`; spec-pyforge-marshal:CAP-254): `marshal/cli/watch.py::_gather_station` — marshal-internal; the CAP-5 re-parents Story 52.1 made in the same file are untouched; no pyforge-core primitive moved.
- (event by claude) Co-governor note 2026-09-19 for marshal Story 51.9's landing (PR #1508 `df813208e9`; spec-pyforge-marshal:CAP-251): `marshal/cli/status.py`, `marshal/dispatch_land_finalize/__main__.py` — marshal-internal; no pyforge-core primitive moved.
- (event by claude) Surface reconcile 2026-09-19 (co-governor; marshal Story 51.1 landing, merge 093d7af52a): src/shared/packages/pyforge-marshal/src/pyforge/marshal/ports/vcs.py (three new Protocol methods), src/shared/packages/pyforge-marshal/src/pyforge/marshal/adapters/vcs_git.py (GitVcs.merge_tree_write / add_worktree_for_tree / prune_worktrees — subprocess through the existing PosixProcess path, no second implementation), src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_verify.py (run_verify_commands_only), src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_land.py (_refuse_via_merge_tree_preview). No new exception class; pyforge-core-test green on main (1883). Owner: spec-pyforge-marshal CAP-249.
- (event by claude) Co-governor note 2026-09-19 for steward Story 65.1's landing (PR #1507; spec-pyforge-steward:CAP-146..149, spec-pyforge-steward's own memlog carries the full entry): `steward/sprint_ledger_query.py` (new), `steward/cli.py` (ledger-query duty), `steward/dashboard/{__init__,admin,models,passport_sync,views_htmx}.py`, `steward/dashboard/migrations/0002_workpassport.py`, `steward/data/sprint-ledger-query.schema.json` — station-internal; no pyforge-core primitive touched; the module's only third-party import is yaml.
- (event by claude) Surface reconcile 2026-09-19 (co-governor; steward Story 65.1, PR #1507 review): steward files under the kernel's conformance surface — src/shared/packages/pyforge-steward/src/pyforge/steward/sprint_ledger_query.py (added; no module-level django/channels/marshal import, the one lazy importlib reach into the [dashboard] extra is pinned by steward's tests/meta/test_invariants.py; no exception class, no subprocess), src/shared/packages/pyforge-steward/src/pyforge/steward/data/sprint-ledger-query.schema.json (added, data), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/models.py (WorkPassport), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/migrations/0002_workpassport.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/admin.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/views_htmx.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/passport_sync.py (all inside the [dashboard] extra; lazy django imports). pyforge-core-test 1883 passed on the branch (CAP-8/CAP-9 hold). Owner is spec-pyforge-steward CAP-146..149; this entry is the co-governor's reconcile only.
- (event by claude) Surface reconcile 2026-09-19 (co-governor; marshal Story 51.4 landing, merge 904555c910): src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/{dispatch.py (KIND_DISPATCH_BLOCKED), dispatch_completion.py (is_spec_only_narration, has_git_progress spec_relative_path), dispatch_harness_done.py, harness_session.py, supervise.py} changed; no new exception class, no subprocess; pyforge-core-test stays green on main. Owner: spec-pyforge-marshal CAP-252.
- (event by claude) Surface reconcile 2026-09-19 (co-governor; steward Story 60.1, spec-pyforge-steward:CAP-117 — estate catalog and config): steward files under the kernel's conformance surface — src/shared/packages/pyforge-steward/src/pyforge/steward/catalog.py (added; `catalog` duty + engine: yaml/json/argparse only, imports stay inside pyforge.steward — bootstrap/frames/interfaces/suite — no django, channels, marshal, network or subprocess; one `CatalogConfigError(ValueError)` at the config boundary, never raised past `CatalogDuty.run`), src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py (`catalog` is the twentieth duty: DUTIES, _HELP, _add_catalog_subparsers, lazy import in resolve_duty). Owner is spec-pyforge-steward CAP-117; this entry is the co-governor's reconcile only.
- (event by claude) Surface reconcile 2026-09-19 (co-governor; marshal Story 51.5 landing, merge 29e4a237df): src/shared/packages/pyforge-marshal/src/pyforge/marshal/{cli/dispatch.py,core/findings.py,core/model_cost.py} changed; no new exception class, no subprocess. Owner: spec-pyforge-marshal CAP-253.
- (event by claude) Surface reconcile 2026-09-19 (co-governor; doctor Story 23.7, spec-pyforge-doctor CAP-54, absorbed from spec-docs-shelf-alignment CAP-7 on 2026-09-17 -- doctor's own memlog carries the full entry): new src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/docs_shelf.py (leftover-shelf path occupancy vs the docs/MAP.md allow-list); src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py (new Source.DOCS_SHELF_OCCUPANCY member), sources/__init__.py (REGISTRY row), sources/__main__.py (DISPATCH entry), data/report-schema.json (enum entry) -- doctor-internal taxonomy/dispatch plumbing; no pyforge-core primitive touched, no subprocess, no new exception class. Owner is spec-pyforge-doctor Story 23.7; this entry is the co-governor's reconcile only.
- (event by claude) Surface reconcile 2026-09-19 (co-governor): (a) doctor Story 30.1 / PR #1529 — src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/docs_map_hygiene.py (added; no exception class, no subprocess), src/shared/packages/pyforge-core/README.md and the other nine station READMEs (one plain-text docs pointer line; wheel-safe); (b) steward Story 61.1 landing (merge 532673c1ca, ledger promote e6997a3a52 — autonomous; twin promoted by finalize) — src/shared/packages/pyforge-steward/src/pyforge/steward/corridor.py (added), dashboard/{admin.py,models.py,corridor_load.py (added),migrations/0003_corridorload.py (added),__init__.py}, cli.py; the [dashboard] extra boundary holds (steward tests/meta/test_invariants.py updated by the story). Owners: spec-pyforge-doctor CAP-83, spec-pyforge-steward CAP-141.
- (note by claude) Path manifest for the 2026-09-19 steward 61.1 reconcile (expanded): src/shared/packages/pyforge-steward/src/pyforge/steward/corridor.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/corridor_load.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/migrations/0003_corridorload.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/admin.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/models.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/__init__.py, src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py, src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/docs_map_hygiene.py, src/shared/packages/pyforge-core/README.md.
- (event by claude) Surface reconcile 2026-09-20 (co-governor; steward Story 61.2, spec-pyforge-steward CAP-142, PR #1532 merge 03112ba2d7): src/shared/packages/pyforge-steward/src/pyforge/steward/passport.py (added), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/passport_mint.py (added), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/migrations/0004_workpassport_vendor_id.py (added), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/models.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/admin.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/__init__.py, src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py — the [dashboard] extra boundary holds (steward tests/meta/test_invariants.py updated by the story); no pyforge-core primitive touched.
- (event by claude) Surface reconcile 2026-09-20 (co-governor; marshal Stories 51.10 + 51.12, spec-pyforge-marshal CAP-257/CAP-259): src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/watch.py (a module-name constant; the subprocess call is the same pyforge.core process primitive — no second implementation, CAP-7 holds), src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/status.py (Tier-3 discovery via cli/dispatch.py's locators; no new exception class, no subprocess). Owner is spec-pyforge-marshal; this entry is the co-governor's reconcile only.
- (event) Surface reconcile 2026-09-20 (co-governor; doctor Story 26.1, spec-pyforge-doctor CAP-77 -- doctor's own memlog carries the full entry): new src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/live_proof_surfaces.py (reads live-proof-surfaces.md's own Surface globs column via pathlib-style glob matching against origin/main..HEAD changed paths; no exception class, no subprocess -- routes git diff through the existing cli_bridge.run_git, no second implementation, CAP-7 holds); src/shared/packages/pyforge-doctor/src/pyforge/doctor/models.py (new Source.LIVE_PROOF_SURFACE member), sources/__init__.py (REGISTRY row), sources/__main__.py (DISPATCH entry), data/report-schema.json (enum entry) -- doctor-internal taxonomy/dispatch plumbing; no pyforge-core primitive touched. Owner is spec-pyforge-doctor Story 26.1; this entry is the co-governor's reconcile only.
- (event) Surface reconcile 2026-09-20 (co-governor; doctor Story 26.1 review-pass patches, spec-pyforge-doctor CAP-77 -- doctor's own memlog carries the full entry): src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/live_proof_surfaces.py (added CatalogRow.cost field + parse_catalog blank-line/cost fixes; no exception class, no subprocess -- still routes through the existing cli_bridge.run_git, no second implementation, CAP-7 holds). Owner is spec-pyforge-doctor Story 26.1; this entry is the co-governor's reconcile only.
- (event by claude) Surface reconcile 2026-09-20 (co-governor; marshal Story 51.13, spec-pyforge-marshal CAP-260): src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/watch.py — an enum import from marshal's own core/dispatch_completion.py and one pure predicate; no subprocess, no exception class.
- (event by claude) Surface reconcile 2026-09-20 (co-governor; marshal Story 51.7, spec-pyforge-marshal CAP-255, PR #1536 merge 88d488d6f3): src/shared/packages/pyforge-core/src/pyforge/core/landing_evidence.py (LandingEvidenceMatch gains the branch-derived shape; parsers unchanged, pure — no filesystem, no subprocess), src/shared/packages/pyforge-core/tests/unit/test_landing_evidence.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/promotion.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/dispatch.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_land.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_land_finalize/__main__.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_supervisor/__main__.py — the spec_status_for reader is injected by marshal, never read inside core (CAP-5/CAP-7 hold). Named here because the 51.13 stamp on this branch already covered them.
- (event by claude) Surface reconcile 2026-09-20 (co-governor): (a) marshal Story 51.11 (CAP-258, PR #1540 merge b96315be89) — src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_supervisor/__main__.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch_completion.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch_harness_done.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch_landing.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch_survival.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/findings.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/status.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/verdict.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/dispatch.py; (b) steward Story 61.3 (spec-pyforge-steward CAP-143, PR #1539 merge 83d4162aa3) — src/shared/packages/pyforge-steward/src/pyforge/steward/glass.py (added), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/glass_query.py (added), src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/views_htmx.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/__init__.py, src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py. No pyforge-core primitive touched; the [dashboard] extra boundary holds (steward tests/meta/test_invariants.py updated by the story).
- (event by claude) Surface reconcile 2026-09-20 (co-governor; doctor Story 29.1, spec-pyforge-doctor CAP-82, merge 7c04dfb415): src/shared/packages/pyforge-doctor/src/pyforge/doctor/sources/sibling_dreams.py — no subprocess, no new exception class; GitHub probe stays fail-open via the existing primitive.
- (event by claude) Surface reconcile 2026-09-20 (co-governor; steward Story 61.4, spec-pyforge-steward CAP-144, merge fa1b45cce4): src/shared/packages/pyforge-steward/src/pyforge/steward/corridor.py, src/shared/packages/pyforge-steward/src/pyforge/steward/cli.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/corridor_load.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/models.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/admin.py, src/shared/packages/pyforge-steward/src/pyforge/steward/dashboard/migrations/0005_corridorload_signer_corridorload_slice_name.py (added) — the [dashboard] extra boundary holds; no core primitive touched.
- (event by claude) Surface reconcile 2026-09-20 (co-governor; marshal Story 53.2, spec-pyforge-marshal CAP-261b, branch dispatch/pyforge-marshal/53.2): src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/status.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/journal.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_land.py, src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_land_finalize/__main__.py changed (dispatch-landing spec-surface reconcile + deferred-work intake publish/restore, MRS-DISP-047/MRS-DISP-048); marshal-internal, no pyforge-core primitive touched. Owner: spec-pyforge-marshal CAP-261b; co-governor's entry only.
