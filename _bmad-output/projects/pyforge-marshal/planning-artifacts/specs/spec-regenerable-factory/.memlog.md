---
topic: Regenerable-factory program: backfill Dream->PRD/spec chains under every realized surface; drift checks on all code
updated: 2026-07-29T20:10
---

- (direction) User doctrine (2026-07-23, inverts no-retroactive-ceremony default): backfill PRDs/specs/decks for realized work so the factory+personas own everything and any code changes through idea->spec->BMAD; add drift checks on all code
- (decision) Program dream: docs/dreams/regenerable-factory.md (owner marshal). Wave 0 (multi-loop isolation harness) SHIPPED first as its own spec - loops for this program and warden 6.3 can run concurrently
- (capability) CAP-1 surface-manifest convention: every SPEC.md declares surface: globs of the code paths it governs; retrofit onto existing kernels (multi-loop-isolation, design-code-bridge)
- (capability) CAP-2 backfill waves: brownfield bmad-spec per realized Dream, pilot-first (factory-console), then enterprise-airgap+modernist-identity, then packaging-factory+fleet-stewardship via bmad-document-project; atlas+design-code-bridge only need chain-verify+manifests
- (capability) CAP-3 repo-wide surface checker (deterministic, scripts/): coverage (every tracked source file -> >=1 spec surface or explicit allowlist) + drift (governed file changed without its spec/memlog moving -> finding); joins bmad-drift-check/llms-full-check as CI gate
- (capability) CAP-4 regeneration drill: delete a governed module (pilot: docs/dashboard/generate.py), rebuild from its spec alone, same gates pass - the program's proof
- (constraint) Checker is a deterministic harness, not a skill (Marshal doctrine); never false-green (Warden temperament); allowlist explicit and logged, no silent exemptions
- (constraint) Backfilled specs describe what IS (brownfield); they do not mandate rewriting shipped code. Execution loop-driven via the Wave-0 harness; CFE-touching waves bind Rule 1 (invoke conda-forge-expert) + Rule 2 (closeout retro)
- (note) Non-goals: decks for backfilled dreams (Herald backlog, communication decision); upstream bmad-method fixes; rewriting code to match specs
- (note) Wave/story matrix lives in companion waves.md (kernel cites it); formal epics+stories via bmad-create-epics-and-stories at loop-prep
- (event) Self-validate pass 1 (coherence): PASS - 4 caps intent+success, constraints bend design, non-goals present, success signal testable (checker exit code + drill outcome)
- (event) Self-validate pass 2 (preservation): PASS - doctrine direction, wave order, harness/no-false-green constraints, Rule1/2 binding all landed in kernel or waves.md companion
- (event) CAP-4 proven: factory-console drill PASS 2026-07-23 (blind rebuild of generate.py, DATA EQUIVALENT); procedure recorded in spec-factory-console/drill-evidence.md
- (event) PROGRAM COMPLETE 2026-07-23: all 14 stories done. CAP-1 manifests on 9 kernels; CAP-2 backfills (factory-console, enterprise-airgap, modernist-identity, packaging-factory, fleet-stewardship) + chain-verify (atlas, bridge, marshal); CAP-3 checker green over 5300 tracked files (4009+ governed, allowlist reason-tagged, drift+sentinel+exempt modes proven live) + meta-test gate; CAP-4 regeneration drill PASS (generate.py, DATA EQUIVALENT). Rule 2 discharged (CFE v8.79.1). Spec shipped; dream realized
- (direction) Terminology (2026-07-25, Charter amendment): this artifact is 'the Spec' — retire 'kernel' (bmad-spec tool-jargon) on next re-derive. Also: the eight are 'Smiths', not 'Forgemasters'.
- (change) Governed the two orphan chain-integrity detectors: scripts/bmad_drift_check.py (artifact<->factory sync + the dream-unowned check Charter §5 cites) and scripts/dream_chain_check.py (INV-0..3). Three of the four repo-level detectors were UNGOVERNED — the instruments enforcing the model sat outside it. Also corrected a stale surface comment: spec_surface_check.py said 'does not exist yet'; it ships.
- (change) scripts/dream_chain_check.py landed as a tracked file (commit 2a199844bc) — the INV-0..INV-3 detector for the Dream-to-Code model, governed by this practice alongside spec_surface_check.py and bmad_drift_check.py. Its findings are the migration backlog, derived rather than hand-listed. Backlog went 38 -> 0 in the same session.
- (change) New detector `scripts/deferred_work_check.py`, governed here beside the other three chain-integrity instruments. It closes a hole in bmad-loop itself: the follow-up-review damping valve, on spending `limits.max_followup_reviews`, refiles a still-recommended review into `implementation-artifacts/deferred-work.md` — which is GITIGNORED. So the mechanism's own "we'll catch it in the ledger later" was false by default, in a repo that has already lost Tier-3 data (pyforge-atlas's live ledger is still truncated to 11 of 64 entries). bmad-loop is an installed conda package, so patching its refile target would be undone by the next reinstall; the durable fix is a repo-side check that FAILS on the leak.
- (event) The detector found far more than the two atlas entries that prompted it: FIVE damped follow-up recommendations across THREE projects (atlas 10.5/10.6, marshal 1.1, warden 6.3/5.1), and — worse — that pyforge-doctor, pyforge-herald, pyforge-marshal and pyforge-warden had **no tracked deferred-work ledger at all**, leaving 97 KB of deferred work in gitignored scratch. All four promoted verbatim (copy, not curation; bodies unedited, provenance header states that), with the three bare `DW-<n>` ids renamed to `DW-FU-<story>` since bmad-loop emits a generic id per run that collides with the next damped story. Detector now green.
- (constraint) MEASURE THE FILE, NOT JUST THE IDS IN IT. The first draft compared `DW-*` ids only, and would have silently passed pyforge-herald (25 KB) and pyforge-doctor (6 KB) because their Tier-3 entries use `## Deferred from: …` headings and carry no `DW-` id at all — and warden, whose 62 KB contained only two ID'd entries. A file-level check ("substantive Tier-3 ledger + no tracked ledger") catches the whole class; the ID-level check alone was a detector that would have reported OK on the largest exposure in the fleet.
