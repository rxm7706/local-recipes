---
topic: loop-home-fleet-refresh (genesis baseline bootstrapped 2026-08-22, marshal Story 25.3)
updated: 2026-09-09T22:08
---

- (event) Genesis baseline 2026-08-22 (marshal Story 25.3, spec-bmad-611-era-alignment CAP-3): .memlog.md bootstrapped so 6.11 bmad-spec updates succeed against this folder — a structural bootstrap only, per the S-13.7 pattern; no governed-file content is claimed reconciled by this entry
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event) Companion doc added 2026-09-06 (marshal Story 31.5, relaying `spec-bmad-suite-lifecycle` G10 / `cutover-readiness.md` P16): `loop-home-cutover-readiness.md` defines what a re-provisioned loop home must contain after the python-foundry cutover flip (remote, `pyforge.toml` name, `marshal homes --json`'s `desynced`/`active_project` fields agreeing, the rendered policy's `[dev] skill` field reading `"bmad-dev-auto"` per the CAP-10 correction, refreshed `_bmad-output` relays, no in-flight run), the checks that prove each item (all existing: `marshal homes --json`, `bmad-loop validate`, manual inspection), and reconciles `DW-CC-2026-09-04-1`'s "retired" line-item (one of six categories in that aggregate entry) with P16's "re-provisioned" wording as describing the same eight homes' old-state-vs-new-state, via the `pyforge.cutover_root` flag flip (epics.md Story 44.12/44.5) rather than a specific destroy-and-recreate mechanism this doc doesn't assume. `SPEC.md` frontmatter's `companions:` updated to list it. A distinct one-time concern from CAP-1's live staleness detection, so it lands as a sibling file, not new SPEC content. Relayed to steward via a note in `spec-bmad-suite-lifecycle/.memlog.md`.
- (correction) 2026-09-06 review pass: R3/R4 corrected against live code (`marshal homes --json` serializes `desynced`/`active_project` only, not raw `HomeFacts` fields; `[dev] skill` is a permanent `"bmad-dev-auto"` discriminator, not the invoked harness name), Scope/Reconciling corrected against `epics.md` Story 44.12/44.5's actual flag-driven mechanism instead of an assumed destroy-and-recreate, Non-goals softened on the memlog-is-writer claim (P4 scopes that convention to `SPEC.md`, not `cutover-readiness.md`), and the DW-CC-2026-09-04-1 paraphrase scoped to its one line-item within the 268-worktree aggregate entry.
- (correction) 2026-09-09 follow-up review pass (bmad-build-auto): fixed wrong line citations (`epics.md:2688-2693` → steward `epics.md:2706-2715` for Story 44.12; `epics.md:2619` → `2636` for Story 44.5; `deferred-work-ledger.md:2571-2576` → `2591-2598` for `DW-CC-2026-09-04-1`); R3 now defines expected slug as the `~/.bmad-loops/` directory basename; R6 names per-home `marshal status --project <slug>` and `bmad-loop status <run_id>`; Reconciling notes the DW entry is `status: resolved` (historiographic).
