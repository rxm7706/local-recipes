---
created: 2026-09-01
updated: 2026-09-09T18:02
---

- (event) Surface reconcile 2026-09-03 (fleet hygiene): post-#1038 `pixi lock` and already-landed Epics 42–43 moved governed paths without this memlog moving. Accept the current tree as the post-land surface; no new product work. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-parallel-dispatch-fanout`.


- (event by claude) First-ever baseline stamp, 2026-09-01: folder-format spec authored for Story 28.16 (parallel dispatch fanout when deps and surfaces are disjoint). CAP-1/CAP-2 not yet implemented — spec status `ready`, no story landing since authorship. Baseline captures current surface hashes as the pre-implementation reference point (same pattern as `spec-pixi-candidate-currency` first stamp). Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-parallel-dispatch-fanout`.
- (change) **Baseline reconciliation, 2026-09-02** (operator-directed fleet hygiene pass, PR #1009): 8 governed path(s) drifted against this spec's recorded baseline (8 changed) — every one of them ALREADY LANDED on `main`; the baseline had simply lagged behind the commits that landed them. This entry records that lag being closed, and is explicitly NOT a re-review of the changes themselves: each was reviewed when it landed, and none was re-litigated here. No `SPEC.md` edit. Drifted paths:
  - `_bmad-output/projects/pyforge-atlas/planning-artifacts/marshal-policy.toml` (changed)
  - `_bmad-output/projects/pyforge-marshal/planning-artifacts/marshal-policy.toml` (changed)
  - `_bmad-output/projects/pyforge-mason/planning-artifacts/marshal-policy.toml` (changed)
  - `_bmad-output/projects/pyforge-steward/planning-artifacts/marshal-policy.toml` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/dispatch.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/dispatch_fleet.py` (changed)
  - `src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_fleet_supervisor/__main__.py` (changed)
  Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-parallel-dispatch-fanout` (scoped, never a bare `--write-baseline`, which would accept every OTHER spec's pending drift as correct). Stamped from a clean worktree so the recorded hashes are committed content, not uncommitted working-tree bytes.
- (change) 2026-09-04 (steward, foreign-drift reconcile): _bmad-output/projects/pyforge-steward/planning-artifacts/marshal-policy.toml gained an [epic_surfaces] "44" block (34 globs — the epic-43 set plus factory/**, skills/**, src/packages/**, .claude/skills/**, .cursor/skills/**, docs/foundry/**, AGENTS.md, CLAUDE.md, README.md, azure-pipelines.yml, .azure-pipelines/**, src/shared/packages/**, src/platform/Containerfile) so MRS-GATE-012 is satisfied whenever the operator flips a 44.x story. No policy semantics changed; every 44.x is ledger-blocked (solutioning only). Same shape as the 2026-09-02 steward 40-43 declarations. Scoped-stamped: --write-baseline --spec pyforge-marshal/spec-marshal-parallel-dispatch-fanout from a clean worktree; no other pending drift on this spec at stamp time (spec-surface run 2026-09-04 showed only this file).
- (change) 2026-09-04 (steward, iteration 2): marshal-policy.toml [epic_surfaces] "44" gained var/** and src/platform/config/flags.json (cutover flag + runtime-state home per fnd:AD-17/AD-19); still all 44.x ledger-blocked; scoped-stamped again from a clean tree after commit.
- (note) 2026-09-06 (spec-bmad-suite-lifecycle planning PR): every station's planning-artifacts/marshal-policy.toml gained [epic_surfaces] entries for the epics minted that day (steward 46/47, marshal 31, doctor 20, warden 11, herald 18, scribe 7, atlas 24, mason 14) plus the three that were missing (steward 45, marshal 29/30) — without an entry effective_surface falls back to an empty tuple and MRS-GATE-007 fires on every changed path. Globs mirror each station's stock block plus the story surfaces named in epics.md; no dispatch/landing/model-tier keys changed
- (note) 2026-09-06 (adversarial review F-7 / lifecycle spine AD-11): steward's [epic_surfaces] "14" widened with .claude/skills/**, _bmad/** and the core-upgrade spec dir — the surfaces the --no-shims apply (14.9) writes; without them MRS-GATE-007 fires on every path the apply changes
- (note) Cross-station surface touch (marshal Story 30.5, 2026-09-06): `marshal-policy.toml`'s difficulty-class comment fixed two dead file-path citations to the current `bmad-build-auto`/`bmad-build` skill directories (6.12 shim-roster rename). Comment-only, no `[epic_surfaces]`/dispatch-fanout key changed. Reconciled + baseline re-stamped under this spec's surface per the foreign-spec-surface procedure.
- (note) Story 32.4 (spec-fleet-consistency-standard CAP-3) retired epics-with-stories.md fleet-wide -- BMAD 6.12 produces it nowhere, six of eight copies were frozen at 2026-08-08 while epics.md moved to 2026-09-06, and a line-diff audit of all eight confirmed nothing normative lived only there (steward's test-tier mandate is at its own epics.md:74; mason's OQ-E2/OQ-E5 and steward's story-sizing convention are likewise in epics.md; herald's 30 stories are a SUBSET of the 64 in its epics.md). No source change for this spec; its surface intersects the retired artifact only through the shared planning-artifacts tree.
- (change by claude) Cross-station surface touch 2026-09-09 (foreign-drift reconcile, same shape as the 2026-09-04 steward "44" entry): four `[epic_surfaces]` entries gained `_bmad-output/projects/pyforge-<station>/planning-artifacts/sprint-status-ledger.yaml` — **mason "14"**, **steward "14" / "43" / "46"**. ROOT CAUSE, and it is a live defect rather than stale residue: every story writes its own status into that ledger at completion, but the entries declared `planning-artifacts/specs/**` and `planning-artifacts/implementation-artifacts/**` only — the ledger sits directly in `planning-artifacts/`, matched by neither. Any story in an undeclared epic therefore re-fires `MRS-GATE-012` on its own bookkeeping. Observed live: steward's `marshal status` still carries the advisory from run `pyforge-steward-20260903T135640861Z-9fc8f532` (story 43.6, `dispatch_verification_verdict: refused`, failed gate `MRS-GATE-001`, advisory path = the ledger). Epic 44 never tripped it because its 37-glob block carries the broad `planning-artifacts/**`. SELECTION: only the four epics that both lack coverage AND hold non-done stories (mason 14-1 in-progress; steward 14-10 / 43-7 / 46-10 backlog) — epics with no pending story can never dispatch, so widening them would be unrequested surface growth. The exact ledger path is used rather than epic 44's broad `planning-artifacts/**`, keeping the narrowest glob that closes the hole. VERIFIED by driving marshal's own resolver, not a reimplementation of its glob semantics: `resolve_policy_surface` → `compute_effective_surface` → `check_scope_with_mode(mode="warn")` over the ledger path for each of the four cases, against `git show HEAD:` for the before-state — **4 `MRS-GATE-012` findings pre-fix → 0 post-fix**, reproducing the live advisory code exactly (the 2026-09-02 steward-39.4 precedent's method). No policy semantics, `scope_violation_mode`, or dispatch-fanout key changed. RECONCILES: _bmad-output/projects/pyforge-mason/planning-artifacts/marshal-policy.toml, _bmad-output/projects/pyforge-steward/planning-artifacts/marshal-policy.toml. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-parallel-dispatch-fanout`.
- (change by claude) Cross-station surface touch 2026-09-09 (foreign-drift reconcile, same shape as the earlier 2026-09-09 entry): steward `[epic_surfaces]` gained **"48"** (the chain tells the truth — syncer guards, R-18..R-22, CAP-axis pass, Single-Spec merge) and **"49"** (shipped becomes in effect — the realization gate), both minted by `sprint-change-proposal-2026-09-09-currency-review.md` on the Unifying chain; each copies the "47" list and adds its own globs (48: `scripts/**`, `AGENTS.md`, `src/platform/deploy/**`, marshal tests, the deferred-work ledger, the PRD, the Dream; 49: `src/platform/**`, the `django-*` portals, marshal, doctor, scribe, atlas). No fanout semantics change; the entries are the declaration `dispatch_fleet` reads. Stamped scoped.
- (decision by claude) 2026-09-09 oq1 CLOSED (fleet-readiness-decision-batch-2026-09-09 row mars-B-B5, operator-approved): the fan-out cap stays a FIXED INTEGER; token-budget-aware capping is revisited only after Epic 33 produces the benchmark artifact. Token-budget awareness needs a measured baseline that does not exist (token economy Gates, bypassed); Stories 28.10/28.11 are done as a cost CATALOG, not a live meter. Rejected: make it budget-aware now -- it would be a second unfed dial.
- (decision by claude) 2026-09-09 oq2 CLOSED (fleet-readiness-decision-batch-2026-09-09 row mars-B-B6, operator-approved): keep the LIVE effective-surface INTERSECTION each tick; do not precompute a surface index. The bottleneck is not cost, it is DECLARATION -- core/dispatch_fleet.py:770-790 refuses on unknown-surface, and with CAP-16 auto-derivation (core/gate.py:348-423) two same-station stories share a surface by construction, so no index helps until story specs declare their own `surface:`. Rejected as premature: it optimises a step that currently always refuses.
- (capability by claude) CAP-6 (new, 2026-09-09, fleet-readiness-decision-batch-2026-09-09 row mars-B-B4 / C10, operator-approved) -- factory fan-out gets its OWN `dispatch.max_parallel` policy key. INTENT: the dispatch wave cap is a first-class marshal policy key resolved by cli/dispatch.py:893-902, independent of bmad-loop scm.max_parallel (core/policy.py:513) which today it falls back to; raising the dispatch cap therefore stops firing _max_parallel_clamp_finding (core/policy.py:1537-1551), whose message names bmad_loop 0.9.0 -- a false-context warn on the dispatch path. SUCCESS: a station that declares dispatch.max_parallel = N forms waves of up to N with no bmad-loop clamp advisory; a station that declares nothing behaves exactly as today (1); the bmad-loop knob continues to govern the spin engine alone, and one knob no longer governs two unrelated concurrency models. Decomposed as marshal Story 33.8.
- (event by claude) status: ready -> shipped -- CAP-1..5 shipped in Story 28.16 (wave scheduler core/dispatch_fleet.py:750-790, narrowed conflict guard cli/dispatch.py:935 station_in_flight_conflict(..., parallel_dispatch=False), wave journal cli/dispatch.py:2775), all `done` (fleet-readiness-decision-batch-2026-09-09 Class B, row mars-B). MECHANISM ONLY -- see the residual entry: max_parallel = 1 on all eight rendered loop homes and no live wave has ever run, so the Dream own "live proof required" gate is unmet and docs/dreams/marshal-parallel-dispatch-fanout.md deliberately KEEPS status: specified.
- (note by claude) 2026-09-09 RESIDUAL, carried into the enablement story (marshal Story 33.8): (1) the cap reuses bmad-loop scm.max_parallel rather than the dispatch.max_parallel key the Dream asked for -- now CAP-6; (2) within-station fan-out cannot form a wave at all, because CAP-16 auto-derives policy_surface to the station whole package tree so two same-station stories overlap by construction and core/dispatch_fleet.py:786 refuses. Fan-out requires story specs to declare their own `surface:` first. Two capabilities shipped one week apart in the same epic and compose into a no-op; nothing in either Spec recorded it until now (fleet-readiness-decision-batch-2026-09-09 row mars-B-D7).
- (change by claude) Surface touch 2026-09-09 (this spec governs _bmad-output/projects/*/planning-artifacts/marshal-policy.toml): pyforge-marshal marshal-policy.toml [epic_surfaces] gained "33" -- the Epic 33 token-economy enablement epic minted today by sprint-change-proposal-2026-09-09-token-economy-enablement.md. The list copies the "31" block and adds Epic 33 own globs (the four marshal supervisor/adapter paths, django-pyforge for the CAP-18 publisher, docs/dreams/marshal-token-economy.md, the CFE SKILL.md the 33.10 pin fix reads). Without an entry effective_surface falls back to an empty tuple and MRS-GATE-007 fires on every changed path. No dispatch/landing/model-tier key changed. NOT stamped: the apply agent was instructed to leave spec-surface-check red for the lead to re-baseline (fleet-readiness-decision-batch-2026-09-09 apply order step 8).
- (event) (self-validate pass 1 -- coherence) 2026-09-09 re-derive: PASS. status ready -> shipped per the memlog status event; open_questions emptied with a dated ANSWERED block carrying both retired answers verbatim in reason form; CAP-6 added with intent+success as the next unused id (CAP-1..5 unchanged, none renumbered); the residual renders as its own section because it is the Dream's unmet live-proof gate, not a note.
- (event) (self-validate pass 2 -- preservation) 2026-09-09 re-derive: PASS. All seven 2026-09-09 entries landed: oq1/oq2 closures as the ANSWERED block, CAP-6 in Capabilities plus a 33.8 Decomposition row, the shipped-is-mechanism-only qualifier in Assumptions, the three-part residual and the marshal-33 / steward-48 / steward-49 [epic_surfaces] touch in Residual. Only frontmatter lines were removed; no Capability, non-goal, Decomposition or Verification line dropped.
- (event by claude) 2026-09-10 (spec-28-32-derived-context-and-output-roll-out-fleet-wide): all 7 non-marshal stations' `marshal-policy.toml` changed -- `_bmad-output/projects/{pyforge-atlas,pyforge-steward,pyforge-warden,pyforge-doctor,pyforge-herald,pyforge-mason,pyforge-scribe}/planning-artifacts/marshal-policy.toml` -- adding `[context."derived-context"]` and `[context.output]` `enabled = true` blocks, mirroring the shape Story 28.27's `planning-graph` rollout already used against this same 7-file surface. Uniform edit across all 7; no per-station content differs beyond the comment wording already present.
- (event by claude) Surface reconcile 2026-09-10: the 2026-09-09 Epic-33-minting touch above was deliberately left unstamped (fleet-readiness-decision-batch-2026-09-09 apply order step 8, "leave spec-surface-check red for the lead to re-baseline") and the 28-32 rollout entry above landed after it without a follow-up stamp either. In between, `6f86eb220d` ("fix(marshal): add harness_preference = [\"cursor\"] to 4 stations") re-touched 4 of the same 7 co-governed `marshal-policy.toml` files -- `pyforge-doctor`, `pyforge-herald`, `pyforge-mason`, `pyforge-scribe` -- adding the same cursor-only `harness_preference` override (Story 22.8, FR-193 CAP-8) that `pyforge-marshal`/`pyforge-atlas`/`pyforge-warden`/`pyforge-steward` already carried, fixing a dispatch-killing model/harness mismatch (repo-default claude-first `harness_preference` resolving Cursor-only `composer-2.5-fast` from `model_tier_map`). Neither touch changed this spec's own `[epic_surfaces]`/dispatch/landing/model-tier keys. No CAP text change. This is the re-baseline the 2026-09-09 note deferred. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-parallel-dispatch-fanout`.
- (event by claude) Surface reconcile 2026-09-12 (fleet hygiene, same pass as spec-pyforge-core above): 3 governed paths changed by already-landed Epic 22 dispatch-supervision work, memlog never moved -- `cli/dispatch.py`, `core/dispatch_fleet.py`, `core/gate.py`. No CAP text change. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-marshal-parallel-dispatch-fanout`.
