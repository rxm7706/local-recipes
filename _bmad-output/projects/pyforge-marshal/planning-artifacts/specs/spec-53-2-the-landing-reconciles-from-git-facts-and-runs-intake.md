---
title: '53.2: The landing reconciles from git facts and runs intake'
type: 'feature'
created: '2026-09-20'
status: 'in-review'
baseline_revision: '7f5584a2e49412b4fe9ee000d8f8b1760d024c4b'
review_loop_iteration: 0
followup_review_recommended: false
context:
  - '{project-root}/src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_land.py'
  - '{project-root}/scripts/spec_surface_reconcile.py'
  - '{project-root}/scripts/deferred_work_intake.py'
deferred: []
declared_low_risk: false
---

<intent-contract>

## Intent

**Problem:** even with the producer told and gated (53.1), a session can exit with drift on its own files, and today that lands green and leaves `main` red until a human writes "Story X landed: <paths>" on the owning Spec and every co-governor the detector names, stamps exactly those Specs, and runs `deferred_work_intake` — the four fallout PRs of 2026-09-20 (#1533, #1537, #1544, #1546) were nothing but that ritual.

**Approach:** `dispatch_land`, after verification and before `forge.merge_pr`, runs the spec-surface verdict over the branch's changed governed paths (`git diff` against the merge base). For every Spec whose drift consists only of this branch's files, it appends one event on that Spec's memlog and on each co-governor the verdict names — the story key, the run id, each path — through `_bmad/scripts/memlog.py append`, scoped-stamps exactly those Specs through `scripts/spec_surface_check.py --write-baseline --spec`, commits onto the dispatch branch, journals `MRS-DISP-047` (warn: the session left N paths unreconciled; the landing named them), and merges. A Spec whose drift includes a file this branch did not change is foreign drift: the landing is refused with `MRS-DISP-048` naming those paths. `dispatch_land_finalize` runs `scripts/deferred_work_intake.py --fix --project <station>` and journals any refusal.

## Boundaries & Constraints

**Always:**
- The entry is derived from `git diff` — per-file naming, never a self-report and never a blanket stamp; the stamp is scoped to the Specs the entry named and nothing else
- Foreign drift refuses the landing (`MRS-DISP-048`) with every foreign path named; it is never absorbed
- A session that reconciled itself produces no entry, no stamp and no finding
- `MRS-DISP-047` is a warn, journaled, visible in `marshal watch` and `fleet-picture`'s ATTENTION rows
- The loop path (`cli/land.py`, CAP-239) is untouched; doctor's `sources/chain.py` is read, never changed

**Never:**
- Never a bare `--write-baseline`; never a stamp for a Spec with foreign drift
- Do not merge with the landing's own reconcile commit unpushed — the push precedes `forge.merge_pr` as today
- Do not add a second gate or verdict owner; the spec-surface verdict stays doctor's

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| the 2026-09-20 fixture | the changed paths of 26.1, 61.3, 51.11, 29.1, 61.4, 46.7 on branches with no memlog entry | every path named on the owner + the co-governors the verdict names; those Specs stamped; `spec-surface` green on the merged tree; `MRS-DISP-047` ×6 | n/a |
| session reconciled itself | memlog already names every path | no entry, no stamp, no finding | n/a |
| foreign drift on a shared Spec | the Spec's drift includes a path the branch did not touch | refused, `MRS-DISP-048` names the foreign paths, nothing stamped | refusal journaled, worktree preserved |
| deferral without `location:` at finalize | intake refuses it | refusal journaled with the spec path and the rule | never silent |
| memlog append fails (locked / missing frontmatter) | `memlog.py` errors | landing refused naming the Spec; nothing merged | refusal journaled |

</intent-contract>

## Binding

Parent Spec capability: `spec-pyforge-marshal CAP-261` (b).
Surface: `src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_land.py` (the reconcile step between verify and merge), `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/findings.py` (`MRS-DISP-047`, `MRS-DISP-048`), `src/shared/packages/pyforge-marshal/src/pyforge/marshal/dispatch_land_finalize/__main__.py` (intake), tests in `tests/unit/` with the 2026-09-20 fixture.
Ledger key: `53-2-the-landing-reconciles-from-git-facts-and-runs-intake`.
Minted 2026-09-20 from `epics.md`; dispatch after 53.1.

## Verification

**Commands:**
- `pixi run --frozen -e pyforge-marshal pyforge-marshal-test` — expected: pass (station policy verify command; MRS-GATE-010 binds the dispatch gate to this Success signal and reads it from the primary tree's tracked spec, so it is declared here before dispatch).
- `pixi run --frozen -e pyforge-ci pyforge-deps-test` — expected: pass (the station policy's second verify command).

**Manual checks:**
- The first autonomous landing after this story: `pixi run -e pyforge-guild spec-surface-check` on `main` is green with no human memlog edit; the run's journal carries `MRS-DISP-047` iff the session had not reconciled.

## Review Triage Log

### 2026-09-20 — Review pass
- verdicts: 21 findings — high 7, medium 9, low 3, false 2, maybe-false 0
- findings:
  - `[medium]` `[defer]` B1: `_refuse_via_merge_tree_preview` (dispatch_land.py:846-871) validates the pre-reconcile tree only; `_reconcile_spec_surface_drift`'s own commit (memlog + baseline JSON, pushed at dispatch_land.py:873-899) is never re-run through a merge-tree preview before `forge.merge_pr` at line ~938 — verified by tracing the call order at dispatch_land.py:830-935. Deferred: the correct fix (re-run the preview after a successful reconcile) is a second expensive preview invocation, and the reconcile only ever touches `.memlog.md`/`.spec-surface-baseline.json`, not runtime code, so practical risk is low; scope-expanding beyond this story's Approach.
  - `[high]` `[patch]` B2: `_reconcile_spec_surface_drift`'s `by_spec` construction (dispatch_land.py, `finding.check not in ("drift", "drift-presumed")` filter) silently drops doctor's per-spec `"no-baseline"` findings — verified via `pyforge.doctor.sources.chain._drift_findings` (chain.py:1871-1975), which emits `{"kind": "no-baseline", "path": name, ...}` for a spec with no stamped baseline entry, a message shape that matches `_SPEC_SURFACE_NAME_RE` but is excluded purely by the `check` membership test. A never-baselined spec drifted by this branch's own files is neither reconciled nor refused: the landing proceeds green while `main` stays unreconciled. Action: extend the filter to treat a `no-baseline` match as unconditionally foreign (fail closed, cannot verify per-file ownership with no baseline to diff against) and refuse with `MRS-DISP-048` naming that spec — mirrors this codebase's own established fail-closed convention (`dispatch_land_finalize._spec_status_for`'s "fails closed... on any git read failure"). Applied: `no-baseline` findings are now collected separately (a `no_baseline` set) and, in the own/foreign split, treated as foreign whenever this branch's own changed files include a path under that spec's tracked folder — refusing with `MRS-DISP-048` naming the spec; a `no-baseline` spec this branch never touched stays out of scope and unreconciled, consistent with the zero-overlap-drift precedent rather than the more blunt "unconditionally foreign" the action item first proposed. Verified by `test_reconcile_spec_surface_drift_refuses_no_baseline_spec_this_branch_touched` and `test_reconcile_spec_surface_drift_skips_no_baseline_spec_this_branch_did_not_touch`.
  - `[medium]` `[defer]` B3: the `sys.path.insert` + `from pyforge.doctor.sources.chain import gather_spec_surface` import (dispatch_land.py ~297-301) runs in-process inside the long-lived `dispatch_supervisor`, unlike `scripts/spec_surface_reconcile.py`'s identical pattern which always runs as a fresh subprocess (verified: that script's own docstring requires "ZERO environment" and is invoked per-call). A supervisor spanning a doctor-code deployment across serial dispatches would keep using the first-imported, now-stale `pyforge.doctor.sources.chain` via `sys.modules` caching. Deferred: proper fix needs either subprocess-based invocation or explicit `importlib` reload handling, both larger than a direct correction; the risk window (a supervisor process outliving a doctor deploy) is narrow.
  - `[medium]` `[patch]` B4: multi-spec reconcile (dispatch_land.py, `_reconcile_spec_surface_drift`'s per-spec loop over `sorted(own.items())`) batches every spec's memlog commit into one `vcs.commit_paths` call at the very end — if an earlier spec's append succeeds and a later spec's append/stamp fails, the earlier spec's edit is left as an uncommitted working-tree change. Worse than cosmetic: verified that a retry on the same worktree would then see the earlier spec as already-clean (doctor reads on-disk text, uncommitted or not) and skip re-touching it, so the final commit/push would never include it — a merged landing could silently leave that spec's fix uncommitted forever, the exact "green but main is still missing something" failure this story exists to close. `VcsPort` has no `checkout_paths`/discard method to add a rollback without new port surface, but a rollback isn't needed: `vcs.commit_paths` (already used elsewhere in this function) can commit each spec's memlog append immediately after it succeeds, inside the loop, rather than batching. Action: move the memlog commit inside the per-spec loop (one small commit per spec, immediately after its append succeeds); keep the baseline-stamp commit + push as the final step. Applied: each spec's memlog append is now committed via `vcs.commit_paths` immediately after it succeeds, inside the per-spec loop; the baseline-stamp commit + push remain the final step. Verified by `test_reconcile_spec_surface_drift_reconciles_own_drift_across_specs`, which now asserts 3 commits for a 2-spec fixture (two per-spec memlog commits plus the final stamp commit).
  - `[high]` `[patch]` B5: `_run_deferred_work_intake` (dispatch_land_finalize/__main__.py:42-85) shells out to `scripts/deferred_work_intake.py --fix`, whose `_ingest_project` (deferred_work_intake.py:97-186) writes `deferred-work-ledger.md` via an atomic `tempfile.mkstemp` + `os.replace` with no commit step at all — verified by reading the full ingest function. A successful intake leaves the primary checkout's tracked ledger file modified and uncommitted, reproducing exactly the "lands green, leaves main dirty" ritual this story exists to close, for this one file. Action: after a successful non-refusing intake run whose `--fix` actually changed the file, commit `deferred-work-ledger.md` in its own commit and push, mirroring `cli/land.py::_promote_deferred_work`'s already-established lock/write/commit shape for this exact tracked file. Applied (together with B7's lock, since both land in the same helper): `_run_deferred_work_intake` now reads the ledger's pre-`--fix` text, runs `--fix`, and — only when the text changed — publishes the new text onto `origin/main` via `vcs.commit_paths_onto_remote_tip` (an isolated detached worktree, never a commit on the primary checkout itself), then restores the primary checkout's own working-tree copy to its pre-`--fix` text; a publish failure is journaled as `MRS-DISP-047`, never silently dropped.
  - `[high]` `[patch]` B6: the intake call's leftover uncommitted `deferred-work-ledger.md` (B5) cascades into the *next* `finalize_dispatch_land` invocation's resync gate — verified by tracing `finalize_dispatch_land` (dispatch_land_finalize/__main__.py:192-196): `vcs.has_uncommitted_changes(root)` is checked at line 192, *before* `_run_deferred_work_intake` runs at line 196, so this call's own leftover dirty ledger silently makes the *next* call skip `_resync_home_branch` (`resynced = False`) even though nothing about that next landing was itself dirty. Same root cause and same fix as B5 (committing the ledger closes both). Applied: see B5 — the primary checkout's own tree is now restored to its pre-`--fix` text immediately after publishing, so it is never left dirty for the next finalize's `has_uncommitted_changes` gate to trip over.
  - `[medium]` `[patch]` B7: `deferred_work_intake.py --fix` has no advisory lock on `deferred-work-ledger.md` (verified: no `flock`/lock-file logic anywhere in the script, unlike `cli/land.py::_promote_deferred_work`'s dedicated sibling `.lock` file) — a concurrent dispatch landing for the same project racing `--fix` could lose an update. Action: acquire the same advisory-lock helper `_promote_deferred_work` uses around the intake subprocess call in `dispatch_land_finalize`, re-reading and re-diffing under the lock before deciding whether a commit is needed. Applied: `_run_deferred_work_intake` now acquires an advisory lock (`fs.acquire_advisory_lock`, mirroring `_promote_sprint_ledger`/`_promote_deferred_work`'s own primitive) around the full read/`--fix`/diff/publish/restore sequence, released in a `finally`.
  - `[high]` `[defer]` E4 (claim): doctor's `_drift_findings` (chain.py:1871-1975, `per_path` collapse) keeps only the lowest-rank, alphabetically-first spec's row when two co-governors are both simultaneously non-clean on the identical path — verified by tracing `rows.sort(key=lambda row: (row[0], row[1])); ... rows[0]` taking exactly one winner. `_reconcile_spec_surface_drift` can only ever see and act on this collapsed output, so the second co-governor's drift on that exact path stays invisible (and unreconciled on `main`) until a later pass. Deferred: doctor's `sources/chain.py` is explicitly read-only per this story's own Boundaries, and the only story-side fix (loop reconcile-and-restamp until `gather_spec_surface` reports fully clean) is a real scope expansion beyond a single-pass reconcile.
  - `[medium]` `[patch]` E2: same defect as B4 (partial multi-spec rollback) — Edge Case Hunter's cited rollback call (`vcs.checkout_paths`) does not exist anywhere in the real code, but the underlying "uncommitted edits left behind on partial failure" consequence is independently verified true. Grouped with B4; same action. Applied: see B4 — the same commit-inside-the-loop fix closes both rows.
  - `[medium]` `[defer]` E3: same defect as B1 (merge-tree preview never re-validates the reconcile's own commit). Grouped with B1; same disposition.
  - `[medium]` `[patch]` V1 (verification-gap, pre-verified): no test exercises `run_id` forwarding from `_run_and_journal_landing` into the real `execute_dispatch_land` call site. Silent-regression risk on a future refactor is real even though a wrong value today only drops the "(run <id>)" annotation from a memlog line. Action: add one test asserting the forwarded `run_id` reaches the memlog `--text` argv. Applied: added `test_run_and_journal_landing_forwards_run_id_to_execute_dispatch_land` (`test_dispatch_supervisor_spec_block.py`).
  - `[medium]` `[patch]` V2 (verification-gap, pre-verified): `_reconcile_spec_surface_drift`'s VCS/subprocess partial-failure branches are unexercised, notably the post-reconcile `resolve_ref` refresh at dispatch_land.py:901-935 (its own `VcsCommandError` → `MRS-DISP-048` path has no test). This is 100% new code from this story, not a pre-existing issue, so it does not fit `defer`'s "not this story's problem" test despite the reviewer's filed `defer` disposition. Action: add tests for the memlog-append failure, stamp-subprocess failure, commit/push failure, and the `resolve_ref`-after-push failure branches. Applied: added `test_reconcile_spec_surface_drift_refuses_when_memlog_append_exits_nonzero`, `..._when_memlog_process_errors`, `..._when_per_spec_commit_fails`, `..._when_stamp_subprocess_exits_nonzero`, `..._when_final_push_fails`, and `test_execute_dispatch_land_refuses_when_resolve_ref_fails_after_reconcile_push` (all in `test_dispatch_landing.py`).
  - `[high]` `[patch]` I1: `MRS-DISP-047`/`MRS-DISP-048` findings from `execute_dispatch_land`'s envelope never reach the journal payload — verified by reading `_run_and_journal_landing` (dispatch_supervisor/__main__.py:1004-1123): both `intent_entry.payload` and `outcome_entry.payload` carry only the coarse `envelope.verdict.value`/`landing_result.verdict.value` strings, never `envelope.findings`. Contrasted against the established `scope_violation_advisories`/`MRS-DISP-046` precedent (cli/dispatch.py:798, 838-852), which does serialize rich finding content into a payload key and reads it back — `cli/dispatch.py`'s `KIND_DISPATCH_LAND` reading loop (lines 832-837) has no analogous extraction at all. This directly violates the intent-contract's "Always: `MRS-DISP-047` is a warn, journaled, visible in `marshal watch` and `fleet-picture`'s ATTENTION rows" — today it is journaled but not visible beyond the coarse verdict. Action: serialize `envelope.findings` (via the existing `Finding.to_json_dict()`) into a new payload key on the outcome entry, add the matching read-back in `cli/dispatch.py`'s `KIND_DISPATCH_LAND` loop mirroring `resolve_scope_violation_advisories_from_payload`, and surface it in `marshal watch`/`fleet-picture` ATTENTION rendering alongside the existing scope-advisory rendering. Applied: `envelope.findings` now serializes via `Finding.to_json_dict()` into a new `land_findings` payload key on the outcome entry; `cli/dispatch.py`'s `KIND_DISPATCH_LAND` loop reads it back unconditionally (regardless of `ok`, since a refused landing is exactly the case that must surface) via the new `resolve_land_findings_from_payload`, threading it through `DispatchJournalFacts.landing_findings` → `FleetHomeFacts.dispatch_landing_findings` → `fleet_picture.py`'s ATTENTION rendering (an ERROR-severity finding, `MRS-DISP-048`, renders under `needs`; anything else, `MRS-DISP-047`, under `watch`), mirroring the established `scope_violation_advisories` precedent. Verified by new tests in `test_dispatch_supervisor_spec_block.py` (`test_run_and_journal_landing_journals_envelope_findings`), `test_dispatch_survival.py` (`test_gather_journal_reads_land_findings_regardless_of_ok`), `test_dispatch_verification.py` (`test_build_fleet_row_surfaces_landing_findings`, `test_build_fleet_row_omits_landing_findings_key_when_empty`), and the new `tests/meta/test_fleet_picture_landing_findings.py` (3 tests).
  - `[high]` `[patch]` I2: `dispatch_land_finalize`'s intake-refusal finding is computed but discarded — verified by reading `finalize_dispatch_land` (dispatch_land_finalize/__main__.py:196-210): `intake_finding` is appended to the local `findings` list, but `deploy_run.write(...)`'s `payload={"story_key": ..., "resynced": ...}` never includes it, and `_DeployRun.write` (cli/deploy.py:1786-1851) confirmed to treat its `findings` parameter purely as a side-channel for write-mechanism meta-failures, never embedding it into `payload`. Since the finding is always `Severity.WARN`, the `blocking` check (only `ERROR` severity) never surfaces it either — it has no observable effect anywhere, contradicting both this function's own docstring ("journaled... never silently dropped") and the story's Approach text ("journals any refusal"). Action: add the intake finding's serialized form into the existing `payload` dict, mirroring the already-present `resynced` boolean. Applied: `payload` now includes `"deferred_work_intake_finding": intake_finding.to_json_dict() if intake_finding is not None else None`, alongside `resynced`.
  - `[low]` `[patch]` B8: `_SPEC_SURFACE_NAME_RE` (dispatch_land.py:240) recovers a spec's name purely by regex-matching doctor's exact `"{name}: run --write-baseline --spec {name}"` message string, with only a code comment (not a test) documenting the coupling — a doctor wording change would silently break the match with no test failure to catch it. Not rejected despite low likelihood: adding one contract test asserting the regex matches doctor's actual current message format is a direct, one-test addition. Action: add that test. Applied: added `test_spec_surface_name_re_matches_doctor_message_formats`, asserting the regex extracts the spec name from all three literal message shapes `pyforge.doctor.sources.chain._drift_findings` emits today (`no-baseline`, `drift`, `drift-presumed`).
  - `[low]` `[patch]` B9: the automated memlog entries `_reconcile_spec_surface_drift` appends via `_bmad/scripts/memlog.py append` omit the optional `--by` flag (verified: `memlog.py`'s `append` subcommand supports `--by` for naming the entry's author, e.g. "user, coach" — confirmed optional, not required). Minor traceability loss, not a functional defect. Action: pass `--by marshal` on the append call. Applied: the memlog `append` call now passes `--by marshal`.
  - `[low]` `[patch]` S3 (secondary): the cross-project co-governor case (a spec name from a different project than the one being landed) is untested, though the code is structurally correct for it via generic `name.partition("/")` string handling — a coverage gap on an already-correct path. Action: add one parametrized test covering a cross-project spec name in `by_spec`. Applied: added `test_reconcile_spec_surface_drift_reconciles_cross_project_co_governor`, exercising a drifted spec named from a different project than the one being landed through the same own/foreign classification.
  - `[false]` `[reject]` S1 (secondary): claims the "could not evaluate drift" (ImportError/generic-Exception) degrade branches are an unspecified interpretive addition. Refuted: no bad outcome is claimed to follow, this is a defensible interpretive choice for a genuine spec gap (the Approach text never discusses an unreachable-doctor case), and it is already covered by the existing passing test `test_reconcile_spec_surface_drift_degrades_when_doctor_unreachable`.
  - `[false]` `[reject]` S2 (secondary): claims a partially-self-reconciled spec might produce a duplicate memlog entry. Refuted: traced `_drift_findings`'s `spec_moved`/rank logic in full — a partially-self-reconciled spec correctly produces an *additional* entry naming only the still-uncovered path, never a duplicate of an already-covered one.
