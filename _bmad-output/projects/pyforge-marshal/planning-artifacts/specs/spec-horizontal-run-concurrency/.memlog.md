---
topic: Horizontal run concurrency — bmad_loop's max_parallel stub, surfaced and readiness-assessed
updated: 2026-08-11T06:46
---

- (note) Input: docs/dreams/horizontal-run-concurrency.md, a rich, code-grounded Dream (file:line citations against both Marshal and the vendored bmad_loop package) -- express mode, no elicitation needed.
- (decision) Slug is horizontal-run-concurrency (Dream's own filename stem).
- (decision) Confirmed via direct read of bmad_loop==0.9.0's own policy.py (:448-451, :815-817, :841-842) that scm.max_parallel is unconditionally clamped to 1 in loads() regardless of the requested value, with the engine's own comment naming the gap 'Phase 5' and 'not built yet' -- this is an upstream stub, not a Marshal-side default choice.
- (capability) CAP-1: an operator who requests scm.max_parallel > 1 is told the request is inert (a registered finding/advisory) instead of it being silently floored to 1 with no signal, the way it is today.
- (capability) CAP-2: the bmad_loop parallel-fan-out gap is tracked in the same upstream-contribution register Story 6.8 already maintains (upstream-register.json), so marshal upstream surfaces it alongside the other 8 known gaps instead of it being invisible outside the vendored source.
- (capability) CAP-3: Marshal's own readiness for N-stories-in-flight (worktree isolation, shared journal, supervisor, landing path) is assessed and recorded, so adopting a future upstream Phase 5 scheduler is a scoped story rather than a fresh investigation.
- (constraint) Marshal must not attempt to build actual concurrent story dispatch while bmad_loop 0.9.0's own loads() clamps max_parallel to 1 server-side -- building against a knob the engine itself unconditionally defeats.
- (constraint) Nothing here touches the vendored bmad_loop package (AD-2/AD-3 wrap-never-fork; adapters/harness_bmadloop.py is the one seam) -- if a fan-out scheduler ships, it ships upstream.
- (constraint) Distinct from, and must not be confused with, Marshal's existing CROSS-project concurrency (multiple loop homes running simultaneously, prd.md:927 SM-5, 7 provisioned today) -- that capability is unaffected and out of scope.
- (note) A downstream story concluding 'still blocked upstream, revisit when Phase 5 ships' is an acceptable, complete terminal state -- mirrors upstream-register.json's own upstream_status: open entries; this Spec does not presuppose concurrent dispatch ships from Marshal's side at all.
- (question) What is the right advisory mechanism for CAP-1: a preflight finding (mirrors MRS-PREFLIGHT-* codes), a marshal config warning at render time, or both? Left to the downstream story.
- (question) For CAP-3's readiness assessment: does it need to be a written analysis only (a companion doc), or does it also want a synthetic/manual smoke test forcing bmad_loop's own internals past the clamp to observe what breaks? The former is safe and cheap; the latter risks depending on unsupported engine behavior. Left to the downstream story.
- (event) Pass 1 (coherence) PASS: all 3 CAPs carry intent+success; intents are WHAT not HOW (CAP-3's mechanism choice left as an open question); constraints rule out real design temptations (building against the clamp, forking bmad_loop, conflating with cross-project concurrency); 3 non-goals stated; success signal is observable per-CAP; CAP-1..3 ids are fresh and stable; prose lean.
- (event) Pass 2 (preservation) PASS: every load-bearing claim from the Dream (the unexplained Marshal-side literal, the upstream policy.py clamp evidence with line citations, the unregistered-gap finding against upstream-register.json's 8 existing entries, the zero grep hits for vertical/horizontal scaling terminology, the cross-project-concurrency distinction) lands in SPEC.md. No wrapper-only content dropped.
- (event) Spec finalized: 3 capabilities, 0 companions, 2 open questions carried from the Dream. status: draft.
