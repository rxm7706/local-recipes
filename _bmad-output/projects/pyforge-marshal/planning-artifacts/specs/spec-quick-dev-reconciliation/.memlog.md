---
topic: Two ways to finish a story, and Marshal has only ever heard of one -- bmad-quick-dev completions are invisible to Marshal's ledger/dashboard
updated: 2026-08-22T05:39
---

- (note) Governs sprint-status-ledger.yaml's generator (scripts/promote_sprint_status.py), core/status.py::derive_home_state (fleet view), and Epic 4's spec-promotion/reconciliation machinery in pyforge-marshal (3 candidate surfaces). Source: docs/dreams/quick-dev-reconciliation.md, captured 2026-08-11.
- (note) Confirmed empirically: grep for quick-dev/quick_dev across src/shared/packages/pyforge-marshal/ returns zero matches -- every reference lives in planning prose (architecture-bmad-infra.md, development-guide.md, source-tree-analysis.md), never in core/, cli/, adapters/, or a schema.
- (note) sprint-status-ledger.yaml's development_status map is a flat done|backlog vocabulary with no field for how a story reached done; scripts/promote_sprint_status.py:24 states plainly that bmad-loop marks a story done at DEV completion -- a quick-dev completion never calls that path, so the key stays backlog forever regardless of real, merged, tested work.
- (note) core/status.py::derive_home_state (Story 5.1, AD-5) derives fleet state from journals and run state only -- a quick-dev session leaves no journal entry, so Marshal cannot distinguish 'loop between stories' from 'a human just landed a real story by hand.'
- (note) Epic 4's durability guarantees (Story 4.1 spec promotion, Story 4.6 reconciliation) key entirely off bmad-loop's own journal/merge signal -- a quick-dev'd story's spec, if one exists, gets none of that promotion/durability treatment today.
- (capability) CAP-1 detect a quick-dev completion after the fact -- something observes git (repository fact) plus existing spec/story-identity artifacts and recognizes that a backlog story's work landed via a path other than bmad-loop, without a hand-maintained flag.
- (capability) CAP-2 fold the detected completion into the tracked ledger -- the story's key advances out of backlog in sprint-status-ledger.yaml (or the mechanism feeding it), and the record distinguishes bmad-loop vs bmad-quick-dev as the completion path, so marshal status / the fleet dashboard can show it without commit archaeology.
- (capability) CAP-3 give a quick-dev'd story's spec the same durability guarantee Story 4.1 gives a loop-landed one -- promoted means will still exist next week, regardless of which path produced it.
- (capability) CAP-4 mixed-mode coherence -- an operator can hand-pick any backlog story for bmad-quick-dev while that station's bmad-loop run is between stories, or mid-run on a DIFFERENT story, and Marshal's tracked state (ledger, dashboard) stays coherent either way; no corruption of the live run's own journal.
- (constraint) Not a routing change -- this spec does not decide whether Marshal ever invokes bmad-quick-dev on an operator's behalf (PRD Q-16's open question, untouched). Strictly reconciliation of a completion that already happened outside Marshal's control.
- (constraint) Not a concurrency/locking mechanism -- mid-run-different-story means the ledger stays coherent when a quick-dev completion is folded in around a live loop run, not that Marshal orchestrates the two simultaneously or arbitrates a same-file conflict. Genuine concurrent-writer coordination is out of scope (a sibling investigation thread owns concurrency more broadly).
- (constraint) Stays inside AD-5/AD-33 -- detection reads git (repository facts) and existing spec/story-identity artifacts, never a new hand-maintained flag, and never a special case bolted onto the loop's own journal format.
- (question) Which concrete detection signal (a merge-commit heuristic, a spec-promotion event, an explicit operator-declared 'this stroy was quick-dev'd' marker checked against observed diff/merge evidence, or some combination) answers 'did this story complete outside the loop' -- a story-level design decision, not resolved by the Dream.
- (note) Non-goal (from the Dream): bmad-quick-dev itself does not change and does not gain any Marshal dependency -- it keeps working exactly as it does today, run directly by a human against any station's codebase.
- (event) Spec drafted and self-validated 2026-08-11 (Pass 1 coherence + Pass 2 preservation against the Dream, both clean).

## Surface reconcile 2026-08-12 — story 5-9 landing (CAP-1..CAP-4 implemented)

- (event) All three of this spec's governed paths moved when story `5-9-a-story-finished-by-hand-isnt-invisible-to-the-ledger` landed. `core/status.py` (CAP-1/CAP-2) ADDS `not_loop_native_completions` -- the pure decision of which corroborated, non-Marshal-native, currently-`backlog` keys advance to `done` this run -- and `render_ledger_advancements`, the pure targeted rewrite of `sprint-status-ledger.yaml`'s text (only each named key's own status token changes, its trailing content and line-ending preserved verbatim). `cli/deploy.py` (CAP-1..CAP-4) ADDS the `deploy reconcile-completions --project <slug>` command: detection reuses `core.promotion.marshal_native_merged_keys`/`merged_story_keys` (both in the sibling `pyforge-marshal/spec-pyforge-marshal` surface) against git alone, corroboration reuses Story 4.1's existing `_scan_promotions` spec-promotion scan (CAP-3 -- the same durability guarantee a loop-landed story's spec gets), the ledger's own advisory lock now wraps the harness-based read that decides the rewrite (closing a TOCTOU) and its commit carries an AD-6 intent/outcome pair, and CAP-4 is proven by a dedicated test: a fake `HarnessPort` whose journal/run-status methods raise `AssertionError` if called, plus a temp-dir journal file asserted byte-identical before/after. `scripts/promote_sprint_status.py` ADDS `repair_feed` (extracted from its own `--repair-feed` inline logic), reused by `run_reconcile_completions` to close the Tier-3 feed divergence its own ledger write creates, scoped to only the keys that run advanced.
- (note) Detection signal resolved (closes this spec's own open question): the answer is a two-way git classification, not a hand-maintained marker -- `marshal_native_merged_keys` recognizes the `land-story` templated form and the bmad-loop-native form as "Marshal already knows"; everything else, corroborated by a valid Tier-3 spec AND real merge evidence, is reported `"not-loop-native"` (not `"bmad-quick-dev"`: `marshal land`'s own plain-PR-shaped landings are indistinguishable from a human's from git alone). PRD Q-16 (whether Marshal ever invokes bmad-quick-dev) stays open, per this spec's own constraint -- untouched by this story.
- (note) Re-verified after landing: `pyforge-marshal-test` 3538 passed (9 slow deselected), `pyforge-deps-test` 67 passed, `lint-imports --config src/shared/packages/pyforge-marshal/pyproject.toml --no-cache` clean (AD-4 core purity holds).

## Surface reconcile 2026-08-12 — trivial post-stamp follow-up on cli/deploy.py

- (note) `cli/deploy.py` moved again after the entry above: a one-line cleanup removing a stray, non-functional `# noqa: SLF001` comment (the rule is not enabled in this repo's ruff config). No behavior change.

## Surface reconcile 2026-08-12 -- review-pass patches on cli/deploy.py + scripts/promote_sprint_status.py

- (note) Both of this spec's governed paths moved again after the entry above, closing two review-pass patch findings: `cli/deploy.py::_render_text_reconcile_completions` now surfaces `missing_from_ledger` as its own text-format summary line; `scripts/promote_sprint_status.py::main()` gained a defensive `assert merged is not None` immediately after its `repair_feed()` call (the call site's own `(lost or missing)` guard already makes a `None` return structurally unreachable today, but the extraction decoupled the check from `repair_feed`'s own internal recomputation, so the assumption is now asserted rather than silently relied on). No behavior change. `pyforge-marshal-test` still 3538 passed (9 slow deselected).

## Surface reconcile 2026-08-13 — incidental overlap, unrelated story 14-4 landing

- (note) Two of this spec's governed paths, `cli/deploy.py` and `core/status.py`, moved when story
  `14-4-the-subprocess-seam-is-reconciled-and-sole-ownership-is-gated` landed -- this spec's own
  CAP-1..CAP-4 (`deploy reconcile-completions`, ledger advancement) are UNTOUCHED. The change is a
  mechanical import-path repoint only: both files' `ProcessPort`/`ProcessResult` imports moved
  from `pyforge.marshal.ports.process`/`pyforge.marshal.adapters.process_posix` to
  `pyforge.core.process` (`spec-pyforge-core`'s own CAP-6 contract) -- `run_reconcile_completions`,
  `not_loop_native_completions`, and `render_ledger_advancements` are byte-for-byte unchanged
  otherwise. Named here only so this spec's own baseline can be scoped-stamped without accepting
  any OTHER pending drift as reconciled.
- (note) Re-verified after landing: `pyforge-marshal-test` 3552 passed (9 slow deselected).
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event by claude) Surface reconcile 2026-08-31: 2 drift-presumed paths since baseline at `1c3a5aa7011e` (Story 14.4 merge). Eleven commits on governed `cli/deploy.py` and `core/status.py`:
  - `26102ea12c` — Story 14.4 (done): subprocess seam reconciled; deploy.py status integration.
  - `87abc37e5f` — Story 5.8 (done): dead supervisor sidecar doesn't hide live engine; status.py engine_alive.
  - `ef5b32fb91` — Story 25.5 (done): 0.11 status vocabulary on status.py.
  - `a1d48c113a` — Story 20.10 (done): landing-evidence grammar on status.py.
  - `d4686b0aa7` — Story 22.1 (done): dispatch verb on deploy.py.
  - `031cf6f550` — Story 22.2 (done): dispatch completion facts on status.py.
  - `331203258c` — Story 22.3 (done): dispatch verification on status.py.
  - `1a083201ce` — Story 22.6 (done): dispatch survives operator on status.py.
  - `58f2378e25` — pyforge-core hooks extraction: deploy.py import reparenting.
  - `e07834580d` — steward Story 30.2: Guildhall generator deletion doc cleanup.
  - `ec5a4e3da2` — review pass: empty-station reopening + stale docstrings on deploy.py.
  No CAP text change. RECONCILES: `src/shared/packages/pyforge-marshal/src/pyforge/marshal/cli/deploy.py`, `src/shared/packages/pyforge-marshal/src/pyforge/marshal/core/status.py`. Scoped-stamped: `--write-baseline --spec pyforge-marshal/spec-quick-dev-reconciliation`.
