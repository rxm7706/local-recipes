---
topic: model_tier_map names Cursor-native models with explicit harness tables; this IDE drives the wave; no unattended drain until the map lands
updated: 2026-09-13T10:34
---

- (note) Input: docs/dreams/cursor-native-tier-map.md plus operator ruling 2026-09-13 (re-point the map in a Story before unattended drain; drive the wave from this IDE). Headless express. Project pyforge-marshal. Physical path.
- (decision) Operator 2026-09-13: campaign executes in Cursor; Ultra included pool first; wall-clock / accuracy / subscription balanced per Story difficulty.
- (decision) Operator 2026-09-13: re-point model_tier_map in a Story before any unattended factory drain; the wave is driven from this IDE, not from drain, until that Story is done.
- (capability) CAP-1: fleet-wide model_tier_map uses the explicit {harness, model} form with harness = "cursor" for easy/medium/heavy, naming models from model-economics.md Cursor first-party catalog. intent: the policy states the Cursor Ultra ladder. success: all eight stations' marshal-policy.toml maps parse via parse_stage_entry as StageCandidate(harness="cursor", model=<catalogued>); no bare composer/grok string.
- (capability) CAP-2: fail-safe from spec-dispatch-tier-routing-fails-safe CAP-1 is unchanged. intent: a Cursor model never writes onto a claude adapter. success: existing provider-mismatch tests stay green; render_policy_toml / dispatch drop a cursor-catalogued model when the resolved adapter is claude.
- (constraint) Bare-string shorthand remains reserved for the base [adapter] provider (fails-safe memlog 2026-09-12). Cursor names are inline tables only.
- (constraint) Do not start marshal factory drain from this Spec. 28.29 Cursor headless wire stays dead until a later Story proves it.
- (constraint) Do not touch [context] layers, dispatch.max_parallel, or 33.11.
- (constraint) Do not flip any Epic 44 blocked key.
- (non-goal) Making Cursor headless dispatch work (28.29).
- (non-goal) Repricing the catalog live; prices stay the 2026-08-30 snapshot; adding missing Cursor rows (grok-4.6) from that snapshot is in scope.
- (assumption) composer-2.5, composer-2.5-fast, grok-4.6 are the Cursor-native ladder that matches the IDE wave (easy / medium / heavy).
- (event) Spec finalized 2026-09-13: status ready; Story 33.15 minted on Epic 33.
