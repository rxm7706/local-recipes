---
topic: Scope the templated merge-subject shape (AD-24 'Merge {key} into main') to project_slug, closing the one unscoped parser in classify_merge_subject's precedence chain
updated: 2026-09-11T18:33
---

- (note) Source: docs/dreams/marshal-templated-merge-subject-cross-project-collision.md, status dreamt. Sibling already-shipped fix for the same bug family: docs/dreams/marshal-land-cross-project-story-key-collision.md (parse_github_pr_merge_subject scoped via _branch_belongs_to_project, landing_evidence.py:161).
- (decision) This is a pain to solve: parse_templated_merge_subject (landing_evidence.py:199), the FIRST parser tried in classify_merge_subject's precedence chain (:332), takes no project_slug -- the one shape among 5 with zero scoping. Confirmed live 2026-09-11 against main (5,629 subjects): merged_story_keys(subjects, 'Merge {key} into main', 'pyforge-doctor') returns 129 keys, majority not doctor's own.
- (capability) CAP-1 intent: a templated-form merge subject ("Merge {key} into main") is trusted as project_slug's own merged key only when corroborated by a project-scoped signal, not text alone (the subject itself carries no station token by construction, AD-24). success: merged_story_keys(subjects, template, 'pyforge-doctor') against real main no longer returns 22.11/22.12 (marshal's own) or any other station's exclusive keys; re-running the reproduction from the Dream returns only keys doctor's own epics.md actually contains.
- (constraint) Must not regress the templated form's correct, intended use: a story genuinely landed BY pyforge-marshal itself (deploy land-story, which renders the template from policy for its OWN project_slug) must still classify as merged for that project.
- (constraint) The templated subject carries no station token in its own text (AD-24) -- the fix cannot rely on parsing the subject string alone, unlike the already-fixed GitHub PR-merge case which had a branch name to scope against. Needs either an out-of-band scoping signal or a documented, deliberate narrowing of what the shape is trusted to prove.
- (constraint) AD-4 pure-function discipline holds for core/promotion.py and pyforge.core.landing_evidence: no I/O, no subprocess, no pathlib methods added to the classifier itself -- any new scoping signal must be computed by an existing impure caller (cli/deploy.py) and passed in as a parameter, not fetched by the pure function.
- (non-goal) Not re-litigating the already-shipped GitHub PR-merge fix (parse_github_pr_merge_subject / _branch_belongs_to_project) -- that parser is untouched.
- (non-goal) Not adding a plausibility bound on raw epic/story numbers -- a core.identity.normalize concern, unrelated to this scoping gap.
- (non-goal) Not auditing every historical false positive this parser may have already produced before the fix lands (e.g. a past run_reconcile_completions mis-confirmation) -- scoped to closing the extraction bug itself.
- (non-goal) Not re-dispatching or re-landing any of doctor's Epic 22 work -- already recovered and landed by hand this session (local-recipes PRs #1251-#1254, #1256-#1257); out of scope.
- (note) Success signal: merged_story_keys/classify_merge_subject's templated-form branch, for any project_slug, returns only keys reachable via a corroborating project-scoped signal -- verified by re-running the Dream's own live reproduction against current main and confirming the contamination list (22.11, 22.12, 23.x, 28.x, 39.x-49.x for a doctor query) is gone.
- (event) Self-validate PASS 1 (coherence, rules 1-6,8): CAP-1 has intent+success; intent states a trust condition (WHAT) not a mechanism (HOW); 3 constraints each genuinely bend the solution space; 4 explicit non-goals; success signal is a concrete re-runnable reproduction. PASS.
- (event) Self-validate PASS 2 (preservation): walked the Dream claim by claim -- the bug location/mechanism, the live reproduction numbers, the AD-4 constraint, the no-station-token design tension, and all 4 non-goals all landed in SPEC.md. No wrapper-only content dropped. PASS.
- (event) CAP-1 landed: known_keys parameter added to _classify_merge_subject/merged_story_keys/marshal_native_merged_keys (core/promotion.py), wired into gather_dispatch_git_facts (dispatch_supervisor/__main__.py) via a new _load_known_story_keys ledger reader. Full test suite green (7,799 passed). Live reproduction: templated-shape matches for pyforge-doctor drop from 79 to 30, contamination gone. Status: ready -> shipped.
