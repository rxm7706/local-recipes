---
topic: Post-upgrade alignment of PyForge artifacts, patterns, and station code to the installed BMAD era (6.11/0.11 now, v7-ready)
updated: 2026-09-07T08:48
---

- (direction) Operator (2026-08-22): implement the retrofit the right way — Dream first capturing everything incl. the final four-wave plan, then spec, then code; full bmad-method 6.11 surface AND v7 trajectory are first-class inputs, not just bmad-loop
- (note) Source: docs/dreams/bmad-611-era-alignment.md (owner marshal — the misaligned surfaces are marshal's orbit: policy chain, status surfaces, seed templates, factory docs, DW scripts governed by spec-regenerable-factory). Grounded in two research passes (6.11 release surface; v7 trajectory incl. ticket-master branch) + a local inventory, all evidence dated 2026-08-22 and carried in companion alignment-inventory.md
- (capability) CAP-1 retired-ID purge + regression guard: no retired skill ID (the 20-shim list) survives in live docs, seed templates, memory, or code; a meta-test reds any reintroduction. Success: sweep of the 6 planning artifacts + dream-first-workflow.md.j2 + AGENTS.md + 3 auto-memory entries lands, and the new guard test passes on clean tree / fails when a retired ID is planted
- (capability) CAP-2 bmad-loop skill refresh: .claude/skills/bmad-loop-{setup,resolve,sweep} match the installed package canon (bmad_loop/data/skills). Success: diff-reviewed refresh lands (217/134/7 diff lines today) and bmad-loop validate stays clean across all 8 homes
- (capability) CAP-3 memlog-format migration: every spec folder accepts a 6.11 bmad-spec update. Success: the 8 frontmatter-less memlogs gain frontmatter, the 14 memlog-less folders gain a genesis-baseline .memlog.md (marshal S-13.7 bootstrap pattern), and memlog.py append succeeds against all 22 (upstream confirms format stable — memlog.py unchanged since 6.11.0)
- (capability) CAP-4 marshal policy-surface parity: the five bmad-loop 0.10/0.11 knobs (review.on_timeout, review.on_status_contradiction, limits.dev_contract_nudge, operator.enabled, verify.stream_capture_kb) are governable through the AD-16 defaults->project->flags chain with deliberate repo defaults. Success: policy.py + render pipeline expose them typed (0.11 rejects mismatched TOML scalar types), rendered policies pass bmad-loop validate, marshal suite green
- (capability) CAP-5 marshal 0.11 status vocabulary: awaiting-operator/confirm/preserve_ref/sweeps_refused are visible wherever an operator looks (marshal status, fleet-picture), a parked run is named awaiting-operator (run bmad-loop confirm) — absorbing DW-BL011-1's stall-check mislabel — and preserve_ref feeds the escalation-preservation flow (the standing non-destructive policy, now upstream-native). Success: fixture-driven status tests + stall-check reports parked runs distinctly
- (capability) CAP-6 deferred-work intake reads both sources: hand-driven build-auto runs' spec-frontmatter deferred: lists reach the tracked ledger without a human relay (bmad-loop's Engine._harvest_spec_deferrals already bridges LOOP runs since 0.9.1 — scope is the hand-driven gap + our scripts' intake). Success: the 2026-08-22 canary's DW-14-1-1 shape is ingested by the pipeline from frontmatter in a fixture; DW-BL011-2 closes
- (capability) CAP-7 living factory docs re-grounded with a named owner: architecture-bmad-infra.md current to 6.11 (render pipeline, TOML layers, new skill set) and the 8 project-context.md rulebooks refreshed via plain re-grounding agents per SYNC-RUNBOOK (NOT waiting on upstream's zero-signal successor); recurrence owned in the runbook cadence. Success: docs re-grounded with bumped source_pins; SYNC-RUNBOOK names the owner+cadence
- (constraint) Do not adopt what upstream is removing: no stories.yaml, no folder+id dispatch, no {spec-folder}/stories/ migration (removed on ticket-master; epic-story path is the surviving v6 route, V7-10 keeps it)
- (constraint) HOLD on the AGENTS.md managed block / project-context ledger — upstream reworking it live (#2715/#2733/#2750/#2754); re-ground existing rulebooks instead
- (constraint) Watch-don't-build for the unscheduled: TOML cutover + bmad-ticket tree get named triggers in companion horizon-watches.md, never code in this chain
- (constraint) Retired-name sweeps annotate historical/narrative text with a gloss rather than rewriting history; shims stay installed until CAP-1's guard is green — never answer remove-shims to the installer before then
- (constraint) Policy-surface work must emit exact TOML scalar types (bmad-loop 0.11 rejects coercible mismatches) and keep [dev] skill = bmad-dev-auto (the adapter discriminator — the invoked name resolves on disk)
- (decision) Wave-C decisions locked by evidence: stories.yaml NOT adopted (being removed); AGENTS.md block HELD (upstream churn); TEA no structural retrofit (module not installed, 1.23.3 is a chore release; tea-test-review CI gate stays optional adoption outside this chain)
- (decision) Non-goals: bmad-ticket tracking adapter (own future marshal Dream, trigger = tagged release); TOML-cutover symlink migration (steward Epic 14 watch — the named consequence rides that spec's failure-modes); upgrade-apply + version-drift detection (steward/doctor Epic 14, shipped or in chain); Paige/explain-system/bmad-ux (zero upstream signal)
- (assumption) memlog.py format stays stable through 6.11.x (unchanged in the 140 files moved since v6.11.0) — the CAP-3 migration will not churn
- (assumption) The v7 cut is months out (no date/milestone; next = 6.11.1 patch line; 6.9->6.10->6.11 monthly-plus cadence with a 6.12-ish bmad-ticket landing expected first) — CAP-1 is cheap insurance, not an emergency
- (question) CAP-7's recurring owner: fold living-doc re-grounding into the SYNC-RUNBOOK cadence as a marshal duty, or relay a per-station refresh obligation? Decide at story time; the one-shot re-ground is unambiguous either way
- (note) Companions: alignment-inventory.md (the 7+4 verified findings with evidence — multi-item table content, outgrows the kernel) and horizon-watches.md (Wave-D triggers table). Success signal: a planted retired ID reds the suite; a hand-driven run's deferral reaches the tracked ledger unaided; bmad-spec update succeeds against every spec folder; marshal governs and names everything 0.11 exposes
- (note) Preservation catch (pass 2): the false-green-window observation from the planning conversation — 6.11's status-before-commit finalize SHRINKS the window marshal's false-green detector contains (done only written at true finalize, post-review); detector stays valid, premise text updates when CAP-5's story touches that surface — rides CAP-5
- (event) Self-validate pass 1 (coherence 1-6+8): PASS — 7 CAPs intent+success, constraints all bend, 5 non-goals, testable success signal, IDs unique, lean
- (event) Self-validate pass 2 (preservation): PASS — Dream + both research passes + the four-wave plan walked claim-by-claim into SPEC.md/alignment-inventory.md/horizon-watches.md; one drop caught and restored (false-green-window note -> CAP-5 orbit); no silent drops
- (event) Spec finalized 2026-08-22: status ready, 7 CAPs, 2 companions, 2 assumptions, 1 open question; decomposing as marshal Epic 25
- (event) Format-migration verification 2026-08-22 (marshal Story 25.3): this append proves 6.11 memlog.py accepts the log
- (event) Story 25.4 (CAP-4) done 2026-08-22: five 0.10/0.11 knobs governable (28-key vocabulary, 5 new SEED; render maps 11; all 8 homes re-rendered validate-clean). Attribution note: the dev-pass diff sits on main inside commit 24c4dce923 (steward-subjected commit made in the shared tree on this story's branch, then fast-forwarded — parent-session commingling incident, deliberately not rewritten); the review pass, ledger flip, and the properly-attributed 'marshal 25.4:' subject ride the story's own commit on marshal/25-4-policy-knob-parity
- (event) Story 25.5 (CAP-5) done 2026-08-22 — landed as PR #612 (commit ef5b32fb91): the 0.11 vocabulary mirrored verbatim (awaiting-operator sixth fleet state, preserve_ref through the snapshot seam, sweeps_refused surfaced, stall-check parked-run classification; absorbs DW-BL011-1). FINALIZE COMPLETED POST-MERGE by the parent session: the orchestrator died at the spend limit after committing but before the ledger flip/memlog events, and its two delivered review layers (Blind Hunter + Edge Case Hunter) were dispositioned post-merge as DW-25-5-1 (see tracked ledger) — followup_review_recommended stands
- (event) Story 25.6 (CAP-6) in flight 2026-08-23: hand-driven build-auto spec-frontmatter `deferred:` intake — `scripts/deferred_work_intake.py` + doctor chain detector; closes DW-BL011-2 on merge. Loop runs unchanged (bmad-loop `_harvest_spec_deferrals` bridge).
- (event) Story 25.6 (CAP-6) done 2026-08-23 — PR #721 merged (`8d5e439f2d`), admin merge (CI billing blocker), maintenance label; finalize records Auto Run Result in spec-25-6.
- (decision) 2026-08-24 CAP-7 recurring owner = **marshal**: fold living-doc re-grounding into the SYNC-RUNBOOK cadence as a marshal duty (not a per-station relay). Matches parent SPEC "Owner: marshal". Cadence: after every CFE MINOR / `surface-changed` trip, and at least once per BMAD core minor bump — re-ground `architecture-bmad-infra.md` + 8× `project-context.md`.
- (event) 2026-08-24 Story 25-7 (CAP-7) in flight: living docs re-grounded to BMAD 6.11.0 / CFE v8.84.0; SYNC-RUNBOOK names marshal + cadence; this SPEC `open_questions` cleared.
- (event) Story 25-7 (CAP-7) done 2026-08-24 — PR #722 merged (`ac6c223c54`), admin merge (CI billing blocker), maintenance label; living docs re-grounded; SYNC-RUNBOOK names marshal+cadence; open_questions cleared. Finalize flips `25-7` + `epic-25: done` → marshal DRAINED.
- (event) Epic 25 done 2026-08-24, 7/7 (25.5 PR #612, 25.6 PR #721, 25.7 PR #722): CAP-1..7 shipped — the 6.11 round is complete; recorded 2026-09-05 (owed at the time). Dream flipped to realized the same day.
- (note) BMAD-METHOD 6.12.0 released 2026-09-04 (CHANGELOG dated 2026-09-03); the _bmad/ install is still 6.11.0 — the apply is steward Epic 14's, operator-gated. Re-check evidence = tagged tree + CHANGELOG + a diff of the installed 6.11 skills vs v6.12.0, never the release page; findings #9-#14 and the verified no-ops live in alignment-inventory.md, watch verdicts in horizon-watches.md.
- (decision) Round structure: this Spec stays the Dream's single contract across BMAD-era shifts — CAP-1..7 = the 6.11 round (shipped), CAP-8.. = the 6.12 round; capability IDs are never reused; status reads ready while a round awaits decomposition.
- (decision) The AGENTS.md HOLD is lifted (2026-09-05): the bmad:context managed block has been live since 2026-09-04 (verified against bd37dfd607) and 6.12's bmad-project-context adds the adopt intent with a retain/rewrite/relocate/delete ledger — the upstream rework the HOLD waited out has landed; bmad-project-context audit is the post-upgrade step.
- (decision) D1 (2026-09-05 upgrade planning session, adopted here): persistent_facts ships empty at 6.12 and stays empty — the AGENTS.md bmad:context block is the project-context surface; the 8 station project-context.md rulebooks retire once every consumer is migrated (never a file nothing loads).
- (capability) CAP-8 6.12 retired-ID roster: the guard follows the installed era's shim list — bmad-checkpoint-preview (new shim forwarding to bmad-walkthrough) joins the tuple; bmad-generate-project-context stays guarded although it is neither a 6.12 shim nor a removals.txt entry (an update orphans its 6.11 dir — the apply must delete it); the two living docs naming checkpoint-preview bare (architecture-bmad-infra.md, development-guide.md) are renamed or glossed. Success: the guard passes on the swept tree with the 6.12 list and fails on a planted bmad-checkpoint-preview; no orphaned shim dir survives the apply.
- (capability) CAP-9 project-context surface follows 6.12: the 8 project-context.md rulebooks retire per D1 with every consumer migrated first — doctor sources/factory.py classification + its pin-behind (context) row, scripts/fleet_scan.py, scripts/bmad_drift_check.py, SYNC-RUNBOOK living-doc cadence (rows 7-9 / 45-49 / 83-85) — and bmad-project-context audit becomes the post-upgrade step. Success: bmad-drift-check and detectors green with zero project-context.md rows; SYNC-RUNBOOK's cadence names architecture-bmad-infra.md + the AGENTS.md block only; no pin-missing HARD finding left behind.
- (capability) CAP-10 documentation pointers follow 6.12: llms.txt / llms-full.txt are discontinued — CLAUDE.md § BMAD Method Documentation re-points (local copy = the last snapshot, generated 2026-08-17, 6.11-era, frozen; live = docs.bmad-method.org task-organized docs + the package CHANGELOG); bmad-checkpoint-preview → bmad-walkthrough in living docs; architecture-bmad-infra.md re-grounded to 6.12 after the apply (CAP-7 cadence: once per core minor). Success: no live doc cites the dead URL; source_pin reads BMAD 6.12.0.
- (capability) CAP-11 bmad-loop 0.11.1 parity (CAP-2's recurrence made durable): refresh .claude/skills/bmad-loop-setup (module_version 0.11.0 → 0.11.1; -resolve / -sweep already identical) and add a meta-test that diffs the three repo skills against the installed bmad_loop/data/skills so CAP-2 holds by test, not memory. Success: diff empty; bmad-loop validate clean across all 8 homes; the test reds a planted one-line divergence.
- (constraint) Shims stay installed through the v7 cut (upstream's own rule); the guard is green so the pre-sweep bar is met, but every apply passes --shims explicitly — 6.12 made shims opt-in on fresh installs; clones of this repo inherit the tracked .claude/skills/, so that default bites only a NEW repo's npx bmad-method install (supersedes the 'never answer remove-shims before the guard is green' wording).
- (assumption) (supersedes) The v7 cut is still months out: 6.12.0 shipped 2026-09-03 without bmad-ticket or the TOML cutover, no date or milestone exists, cadence stays monthly-plus — CAP-1 / CAP-8 remain cheap insurance, not an emergency.
- (assumption) (supersedes) memlog.py and bmad-spec's spec-template are byte-identical 6.11 → 6.12 — CAP-3's migration holds; re-check at each core minor.
- (note) Verified no-ops at 6.12 (do not re-check): {diff_output} → {diff_file} (no _bmad/custom override, bmad-loop 0 refs); build-auto spec-template drift (Block If tier gone, review-log shape, followup_review_recommended semantics — repo readers consume only status / deferred / blocking condition); bmad-loop 0.11.1 adds no policy key (CAP-4 holds) and no marshal surface reads stop-request state (no CAP-5 gap yet); bmad-ticket and the TOML cutover did not ship (both watches stay).
- (question) CAP-8: derive the guard tuple from steward's release catalog (pyforge-steward data/bmad_core_releases/<ver>.yaml skill_renames + removals) instead of a hand-kept tuple? Default yes at story time — derive, don't declare — once the 6.12.0 catalog lands (its 2026-09-05 draft was lost with the swept worktree and must be re-derived from the unpacked package).
- (event) Self-validate pass 1 (coherence): PASS — CAP-8..11 carry intent + success; constraints bend (--shims, HOLD lifted → D1); non-goals carried with the WDS half glossed; success signal extended for the round.
- (event) Self-validate pass 2 (preservation): PASS — Dream § The 6.12.0 era shift (9 items) walked into SPEC.md, alignment-inventory.md #9-#14 + no-ops, and horizon-watches.md's verdict column; nothing dropped.
- (event) SPEC re-derived 2026-09-05 (bmad-spec update, executed per the skill's Memory-and-derivation procedure): status ready — CAP-1..7 shipped (Epic 25), CAP-8..11 await decomposition as a new marshal epic; companions alignment-inventory.md + horizon-watches.md updated.
- (note) 2026-09-06 correction to CAP-8 / Story 30.1: `bmad-generate-project-context` is NOT orphaned at 6.12 — it ships as `lifecycle: shim` under `src/bmm-skills/plan/` (steward catalog `data/bmad_core_releases/6.12.0.yaml:22-24`; every `skill-manifest.csv` row uses the `_bmad/bmm/<category>/` notation, so the missing directory is notation, not an orphan). The apply deletes nothing; it retires with the other 20 shims via the `--no-shims` apply (spec-bmad-suite-lifecycle CAP-10, 2026-09-06). Guarded ids become 21 (20 `v6-shims` + this plan-tree shim)
- (note) 2026-09-06 found while minting Epic 30.5/31 ledger keys: .claude/skills/bmad-sprint-planning/scripts/sprint_plan.py writes the Tier-3 feed with yaml.dump(doc, buf) at PyYAML's default width=80, so any key: value pair past 80 columns wraps the value onto an indented second line (valid YAML); scripts/promote_sprint_status.py's line-based _ENTRY parser then reads those stories as ABSENT and refuses the sync as a regression (steward 8, marshal 9, doctor 8, warden 1 done keys). Worked around by joining the wrapped pairs before sync; the durable fix is one width= argument in the in-place-edited sprint_plan.py (C5 file, same governance as the N.M key fix) — folded into marshal Story 31.4's surface widening as an explicit acceptance clause, and an upstream-PR candidate (needs an explicit ask)
- (decision) 2026-09-06 (operator): the two constraints 'Shims stay installed through the v7 cut … every apply passes --shims' and 'Policy work … keeps [dev] skill = "bmad-dev-auto" (the adapter discriminator)' are SUPERSEDED — shims retire now, because v7 has no date, the python-foundry cutover assumes an estate with no shims (spec-python-foundry-cutover, cutover-readiness.md P11), and bmad-loop 0.11.1 resolves whichever skill the policy names; the harness discriminator becomes bmad-build-auto. Recorded by spec-bmad-suite-lifecycle CAP-10; the retirement is one --no-shims apply (steward 14.9) after the harness flip (marshal 30.5)
- (capability) CAP-12 — Shim retirement follows the harness. intent: the marshal harness template and all 8 rendered loop-home policies name bmad-build-auto, every live caller leads with the live skill id (history glossed, never rewritten), and the retired-ID guard scans the harness template. success: test_harness_policy_render asserts bmad-build-auto; bmad-loop validate 8/8 after re-render; the guard reds a reintroduced bmad-dev-auto in harness_bmadloop.py; steward 14.9's pre-flight refusal reads green (Story 30.5)
- (decision) 2026-09-06 (operator): the non-goal 'TEA adoption (module install, tea-test-review CI gate) — optional, not alignment; the repo-custom bmad_tea_playwright.py generator stays' is RETIRED — TEA is fully adopted under spec-bmad-suite-lifecycle CAP-4 (operator decision 2026-09-06); the generator retires only behind a recorded equivalence check (lifecycle spine AD-5)
- (capability) CAP-13 — TEA adoption is marshal Epic 31 (relay). intent: TEA's bmad-testarch-* workflows produce every station's test-architecture.md, tea-test-review is a review lens in the harness policy, the generator + its two meta-tests + two pixi tasks retire behind an equivalence check, and the seven in-place-edited installer-owned files are governed. success: Stories 31.1–31.4 done; equivalence report on disk; spec-surface reports zero uncovered for the seven
- (event) SPEC re-derived 2026-09-06 (surgical, per the status-flip precedent): CAP-12/CAP-13 added to Round 6.12; the two shim constraints and the TEA non-goal replaced by their superseding text; status stays ready (Epic 30 + Epic 31 decomposed, not started; 30.5 and 31.1–31.6 minted the same day)
- (correction by claude) 2026-09-06 CAP-12 spec correction (Story 30.5, operator-directed "spec correction" over "new story"): the harness-rename half of CAP-12 -- renaming `harness_bmadloop.py`'s `[dev] skill = "bmad-dev-auto"` literal to `"bmad-build-auto"`, widening the retired-ID guard to scan the harness template, and re-rendering all 8 loop-home policies -- is FALSE. `bmad_loop` 0.11.1's `DevPolicy.skill` is a permanent internal adapter discriminator (`DEV_SKILLS = {"bmad-dev-auto"}`, hard-validated), verified against the installed package source; the skill actually invoked is resolved from disk at runtime, independent of this field. Renaming it throws `PolicyError` and would break `bmad-loop validate` on all 8 real loop-homes. SPEC.md's CAP-12 and the two shim/policy constraint bullets corrected in place. Story 30.5 landed only the independently-true caller-glosses (two dead-path fixes, three present-tense docstring/prose renames); the harness rename, render-test update, and guard-widening are permanently out of scope, not deferred. Matching corrections: spec-bmad-method-core-upgrade CAP-9 (steward), spec-bmad-suite-lifecycle CAP-10 (steward).
- (event) Story 30.2 (project-context surface follows 6.12, D1) implemented: migrated factory.py/bmad_drift_check.py/fleet_scan.py off the 8 project-context.md files, repointed fleet_scan.py's context glob at root AGENTS.md, rewrote SYNC-RUNBOOK.md's cadence language, ran bmad-project-context adopt for real on pyforge-marshal (unique content: Planner Constraints -> CLAUDE.md Rule 3, three-place-script-rule -> CLAUDE.md; JFrog content found already superseded by a more-current docs/reference/enterprise-deployment.md copy) and pyforge-atlas (unique content -> new child src/shared/packages/pyforge-atlas/AGENTS.md bmad:context block, Children rule), verified the other 6 stations boilerplate-only, then deleted all 8 project-context.md files. Reconciled + re-stamped 4 foreign spec-surface findings this touched (spec-pyforge-atlas, spec-pixi-candidate-currency, spec-factory-console, spec-pyforge-unifying-strategy) -- this spec's own surface stays [].
- (event by claude) CAP-13 outcome, Stories 31.1-31.2 (2026-09-07). 31.1 ran bmad-testarch-test-design for real (system-level, autonomous) against all 8 stations, substituting _bmad/custom/config.toml's [modules.tea] values for the confirmed-missing _bmad/tea/config.yaml -- real, grounded content every run (Contract: spec-bmad-suite-lifecycle CAP-4 / AD-5, AD-9, AD-10). bmad-testarch-framework was applicability-checked, not run live (no Playwright/Cypress surface anywhere in the repo). TEA's risk-tiered templates cannot enumerate every story id / test path the generator's Story Coverage Matrix does (0%-37% approximate story-id density per station -- see the report for exact per-station figures and two disputed baselines, herald 50 vs 64 and doctor 113 vs 73); equivalence narrowed for all 8 stations (0/8 full pass), full detail in planning-artifacts/reviews/tea-equivalence-2026-09-07.md. TEA's own 'owner: TBD' convention appears 7 times across 2 stations (atlas, herald) -- disclosed, distinct from a fabricated content gap; the generator's own TBD-free hard-fail rule holds trivially since no station's test-architecture.md was regenerated. Per AD-5's escape hatch, 31.2 is refused in full: the generator, its two meta-tests, and its two pixi tasks are byte-identical to before this pass (verified via git diff) -- none of CAP-13's Then-clause deletion actions fired. CAP-13's Success bar reads 'Stories 31.1-31.4 done; the equivalence report on disk; spec-surface reports zero uncovered for the seven' -- this entry covers only 31.1/31.2; 31.3 (tea-test-review review lens) and 31.4 (spec-surface governance, the third clause) are separate, not-yet-landed stories, not implied done here.
- (decision by claude) Story 31.3 (CAP-4): tea-test-review is a marshal review lens. Added _bmad/custom/bmad-review.toml with the tea-test-review lens (code, beside edge-case-hunter; refused when the AD-9 module roster lacks tea, AD-10). New core/policy.py SEED key review_min_score (33rd key), default 80 -- spec-bmad-suite-lifecycle's own open question 2 answered: upstream's example value, not a from-scratch calibration. Calibration plan: track this lens's findings against the first ten PRs it reviews after this story lands; if the 80 floor proves too strict (frequent warn-noise on genuinely-fine reviews) or too loose (real gaps it should have caught pass silently), retune via a project-layer marshal-policy.toml override, not a DEFAULT_POLICY change -- record the retune decision here when it happens. Rendered into all 8 loop-home policy.toml files (min_score=80, confirmed via bmad-loop validate's policy-OK check on all 8); bmad_loop 0.11's own [review] parser ignores the extra key (verified against the installed package), so it is inert to the harness and consumed only by the lens's own instruction at review time.
