---
topic: Dispatch tier-routing resolver never silently produces a hybrid adapter/wrong-provider-model launch
updated: 2026-09-12T20:22
---

- (note) Input: docs/dreams/dispatch-tier-routing-fails-safe.md (status: dreamt, owner: marshal). Headless, express mode.
- (decision) Scope: fix resolve_tier_launch's fall-through behavior (never write a StageCandidate's model unless its harness is genuinely dispatchable) AND revert model_tier_map's dev/review values fleet-wide (all 8 stations' marshal-policy.toml) to match the plain [adapter]/[adapter.review] baseline, since Cursor is confirmed permanently dead for headless dispatch (spec-28-29, shipped 2026-09-10).
- (capability) CAP-1 -- fail-safe tier resolution: intent = a StageCandidate whose harness cannot be resolved to a currently-dispatchable adapter in the loop home never partially applies (no hybrid real-adapter/wrong-provider-model write); success = a synthetic fixture with an unresolvable-harness candidate produces either the full unmodified baseline [adapter]/[adapter.review] with no stage override written, or a distinct loud finding -- never a written [adapter.dev].model mismatched to the adapter that will actually launch.
- (capability) CAP-2 -- fleet-wide sane defaults: intent = model_tier_map's heavy/medium/easy dev and review entries, across all 8 stations' marshal-policy.toml, resolve to the same models the plain baseline already uses, so no story's dispatch is silently routed through Cursor; success = re-running the same resolve_tier_launch/render_policy_toml probe that found the bug shows heavy/medium/easy launches with the exact same adapter+model as an untagged story, fleet-wide.
- (constraint) Never touch model_cost_catalog, pricing display, or the context.* compression layers -- resolver + config-values scope only.
- (constraint) Never make a Copilot/second-harness adoption decision here -- stays a separate, explicit operator call per spec-28-29's own recorded finding (quota history + wrap-composition rework cost).
- (constraint) Any FUTURE model_tier_map entry naming a model from a non-default-adapter provider must use the explicit {harness, model} inline-table form (already supported by parse_stage_entry) -- the bare-string shorthand is reserved for models belonging to the base [adapter].name's own provider, closing the exact ambiguity that let Cursor's model land on the claude adapter unnoticed.
- (non-goal) Deciding whether/when Copilot (or any other harness) becomes a real second dispatch target -- deferred.
- (non-goal) Building the per-station opt-in rollout / preflight-liveness-check machinery for a future second harness -- follow-on work once a harness is genuinely viable, not scoped here.
- (non-goal) Retroactively auditing every historical dispatch that may have silently hit this bug before now -- this Spec fixes the mechanism and the config going forward, not a history audit.
- (note) Success signal: a marshal factory spin on any station whose governing difficulty resolves to heavy/medium/easy launches with the exact same adapter+model the plain baseline would use, fleet-wide -- provable by re-running the same direct resolve_tier_launch/render_policy_toml probe used to find the bug; a synthetic unresolvable-harness fixture never again produces a mismatched stage override.
- (event) Self-validate pass 1 (coherence, rules 1-6+8): PASS -- both CAP-1/CAP-2 carry intent+success; 4 constraints each rule something out; 3 non-goals present; success signal is a re-runnable probe, not a dashboard; CAP IDs fresh and stable; prose lean.
- (event) Self-validate pass 2 (preservation): PASS -- every load-bearing Dream claim (cursor-dead precedent, historical trace to 5e7f8cac4b, the already-supported explicit {harness,model} form, all-8-stations scope, Copilot-decision deferral, cost-tracking layers untouched) lands in Why/Constraints/Non-goals. No wrapper-only drops.
- (event) SPEC.md rendered, status: draft. No open_questions or assumptions -- the Dream's own evidence (direct resolver execution) left nothing underspecified for CAP-1/CAP-2's scope.
- (event) Implemented + shipped 2026-09-12: resolve_stage_candidate returns None instead of a fabricated fallback (tier_routing.py); new provider_declaring_model reverse-lookup (model_cost.py); render_policy_toml skips a stage override whose catalogued provider mismatches the resolved [adapter].name (harness_bmadloop.py); all 8 stations' marshal-policy.toml model_tier_map dev/review reverted composer-2.5(-fast) -> sonnet/opus. Verified via direct re-execution of the bug-finding probe (no more [adapter.dev] override for heavy/medium/easy) + full pyforge-marshal-test suite (7919 passed). Story spec: planning-artifacts/specs/spec-dispatch-tier-routing-fails-safe.md. Residual, documented, not-yet-covered: marshal factory dispatch's own resolve_dispatch_model_with_retry_escalation reads resolved_models directly without render_policy_toml's new guard -- safe in practice today (config no longer contains any cross-provider model) but not code-gated for a future regression; cli/dispatch.py's own live binary+authcheck preflight is a separate, already-hardened protection this effort did not duplicate.
- (event) Residual gap closed same day, on operator request: cli/dispatch.py's own resolve_dispatch_model_with_retry_escalation path now carries the identical provider-mismatch guard as render_policy_toml, applied right after the live binary+authcheck walk's real resolution.profile is known. A mismatch drops the model override to None (clearing escalated/from_model/to_model/resolved_models['dev']) and records a new MRS-DISP-043 (WARN, registered in findings.py + verdict.py). New regression test test_dispatch_drops_a_tier_mapped_model_catalogued_under_a_different_provider passes; full suite 7920 passed. Both dispatch engines (spin and dispatch) now carry the identical guarantee -- no known residual gap remains.
- (event) Hygiene sweep found this spec's own surface: list was stale -- written before the dispatch-engine gap closure, it omitted core/model_cost.py, core/findings.py, core/verdict.py, cli/dispatch.py, and all 6 touched test files. Corrected to name every file actually touched by both commits (6113ac34f2, 97ca64d1e3), so this spec is the complete, freshly-reconciled owner spec-surface-check can defer to for the 5 broader/kernel specs that also govern these same files. Never had an initial baseline stamped at all -- fixing that now via a scoped --write-baseline.
