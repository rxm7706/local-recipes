---
topic: PRD: Marshal (pyforge-marshal) — graduated autonomy productized
updated: 2026-08-01T09:30
---

- (decision) RECOVERED (bootstrap from final PRD of 2026-07-25): Q-1 wrap-versus-absorb resolved as wrap+supervise — harness already conda-packaged, all known gaps outside the engine, fork triggers + FR-52 single seam preserve the escape hatch
- (decision) RECOVERED: agent-portability fold — v1 = skill-tree projection, adapter probe/conformance matrix, project-scoped policy, entry-file drift detection; copilot-api HTTP bridge SUPERSEDED; VS Code extension + @bmad chat adapter DEFERRED
- (decision) RECOVERED: 8 features / FR-1..58 / NFR-1..14 / C-1..10; detached-by-default launch; supervisor outside the session (NFR-4); journal self-owned (FR-18); no AI attribution (FR-35)
- (change) 2026-08-01 update: CAP-9 decomposed into FR-59 (landing rules are declared policy, incl. this repo's maintenance label + ungated env-sync) and FR-60 (marshal land — idempotent open/label/wait/merge/retire/resync with teardown-grade refusals and a journal verdict per landing); resolves Q-3; MVP scope 9.1 updated; counts 58->60
- (change) 2026-08-01: competitive framing re-scoped in section 4 — four unclaimed properties replace the eroded generic claim; Composio AO + Auto Mode + Ralph-loop practice added to the evidence; source market-agent-orchestration-research-2026-07-31.md
- (change) 2026-08-01: FR-13 motivation re-scoped — upstream v0.9.0 in-session budget guards credited; Marshal's requirement narrows to EXTERNAL un-disableable enforcement per NFR-4
- (change) 2026-08-01: FR-58 register — non-POSIX multiplexer entry closed as delivered upstream (v0.9.0 Windows psmux backend)
- (decision) 2026-08-01: convergence watch item added to 5.4 — upstream method roadmap names Dev Loop Automation; on overlap, re-evaluate the seam, not the wrap (watch, not a fork trigger)
- (event) 2026-08-01 surgical update complete; Q-10..14 markers normalized to post-re-render state; frontmatter updated; status remains final
- (change) 2026-08-01 update: durable-runs Spec (0 FR coverage, 0 epic coverage) decomposed into 3 new FRs. FR-61 bounded-loss durability (7.2 Run supervision) -- stage-boundary push + fleet-launch wiring, from spec-durable-runs CAP-3/CAP-4. FR-62 durability as a reported fleet property (7.5 Fleet visibility), from CAP-5. FR-63 fleet-wide branch retirement (7.4 Landing and paper trail), from CAP-6 -- explicitly distinguished from FR-59's per-landing branch-retirement policy key (narrower: retires only the branch just landed) since both now exist in the same PRD. CAP-1/CAP-2 (unpushed-work-check, loop-push-watch) already shipped as standalone repo scripts outside the marshal CLI package surface -- cited as motivating evidence in FR-61, not given their own FR numbers, since they are not marshal-CLI features. MVP scope 9.1 updated; counts 60->63.
- (decision) 2026-08-01 update: spec-fidelity-enforcement decomposed MARSHAL-ONLY (operator scoping decision) -- CAP-9 (Doctor: install the judge) and CAP-6 (Scribe: actor-attributed event record) explicitly out of scope for this PRD, flagged as named cross-project follow-ups for pyforge-doctor/pyforge-scribe's own chains, not touched. Of the remaining 7 Marshal-attributed capabilities, only 1 (CAP-4) became a new FR (FR-64). Rationale per capability: CAP-1 (detector CI trigger) already shipped and already has its own governing surface (spec-fidelity-enforcement claims .github/workflows/detectors.yml + scripts/detectors.py directly) -- not marshal-CLI-package code, cited as evidence only, same treatment as durable-runs CAP-1/CAP-2. CAP-2 (INV-4 story-done-without-spec check), CAP-5 (Spec<->chain reverse gate, explicitly extends dream_chain_check.py), CAP-7 (ungated-boundary reporting), CAP-8 (fixed-point-per-reconciliation record) are all repo-level detector/BMAD-skill tooling (dream_chain_check.py / bmad-drift-check's reconciler), governed by spec-fidelity-enforcement itself or spec-regenerable-factory -- not the marshal CLI package, so no marshal FR. CAP-3 (automatic Tier-2 promotion) is CONVERGENT with the already-specified FR-30/Story 4.1 (promote every merged story's spec from Tier-3 scratch into tracked planning-artifacts/specs/, committed by Marshal) -- verified by re-reading FR-30's exact consequences, which already state this in full; no new FR needed, no gap. CAP-4 (gate evaluation binds to a tracked spec's Success signal) is the one genuinely new marshal-CLI capability -- squarely Epic 2's charter (marshal gate evaluate) -- added as FR-64.
- (decision) 2026-08-01 update: spec-one-front-door decomposed. Of its 5 capabilities, 2 are CONVERGENT with already-specified FRs and got no new FR -- CAP-1 (marshal run) is substantively FR-9/FR-10/FR-11 (Epic 3, marshal factory spin) under a candidate new verb name; CAP-4 (marshal land) is exactly FR-59/FR-60 (already shipped-spec'd 2026-08-01 for pr-lifecycle) with no new behavior. CAP-2 (marshal status composing fleet+durability+detectors) is MOSTLY convergent with FR-36 + FR-62 (just added this session) -- its one residual (summarizing check's detector-registry findings within status) folded into FR-65 rather than a separate FR. CAP-3 (marshal check) is genuinely new -- FR-65. CAP-5 (context supplied once) folded into FR-65 as a cross-cutting consequence rather than its own FR, since marshal is already one CLI (not multiple binaries) and FR-49's layered policy composition already supplies policy context once; what CAP-5 adds beyond existing architecture is threading that same discipline explicitly to the NEW check verb, which FR-65 states directly. Q-14's queued spec-one-front-door reference resolved with a pointer to Q-15 (exact verb naming for run/status/land, still open -- FR-65 does not rename them) and Q-16 (route-vs-contain boundary per bmad-* skill, still open). Row-6 package triage (keep/wrap/remove 5 unexercised bmad-* packages) is NOT a marshal-CLI PRD question -- belongs to FR-58's upstream register or a repo-level decision, not recorded here.
