# Project-scoped BMAD config for `deptry-scanner` (unified dependency-hygiene +
# vulnerability scanner). Loaded as resolver layer 5 (team, committed) when this
# project is active.

# Override the global output_folder so this project's artifacts land under its own subdir.
output_folder = "_bmad-output/projects/deptry-scanner"

# Project metadata (informational; not consumed by skills today, but useful for tooling).
[project]
slug = "deptry-scanner"
description = "Unified dependency-hygiene + vulnerability scanner orchestrating deptry + osv-scanner over Python/Conda/Pixi manifests; schema-validated ComplianceReport + strict CI exit-code gate. In-repo pixi build workspace member at src/shared/packages/deptry-scanner (builds conda + wheel + sdist). Intake spec: docs/specs/deptry-scanner.md."
status = "active"
