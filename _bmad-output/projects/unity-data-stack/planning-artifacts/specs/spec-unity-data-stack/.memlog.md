---
topic: unity-data-stack — enterprise innersource python-first monorepo platform (Constitution + pixi workspace + compliance chain), PRD+architecture depth only, no epics by design
updated: 2026-07-25T14:50
---

- (direction) User (via coordinator): distill unity-data-stack's approved brief+PRD+architecture-spine chain into the Spec, express/headless, non-interactive; do not re-plan or invent scope. Note: no epics exist by design (far-horizon platform, decomposes fresh when scheduled) - distill from PRD+architecture depth only
- (decision) Slug inherited from BMAD project unity-data-stack; spec folder under active project planning-artifacts/specs
- (decision) Express distill (input very rich: Dream + brief+addendum + PRD 1361 lines/60FRs/9 features + PRD addendum + architecture spine 23 ADs/final + 2 research reports); non-interactive headless session
- (decision) CAP-N mapped 1:1 to the PRD's own 9 feature groups (5.1-5.9), each already FR-grouped - no re-grouping needed, the PRD's structure is the capability map
- (decision) ARCHITECTURE-SPINE.md adopted as companion (not source) - 23 ADs + Stack + Structural Seed + Capability-to-Architecture-Map + its own Deferred/Open-Questions/Assumptions tables are too detailed to collapse into the kernel without losing load-bearing rule text; Constraints field carries only the sharpest subset directly, referencing the companion for the rest
- (decision) constitution-provenance.md authored as a companion - the PRD's Constitution Article map (14.1/14.2, 14 Articles) and its 8 required amendments (14.3) are a multi-item catalog per Spec Law rule on companions, and are explicitly named load-bearing by the task
- (capability) CAP-1 Workspace Substrate: a platform engineer declares one Workspace root (platform matrix, channels, system-requirement floors, Packages) from which Environments compose from Features with no inherited bloat and every Package carries a declared owner; success = FR-1..9 - one-place Package addition, no duplicated dependency declarations, minimal Environments measurably lean against a ceiling, Stages modelled separately from Environments so solve count is bounded by genuine dependency variation not Stage naming (AD-4)
- (capability) CAP-2 Dependency Resolution and Lock Architecture: the workspace produces one authoritative Workspace Lock (native+Python together) that reproduces an Environment offline on every declared platform, with a derived PEP 751 export and an air-gapped Offline Bundle, and credentials are host-scoped and never in a URL or argument; success = FR-10..17 - multi-platform coverage verified by materialization not assumed (FR-11/AD-3), the Exported Lock drift-checks against the Workspace Lock and fails the gate on mismatch (FR-12/AD-2, resolves PRD OQ-1)
- (capability) CAP-3 Quality Gate: a developer runs one command executing exactly what CI executes - lint/format/type/coverage/security/behavioural-test tiers - with pre-commit mirroring a fast subset within a time budget; success = FR-18..25 - a parity check fails the gate when the local and CI check-sets diverge (AD-9), coverage thresholds enforced with no regression allowed
- (capability) CAP-4 Constitution and Governance: every Constitution Mandate is classified machine-readably as a Platform Invariant or an overridable Domain Default, violations name their clause, and amendment is a governed versioned process; success = FR-26..32 - an unclassified Mandate or an override without a linked decision record fails the gate (AD-8), the Constitution carries semver+ratified/amended/next-review dates, a coverage report shows enforced-vs-human-only Mandates
- (capability) CAP-5 Innersource Contribution Model: every Package names a Trusted Committer, an outside contributor finds a documented path to contribute to code they do not own, and branch/commit/merge conventions are enforced automatically; success = FR-33..38 - a Package without a Trusted Committer fails the gate, a generated scaffold passes the Quality Gate with no manual fixes (FR-37), cross-team contribution rate and internal-fork counter-signal are both measured (FR-38)
- (capability) CAP-6 Supply-Chain Compliance and Evidence: every built artifact carries a versioned SBOM (populated dependency graph, runtime+full variants) and a build-provenance attestation, continuously gated against exploitation-aware vulnerability data via one schema-validated Compliance Report, with baselining and opt-in remediation proposals; success = FR-39..47, delivered by integrating pyforge-warden (a strict superset of the intake approach) not reimplementing it - SBOM generation runs against the built artifact asserting a populated transitive edge (AD-11), an artifact without provenance attestation cannot promote to an approval-gated Stage (AD-12)
- (capability) CAP-7 Data Product Platform: each Domain owns Data Products layered Raw/Curated/Consumption with enforced naming/metadata/versioned-contract conventions, and one reference Domain (customer) is implemented end to end as the pattern others follow; success = FR-48..54 - a schema change breaking a declared consumer is detected before merge (FR-52/AD-16), the reference Domain exercises all three Layers and passes every gate, its structure is what the scaffolding templates generate
- (capability) CAP-8 Deployment, Environments, and Air-Gap: every capability available online is available in Air-Gap Mode or declares why not, deployment is declarative and environment-promoted with Stage-gated policy, secrets are never committed and are validated at startup, and a Stage's Data Classification bounds what datastores/network posture it may use; success = FR-55..58 - a parity test enumerates capabilities and asserts each works air-gapped (SM-6 target 100%), a committed secret-shaped string fails the gate (FR-57)
- (capability) CAP-9 Developer Experience Surface: a developer starts/stops/inspects the full local service stack (aggregate and per-service) with single commands, and the Workspace names a small, stable public task API; success = FR-59..60 - status reports actual health not process existence, removing or renaming a public task is a breaking change requiring a decision record
- (constraint) AD-2: exactly one lock - the Workspace Lock (conda+PyPI together) - is authoritative and committed; the Exported Lock (PEP 751) and Offline Bundle are generated FROM it, never hand-edited nor used as a resolution input; every derived artifact for a release/Stage-promotion is generated from one pinned Workspace Lock commit SHA and drift-checked against that SHA, not against current state - resolves PRD OQ-1
- (constraint) AD-3: multi-platform coverage is proven by materialization, never inferred - for every declared platform x every deployable Environment, a gate materializes it from the lock and fails if it cannot; coverage is reported per platform, never as a single boolean
- (constraint) AD-7: dependency direction is one-way (shared -> platform-infrastructure -> domain, never upward/sideways) and Domains are peers; a Domain may consume another Domain's published Data Product/API only, never import its Package or reach its datastore directly; a cycle detector runs in the Quality Gate
- (constraint) AD-8: every Constitution Mandate carries a stable identifier and a machine-readable classification of exactly platform-invariant or domain-default; a Platform Invariant admits no override; a Domain Default override requires a linked decision record and is enumerable; an unclassified Mandate or an unattributed check both fail the Quality Gate
- (constraint) AD-9: every gate check is a named task; CI invokes task names and never inlines a command, installation step, or environment mutation; task names are globally unique, <target> in <verb>-<target> is always a Package name never a tag; a parity check + a name-uniqueness check both run in the gate
- (constraint) AD-10: credentials live only in the workspace manager's credential store or masked runner inputs; no committed file contains a credential-bearing URL (incl. variable-interpolated forms); no process receives a credential as a command-line argument; a request attaches a credential only when its host matches the credential's configured host
- (constraint) AD-13: every deployable Environment is declared with no inherited default dependency set and composes only what it names; installed size is measured, asserted against a recorded ceiling, and a regression fails the Quality Gate (exempt: the FR-7 compatibility-detection Environment, explicitly non-deployable)
- (constraint) AD-17: each plane and cross-cutting concern resolves to exactly one accountable pyforge-crew station (Marshal/Atlas/Warden/Mason/Steward/Doctor/Scribe/Herald); a capability with no station, or claimed by two, is a defect - full station map in the architecture-spine companion
- (constraint) The Constitution's 14 Articles (I-XIV + Governance) are the requirement spine this PRD traces every FR against; full Article-to-FR provenance map + the 8 required amendments are in the constitution-provenance.md companion - not restated here
- (constraint) The intake toolchain spec's flagship lock-generation command, pdm export --format pylock --override-platform=..., uses a flag that does not exist on pdm export (verified 2026-07-25) - this specific mechanism cannot be reused as written; AD-2/AD-3 replace the promised-but-unverified multi-platform guarantee with gate-verified materialization instead
- (constraint) Compliance chain is delivered by integration, not reimplementation - pyforge-warden (merged 2026-07-25) already implements a strict superset of the intake spec's compliance approach; AD-6 consumes it as a CLI in a lean isolated Environment, never imported as a library, output is the Compliance Report file and nothing consumes its internal APIs
- (constraint) Primary Python targets are 3.13 and 3.14; 3.12 is legacy-consumer-only (security-phase upstream, no further binary releases); 3.15 first-releases 2026-10-01 and must be planned for inside this horizon - revises the Constitution's stated preference (Art. XIV amendment, constitution-provenance.md)
- (non-goal) Unity is not an Internal Developer Portal - no catalog UI, no service-discovery portal; it emits catalog-consumable facts, an adopter running Backstage integrates rather than migrates
- (non-goal) Unity is not a build-graph engine - no attempt to out-cache Pants/Bazel/Nx on fine-grained caching, affected-target computation, or remote execution; orthogonal and unwinnable
- (non-goal) Unity does not maintain a second registry of truth - manifests are the source, everything else (catalogs, ownership maps, portal feeds) is derived
- (non-goal) Unity is not a product to be sold - it is a platform an enterprise runs
- (non-goal) Unity does not replace a Domain's judgement about its own data models - global interoperability concerns are Platform Invariants, local modelling is a Domain Default (FR-26)
- (non-goal) Unity does not target SLSA Build L3 in v1 - L1 mandatory, L2 goal; L3 requires hardened builders, a different problem class, deferred
- (non-goal) Unity does not perform data-content inspection in v1 - Data Classification is enforced at the configuration boundary (which datastore, which network), not by content-level PII detection/masking/right-to-deletion (FR-58)
- (non-goal) Unity is not a general-purpose polyglot monorepo - Python-first by mandate
- (non-goal) Bootstrapping new Unity instances - depends on pyforge-genesis, unbuilt; a v2 dependency not v1
- (non-goal) Local Kubernetes development - the required cluster tool is not available through the mandated channel on all platforms; keep the intake root's documented stub
- (assumption) A-3/AA-1: pixi-primary lock architecture - Workspace Lock (pixi.lock) is authoritative, PEP751 pylock.toml is a derived export, offline deployment uses pixi-pack/pixi-unpack; conda-native resolution is the differentiator and the alternative (PDM/PEP-751-primary) discards it - this is the architecture's resolution (AD-2) but per README still needs explicit human confirmation, see open questions
- (assumption) A-4: v1 targets SLSA Build L1 mandatory, L2 (signed provenance from a hosted build platform) as the goal; L3 is out of scope
- (assumption) A-5: v1 delivers the Domain pattern plus one worked reference Domain (customer); the remaining ten Domains are adoption work, not build work - changes effort by an order of magnitude if wrong, see open questions
- (assumption) A-6: Data Classification is enforced at the configuration boundary (datastore/network posture); content-level inspection is deferred to v2
- (assumption) A-9: primary Python targets are 3.13 and 3.14; 3.12 is legacy-only; 3.15 (2026-10-01) must be planned for inside this horizon - revises the Constitution, part of the required amendments
- (note) Success signal source: brief Success Criteria S1-S6 + PRD Success Metrics SM-1..SM-9 (with SM-C1..C4 explicit counter-metrics not to be gamed) - onboarding under 1hr/single-digit-commands (S1/SM-1), cross-team contribution trending up with internal-fork counter-signal not rising (S2/SM-2 vs SM-C1), reproducibility 100% verified-not-assumed on every declared platform+offline (S4/SM-4), compliance latency in minutes ahead of the EU CRA 2026-09-11 reporting-obligation deadline (S5/SM-5), air-gap parity 100% with exceptions declared not silent (S6/SM-6)
- (question) OQ-1 (README-flagged, blocks everything): is the Workspace Lock authoritative with the PEP751 export derived, or the reverse, or split by tier? AD-2 (architecture) already resolves this as workspace-lock-primary and this Spec carries it as an assumption, but the PRD/README state it still requires explicit HUMAN confirmation before any build work - not yet independently ratified by a human
- (question) OQ-2 (README-flagged, order-of-magnitude sizing): does v1 deliver the pattern plus one worked Domain (customer), or all eleven Domains? Carried as Assumption A-5 but not confirmed at PRD sign-off - changes MVP effort by an order of magnitude if wrong
- (question) OQ-5: which Constitution Mandates are Platform Invariants (no override) and which are Domain Defaults (Domain-overridable with a recorded decision)? AD-8 supplies the classification MECHANISM but the actual per-Mandate classification is an unresolved PRD-sign-off decision - directly determines whether Unity is genuinely innersource (federated governance, Data Mesh principle 4) or centrally imposed
- (question) OQ-18: where is the boundary between the Constitution's spec-kit governance and this repo's BMAD planning chain? Both are live simultaneously (Risk R-5, 'governance double-stack') - unresolved, risks the two drifting independently if not decided explicitly
- (event) Self-validate pass 1 (coherence, Spec Law 1-6+8): PASS - 9 capabilities all intent+success mapping 1:1 to PRD's own 9 feature groups; intents WHAT-only (mechanism detail like AD rule text isolated to companions); every constraint rules something out (AD-2/3/7/8/9/10/13/17 + 14-Article spine + pdm-flag finding + integration-not-reimplementation + Python targets); 10 non-goals; success signal is measurable (SM-1/2/4/5/6 with explicit counter-metrics); CAP IDs fresh CAP-1..9
- (event) Self-validate pass 2 (preservation): PASS - every PRD feature group (5.1-5.9, FR-1..60) lands in a capability; the Constitution's 14 Articles + 8 amendments land in constitution-provenance.md companion per the task's explicit load-bearing instruction; the pdm --override-platform flag finding lands in both a Constraint and the companion, not dropped; the 4 headline open questions (lock authority, domain count, mandate classification, governance boundary) all land in Open Questions, matching README's own before-resuming callout. Wrapper-only content dropped: the full 23-item PRD Open-Questions table and the architecture spine's own AQ-1..7/AA-1..3 tables (these are verify-before-build technical items, not sign-off decisions, and stay live in the adopted architecture-spine companion + PRD source rather than duplicated); the detailed research-deltas D1-D17 grading table (evidence, cited by conclusion only)
- (event) Spec finalized (create): SPEC.md derived from memlog; two companions - constitution-provenance.md (spec-authored: 14-Article map + 8 amendments + the pdm-flag finding) and ARCHITECTURE-SPINE.md (adopted, not edited: 23 ADs + Stack + Structural Seed + Capability-to-Architecture-Map, too detailed to collapse into the kernel without losing load-bearing rule text); sources docs/dreams/unity-data-stack.md + brief + brief-addendum + prd.md + prd-addendum fully absorbed. No epics exist by design (README: far-horizon platform, decomposes fresh when scheduled) - Spec derived from PRD+architecture depth only, per task instruction
