---
scope: cf_atlas Kedro/Dagster/DuckDB migration (22 FRs, waves 0+A-H)
purpose: build-substrate
altitude: initiative
updated: 2026-07-17T02:13
---

- (event) Headless create run; inputs: PRD+addendum (final, 2026-07-17), spec v5.6, technical research 2026-07-16, brownfield architecture-cf-atlas/-conda-forge-expert/-mcp-server (read-only), agents-and-skills.md, intake-groundtruth-2026-07-17. Altitude=feature (feeds epics), purpose=build-substrate.
- (decision) Paradigm: declarative dataflow (pipes-and-filters over a declared Data Catalog) — Kedro modular pipelines of pure nodes; binds all FR surfaces; prevents procedural-orchestrator regrowth. [ADOPTED from spec §4-5]
- (decision) AD-1 Kedro DAG single source of truth; all glue (kedro-dagster, kedro-mcp, BSL libs) thin+replaceable; prevents orchestrator lock-in given Prefect/Dagster acquisition risk. [ADOPTED spec §4.4/addendum §2]
- (decision) AD-2 catalog-owned IO, zero data access in nodes; credentials scoped per destination host in catalog/dataset config — fixes legacy _http.py JFrog-on-every-request defect; prevents ambient-credential leakage under loop bypass-permissions. (FR-1)
- (decision) AD-3 seven domain pipelines fixed; dataset ownership = producing pipeline; cross-pipeline access only via named catalog datasets (Phase H: PyPI Intelligence produces, VCS & Health consumes). (FR-2, spec §5.2)
- (decision) AD-4 partitioned Parquet canonical persistence from Wave A; DuckDB the only query/compute/graph/vector engine; sqlite3 import forbidden outside retired legacy tree (grep-gated at F1). (FR-5)
- (decision) AD-5 IncrementalParquetDataset carries per-dataset TTL (never global); phase_state deleted; resumability = Kedro runner + persisted intermediates. (FR-3/FR-4)
- (decision) AD-6 Dagster via kedro-dagster compile; per-node timeouts mandatory (1800s coarse-cap class structurally impossible); 3 bootstrap profiles = named job configs, explicit config beats profile defaults; Phase P PHASE_P_ENABLED=1 admin-only never default-scheduled. (FR-6)
- (decision) AD-7 MCP tools authored directly over Kedro session/catalog APIs (FastMCP patterns); kedro-mcp wrapped never load-bearing; audit scope = 23 atlas-relevant tools of conda_forge_server.py; library-futures/add-handoff/seed-gap suggesters stay CLI-only. (FR-7)
- (decision) AD-8 BSL (Ibis→DuckDB) is the single semantic translation interface; Vizro, Vizro-AI, MCP reads and A2A insights consume declared dimensions/measures, never raw SQL. (FR-8/FR-9)
- (decision) AD-9 pandera-first inline contracts; single validator-agnostic AfterNodeRunHook; GX capped at conda-forge 1.18.2 (no >=1.19 features); violation = native exception → Dagster halt + A2A alert; FR-18 policy gate shares identical failure semantics. (FR-10/FR-18)
- (decision) AD-10 legacy behavioral contracts are binding port constraints (spec §3.3: Phase P cost gates, K token bucket, F provenance, H serial gate, B.5 _pick_feedstock, g10_spelling no-clobber, KEV overlay + ScoreType coercion, cfe:* + ?channel=conda-forge, EPSS 0-100, v_* view discipline, post-v25 schema shape); fixtures carry over green. (FR-2/FR-13)
- (decision) AD-11 verify-first sequencing: wave's first deliverable is its deterministic gate (kedro-test A1, kedro-catalog-check A2, parity-diff B1-B3→B4, dagster-dryrun C1, bsl-metric-check D1, wasm-smoke G1); gates fixture-based, non-credentialed, --frozen, tracked test tree never .claude/data/. (spec §2.5) [ADOPTED]
- (decision) AD-12 frozen exit-code convention 0/1/2 (+130), verdict lattice per FR-18; inventory-match enum flipped with one-release INVENTORY_MATCH_LEGACY_EXIT=1 window; ComplianceReport = pyforge-warden four-axis schema; atlas fills security from inventory-match/cve, never re-invokes osv-scanner. (FR-16/FR-18)
- (decision) AD-13 offline degradation rule: external-source nodes skip gracefully and mark dataset stale, never fail (consumer profile air-gapped); all endpoints route via resolve_*_urls (20 helpers incl. new resolve_basilisk_urls); FR-21 rides existing resolve_github_raw_urls. (FR-1/FR-19/FR-21)
- (decision) AD-14 new signals (FR-19/20/21) additive, never parity-gated; binding guards: match by package name not OSV ecosystem tag; fix_available tri-state (unknown never collapses to false); version currency ≠ security currency; 90-day recency gate + first-availability (min per-build repodata timestamp, never latest_conda_upload); not-in-tracker labeled inferred.
- (decision) AD-15 derived layer (export-purls, universe-sbom, seed-gap reports, ComplianceReport) regenerates after every rebuild under the 14-day freshness contract; seed-gap nodes strictly read-only (mapping-gap writeback lives in PyPI Intelligence pipeline). (FR-17, spec §3.4)
- (decision) AD-16 pixi-first conda-forge-only, py3.14 floor, nebi scaffold; scaffolded project ships its own lean pixi env + kedro-test; llms-full-check updated same PR as any dep change; air-gap covers both routing layers (_http.py analog + .pixi/config.toml [pypi-config]). (FR-15) [ADOPTED — stack already resolved in-env, verified against pixi.toml this run]
- (decision) AD-17 pipeline snapshots are advisory: never substitute the authoring loop's live re-verification (channeldata, gh pr, per-subdir installability) — G66/G74/G78. (spec §3.4)
- (decision) AD-18 execution seam: loop stories run in worktrees with symlink bootstrap (A3 = first loop story + smoke); BMAD artifacts written only through _bmad-output symlinks; bmad-switch conda-forge-atlas-datapipeline supersedes spec literal 'local-recipes' (PRD §9.11).
- (decision) AD-19 migration boundary: legacy orchestrator parallel until B4 parity (Q1 default: exact row-count+value parity on v_actionable_packages-family views, timestamp/ordering diffs benign); §3.4 fixes scope — 3 external-refresh assets in, seeds/templates/live-authoring-fetches/user inputs out.
- (decision) AD-20 observability: OpenLineage events per node + OTel end-to-end traces down to named API calls (FR-12); A2A structured payloads are the sole inter-agent channel between cf_atlas analytical agent and conda-forge-expert agent (FR-11); alerts (FR-10/FR-18) raised on A2A.
- (decision) AD-21 WASM read surface: Vizro-AI+BSL run via duckdb-wasm/Pyodide against statically-hosted Parquet over HTTP Range, zero backend; emitter host-agnostic (Q4 default GitHub Pages). (FR-14)
- (decision) AD-22 Wave-H factory layer consumes pipeline outputs via catalog/BSL and writes only the wiki tree + CMS (never atlas datasets); wiki/raw→compiled→outputs; agno crews; Wagtail/La Suite REST sync; PostgreSQL+MinIO conda-forge-provisioned. (FR-22)
- (constraint) Brownfield ratifications: three-tier script layout (canonical scripts / thin wrappers / MCP-as-wire-format) stays; non-atlas recipe-authoring tools remain on legacy FastMCP server; meta tests (three-place rule, actionable-scope) stay green or retire explicitly with the legacy path.
- (version) Stack verified against live pixi.toml 2026-07-17: kedro>=1.5.0, kedro-datasets>=9.5.0, kedro-dagster>=0.7.0, kedro-viz>=12.4.0, kedro-mcp>=0.1.2, dagster>=1.13.13, duckdb>=1.5.4, ibis-framework>=12.0.0 (+ibis-duckdb), boring-semantic-layer>=0.3.15, vizro>=0.1.59, vizro-ai>=0.4.1, pandera>=0.32.1, great-expectations>=1.18.2 (CAP 1.18.2), deptry>=0.25.1, nebi-cli>=0.13, agno>=2.6.22, wagtail>=7.4.2<8, django-lasuite>=0.0.27, openlineage-python>=1.51.0, opentelemetry-sdk/api>=1.43.0, tomlkit<0.13.3, structlog>24.2.0<26, sqlglot>26.32.0<28.7.0, python 3.14 floor.
- (assumption) Q1-Q4, Q6, Q7 resolved at spec §11 recommended defaults (per PRD §8); each remains a scheduled re-check at its gating wave — recorded as Deferred items, not re-decided here.
- (assumption) Altitude=feature: the spine keeps the epics (waves) coherent; per-story detail deferred to bmad-create-epics-and-stories + TEA atdd.
- (question) Tension (minor): brownfield architecture-cf-atlas.md carries stale pre-re-ground literals (SCHEMA_VERSION=28, 22 phases, 42/46 MCP tool counts) vs spec §3.3 v29/23/46 — groundtruth 2026-07-17 confirms spec §3.3 authoritative; spine cites §3.3 and avoids inline counts per drift rule.
- (question) Tension (minor): spec §5.9 names Dagster Sensors 'tied to PyPI/GitHub webhooks or RSS' but Q2 default is on-demand invocation with no persistent daemon — sensors (G3) may force the daemon revisit; captured as Deferred with revisit condition at Wave G.
