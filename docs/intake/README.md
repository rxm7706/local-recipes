# docs/intake — the inbox

Raw imports and drops land here before triage: gist snapshots, exported chats,
pasted white papers, downloaded material. **Nothing lives here permanently** —
triage routes each item to its home: an aspiration → `docs/dreams/` (or a
companion dir of its Dream) · a how-things-work description → `docs/reference/`
· superseded/out-of-scope → `/archive/docs/` (structure-preserving) · a build
contract → never here (BMAD planning-artifacts).

- `gists/` — the 2026-07-23 snapshot of @rxm7706's gists (see its `INDEX.md`;
  13 of 30 already archived, the rest cited as Dream grounding).
- ~~`sentinel/`~~ — **archived 2026-08-15** to `archive/docs/intake/sentinel/`
  (structure-preserving, per this doc's own routing rule). Its Dream
  (`docs/dreams/sentinel.md`, `status: archived`, `archived-reason: absorbed`)
  was fully absorbed into the `pyforge-scribe` BMAD project, now complete
  (9/9 stories) — the raw intake material has no further staging purpose.
- `agentic-sdlc/` — the four-views white paper, infographic + masterclass HTMLs,
  and updated Marp draft, staged for the `presentations/agentic-sdlc` four-views
  refresh (`docs/dreams/agentic-sdlc-autonomy.md`, still `status: pitched` —
  the presentation-deck *worked example* already shipped from an earlier cut
  of this material, but the broader four-views refresh this staging area feeds
  has not).
- `jira-github-projects-sync/` — a complete externally-authored technical
  draft (PRD, two candidate architectures, epics/stories, acceptance
  checklist) for a bidirectional GitHub Projects V2 ↔ Jira Cloud sync engine,
  staged for `docs/dreams/jira-github-projects-sync.md` (still `status:
  specified` — genuinely open, blocked on the operator decision named in
  `_bmad-output/DREAM-TRIAGE-2026-08-08.md`'s 2026-08-15 refresh).
- `secure-live-dashboards/` — role-based live-dashboard blueprint + a Vizro/
  GitHub Pages static-hosting workaround note, staged material with no Dream
  file citing it by name yet — genuinely open, not absorbed anywhere.
