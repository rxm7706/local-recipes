# docs/intake — the inbox

Raw imports and drops land here before triage: gist snapshots, exported chats,
pasted white papers, downloaded material. **Nothing lives here permanently** —
triage routes each item to its home: an aspiration → `docs/dreams/` (or a
companion dir of its Dream) · a how-things-work description → `docs/reference/`
· superseded/out-of-scope → `/archive/docs/` (structure-preserving) · a build
contract → never here (BMAD planning-artifacts).

- ~~`gists/`~~ — **archived 2026-09-19** to `archive/docs/intake/gists/`
  (structure-preserving, per this doc's own routing rule; moved unmodified —
  no Dream YAML added). 2026-07-23 snapshot (see `INDEX.md`); 14 of 30 were
  already discarded before this pass, the remaining 16 were cited as Dream
  grounding only — doctor 23.4 archives the rest of the dump, the grounding
  citations themselves are untouched.
- ~~`sentinel/`~~ — **archived 2026-08-15** to `archive/docs/intake/sentinel/`
  (structure-preserving, per this doc's own routing rule). Its Dream
  (`docs/dreams/sentinel.md`, `status: archived`, `archived-reason: absorbed`)
  was fully absorbed into the `pyforge-scribe` BMAD project, now complete
  (9/9 stories) — the raw intake material has no further staging purpose.
- `agentic-sdlc/` — four-views white paper / HTMLs / Marp for the teaching
  deck. Dream `docs/dreams/agentic-sdlc-autonomy.md` is `status: archived`
  (folded into the `pyforge-marshal` station Dream 2026-09-16; the standing
  autonomy Spec itself stays `specified` — not a deliverable). Steward Story
  **59.4** names the deck and is still `backlog`. Do not mint a second
  autonomy epic. **2026-09-19 (doctor 23.4):** stays — 59.4 hasn't run yet;
  archive once it lands and nothing remains.
- ~~`jira-github-projects-sync/`~~ — **archived 2026-09-19** to
  `archive/docs/intake/jira-github-projects-sync/` (structure-preserving).
  Grounding for shipped steward **Epic 8** (Jira↔GitHub, 7/7 stories `done`).
  That epic is **not** work-passports (Epic 61).
- ~~`secure-live-dashboards/`~~ — **archived 2026-09-19** to
  `archive/docs/intake/secure-live-dashboards/` (structure-preserving).
  Grounding for shipped steward **Epic 9** (`pyforge/steward/dashboard/`,
  7/7 stories `done`); `docs/dreams/secure-live-dashboards.md` is
  `status: archived` (folded into the `pyforge-steward` station Dream).
- ~~`local-ocp-hybrid-environment/`~~ — **archived 2026-09-19** to
  `archive/docs/intake/local-ocp-hybrid-environment/` (structure-preserving).
  Source for `spec-local-ocp-hybrid-environment`; steward Epic 12 including
  12.9 is `done`; `docs/dreams/local-ocp-hybrid-environment.md` is
  `status: archived` (folded into the `pyforge-steward` station Dream).
- `external-repos-analysis-2026-08-22/` — research record (sibling OpenTeams
  fleet, prf pipeline) grounding five separate dream/fold targets across
  atlas, CFE/mason, steward, herald, and warden. Only the mason fold
  (`machine-checked-recipe-knowledge`, now `archived`/realized) is confirmed
  done; the rest are still open "next story/spec pass" folds per the
  report's own fold list — not yet fully absorbed. **2026-09-19 (doctor
  23.4):** stays; fold notes only, no new station.

Full revisit table:
`_bmad-output/projects/pyforge-steward/planning-artifacts/research/fleet-inbox-disposition-2026-09-16.md`.
