<!-- GENERATED by scripts/docs_station_cli.py -- do not hand-edit. Regenerate with:
         pixi run -e pyforge-guild docs-station-cli
     derived_at: 2026-09-24T06:49:16-05:00
     tree: d88e6e83d7e142b15c9378b29a3f415d5b8eb7be-dirty -->


# PyForge Station Cheat Sheet

Quick lookup for every PyForge station's own console script: which package
declares it, which environment(s) carry it, and (when captured from the
CURRENT environment -- see this generator's own docstring for why the other
three are noted rather than captured) its live `--help` text.

The `pyforge` router (`pyforge-core`'s own console script,
`pyforge.core.dispatch:main`) fronts every station as `pyforge <station>
<args>`; each station also ships its own direct console script, listed
below.

## Atlas

Package: `pyforge-atlas` -- Environment: `pyforge-atlas`

`pyforge-atlas --help`:

_not resolvable on PATH in this generator's own environment -- run `pixi run -e pyforge-atlas pyforge-atlas --help` directly._

## Doctor

Package: `pyforge-doctor` -- Environment: `pyforge-guild`

`doctor --help`:

```text
usage: doctor [-h] [--version] {check,monitor,diagnose,backlog-intake,flags} ...

Pre-flight + fleet-watch diagnostics for the pyforge factory.

positional arguments:
  {check,monitor,diagnose,backlog-intake,flags}
    check               run pre-flight diagnostics (engine availability + env hygiene)
    monitor             fleet-pulse watch over cf_atlas's staleness/cve/abandonment signals
    diagnose            gather + (optionally) partition/rank/root-cause one target
    backlog-intake      scan every station's tracked deferred-work-ledger.md for entries naming an
                        epic/story, fleet wide
    flags               feature-flag actuators (Story 48.5 / R-21)

options:
  -h, --help            show this help message and exit
  --version             show program's version number and exit
```

## Herald

Package: `pyforge-herald` -- Environment: `pyforge-guild`

`herald --help`:

```text
usage: herald [-h] [--version] command ...

Dream-to-deck bridge CLI -- seeds, pulls, and syncs Claude Design decks against this repo's docs/dreams/ and presentations/ trees; also the Herald Moments 2-4 CLI (progress/success/notice).

positional arguments:
  command
    deck       manage Claude Design decks (seed/pull/status/watch)
    progress   show or record progress (Moment 2)
    success    manage success claims (Moment 3; publish requires operator role)
    notice     manage operational notices (Moment 4; author/publish/close/archive require operator
               role)
    scheduler  run the scheduled evidence-revalidation + progress-snapshot jobs (Story 13.5)

options:
  -h, --help   show this help message and exit
  --version    show program's version number and exit

examples:
  herald deck seed pyforge-warden
  herald progress --json
  herald progress --station warden --date-range 2026-08-01..2026-08-31
  herald success publish claim-123
  herald scheduler run
```

## Marshal

Package: `pyforge-marshal` -- Environment: `pyforge-guild`

`marshal --help`:

```text
usage: marshal [-h] [--version]
               {config,init,homes,preflight,teardown,gate,factory,deploy,land,retire,status,check,watch,adapters,upstream,seed,refresh,chain,planning,context,login,benchmark} ...

Deterministic BMAD-loop supervisor.

positional arguments:
  {config,init,homes,preflight,teardown,gate,factory,deploy,land,retire,status,check,watch,adapters,upstream,seed,refresh,chain,planning,context,login,benchmark}
    config              Compose and print Marshal's effective policy (AD-10/AD-16).
    init                Provision an isolated loop home for a BMAD project (AD-11/AD-21).
    homes               List every discovered loop home and verify isolation (FR-4/FR-8).
    preflight           Verify a loop home can run, seed adapter config, and gate on first-run
                        acknowledgement.
    teardown            Remove a loop home's worktree and branch, refusing when work would be
                        lost.
    gate                Evaluate Marshal's gate (AD-17/AD-26).
    factory             Launch and attach to bmad-loop runs (AD-3/AD-22/AD-25).
    deploy              Promote merged story specs into the tracked archive (AD-13/AD-29).
    land                Merge a wave's landed stories and retire its branch, under required checks
                        (FR-60).
    retire              Sweep every project's loop home for provably-safe-to-delete per-story
                        branches (FR-63/AD-47).
    status              Fleet-wide runtime status: one row per loop home (FR-36/AD-5).
    check               Route to the repo's detector registry (scripts/detectors.py) through the
                        front door (FR-65/AD-50).
    watch               Operator ritual as a CLI verb: ground-truth + delta + boundary-aware next-
                        check delay (Story 44.1).
    adapters            Project the canonical skill tree into every configured adapter's own tree
                        (FR-41).
    upstream            Report the tracked upstream contribution register (FR-58).
    seed                Scaffold/adopt/check/update a project from Marshal's seed templates
                        (AD-70).
    refresh             Refresh every loop home from main: behind-count, fast-forward, push
                        loop/<slug>, re-render harness policy (FR-133..135).
    chain               Orchestrate planning-chain regeneration for one project (FR-148/149/151;
                        AD-72).
    planning            Orchestrate planning-chain regeneration (FR-192 CAP-1…CAP-5; Stories
                        21.2–21.5).
    context             Derived planning-context freshness for the declared [context] pipeline
                        (SPEC-marshal-token-economy CAP-5; Story 28.8).
    login               Mint a local-dev IdP bearer and write PYFORGE_IDP_BEARER_FILE (0600).
    benchmark           Token-economy wrapped-vs-unwrapped benchmark (Story 28.5, CAP-9).

options:
  -h, --help            show this help message and exit
  --version             Show marshal's version and the resolved harness version, then exit.
```

`marshal-mcp --help`:

```text
Traceback (most recent call last):
  File "/home/rxm7706/UserLocal/Projects/Github/rxm7706/local-recipes/.worktrees/dispatch-pyforge-doctor-30.3/.pixi/envs/pyforge-guild/bin/marshal-mcp", line 11, in <module>
    sys.exit(main())
             ~~~~^^
  File "/home/rxm7706/UserLocal/Projects/Github/rxm7706/local-recipes/.worktrees/dispatch-pyforge-doctor-30.3/src/shared/packages/pyforge-marshal/src/pyforge/marshal/mcp/server.py", line 68, in main
    build_server().run()
    ~~~~~~~~~~~~^^
  File "/home/rxm7706/UserLocal/Projects/Github/rxm7706/local-recipes/.worktrees/dispatch-pyforge-doctor-30.3/src/shared/packages/pyforge-marshal/src/pyforge/marshal/mcp/server.py", line 15, in build_server
    from fastmcp import FastMCP  # lazy: registration-time only
    ^^^^^^^^^^^^^^^^^^^^^^^^^^^
ModuleNotFoundError: No module named 'fastmcp'
```

## Mason

Package: `pyforge-mason` -- Environment: `pyforge-mason`

`mason --help`:

_not resolvable on PATH in this generator's own environment -- run `pixi run -e pyforge-mason mason --help` directly._

## Scribe

Package: `pyforge-scribe` -- Environment: `pyforge-guild`

`scribe --help`:

```text
                                                                                                    
 Usage: scribe [OPTIONS] COMMAND [ARGS]...                                                          
                                                                                                    
 Capture decisions directly into checked-in team memory (.claude/memory/).                          
                                                                                                    
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --version                     Show scribe's version and exit.                                    │
│ --install-completion          Install completion for the current shell.                          │
│ --show-completion             Show completion for the current shell, to copy it or customize the │
│                               installation.                                                      │
│ --help                        Show this message and exit.                                        │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ───────────────────────────────────────────────────────────────────────────────────────╮
│ capture  Append a new record directly into `.claude/memory/<type>/` (AD-1), or                   │
│          with `--promote`, scan user-local auto-memory and propose promotions, or                │
│          with `--transcripts`, scan raw session transcripts and propose captures.                │
│ recall   Answer from the compiled graph with a resolvable citation, or report                    │
│          no grounded coverage (Story 2.4, AD-8) -- zero network calls (AD-6).                    │
│ graph    Knowledge-graph projection commands (Epic 2).                                           │
│ index    graphify compile_surface ingest + report verbs (Story 6.1); cocoindex incremental       │
│          refresh (Story 6.2).                                                                    │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## Steward

Package: `pyforge-steward` -- Environment: `pyforge-guild`

`steward --help`:

```text
usage: steward [-h] [--version]
               {keys,deploy,provision,budget,sync,workspace,upgrade,suite,init,shell-init,setup,initrepo,validate-fast,restore,revoke,track,guards,cutover,ledger-query,catalog,load,passport,glass} ...

Steward — the Provisioner's station.

positional arguments:
  {keys,deploy,provision,budget,sync,workspace,upgrade,suite,init,shell-init,setup,initrepo,validate-fast,restore,revoke,track,guards,cutover,ledger-query,catalog,load,passport,glass}
    keys                credential lifecycle — encrypt/decrypt/rotate/list/audit/revoke
    deploy              dashboard build/reconcile/status; perimeter: AD-5 shareability +
                        daphne/nginx manifests; static: CAP-8/AD-10 static-export publish
    provision           environment and substrate provisioning; non-module suite pieces follow the
                        class-keyed playbook (see provision --help)
    budget              cost budgeting and enforcement
    sync                bidirectional GitHub Projects V2 <-> Jira Cloud reconciliation
    workspace           story-scoped scratch worktrees — start/ls/status/clean (single-repo or
                        repo-set; own-worktrees-only; archive-not-delete)
    upgrade             BMAD-METHOD core upgrade — bmad-core pre-flight (CAP-1), deliberate
                        --apply (CAP-2), CAP-3 clobber detect/re-apply, pin-fan-out report
                        (CAP-4), prove-landed single verdict (CAP-5); never edits foreign-station
                        pin sites beyond documented loop-home relay refresh
    suite               bmad-suite channel product — pipeline-truth (CAP-1) reports
                        upstream/recipe/channel/installed/wired for all 13 suite packages with
                        per-stage drift and fail-open probes; advance (CAP-2) chains
                        autotick→build→test→publish→listing→reviewable PR (never auto-merged)
    init                machine bootstrap — report pixi/git/gh/podman prereqs with named remedies;
                        pixi floor from pixi_version_registry (Story 17.1)
    shell-init          emit idempotent PATH/completions/env shell snippet to stdout for eval
                        (Story 17.1)
    setup               machine bootstrap — clone (when missing), pixi install, and pre-commit
                        hooks (Story 17.2)
    initrepo            onboard a pixi checkout — scaffold pyforge.toml when absent, materialize
                        env, run validate-fast (Story 17.2)
    validate-fast       fast green gate — prereqs, environment.yaml sync, steward CLI smoke (Story
                        17.2)
    restore             PostgreSQL restore drill — scratch DB + manifest count assertions (Story
                        41.1; operator full restore is deploy/restore.md)
    revoke              stop one runaway subject — revoke its queued Celery tasks and cancel its
                        live supervisor runs (Story 42.2)
    track               assemble one tracked track.json per run from journal/gate-record/state
                        (Story 53.3 / hub:CAP-3); not a detector
    guards              Hub Guard library — catalog, spec lacking, source-ground (AD-8 copy);
                        never a PR verdict (Story 53.4 / hub:CAP-4)
    cutover             flag-gated cutover plan/apply/flip (Story 44.12 / fnd:CAP-8); default root
                        stays local-recipes
    ledger-query        pluggable estate sprint ledger query & telemetry reporting
                        (markdown/summary/json/table/sync-matrix/herald-facts/atlas-
                        dataset/static-dossier/jira-csv/github-json; exporters flag-gated via
                        --flag); every story carries a next field (done/running/ready/waits on
                        .../blocked/?), filterable with --ready / --running
    catalog             estate BMAD catalog — check/list/render/pointers over catalog.yaml
                        (backends + sources declared in config, git is the edit store; generated
                        Claude/Codex marketplace manifests; Story 60.1)
    load                extract corridor -- idempotent inbound/outbound file loads keyed by batch
                        sha + waybill; transports declared in corridor.yaml (Story 61.1); outbound
                        additionally requires --slice + --signer -- default deny (Story 61.4)
    passport            vendor work-passport identity -- mints a FRESH UUID per inbound key, never
                        merges by Jira key or GitHub number (Story 61.2)
    glass               as-of glass -- standup/shipped freshness over the inbound corridor (cites
                        a waybill; empty on-time file fails; late drop leaves yesterday stale;
                        unborn before the first waybill); export is a flag-gated CSV/markdown
                        plugin (Story 61.3)

options:
  -h, --help            show this help message and exit
  --version             show program's version number and exit
```

## Warden

Package: `pyforge-warden` -- Environment: `pyforge-warden`

`warden --help`:

_not resolvable on PATH in this generator's own environment -- run `pixi run -e pyforge-warden warden --help` directly._

## Universal Station Invariants

1. **Tests:** Station-specific tests live in
   `src/shared/packages/pyforge-<station>/tests/{unit,meta,integration,fixtures}/`
   (every station has `unit/` and `meta/`; only some have `integration/`).
   Run them with `pixi run -e pyforge-<station> pyforge-<station>-test`, or
   all stations at once with `pixi run -e pyforge-guild pyforge-station-tests`.
2. **Dashboard Isolation:** Django lives only behind the `[dashboard]`
   extra, in `src/shared/packages/pyforge-<station>/src/pyforge/<station>/dashboard/`.
   The core station logic must never import Django at the module level, and
   the base package must run its CLI without the extra installed.
3. **Verdict Projection:** A station that projects an exit-code verdict
   implements a `verdict.py` module; a station whose report is externally
   consumed also ships a frozen JSON schema for it under
   `src/pyforge/<station>/data/report-schema.json`.
