# .memlog.md — spec-coverage-gate-independence

- (provenance by claude) THIS RECORD STARTS HERE, 2026-09-14. The SPEC.md beside it was hand-seeded in the same pass rather than derived by `bmad-spec`, so this log does not begin at a derivation. Recorded explicitly so a later reader does not read the absence of earlier entries as lost history.
- (event by claude) Seeded 2026-09-14 from `docs/dreams/coverage-gate-independence.md` at `status: draft`, so `dream-chain` INV-1 has a durable Dream->Spec link from birth. Five open questions, all design decisions about where a fleet-wide blocking gate may live; nothing downstream may bind until they are answered, and the owning Dream stays `dreamt` per docs/dreams/README.md:71-78.
- (decision by claude) Owner is **doctor**, not marshal. Charter §6 gives Doctor the verdict on Marshal's row precisely so the subject does not carry its own independence question. Doctor owning the Dream/Spec does NOT mean the code lands in `pyforge-doctor` -- it cannot, because Doctor is constitutionally advisory and a blocking gate there would trade one violation for another. That tension is CAP-1's open question, stated rather than resolved.
- (note by claude) Recorded so it is not re-litigated: the investigation that produced this Spec CLEARED marshal's six-rung verdict lattice (it judges the story's work; `core/gate.py:41-46` keeps "a project's own gate failed" and "an internal Marshal operation failed" on separate rungs by design), cleared `marshal seed check`'s "(CI gate)" label (zero hits in `.github/`; literal only in the adoption guide, where an adopting repo gates its own conformance), and confirmed `doctor/sources/marshal.py` already achieves the independence this Spec wants. That module is the exemplar to copy, not a thing to change.
- (decision by operator, 2026-09-14) **Home ruled: the Guild.** All five open questions closed by one decision — the narrow Charter §5 amendment widening `owner: guild` to "the constitutive Dream, and a gate that judges all eight Smiths" (test: no Smith *can* be accountable, not merely none is). Every station-side home had been measured and failed — `pyforge-core`, `pyforge-testing-kit` and `scripts/` are all governed by marshal's planning tree, and doctor is constitutionally advisory — so the question was "who owns a policy that binds all eight", not "where does the file go". Rejected shapes recorded so they are not re-proposed: a `pyforge-gates` package (a Smith-less ninth package is `owner: crew` again) and the data-only split (protects one of §6's three verbs — *weaken, re-threshold, disable*). Consequences: this Spec moved `_bmad-output/projects/pyforge-doctor/planning-artifacts/specs/spec-coverage-gate-independence/` → `docs/governance/spec-coverage-gate-independence/` by `git mv`; status `draft` → `ready`; the Dream re-owned doctor → `guild` and `dreamt` → `specified`; `guild_dreams` in `guild-roster.json` enumerates it; `DEFERRED_SPECS` in `board.py` drops its entry (chain-completeness does not scan `docs/governance/`, exactly as for `spec-pyforge-charter`). `surface:` stays `[]` on purpose until the mechanism story moves the files — declaring the target surface now would govern files that still sit inside marshal's package and testing-charter's surface.
- (decision by claude, 2026-09-14) **Mechanism stays Doctor's** under §5's outcome/mechanism rule — Doctor is the Smith §6 names as Marshal's judge, and `sources/marshal.py` is the exemplar CAP-1 copies. Stories minted in doctor's `epics.md` + `sprint-status-ledger.yaml` bound to CAP-1..3; no code moved in this pass ("Spec → Story before code").
- (note by claude, 2026-09-14) The force-ratio non-goal was ruled separately the same day (`DW-VOCAB-2026-09-14-16`): `ledger-regression` is a scoped blocking step in `detectors.yml`. This Spec's non-goal now reads "fleet-wide", since one row is no longer advisory.
- (event by claude, 2026-09-20) **Story 24.1 landed (CAP-1/CAP-2, together).** `scripts/coverage_gate.py` (moved from `pyforge.marshal.coverage_gate`) and `docs/governance/coverage-thresholds.toml` (moved from `pyforge.marshal.coverage_thresholds.toml`) now hold the evaluator and its floors outside every `pyforge.<station>` package. Every caller re-pointed: `scripts/coverage_gates_ci.py`, `scripts/run_station_coverage_gate.py`, `.github/workflows/coverage-gates.yml` (trigger paths + shared-surface check + the named-module fixture-proof step, now on doctor's job instead of marshal's), and the four tests that exercise the evaluator itself (`tests/scripts/test_coverage_gate_names_module.py`, `test_coverage_gates_ci_driver.py`, `test_coverage_gate_ast_fingerprint.py`, `test_coverage_gate_module_floors.py`, moved out of pyforge-marshal's own suite). Marshal's AD-3 import-linter contract (`pyproject.toml` + `tests/meta/test_ad3_ad4_import_linter.py`) dropped `pyforge.marshal.coverage_gate` from its `source_modules`, with the reasoning inline: the seam has nothing left to say about a module the station no longer ships. `docs/governance/coverage-thresholds.toml` carries the governance-act comment (TOML has no `$comment` key idiom, so a `#` block carries the same text as `guild-roster.json`'s). Effective floors are byte-identical (defaults 80/70 unit/integration, no station overrides, no module exceptions — verified by diffing the moved file's own values). `surface:` above now names both new paths; `scripts/coverage_gate.py` is allowlisted in `scripts/spec_surface_allowlist.txt` rather than declared here for `spec_surface_check.py`'s own purposes, because that detector's `SPEC_GLOB` does not discover `docs/governance/` Specs at all (same as `spec-one-chain-per-station`'s `chain_sprawl_baseline.py`/`fr_baseline.py` precedent) — this `surface:` field is this Spec's own documentation contract, per Story 24.1's own acceptance criteria, not a live input to that script. The two `scripts/` drivers stay named in `spec-pyforge-testing-charter`'s surface for now (their own content changed here — repointed import — and that Spec's memlog records it); Story 24.3 hands their governance to this Spec so no file ends up governed twice or not at all.
- (event by claude, 2026-09-24) **Story 24.3 landed (the hand-over CAP-1/CAP-2 note above named).** `scripts/coverage_gates_ci.py` and `scripts/run_station_coverage_gate.py` drop out of `spec-pyforge-testing-charter`'s `surface:` (its own `.memlog.md` records the removal) and are added to THIS Spec's `surface:` frontmatter above, for documentation only — same as `scripts/coverage_gate.py`, `spec_surface_check.py`'s `SPEC_GLOB` still does not discover `docs/governance/`. Both are added to `scripts/spec_surface_allowlist.txt` (mirroring `scripts/coverage_gate.py`'s existing entry there) so the live `pyforge.doctor.sources.chain::gather_spec_surface` verdict — the one that actually walks tracked files — still finds them governed rather than reporting `ungoverned`; verified `_collect_surfaces` there enumerates only `_bmad-output/projects/*/planning-artifacts/specs/spec-*/SPEC.md`, the same as `spec_surface_check.py`'s `SPEC_GLOB`, so `docs/governance/` Specs were never a live coverage input and this hand-over does not change that. Neither driver's own content changed in this pass — governance-only. `spec-pyforge-testing-charter`'s surface (now three entries, none of them these two files) and this Spec's surface (four entries, including these two) are disjoint by construction. Re-stamped scoped after the reconcile: `--write-baseline --spec pyforge-marshal/spec-pyforge-testing-charter` (the only key `spec_surface_check.py` can name — governance Specs are not in its `SPEC_GLOB`, so there is no second key to stamp here).
