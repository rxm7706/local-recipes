---
topic: One Dream, one Spec, one PRD, one architecture, one epic chain — per station (fleet consolidation + the Dream-append-first minting rule)
updated: 2026-09-16T06:16
---

- (direction) 2026-09-16 operator: 'lets now scale this across the fleet ... One Dream, One PRD, One Arch, One Spec and One Epic Chain — ask questions, give recommendations, and be aggressive'. Seeded after PRs #1382/#1383 proved the shape on marshal's token-economy chain.
- (note) Fleet analysis 2026-09-16: 171 Dreams (68 open/61 realized/42 archived), 172 Specs (73 open with 334 CAPs and 1,764 memlog entries; 67 shipped; ~30 terminal-other), 8 PRDs, 8 ARCHITECTURE-SPINE.md, 8 epics.md + ledgers (226 epics, ~1,000 stories), 5,298 memlog entries fleet-wide. PRD/arch/epics are ALREADY one per station; Dream and Spec sprawl.
- (note) The sprawl is by rule: Dream-first (no gap-closure exemption, 2026-09-12) + bmad-spec one-folder-per-slug + Spec->Story-before-code compose into a Dream+Spec pair per effort. 61 Dreams folded 2026-08-08 (README:155); regrown to 171 in five weeks; W37 alone minted 30 Dreams + 33 Specs. A fold without a minting-rule change is a treadmill.
- (note) Two requirement namespaces: stories cite spec-X CAP-n 228x and FR-n 185x; INV-A accepts both. Marshal PRD 196 FRs vs ~290 CAPs across 58 Specs; the PRD is already downstream (FR-182..187 backfilled from Specs after shipping). Steward PRD already carries 26 FR<->CAP cross-refs, marshal 13.
- (decision) OPERATOR RULING 1: Dream-append-first. New work = dated section in the station Dream + memlog append + CAP-n on the station Spec + Story. A NEW docs/dreams/*.md or specs/spec-*/ folder needs frontmatter fold-exemption: from a closed list {different-owner, different-lifecycle, cross-station-seam, governance}; doctor detector chain-sprawl-check fails an unexempted new folder. Compatible with the 2026-09-12 ruling: a dated section IS a Dream seed.
- (decision) OPERATOR RULING 2: target shape = 8 station chains + 1 Guild chain (Charter + spec-pyforge-unifying-strategy). Not one fleet chain — the Charter's ownership grammar (Spec in owner's project; station dispatches its stories) is what made the token-savings fold work.
- (decision) OPERATOR RULING 3: fold EVERYTHING, shipped Specs included. Each station ends with exactly one Spec folder (spec-pyforge-<s>). Shipped CAPs re-mint into the station namespace with provenance ('CAP-n <- spec-old CAP-m, shipped <date>'); old folders become absorbed pointers keeping their memlogs. Nothing deleted; history stays on A (regenerate-not-fold honored).
- (decision) OPERATOR RULING 4 (PRD): operator challenged the 'retire' option — 'isn't PRD a required and critical artifact of the BMAD-METHOD'. Yes: bmad-prd is the planning chain's spine and bmad-spec names PRD as an input. Retire withdrawn. Resolution inside BMAD: the PRD is the Spec's decomposition (Charter Lexicon), so FRs DERIVE from CAPs — every FR minted after the rule date carries its source CAP; PRD re-derived as part of each fold, not backfilled later; doctor check fr-without-cap (post-rule-date only).
- (decision) OPERATOR RULING 5: the fold is the foundry on-ramp — the 8 folded station Specs are B's starting five-fields (spec-foundry-regenerate-not-fold CAP-5 'one starting contract across two git roots'). Record in that Spec's memlog when the first station fold lands.
- (decision) OPERATOR RULING 6: pilot = marshal (58 Specs -> 1, 55 Dreams -> 1; 8 already folded 2026-08-08). Biggest payoff; forces memlog sharding on day one.
- (constraint) No new machinery beyond one detector (chain-sprawl-check) and one FR check (fr-without-cap). Every fold mechanism exists with precedent: archive-in-place banners (14), covers-dreams (chain.py:348), absorbed/absorbed-into (5), superseded/superseded_by (2), bmad-spec ID preservation + next-N, INV-A CAP-a..b ranges, surface-overlap tolerance (Epic 42).
- (constraint) Station Specs become living contracts: spec-pyforge-<s> flips shipped -> in-progress and stays. 'shipped' belongs to CAPs and stories. Station Dreams need a living status or a README rule (perpetually specified) — a README change, not a new detector.
- (constraint) Memlog sharding is mandatory at marshal scale: one log would pass 1,100 entries and every bmad-spec re-derive re-reads it whole — the epics.md re-read problem again. Shard per CAP range or compile a summary the derive reads first.
- (constraint) Known costs accepted: (a) surface drift degrades from 'which Spec's memlog must move' to 'which station's'; (b) each station fold is multi-day real-story work, not a script.
- (capability) CAP-1 Dream-append-first is the Lexicon's reading of Dream-first: dated section + CAP + Story; fold-exemption vocabulary (closed list); Charter amendment records it. success: AGENTS.md/CLAUDE.md/README state the rule; the Lexicon amendment is logged in spec-pyforge-charter's memlog.
- (capability) CAP-2 chain-sprawl-check: a doctor detector that fails any new docs/dreams/*.md or specs/spec-*/ folder (vs a dated baseline) lacking a fold-exemption from the closed list; wired into detectors-ci; advisory-free (a real FAIL). success: an unexempted new folder reds; this Dream's own governance exemption passes.
- (capability) CAP-3 The station fold procedure — one PR per station: (a) covers-dreams on spec-pyforge-<s> for every folded Dream; (b) CAP re-mint with provenance for every open and shipped Spec; (c) old folders -> absorbed pointers keeping memlogs; (d) Dreams -> archived-in-place banners into pyforge-<s>.md; (e) surface manifests merged, scoped baseline stamps; (f) PRD re-derived with FR<-CAP; (g) epics/ledger untouched except new fold Epic. success: exactly one Spec folder with open status per station; dream-chain, dreams-hygiene, spec-surface, chain-completeness, story-status green.
- (capability) CAP-4 Memlog sharding: bmad-spec reads a per-CAP-range shard set (or a compiled summary) for the station Spec; the append script routes by CAP. success: a marshal re-derive reads < the whole 1,100-line log; no entry lost (count-preserving).
- (capability) CAP-5 FRs derive from CAPs: PRD frontmatter carries fr-derivation-from: <date>; every FR after it cites its CAP; doctor fr-without-cap flags a post-date FR with no CAP. success: zero post-date FRs without a CAP fleet-wide; PRD re-derive is a step of the fold PR.
- (capability) CAP-6 Living station tiers: the eight spec-pyforge-<s> flip shipped -> in-progress; README rules station Dreams' status; dreams-hygiene tolerates it. success: no station Spec is terminal; no station Dream flips status on a fold.
- (capability) CAP-7 Foundry on-ramp recorded: spec-foundry-regenerate-not-fold memlog names the 8 station Specs as B's starting five-fields; ab-sync case list references them. success: the memlog entry exists before the second station fold.
- (capability) CAP-8 Marshal pilot: CAP-3 executed on pyforge-marshal — 58 Spec folders -> 1, 55 Dreams -> 1, CAP-4 sharding live, CAP-5 PRD re-derive done. success: marshal renders one Dream, one Spec, one PRD, one spine, one epics.md; all planning detectors green; fleet-picture unchanged in story counts.
- (question) Do the station Dreams get a fifth README status ('living') or the rule 'a station Dream is perpetually specified'? (README change either way; decide at CAP-6.)
- (question) Where does spec-pyforge-core live after the fold — it is a package, not a station (marshal-owned today, 528 governed files)? Candidates: stays a marshal CAP range, or moves to the Guild chain beside unifying-strategy.
- (question) Does the Charter amendment need a Guild vote ritual (Charter §5 precedent) or is the operator ruling sufficient as recorded?
- (decision) 2026-09-16 OQ-1 RULED (research, operator delegated): NO fifth status. guild-roster.json's own doctrine — 'each state names the ACT THAT COMPLETED … a building status was considered and rejected, it goes stale' — rules out 'living' (same failure mode). The existing ladder already yields the answer: README 'specified requires a Spec at ready or beyond'; once CAP-6 flips spec-pyforge-<s> to in-progress (beyond ready) a station Dream is specified by rule and never flips again (realized would need the Spec shipped, which CAP-6 forbids). Fixes a live defect: all eight station Dreams read realized while status-body-consistency warns on herald ('4 of 17 stories', 'zero-stories-yet'), marshal ('2-of-6'), mason ('4 of 38'). Realization lives where the board derives it — CAP status in the Spec, story status in the ledger. CAP-6 story adds one README sentence; zero vocabulary change.
- (decision) 2026-09-16 OQ-2 RULED (research, operator delegated): pyforge-core is NEITHER a Guild artifact NOR a marshal CAP range — it is the first cross-station-seam exemption. Guild is ruled out by the Charter's structural test verbatim ('cross-cutting, shared or unowned does NOT qualify; the artifact must judge every Smith'): core is imported by all eight stations (marshal 64 files, atlas 22, warden 10, scribe 8, steward 8, herald 7, doctor 4, mason 3; platform 0 by AD) — it serves every Smith and judges none; filing it under guild is the retired owner: crew failure. A marshal CAP range is ruled out by the folder's job: seven other stations append against the kernel's contract when they touch it, and the foundry regenerates core AS ITS OWN LEAF (spec-foundry-regenerate-not-fold CAP-2) — burying 528 governed files + 92 memlog entries at marshal CAP-30..36 makes seven stations write to marshal's log and hands B a kernel contract inside a station Spec. RULING: marshal stays the accountable Smith (roster; coverage-gate ruling 'core, testing-kit and scripts/ are marshal-governed'); spec-pyforge-core keeps its own folder with fold-exemption: cross-station-seam; the archived pyforge-core.md Dream stays folded in pyforge-marshal.md (the exemption is on the Spec folder). Same class, same treatment: spec-pyforge-testing-charter (the testing kit, imported by all seven other stations). CAP-3 success refines to 'one Spec folder per station PLUS declared seams'.
- (decision) 2026-09-16 OQ-3 RULED (research, operator delegated): there is no §5 ritual beyond Charter CAP-1 — the Charter has no vote. Its amendment doctrine is CAP-1 'amendment, never silent edit' (dated Realization-log entry quoting what it superseded) + 'the amendment must move the artifacts with it' + 'a constitutional change may precede its enforcement, but the gap is recorded'. The 09-14 precedent used exactly that: operator decision in spec-pyforge-charter/.memlog.md, a Charter Dream log entry, roster moved, SPEC.md re-derive deferred. Two things are in play, neither a new §-level amendment: (1) registering one-chain-per-station under guild is the SECOND INSTANCE of the 09-14 shape, not a widening — chain-sprawl-check grades every Smith's Dream/Spec count; counter-precedent vocabulary-one-name-one-job is a fleet rule steward owns fine because it grades no one's row, whereas this gate grades steward's 22 open Dreams (steward cannot judge itself) — still constitutional, so CAP-1 entry required; (2) Dream-append-first supersedes no Lexicon text (the Lexicon defines Dream and Spec without per-file granularity; the §-amendment bar is 'adopting a term as a Lexicon noun') — a READING of granularity, recorded under CAP-1 as a clarification. The operator ruling SUFFICES as the decision but was half-applied until the Charter memlog + Charter Dream log entries landed — both land in this PR. Enforcement gap (chain-sprawl-check not yet built) recorded per the tooling constraint.
- (event) 2026-09-16 status: draft -> ready. All three open questions ruled; the rule (CAP-1/2), the fold procedure (CAP-3, refined for seams), the pilot (CAP-8) and the living-tier rule (CAP-6, resolved with zero vocabulary change) are settled. Downstream may bind: doctor stories for CAP-2 (chain-sprawl-check) and CAP-5 (fr-without-cap); the marshal pilot as its own effort.
- (direction) 2026-09-16 operator: 'eventual consistency, perpetual standards and evergreen standards (here in local-recipes and soon in pyforge-foundry) take precedence over historical accuracy and implementation — Marshal goes first, then steward.'
- (constraint) PRECEDENCE ORDER (operator 2026-09-16): the evergreen standard > historical accuracy > current implementation. Consequences: (a) CAP re-mint renumbers freely into the station namespace — provenance lives in the memlog line, not in preserved numbering; (b) an absorbed Spec folder is reduced to a pointer header + its memlog — the body is not preserved for history's sake, git history is the historical record; (c) the PRD is re-derived TO the standard (FR <- CAP), not reconciled to past FR numbering; (d) where a detector, a doc, or a README convention encodes the old shape, the standard wins and the artifact moves — never the reverse. This supersedes the earlier 'nothing deleted' phrasing: the decision RECORD (memlogs, git) is never deleted; the derived bodies are disposable.
- (constraint) EVENTUAL CONSISTENCY (operator 2026-09-16): the fleet converges station by station; a station that has folded and one that has not both pass every detector during the transition. chain-sprawl-check therefore baselines from a dated snapshot (pre-existing folders are not findings; only NEW unexempted folders are); CAP-3's per-station PR is the unit of convergence; no fleet-wide flag day.
- (constraint) EVERGREEN AND PORTABLE (operator 2026-09-16): the standard is written once and applies to both roots — local-recipes (A) now, pyforge-foundry (B) on cutover. CAP-7's on-ramp record names the standard itself, not only the eight folded Specs; B inherits Dream-append-first, the exemption vocabulary, the seam rule, and FR <- CAP as its starting discipline.
- (decision) SEQUENCE (operator 2026-09-16): marshal first (CAP-8), then steward (the second fold, the same PR shape), then the remaining six by pain order (scribe, herald, doctor, atlas, warden; mason is already one Spec). The rule + detector (CAP-1/2) land BEFORE marshal's fold so it does not refill while underway.
- (direction) 2026-09-16 operator: 'totally fine with renumbering epics, caps, stories etc. to make it all rebased to one current state — sequential and consistent and coordinated BMAD analysis, planning, solutioning and implementation. Marshal is the pilot, Steward is second in line for eventual consistency, then goes Herald and the remaining all go in parallel.'
- (decision) REBASE, NOT APPEND (operator 2026-09-16): a station fold rebases the WHOLE chain to one current state across all four BMAD phases — analysis (brief/research), planning (PRD), solutioning (architecture), implementation (epics/stories/ledger) — numbered sequentially from 1 with no gaps, no legacy ranges, no 'Epic 46 appended after 45'. This widens the precedence constraint from CAPs to epics and stories: CAP-3 step (g) changes from 'epics and ledger untouched except one new fold Epic' to 'epics.md re-derived sequentially; ledger re-keyed'. Done work keeps its status, not its number.
- (constraint) RE-KEY MAP IS PART OF EVERY FOLD PR (2026-09-16): ledger keys are <epic>-<seq>-<slug>; renumbering changes every key. To keep ledger-regression-check and story-status-check honest, each fold PR ships a re-key map (old key -> new key, one line each, in the station's planning-artifacts) and the ledger is regenerated from the Tier-3 feed through that map — a done row moves as done, never as drop+add (which reads as a regression). Story-spec files rename with their stories; merged-story proof stays git merge-base, never the key. The map is the provenance the renumbering discards.
- (decision) SEQUENCE, FINAL (operator 2026-09-16): marshal (pilot) -> steward (second; the eventual-consistency proof, 22 open Dreams) -> herald (third) -> scribe, doctor, atlas, warden IN PARALLEL (four worktrees, one agent each, physical paths, no bmad-switch). Mason is already one Spec and only takes the living-tier flip (CAP-6). Supersedes memlog 34's pain-order tail.
- (direction) 2026-09-16 operator: 'this is our chance to bring consistency across the fleet, for everything, including vocabulary, naming conventions — don't make the operator think; we know sequence, hierarchy, relations much better than when we started — let's leverage that.'
- (capability) CAP-9 — THE CHAIN STANDARD IS ONE PAGE AND EVERY FOLD CONFORMS TO IT. intent: the hierarchy (one of each per station + declared seams + the Guild chain), the relations (every arrow points up: Dream section -> CAP -> FR -> AD -> Story -> ledger key -> story spec -> code), the sequence (the four BMAD phases, seven acts, none skipped), one closed status enum per tier, one name-form per concept, one id family per job, and the fold-PR checklist live in CHAIN-STANDARD.md beside this SPEC.md — composed from the existing rulings (vocabulary-one-name-one-job CAP-1..8, steward spine § Station token, README ladder, AGENTS.md § Dates, Charter Lexicon) with their grandfathering removed, never new vocabulary. success: the page exists and is a companion of this Spec; each fold PR's checklist (§7) is green; detectors read the declared vocabulary source (vocabulary CAP-2), never the page; a rule on the page cites the ruling it comes from.
- (decision) GRANDFATHERING ENDS AT THE REBASE (2026-09-16): vocabulary-one-name-one-job left three carve-outs because renaming history was not worth it — CAP-1 'in-progress grandfathered only', CAP-6 'the 53 slug divergences and 1338 DW- ids are not renamed', Q14 'no retro-rename'. The rebase renumbers every story anyway, so the carve-outs cost nothing to close: (a) every story minted through the one function -> heading, key and filename agree for all 1,445 stories; (b) DW ids collapse to one family DW-<STATION>-<n>; (c) station rule ids collapse to one family <STATION>-<n> (the 14-prefix long tail MRS-DISP-/HER-/GATE-/SM-/LB-/BS-/SC-/FU-/RB-/PORT-/DC-/DRAIN-/ATLAS-/MEM- retires); (d) memlog type enum closed at the 13 live types, the 19 malformed '(non' entries repaired. Code-cited ids (MRS-* 100 files, GATE-* 27, FU-* 22, SC-* 15, ATLAS-* 9, DRAIN-* 6) migrate when their module is next touched — eventual consistency; a planning artifact never waits on code.
- (correction) CAP-6 CORRECTED (2026-09-16): the living station Spec reads READY, not in-progress. Earlier entries said the eight spec-pyforge-<s> flip shipped -> in-progress and stay. That contradicts vocabulary CAP-1/CAP-2 ('in-progress grandfathered only; new Specs are not written in-progress') and the act-based ladder: in-progress is a state, ready is an act ('the contract binds'). A station Spec is perpetually ready; CAP-n carry (Met <date>); shipped stays CAP/story-level. Consequence: in-progress retires from the Spec enum entirely at the rebase (17 live uses -> 0). Dream coupling unchanged: specified <=> Spec at ready or beyond.
- (decision) STATION TOKEN IS THE SHORT NAME (2026-09-16): steward spine § Station token — 'warden, mason, atlas, doctor, herald, marshal, scribe, steward — same token in CLI, URL, distribution, skill, persona'. Ids uppercase it (DW-MARSHAL-7, MARSHAL-033). No 3-letter codes; the first draft of CHAIN-STANDARD.md invented some and was corrected before commit.
- (event) 2026-09-16 DECOMPOSED (mechanism stories, Doctor's under §5 outcome/mechanism): doctor Epic 25 — 25.1 chain-sprawl-check (CAP-2; exemption list read from guild-roster.json fold_exemptions; dated baseline at ruling SHA e630e43330; baseline writes only remove), 25.2 fr-without-cap (CAP-5; per-station FR baseline; fr-cap-unresolved sibling code), 25.3 ledger-regression reads the fold PR's rekey-<date>.md so a rebase moves done rows as done (CAP-3(g); promote_sprint_status.py --rekey). All three gate the marshal pilot. Operator plan: once doctor lands and main is current, the station folds run as Cursor Cloud Agents (environment d65eb8ab) — marshal, then steward, then herald, then scribe/doctor/atlas/warden in parallel — each from CHAIN-STANDARD §7.
- (event) 2026-09-16 MECHANISM LANDED (doctor 25.1/25.2/25.3, one PR): chain-sprawl-check and fr-without-cap-check live in detectors-ci; baselines snapshotted at the ruling SHA e630e43330 (164 Dreams + 164 Spec folders; 421 FR/NFR ids across 8 PRDs); guild-roster.json declares fold_exemptions (the one source); ledger-regression, story-status and sprint-ledger-sync --rekey read the fold PR's rekey-<date>.md through one parser (pyforge.doctor.rekey). Surface declared on this Spec for the governance-side companions (two scripts, two baselines). CAP-2 met; CAP-5 met; CAP-3(g) tooling met. The marshal pilot may begin.
