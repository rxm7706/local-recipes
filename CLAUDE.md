# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Behavioral Guidelines

These four principles govern all work in this repo. The `conda-forge-expert` skill specializes them for recipe work; the BMAD skills apply them to planning/dev. Apply them globally.

1. **Think Before Coding** — state assumptions explicitly; for ambiguous requests, present interpretations, don't pick silently.
2. **Simplicity First** — minimum code that solves the problem; nothing speculative.
3. **Surgical Changes** — touch only what the task requires; match existing style.
4. **Goal-Driven Execution** — transform tasks into verifiable goals; loop until verified.

---

## Project Overview

This repository is an **AI-assisted, semi-autonomous packaging factory** for conda-forge recipes. It mirrors the workflow of `conda-forge/staged-recipes` but is supercharged with a suite of custom tools that enable Claude to handle nearly the entire recipe lifecycle, from generation and security scanning to building, debugging, and maintenance.

This is a multi-skill repo: conda-forge recipe work uses the `conda-forge-expert` skill (loads on-demand), and BMAD-driven planning/dev for sibling projects lives under `_bmad-output/projects/`.

**Critical Rule**: Do not mix `meta.yaml` and `recipe.yaml` formats in the same build run. The tooling will reject mixed-mode runs.

## BMAD Method Documentation

The BMAD Method is an AI-driven software development framework used in this project.

- **Local copy** (offline): `.claude/docs/bmad-method-llms-full.txt`
- **Live source**: https://docs.bmad-method.org/llms-full.txt

Fetch the live source for the latest version, or reference the local copy with `@.claude/docs/bmad-method-llms-full.txt` when working offline.

### Multi-Project Pattern (this repo hosts multiple BMAD projects)

This repository uses a single BMAD installation to drive multiple projects. Each project has its own subdirectory under `_bmad-output/projects/<slug>/` containing planning artifacts, implementation artifacts, project context, and project-scoped config overrides. See **`_bmad-output/PROJECTS.md`** for the index and detailed documentation.

**At session start with the user**, ask which project they're working on (or check `scripts/bmad-switch --current`) before invoking BMAD skills that write artifacts. Reading another project's artifacts is fine without switching — read directly from the file path.

**Active-project resolution priority** (used by `_bmad/scripts/resolve_config.py`):
1. `--project <slug>` per-call CLI flag (highest priority).
2. `BMAD_ACTIVE_PROJECT` environment variable.
3. `_bmad/custom/.active-project` marker file (managed by `scripts/bmad-switch`, gitignored).
4. None — only global config layers resolve; skills fall back to repo-root `_bmad-output/`.

**Six-layer config merge** (highest priority last):

| Layer | Path                                                         | Scope                                |
|-------|--------------------------------------------------------------|--------------------------------------|
| 1     | `_bmad/config.toml`                                          | Installer team (regenerated)         |
| 2     | `_bmad/config.user.toml`                                     | Installer user (regenerated)         |
| 3     | `_bmad/custom/config.toml`                                   | Global custom team, all projects     |
| 4     | `_bmad/custom/config.user.toml`                              | Global custom user, all projects     |
| 5     | `_bmad-output/projects/<slug>/.bmad-config.toml`             | Project team, active project only    |
| 6     | `_bmad-output/projects/<slug>/.bmad-config.user.toml`        | Project user, active project only    |

Layers 5 and 6 only load when an active project resolves. To set the active project: `scripts/bmad-switch <slug>`. To list projects: `scripts/bmad-switch --list`.

**Adding a new project:** see `_bmad-output/PROJECTS.md` § "Adding a new project."

### Spec-driven, framework-neutral layout (the three tiers)

This repo is spec-driven and tool-agnostic — the spec is the contract; the agent/framework is interchangeable. **`AGENTS.md`** (repo root) is the cross-tool entry point (thin per-tool pointers: `CLAUDE.md`, `.cursor/rules/specs.mdc`, `GEMINI.md`, `.github/copilot-instructions.md`). Three tiers, never crossed:

| Tier | Location | Purpose | Git |
|---|---|---|---|
| **1 — Intake spec** | `docs/specs/*.md` | The "what to build" contract every tool/framework reads (the `bmad-quick-dev` entry point) | tracked, permanent |
| **2 — Planning** | `_bmad-output/projects/<slug>/planning-artifacts/` | PRD, architecture/API specs, epics+stories list, gate reports | tracked, permanent |
| **3 — Execution output** | `_bmad-output/projects/<slug>/implementation-artifacts/` | story files, sprint YAMLs, test outputs, retros, derived per-effort specs | **gitignored / local-only** |

Rules: an **intake spec belongs in Tier 1 (`docs/specs/`)** — never a Tier-3 output dir; `implementation-artifacts/` is gitignored, so **nothing there may be git-tracked**. `bmad-drift-check` enforces both (HARD `tracked-impl-artifact` finding).

**Spec-first (MANDATORY, always-on):** before implementing any non-trivial effort, a spec MUST exist in `docs/specs/<name>.md` — if none exists, create it first (`spec-driven-development` skill or `bmad-create-*`) and bring it to `status: ready` before writing code. **Keep the spec's `status:` frontmatter current** (`draft → ready → in-progress → shipped`, with `implemented_by:` + `shipped_ref:` when shipped) — it is the framework-neutral source of truth, updated regardless of which agent/tool/human did the work. Full convention + frontmatter contract: **`AGENTS.md`** (repo root). Check status anytime with `pixi run -e local-recipes bmad-drift-check --specs`.

### Keeping BMAD artifacts in sync with the live repo (always-on)

The `_bmad-output/projects/local-recipes/` artifacts (PRD, architecture set, epics, project-context, overview, specs) hard-code volatile facts about the factory (skill version, cf_atlas schema, MCP tool / atlas-phase / pixi-env counts, gotcha range) and drift behind the fast-moving `conda-forge-expert` skill. A **two-layer sync loop** keeps them accurate and able to catch up after *any* out-of-band change (BMAD or not):

- **Detector** (cheap, deterministic): `pixi run -e local-recipes bmad-drift-check` (and `bmad-groundtruth` for live facts as JSON). Reports pin drift, count/phase-list staleness, stale rules, archive-hygiene + stray files (auto-fix with `-- --fix`), coverage completeness (every project file must be classified), and baseline-vs-live surface change. Enforced in the test suite by `.claude/skills/conda-forge-expert/tests/meta/test_bmad_artifacts_in_sync.py` (integrity only).
- **Reconciler** (correctness): the **BMAD skills themselves** — `bmad-document-project` re-grounds the living architecture/overview/source-tree/parts docs; `bmad-generate-project-context` the rulebook; `bmad-correct-course` + `bmad-create-epics-and-stories` the PRD/epics; `bmad-validate-prd` + `bmad-check-implementation-readiness` the gate reports; `bmad-index-docs` the index. Then re-stamp the baseline (`bmad-drift-check -- --write-baseline`).

**When to run:** after every CFE retro / skill MINOR bump, and whenever the detector reports `surface-changed` (an out-of-band edit to `recipes/`, `.claude/`, `pixi.toml`, or `docs/specs/`). Full procedure + finding→remedy mapping: **`_bmad-output/projects/local-recipes/SYNC-RUNBOOK.md`**. The detector is `scripts/bmad_drift_check.py`.

## Skill Reference

| Skill | Purpose | When to invoke |
|---|---|---|
| `conda-forge-expert` | Full conda-forge recipe lifecycle (generate → validate → build → submit) | Creating/updating recipes, fixing build failures, any conda packaging |
| `bmad-quick-dev` | Implement story / feature / fix from a spec | Direct implementation requests when the story spec exists |
| `bmad-create-prd` / `-create-architecture` / `-create-epics-and-stories` / `-create-story` | BMAD planning chain | Starting a new product or feature in `_bmad-output/projects/<slug>/` |
| `bmad-document-project` | Brownfield project documentation | "Document this project" requests |
| `bmad-agent-*` (analyst/architect/dev/pm/tech-writer/ux-designer) | Persona-led workflows | "Talk to John/Mary/Winston/…" requests |

For full skill list and disambiguation defaults (which review skill, simplify-vs-code-simplification, schedule-vs-loop, etc.) see auto-memory entry `feedback_skill_disambiguation.md`.

## BMAD ↔ conda-forge-expert integration

These two rules govern any BMAD-driven effort that touches conda-forge work in this repo. They apply to every BMAD skill (`bmad-quick-dev`, `bmad-agent-dev`, persona agents, planning agents, code-review agents — everything). They are **always-on**; no opt-in.

### Rule 1 — BMAD must invoke `conda-forge-expert` for any conda-forge work

When a BMAD agent's current story, task, or sub-task involves any of:

- creating, editing, validating, optimizing, building, or submitting a conda recipe (`recipe.yaml`, `meta.yaml`, multi-output, patches under `recipes/<name>/patches/`)
- responding to a conda-forge build failure or staged-recipes review comment
- packaging a PyPI / npm / CRAN / CPAN / LuaRocks / GitHub source as a conda artifact
- working with `pin_subpackage`, `compiler()`, `stdlib()`, `noarch: python`, conda-forge selectors, or rattler-build features
- interacting with `pixi run -e local-recipes …` recipe-build / autotick / submit-pr tasks
- reading or modifying anything under `.claude/skills/conda-forge-expert/`, `.claude/scripts/conda-forge-expert/`, `.claude/data/conda-forge-expert/`

…the agent **must** invoke the `conda-forge-expert` skill (via the `Skill` tool with `skill: conda-forge-expert`) before producing recipe code or running recipe-related tooling. The skill's 9-step autonomous loop, Operating Principles, Critical Constraints, and Build Failure Protocol are authoritative — the BMAD story file does not override them.

If a BMAD story's instructions conflict with `conda-forge-expert`'s guidance (e.g., the story says "loosen this pin to `>=1.0`" but conda-forge-expert's pin-loosening convention applies a different rule), the skill wins and the agent updates the story comment to record the deviation.

### Rule 2 — Every conda-forge BMAD effort ends with a retro that improves the skill

When a BMAD effort that did conda-forge work reaches its closeout (final story complete; PR merged or final review-comment resolved; or the user marks the effort done), the agent **must** run a retrospective focused on the `conda-forge-expert` skill itself. The retro:

1. Invokes the `bmad-retrospective` skill (or follows its protocol manually if BMAD is not loaded).
2. Reviews session logs, build failures encountered, recipe diffs, and reviewer comments to identify:
   - **Corrections** — guidance in the skill that turned out to be wrong, stale, or misleading.
   - **Refinements** — guidance that worked but was harder to apply than it should have been (missing examples, ambiguous wording, missing edge cases).
   - **Additions** — patterns, constraints, gotchas, or build-failure recipes encountered for the first time during this effort that future efforts should benefit from.
3. Lands the findings as edits to:
   - `.claude/skills/conda-forge-expert/SKILL.md` (Operating Principles, Critical Constraints, Recipe Authoring Gotchas, Build Failure Protocol)
   - `.claude/skills/conda-forge-expert/reference/*.md` (per-topic deep references)
   - `.claude/skills/conda-forge-expert/guides/*.md` (workflow / troubleshooting guides)
   - `.claude/skills/conda-forge-expert/CHANGELOG.md` (a new version entry summarizing the retro's deltas, dated, with a one-line summary per finding)
4. Bumps the skill version per semver (PATCH for fixes/clarifications, MINOR for new gotchas / new sections, MAJOR only if breaking workflow changes).
5. Saves a corresponding auto-memory feedback entry only if the finding crosses skill boundaries (e.g., affects how BMAD interacts with `conda-forge-expert`); skill-internal findings stay in the skill files, not in auto-memory.

The retro is not optional and not deferrable. An effort is not "done" until the retro lands.

If the effort produced no novel findings (rare — almost every effort surfaces at least one refinement), the retro still runs and produces a CHANGELOG entry stating "no skill changes; verified existing guidance held for: <summary of effort>".

## Project Documentation Reference

For extended architectural context, please reference the centralized `docs/` folder:
- **`docs/mcp-server-architecture.md`** — FastMCP server integration and PyPI name mapping subsystem.
- **`docs/enterprise-deployment.md`** — Air-gapped environments and JFrog Artifactory integration.
- **`docs/developer-guide.md`** — Local testing and general recipe development guidelines.
- **`docs/copilot-to-api.md`** — Five ways to drive a GitHub Copilot subscription as a local model backend (`copilot-api`, `litellm`, `copilot-openai-api`, `copilot-api-proxy`, `c2p`); decision tree, auth flows, configuration reference.
- **`docs/specs/copilot-bridge-vscode-extension.md`** — BMAD-consumable tech-spec for a sideload-only VS Code extension that wraps the bridge pattern. Run via `bmad-quick-dev`.
- **`docs/specs/conda-forge-tracker.md`** — BMAD-consumable tech-spec for a local sibling-directory tracker that mirrors feedstocks I'm involved with and captures follow-ups (outdated upstreams, package requests, stuck PRs, rerender requests, mark-broken/yank) as offline markdown issues with GitHub-Issues-compatible frontmatter. 13 stories, channel-aware migration path. Run via `bmad-quick-dev`.
- **`docs/specs/feedstock-platform-expansion.md`** — Parameterized BMAD-consumable tech-spec for the **dual-goal** workflow: (1) refresh `recipes/<feedstock>/` (and upstream feedstock) to the latest CFE-generated shape at the latest upstream version, and (2) widen the conda-forge build matrix by adding subdirs (e.g. `osx-arm64`, `linux-aarch64`) in the same PR. 13 stories in 3 waves. Parameters block at top (`<feedstock>`, `<upstream_version>`, `<target_platforms>`, `<fork_owner>`, …); per-case state appended to "Worked Examples". First worked example is cocoindex 1.0.10 → +osx-arm64 + linux-aarch64 (2026-06-14, Q1–Q5 resolved inline). Timeless workflow lives in `.claude/skills/conda-forge-expert/guides/feedstock-platform-expansion.md` (decision matrix, Wave A/B/C procedural detail, risk catalog, Stop-the-Line conditions). Run via `bmad-quick-dev` with feedstock + target platforms named in the prompt.
- **`docs/specs/feedstock-failure-remediation.md`** — Parameterized BMAD-consumable tech-spec for the **failing-feedstock-PR remediation loop**: take one or more red conda-forge feedstock PRs (almost always `[bot-automerge]` autotick version bumps) and drive each to MERGED-green, fix-pushed-pending-CI, or deferred-as-blocked, **executing every recipe — new (staged-recipes) or existing (feedstock) — locally in `recipes/<name>/` before any push or PR**. Rests on **three pillars**: (1) **BMAD** drives execution (`bmad-quick-dev` per run, `bmad-retrospective` at closeout); (2) the **`conda-forge-expert` skill** is the authoritative interface for *all* conda-forge work — recipe creation/edit, staged-recipes, and feedstocks + their forks — and owns the procedural detail (gotchas G10/G14/G19/G24/G26/G31–G34); (3) the **conda-forge-atlas** intelligence layer (offline `behind-upstream`/`whodepends`/`feedstock-health`/`my_feedstocks`, exposed through the CFE skill) supplies the dependency/feedstock facts that drive BLOCKED triage + prerequisite naming (sidesteps the G33 `conda search` misread). Wave A triages each PR into FLAKE (transient infra → restart ci) / REAL_FIX (recipe edit) / BLOCKED (prerequisite dep feedstock behind) via the CFE skill's flake-vs-fix signature catalog (G32); Waves B/C/D act per class; Wave E verifies merges + runs the CFE skill closeout retro. Parameters block (`<pr_refs>`, `<mode>` single/batch, `<fork_owner>`, `<bot_fork>`). Load-bearing rules: route-all-conda-forge-work-through-CFE, execute-locally-first (new + existing, push + PR), local-mirror-is-source-of-truth, maintainer-edit push to the bot fork (a `gh pr checkout` clone's `origin` is the upstream, not the fork), rerender-after-push. First worked example: the 2026-06-17/18 12-PR batch (10 merged, 2 deferred-as-blocked) that authored CFE gotchas G31–G34 (v8.30.0). Adjacent case documented: staged-recipes review-comment remediation (lyric-py #33764). Run via `bmad-quick-dev` with the failing PR(s) named in the prompt.
- **`docs/specs/sole-maintainer-feedstock-refresh.md`** — Parameterized BMAD-consumable tech-spec for the **bulk sole-maintainer feedstock refresh**: bring every `recipes/<name>/` that backs a **sole-maintainer** conda-forge feedstock up to the feedstock's published version via full CFE regenerate (diff-apply), modernizing grayskull regressions, platform-expanding the compiled subset, and migrating v0 feedstocks to v1 — **reviewing every diff, committing/pushing nothing**. The bulk-orchestration layer over `feedstock-platform-expansion.md` (delegates the per-feedstock "how"). Three pillars (BMAD + CFE skill + atlas). cf_atlas facts: rxm7706 maintains 769 feedstocks → **537 sole-maintainer** (512 local recipes); **258 confirmed-BEHIND** (~103 v1-refresh + ~142 v0-migration + 13 GitHub-tag-numbering-to-verify + 10 AHEAD/in-flight). Resume list persisted at `.claude/data/conda-forge-expert/feedstock-update/behind_verified.json`. Waves: A re-baseline (atlas refresh + live re-verify with the GitHub-tag-numbering guard — copilotkit `v1.57.2`=PyPI`0.1.88`), B v1-refresh, C v0-migration (the bulk; keep meta.yaml mirror), D compiled-platform-expansion, E gh-numbering special-case, F closeout (no commit/push; CFE retro). Resumes the 2026-06-19 effort that applied only ~25 before the cfe-* convention + langflow absorbed the session (memory `project_sole_maintainer_feedstock_update`). Run via `bmad-quick-dev`.
- **`docs/specs/co-maintainer-feedstock-refresh.md`** — Parameterized BMAD-consumable tech-spec; the **co-maintainer sibling** of `sole-maintainer-feedstock-refresh.md`. Targets the feedstocks where rxm7706 is a **co-maintainer** (in the maintainer list alongside ≥1 other handle) — disjoint from the 537 sole; together the two specs cover **every feedstock rxm7706 can modify** (537 sole + 232 co = 769). cf_atlas facts (2026-06-19): **232 co-maintained**, of which **190 have a local recipe** (62 v1 + 128 v0) + **42 with no `dir==conda` mirror** (mapping-resolve or net-new); rough delta ~143 BEHIND/8 AHEAD/39 MATCH. Same three pillars + per-recipe machinery delegated to `feedstock-platform-expansion.md`. The new dimension the sole spec didn't need: **co-maintainer coordination etiquette** — never drop a co-maintainer from `extra.recipe-maintainers`; modernize grayskull regressions but preserve *deliberate* maintainer choices (park proposals in `# CFE comments`); platform-expand without add-user gating (already a maintainer); at submission, **PR-not-self-merge**. Buckets mirror the sole spec (v1-refresh / v0-migration C1+C2 / gh-numbering / compiled-platform) plus a NEW **no-local-recipe** bucket. Waves A–G (A discovery+maintainer-snapshot, B v1-refresh, C v0-migration, D compiled-platform, E gh-numbering, F net-new mirrors, G closeout incl. maintainer-list audit). `status: ready` (intake complete, unimplemented). Run via `bmad-quick-dev`.
- **`docs/specs/graphifyy-osx-arm64-fanout.md`** — BMAD-consumable tech-spec for making `graphifyy` (noarch:python) installable on osx-arm64 by fanning out platform-expansion PRs across its 22 tree-sitter-* run-dep feedstocks. Audit (2026-06-15) splits the 26 tree-sitter-* deps into: 4 already shipping osx-arm64 (skip), 7 in-flight via `sumanth-manchala`'s 2026-06-07 PRs (watch + 14-day takeover), and 15 with no PR yet (our work). All 22 affected feedstocks have identical maintainers (`killua156, mgorny`) — so every recipe PR is gated behind a `@conda-forge-admin, please add user @rxm7706` issue per the user-set policy. Scope deliberately matches sumanth's osx-arm64 + linux-aarch64 (drops ppc64le). ~48 stories in 5 waves (A: 22 issues, B: 15 PRs in 3 batches of 5, C: watch + conditional takeover, D: smoke-test graphifyy on osx-arm64, E: consolidated retro). Delegates per-feedstock procedural detail to `feedstock-platform-expansion.md`. Q1–Q5 pre-resolved. Run via `bmad-quick-dev`.
- **`docs/specs/db-gpt-conda-forge.md`** — BMAD-consumable tech-spec for packaging DB-GPT (`eosphoros-ai/DB-GPT` v0.8.0) on conda-forge as a 7-output multi-output recipe with 7 prerequisite recipes (3 trivial pure-Python, 3 lyric-* itkwasm-pattern noarch with vendored WASM, 1 cocoindex-class `lyric-py` Rust+PyO3). Q1 resolved (`B-full`). 13 stories in 4 waves; S12 documents a `B-six-plus-app-patched` fallback if any lyric-* worker is rejected during review. Run via `bmad-quick-dev`.
- **`docs/specs/langflow-conda-forge.md`** — BMAD-consumable **submission** tech-spec for getting the entire **langflow-suite** (`langflow-ai/langflow` v1.10.0, MIT) onto conda-forge. Rewritten 2026-06-23 from the old separate-recipes/authoring model to the **multi-output suite** (`recipes/langflow-suite/` → one feedstock producing `lfx`+`langflow-base`+`langflow`) + full-closure submission model. The full transitive closure (~46 net-new recipes — langflow-family + the 4 `lfx-*` extension feedstocks, `ddgs`→`primp` (Rust), `opendsstar`→`pymilvus-model`/`milvus-lite`/`langchain-milvus`, the IBM chain `ibm-watsonx-ai`→`ibm-cos-sdk`→core/s3transfer, `langchain-ibm`, `agent-lifecycle-toolkit`, `jigsawstack`, `langwatch`, `jsonquerylang`, the `langchain-*` provider wrappers, …) is authored + built GREEN into the local channel; `langflow-suite` builds clean for all 3 outputs (`--test skip`) but its test-env solve is **blocked on one PRIMARY external cf skew**: cf `langchain 1.2.x` carries a **stale `langchain-text-splitters <1.0.0` pin that upstream 1.2.x no longer declares**, conflicting with `langchain-classic`/`opendsstar` (`>=1.1.0`) — fix is a ~one-line `langchain-feedstock` PR (Wave A1). Two further skews (litellm/`fastapi`, otel/`traceloop-sdk` cluster) gate only the optional-integration closure. Real `python_min` is **3.11** (`jsonquerylang` hard-imports `typing.NotRequired`, PEP 655 — G41). Submission is waved leaves-first (Wave A clear skews → B core hard-dep closure → C optional integrations → D CFE retro); **headline design decision: split `lfx` into its own feedstock** to break the `langflow → lfx-* → lfx` cross-feedstock cycle (Q1). Caveats encoded: `ragstack-ai-knowledge-store` BUSL-1.1 (drop, non-OSI), `langchain-elasticsearch` blocked on the `elasticsearch >=8.19` cf gap, `apify-client` blocked on `impit` py311+ (G38). `status: ready`. Surfaced CFE gotchas **G39–G43** + the external-cf-skew Build-Failure-Protocol note (skill v8.35.0). Run via `bmad-quick-dev`/`bmad-dev`.
- **`docs/specs/flyte-conda-forge.md`** — BMAD-consumable **submission** tech-spec for getting the **Flyte 2 SDK** — the PyPI **`flyte`** package (`flyteorg/flyte-sdk` v2.5.7, Apache-2.0) — onto conda-forge. **Identity-critical:** `flyte` (v2) is a *distinct* package from cf's existing `flytekit` (v1) and `flyteidl2` (v2 IDL) from cf's `flyteidl` (v1) — they coexist, do not conflate. The Go backend `flyteorg/flyte` is **out of scope** (NG1). Prerequisite-chain model (like db-gpt, not a multi-output suite): **5 net-new recipes** — `flyte`, `flyteidl2` (==2.0.26), `protovalidate`, `connectrpc` (`<0.11` — avoids the net-new `protobuf-py` that 0.11 pulls), and `pyqwest` (the **only** compiled recipe — Rust/maturin, obstore/cocoindex-class). All other 19 base deps already on cf (consume; G10 spellings `docstring_parser`/`msgpack-python`/`pyopenssl`). **Headline finding: real `python_min` is 3.11** (G40/G41) — the chain `flyte→flyteidl2→protovalidate→cel-python` floors at 3.11 because cf `cel-python` ships py3.11+ only, despite upstream `requires-python >=3.10`. 8 stories in 5 waves (1 leaves: pyqwest+protovalidate → 2: connectrpc+flyteidl2 → 3: flyte → 4 Q1-gated: rust-controller/sandbox extras → 5: CFE retro). 5 open questions (Q1 extras scope [rec: base-only], Q3 python_min 3.11 [rec], Q4 rich-click==1.8.9 pin). `status: ready` (complete intake, unimplemented). Run via `bmad-quick-dev`/`bmad-dev`.
- **`docs/specs/microsoft-conda-forge.md`** — BMAD-consumable tech-spec for the June 2026 audit of the `github.com/microsoft/*` org against conda-forge feedstocks. Bundles 14 stories in 3 waves covering ~10–14 new recipes: Wave 1 quick wins (`microsoft-edit` Rust CLI, `agent-framework` umbrella, `pyqlib`, `pyrit`), Wave 2 mid-tier Python (`promptflow` + 3 subpackages, `semantic-kernel`, `torchscale`, `promptwizard` gated on Q1), Wave 3 native/compiled (`microsoft-seal` CMake C++ lib, `diskannpy` multi-output). Three open questions: Q1 (PromptWizard PyPI presence), Q2 (DiskANN win-64), Q3 (microsoft-edit binary name). Explicit out-of-scope list documents Windows-only, .NET-heavy, research-code-only, and already-shipped Microsoft repos. Run via `bmad-quick-dev`.
- **`docs/specs/trendshift-conda-forge.md`** — BMAD-consumable tech-spec (codename **trendshift**) for packaging the top GitHub-**trending** Python repos to conda-forge. **Hybrid shape**: a *timeless, re-runnable* discover→triage→package workflow PLUS a concrete first worked-example batch (the 2026-06-20 trending snapshot, seeded by `HKUDS/CLI-Anything` = `cli-anything-hub`). **Two layers**: (1) a **new cf_atlas discovery engine** — **Phase T** ingests `github.com/trending/python` (HTML scrape primary; GitHub Search API fallback per Q1; **never** trendshift.io, which has no public API) into a new `github_trending_repos` table + materialized `trending_classification` side-table + `v_trending_candidates` view (schema **v28→v29**), exposed via a `trending-candidates` CLI + MCP tool; (2) the **packaging workflow** that consumes the tiered candidate list. **Tiers** (data-driven, extensible): Tier-1 curated-gap `noarch:python`, Tier-2 broad/compiled/multi-output/prereq-closure, `skip` (awesome-lists/apps/datasets/already-on-cf), Tier-3+ reserved for empirical patterns. ~10–18 stories in 4 waves (A engine A1–A5; B Tier-1 incl. named `cli-anything-hub`; C Tier-2; D cadence-guide + CFE retro); first-batch recipe count open (B1 finalizes). 5 open questions (Q1 source robustness gates Wave A; Q2–Q5 non-blocking, all with defaults). Validated 2026-06-20 via an intake-spec readiness audit (it's a `bmad-quick-dev` *intake* doc like the other specs, not BMAD planning *output*); F2 data-model gap fixed (tiers materialized, not view-computed). Run via `bmad-quick-dev`.
- **`docs/specs/atlas-phase-f-s3-backend.md`** — BMAD-consumable tech-spec for an S3/parquet backend that replaces `api.anaconda.org` as Phase F's required data source (the only firewall-blocking dependency in the atlas), adds Phase F+ richer metrics (rolling 30/90-day downloads, trend slope, platform/python/channel breakdowns), and exposes 3 new MCP tools (`platform_breakdown`, `pyver_breakdown`, `channel_split`). 14 stories in 4 waves; Wave 1 ships the air-gap fix as MVP. Run via `bmad-quick-dev`. **Wave 1 shipped v7.6.0 (S3 backend + dispatcher) + v8.17.0 (admin profile default-flip). Wave 2 split out to its own focused intake at `docs/specs/atlas-phase-f-wave2-richer-metrics.md`.**
- **`docs/specs/atlas-phase-f-wave2-richer-metrics.md`** — BMAD-consumable tech-spec scoped to Wave 2 of the Phase F+ effort. 6 stories: schema migration v26 → v27, rolling 30/90-day downloads, 90-day trend slope, per-platform + per-Python breakdown tables, all computed in one extended parquet sweep (zero new network). MINOR bump; additive only. **Excludes** Wave 3 CLIs (`platform_breakdown`, `pyver_breakdown`, `channel_split`) which ship as a separate effort once Wave 2 lands. Run via `bmad-quick-dev`. **Shipped v8.18.0 (commit f28ed3ea56, 2026-06-13). v8.18.1 retro deltas added 3 new sub-rules to `reference/atlas-phase-engineering.md` § 10: (g) `INSERT OR REPLACE` ≠ "replace" semantics on partial reruns; (h) provenance-column claims verified against dispatcher writers; (i) step-04 adversarial review is load-bearing for Phase-F-class changes. Wave 3 split out to its own focused intake at `docs/specs/atlas-phase-f-wave3-cli-surface.md`.**
- **`docs/specs/atlas-phase-f-wave3-cli-surface.md`** — BMAD-consumable tech-spec scoped to Wave 3 of the Phase F+ effort. 6 stories: schema migration v27 → v28 + 1 new table (`package_channel_downloads`); 3 new operator CLIs/MCP tools (`platform_breakdown` for ARM/win/EOL share; `pyver_breakdown` incl. `--policy-check` to flag python_min bump-safe candidates from real download data; `channel_split` to surface defaults-channel migration opportunities). All read-side, offline-safe, no new external data sources. MINOR bump (v8.19.0); additive only. Applies v8.18.1 § 10 (g) (DELETE+INSERT for the new breakdown table) and § 10 (h) (verified provenance attribution) by construction. Run via `bmad-quick-dev`. **Shipped v8.19.0 (commit d5095dde18, 2026-06-13); v8.19.1 retro deltas added 2 new sub-rules to `reference/atlas-phase-engineering.md` § 10: (j) population-distribution claims need empirical-verification discipline; (k) Ask First boundary clauses trigger investigation, not necessarily HALT.**
- **`docs/specs/atlas-phase-k-cron-runner.md`** — BMAD-consumable tech-spec for the Phase K secondary-rate-limit fix (carryover P2 from v8.16.5 session retro). Replace the 8-worker `ThreadPoolExecutor` burst with a single-token-bucket sustained-rate scheduler (default 3.0 req/s). Cuts the empirically-measured 15% HTTP 403 rate on full-channel fanouts (4,400 net-new rows, 2026-05-12 incident per auto-memory `project_phase_k_secondary_rate_limit.md`) to <1%. Trade-off: cold full-channel Phase K wall-clock grows ~30 min → ~60-75 min (still inside v8.16.6's 14,400s wrapper cap with slack). Operator escape hatch: `PHASE_K_AGGRESSIVE=1` restores previous burst behavior for tight-time-budget operators. 5 stories; no schema change; no new tables; MINOR bump (v8.20.0). Applies v8.18.1 § 10 (i) (step-04 adversarial review load-bearing for dispatcher-touching changes). Run via `bmad-quick-dev`.
- **`docs/specs/atlas-pypi-universe-split.md`** — BMAD-consumable tech-spec for the 2026-05-13 actionable-scope audit. Bundles four findings: (1) Phase H pypi-json denominator one-line fix (672k → 12k packages, 56× cut); (2) Phase J + M archived-feedstock filters; (3) new `pypi_universe` side table (schema v20) extracting the ~660k `pypi_only` rows out of `packages` so the working set stays conda-actionable + Phase D splits into daily-lean + weekly-universe-upsert; (4) new `pypi-only-candidates` CLI + MCP tool surfacing the universe. 11 stories in 4 waves; Wave 1 ships the Phase H fix immediately. Run via `bmad-quick-dev`. **Shipped v7.9.0 (commit 958ae694dd, 2026-05-13).**
- **`docs/specs/conda-forge-expert-v8.0.md`** — BMAD-consumable tech-spec for the v8.0.0 bundle closing the four deferred follow-ups from the v7.9.0 audit retro. Schema v21 migration covering: (A5) `v_actionable_packages` SQL view + structural-enforcement meta-test that prevents phase-selector drift; (A3) Phase H `pypi_last_serial` freshness gate — Phase H warm-daily drops ~5 min → ~30 s; (A6) drop unused `vuln_total` column; (A4) persona-aware default profiles (`--profile maintainer|admin|consumer`) for `build-cf-atlas` with auto-detection of `gh api user`. **MAJOR bump (v7.9.0 → v8.0.0) — A4 changes default behavior.** ~22 stories in 4 waves; two-PR strategy (A+B+C bundled in PR #1, D in PR #2). Five 📋-open Phase-N-gated catalog rows flip to ✅. Run via `bmad-quick-dev`. **Shipped v8.0.0 (Wave A+B at commit e671ef463d, 2026-05-13; Wave D + closeout this session, 2026-05-13). Wave C (A6 `vuln_total` drop) DEFERRED — consumer audit found 4 actual consumers (`detail_cf_atlas`, `cve_watcher`, `staleness_report`, `scan_project`); column kept. Retro: `_bmad-output/projects/local-recipes/implementation-artifacts/retro-conda-forge-expert-v8.0-2026-05-13.md`.**
- **`docs/specs/cfe-pr-artifact-downloader.md`** — BMAD-consumable tech-spec for the v8.14.0 PR-artifact downloader. Given a staged-recipes or feedstock PR (URL or number), resolve the Azure DevOps `buildId` via `gh pr checks`, list published artifacts via the Azure REST API, download all `conda_pkgs_*` ZIPs, optionally extract them into a valid local mamba channel layout. New `pr-artifacts` pixi task + `pr_artifacts.py` CLI + `download_pr_artifacts` MCP tool. Read-only, anonymous Azure auth, idempotent caching. ~9 stories in 4 waves (Core fetch → CLI + pixi task → MCP tool → docs + closeout). **MINOR bump (v8.13.2 → v8.14.0) — net-new feature, additive only.** Single-PR strategy (no schema change, no breaking surface). 8 open questions pre-resolved with recommendations. Live case study: PR #33693 buildId 1536673 (gh-copilot-cli session). Run via `bmad-quick-dev`.
- **`docs/specs/atlas-pypi-intelligence.md`** — BMAD-consumable tech-spec for the v8.1.0 PyPI intelligence layer. Architecture: keep `pypi_universe` reference-data-only (3 columns); push all enrichment into a new `pypi_intelligence` side table joined on `pypi_name`. Schema v22 adds `pypi_intelligence` + `pypi_universe_serial_snapshots` + `v_pypi_candidates` view. 5 new phases: (O) daily serial-snapshot deltas + activity_band classification (no HTTP); (P) BigQuery `pypi.file_downloads` for 30/90 d official download counts; (Q) cross-channel `in_bioconda|pytorch|nvidia|robostack|homebrew|nixpkgs|spack|debian|fedora` BOOLs via bulk repodata; (R) per-project `pypi.org/pypi/<name>/json` enrichment bounded to top-N candidate slice (license, requires_python, classifiers, repo_url, packaging_shape classifier); (S) computed `conda_forge_readiness` score (0-100) + `recommended_template`. New `pypi-intelligence` CLI + MCP tool. Persona profile integration: `admin` enables all 5, `maintainer` enables O+Q only (cheap), `consumer` enables O only. **MINOR bump (v8.0.x → v8.1.0) — additive features, no breaking change.** ~24 stories in 5 waves; two-PR strategy (A+B bundled in PR #1, C+D+E in PR #2). Run via `bmad-quick-dev`. **Shipped v8.1.0 (Wave A `bfb595104c`, Wave B `fd6d1c3ea1`, Wave C `bbe1c42fce`, Wave D `7898ef2865`, Wave E closeout — 2026-05-15). All 8 spec open questions pre-resolved. 1,064 tests passing. Retro: `_bmad-output/projects/local-recipes/implementation-artifacts/retro-atlas-pypi-intelligence-2026-05-15.md`.**
- **`docs/specs/atlas-phase-p-incremental.md`** — BMAD-consumable tech-spec for the **v8.15.0** Phase P refactor. Driver: 2026-06-12 BigQuery invoice surprise ($500+ for one refresh; spec/code/docs all repeated a "~30 GB scanned" claim off by ~1000× — real cost is ~$15–25/run, $170/run when partition pruning degrades). Replaces v8.1.0's single-shot 90-day query with an incremental, partition-by-partition refresh that stores per-day per-package counts in a new `pypi_downloads_daily` table (schema v25 → v26); recomputes `pypi_intelligence.downloads_30d/90d` from local SQL aggregation. Adds dry-run preflight (aborts above operator-set USD cap), `maximum_bytes_billed` hard server-side cap, `job_timeout_ms` wall-clock cap (10 min). Preserves full per-package exactness across the entire ~600 k PyPI namespace — no top-N filter, no aggregator fallback. Cost: ≤$10/refresh in 99% of runs (vs. $170+ pre-fix); ≤$100 for the first-pull; annual ~$60 (vs. $500+). Q1/Q2/Q5/Q7 pre-resolved (monthly cadence, $10 default cap, 10-min job timeout, 95 d retention); Q3/Q4/Q6/Q8 carry recommendations only. **MINOR bump (v8.14.x → v8.15.0)** — additive schema + new env vars; consumer surface (`downloads_30d/90d` columns) unchanged. ~12 stories in 3 waves; single-PR strategy. New regression guard `tests/meta/test_no_thirty_gb_lie.py` greps spec/docs/code for the bad number — structural fix for the lesson, not just a one-time correction. Predecessor: `atlas-pypi-intelligence.md`. Run via `bmad-quick-dev`. Hot-patch **v8.14.3** (cost caps + doc-lie corrections, no schema change) lands first to stop the bleeding.
- **`docs/specs/atlas-appthreat-deep-signals.md`** — BMAD-consumable tech-spec for the AppThreat Deep Signals release: EPSS + CWE-rollup overlays on the Phase G/G' vulnerability columns (+`epss_scores`/`cwe_categories` side tables, `fetch-epss`/`fetch-cwe-catalog` CLIs). Schema v23→v24→v25 round-trip (Wave C blint + withdrawn-filter columns cancelled/dropped pre-implementation). `status: shipped` (conda-forge-expert v8.6.0, 2026-05-24). Run via `bmad-quick-dev`.
- **`docs/specs/conda-forge-expert-v8.9.md`** — BMAD-consumable tech-spec for the v8.9.0 generator hardening: maturin/PyO3 routing + sdist-driven import-name extraction + abi3-gated `version_independent`. `status: shipped` (conda-forge-expert v8.9.0). Run via `bmad-quick-dev`.
- **`docs/specs/claude-team-memory.md`** — BMAD-consumable tech-spec for a `.claude/memory/` team-shared memory layer + a `team-memory` skill (10 waved stories, FR/AC). `status: ready` (complete intake, unimplemented). Run via `bmad-quick-dev`.
- **`docs/specs/cfe-atlas-datapipeline-kedro-migration.md`** — BMAD-consumable tech-spec for migrating the cf_atlas orchestrator from hand-rolled phases to a Kedro/Dagster/DuckDB stack (Implementation Waves A–G). `status: ready` (intake; relocated from `implementation-artifacts/` to its correct Tier-1 home 2026-06-20). Run via `bmad`.

Skill-internal documentation (loaded on-demand when the skill activates):
- **`.claude/skills/conda-forge-expert/SKILL.md`** — Recipe authoring agent operating principles, 10-step lifecycle loop (step 8b: prepare submission branch on fork, step 9: open PR), build-failure protocol.
- **`.claude/skills/conda-forge-expert/reference/`** — `recipe-yaml-reference.md`, `meta-yaml-reference.md`, `python-min-policy.md`, `mcp-tools.md`, `conda-forge-ecosystem.md`, `pinning-reference.md`, `selectors-reference.md`, `jinja-functions.md`, `atlas-actionable-intelligence.md` (persona-mapped catalog of every actionable signal cf_atlas exposes — shipped + open + gap), `atlas-phases-overview.md` (phase-indexed companion: per pipeline stage, what's downloaded, the purpose, and the actionable intelligence it produces), `atlas-phase-engineering.md` (engineering patterns for writing or refactoring phases — rate limits, GraphQL batching, atomic writes, enterprise routing), `dependency-input-formats.md` (manifest / lock-file / SBOM / container-input support matrix — the canonical "what does scan_project accept?" reference), `conda-forge-yml-reference.md` (high-signal subset of conda-forge.yml keys — staged-recipes per-recipe override + feedstock-level — covers `azure.store_build_artifacts`, `os_version`, `provider`, `bot.version_updates.exclude`, deprecated keys, and common patterns).
- **`.claude/skills/conda-forge-expert/guides/`** — getting-started, migration, ci-troubleshooting, cross-compilation, feedstock-maintenance, testing-recipes.
- **`.claude/skills/conda-forge-expert/quickref/`** — `commands-cheatsheet.md` (incl. project pixi tasks), `bot-commands.md`.

### conda-forge-expert v7.0.0 layout (3-tier + MCP layer)
- **`.claude/skills/conda-forge-expert/scripts/`** — canonical implementation (source of truth). Edit code here.
- **`.claude/scripts/conda-forge-expert/`** — public CLI entrypoint layer (~30 thin subprocess wrappers). What `pixi run` calls.
- **`.claude/data/conda-forge-expert/`** — mutable runtime state (cf_atlas.db, vdb/, cve/, mappings, caches). Gitignored.
- **`.claude/tools/conda_forge_server.py`** — FastMCP server exposing 30+ tools across recipe-authoring + atlas-intelligence + project-scanning surfaces. Started by Claude Code at session boot; tool schemas surface at call time.

**Atlas intelligence (v7.0+)** — `cf_atlas.db` ships 16 schema versions, 15 pipeline phases (B → N), and 17 CLIs. Daily-use entrypoints: `detail-cf-atlas`, `staleness-report`, `feedstock-health`, `whodepends`, `behind-upstream`, `cve-watcher`, `version-downloads`, `release-cadence`, `find-alternative`, `adoption-stage`, `scan-project`. All read-side CLIs are offline-safe. See `.claude/skills/conda-forge-expert/SKILL.md` § "Atlas Intelligence Layer" for the persona-mapped guide.

Enterprise routing (JFrog Artifactory, internal mirrors) is **runtime-driven** via `_http.py` (truststore + JFrog/GitHub/.netrc auth chain) — env vars only, never committed config. See `.claude/skills/conda-forge-expert/CHANGELOG.md` v6.0.0 / v7.0.0 entries for the full release notes.

Repo-wide pointers:
- **`_bmad-output/PROJECTS.md`** — BMAD multi-project index.
- **Auto-memory** — `~/.claude/projects/-home-rxm7706-UserLocal-Projects-Github-rxm7706-local-recipes/memory/MEMORY.md` indexes accumulated feedback (skill disambiguation, recipe pin-loosening, .bat shim rules, BMAD multi-project pattern) and project context.