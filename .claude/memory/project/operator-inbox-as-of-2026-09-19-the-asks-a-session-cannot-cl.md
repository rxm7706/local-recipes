---
name: "operator-inbox-as-of-2026-09-19-the-asks-a-session-cannot-cl"
description: "Operator inbox as of 2026-09-19 (the asks a session cannot close itself; tracked here so every harness and session sees…"
metadata:
  type: project
---

Operator inbox as of 2026-09-19 (the asks a session cannot close itself; tracked here so every harness and session sees them — not one operator's auto-memory). (1) Retire or keep the five stale August bmad-loop runs that sit paused/ESCALATED in the loop homes and resync the loop homes (447 commits behind main): marshal DW-OPS-2026-09-19-1 — say 'retire' and an agent runs marshal retire --execute + marshal refresh. (2) Hand-only cleanup in the main checkout: git stash clear (14 stashes; patches archived at ~/UserLocal/Projects/Github/rxm7706/_stash-archive/) and git worktree remove of the merged .worktrees/dispatch-* plus their dispatch/* branches: marshal DW-OPS-2026-09-19-2. (3) DW-FU-23-6 (herald): the live Claude Design sync proof — HERALD_LIVE_SYNC_PROOF=1 pixi run -e pyforge-herald deck-sync-proof -- --slug <deck>, twice, with the operator's credentials. (4) Live-verify the one-AGENTS.md instruction surface on the harnesses only an operator seat can run — Gemini /memory show, a Cursor chat, a Copilot chat's References, a Devin session's Accessed Knowledge (+ its app-side Repo Setup and a 'land one story' Playbook): scribe DW-FU-19-1-3; only Claude Code was proven live. (5) Drain concurrency: three Claude Code dispatch lanes run at once (marshal in lr-m50, doctor in the main checkout, steward in lr-s59) because Cursor is out of usage — cap at two if the Claude quota is a concern (herald lost ~12 h to a usage stall on 2026-09-16). (6) Optional: export GH_TOKEN (or GITHUB_TOKEN) in the shell that runs fleet-picture to silence the recurring sibling-dreams-unreachable advisory. Close each item by editing its DW row (status: done + verified line) or, for (5)/(6), by updating this entry.
