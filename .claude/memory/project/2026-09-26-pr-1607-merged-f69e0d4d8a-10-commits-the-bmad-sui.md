---
name: "2026-09-26-pr-1607-merged-f69e0d4d8a-10-commits-the-bmad-sui"
description: "2026-09-26: PR #1607 MERGED (f69e0d4d8a, 10 commits) — the bmad-suite advance is complete and supersedes the 2026-09-25…"
metadata:
  type: project
---

2026-09-26: PR #1607 MERGED (f69e0d4d8a, 10 commits) — the bmad-suite advance is complete and supersedes the 2026-09-25 capture that still shows it unmerged. Channel (SelfExplainML): bmad-loop 0.12.0, bmad-method-test-architecture-enterprise 1.27.2, bmad-eval-quality 4.3.0 (BOTH variants — __win came from the windows-2022 dispatch of .github/workflows/test-windows.yml with upload_channel=SelfExplainML, the G115 path), bmad-module-skill-forge 2.2.0, bmad-suite 2026.9.26; pixi.toml floors follow the published builds (eval-quality 4.3.0 x3 sites, TEA 1.27.2 x2, bmad-loop 0.12.0 x3, bmad-suite-full >=2026.9.26); all 8 loop homes at f69e0d4d8a. Two facts every harness must know: (1) conda-forge/bmad-loop-feedstock EXISTS since 2026-09-20 (not this repo's) and ships bmad-loop gated __unix with tmux/rich/httpx — under strict channel priority it shadows the estate build and kills every win-64 solve, so pixi.toml pins bmad-loop = { version = ">=0.12.0", channel = "SelfExplainML" } in the pyforge-marshal, pyforge-guild and local-recipes features; never reorder the channels; run lookup_feedstock before assuming a SelfExplainML package is estate-only (the recipe's cfe-on-conda-forge fields are hints, G78). (2) pyforge-marshal's bmad-loop range is now >=0.11.0,<0.13 — pyproject.toml, the package pixi.toml and the FR-52 constants in adapters/harness_bmadloop.py move together (test_manifest_sync.py is the tripwire; four fixture tests encode the ceiling too); bmad-loop 0.12.0 validates a non-mapping config.yaml itself ahead of marshal's guard. Process facts: any commit touching .claude/skills/conda-forge-expert must be a retro(cfe): commit that moves the CHANGELOG (four station guards); a bmad-suite member bump needs `steward catalog render` (the catalog manifest embeds member versions); generate-bmad-suite pins UPSTREAM-LATEST floors, so regenerate only after every member recipe is current (that is how eval-quality 4.3.0 and skill-forge 2.2.0 joined this PR); a marshal src change beyond the chain-currency grace window needs the PRD/spine/epics reconcile AND a retro (retros/retro-pyforge-marshal-2026-09-26.md). Still owed: DW-steward-suite-advance-gaps-2026-09-25 (suite advance cannot run live: registry routing, open_pr branch/commit, publish hook, worktree), DW-marshal-disp020-stale-refusal-2026-09-25, DW-marshal-baseline-drift-supersession-2026-09-25. Operator-owned next: dispatch steward 67.1 first, then herald 26.1 / scribe 21.1 / scribe 22.1; python-foundry PR #19 after 21.1; marshal 46.9, 46.10, 47.1-47.4, 54.1 queued (a stale dispatch worktree .worktrees/dispatch-pyforge-marshal-47.1 @ b62bf9f79e, unlanded and clean, was left in place); the kept worktree local-recipes-wt-sprint-ledger-query-module still holds 3 AGENTS.md checklist items for 21.1.
