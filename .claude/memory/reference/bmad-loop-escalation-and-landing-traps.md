---
name: "bmad-loop-escalation-and-landing-traps"
description: "Five traps in bmad-loop escalation recovery, landing, and resume — the destructive re-arm default, the misleading --no-interactive flag, the baseline the restore patch must be diffed against, the intent feed dashboard-gen reads by default, and resume's foreground-blocking + self-killed-session hazard."
metadata:
  type: reference
---

bmad-loop escalation recovery, landing, and resume mechanics — five traps that cost real work to rediscover.

(1) `rearm_escalation` DEFAULTS TO DESTRUCTIVE. With `restore_patch=None` it flips the escalated story back to PENDING, resets the tree to the story's baseline, and sets the spec status to `ready-for-dev` — so the dev session RE-IMPLEMENTS FROM SCRATCH. Committed work on the story branch is not consulted. On doctor 6-8 that would have discarded 1,197 lines of `sources/factory.py`, a 1,197-line test suite, and roughly 677K output tokens.

(2) `--no-interactive` IS NOT THE SAFE FLAG. It reads like the conservative option — it only skips the interactive resolve agent. ONLY `--restore-patch` changes the re-drive mode: it sets the spec to `in-review` and re-applies the saved diff, so review resumes on the attempted change instead of re-implementing it. 'Just re-arm and resume' is the destructive path.

(3) THE RESTORE PATCH MUST BE DIFFED AGAINST CURRENT MAIN, not against the story's recorded baseline. `rearm_escalation` advances the baseline to the project's current HEAD, so a patch diffed from the stale baseline fails to apply and the engine escalates again. Build it as `git diff origin/main...<story-HEAD>` and prove it with `git apply --check --3way` BEFORE invoking resolve.

(4) `dashboard-gen` DEFAULTS TO THE INTENT FEED. `--source sprint-status` reads the Tier-3 sprint feed, which marks a story done at attempt-1 dev completion — before review. Because `docs/dashboard/data.js` is shared across all eight stations, regenerating it during a landing imports OTHER stations' premature state and makes board-vs-ledger diverge whichever way you move; `apply_tracked_ledger` is upgrade-only by design, so the board cannot be walked back. Use `--source git` for landings: it derives the done set from merge commits reachable from HEAD, so it renders what actually merged and cannot inherit the feed's claims.

(5) `bmad-loop resume <id>` BLOCKS IN THE FOREGROUND for the run's entire remaining lifetime (`_resume_paused_run` → `engine.run()`, synchronous; `bmad_loop/cli.py:1224-1225`) — it does not self-detach into a tmux session the way the naming convention `bmad-loop-<run_id>` (`runs.py:127-128`) suggests. That session name belongs to the per-story dev/review ADAPTER pane the engine manages internally, and `_resume_paused_run` explicitly kills any stale one with that exact name at startup (`runs.kill_session(run_dir.name)`, `cli.py:1181`) — so wrapping the top-level `bmad-loop resume` call in your OWN tmux session named `bmad-loop-<run_id>` gets that wrapper killed the instant resume starts. Use a distinct session name (e.g. `resume-<slug>`) and `cd` into the run's actual loop home (`~/.bmad-loops/<slug>/`, a real separate git worktree) first — `--project` defaults to cwd and there is no resume-time flag for it (`cli.py:2268,2404-2405`). No token-budget flag exists at resume either: the budget lives in `<loop-home>/.bmad-loop/policy.toml`, reloaded fresh from disk every resume (`cli.py:1124`) — but `max_tokens_per_story` there is advisory-only (a harmless post-hoc journal entry, `engine.py:2170-2177`) unless `session_budget_mode = "enforce"`, which was `warn` on every station checked 2026-08-11. A prior session's "stopped: token budget" was very likely a manual `bmad-loop stop --graceful` triggered by seeing that advisory next to a natural stopping point, not an automatic kill — confirm via each run's `journal.jsonl` (look for `run-stop, graceful: true` with no `budget-stop` entry) before assuming enforcement fired.

BUT DO NOT ACTUALLY USE BARE `bmad-loop resume <id>` FOR A MARSHAL-TRACKED STATION — this whole trap is a description of its mechanics, not an endorsement. Use **`marshal factory resume <slug>`** instead (`cli/spin.py:940-960`; no run id needed, it finds the most recent run for the loop home itself, refuses live if an escalation is unresolved per AD-45, then detach-launches `bmad-loop resume` FOR you and re-attaches a fresh supervisor sidecar). Bare `bmad-loop resume` never spawns that sidecar, and `marshal status`/`fleet-picture`'s `derive_home_state` treats a dead/absent supervisor pid as an unconditional override to `"unsupervised"` — `if not finished and supervisor_alive is False: return "unsupervised"` fires *before* the function ever looks at whether a task is actively `dev-running` (`core/status.py:685-731`). The run is completely healthy; the fleet report just can't see it, and stays wrong for the run's ENTIRE remaining lifetime (there's no lazy re-probe — once `supervisor_alive` reads `False` at launch it's `False` forever for that process). Confirmed 2026-08-11: resumed 5 stopped stations via bare `bmad-loop resume`, all 5 read `UNSUPERVISED — needs re-spin` on every subsequent `fleet-picture` call despite being verifiably alive the whole time. To confirm engine liveness use the primary check in [[fleet-landing-pass-liveness]] (`bmad-loop status <run_id> --json` + `list --json`) — not `cat engine.pid`, not bare `grep 'bmad-loop run'`.
