[pytest]
testpaths = unit integration meta
markers =
    network: requires live internet access (skipped by default; opt in with -m network)
    slow: takes > 5 seconds; skip in fast runs
    integration: end-to-end multi-tool workflow
    meta: test that validates the skill itself (docs/CLI/version consistency)
addopts = -ra --strict-markers
