---
topic: pyforge-genesis — the operating model, recorded
updated: 2026-07-31T19:40
---

- (decision) Constitutive Spec authored 2026-07-28. Genesis holds the operating model's records (Charter, Lexicon, membership); the installer split out to marshal as spec-genesis-installer. Surface is the constitutive documents themselves — this Spec declares no code, because the Charter is the unit of legitimacy and does no work. Records the GUILD_DREAMS mirror hazard: the constant is duplicated in bmad_drift_check.py and generate.py, and updating one alone made the board warn on a Dream the Charter permits.
- (governed-change) 2026-07-31 — `docs/dreams/README.md`: the Dream index gains **`fidelity-enforcement`** (Capabilities; `owner: marshal`, `status: dreamt`, PR #170), and two `title:` fields are shortened to their common names on operator call — `Wasm-first analytical data stack (OCP-ready)` → **WASM Data Stack**, `The factory behind the firewall` → **Firewalled Factory** (frontmatter only; both files keep their descriptive H1, the existing pattern). The Spec is unaffected: it constrains the ownership model and the constitutive set, and neither moved — the index gains a row under an existing station, no `owner:` changed, and INV-0…INV-3 hold (33 dreams, 34 specs, 9 projects), the single open INV-1 being `fidelity-enforcement`'s own missing Spec. Written from the diff by the agent that made the change — not a re-derivation. Worth recording why this entry exists at all: the drift was introduced by PR #170 earlier in the same session and **merged unnoticed, because nothing in CI runs `spec_surface_check`**. Wiring the detectors is the work this entry accompanies.
- (decision) 2026-07-31 GENESIS FOLDS INTO THE CHARTER; THIS DREAM IS RETIRED (operator call). Its own text declares "Genesis is three things in one"; audited against what remains, one is gone and one was never its own. THE SEED — `init`/`adopt`, the bootstrapper — split to [[genesis-installer]] on 2026-07-28 and is Marshal's buildable work; zero artifacts of it remain here. THE MASTER IDEA — the umbrella narrative of personas, setup and vision — is pyforge-charter.md §1-8 plus the Lexicon, restated; it was never a second thing, only a second telling. THE ALIGNMENT INSTRUMENT — the 13-slide deck at presentations/pyforge-genesis/ — is a communication artifact, and communication is Herald's craft (§4, each works one craft not all). What is left after removing the first two is a deck, and a deck is not a Dream.
- (decision) 2026-07-31 DISPOSITION. `docs/dreams/pyforge-genesis.md` -> `status: archived`, retaining its frontmatter and a pointer to this decision, per the convention that an archived Dream keeps its Spec so a future reader learns from the retirement instead of rediscovering the idea (EXEMPLAR INV-1). This Spec is therefore NOT deleted; it becomes the record of a contract that was folded, and its `status` moves accordingly. The deck stays where it is and keeps its name — it documents the origin faithfully and renaming a shipped presentation buys nothing. Marshal's `spec-genesis-installer` is untouched by this: it already lives under pyforge-marshal and shares zero artifacts with Genesis, which is what made the fold safe to propose.
- (constraint) 2026-07-31 THE FOLD IS NOT A DELETION OF HISTORY. The 2026-07-28 split is the reason this Dream became foldable, and the record must say so in that order: the split removed the buildable half correctly, and left a narrative half that already had an owner. Reading the fold as "Genesis was redundant all along" would be wrong and would obscure the actual lesson — that a Dream doing three jobs was corrected once, halfway, and the remaining half stayed invisible for three days because `dream_chain_check` verifies a Dream HAS a Spec, never that it still has a JOB. That gap is recorded against docs/dreams/fidelity-enforcement.md, not here.
- (open) 2026-07-31 DOES THE ORIGIN NARRATIVE NEED A HOME? The Dream carries material the Charter does not: `archive/docs/bmad-setup-plan.md` as the origin document, and the account of this repo being the first brownfield adoption. That is provenance, and it is genuinely useful — it is the evidence [[genesis-installer]] cites for "the installer is that procedure, generalized". Candidates: fold into the Charter's Realization log, move to the installer Spec as its `sources:`, or leave here in an archived Dream. Not decided; flagged so the fold does not quietly drop the one thing that was never duplicated.

