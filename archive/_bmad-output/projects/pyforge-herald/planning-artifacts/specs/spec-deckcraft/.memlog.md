---
topic: deckcraft — air-gapped conda-native editable-deck pipeline (PPTX/Marp/infographics), the deck family's designated PPTX engine
updated: 2026-07-25T14:42
---

- (direction) User (via coordinator): distill deckcraft's approved PRD+architecture+epics chain into the Spec (five-field contract), express/headless, non-interactive; do not re-plan or invent scope
- (decision) Slug inherited from BMAD project deckcraft; spec folder under active project planning-artifacts/specs per Dream-first tier model
- (decision) Express distill (input very rich: Dream + brief + PRD 52FR/23NFR + architecture 15AD + epics 28 stories + 2026-07-25 research backfill); non-interactive headless session
- (capability) CAP-1 Document ingestion: user passes PDF/DOCX/PPTX/MD/XLSX/URL/multi-source input and deckcraft extracts text/structure via markitdown, with RAG fallback for long docs and near-dup slide collapse; success = FR-01..08 formats extract cleanly, FR-43 RAG triggers above 16K tokens, FR-44 dedup collapses near-identical slides
- (capability) CAP-2 LLM-driven outline and slide generation: user provides a prompt (and/or extracted doc content) plus optional slide-count/tone/audience and deckcraft produces structured slide JSON via the configured LLM backend; success = FR-09..12 produce a complete, schema-valid Deck JSON; SC-02/03/04 tier timing budgets met
- (capability) CAP-3 Editable PPTX rendering: deckcraft renders slide JSON into a .pptx opening cleanly in PowerPoint 2019+/O365 with 100% native DrawingML text/shapes, accessed only through the pptx_engine adapter; success = FR-13..15, SC-05 zero rasterized content, NFR-14/15
- (capability) CAP-4 Marp markdown rendering + round-trip: deckcraft emits a Marp .md sibling for every .pptx and can regenerate an updated .pptx from a hand-edited .md, including inverse PPTX-to-Marp conversion; success = FR-23/24, J5 round-trip preserves text/order/images/chart data with lossy elements flagged in a sidecar
- (capability) CAP-5 Diagrams and charts: user requests a Mermaid diagram or a typed chart with structured data and deckcraft renders it offline (mermaid-py+playwright; matplotlib/plotly) embedding as native PowerPoint shapes/charts where the data fits; success = FR-16..18, embedded SVGs convert to native shapes in PowerPoint
- (capability) CAP-6 Image operations: user can get an existing image described by a local vision LLM, and optionally (opt-in, default-off) generate a photorealistic image via SDXL-Turbo lazy-loaded on first use; success = FR-19..22, failures degrade gracefully per FR-26/NFR-06, no crash
- (capability) CAP-7 Bring-your-own style sources: user passes --style <path> (.potx/.pptx/.pptx --as-sample/.docx/.pdf, .odp/.odt stretch) and deckcraft extracts a common Style JSON (theme, fonts, layouts, brand assets) applied to the generated deck, with LLM-guided layout mapping and heuristic fallback; success = FR-25/45..52, NFR-23 visual match across a documented style-source corpus
- (capability) CAP-8 Cross-platform + hardware-tier operation: deckcraft runs identically on linux-64/osx-arm64/win-64, auto-detects RAM/GPU via deckcraft init and recommends small/medium/large tier with per-tier model defaults; success = FR-27..32, SC-08 structurally-equivalent .pptx across platforms, AD-14 tier detection
- (capability) CAP-9 Multi-surface distribution: deckcraft is invocable as a CLI (typer), an MCP server (fastmcp stdio), a Claude Skill (.claude/skills/deck-builder/), and installable via conda-forge; success = FR-35..42, SC-09 3-of-6 surfaces live at V1, SC-11 recipe accepted to staged-recipes
- (constraint) Layered architecture with strict adapter boundaries (A-01): surfaces consume Pipeline only; renderers consume engine adapters only; three swap points are LLMAdapter, PptxEngineAdapter, PlatformAdapter
- (constraint) Single immutable JSON-serializable Pydantic data model for all inter-module data (A-02); no pandas.DataFrame or hidden globals across module boundaries (P-05)
- (constraint) P-01: all pptx access via deckcraft.engines.pptx_engine adapter only; code elsewhere MUST NOT import pptx directly (vendor-fallback discipline for dormant python-pptx, FR-15/AD-07)
- (constraint) P-02: all LLM calls via pydantic-ai through LLMAdapter only; no direct httpx/ollama/anthropic SDK calls from business logic (backend swap, FR-33/34)
- (constraint) P-06/FR-26: optional capabilities (image gen, vision, mermaid, chart render) use the OptionalCapability pattern - failures are caught, logged, and produce a sidecar warning; the pipeline never crashes on a failed optional capability
- (constraint) P-07/DR-01/NFR-11: no outbound network call without consulting the offline gate first; default mode makes zero outbound connections after first-run model download; CI verifies under unshare -n (NFR-08)
- (constraint) DR-02: all runtime dependencies come from conda-forge; the only non-conda-forge runtime artifact is pre-downloadable GGUF/safetensors model weights (air-gappable via DECKCRAFT_MODEL_DIR)
- (constraint) DR-05/SC-08: cross-platform parity - linux-64/osx-arm64/win-64 must produce structurally equivalent .pptx from the same prompt+tier; only LLM text content varies
- (constraint) License must be MIT or Apache-2.0 only (founding acceptance criterion, no proprietary/copyleft code) - but pymupdf, pinned as the primary PDF style-extraction library for FR-46c/AD-13, is AGPL-3.0/Artifex-commercial dual-licensed, contradicting this constraint (research/plan-validation-notes-2026-07-25.md Finding 2); UNRESOLVED, a human call, blocking before Story 3.2 not before this Spec - see open question
- (question) pymupdf AGPL-3.0/commercial dual-license conflicts with the MIT/Apache-2.0-only acceptance criterion for FR-46c's PDF style extraction (AD-13) - accept AGPL for this one opt-in capability path with documented rationale, substitute pdfplumber (already in env, MIT-family) with a documented quality trade-off, or scope out FR-46c's OCR-fallback depth? Must resolve before Story 3.2 (Sprint 4)
- (assumption) PPTX engine is python-pptx 1.0.2 (dormant since 2024-08-07 but stable), wrapped behind pptx_engine; vendor-and-patch only on the AD-07 trigger criteria (blocker bug, format-support gap, or unpatched 30-day compat break), never preemptively
- (assumption) LLM backend default is llama-server (conda-forge, on-demand); Ollama and mlx-lm (osx-arm64 preferred per AD-02) are alternatives; LiteLLM routing deferred until conda-forge relaxes its pydantic pin conflict
- (assumption) Hardware tiers: small (<=32GB)/medium(32-48GB)/large(>=48GB) with per-tier GGUF model defaults locked in AD-03; deckcraft init auto-detects and recommends, user can override
- (non-goal) VS Code extension with /deck slash command (V2; reuses copilot-bridge-vscode-extension.md spec) - out of this Spec's scope
- (non-goal) MCP HTTP transport (V2; AD-04) - V1 ships stdio only
- (non-goal) LiteLLM-based routing layer - deferred until conda-forge litellm relaxes its pydantic pin conflict
- (non-goal) Expanded template library beyond default-professional + bmad-prd-pitch - V2
- (non-goal) Jupyter notebook surface - free via pixi-kernel but unfocused, not a committed surface
- (non-goal) MS365 Copilot Power Platform connector (V3, gated on tenant admin availability - not a V1/V2 commitment)
- (non-goal) pptx-assembler / template-style-extractor library convergence with presenton-pixi-image (V-infinity discretionary target, AD-05); V1 keeps separate engines
- (non-goal) Upstream contribution back to python-pptx (equation parsing, TIFF support) - discretionary, not committed
- (non-goal) ODP/ODT style sources (FR-46d) - slipped to V2 per AD-10 to keep V1 inside the <=12-week kill-criteria envelope
- (note) Success signal source: PRD primary criterion (>=1 real ship-quality deck/week x4 consecutive weeks post-V1, kill-criteria at week 6 otherwise) plus SC-05 (100% editable native DrawingML, zero rasterized - non-negotiable), SC-07 (air-gapped functional parity via CI), SC-11 (conda-forge recipe accepted to staged-recipes)
- (event) Self-validate pass 1 (coherence, Spec Law 1-6+8): PASS - 9 capabilities all intent+success; intents WHAT-only; every constraint rules something out (adapter boundaries, pptx-import ban, LLM-adapter ban, offline gate, conda-forge-only, cross-platform parity, MIT/Apache-vs-AGPL conflict); 9 non-goals; success signal machine-checkable (dogfooding + SC-05/07/11); CAP IDs fresh CAP-1..9
- (event) Self-validate pass 2 (preservation): PASS - every load-bearing PRD FR/NFR/SC lands in a capability or the success signal; every hard architecture AD/pattern lands in Constraints or Assumptions; research Finding 2 (pymupdf AGPL) lands in both a Constraint and an Open Question per instruction, not dropped. Wrapper-only content dropped: brief's competitive-differentiation narrative (findings 8/9/10, ppt-master/presenton comparables) and dated citation fixes (findings 3/7) - these affect narrative framing only, no FR/AD/story
- (event) Spec finalized (create): SPEC.md derived from memlog; no companions needed (no diagrams, no multi-entity catalogs load-bearing enough to warrant a spec-authored companion this pass); sources docs/dreams/deckcraft.md + product-brief-deckcraft.md + prd.md + architecture.md + epics.md + research/plan-validation-notes-2026-07-25.md fully absorbed
