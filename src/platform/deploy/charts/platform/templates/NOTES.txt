{{ .Chart.Name }} {{ .Chart.Version }} released as {{ .Release.Name }} (namespace {{ .Release.Namespace }}).

REQUIRED PRE-CREATED SECRET (this chart never renders one -- AD-12):
  name: {{ .Values.existingSecret }}
  keys:
    DJANGO_SECRET_KEY  Django's SECRET_KEY.
    DATABASE_URL       App-role DML URL. With this release's postgres:
                       postgres://{{ .Values.postgres.auth.appUsername }}:<password>@{{ include "platform.postgres.fullname" . }}:5432/{{ .Values.postgres.auth.database }}
    MIGRATION_DATABASE_URL  Migration-role DDL URL for the Liquibase Job:
                       postgres://{{ .Values.postgres.auth.username }}:<password>@{{ include "platform.postgres.fullname" . }}:5432/{{ .Values.postgres.auth.database }}
                       (direct postgres Service DNS -- never a pooling proxy)
    POSTGRES_PASSWORD  The same <password>, consumed by the postgres
                       container itself (POSTGRES_USER={{ .Values.postgres.auth.username }}).
    REDIS_PASSWORD     Redis AUTH password (Story 12.6); wired into
                       REDIS_BROKER_URL / REDIS_CACHE_URL / REDIS_URL
                       (broker alias) and --requirepass on both Redis pods.

In-cluster service DNS names (compose DATABASE_URL from these):
  PostgreSQL     {{ include "platform.postgres.fullname" . }}:5432
  Redis broker   {{ include "platform.redisBroker.fullname" . }}:6379
  Redis cache    {{ include "platform.redisCache.fullname" . }}:6379
  Media PVC      {{ include "platform.media.pvcName" . }} (ReadWriteMany)
  Web            {{ include "platform.fullname" . }}:{{ .Values.service.port }}

Exposure flows:
{{- if .Values.ingress.enabled }}
  Vanilla K8s (active): Ingress on host {{ .Values.ingress.host }}.
{{- else }}
  Ingress is DISABLED. On OpenShift, expose the web Service with the thin
  Route overlay chart (src/platform/deploy/overlays/ocp/chart), installed
  beside this release with route.service.name={{ include "platform.fullname" . }}.
{{- end }}
  OCP install shape: helm install {{ .Release.Name }} <core-chart> \
      -f src/platform/deploy/overlays/ocp/core-overrides.yaml
  then: helm install {{ .Release.Name }}-ocp src/platform/deploy/overlays/ocp/chart \
      --set route.service.name={{ include "platform.fullname" . }}

Migrations: {{ include "platform.liquibase.fullname" . }} (hook-weight -1,
liquibase update on the platform image) then {{ include "platform.migrate.fullname" . }}
(hook-weight 0, migrate --fake). Never an initContainer on web/worker. On a
FRESH install the web pods go Ready before those Jobs finish (the /ht/
Database check is connectivity-only), so ORM-touching pages 500 transiently
until Liquibase + fake-migrate complete; upgrades are covered by pre-upgrade
ordering.

Flags (Story 26.4 / AD-11): ConfigMap {{ include "platform.fullname" . }}-flags
is the in-cluster copy of src/platform/config/flags.json. Install with
  --set-file flags.tree=src/platform/config/flags.json
The FILE provider polls /etc/pyforge/flags.json in-process — do not add a
Reloader or flagd sidecar.
