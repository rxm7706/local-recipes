# Two Redis Deployments, same image class (canopy AD-10 / parent AD-1):
# redis-cache evicts (allkeys-lru) on emptyDir; redis-broker is durable
# (AOF on PVC) and bounded (noeviction + required maxmemory < limit).
{{- range $role := tuple "cache" "broker" }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "platform.redis.roleFullname" (dict "root" $ "role" $role) }}
  labels:
    {{- include "platform.labels" $ | nindent 4 }}
    app.kubernetes.io/component: redis-{{ $role }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "platform.selectorLabels" $ | nindent 6 }}
      app.kubernetes.io/component: redis-{{ $role }}
  template:
    metadata:
      labels:
        {{- include "platform.selectorLabels" $ | nindent 8 }}
        app.kubernetes.io/component: redis-{{ $role }}
    spec:
      serviceAccountName: {{ include "platform.dataServiceAccountName" $ }}
      {{- with $.Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
        {{- if not (kindIs "invalid" $.Values.redis.runAsUser) }}
        runAsUser: {{ $.Values.redis.runAsUser }}
        {{- end }}
      containers:
        - name: redis
          image: {{ include "platform.imageRef" (dict "image" $.Values.redis.image "path" "redis.image") }}
          imagePullPolicy: {{ $.Values.redis.image.pullPolicy }}
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "platform.existingSecretName" $ | quote }}
                  key: {{ required "redis.passwordSecretKey is required" $.Values.redis.passwordSecretKey | quote }}
          command:
            - redis-server
            - --requirepass
            - $(REDIS_PASSWORD)
            {{- if eq $role "cache" }}
            - --maxmemory
            - {{ required "redis.cache.maxmemory is required" $.Values.redis.cache.maxmemory | quote }}
            - --maxmemory-policy
            - {{ required "redis.cache.maxmemoryPolicy is required" $.Values.redis.cache.maxmemoryPolicy | quote }}
            {{ else }}
            {{ $maxmemory := required "redis.broker.maxmemory is required" $.Values.redis.broker.maxmemory }}
            {{ $memLimit := required "redis.broker.resources.limits.memory is required" $.Values.redis.broker.resources.limits.memory }}
            {{ $maxBytes := include "platform.parseMemoryBytes" $maxmemory | int }}
            {{ $limitBytes := include "platform.parseMemoryBytes" $memLimit | int }}
            {{ if ge $maxBytes $limitBytes }}
            {{ fail (printf "redis.broker.maxmemory (%s) must be strictly less than redis.broker.resources.limits.memory (%s)" $maxmemory $memLimit) }}
            {{ end }}
            - --appendonly
            - "yes"
            - --appendfsync
            - {{ required "redis.broker.appendfsync is required" $.Values.redis.broker.appendfsync | quote }}
            - --maxmemory
            - {{ $maxmemory | quote }}
            - --maxmemory-policy
            - {{ required "redis.broker.maxmemoryPolicy is required" $.Values.redis.broker.maxmemoryPolicy | quote }}
            {{- end }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - name: redis
              containerPort: 6379
              protocol: TCP
          livenessProbe:
            exec:
              command:
                - redis-cli
                - -a
                - $(REDIS_PASSWORD)
                - ping
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - redis-cli
                - -a
                - $(REDIS_PASSWORD)
                - ping
            initialDelaySeconds: 5
            periodSeconds: 5
          {{- if eq $role "cache" }}
          {{- with $.Values.redis.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- else }}
          {{- with $.Values.redis.broker.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- end }}
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          {{- if eq $role "broker" }}
          persistentVolumeClaim:
            claimName: {{ include "platform.redisBroker.pvcName" $ }}
          {{- else }}
          emptyDir: {}
          {{- end }}
{{- end }}
