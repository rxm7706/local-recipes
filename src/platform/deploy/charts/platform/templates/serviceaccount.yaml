# One ServiceAccount for every workload in this release -- never the
# namespace default, so OCP SCC bindings (and any future RBAC) have a
# single, named subject.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "platform.fullname" . }}
  labels:
    {{- include "platform.labels" . | nindent 4 }}
