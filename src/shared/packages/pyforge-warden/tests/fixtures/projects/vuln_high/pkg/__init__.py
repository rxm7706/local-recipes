# The source imports nothing, so the declared `pdos-vuln-fixture-high`
# dependency is also flagged unused by deptry (DEP002) -- harmless alongside
# the HIGH-severity vuln: finding this fixture exists to prove (Story 1.6).
VALUE = 1
