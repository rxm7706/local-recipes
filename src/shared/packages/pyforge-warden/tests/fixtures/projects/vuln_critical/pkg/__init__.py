# The source imports nothing, so the declared `pdos-vuln-fixture` dependency
# is also flagged unused by deptry (DEP002) -- harmless alongside the
# critical vuln: finding this fixture exists to prove (Story 1.6 AC1).
VALUE = 1
