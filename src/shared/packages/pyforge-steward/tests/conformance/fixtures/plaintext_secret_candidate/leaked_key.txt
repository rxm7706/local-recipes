# Synthetic fixture only (Story 1.3) — never a real credential.
# Deliberately short and word-marked (not a plausible-length real key) so it
# cannot be mistaken for a real secret by GitHub push protection or similar.
ANTHROPIC_API_KEY = "sk-ant-api03-TEST00FAKE00PLACEHOLDER00NOTREAL"
