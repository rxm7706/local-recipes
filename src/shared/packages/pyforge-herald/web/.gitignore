node_modules/
dist/

# Stories 8.4/9.4/10.5: the local snapshots each panel's export script
# writes from the operator's .herald/*.json local storage -- generated
# data, not source. success.json/notices.json previously slipped in as
# tracked files (an inconsistency with progress.json's own established
# convention here); untracked and added below to match.
public/progress.json
public/success.json
public/notices.json
