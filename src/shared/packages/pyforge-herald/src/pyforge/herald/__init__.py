"""pyforge.herald — Dream-to-deck bridge CLI.

Seeds, pulls, and syncs Claude Design decks against this repo's
`docs/dreams/` and `presentations/` trees via the `herald deck` subcommands.

This module is the Story 1.1 build-wiring skeleton; the transport/
bridge-core/state/error-hierarchy/registry implementation is delivered by
later stories against
``_bmad-output/projects/pyforge-herald/planning-artifacts/``.
"""

__version__ = "0.1.0"
