"""``pypi_intelligence`` pipeline (Story B2 — Phases C, C.5, D, H, O, P, Q, R, S).

Auto-discovered by ``find_pipelines()`` (B1 pattern — ``pipeline_registry.py`` is not
edited).
"""

from .pipeline import create_pipeline

__all__ = ["create_pipeline"]
