"""Port Protocol definitions (Structural Seed) -- reserved for a later
story. Each port will declare ``egress: true|false``; egress ports will
accept only ``Redacted`` payloads (AD-34). No Protocol is defined yet --
Story 1.1 only reserves this package's location. Inventing port shapes now,
before a real consumer exists, risks getting them wrong (spec's "Never"
list)."""

from __future__ import annotations
