---
description: Dream-first workflow + repo tier model (generated from the neutral contract)
alwaysApply: true
---

# Cursor rules — the neutral contract

Read `AGENTS.md` at the repo root for the full cross-tool guide. The sections below are
generated from the SAME fragments `AGENTS.md`'s own managed regions render from — same
tiers, same paths, same git dispositions — framed for Cursor.

## The tiers (do not cross them)

{{ tiers }}

## Portability contract (why this stays framework-neutral)

{{ portability-contract }}

## Dream-first workflow (MANDATORY — every agent, every framework)

{{ dream-first-workflow }}
