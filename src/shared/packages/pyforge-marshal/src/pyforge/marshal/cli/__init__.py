"""``pyforge.marshal.cli`` -- the argparse dispatch tree, envelope
rendering, and exit-code emission (Structural Seed). Story 1.1 scaffold:
only ``--version``/``--help`` are wired via ``cli/main.py``; no real
command exists yet."""

from __future__ import annotations
