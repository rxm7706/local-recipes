"""The impure edge -- one adapter per port (Structural Seed, AD-4). Only
``harness_bmadloop.py`` exists this story (the AD-3 seam declaration); the
other 7 adapters (``vcs_git``, ``process_posix``, ``observer_mux``,
``fs_local``, ``clock_system``, ``notify_file_desktop``, ``forge_gh``) are
later stories' explicit scope."""

from __future__ import annotations
