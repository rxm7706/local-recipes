"""pyforge.scribe.extras -- optional `compile_surface` ingest extras (Story
6.1, unifying-strategy Grounding 2026-08-30). Extras are off by default
(AD-6, air-gap); each submodule imports its own optional third-party
dependency lazily, never at module import time.
"""
