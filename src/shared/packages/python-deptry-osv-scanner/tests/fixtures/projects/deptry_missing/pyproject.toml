# deptry DEP001 fixture (Story 1.3): the source imports a module that is
# neither declared nor installed -> deptry emits DEP001, which the default
# hygiene policy maps to WARN (exit 0) in 1.3 — its block is gated on Story
# 2.1's name-mapping confidence (follow-up Opus review, 2026-07-14). The
# finding is still surfaced (never a false-green). requests is declared and
# imported (clean) so the inventory is non-empty.
[project]
name = "deptry-missing-fixture"
version = "1.0.0"
dependencies = [
    "requests==2.31.0",
]
