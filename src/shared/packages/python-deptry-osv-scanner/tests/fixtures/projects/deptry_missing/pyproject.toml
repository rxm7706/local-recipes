# deptry DEP001 fixture (Story 1.3): the source imports a module that is
# neither declared nor installed -> deptry emits DEP001, which the default
# hygiene policy escalates to policy-violation (exit 1). requests is declared
# and imported (clean) so the inventory is non-empty.
[project]
name = "deptry-missing-fixture"
version = "1.0.0"
dependencies = [
    "requests==2.31.0",
]
