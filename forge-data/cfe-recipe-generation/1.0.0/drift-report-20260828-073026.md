---
workflowType: 'audit-skill'
stepsCompleted: ['init', 're-index', 'structural-diff', 'severity-classify', 'doc-drift', 'report']
lastStep: 'report'
date: '2026-08-28'
user_name: 'Rxm7706'
skill_name: 'cfe-recipe-generation'
skill_path: '.claude/skills/cfe-recipe-generation/active/cfe-recipe-generation/'
source_path: '.claude/skills/conda-forge-expert'
forge_tier: 'Quick'
drift_score: 'SIGNIFICANT'
nextWorkflow: ''
previousWorkflow: 'create-skill'
audit_ref: 'local'
audit_ref_source: 'baseline'
degraded_mode: false
confidence_mode: 'normal (T1-low; Quick tier text-diff -- no ast-grep/qmd/ccc detected)'
provenance_reconstructed: true
---

# Drift Report: cfe-recipe-generation

## Audit Summary

**Confidence Mode:** normal (T1-low; Quick tier text-diff — no ast-grep/qmd/ccc detected)
**Skill:** cfe-recipe-generation v1.0.0
**Source:** .claude/skills/conda-forge-expert
**Tier:** Quick (created at Quick)
**Date:** 2026-08-28
**Overall Drift Score:** SIGNIFICANT

**Provenance note:** The original create-skill provenance map (Stories 6.1-6.3) was written only
under the ephemeral agent worktree those stories ran in
(`.claude/worktrees/agent-a00a0f6206d94a499`, per this package's own `metadata.json.source_root`)
and never persisted anywhere durable — that worktree no longer exists, so no provenance map for
`cfe-recipe-generation` existed anywhere in this worktree before this audit ran (confirmed: no
`forge-data/cfe-recipe-generation/` prior to this session). This is a second, previously-unknown
gap in slice 1's CAP-2 clause 5 record, distinct from the `forge-tier.yaml`-missing gap Story 12.3
was scoped to close.

Rather than run in degraded mode (an unbounded source-tree scan of all ~68 conda-forge-expert
scripts, which would drown this slice's 5-script/7-template scope in false "added" noise from
unrelated slices), a minimal `provenance-map.json` was reconstructed with `file_entries[]` only —
the 12 tracked files' real, current SHA-256 hashes from the compiled package, mapped to their
`metadata.json`-recorded `source_file` locations (`forge-data/cfe-recipe-generation/1.0.0/provenance-map.json`,
self-documented via its own `schema_note` field). `entries[]` (a per-export AST/text baseline) is
left empty — no such baseline survived, and inventing one would violate this workflow's
zero-fabrication rule. This lets the real `skf-compare-file-hashes.py` (structural-diff §4b) run
its real, decisive file-level drift check. The export-level diff (§1, below) necessarily shows
every currently-extracted export as "added" against the empty baseline — this is a known artifact
of the missing compile-time baseline, **not genuine new-export drift**, and is excluded from
severity classification below (see Structural Drift's own caveat).

**Severity Classification:**

| Category  | Count |
|-----------|-------|
| CRITICAL  | 0     |
| HIGH      | 0     |
| MEDIUM    | 2     |
| LOW       | 1     |
| **Total** | 3     |

**Doc Drift:** doc-drift check could not run for this skill's one tracked source — see
Documentation Drift section below. Does not affect the source-code drift score above.

---

## Structural Drift

**Comparison:** Reconstructed provenance map (file_entries only, hashes as of 2026-08-28; no
compile-time `entries[]` baseline survived) vs current scan of `.claude/skills/conda-forge-expert`
(2026-08-28).
**Method:** Quick: text-diff (function/class declarations, deterministic `grep`-equivalent
pattern match — real extraction from real files, not LLM eyeballing).

### Export-level diff (§1) — NOT scored as drift (see caveat)

The deterministic `skf-structural-diff.py` (baseline `entries: []` vs. the 73 exports extracted
from the 5 live `.py` files) reports summary `{"added": 70, "removed": 0, "changed": 0, "moved": 0,
"unchanged": 0}`. Every one of the 70 "added" rows is an artifact of the empty compile-time
baseline (there is nothing to diff against, so every currently-found export trivially reads as
"new") — **not evidence that 70 exports were actually added to the source since compile time**.
Full machine output: `forge-data/cfe-recipe-generation/1.0.0/structural-diff-result.json`. None of
these 70 rows are carried into Severity Classification below.

### Script/Asset Drift (added 106, removed 0, changed 1) — real, decisive check

`skf-compare-file-hashes.py compare` walked `file_entries[]` (this slice's 12 tracked files) against
the live `.claude/skills/conda-forge-expert` tree. Full machine output:
`forge-data/cfe-recipe-generation/1.0.0/file-hash-diff-result.json`.

| Bucket | Count | Meaning here |
|---|---|---|
| **Unchanged** | 11 | All 4 other scripts (`recipe-generator.py`, `name_resolver.py`, `_cfy_template.py`, `github_version_checker.py`) and all 7 templates are byte-identical to the compiled package's copies — zero drift. |
| **Changed** | 1 | `scripts/github_updater.py` — see below. **Real drift.** |
| **Removed** | 0 | No tracked file disappeared from source. |
| **Added** | 106 | Every other `scripts/`/`templates/`/`examples/` file under `.claude/skills/conda-forge-expert/` (Slices 2–5's own canonical scripts, e.g. `submit_pr.py`, `cve_manager.py`, `scan_project.py`, plus example docs). **Not drift** — these files were never part of Slice 1's 12-file scope; "added" here just means "exists in the wider CFE tree, outside this slice." Full list in the machine-output JSON. |

#### Changed: `scripts/github_updater.py`

| | Compiled package | Live source |
|---|---|---|
| Lines | 311 | 469 (+158) |
| SHA-256 | `03f70336…0ca9bbc` | `9901c146…8a7692b4` |

The live file gained two functions the compiled copy does not have, plus a CLI flag wiring them
in — the CFE v8.84.0 Rule-2 retro (commit `621ab29c`/`565ef7d194`, PR #676, gotcha G109,
"HEAD-advance for commit-pinned recipes"), landed **after** this compiled package was created
(2026-08-21) and after Story 6.3's manual sha256 verification (which was correct for its own
2026-08-21 snapshot). Story 12.1 (2026-08-27) already mirrored this retro into slice 1's *brief*
(`brief_mirrored_through`) — that only updates the brief's prose scope-amendments; it does not
re-port the actual script content, which is what this audit checks and what remains outstanding.

### Summary

| Category | Count |
|----------|-------|
| Added (out-of-slice-scope files; not drift) | 106 |
| Added (export-level artifact of empty baseline; not drift) | 70 |
| Removed | 0 |
| Changed | 1 |
| **Total real drift items** | **1** (decomposed into 3 severity-classified findings below — 2 new functions + 1 changed CLI surface, all in the same file/root cause) |

---

## Semantic Drift

Skipped — Quick tier (no `ast-grep`/`qmd`/`ccc` detected by `skf-setup`; semantic diff requires
Deep tier).

---

## Severity Classification

**Overall Drift Score: SIGNIFICANT**

Classified via the real deterministic helper (`skf-severity-classify.py`, encoding
`references/severity-rules.md`) over the 3 real findings from Script/Asset Drift above (the 70 +
106 "added" rows are excluded per their own caveats — they are not drift, and feeding them through
would misrepresent an artifact of the missing baseline / out-of-slice scope as real severity).

### CRITICAL (0)

| # | Finding | Type | Detail | Location | Confidence |
|---|---------|------|--------|----------|------------|
| — | none | — | — | — | — |

### HIGH (0)

| # | Finding | Type | Detail | Location | Confidence |
|---|---------|------|--------|----------|------------|
| — | none | — | — | — | — |

### MEDIUM (2)

| # | Finding | Type | Detail | Location | Confidence |
|---|---------|------|--------|----------|------------|
| 1 | `update_recipe_head()` added to live source, absent from compiled package | added (export) | New public function — HEAD-advance mode for commit-pinned recipes (Story 15.2/CAP-2, gotcha G109; CFE v8.84.0) | `scripts/github_updater.py:290` | T1-low |
| 2 | `main()` gained a `--head` CLI flag not present in the compiled copy | changed (implementation) | Optional flag (`action="store_true"`, default `False`) that dispatches to `update_recipe_head()`; the compiled package's `main()` has no `--head` option at all, so invoking it with `--head` raises an argparse error rather than degrading gracefully | `scripts/github_updater.py:408` | T1-low |

### LOW (1)

| # | Finding | Type | Detail | Location | Confidence |
|---|---------|------|--------|----------|------------|
| 1 | `_fetch_default_branch_head()` added to live source, absent from compiled package | added (private) | New private helper backing `update_recipe_head()` | `scripts/github_updater.py:267` | T1-low |

### Classification Summary

| Severity | Count |
|----------|-------|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 2 |
| LOW | 1 |
| **Total** | 3 |

---

## Documentation Drift

This skill's `metadata.json.doc_sources` records exactly one entry, with `detected_via:
"readme_always"`:

```
"url": "/home/rxm7706/UserLocal/Projects/Github/rxm7706/local-recipes/.claude/worktrees/agent-a00a0f6206d94a499/README.md"
```

That "url" is a local filesystem path into the same now-deleted ephemeral agent worktree
(`metadata.json.source_root`), not a fetchable URL — `skf-detect-docs.py compare-hashes` raised
`ValueError: unknown url type` attempting `urllib.request.Request()` on it (a real, reproducible
tool failure for this malformed entry, not a network/availability issue). Per this step's own
"never abort the audit pipeline on any failure" rule, the doc-drift check is skipped for this run
rather than treated as a hard halt. This is itself a small, real provenance-quality gap in this
compiled package's `metadata.json` (the doc source should have been recorded as a repo-relative
path or omitted, not an ephemeral absolute path) — informational only; does not affect the
source-code drift score above.

---

## Remediation Suggestions

### Priority Actions (CRITICAL + HIGH)

None — no CRITICAL or HIGH findings.

### Recommended Updates (MEDIUM)

| # | Finding | Remediation | Effort |
|---|---------|-------------|--------|
| 1 | `update_recipe_head()` missing from compiled package | Port `update_recipe_head()`, its helper `_fetch_default_branch_head()`, and the `main()` `--head` CLI wiring from `.claude/skills/conda-forge-expert/scripts/github_updater.py` (lines 267–469) into `.claude/skills/cfe-recipe-generation/1.0.0/cfe-recipe-generation/scripts/github_updater.py`, sha256-verify the result matches the live file, update `metadata.json`'s recorded hash/line-count if tracked, and re-run `test_slice1_equivalence.py` + `test_github_updater.py` to confirm parity — the same pattern Story 6.3 used to port `github_version_checker.py`. | low |
| 2 | `main()` `--head` CLI flag missing | Covered by remediation #1 above (same file, same port) — not a separate action. | low |

### Optional Improvements (LOW)

| # | Finding | Remediation |
|---|---------|-------------|
| 1 | `_fetch_default_branch_head()` missing from compiled package | Covered by remediation #1 above (same port). |

### Workflow Recommendation

**Optional:** Minor-to-moderate drift detected (SIGNIFICANT overall, but capped at MEDIUM per-finding
severity — no CRITICAL/HIGH). Manual re-port of `github_updater.py` per remediation #1 is
sufficient; `[US] Update Skill` could also apply it, but the fix is a single well-isolated file
port with an existing precedent (Story 6.3), so manual is likely faster here.

---

## Provenance

| Field | Value |
|-------|-------|
| **Audit Date** | 2026-08-28 |
| **Audited By** | Ferris (Audit mode) — driven directly through skf-audit-skill's own deterministic helper scripts (`skf-load-provenance.py`, `skf-structural-diff.py`, `skf-compare-file-hashes.py`, `skf-severity-classify.py`, `skf-detect-docs.py`) rather than the fully interactive prose flow, because the prose flow's degraded-mode path has no headless-consumable way to supply a source path or a bounded scan scope — see Audit Summary's Provenance note |
| **Forge Tier** | Quick |
| **Tools Used** | direct file I/O + text-pattern matching (no ast-grep/qmd/ccc — none detected by skf-setup this session) |
| **Source Path** | .claude/skills/conda-forge-expert |
| **Skill Path** | .claude/skills/cfe-recipe-generation/active/cfe-recipe-generation/ |
| **Provenance Map** | forge-data/cfe-recipe-generation/1.0.0/provenance-map.json (reconstructed this session — file_entries only; see schema_note field in the file itself) |
| **Provenance Age** | 0 days (reconstructed today; compile-time provenance map from 2026-08-21 was never durably persisted — see Audit Summary) |
| **Mode** | normal (file_entries-backed; degraded mode was deliberately avoided — see Audit Summary Provenance note) |
| **Baseline Ref / Commit** | `local` @ `(none recorded)` |
| **Audit Ref / Commit** | `local` @ `(none recorded)` (baseline — `source_ref: "local"` in metadata.json short-circuits upstream-drift detection per init.md §5b) |
| **Upstream Latest** | (not applicable — local/internal source, not fetched) |

**Confidence Legend:**
- **T1:** AST extraction — high reliability, structural truth
- **T1-low:** Text pattern matching — moderate reliability
- **T2:** QMD temporal context — evidence-backed semantic analysis
