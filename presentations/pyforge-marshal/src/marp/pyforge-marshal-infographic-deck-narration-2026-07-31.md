# Narration script — PyForge Marshal - Infographic Deck

> Extracted from `PyForge Marshal - Infographic Deck.dc.html` speaker notes (regenerable — edit `data-speaker-notes` in Design and re-extract; never hand-edit). 20 scenes. Generation: the 2026-07-31 six-act rebuild.

## Scene 01 — Infographic — at a glance

The one-pager as a deck. Marshal, the Commander: the factory line of the Dream-to-Code operating model. Spec in, gated loop, validated code out. Six commands shipped; the supervisor is the product. Ten verbs — six shipped, four charter — set the frame.

## Scene 02 — The problem

Autonomous coding fails two ways: unattended agents ship plausible-but-wrong code, or humans review everything and the speed evaporates. Marshal's answer is structural — deterministic orchestration, spec gates before merge, scope checks on frozen surfaces, independent verification. Gates are never weakened to raise the autonomy share. The receipts: 128 stories shipped fleet-wide, zero frozen-surface violations reaching a merge.

## Scene 03 — The Dream-to-Code pipeline

The operating model, tier by tier. The Dream is yours — raw aspiration in a markdown file. Herald renders the Pitch. Then the two tiers Marshal owns: the Spec, a five-field machine contract, and the Build, gated story loops. At the top: Realized — shipped and governed, patrolled by deterministic detectors. Every Dream shows at its furthest stage on the live console.

## Scene 04 — The Guild — stations 01–04

The first four stations. Herald the Proclaimer — decks, infographics, broadcasts, and now video scripts for manticore. Marshal the Commander — this deck's subject, six CLI commands shipped. Atlas the Navigator — the dependency map, complete at 57 of 57. Warden the Guardian — the six-axis compliance gate, complete at 43 of 43, never false-green.

## Scene 05 — The Guild — stations 05–08

The second four. Mason the Artisan Builder — wheels and conda-forge in one deterministic pass, 769 feedstocks tended. Doctor the Physician — pre-flight and fleet vitals, and the verdict on Marshal's own conformance row. Scribe the Chronicler — team memory and the knowledge graph. Steward the Provisioner — the estate, the keys, the pager.

## Scene 06 — The BMAD team

Two persona layers. The Guild are the factory's stations; the BMAD team are Marshal's inner workforce — six sub-agents his line mobilizes per story: Mary for analysis, John for product, Winston for architecture, Sally for UX, Amelia for development, Paige for documentation. Who decides: the human governs intent and answers escalations; Marshal owns execution; stations hold independent verdicts.

## Scene 07 — One story's journey

Watch one story travel the line: spec drafted, gate pause for approval, build in an isolated worktree, the independent multi-lens review standing at every autonomy level, then a mechanical merge only after the full suite runs green. What ships is what was specified.

## Scene 08 — Three ways work flows

Three flows: one attended story for judgment-heavy work; an epic batch loop-gated — the workhorse that shipped Atlas and Warden; and the unattended fleet — eight concurrent homes overnight, gates still on, review pass still standing.

## Scene 09 — Graduated autonomy

The differentiator. L1 assistive through L5 self-governance; L3 — conditional autonomy inside context gates — is the production ceiling running here today. The supervisor watches from outside; every incident that built this slide was paid for in production: 25.8M tokens to a blind cap, three attempts to an idle strand, 13 of 31 specs to a teardown.

## Scene 10 — The SDLC, staffed

Every pipeline stage has an owner and a skill set — today, shipped and running, and the frontier. Deployment and operations was the one orphaned stage until Steward joined the crew. Note the Build row: the marshal CLI is now in the today column.

## Scene 11 — The Master Pipeline relay

The stations hand work along a relay: Doctor pre-flight, Herald captures the Dream, Marshal spins the factory, Atlas maps, Warden audits, Mason ships, Doctor watches, Herald proclaims. Supervised start to finish by Marshal — sequencing on verdicts it never authors; each hand-off is a durable, revision-pinned verdict artifact.

## Scene 12 — Shipped — the marshal CLI

This is no longer aspiration. Epic 1 landed 10 of 10 stories on 2026-07-31. Six commands, 785 fast tests plus 8 slow real-git tests. Every finding a coded MRS envelope; frozen exit domain; teardown refuses unmerged work; one harness seam enforced by import-linter. The cadence on the right is a real session.

## Scene 13 — The execution doctrine

Three laws at every autonomy level: skills are the unit of execution; the harness is the unit of governance and deliberately not a skill; station verdicts stay independent. And the 2026-07-31 ruling: Marshal sequences on verdicts it never authors — gating reads durable, revision-pinned verdict artifacts. Marshal never runs the judge.

## Scene 14 — The stack Marshal drives

Eleven bmad-code-org packages, one conda install: the method core, the engine wrapped never absorbed, builder, test architect, creative intelligence — and bmad-manticore, the AI video production pipeline that turns these very artifacts into marketing and demo videos. Plus Skill Forge and the ten detectors.

## Scene 15 — Which verb, when

The selection guide: init for a new line, preflight before every launch, homes to audit isolation, config for policy, dev-auto for judgment-heavy stories, the loop for epics, unattended for the fleet, gate evaluate for CI, deploy and land for the last mile, teardown to retire safely.

## Scene 16 — Escalation & the ledger

When the loop blocks it defers with a durable ledger entry or escalates to a named human — never a silent skip. What it writes: run journals with weighted token spend, ATTENTION files at gates, labeled merge commits, promoted story specs that survive teardown. The line is auditable end to end.

## Scene 17 — Who it serves, where it runs

From a solo developer to an air-gapped enterprise. The solo dev gets a second shift; the team lead a review floor that never rubber-stamps; the manager a board that cannot lie; the CTO autonomy with an audit trail. Enterprise: three pure policy layers with site config materialized at install time, internal tooling mounting through adapter profiles and the declared tool surface, local-first with no hosted control plane.

## Scene 18 — The seed

The operating model installs. Greenfield: a repo initialized Dream-first — tiers, crew charters, BMAD wiring, the deck family from day zero. Brownfield: adopt in place, backfilling Dreams and spec kernels over the existing surface — this repository was the first patient. Formerly Genesis's charge, now folding into Marshal, Epics 10 through 12.

## Scene 19 — Proof & road

Act VI: the receipts, then the map. Atlas 57 of 57; Warden 43 of 43; Marshal's own Epic 1 built by the line it now runs; 128 of 333 fleet-wide. Ahead: gates as objects, the outside supervisor, the landing paper trail, fleet status, portability proven — then the four Dreams: pr-lifecycle, durable-runs, fidelity-enforcement, one-front-door, and the installer fold.

## Scene 20 — Validated code, not vibes

Close on the creed: the Dream deserves better than vibes. Marshal ships exactly what was specified — enforced, contained, validated. Hand off to the Guild: Atlas charts, Warden clears, Mason builds, Doctor judges, Scribe records, Steward runs the estate, Herald tells the world. Write the Dream; the factory does the rest.
