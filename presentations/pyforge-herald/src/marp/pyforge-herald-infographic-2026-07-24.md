---
marp: true
paginate: true
size: 16:9
title: Herald — the voice of the factory, at a glance
style: |
  section { background:#f3f2f2; color:#201e1d; font-family:'Archivo',Arial,Helvetica,sans-serif; font-size:25px; }
  h1 { letter-spacing:-0.02em; color:#201e1d; }
  h2,h3 { letter-spacing:-0.01em; color:#201e1d; }
  strong { color:#c22a10; }
  code { background:#eae9e9; color:#c22a10; padding:0 .3em; }
  section.lead { background:#ec3013; color:#f3f2f2; }
  section.lead h1, section.lead h2, section.lead strong, section.lead code { color:#f3f2f2; }
  section.lead code { background:rgba(255,255,255,.15); }
  section.dark { background:#201e1d; color:#f3f2f2; }
  section.dark h1, section.dark h2, section.dark code { color:#f3f2f2; }
  section.dark strong { color:#ec3013; }
  hr { border:none; border-top:3px solid #201e1d; margin:.4em 0; }
  table { font-size:.76em; border-collapse:collapse; }
  th { background:#201e1d; color:#f3f2f2; text-align:left; }
  th,td { border:1px solid #d3d0cf; padding:6px 10px; }
---

<!-- _class: lead -->

# The voice of the factory
## Herald · The Proclaimer — at a glance

First in: the Dream becomes **The Deck**. Last out: the release becomes **the story**.

---

## The bridge — one surface, both directions

| Step | Direction | What crosses |
| --- | --- | --- |
| **seed** | repo → Design | prototype + runtime, Modernist-bound |
| **design** | in Design | the human refines visually |
| **pull** | Design → repo | etag-checked prototype, entity-safe |
| **extract → build** | in repo | slides → React deck, byte-identical engine |
| **export** | in repo | Marp → standalone HTML + editable PPTX |

Zero manual downloads. Conflicts caught by etags, never overwritten.

---

## The deck family Herald keeps

**1 origin** (agentic-sdlc) + **1 master** (genesis) + **8 Smith chapters** — all in the Modernist / Archivo system, each with a 6-artifact standard: prototype · slides · built deck · Marp · standalone infographic · editable PPTX.

---

## The megaphone loop

**harvest** run telemetry (Atlas maps, Warden verdicts, loop journals) → **illustrate** (infographics, timeline charts) → **compile** (weekly briefs, executive highlights) → **broadcast** (Slack · email · wikis).

---

<!-- _class: dark -->

## The boundary

The multiproject machinery + agent portability belong to **Marshal** — Herald keeps their **communication face**. Stations own verdicts; Herald owns the story.

# Make the factory seen.
